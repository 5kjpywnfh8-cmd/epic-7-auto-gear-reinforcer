# 纯视觉采样强化执行器任务说明

## 目标

在不依赖 MuMu/Fribbels/player_data 的开发环境中，新增一个 fail-closed 的纯视觉采样强化原型：根据用户提供的目标装备参数、截图采样状态和资源上限，规划并验证 `+0 -> +3` 的下一步动作。原型默认 dry-run，不连接模拟器、不发送点击、不强化、不选材、不消耗资源。

## 范围

- 新增独立的纯视觉采样状态模型、截图采样输入协议、目标装备匹配和 `+0 -> +3` 状态机。
- 支持目标：红装、85级、武器、爆伤套、主属性攻击 100；副属性生命 4%、爆伤 4%、暴击 5%、速度 2；起始 `+0`。
- 支持资源上限：粉末 2、下级强化石 1、上级强化石 0、金币 17600；每段与累计上限相同。
- 输出下一步动作、停止原因、资源账本和采样证据摘要；缺字段、页面不明确、目标不唯一、节点未知或资源超限均 fail-closed。
- 允许通过依赖注入替换截图采样器和点击执行器，但本任务不接入真实 ADB/MuMu，也不执行真实点击。

## 禁止修改项

- 不修改正式策略、DP、评分、资源模型、GUI、OCR 置信度门槛、Holdout、自动化规则和多节点新鲜快照契约。
- 不读取、恢复、暂存、上传私人归档或历史 `current` 作为运行时输入。
- 不启动 MuMu、ADB、Fribbels、Airtest，不强化、不选材、不点击页面、不消耗游戏资源。

## 权威输入

- 本文件是本轮实现唯一任务来源。
- 用户本轮目标装备与资源参数以上述“范围”章节为准。
- 现有项目接口和测试风格以 `src/e7_enhance` 与 `tests` 为准。

## 执行步骤

1. 审阅现有 `airtest_adapter`、视觉/OCR 输入和强化策略边界，确定最小独立模块接口。
2. 先写失败测试，覆盖目标匹配、采样状态解析、资源上限、节点连续性、dry-run 默认和 fail-closed 分支。
3. 实现纯视觉采样状态机与可注入适配器，不接入真实设备。
4. 运行定向测试、Python 语法检查和 `git diff --check`。
5. 精确审阅变更，只提交本任务相关文件并推送；真实点击另建任务并重新授权。

## 产物

- 纯视觉采样强化原型模块及公开测试。
- 本任务说明、总计划同步记录和验证结果。

## 停止条件

- 需要真实截图采集、ADB/MuMu、OCR 字段采集、网络读取、GUI 接线或实际点击时立即停止并报告。
- 任一目标字段、节点、资源或页面状态无法由采样证据唯一确认时 fail-closed。

## 当前状态

`dry_run_prototype_implemented_verified_20260726`

- 执行模型：`gpt-5.6-terra + high`。
- 当前仅授权开发原型，不授权真实游戏操作。
- 已新增 `src/e7_enhance/visual_sampling.py` 与 `tests/test_visual_sampling.py`；定向测试 `6/6`、Python 3.9 语法检查和 `git diff --check` 通过。
- 原型只接受结构化视觉采样证据，始终输出 dry-run 建议且不会调用注入的点击执行器；真实截图/OCR/ADB 接线未完成，必须另建任务并重新授权。
