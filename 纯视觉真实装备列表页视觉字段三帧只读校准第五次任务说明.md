# 纯视觉真实装备列表页视觉字段三帧只读校准第五次任务说明

## 目标

在用户于 2026-08-01 明确批准一次后，使用官方 MuMu `adb.exe` 对当前装备列表页执行一次性、只读的三帧内存校准。稳定性按新契约定义为三帧局部识别后的视觉字段签名一致；PNG 字节哈希只作审计元数据。

## 范围

- 只运行一次 `adb.exe devices -l`，接受唯一状态为 `device` 的设备。
- 只从该设备读取三帧 `exec-out screencap -p`，PNG 只保留在进程内存。
- 只使用固定 `1280x720` 注册列表区域、局部 OCR/模板读取器和 `>=0.98` 置信度门槛。
- 仅校验页面、视口、来源、时间单调、视觉字段签名一致、目标唯一和字段完整；输出通过或 fail-closed。

## 禁止操作

- 禁止 `input`、`tap`、`swipe`、`keyevent`、点击、导航、强化、选材、确认、资源消耗、底层数据读取、联网、上传、模型下载。
- 禁止截图落盘、追加采样、自动重试、第二批采样、动态区域搜索和人工字段注入。
- 校准通过也不授权后续点击；进入详情页或强化界面需另建任务并重新授权。

## 输入与权威口径

- 本文档、根目录 `AGENTS.md`、`e7-gear-enhance-plan.md` 和列表页视觉字段稳定性变更任务说明。
- 目标字段：武器、红装/传说、85 级、强化 `+3`、生命值套装、主属性攻击、副属性攻击 `13%`、暴击 `3%`、速度 `2`、效果抗性 `8%`、分数 `37`。
- 固定区域与识别边界：`equipment_list_region_registry()`、`RegisteredListPageLocalRecognizer`、`InMemoryPngListPageObservationSource`；不得用 fixture、历史截图或人工结果替代真实帧。

## 执行步骤

1. 只运行官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l` 并确认唯一 `device`。
2. 从该设备固定读取三帧内存 PNG，校验 PNG 结构、非黑帧和 `1280x720` 视口；不落盘、不重试。
3. 若真实列表页 OCR/模板读取器接线存在，对三帧分别执行固定局部识别，并比较页面锚点、滚动/页面边界、候选身份/指纹/槽位和字段值签名；置信度逐帧保持 `>=0.98`。
4. 若读取器缺失、页面不匹配、视觉字段不一致、目标不唯一或任一门槛失败，立即 fail-closed。
5. 记录设备发现、三帧元数据、哈希变化、识别结果/拒绝原因及所有禁止操作均未执行。

## 产物

- 本次一次性只读校准记录或 fail-closed 记录。
- 本任务说明、总计划和真实校准报告同步更新。

## 停止条件

- 设备非唯一、状态不是 `device`、PNG/视口无效、读取器缺失、字段签名不一致、置信度低于 `0.98` 或任何异常。
- 任何需要输入、导航、截图落盘、联网、重试或额外采样的要求。

## 当前状态

`real_list_readonly_calibration_fail_closed_missing_production_list_reader_20260801`

## 授权记录

- 用户已于 2026-08-01 明确批准重新进行一次真实列表页只读校准；该授权仅覆盖本任务固定三帧读取，不可复用。

## 接口前置审阅结果（2026-08-01）

- 已只读检索列表页生产接线：`equipment_list_region_registry()` 和 `RegisteredListPageLocalRecognizer` 存在，`InMemoryPngListPageObservationSource` 只接受显式注入的 `ListPagePngOcrReader`/`ListPagePngTemplateReader`。
- 仓库内没有真实运行时的列表页 OCR 读取器、模板读取器实例或从 ADB `VisualFrame` 接入该观察源的调用者；现有实现仅在 `tests/test_visual_list_png_source.py` 等测试中使用假读取器。
- 因生产识别器缺失，无法从真实 PNG 产生列表视觉字段签名。按本任务“读取器缺失立即 fail-closed”条件，本次不运行 `adb devices -l`，不读取三帧，不执行 OCR/模板识别。
- 本次用户授权在前置审阅闸门处结束，不得把旧授权或本次批准复用于后续设备读取；后续需先建立并完成独立离线生产读取器接线任务，再新建任务说明并重新取得一次性只读授权。

## 本次停止记录（2026-08-01）

- 已执行：仅读取本任务、`AGENTS.md`、视觉字段稳定性契约和总计划，并检查生产符号引用。
- 未执行：ADB 设备发现、截图、OCR/模板真实推理、截图落盘、重试、输入、点击、导航、强化、选材、确认、资源消耗、联网、上传和模型下载。
- 详细记录见 [第五次真实列表页视觉字段只读校准前置失败报告](reports/visual_list_visual_fields_calibration_fifth_20260801.md)。

## 下一步

建立独立离线的列表页生产 OCR/模板读取器接线任务，明确真实引擎、固定区域输入和 `>=0.98`/fail-closed 回归；完成并由主 agent 审阅后，才可重新申请新的真实三帧只读校准。校准通过也不授权点击或导航。

## 模型记录

用户偏好为 `gpt-5.6-luna + max`；当前协作接口不可用 Luna，本任务实际使用 `gpt-5.6-terra + high`。
