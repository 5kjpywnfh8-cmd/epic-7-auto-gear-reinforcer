# Heroic 中后期 22 速 v5 估计器审计与 v6 修正任务说明

## 目标

修复 v5 差异分层研究器的抽样分配、自然权重和联合账本汇总错误，建立可验证的 v6 离线估计器。仅在估计器通过确定性自检后运行小规模 pilot；本任务不得修改正式强化策略、跳值表、lambda、评分、DP、GUI 或前瞻批次。

## 已确认的 v5 失效证据

1. `stratified_plan_specs()` 声明八个普通 proposal 各占 `0.0625`、高值 proposal 占 `0.5`。
2. `simulate_stratified_shard()` 实际按 `path_index % len(plans)` 轮询，9个 plan 各抽约 `1/9`，与声明混合概率不一致。
3. `_record_stratified_delta()` 的 metrics、资源、GS、速度产量及节点增量只乘 `common_runs / targeted_runs`，没有乘 importance weight。
4. `M1_reachable_22/all_sets` 主表报告 `114.1220 Heroic22+/10万体力`，但 `weighted_rescued_22_per_100k` 仅 `0.196815`，两套口径相差约 `579.84` 倍。
5. 官方模型96个救回事件中，唯一普通 `h111` 事件贡献 `99.104%` 权重，正贡献 ESS 仅 `1.018`。

因此 `reports/heroic_midgame_speed22_v5_stratified_formal_20260713.{md,json}` 只能作为失败证据，禁止作为候选比较结果。v5 resume 不得续跑或迁移到 v6。

## 第一步：先补失败测试

在修改实现前增加最小失败回归测试，至少覆盖：

- 实际每个 proposal 的样本分配比例必须与用于密度计算的混合权重完全一致；不能一边轮询等量抽样，一边按 `0.0625/0.5` 计算权重。
- 对同一条定向轨迹，终局22+增量、正式GS增量、转换可达GS增量、实际策略GS增量、强化经验、金币、出售回收、转换成本和节点计数必须统一乘相同的自然权重。
- 构造一个已知 importance weight 不等于1的救回轨迹，断言 `candidate speed22 - M0 speed22 == weighted_rescued_22_count`，并在每十万体力换算后仍相等。
- 构造一个候选救回但终局未达22的轨迹：必须计入加权强化成本和失败终局变化，但不能计入救回22产量。
- 高值 proposal 与普通 `h111` 重叠时，同一轨迹只能通过正确的 mixture density 或互斥分层质量计一次，不能重复加权。
- schema 升级至 v6；v3/v4/v5 分片全部拒绝读取。

## 第二步：改用互斥自然分层

优先采用可直接审计的分层条件期望，不继续把重叠 proposal 当作互斥 strata。

速度签名至少包含：

- Heroic 初始速度值；
- `+3/+6/+9` 是否命中速度；
- 每次速度命中的官方跳值；
- `+15` 是否命中速度及其跳值；
- 稀有1速端点是否启用。

每个速度签名必须有精确自然概率 `P(stratum)`。在该签名内只对套装、部位、主属性、其余副属性、非速度跳值、`+12` 合法第四词条及策略分类条件抽样。汇总公式固定为：

```text
E[metric] = sum(P(stratum) * mean(metric | stratum))
```

同一个 stratum 内的成功和失败路径都必须保留。候选相对 M0 的所有产量、GS与资源项目使用同一套分层权重。不得只对22+成功事件加权、却对资源和正式价值使用未加权样本。

如果实现者选择保留 multiple importance sampling，而不是互斥分层，则必须：

- 预先生成实际抽样配额；
- 用实际 `n_j / N` 作为 mixture coefficient；
- 对重叠 proposal 使用 balance heuristic；
- 用小型可枚举状态空间逐项证明估计无偏；
- 不得仅通过让 `proposal_probability` 求和为1来代替上述验证。

## 第三步：诊断指标

报告必须同时显示：

- 原始唯一救回22事件数；
- 全部定向样本的 design ESS；
- 22+正贡献 ESS；
- 最大单样本贡献占比和前5样本贡献占比；
- 每个自然 stratum 的质量、样本数、救回数、加权贡献和方差贡献；
- 至少5个 seed 的独立加权估计与成对95%区间；
- 主表22+增量与 `weighted_rescued_22_per_100k` 的一致性校验。

任一候选若最大单样本贡献超过5%，即使原始事件数和ESS达到旧门槛，也不得进入人工确认。

## 第四步：运行顺序

1. 只运行确定性单元测试和极小枚举，证明分层概率和权重守恒。
2. 运行每 seed 小规模 pilot，检查自然质量总和为1、候选增量恒等式、资源账本守恒和最大贡献占比。
3. pilot 通过后，根据每个 stratum 的方差配置后续样本；不得直接沿用 v5 的每 seed `64 + 256`。
4. 正式扩样仍要求每候选原始独立救回22事件 `>=200`、正贡献 ESS `>=100`、至少5 seed非零且成对95%区间可计算；同时新增最大单样本贡献 `<=5%`。
5. 达标后只生成“可进入人工确认”的离线结论，不直接发布正式策略。

## 报告口径

最终报告必须使用人类可读的每 `100,000` 总体力口径，并拆分：

- 维度裂缝体力、次数和循环数；
- 圣女3-7补充体力和次数；
- Heroic 22--27+产量；
- 各正式体系百里分与总百里分；
- 原生75+、合法满值转换可达75+、实际承担转换后的策略价值；
- 相对 M0 增加一件22+所需圣女体力、损失的来源循环和正式GS代价。

22速继续单列，不折算进正式体系百里分。

## 验证与边界

- 先运行新增研究器测试，再运行速度、策略、资源和联合账本相关测试。
- 最后运行 `tools/run_all_tests.py`；既有 `test_gui_app.py` 失败需单独记录，禁止借本任务修改GUI。
- 运行语法检查和 `git diff --check`。
- 不修改或清理用户现有未提交改动。
- 不创建前瞻批次，不修改正式策略，不解盲任何验证集。
