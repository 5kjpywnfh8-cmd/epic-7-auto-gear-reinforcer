# 策略 V1 剩余冻结闸门收口任务说明

## 权威口径

- 本文档是本轮功能开发的唯一任务说明，同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、《强化策略V1收口与GUI延期任务说明.md》和《策略V1冻结清单与覆盖矩阵生成器任务说明.md》。
- 用户于 2026-07-25 明确要求修复 manifest 对 Epic `+0/+3` 内部分组规则可见性，并继续完成剩余两个冻结闸门。
- 当前由主 agent 执行；未创建或派发子 agent。

## 目标

1. 在统一装备结构校验入口拒绝未知套装，使 `advise_gear` 及其 CLI、GUI 支持层、OCR 规范化和路线求解调用方共同 fail closed。
2. 在 V1 manifest 的 JSON/Markdown 中显式展开已发布 Epic 非速度 `+0/+3` 三组门槛、适用范围和联合止损条件，不改变既有正式数值或动作。
3. 新增可重跑、机器可读的完整回归矩阵证据，绑定当前策略版本、行为输入哈希、测试输入哈希、测试结果和要求覆盖项；证据缺失、失败或哈希漂移时闸门必须关闭。
4. 重新生成 V1 manifest，只有五个冻结闸门全部通过时才输出 `ready`。

## 范围

- 修改 `src/e7_enhance/models.py` 的统一结构校验，仅新增未知套装拒绝；若回归发现 GUI 已公开支持但正式别名表遗漏的套装，只允许补齐既有别名/中文名，不新增体系权重或策略门槛。
- 扩展 `src/e7_enhance/policy_manifest.py`、CLI 报告和定向测试。
- 新增只读回归证据生成工具及机器可读证据报告。
- 同步本任务说明、总计划和 V1 manifest 报告。
- 完成后按用户长期要求精确提交并推送当前分支。

## 禁止修改项

- 不修改 Epic `+0/+3` 已发布三组阈值、四项联合条件、候选选择、动作或 `next_check_at`。
- 不修改其他正式策略、DP、评分、资源模型、转换规则、GUI、OCR 门槛、Holdout 或自动化。
- 不操作模拟器，不选择材料，不消耗游戏资源。
- 不读取、暂存、提交或上传私人证据；若全量测试需要已归档私人夹具，只能沿用既有精确临时恢复授权和校验边界，测试后必须删除临时副本并复核归档哈希。
- 不修复或删除 `.git` 内既有异常引用 `refs/heads/main - 副本`。

## 输入与权威口径

- 套装规范与正式映射：`src/e7_enhance/rules.py` 中 `SET_ALIASES`、`SET_CODE_TO_NAME`。
- 统一结构校验：`src/e7_enhance/models.py::validate_gear_structure`。
- 策略入口：`src/e7_enhance/enhance_policy.py::advise_gear`。
- Epic 早期止损正式规则：`src/e7_enhance/epic_early_stop.py`；门槛和类别映射必须从代码常量读取，不在 manifest 中复制另一份独立数值。
- V1 manifest：`src/e7_enhance/policy_manifest.py`。
- 全量测试入口：`tools/run_all_tests.py`。

## 执行步骤

1. 补失败测试，固定未知套装拒绝、Epic 三组规则展示、证据哈希校验和五闸门 `ready` 契约。
2. 在统一结构校验中加入未知套装拒绝，并验证所有共享调用方继续使用同一校验入口。
3. 从正式早期止损常量生成 manifest 规则详情和 Markdown 表格，保持现有动作不变。
4. 实现回归证据工具，运行固定测试矩阵并记录覆盖项、真实退出码、策略版本及输入哈希；失败证据不得打开闸门。
5. 生成证据和 V1 JSON/Markdown，运行定向测试、全量测试、语法检查与 `git diff --check`。
6. 同步任务说明和总计划，精确审阅、提交并推送。

## 产物

- `src/e7_enhance/models.py`
- `src/e7_enhance/rules.py`
- `src/e7_enhance/policy_manifest.py`
- `tools/capture_policy_v1_regression_evidence.py`
- `tests/test_enhance_policy.py`
- `tests/test_policy_manifest.py`
- `reports/policy_v1_regression_evidence_20260725.json`
- `reports/policy_v1_manifest_20260725.json`
- `reports/policy_v1_manifest_20260725.md`
- `策略V1剩余冻结闸门收口任务说明.md`
- `e7-gear-enhance-plan.md`

## 验证要求

- 未知、空白或未规范化套装必须由策略入口抛出 `ValueError`；所有正式套装保持兼容。
- Epic 非速度 `+0/+3` 报告必须明确展示 `pure_output=8/13`、`pure_tank=10/17`、默认组 `12/17`，并说明四项条件必须同时满足；未知候选分组不得与未知套装混淆。
- 回归证据至少覆盖 `normal_85 Epic/Heroic`、`rift_85 Epic`、`+0/+3/+6/+9/+12/+15`、鞋、速度硬路线、Heroic M1、停止回收和转换边界。
- 证据必须绑定当前策略版本、行为输入和测试输入 SHA-256；任一漂移、测试失败、覆盖缺失或 schema 不符时 manifest 输出 `not_ready`。
- 定向测试、证据生成、CLI smoke、统一全量测试、Python 3.9 语法检查和 `git diff --check` 必须报告真实结果。

## 停止条件

- 若未知套装拒绝会要求重定义正式套装或别名，停止并报告冲突。
- 若三组门槛与正式模块不一致，停止，不在 manifest 内自行修正策略。
- 若回归证据不能从真实测试结果生成、输入哈希不一致或覆盖项不完整，保持 `not_ready`。
- 若私人测试夹具恢复前后任一哈希、路径、数量或 Git 状态不符，立即 fail closed，不提交或推送。

## 当前状态

`completed`

## 首轮验证记录（2026-07-25）

- 固定策略矩阵首轮因两个既有测试文件读取已归档 Holdout/真实样本而退出 `1`；失败证据正确保持 `failed`。矩阵已改为只运行同文件内无私人依赖的精确方法，并以合成 `start/stop/net_stamina` 用例覆盖停止回收。
- 第二轮矩阵 `9` 个文件、`123` 项测试通过，`13/13` 覆盖要求通过；manifest 定向测试 `10/10` 通过并达到五闸门 `ready`。
- 首次统一全量入口在外层 `600` 秒达到退出码 `124`，终止前 `test_gui_support.py` 出现 `1` 失败、`1` 错误：项目 GUI 已把 `UnitySet` 作为“夹攻”展示，但 `SET_ALIASES/SET_CODE_TO_NAME` 未登记，统一未知套装拒绝后少导入两件。
- 修复边界固定为补齐 `UnitySet -> set_unity -> 夹攻` 的既有公开别名，不把夹攻套写入任何 `SET_GROUPS`、类别权重或新策略。其余未登记套装继续 fail closed。
- 首轮全量测试使用的 `11` 个临时私人夹具已全部删除；归档 `339/339` 个文件、`223321159` 字节逐项 SHA-256 通过，集合 SHA-256 仍为 `1b33e73a3cfdb243bb1cf91bb58d0300ae727c9152a7698463f192fd94f9b76a`。

## 完成记录（2026-07-25）

- `validate_gear_structure` 现在按 `SET_CODE_TO_NAME` 拒绝未知套装；`UnitySet -> set_unity -> 夹攻` 仅补齐既有 GUI 兼容别名，未加入任何体系分组、权重或门槛。
- manifest 从 `epic_early_stop.py` 正式常量展开三组门槛：`pure_output=8/13`、`pure_tank=10/17`、默认组 `12/17`；同时记录四项联合条件、适用范围和未知候选/未知套装边界。
- 新增机器证据工具和 [回归证据 JSON](reports/policy_v1_regression_evidence_20260725.json)。固定矩阵真实通过 `9` 个文件、`123` 项测试、`13/13` 覆盖要求；证据核心 SHA-256 为 `db625509456c59ab53dd1bd7cd217a43eed248760a7923fd7003986cc680393c`，文件 SHA-256 为 `892817868b18cc0e6b73a554c3c1eb68e2d4fd228aa98d8fa5342cd4eb1c9b70`。
- 证据校验绑定当前策略版本、`12` 个行为输入、`9` 个测试输入和执行工具哈希；缺失、非对象 JSON、schema/版本不符、覆盖缺失、测试失败或任一输入哈希漂移均关闭闸门。
- [V1 manifest JSON](reports/policy_v1_manifest_20260725.json) 与 [Markdown](reports/policy_v1_manifest_20260725.md) 已重新生成：`5/5` 闸门通过、缺口 `0`、状态 `ready`，核心 SHA-256 为 `050041e19c7626d20deb73e95e2894ce48a5c0e7cd685a4ae471914d7d97423b`。
- 定向 manifest 测试 `11/11`、策略矩阵 `123/123`、Python 3.9 语法检查和 CLI `--require-ready` 均通过。
- 最终统一全量入口真实退出码 `0`、耗时 `278.7` 秒并输出 `ALL TEST FILES PASSED`。`real_sample_import_selects_its_acceptance_batch` 首次原生退出码 `3221226505`、`425_item_review_save_keeps_qt_heartbeat_and_updates_one_row` 首次退出码 `3221226356`，均由既有 runner 第二次重试通过。
- 最终测试后 `11/11` 个临时私人夹具已删除；归档 `339/339` 个文件、`223321159` 字节逐项哈希通过，集合 SHA-256 和恢复 JSON/CSV 哈希不变，工作区私人测试文件剩余 `0`。
- 功能、测试、证据、报告和文档已由提交 `b1233c3` 推送至 `origin/codex/policy-v1-manifest-20260725`。提交后的 geometric repack 仅因既有异常引用 `refs/heads/main - 副本` 报警，不影响提交对象或远程推送，本任务未修改该引用。
