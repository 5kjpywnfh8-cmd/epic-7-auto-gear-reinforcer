# 纯视觉真实截图与窗口后端接线任务说明

## 目标

在已发布的纯视觉运行时、点击执行器和离线截图适配层之上，提供可注入的真实平台截图/窗口后端接口，使后续能够在用户明确授权下进行只读截图和局部页面状态识别。当前任务不执行真实设备操作。

## 范围

- 审阅 `visual_adapter.py`、`visual_runtime.py`、`visual_click.py` 的现有接口。
- 新增平台后端协议和最小实现边界：窗口定位、视口读取、截图采样、时间戳/来源标记；后端必须可替换、可测试、默认不启动设备。
- 保持 callback/file-backed fake 兼容，并补充离线 fake 后端回归。
- 为后续局部模板/OCR 接入预留明确的只读 `FrameSource` seam；本任务不运行真实 OCR。
- 记录后端异常、窗口消失、视口变化、截图非字节载荷和时间戳异常的 fail-closed 行为。

## 禁止修改与禁止操作

- 不连接或控制 MuMu/ADB，不启动或重启游戏，不采集真实截图，不点击游戏页面。
- 不强化、不选材、不改装备、不消耗资源，不上传游戏数据。
- 不修改 MuMu/Fribbels 读取器、413 分批方案、新鲜快照契约、正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。
- 不读取、恢复、暂存或上传私人归档；不修改或删除 `refs/heads/main - 副本`；不得使用 `git add -A`。

## 权威输入

- 本文件是本轮唯一任务来源。
- 设计与接口依据：`纯视觉采样方案设计任务说明.md`、`纯视觉运行时接线实现任务说明.md`、`纯视觉点击执行器开发任务说明.md`、`纯视觉截图与窗口适配层开发任务说明.md`。
- 代码依据：`src/e7_enhance/visual_adapter.py`、`src/e7_enhance/visual_runtime.py`、`src/e7_enhance/visual_click.py` 及对应公开测试。

## 执行步骤

1. 只读审阅现有适配层和测试，冻结兼容接口。
2. 先补充失败测试，再实现平台后端协议/最小实现和 fake 后端。
3. 让真实后端构造和采样保持惰性；没有显式调用时不得触碰窗口或设备。
4. 运行定向测试、Python 语法检查、`git diff --check`，精确审阅 diff。
5. 将完成项、验证结果、未完成项、风险和下一步同步回本文件与 `e7-gear-enhance-plan.md`，再精确提交并推送。

## 产物

- 平台截图/窗口后端协议及最小可注入实现。
- 对应公开测试和离线 fake 验证。
- 本文件与总计划的状态记录。

## 验证要求

- 既有视觉运行时、点击器和采样测试不回归。
- 新增窗口缺失、截图失败、视口漂移、来源漂移、时间戳异常和惰性构造测试。
- 定向测试退出码为 `0`；Python 语法检查和 `git diff --check` 通过。
- 报告必须明确仅使用 fake/offline 输入，未进行真实截图、OCR、点击或强化。

## 停止条件

- 任何步骤需要真实窗口、MuMu/ADB、OCR、游戏点击或资源操作时立即停止并报告，另建任务后再执行。
- 无法唯一确认帧来源、视口、时间戳或窗口状态时必须 fail-closed，禁止自动重试。

## 当前状态

`implementation_verified_offline_pending_real_runtime_authorization`

- 已完成 `src/e7_enhance/visual_adapter.py` 的 `PlatformWindow`、`PlatformWindowBackend`、`PlatformFrameSource` 和 `LazyPlatformFrameSource`；真实后端构造保持惰性，仍只通过注入接口进入采样。
- 已补充 `tests/test_visual_adapter.py` 的 fake 后端覆盖：惰性构造、窗口元数据、窗口缺失、后端异常、非字节载荷、时间戳异常、视口漂移和来源漂移。
- 主 agent 复核并重跑：`python -m unittest tests.test_visual_adapter tests.test_visual_runtime tests.test_visual_click tests.test_visual_sampling` 为 `27/27` 通过；Python 语法检查通过；本轮目标文件 `git diff --check` 通过。
- 执行模型记录为 `gpt-5.6-terra + high`。
- 仅使用 offline fake 后端；未连接或控制 MuMu/ADB，未采集真实截图，未运行 OCR，未执行窗口输入、游戏点击、强化、选材或资源消耗。
- 未完成项：真实平台后端实例化、真实截图采样、局部模板/OCR、只读页面识别和人工监督单节点验证；这些必须另建任务并取得明确运行授权。
