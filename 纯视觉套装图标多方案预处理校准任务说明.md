# 纯视觉套装图标多方案预处理校准任务说明

## 目标

针对用户本轮明确指定的未标注原图 `C:\Users\orangine\Documents\第七史诗强化装备脚本\manual_acceptance\single_item_confirmation\operation_013_plus3_20260724_112042\before_page.png`，在不改变正式视觉门槛和自动化边界的前提下，离线比较多种套装图标裁切与受控预处理方案，判断本地 Fribbels 套装模板是否能形成唯一且达到 `0.98` 的图标证据。两张 `Downloads` 参考图只作历史对照，不进入本轮正式置信度结论。

## 当前状态

`offline_visual_set_icon_raw_multi_crop_speed_identified_below_threshold_fail_closed_20260727`。

执行模型：`gpt-5.6-terra + high`。本任务只处理用户截图的内存读取和公开/本地模板离线比较，不执行真实 ADB 或游戏操作。

## 权威输入

- 根目录 `AGENTS.md`。
- 根目录 `e7-gear-enhance-plan.md`。
- 既有[纯视觉套装多裁切方案离线对比任务说明](纯视觉套装多裁切方案离线对比任务说明.md)及其报告。
- 用户本轮明确指定的主输入 `C:\Users\orangine\Documents\第七史诗强化装备脚本\manual_acceptance\single_item_confirmation\operation_013_plus3_20260724_112042\before_page.png`，只允许本轮内存读取，不复制、不改写、不上传。
- `C:\Users\orangine\Downloads\before_page.png` 和 `before_page(1).png` 仅作带标注坐标历史对照，不参与本轮通过/拒绝结论。
- 已固定的本地 `assets/visual/set_icons/fribbels/manifest.json` 及模板资产。

## 范围

1. 审阅现有 `ocr_regions.py`、`set_crop_comparison.py`、`visual_set_template_matcher.py` 和公开测试。
2. 为红装详情页套装图标定义有限、显式、可审计的多方案候选：紧裁切、含边框宽裁切、主体内缩裁切，以及现有固定最近邻尺度；每个候选必须记录归一化边界、像素边界、预处理和拒绝原因。
3. 在同一张截图内一次性提取所有候选并比较本地模板；允许比较 RGB/RGBA、固定最近邻尺度和不改变语义的透明/背景处理，禁止动态搜索、阈值映射、分数伪造和自动重试。
4. 候选选择必须保持图标阈值 `0.98`、唯一候选和 fail-closed；多个高分、低分、模板解析失败、区域越界、坏 PNG 或视口不符均拒绝。不得用套装文字、人工绿色标注或其它字段反向补齐图标。
5. 补充合成/公开测试和一份离线报告，报告每种方案的分数、唯一性、是否通过及阻断原因。

## 禁止修改与操作

- 禁止 ADB `devices`、截图、OCR 推理、页面导航、ADB 输入、点击、强化、选材、资源消耗、重启游戏、底层装备读取和外部上传。
- 禁止联网、下载模型或修改 Fribbels/PCAP/TCP/player_data 读取链路。
- 禁止降低或绕过 `0.98`、稳定帧、视口、哈希、唯一性、`mode=visual_only`、`verification=unverified` 和 fail-closed 规则。
- 禁止修改正式策略、DP、评分、资源模型、GUI、默认 OCR 门槛、Holdout 或自动化规则。
- 不得把带绿色人工标注的文字或图形当作正式字段证据；不得把用户截图裁切写入仓库。
- 只暂存本任务文件、必要实现、测试和报告；禁止 `git add -A`，不得触碰异常副本引用。

## 预期产物

- 最小的多方案图标裁切/预处理比较实现，优先复用现有候选和模板匹配器。
- 对应公开测试，覆盖候选边界、固定尺度、低分、多重高分、坏输入和 fail-closed。
- `reports/visual_set_icon_multi_crop_preprocess_20260727.md` 离线结果报告。
- 本任务说明与 `e7-gear-enhance-plan.md` 同步记录完成项、验证结果、未完成项、风险和下一步。

## 验证要求

- 定向新测试、相邻视觉测试、Python 3.9 语法检查、`git diff --check`。
- 主输入 PNG 只在内存中读取；记录视口、输入完整性、候选像素边界、每种方案结果和拒绝原因，不记录私人截图内容。

## 停止条件

任一候选出现越界、视口漂移、输入损坏、模板无法解析、图标分数低于 `0.98`、结果不唯一或与其它通道矛盾时，保持 fail-closed；即使离线候选通过，也不得自动进入真实 ADB 校准或点击接线，必须另建任务说明并取得新的明确授权。

## 当前执行记录

未标注原图已按授权在内存中完成比较：视口 `1280x720`，既有正式尺度下宽、紧、主体内缩三种裁切最高仅 `0.805320`，且最佳模板分别误选 `destruction`/`fervor`/`destruction`。有限固定尺度对照显示 `0.75` 在三种裁切上均唯一、正确选出 `speed @ 0.862984`，但仍低于 `0.98`；该尺度仅为离线候选，没有修改生产常量或正式 matcher。

因此 matcher 仍未返回图标锚点或正式套装字段，结果保持 `mode=visual_only`、`verification=unverified`、`click_performed=false` 与 fail-closed。实现只保留固定主体内缩裁切候选；默认 parser、正式尺度、策略、门槛和自动化边界均未变。报告：[reports/visual_set_icon_multi_crop_preprocess_20260727.md](reports/visual_set_icon_multi_crop_preprocess_20260727.md)。
