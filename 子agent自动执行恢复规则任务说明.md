# 子 agent 自动执行恢复规则任务说明

## 目标

恢复子 agent 的默认自动执行分工，同时持续禁止在最终回复中自动生成面向人类复制的子 agent 续作指令。

## 范围

- 更新 `AGENTS.md`、`README.md` 与 `e7-gear-enhance-plan.md` 的执行规则。
- 将旧“续作指令临时禁用”结论标记为仅对聊天交接文本有效，不再限制内部子 agent 派发。

## 禁止修改项

- 不修改正式策略、DP、资源模型、评分、GUI、OCR、自动化或 Holdout。
- 本任务仅更新规则文档，不启动新的实际子 agent 任务。
- 不恢复最终回复中自动生成 `给子 agent 的续作指令：` 或 `text` 代码块。

## 输入与权威口径

- 用户于 2026-07-26 明确说明：以后自动启用子 agent；此前禁用的仅是给人类使用的自动派发指令。
- 本任务说明、更新后的 `AGENTS.md` 与 `e7-gear-enhance-plan.md` 为本轮唯一权威来源。

## 执行步骤

1. 将项目规则中的内部派发限制改为默认自动启用。
2. 保留聊天中自动发布续作指令的禁用规则和显式索要格式。
3. 同步 README、任务状态与总计划，并标明旧结论的覆盖关系。
4. 执行文本检索和 `git diff --check`。

## 产物

- 更新后的 `AGENTS.md`、`README.md`、`e7-gear-enhance-plan.md`。
- 本任务说明。

## 验证要求

- 内部子 agent 派发被明确允许按既有模型与执行分工自动发生。
- 自动发布面向人类的子 agent 指令仍被明确禁止，除非用户当轮显式索要。
- 文档同步义务和独立任务说明前置要求保持有效。

## 停止条件

- 完成规则同步与文本验证后停止。

## 当前状态

`completed_validated_20260726`：规则已同步，文本检索与 `git diff --check` 已通过。
