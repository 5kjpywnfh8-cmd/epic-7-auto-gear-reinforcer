# 纯视觉装备列表页区域与唯一目标视觉解析器离线开发任务说明

## 目标

补齐装备列表页纯视觉证据链的离线解析接口：在已提供的目标期望指纹约束下，定义列表页锚点、局部区域、候选卡片、字段解析、候选去重、可见边界和唯一目标证据，并将产物接入既有 `NavigationPageEvidence` 的纯内存构造路径。

## 范围

- 仅修改 `src/e7_enhance` 中与装备列表页离线视觉解析直接相关的最小模块，以及对应公开单元测试和本任务/总计划记录。
- 仅使用合成或公开 fixture；fixture 只能证明离线解析契约，不能作为当前 MuMu 页面或真实目标的视觉证据。
- 使用目标期望指纹：武器、红装/传说、85 级、`+3`、生命值套装、攻击主属性、攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`、装备分数 `37`。

## 禁止修改项

- 禁止 ADB、截图、真实 OCR 推理、窗口输入、导航、点击、强化、选材、确认、资源消耗、底层读取、上传和模型下载。
- 禁止修改正式策略、DP、评分、资源模型、GUI、现有 OCR 门槛、Holdout、真实帧源或自动化后端。
- 禁止全屏盲 OCR、硬编码真实屏幕坐标、把用户文字或历史样本写成当前真实页面通过结论，或降低 `0.98` 字段/锚点门槛。

## 输入与权威口径

- 本文件是本轮唯一权威任务来源；同时遵守根目录 `AGENTS.md` 与 `e7-gear-enhance-plan.md`。
- 所有输出固定为 `mode=visual_only`、`verification=unverified`，任一不确定性均 fail-closed。
- 用户已确认当前处于装备列表页、目标无需筛选/滚动/切换页面，且已批准进入下一步；该信息仅定义后续真实校准的期望状态，不构成当前视觉证据或任何真实操作授权。

## 执行步骤

1. 审阅既有 `visual_navigation`、局部 OCR 区域与测试接口，确定不改变既有详情页识别语义的最小扩展边界。
2. 定义列表页页面锚点、局部区域、候选卡片边界、字段观测和置信度的纯内存数据模型与校验。
3. 实现候选去重、可见边界、全部字段 `>=0.98` 的唯一目标解析，以及向 `NavigationPageEvidence` 的受控转换。
4. 用合成/公开 fixture 测试：正常唯一目标、低置信度、字段缺失、候选重复、相似项、越界卡片、滚动/页面边界和锚点缺失；所有异常必须在产生可点击目标前 fail-closed。
5. 运行定向单元测试、Python 3.9 语法检查和 `git diff --check`，记录真实退出码；同步本任务说明、被替代的真实只读校准任务说明和总计划。

## 产物

- 列表页离线区域/候选/唯一目标解析器及公开测试。
- 可审计的 `NavigationPageEvidence` 构造结果，且不含真实坐标、输入后端或设备调用。
- 本任务说明、真实只读校准任务说明和总计划的状态记录。

## 验证要求

- 仅当页面锚点唯一、局部区域有效、候选卡片位于可见边界、字段齐全且逐项 `>=0.98`、去重后唯一时，才可产生 `NavigationPageEvidence`。
- 候选重复、字段冲突、低置信度、锚点不足、不可见或滚动状态不明时，必须 fail-closed，且不得产出点击点。
- 不得调用任何真实设备、截图、OCR 引擎、网络、输入或资源操作。

## 停止条件

- 需要当前真实页面、真实截图、真实 OCR、ADB、窗口输入、导航、点击或任何资源操作时立即停止，并另建任务。
- 实现会改变既有详情页 OCR 语义、门槛、真实帧源或正式策略时立即停止。

## 当前状态

`offline_list_parser_development_completed_20260729`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口未提供 Luna，本轮实际执行模型为 `gpt-5.6-terra + high`。

## 阶段初始化记录（2026-07-29）

- 真实列表页只读校准在接口审阅阶段按缺少列表解析器 fail-closed，未运行 ADB、未截图、未 OCR、未输入或导航。
- 因此本离线开发是重新申请真实只读校准的唯一前置；开发完成和离线回归通过前，不得重新尝试真实校准。

## 完成记录（2026-07-29）

- 新增 `src/e7_enhance/visual_list_parser.py` 与 `tests/test_visual_list_parser.py`。解析器仅接收已产生的局部观测，不截图、不调用 OCR 或输入后端；它要求唯一的列表锚点、唯一的局部区域、已知滚动/页面边界、有效可见边界以及三帧稳定页面元数据。
- 候选卡片必须同时完整位于已验证的 `candidate_list_region` 和 `visible_bounds` 内；候选 ID 或视觉指纹重复、字段重复/冲突/缺失、期望字段低于 `0.98`、相似目标不唯一或局部区域边界无效，均在构造 `NavigationPageEvidence` 前 fail-closed。
- 成功结果只构造 `NavigationPageEvidence`，不携带点击点；其目标字段匹配与既有 `NavigationEvidenceGate` 一致，允许额外可见字段但要求所有期望字段逐项相等。
- 主 agent 审阅期间补充并验证了候选卡片必须在局部列表区域内的约束，以及局部区域缺失边界时返回 `candidate_list_region_not_confirmed` 而非抛出异常。
- 本任务未运行 ADB、截图、真实 OCR 推理、窗口输入、导航、点击、强化、选材、确认、资源消耗、底层读取、上传或模型下载。

## 验证结果

- `C:\\Users\\orangine\\AppData\\Local\\Programs\\Python\\Python39\\python.exe -m unittest discover -s tests -p 'test_visual_list_parser.py' -v`：`8/8` 通过，退出码 `0`。
- `C:\\Users\\orangine\\AppData\\Local\\Programs\\Python\\Python39\\python.exe -m unittest discover -s tests -p 'test_visual*.py' -v`：`88/88` 通过，退出码 `0`。
- Python 3.9 `py_compile`（列表解析器、列表测试、导航模块与导航测试）：退出码 `0`。
- `git diff --check`：退出码 `0`；仅报告既有 `e7-gear-enhance-plan.md` LF/CRLF 警告。

## 剩余风险与下一步

- 合成/公开 fixture 只证明离线解析契约，不证明当前 MuMu 的页面布局、列表锚点、字段识别、目标唯一性或坐标。
- 下一步只能在用户重新明确授权后，另建一次真实列表页只读校准任务：仅一次设备发现和固定三帧内存截图/局部识别，不发送输入、不导航、不点击或消耗资源。
