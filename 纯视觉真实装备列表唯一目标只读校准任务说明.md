# 纯视觉真实装备列表唯一目标只读校准任务说明

## 目标

在用户确认当前 MuMu 停在装备列表页的前提下，执行一次受控的真实只读校准，核对唯一设备、稳定视觉帧、列表页面锚点和用户提供目标指纹的可见视觉证据。结果仅代表本地视觉观测，固定为 `mode=visual_only`、`verification=unverified`，不得表述为服务器确认或操作成功。

## 范围

- 只允许一次官方 MuMu `adb.exe devices -l` 自动发现唯一 `device`。
- 仅由该自动发现序列号派生固定三次 `exec-out screencap -p`；PNG 只在内存中进行结构、视口、稳定性、哈希和受控局部识别，不落盘、不上传、不重试。
- 仅复用已有且已经离线验证的局部视觉接口；若不存在列表页局部识别器或页面/目标证据不完整，立即 fail-closed。
- 记录设备发现、帧视口/哈希、局部识别结果、停止原因与禁止操作遵守情况。

## 禁止修改项

- 禁止 ADB `input`、`tap`、`swipe`、`keyevent`，禁止窗口输入、页面导航、点击、强化、选材、确认、重启游戏或资源消耗。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、外部上传、模型下载、全屏盲 OCR、第二次采样批次或自动重试。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout、真实 ADB 帧源或自动化规则。
- 禁止把用户文字描述、历史截图或离线 fixture 当作当前真实帧通过结论。

## 输入与权威口径

- 本文件是本轮唯一权威任务来源；同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md` 和现有纯视觉接口。
- 目标期望指纹：武器、红装/传说、85级、`+3`、生命值套装、攻击主属性、副属性攻击力 `13%`/暴击率 `3%`/速度 `2`/效果抗性 `8%`、装备分数 `37`。
- 用户已明确批准本次真实只读校准；该批准不覆盖任何输入、导航、点击、强化、选材、确认或资源消耗。

## 执行步骤

1. 只读审阅现有列表识别与稳定帧接口，确认可复用的局部页面/目标证据路径。
2. 运行官方 `adb.exe devices -l` 一次；非唯一或非 `device` 状态立即停止。
3. 由唯一序列号执行固定三次内存 `exec-out screencap -p`，验证 PNG、CRC、同一视口、非黑屏、时间顺序与三帧稳定哈希；任何不通过立即停止。
4. 仅执行既有受控局部识别；列表页锚点、唯一目标或任一期望字段不完整、低于 `0.98`、矛盾或没有可用解析器时 fail-closed。
5. 同步本任务说明和总计划；不执行真实导航或输入。

## 产物

- 一次真实只读校准记录，或明确、可审计的 fail-closed 原因。
- 本任务说明和总计划中的真实验证结果、风险与下一步。

## 验证要求

- 三帧必须来自同一设备、同一视口、同一页面稳定哈希；三帧是固定契约组成部分，不是失败重试。
- 目标必须是唯一候选，且所有用作匹配的当前帧字段均 `>=0.98`；不满足时不得输出点击点或开始导航。
- 设备、帧、目标、页面和停止原因必须可审计；任何真实读取结果均不得写成服务器状态。

## 停止条件

- ADB 设备不唯一/不可用、截图失败、PNG/稳定性/视口/黑屏校验失败、局部识别器缺失、页面或目标证据缺失/低置信度/矛盾时，立即停止。
- 需要任何输入、导航、点击、第二次采样批次、全屏 OCR、落盘、上传、模型下载或真实资源操作时，立即停止并另建任务。

## 当前状态

`real_list_readonly_calibration_not_started_fail_closed_missing_list_recognizer_20260729`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口未提供 Luna，本轮实际执行模型为 `gpt-5.6-terra + high`。

## 接口审阅与停止记录（2026-07-29）

- 已只读审阅 `ocr_paddle.py`、`ocr_backpack_pair.py`、`ocr_backpack_shadow.py`、`visual_adb.py`、`visual_adb_recognition.py` 和视觉导航接口。`ocr_paddle.py` 明确只识别已选中装备的右侧详情面板；`ocr_backpack_pair.py` 是对底层同批装备数据的离线配对，不能作为纯视觉列表定位器。
- 当前不存在装备列表页的页面类型/锚点、局部 OCR 区域、候选卡片边界、候选去重或目标字段解析器；因此无法把真实帧转换为 `NavigationPageEvidence`，也不能使用已实现的唯一目标/页面跃迁契约。
- 按本任务“局部识别器缺失立即停止”条件，本次真实 ADB 校准未开始：未运行 `adb.exe devices -l`，未截图，未执行 OCR 推理、输入、导航、点击、强化、选材、确认、资源消耗、底层读取或上传。
- 当前状态：`real_list_readonly_calibration_not_started_fail_closed_missing_list_recognizer_20260729`。下一步需另建纯离线“装备列表页区域与唯一目标视觉解析器”任务，使用合成/公开 fixture 建立和验证列表页证据链；通过前不得重新申请真实列表页校准。

## 后续状态（2026-07-29）

- 本任务的 `real_list_readonly_calibration_not_started_fail_closed_missing_list_recognizer_20260729` 状态已被《纯视觉装备列表页区域与唯一目标视觉解析器离线开发任务说明.md》的 `offline_list_parser_development_in_progress_20260729` 前置开发状态覆盖。
- 当前仍未运行 ADB、截图、OCR、输入、导航、点击、强化、选材、确认、资源消耗、底层读取或上传；离线解析器完成并通过回归前，不得重新申请本任务的真实只读校准。

## 前置开发完成后的状态（2026-07-29）

- 上述 `offline_list_parser_development_in_progress_20260729` 已被《纯视觉装备列表页区域与唯一目标视觉解析器离线开发任务说明.md》的 `offline_list_parser_development_completed_20260729` 覆盖。离线解析器与视觉回归已通过，但它只处理合成/公开 fixture，不能作为当前真实页面证据。
- 本任务先前的真实只读授权已在接口审阅 fail-closed 后终止，未执行任何 ADB、截图、OCR、输入、导航、点击、强化、选材、确认、资源消耗、底层读取或上传。若需继续，必须新建并取得新的、明确的真实只读校准授权；不得复用或推定旧授权。
- 当前状态：`real_list_readonly_calibration_ready_for_new_authorization_after_offline_parser_20260729`。

## 后续真实只读校准接口审阅（2026-07-29）

- 本任务的上述 `ready_for_new_authorization` 状态已被《纯视觉真实装备列表页三帧只读校准任务说明.md》的接口审阅记录覆盖。用户重新授权后，审阅确认列表解析器尚无从内存 PNG 形成其局部观测的调用方；现有 ADB 识别器仍只支持详情面板。
- 因而新的真实只读校准也在 ADB 前 fail-closed，未运行设备发现、未截图、未 OCR、未输入、未导航、未点击、未强化、未选材、未确认、未消耗资源、未读取底层数据或上传。
- 当前状态：`real_list_readonly_calibration_not_started_fail_closed_missing_memory_png_list_observer_20260729`。下一步为独立的纯离线列表页内存 PNG 局部观测器开发，不得以当前或历史授权直接重试真实校准。
