# 纯视觉真实装备列表页视觉字段三帧只读校准第七次任务说明

## 目标

在用户确认模拟器已调整并授权后，重新尝试一次装备列表页三帧只读视觉字段校准。仅当同一生产 OCR/模板链路能够解析三帧并形成一致的视觉字段签名时，才允许输出校准结论；不执行任何输入或导航。

## 范围

- 先检查生产 OCR/模板读取器是否已显式装配；缺失或不可审计时立即 fail-closed。
- 闸门通过后，才允许运行官方 MuMu `adb.exe devices -l`，接受唯一状态为 `device` 的设备。
- 设备唯一后仅读取固定三帧 `exec-out screencap -p` 内存 PNG，使用固定 `1280x720` 注册区域和生产 OCR/模板读取器。
- 比较页面锚点、滚动/边界、候选身份/视觉指纹和字段规范化签名；PNG 哈希仅作审计，不作为字段稳定性判定。

## 禁止操作

- 禁止 `input`、`tap`、`swipe`、`keyevent`、窗口输入、点击、导航、强化、选材、确认和资源消耗。
- 禁止截图落盘、自动重试、追加采样、动态区域搜索、底层数据读取、联网、上传和模型下载。
- 禁止使用测试 reader、历史 crop、人工字段或用户描述冒充当前页面证据。
- 即使校准通过，也不授权选择装备、进入详情页或进入强化界面；这些动作必须另建任务说明并重新取得逐次授权。

## 输入与权威口径

- 本文件是本轮唯一权威来源，同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md` 和列表页视觉字段稳定性契约。
- 目标期望指纹：武器、红装/传说、85 级、强化 `+3`、生命值套装、主属性攻击力；副属性攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`；装备分数 `37`。
- 用户已说明页面已调整并批准重新尝试；该授权仅覆盖本文件定义的一次只读校准，不包含点击或导航。
- 生产接线入口为 `build_production_list_page_pipeline`；读取器缺失、异常、字段不完整、目标不唯一或任一置信度低于 `0.98` 均 fail-closed。

## 执行步骤

1. 检查显式生产 OCR/模板读取器工厂、列表字段映射和卡片视觉指纹模板；缺失即记录阻断并停止，不连接设备。
2. 闸门通过后执行一次官方 `adb devices -l`，确认唯一 `device`。
3. 仅读取恰好三帧内存 PNG，校验 PNG、非黑帧、固定视口、来源和时间单调。
4. 对三帧运行同一注册局部 OCR/模板链路，比较视觉字段签名、候选槽位、页面锚点和目标唯一性。
5. 任一门槛失败立即 fail-closed，不重试、不追加采样，并记录未执行的禁止操作。

## 产物

- 本次只读校准通过报告或 fail-closed 阻断报告。
- 本任务说明和 `e7-gear-enhance-plan.md` 的执行记录、状态、剩余风险和下一步。

## 验证要求

- 生产 OCR/模板读取器必须来自显式工厂，不能是 fixture 或人工注入。
- 三帧必须满足固定来源、视口、时间单调、字段签名一致、字段完整、目标唯一和所有置信度 `>=0.98`。
- 报告必须明确未发送输入、未点击、未导航、未强化、未选材、未确认、未消耗资源，且未落盘截图、上传或重试。

## 停止条件

- 生产读取器或模板资产缺失、设备不唯一、PNG/视口无效、页面不匹配、字段/候选漂移、目标不唯一、字段缺失/冲突、低置信度或任何异常时立即停止并 fail-closed。

## 当前状态

`real_list_readonly_calibration_fail_closed_missing_production_reader_seventh_20260801`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口不可用 Luna，本任务实际执行模型为 `gpt-5.6-terra + high`。

## 建立记录（2026-08-01）

- 用户表示模拟器页面已调整并批准重新进行。本记录将该授权限定为一次本文件定义的只读校准。
- 第六次校准因生产 OCR/模板读取器工厂缺失 fail-closed；旧授权和旧结论不可复用。
- 在生产闸门通过前不运行 ADB、不读取截图、不执行 OCR/模板推理。

## 执行记录（2026-08-01）

- Python 3.9 生产装配闸门检查发现 `ocr_reader_factory` 和 `template_reader_factory` 未配置，入口返回 `ProductionListPageReaderUnavailable`。
- 按停止条件在 ADB 前 fail-closed；本次未运行 `adb devices -l`、未采集三帧、未执行 OCR/模板识别、未截图落盘、未重试、未输入、未点击、未导航、未强化、未选材、未确认、未消耗资源、未联网或上传。
- 详细结果见[第七次校准报告](reports/visual_list_visual_fields_calibration_seventh_20260801.md)。

## 下一步

本次已在生产闸门处停止。先补齐离线字段映射、卡片模板和生产 reader 工厂；完成并审阅后重新建任务和取得新的只读授权，不能复用本次授权。
