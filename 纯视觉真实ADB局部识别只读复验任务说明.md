# 纯视觉真实 ADB 局部识别只读复验任务说明

## 目标

在 Paddle 依赖导入复验通过后，对当前 MuMu 窗口执行一次真实、只读、内存内的 ADB 局部视觉识别，验证 ADB 帧源、装备详情局部裁剪、模板锚点、PaddleOCR 行解析和 `VisualEvidence` 证据绑定是否能够形成完整闭环。输出只能标记 `mode=visual_only`、`verification=unverified`，不得表述为服务器确认、底层装备状态或强化结果。

## 唯一权威来源

本文件是本阶段唯一权威任务来源。ADB 帧源继承《纯视觉 ADB 截图与受控点击迁移任务说明.md》；局部裁剪、模板和 OCR 协议继承《纯视觉 ADB 局部模板与 PaddleOCR 只读识别任务说明.md》；依赖闸门继承《纯视觉 OCR 运行环境修复与依赖复验任务说明.md》；字段、区域和 `0.98` 置信度门槛以现有 `src/e7_enhance/ocr_regions.py`、`ocr_paddle.py`、`ocr_normalize.py` 和识别适配器为准。

## 范围

1. 在已批准的 ASCII 工作区缓存配置下，使用 Python 3.9.2 只读导入 `paddle`/`paddleocr`；不得安装依赖、联网或下载模型。
2. 运行 MuMu 官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`，只接受自动发现且状态为 `device` 的唯一设备。
3. 仅执行一次由该自动发现设备派生的 `exec-out screencap -p`，PNG 只在内存中完成结构、CRC、视口、黑屏、时间戳、稳定帧、帧哈希和局部区域校验。
4. 使用装备详情页规范区域执行局部模板锚点和 PaddleOCR 行解析，复用现有 `0.98` 字段置信度门槛，并将来源、帧哈希、视口、时间戳、锚点和字段证据绑定到 `VisualEvidence`。
5. 记录依赖审计、设备发现、单次截图、局部识别结果和停止原因；不保存截图或私人视觉数据。

## 明确禁止

- 禁止 ADB `input tap`、`swipe`、`keyevent`，禁止页面导航、点击、强化、选材、重启游戏和资源消耗。
- 禁止使用 Fribbels、PCAP、TCP 载荷、`player_data.json`、底层装备读取器或任何外部上传。
- 禁止将视觉字段写成服务器确认、真实装备底层数据、资源扣除或强化成功；缺失、低置信度、字段矛盾、模板缺失和页面不匹配均 fail-closed。
- 禁止降低或绕过 `0.98` 门槛、稳定帧/视口/区域校验，禁止自动重试、全屏盲识别、硬编码点击坐标和截图落盘。
- 禁止读取、恢复、暂存或上传私人归档；不得触碰或删除 `refs/heads/main - 副本`；不得使用 `git add -A`。
- 不修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。

## 输入与执行步骤

1. 完整阅读本文件、`AGENTS.md`、`e7-gear-enhance-plan.md` 和前置纯视觉任务说明，确认当前分支与工作区范围。
2. 以已批准 ASCII 缓存配置在全新 Python 3.9.2 进程中确认 `paddle` 与 `paddleocr` 导入；导入失败立即停止，不执行 ADB。
3. 执行一次 `adb.exe devices -l`，若不是唯一 `device` 立即停止且不重试。
4. 由自动发现结果派生一次 `exec-out screencap -p`，不传入固定 IP/端口；PNG 在内存中通过 ADB 帧源和稳定帧校验后继续。
5. 对装备详情规范区域做内存裁剪，执行模板锚点和 PaddleOCR 行解析；任一区域越界、锚点缺失、OCR 失败、字段不完整、字段矛盾或字段低于 `0.98` 时立即停止。
6. 仅在完整证据闭环成立时输出 `VisualEvidence`，并明确 `mode=visual_only`、`verification=unverified`；不得触发任何输入或后验点击。
7. 运行定向公开测试、语法检查和 `git diff --check`；精确审阅差异，仅提交本任务文档及必要的本任务代码/测试/报告。

## 产物

- 一次真实只读 ADB 局部识别复验记录（仅元数据和结果，不包含截图内容）。
- 依赖、设备、帧源、局部模板、PaddleOCR 和证据绑定的通过/停止原因。
- 本文件与 `e7-gear-enhance-plan.md` 的完成项、验证结果、未完成项、风险和下一步记录。

## 验证要求

- fake/offline 测试与真实只读复验必须分开报告，真实复验不得写成服务器确认或强化成功。
- 真实识别必须由唯一自动发现设备和单次截图产生；失败时禁止重试。
- 任何未满足的门槛都必须 fail-closed，保留原始错误类型与退出码；不得以猜测代替证据。
- 执行模型记录为 `gpt-5.6-terra + high`；如实际不可用必须如实记录替代模型。

## 停止条件

- 文档未创建/阅读、依赖导入失败、设备不唯一、截图或 PNG 校验失败、视口/稳定帧失败、区域越界、模板缺失、OCR 依赖或模型不可用、低置信度或字段矛盾时停止，不重试。
- 任何需要安装、联网、下载模型、写入工作区外路径、点击、导航、强化或资源操作的步骤超出本任务，必须停止并另行授权。

## 当前状态

`real_adb_readonly_capture_failed_probe_validator_fail_closed_20260726`

## 模型记录

本轮按用户要求使用 `gpt-5.6-terra + high`；实际执行若遇到工具或环境限制，必须在本文件与总计划中如实记录。

## 本轮执行记录

- 依赖闸门通过：全新 Python 3.9.2 进程在批准的 ASCII 工作区缓存中导入 `paddle 2.6.2` 和 `paddleocr 2.10.0`，退出码 `0`；未构造 OCR 引擎，未下载模型。
- 设备发现通过：官方 MuMu `adb.exe devices -l` 返回唯一 `emulator-5554 device`。
- 按“单次截图”约束调用了一次由自动发现设备派生的 `exec-out screencap -p`。截图未落盘；随后内存内容校验回调因探针未定义 `_nonblack` 抛出 `NameError`，适配链路以 `FrameCaptureError` fail-closed 停止。
- 截图校验回调失败发生在本地探针代码，不足以判定设备或 PNG 损坏；由于本任务禁止自动重试，本轮不再调用 ADB 截图，不执行局部 OCR、模板锚点或证据生成。
- 未执行 ADB 输入、页面导航、点击、强化、选材、重启、资源消耗、底层读取、外部上传或截图持久化。

## 未完成项与风险

- 本轮未获得可用的真实帧/局部裁剪/模板/OCR/`VisualEvidence` 证据，不能声称页面或目标装备识别成功。
- 下一次尝试前必须另行更新任务说明，修复只读探针内容校验 wiring，并重新取得用户对新一次单次截图的明确授权；不得在本任务中隐式重试。
- 当前阻断是探针实现错误，不是 Paddle 导入失败；OCR 模型缓存仍未验证可用，禁止在未授权时下载模型或联网。
