# Epic 非速度 +0/+3 安全止损正式发布任务说明

## 前置条件

仅当用户明确确认发布冻结候选 `78567df462a783ba739d9fcb56735ec4ecaf3797b576b83975293045d95a3966` 时执行。用户未确认时，本文件只作为发布准备，不得修改正式策略。

## 已通过证据

- 开发组157件、冻结验证组92件真实 `normal_85 Epic +0`，使用官方概率精确枚举合法 `+3` 分支。
- 冻结验证 `+0/+3` 明确正效用误停均为0，正效用召回均为100%，regret低于当前正式策略，并节省下一节点资源。
- 五个共同 seed 的联合资源池中，Heroic 产出基线及 `+/-20%` 三个场景均稳定优于当前正式策略，候选减当前的95%区间下界全部大于0。
- 统一全量测试通过。

## 冻结正式规则

仅覆盖 `normal_85 Epic`、非鞋、未进入初始速度 `>=2` 硬路线的非速度装备。

在 `+0`，当前正式策略原本准备继续时，仅当以下条件全部成立才改为停止：

- 当前体系有效 GS `<=10`；
- 当前有效词条数 `<=2`；
- 精确终局达标概率 `<=0.002`；
- 合法满值转换改善值 `<=0`。

在 `+3`，当前正式策略原本准备继续时，仅当以下条件全部成立才改为停止：

- 当前体系有效 GS `<=14`；
- 当前有效词条数 `<=2`；
- 精确终局达标概率 `<=0.01`；
- 合法满值转换改善值 `<=0`。

规则必须使用 `<=`，不得擅自改成 `<`、四舍五入门槛或合并 `+0/+3`。

## 保持冻结

- Epic非鞋初始速度 `>=2` 硬路线及 `+3`命中速度路线；
- `+6`以后正式 Epic DP；
- Heroic M1；
- `rift_85`；
- 跳值表、评分、资源、转换规则和 lambda；
- OCR、ADB、Airtest、自动点击和 GUI 主流程。

安全止损只能把当前继续变成停止，不能把当前停止救回，也不能覆盖速度硬路线。

## 实现要求

1. 先增加失败回归，再作最小生产实现。
2. 正式代码不得导入 `tools/`、研究报告或候选 JSON。
3. 优先复用生产侧已有轻量预测特征；若需新增纯谓词，放在 `src/e7_enhance` 中并保持接口只接收已计算特征。
4. 使用冻结候选哈希标记规则版本，debug展示：节点、适用范围、当前有效GS、有效词条数、终局概率、转换改善、每项门槛、是否触发和未触发原因。
5. 前台仍只输出最终动作；“谨慎继续”和“继续”在自动端都属于 enhance，安全止损命中时必须明确输出 stop。
6. 不修改离线候选、开发/冻结分割或官方概率分支结果。

## 回归测试

至少覆盖：

- `+0` 四条件边界及任一条件超限；
- `+3` 四条件边界及任一条件超限；
- `+0/+3` 门槛不会互换；
- 速度硬路线不受覆盖；
- 鞋子、Heroic、rift、`+6+` 不适用；
- 当前正式已经停止时保持停止；
- 冻结验证92件及其官方 `+3` 加权分支与候选动作完全一致；
- debug中文字段完整；
- 正式资源、DP和评分结果未变化。

## 验收

- 定向策略、精确分支、资源、DP和GUI debug测试通过；
- `tools/run_all_tests.py` 全量通过；
- 语法检查与 `git diff --check` 通过；
- 更新联合报告和总计划，记录用户选择、正式实现文件、影响范围和验证结果。

不得创建新的48件批次；未来真实pair只作为发布后的长期实测复核，不阻断本规则发布。
