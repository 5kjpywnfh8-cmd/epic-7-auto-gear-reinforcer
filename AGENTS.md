# 项目 Agent 工作规则

本文件是本仓库所有主 agent、子 agent 和后续任务的强制项目级说明。

## 文档同步

- 每次讨论只要形成或改变了范围、假设、规则、指标、阈值、结论、状态、阻断或下一步，必须在同一轮同步更新 `e7-gear-enhance-plan.md`。即使结论是“维持现状”或“暂不执行”，也要记录原因。
- 仅讨论、确认、维持现状或无后续执行时，计划记录应简短写明日期、结论、原因和下一步（若有）；不得因此创建独立任务说明或 `reports/` 报告。
- 每个可执行任务在下发或开始前，必须创建或更新独立的 `*任务说明.md`。聊天中的指令不能作为唯一任务来源。
- 同一连续任务在范围、权威口径和停止条件不变时复用原任务说明，不按每轮反馈拆分；范围或约束改变时才更新该说明或建立新说明。
- 独立任务说明必须写明：目标、范围、禁止修改项、输入与权威口径、执行步骤、产物、验证要求、停止条件和当前状态。
- 总计划必须链接对应任务说明、报告和原始结果。旧结论被新口径覆盖时，必须明确标记“已被后文覆盖”或“仅作历史对照”，不得静默保留为当前结论。
- 只有产出需要独立复现、包含测试或实验原始结果、形成正式发布结论，或任务说明明确要求时，才创建独立报告；其余完成信息写回任务说明和总计划的简要状态即可。
- 子 agent 返回后，主 agent 必须先把完成项、验证结果、未完成项和下一步写回任务说明与总计划，再下发后续任务。
- 下一任务若尚未写入 Markdown，不得只在聊天中给出并开始执行。子 agent 交接应以任务说明为唯一权威来源。
- 若当前环境无法写入文档，应将任务标记为未完成并说明阻断，不得声称已经完成文档同步。

## 子 agent 交接

- 当前临时禁用自动发布子 agent 续作指令。即使存在明确、可执行的下一任务，主 agent 也不得自动在最终回复中生成 `给子 agent 的续作指令：` 或对应 `text` 代码块；只简要说明下一步并继续维护本地任务文档。
- 只有用户在当前轮明确要求“给出子 agent 指令”“给出续作指令”或同等含义时，才允许生成可复制的子 agent 交接内容。该临时规则由用户明确恢复自动发布后才失效，并优先于本节其他关于交接格式的条款。
- 临时禁用只影响聊天中自动发布交接指令，不取消任务说明与总计划同步；内部子 agent 可按“模型与执行分工”默认自动创建、启动、续跑或派发。自动派发不得自动生成面向人类的交接文本。
- 固定标题后必须紧跟一个标记为 `text` 的独立代码块，所有可执行交接内容完整放在该代码块内，方便一次复制；不得把 Markdown 链接、项目解释或代码块外段落作为指令的一部分。
- `text` 代码块首句统一写成“请完整阅读并执行根目录《明确命名的任务说明.md》。”，随后声明该文档是本轮唯一权威来源，再写必要的执行边界；不得使用只有路径、没有动作的缩略指令。
- 子 agent 指令必须要求完整读取一个明确命名的 `*任务说明.md`，并声明该文档是本轮唯一权威来源。聊天指令不能替代或悄悄扩展任务文档。
- 若讨论改变了任务范围，必须先更新任务说明和总计划，再生成新的子 agent 指令。不得继续发送已经执行、已失效或被后文覆盖的旧指令。
- 一次交接只下发当前最先执行的任务。存在前置依赖时，后续任务只记录在计划中，不得合并执行或越过前置闸门。
- 子 agent 返回后，主 agent 必须先审核产物并同步文档；若仍有下一任务，只记录下一步，除非用户明确要求，否则不生成新的续作指令。
- 当前没有可执行下一任务时，不生成空指令或虚构任务。

## 模型与执行分工

- 简单代码实现、测试补充、研究器改动和文档机械同步默认交给子 agent；主 agent 只负责与用户讨论、确认范围、下发任务、审阅产物和决定是否进入下一阶段。
- 子 agent 启动时必须记录实际使用的模型和 reasoning effort。用户指定的模型若不在当前协作接口可用列表中，必须使用明确可用的最近替代并如实写入任务说明和 `e7-gear-enhance-plan.md`，不得声称使用了不可用模型。
- 当前用户偏好为 `gpt-5.6-luna + max`；若 Luna 不可用，按用户最新指定使用 `gpt-5.6-terra + high`，并在阶段记录中保留该回退事实。
- 同一文件范围不得由多个子 agent 并行修改；切换模型或重派任务前，先停止旧子任务并核对工作区已有改动。
- 子 agent 返回后，主 agent 必须先只读审阅代码、分片、测试和报告，再同步 Markdown 文档；未完成的 smoke、pilot 或正式研究不得写成发布结论。

## 范围与实现

- 默认中文简洁沟通，先给结论和下一步。
- 遵循现有技术栈、目录、命名和已有抽象；不做无关重构，不修改无关文件。
- 正式策略、DP、资源模型、评分、GUI、OCR 和自动化的修改边界，以当前任务说明为准；离线研究不得静默写入正式配置。
- 工作区可能包含用户或其他任务的未提交修改，不得回退、覆盖或清理无关改动。

## MuMu 装备数据抓取

- 用户授权抓取真实装备时，必须先完整阅读根目录《MuMu抓包读取与真实装备导入任务说明.md》，该文档是抓取与转换流程的唯一权威来源。
- 当前唯一有效读取器根目录为 `D:\VScode\Epic-7-tools.v1.2`；Python 入口为 `local_tools\read_mumu.py`，命令包装器为 `scripts\read_mumu.cmd`。旧 `D:\VScode\E7-tools\...\game_read\fribbels_mumu_reader.py` 流程只作历史对照，不得继续作为当前命令来源。
- 执行前必须运行 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`；读取器自动选择状态为 `device` 的 MuMu。不得写死、复用或主动传入任何 `192.168.x.x:5555` 地址，除非任务说明明确授权诊断设备发现故障。
- 标准真实读取命令固定为 `C:\Users\orangine\AppData\Local\Programs\Python\Python39\python.exe -B D:\VScode\Epic-7-tools.v1.2\local_tools\read_mumu.py --player-id mumu_live --seconds 240 --auto-enter-seconds 105`。Epic Seven 包名固定为 `com.zlongame.cn.epicseven`。
- MuMu 必须启用 Root，读取器必须能在模拟器内通过 `su -c tcpdump` 抓包。不得安装 Npcap、修改系统网络设置或修改 FribbelsE7Optimizer 安装目录。
- 用户已授权读取器把本账号抓到的 TCP 载荷发送至 `https://krivpfvxi0.execute-api.us-west-2.amazonaws.com/dev/getItems` 解码；该授权仅覆盖本地真实装备读取，不扩展到其他外部传输。
- 当前可信输出为 `D:\VScode\Epic-7-tools.v1.2\data\players\mumu_live\current\player_data.json`；原始 PCAP 与 Fribbels 响应在 `imports\<时间戳>\`，不可变快照在 `snapshots\<时间戳>\`。禁止直接修改 JSON、把 PCAP 当样本导入，或用旧 `gear_fribbels_*.json` 冒充本次新快照。
- 外层命令超时不等于失败：必须先检查 `current\player_data.json`、对应 `imports` 和 `snapshots` 是否已落盘并校验时间、装备数量、实例 ID 和 `raw` 字段，再决定是否重跑；不得因超时直接重复抓包。
- 失败时必须保留并报告读取器完整输出，区分 ADB 未连接、Root/tcpdump 不可用、抓包为空、未抓到账号同步、Fribbels 解码失败，以及加速器导致 UDP 或其他接口流量的诊断。
- 每次抓取后必须记录输入命令、输出路径、时间、设备自动发现结果、装备数量、实例 ID/`raw` 校验、导入数量与排除原因。当前 Holdout 状态若不是 `collecting_blind`，不得继续写入旧批次。

## 验证与汇报

- 完成后执行与风险相称的定向测试；任务说明要求全量测试时必须报告真实退出码。
- 报告必须区分正式结果、pilot、历史对照和未发布候选，不能把离线排序写成正式策略。
- 最终汇报只列：修改内容、验证结果、剩余风险和下一步，并提供对应 Markdown 文档链接。
