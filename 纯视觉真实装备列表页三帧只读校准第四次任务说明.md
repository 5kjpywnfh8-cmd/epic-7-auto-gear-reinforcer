# 纯视觉真实装备列表页三帧只读校准第四次任务说明

## 目标

在用户于 2026-08-01 明确批准本次重试后，使用官方 MuMu `adb.exe` 自动发现唯一 `device`，读取固定三帧内存 `screencap` PNG，并仅用既有固定局部 OCR/模板识别链路校准当前装备列表页、目标唯一性与稳定帧证据。

本任务只读取模拟器画面，绝不操纵模拟器；此前第三次任务因没有 `device` 条目而 fail-closed，本次授权与任务独立建立。

## 范围

- 仅允许一次官方 MuMu ADB 设备发现和固定三帧内存截图读取。
- 仅允许对三帧执行既有固定 `1280x720` 注册区域的内存局部 OCR/模板识别与稳定性、唯一性、`>=0.98` 校验。
- 结果仅可形成 `mode=visual_only`、`verification=unverified` 的校准证据或 fail-closed 拒绝结果；不产生点击点、屏幕坐标或输入动作。

## 禁止修改项

- 禁止发送或调用 `input`、`tap`、`swipe`、`keyevent`、窗口输入、点击、导航、强化、选材、确认或任何资源操作。
- 禁止截图落盘、上传、全屏 OCR、动态区域搜索、自动重试、第二批采样、底层数据读取、联网和模型下载。
- 禁止修改代码、正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout、自动化规则或既有任务说明。
- 禁止把用户确认的目标指纹、历史截图、离线 fixture 或旧校准结果写成当前真实页面证据。

## 输入与权威口径

- 本文件是本轮唯一权威任务来源，同时遵守根目录 `AGENTS.md` 与 `e7-gear-enhance-plan.md` 的最新记录。
- 所有旧的一次性真实只读授权（含第三次任务）均已终止，仅作历史对照，不得复用；本次用户授权只覆盖本文件定义的一次执行。
- 用户确认的目标指纹为：武器、红装/传说、85 级、`+3`、生命值套装、主属性攻击力、副属性攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`、装备分数 `37`。
- 用户确认 MuMu 位于装备列表页；目标详情页存在但当前未打开；无相似装备；无需筛选、滚动或切页。
- 局部识别仅使用既有 `equipment_list_region_registry()`、`RegisteredListPageLocalRecognizer`、`InMemoryPngListPageLocalObservationSource` 与 `InMemoryPngListPageObservationSource`，并保持 `>=0.98`、唯一性、`mode=visual_only`、`verification=unverified` 和 fail-closed。

## 执行步骤

1. 仅使用官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l` 发现设备，只接受唯一状态为 `device` 的 MuMu；不写死、复用或主动传入 `192.168.x.x:5555` 地址。
2. 若设备不存在或不唯一，立即 fail-closed，不执行任何截图或重试。
3. 设备唯一后，仅从该设备读取固定三帧内存 `exec-out screencap -p`，不写入文件、不追加采样。
4. 对三帧按固定区域进行局部 OCR/模板识别、稳定性和唯一目标校验；任一页面、视口、字段、置信度、边界、候选唯一性或稳定性失败均 fail-closed。
5. 记录设备发现结果、三帧内存校验、局部识别结果、拒绝原因（如有）和明确未执行的禁止操作；同步本任务说明与总计划。

## 产物

- 一份仅含三帧内存校准证据或 fail-closed 拒绝原因的执行记录。
- 本任务说明和总计划中的授权使用情况、验证结果、剩余风险与下一步。

## 验证要求

- 设备发现必须只接受唯一状态为 `device` 的 MuMu，且不得写死、复用或主动传入模拟器地址。
- 三帧必须满足既有稳定帧、固定区域、字段完整、目标唯一和所有置信度 `>=0.98` 的契约；否则不得形成通过结论。
- 执行记录必须明确证明未发送输入、点击、导航、强化、选材、确认或资源操作，且未落盘截图、上传或重试。

## 停止条件

- 设备不唯一、页面不匹配、稳定帧不成立、目标不唯一、字段缺失/冲突、置信度低于 `0.98`、边界未知或任何异常时，立即 fail-closed，不进行后续采样或操作。
- 任何需要输入、点击、导航、强化、选材、确认或资源操作的需求，都必须停止并另建独立任务，重新取得逐次明确授权与新鲜后验条件。

## 当前状态

`real_list_readonly_calibration_fail_closed_unstable_frames_20260801`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口不可用 Luna，本任务实际执行模型按项目回退规则记录为 `gpt-5.6-terra + high`。

## 建立记录（2026-08-01）

- 用户明确询问契约并批准再次进行一次只读校准；本次授权不包含任何输入、点击、导航或资源操作。
- 第三次任务的设备发现已 fail-closed，旧授权不复用；本任务建立后才允许执行本次唯一的设备发现。

## 授权执行记录（2026-08-01）

- 已执行官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`，退出码为 `0`；自动发现唯一设备 `emulator-5554`，状态为 `device`。
- 已对该设备执行固定三次内存 `exec-out screencap -p`。三帧均通过 PNG 结构、非黑帧和固定 `1280x720` 视口校验，但 `StableFrames` 检测到三帧哈希不一致，原始执行以 `UnstableFrameError: frame hashes changed during collection` 退出。
- 按稳定帧契约立即 fail-closed：未执行局部 OCR/模板识别、未落盘截图、未重试、未追加采样、未输入、未点击、未导航、未强化、未选材、未确认、未消耗资源、未联网或上传。
- 本次实际执行模型为 `gpt-5.6-terra + high`；用户偏好的 `gpt-5.6-luna + max` 当前不可用。本次一次性授权已使用且不可复用。
