# 早期校准与人工验收操作说明

更新时间：2026-07-11

本说明用于下一阶段的“校准数据与人工验收”。它不直接发布自动继续规则，也不修改策略代码。

## 两类样本，不要混用

| 用途 | 样本来源 | 是否手动录入 | 结果用途 |
| --- | --- | --- | --- |
| 离线精确 DP 校准 | 校准器按合法规则自动生成 | 否 | 为 `+0/+3` 四条完整命中的自动继续规则提供标签与保留集验证。 |
| 真实人工验收 | 玩家实际装备 JSON | 是 | 验证候选体系、转换路径、工具建议与实际资源价值是否合理。 |

三条命中、一条偏离、转换候选在当前规则中固定为“谨慎继续，待 +6 精确复核”，不需要用少量合成数据强行发布自动继续规则。

## 校准分组

一个可发布的早期自动继续分组至少由以下维度组成：

```text
来源 × 品质 × 部位 × 主属性约束 × 体系 × 套装组
× 当前有效副属性数 × 可行有效副属性数
```

当前实现把 `+0/+3` 放在同一分组，并将“剩余强化事件数”作为阈值特征；校准报告必须仍分别列出 `+0` 与 `+3` 的样本和结果，避免一方掩盖另一方。

终局标签的计算口径固定为：

```text
当前合法副属性
+ 剩余强化事件（来源/品质独立跳值分布）
+ 85 级默认重铸
=> 最终 90 级分类有效 GS
=> 是否达到《装备强化与评分规则审阅.md》第 6.4 节
   对应“体系 × 部位”的低档 GS 门槛
```

不得用当前 `GS` 直接比较 90 级门槛，也不得线性放大当前分数。

## 只能三条有效的正式路径

以下列表按当前正式分类、部位禁用规则和第 6.4 节存在终局公式的组合计算：

| 体系 | 部位与主属性条件 | 最多合法有效副属性数 | 原因 |
| --- | --- | ---: | --- |
| 输出 | 衣服 | 3 | 衣服禁止固定攻击和攻击%，输出只剩暴率、暴伤、速度。 |
| 输出（必爆） | 武器 | 3 | 武器主属性为固定攻击，输出必爆有效属性中的固定攻击不能作为副属性。 |
| 输出（必爆） | 项链，暴伤主属性 | 3 | 暴伤主副属性不能重复。 |
| 输出（必爆） | 戒指，攻击%主属性 | 3 | 攻击%主副属性不能重复。 |
| 输出（必爆） | 鞋子，速度或攻击%主属性 | 3 | 主副属性不能重复。 |

补充：输出（必爆）衣服理论上最多仅有两条相关副属性，但第 6.4 节该格为 `-`，没有正式终局公式，因此不能发布自动继续规则，只能人工复核。

紫装在 `+0/+3` 只有三条现有副属性是品质/补词条时点造成的，不等同于上表的“部位永久最多三条有效”。紫装会在 `+12` 补第 4 条，必须按既有紫装规则单独建组。

## 离线精确 DP 校准流程

### 1. 先做安全试跑

校准器会写入 JSON、Markdown 和规则文件。**不要使用默认 `--rules-output`**，默认路径会覆盖正式的 `src/e7_enhance/lightweight_calibration_rules.json`。

建议先把所有产物写到 `reports/early-calibration/`：

```powershell
python -m tools.calibrate_full_category_lightweight `
  --samples-per-group 12 `
  --max-groups 1 `
  --workers 1 `
  --seed 20260711 `
  --output reports/early-calibration/pilot.json `
  --markdown-output reports/early-calibration/pilot.md `
  --rules-output reports/early-calibration/pilot-rules.json
```

`12` 个每模板样本会按当前固定切分产生 `8` 个训练样本与 `4` 个保留样本。只有训练样本中至少有 `8` 个精确 DP `continue` 标签时，校准器才可能生成该组自动继续规则；若其中有 DP `stop`，应增加样本数，而不是降低门槛。

### 2. 分阶段扩大覆盖

1. 首先覆盖高优先级体系：输出、输出（必爆）、抗坦、纯肉、命坦。
2. 输出衣服和输出（必爆）的三条合法路径单独查看，不与四条路径混合。
3. 再覆盖低优先级体系：双效、半肉（血防/通用/白字）。
4. 每轮只扩大有限模板；记录随机种子、样本数、进程数、耗时和具体分组。
5. 全覆盖命令使用 `--max-groups 0`，耗时会显著增加，只在定向结果稳定后运行。

建议的下一轮不是直接发布规则，而是增加样本并输出暂存报告：

```powershell
python -m tools.calibrate_full_category_lightweight `
  --samples-per-group 24 `
  --max-groups 1 `
  --workers 4 `
  --seed 20260712 `
  --output reports/early-calibration/group-01.json `
  --markdown-output reports/early-calibration/group-01.md `
  --rules-output reports/early-calibration/group-01-rules.json
```

`24` 只是推荐的第二轮样本量，不是自动发布条件。发布前必须看精确 DP 标签分布和保留集误继续；如果训练继续标签仍不足 8 条，该组继续保守复核。

### 3. 发布审核门槛

一条暂存规则写入正式 `lightweight_calibration_rules.json` 前，主 agent 必须确认：

1. 训练集中至少 8 条精确 DP `continue` 标签；
2. 存在独立保留集；
3. 保留集没有“轻量继续、精确 DP 停止”的误继续；
4. 手工硬回归（07、14、速度主属性鞋、三条合法有效路径）不被误覆盖；
5. normal/rift、品质和部位没有被合并；
6. 报告完整记录种子、样本分布、训练/保留切分和拒绝规则原因。

当前校准器只要求保留集非空。保留集数量是否应进一步提高为固定最小值，需要在发布第一条正式规则前由主 agent 与用户确认；在此之前不发布即可避免风险。

## 真实人工验收流程

真实样本可从已有兼容装备 JSON 导入，不能由校准器替代。当前 GUI 已支持本次实际验证的 Fribbels `items` 导出，并可通过独立“真实样本管理”窗口保存人工结论；其他第三方格式仍不做猜测解析。

1. 不改动现有回归样本。当前 Fribbels 导出可直接由主窗口筛选，也可使用已生成的 `samples/real_acceptance_fribbels_20260610_plus0_plus3.json`；其他不兼容格式应先实现独立只读转换/筛选工具。
2. 按 [manual_acceptance/README.md](manual_acceptance/README.md) 导入 GUI，逐件执行判断并保存建议结果。导入后已自动判断；只有编辑字段后才需要点击“判断”重新计算。
3. 在外部 CSV 模板 `manual_acceptance/manual_test_records.csv` 记录：来源、品质、强化等级、套装、部位、主属性、副属性、工具建议、人工判断、是否一致、分歧原因。
4. 对同一件装备在 `+0 / +3 / +6 / +9 / +12 / +15` 依次补充记录。`+6` 以后要记录精确 DP 是否认可早期“谨慎继续”。
5. 优先收集以下真实情形：
   - 三条命中、一条偏离且可转换；
   - 输出衣服三条合法有效；
   - 输出与坦克候选同时出现；
   - 输出/坦克与双效/半肉同时出现；
   - 普通红、普通紫、异界红；
- 速度硬路线与非速度路线。

人工样本用于检验实际玩家资源价值和排序是否合理；不能代替精确 DP 校准标签，也不能仅凭少量人工意见发布自动继续。

## 输出（必爆）衣服的处理

输出（必爆）衣服在正式第 6.4 节矩阵中没有单独公式，不能因此建立“输出必爆衣服”的自动继续规则，也不能把它直接判为无价值。它应根据实际副属性继续竞争普通输出或半肉体系：

- 满足普通输出的套装、主属性、有效属性和终局公式时，走普通输出候选；
- 满足半肉体系条件时，走对应半肉候选；
- 两者均不满足时保留人工复核。

这是一条候选归属规则，不是为输出（必爆）衣服新增虚构 GS 门槛。

## 当前状态与下一步

- 已完成：14 的候选方向、中文 debug、输出/坦克优先级、三条合法有效路径和回归测试。
- 当前自动继续规则：0 条。
- 当前校准：6 条精确 DP 标签，训练/保留为 `6 / 0`，不足以发布。
- 下一步：执行安全试跑或扩容的暂存校准，审核报告后再决定是否发布任何一条规则；同时开始独立真实样本记录。
