# 纯视觉装备列表页真实生产 OCR 模板引擎实例接线离线开发任务说明

## 目标

补齐装备列表页生产运行时所需的 OCR 与模板读取器实例，使 `build_production_list_page_pipeline` 可以在显式依赖注入下构造真实本地识别链路，并以合成/公开 PNG fixture 完成 fail-closed 回归。

## 范围

- 只读审阅现有 OCR 依赖、模板资产、列表区域注册、PNG 裁切器、观察源和生产装配入口。
- 实现或接线真实本地 `ListPagePngOcrReader` 与 `ListPagePngTemplateReader` 实例；不得在生产入口隐式创建测试假实现。
- 保持固定 `1280x720` 区域、字段白名单、`>=0.98` 门槛、视觉字段签名稳定、唯一目标、`mode=visual_only`、`verification=unverified` 与 fail-closed。
- 使用公开/合成 fixture 验证正常路径、读取异常、字段冲突、低置信度、未知区域和缺失资产。

## 禁止操作

- 禁止 ADB、真实设备截图、真实页面 OCR、输入、点击、导航、强化、选材、确认和资源消耗。
- 禁止联网、模型下载、修改系统 OCR 环境、写入真实页面证据或截图落盘。
- 禁止把测试 fixture、人工字段、历史截图或默认空实现作为生产读取器。
- 禁止修改正式策略、DP、评分、资源模型、GUI、Holdout 或自动化执行边界。

## 输入与权威口径

- 本文件为本轮唯一权威任务来源，同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉真实装备列表页视觉字段三帧只读校准第六次任务说明.md` 及其报告。
- 目标字段：武器、红装/传说、85 级、强化 `+3`、生命值套装、主属性攻击力；副属性攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`；装备分数 `37`。
- 生产装配入口为 `build_production_list_page_pipeline`；真实 OCR/模板工厂缺失时必须继续 fail-closed，不能用测试实现绕过。
- 实际执行模型：`gpt-5.6-terra + high`；用户偏好的 `gpt-5.6-luna + max` 当前协作接口不可用。

## 执行步骤

1. 只读审阅现有 OCR 包、模板资产和接口协议，确认可复用的本地依赖与版本。
2. 实现最小真实本地读取器适配，显式接收区域裁切和读取配置；未知区域、资产、格式或结果必须 fail-closed。
3. 将读取器工厂接入生产装配入口，确保不产生坐标、点击或设备副作用。
4. 增加/补充离线回归，覆盖正常三帧视觉字段一致、PNG 哈希变化、读取异常、低置信度、字段冲突、缺失资产和非法视口。
5. 运行定向列表测试、相关 `test_visual*.py`、Python 3.9 `py_compile` 和 `git diff --check`。
6. 主 agent 只读审阅代码、测试和报告后，更新本任务说明与总计划；完成前不得申请真实三帧校准。

## 产物

- 生产 OCR/模板读取器实例及装配代码。
- 离线回归测试与验证报告。
- 本任务说明和总计划的完成/阻断记录。

## 验证要求

- 生产入口必须能显式构造读取器；缺失或非法依赖必须 fail-closed。
- 正常 fixture 只能证明接口和解析契约，不得写成真实 MuMu 页面证据。
- 全部定向测试、语法检查和 `git diff --check` 必须报告真实退出码。

## 停止条件

- 需要联网、下载模型、真实设备、真实截图或系统环境修改时立即停止并记录阻断。
- 若没有可审计的本地 OCR/模板资产，保持读取器缺失的 fail-closed 状态，不伪造通过结果。

## 当前状态

`offline_production_ocr_template_engine_wiring_blocked_missing_assets_20260801`

## 授权记录（2026-08-01）

- 用户明确批准并授权进入本离线开发步骤；授权不包含 ADB、截图、真实 OCR 推理、输入、点击、导航或资源操作。
- 第六次真实三帧只读校准因缺少生产读取器 fail-closed；其一次性真实设备授权已使用且不可复用。

## 阻断记录（2026-08-01）

- `build_production_list_page_pipeline` 已有显式 OCR/模板工厂边界，缺失或非法依赖继续 fail-closed。
- 本地 PaddleOCR 包和模型缓存可复用通用引擎核心，但现有适配器仅覆盖详情页强化文本；仓库没有列表页卡片字段映射、视觉指纹模板或审定的列表版式配置。
- 套装图标资产不能替代列表卡片模板；本轮未新增伪造实例，未运行测试，未连接 ADB、未截图、未执行真实 OCR 或任何输入。
- 详细审阅见[阻断报告](reports/visual_list_production_engine_instance_wiring_blocked_20260801.md)。

## 下一步

另建[列表页 OCR 字段映射与卡片模板资产准备任务](纯视觉装备列表页OCR字段映射与卡片模板资产准备任务说明.md)，先提供并审定列表卡片字段映射、版式参考和候选卡片视觉指纹模板资产；资产完整前保持生产读取器缺失的 fail-closed 状态。
