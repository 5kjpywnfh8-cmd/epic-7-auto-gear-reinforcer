# 纯视觉运行时接线实现任务说明

## 目标

在现有纯视觉 dry-run 原型基础上，接入离线可测试的纯视觉运行时接口：结构化 `VisualEvidence`、连续稳定帧校验、纯视觉单节点状态机和可注入的采样器/点击器 seam。运行时输出必须明确标记 `mode=visual_only`、`verification=unverified`，不得依赖 MuMu/ADB/Fribbels，也不得把视觉结果转换为新鲜快照或服务端已确认结论。

## 授权与执行模型

- 用户已于 2026-07-26 明确授权“开始进行接线”。
- 本任务代码实现和测试使用 `gpt-5.6-terra + high`；实际模型和 reasoning effort 必须如实记录在总计划。
- 本任务只做离线接口与状态机接线，使用 fake sampler/fake clicker 验证；真实截图采样、ADB/MuMu、网络上传、游戏页面点击、强化、选材和资源消耗另建任务并重新授权。

## 范围

- 新增可校验的 `VisualEvidence` 输入模型，覆盖页面身份、稳定帧、局部锚点、目标字段/指纹、节点和资源显示。
- 新增稳定帧/字段置信度/目标唯一性/节点连续性/视觉资源账本的 fail-closed 校验。
- 新增纯视觉单节点编排 seam：`pre_action -> single_action -> post_action -> next_node`；每个节点最多调用一次注入式点击器，点击后未知结果禁止自动重试。
- 保持现有 `PureVisualSamplingExecutor.evaluate()` 兼容，并保留 dry-run 默认行为。
- 为 `+0 -> +3` 目标和用户给定资源上限补充公开单元测试、错误码和结果字段。

## 禁止修改项

- 不修改 MuMu/Fribbels 读取器、Fribbels 分批策略或新鲜快照契约。
- 不修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或正式自动化规则。
- 不连接或控制 MuMu/ADB，不读取或上传游戏数据，不采集真实截图，不启动真实 OCR，不点击游戏页面，不强化、不选材、不消耗资源。
- 不读取、恢复、暂存或上传私人归档，不修改 `refs/heads/main - 副本`。

## 权威输入

- 本文件是本轮代码实现唯一权威任务来源。
- 设计口径见 [纯视觉采样方案设计任务说明.md](纯视觉采样方案设计任务说明.md) 和 [reports/纯视觉采样方案设计_20260726.md](reports/纯视觉采样方案设计_20260726.md)。
- 现有兼容入口为 `src/e7_enhance/visual_sampling.py`，公开测试为 `tests/test_visual_sampling.py`。

## 执行步骤

1. 只读审阅现有视觉原型、公开测试和任务说明，确定最小新增模块边界。
2. 先补失败测试，覆盖稳定帧、局部锚点、字段置信度、目标唯一性、节点顺序、资源显示矛盾、单次点击和未知结果停止。
3. 实现 `VisualEvidence`/稳定帧校验与纯视觉单节点编排器；保持 dry-run 和旧入口兼容。
4. 使用 fake sampler/fake clicker 运行定向测试、Python 语法检查和 `git diff --check`；不运行任何真实设备流程。
5. 精确审阅 diff，确认未改动读取、策略、资源模型或自动化正式规则；更新本任务和总计划，之后按项目规则精确提交并推送相关文件。

## 产物

- 纯视觉运行时接线模块及公开测试。
- 本任务说明、总计划同步记录和验证结果。

## 停止条件

- 任一实现步骤需要真实截图、ADB/MuMu、网络上传、GUI 接线、页面点击或资源操作时立即停止并报告。
- 任一视觉证据缺失、字段矛盾、稳定帧不足、节点跳跃、资源数字矛盾或点击后未知结果必须 fail-closed，禁止自动重试。
- 若需要修改正式策略、DP、评分、资源模型、新鲜快照契约或正式自动化规则，立即停止并另建任务。

## 当前状态

`implementation_verified_offline_pending_commit_push_20260726`

- 已取得用户代码接线授权，并完成离线运行时接线：`VisualEvidence`、稳定帧/双锚点/字段置信度/唯一目标/资源账本校验，以及单节点 `pre_action -> click once -> post_action` 编排。
- 已更新 `src/e7_enhance/visual_sampling.py` 的兼容结果字段，保留旧 dry-run 行为；新增运行时公开测试并覆盖节点跳跃、资源冲突、旧帧复用、时间戳不递增和点击后禁止重试。
- 验证：运行时测试 `7/7`、旧 dry-run 测试 `6/6`、Python 语法检查通过、`git diff --check` 通过；实际模型为 `gpt-5.6-terra + high`。
- 尚未提交或推送；主 agent 已完成精确审阅，下一步按项目规则只提交本任务相关文件。
- 真实运行时操作仍未授权，本任务只允许离线 fake adapter 验证。
