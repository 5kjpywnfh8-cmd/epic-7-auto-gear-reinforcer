# Epic 非速度均衡候选正式发布任务说明

## 状态

`released_20260720`

用户已于 2026-07-20 明确确认发布 `output_8_13_tank_10_17`。本文件是本轮正式发布的唯一权威任务来源。

## 目标

将已通过独立 128 件 Holdout 八项预注册闸门的 `output_8_13_tank_10_17` 写入正式 `normal_85 Epic` 早期策略，并保持候选验证时的二元动作语义不变。

## 输入与权威口径

- 候选冻结文件：`samples/epic_output_8_13_tank_10_17_holdout_freeze_20260718.json`。
- Holdout 报告：`reports/epic_output_8_13_tank_10_17_holdout_validation_20260719.md`。
- Holdout 原始结果：`reports/epic_output_8_13_tank_10_17_holdout_validation_20260719.json`。
- 冻结候选：默认 `+0/+3=12/17`，`pure_output=8/13`，`pure_tank=10/17`。
- 体系映射继续使用已验证的正式分类：输出、输出(必爆)归入 `pure_output`；抗坦、纯肉、命坦归入 `pure_tank`；半肉归入 `bruiser`；双效归入 `dual`；其余为 `unknown`。

## 正式动作语义

- 适用范围仅为 `normal_85 Epic`、等级 85、非鞋、`+0/+3`、无速度或速度 `<2` 的非速度早期路径。
- 候选只能把当前正式二元 `continue` 增加为 `stop`，不得把当前正式 `stop` 救回为 `continue`。
- 使用同一 selected candidate 的分类、`current_pre_reforge_gs`、当前有效词条数、终局达标概率和满值转换价值。
- 仅在以下条件全部满足时停止：当前有效 GS 小于等于体系节点门槛、当前有效词条数小于等于 2、终局达标概率在 `+0` 小于等于 `0.002` 或在 `+3` 小于等于 `0.01`、满值转换 GS 增益小于等于 0。冻结研究使用的原始转换值与转换 GS 增益在该零/正二元门槛下完全等价；正式实现使用确定性的 GS 增益，避免并列转换目标导致原始值显示漂移。
- `+0` 的下一复核点固定为 `+3`；`+3` 的下一复核点固定为 `+6`。
- Debug 必须显示候选版本、适用范围、体系、节点门槛、五个判定量、当前正式二元动作、最终动作及是否实际增加止损。

## 禁止修改

- Epic 初速 `>=2` 的速度硬路线。
- 鞋子规则、`+6/+9/+12` 正式 Epic DP、DP lambda、资源模型、评分、跳值表和转换规则。
- Heroic 基础策略及已发布 M1 22 速救回。
- OCR、ADB、Airtest、GUI 业务流程和 Holdout 冻结产物。
- Phase A/B/C 历史报告、原始 JSON 和 resume。

## 执行步骤

1. 在 `src/e7_enhance` 建立独立正式早期止损模块，不从 `tools` 导入研究实现。
2. 在 `advise_gear` 的 `+0/+3` 正式动作完成后应用单向止损，并输出结构化 Debug。
3. 更新正式策略版本和 GUI 的版本显示名称，不改变 GUI 业务逻辑。
4. 增加规则边界、体系映射、范围排除、单向动作、`next_check_at`、Epic 后续 DP 与 Heroic M1 不变的回归测试。
5. 用冻结候选逐状态对拍 production 与 Holdout 候选动作；发现任何差异时停止发布，不得放宽规则绕过。
6. 运行定向测试、语法检查、`git diff --check` 和 `tools/run_all_tests.py`。
7. 生成正式发布报告并更新 `e7-gear-enhance-plan.md`；只有全部验证通过后才将状态改为 `released`。

## 产物

- 正式 Epic 早期止损模块及接线。
- 生产规则回归与冻结候选对拍测试。
- `reports/epic_output_8_13_tank_10_17_release_20260720.md`。
- 更新后的策略版本、任务说明和总计划。

## 停止条件

- Holdout 八项闸门、冻结哈希或 64/64 隔离无法复核。
- 生产动作无法与冻结候选语义一致。
- 速度硬路线、鞋子、Epic `+6` 后 DP 或 Heroic M1 出现动作回归。
- 定向测试或全量测试失败且不能归因并修复。

任一停止条件触发时，保持原正式 Epic 策略并将任务标记为阻断，不得宣称已发布。

## 发布阻断修复记录

- 首次全量回归发现转换目标 GS 增益并列时，集合遍历顺序会令原始转换值在 `75.0/6.1` 间变化；4,610 个冻结动作始终无差异。正式门槛改用同义且确定的 `conversion_max_gs_gain <= 0`，不改变转换规则、GS 或动作。
- 正式版本变化会使旧 R50/R400/R800 审计用当前源码哈希误拒绝历史分片。只读审计改为读取各历史正式报告已经保存的旧代码哈希验证分片；不修改、重算或覆盖任何历史分片和报告。

## 完成记录

- 已发布策略版本：`baili-formal-dp-v1-epic-balanced`。
- 4,610 个冻结状态动作逐条对拍一致，且 `PYTHONHASHSEED=0/1/42` 均通过。
- 新策略回归、历史只读聚合回归、相关策略/GUI/Heroic M1 回归和统一全量入口全部通过；全量真实退出码 0。
- 正式报告：[epic_output_8_13_tank_10_17_release_20260720.md](reports/epic_output_8_13_tank_10_17_release_20260720.md)。
- 本任务已完成；下一任务按《强化策略V1收口与GUI延期任务说明.md》进入超高攻击/生命集中属性研究。
