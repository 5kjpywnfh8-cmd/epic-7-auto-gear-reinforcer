# 超高攻击集中属性基础策略研究任务说明

## 状态

`completed_p90_p95_rejected_p975_unresolved_20260721`

当前正式策略 `baili-formal-dp-v1-epic-balanced` 继续作为可用基线。攻击集中研究只产生候选和 debug 结果；在独立验证通过前，不得写入正式策略或改变自动建议。

本轮由子智能体执行代码研究。用户指定 `gpt-5.6-luna + max`，但当前协作接口不提供 Luna，按最新调整实际使用 `gpt-5.6-terra + high`；不得将本轮结果标记为 Luna 执行。

最小 smoke 已通过：1 个 Epic、1 个 Heroic、1 个 seed、`runs=1`，新攻击 schema 分片 `2/2` 完成，复跑 `scheduled=0/reused=2`；三种 Heroic 产出情景下每行裂缝体力加圣女体力均为 `100000`。smoke 的终局价值全为 0，原因是样本极小，只能证明流程和账本，不构成策略结论；该阶段已被后文正式五 seed 收口覆盖。

## 研究范围

- 候选只研究攻击集中，不与生命集中合并，不先研究 OR 候选。
- 研究范围与生命 v4 保持一致：`normal_85 Epic/Heroic`、非鞋、非速度、`+6/+9/+12` 基线停止节点。
- 只有正式分类兼容攻击体系时才允许计算攻击集中：纯输出、必爆输出，以及明确允许攻击词条的半肉；纯坦、双效、一速和 unknown 必须 fail closed。
- 攻击集中 GS 只计算 `atkPct` 与 `atkFlat` 的官方 GS 权重，不计主属性、套装加成或英雄面板值。固定攻击使用现行官方权重，不得把百分比和白字裸值相加。

## 阈值与概率

- 冻结重铸库存攻击集中副属性 GS 分位数作为阈值网格：P90 `23.00`、P95 `26.99`、P97.5 `32.00`。
- 每个 `+6/+9/+12` 状态按当时 selected candidate 的 `CATEGORY_RULES/sourceRow/validGroup` 和有效攻击词条集合计算。
- 使用官方离散跳值的状态级精确 DP，候选条件为终局重铸后攻击集中 GS 达到阈值的概率 `>=10%`；`P>0` 只作为敏感性参考，不得直接发布。
- 同一状态的基线动作、候选动作、资源、终局价值和产量必须成对计算，缓存键必须包含候选、体系、有效词条和代码哈希。

## 执行阶段

1. 先完成小状态精确对拍、+12 补词条、+15、重铸和体系兼容回归。
2. 再做五 seed 联合资源池正式研究，报告每十万总体力的正式百里分、攻击集中新增件数、每件体力、原生/转换75+、22速、资源和裂缝/圣女循环。
3. 只有候选在主场景及 Heroic 产出率 `+/-20%` 下价值区间下界大于零，且无高损失误停，才允许另建独立真实 Holdout；不得直接发布。
4. 若研究未通过，维持当前正式策略，只保留失败原因和可复跑结果。

## 手工与 OCR 采样边界

- 手工强化与软件执行的差异必须分开记录：游戏资源消耗和策略价值按游戏事件计量；软件耗时、截图等待、OCR 重试、人工确认次数另列为执行指标。
- OCR 真实采样只用于检验字段读取、节点识别、建议一致性和执行耗时，不得用少量软件样本改写离线概率、资源成本或正式门槛。
- 任何真实采样都保留原图哈希、OCR 原文、规范化字段、置信度、建议哈希、人工最终动作和结果快照；失败样本不得删除。

## 禁止项

- 不修改正式策略、DP、评分、资源模型、跳值表、现有 Holdout 或 GUI 业务逻辑。
- 不把攻击集中候选直接作为自动救回规则，不在没有独立验证前创建发布任务。
- 不启用无人值守点击；OCR 先限于只读和影子模式。

## 产物

- 本任务说明、攻击集中研究器、定向回归、五 seed 原始 JSON 和人类可读报告。
- 若候选通过离线闸门，再单独建立攻击集中独立验证任务说明和 64/64 manifest。
- 每轮讨论、研究或策略决策必须同步本文件、总计划和 V1 收口说明。

## 2026-07-21 正式结果与收口

- 正式联合资源池已完成：5 个 seed、157 Epic、80 Heroic、每件 `runs=10`，共 `1185/1185` 新攻击 schema 分片。正式 resume 为 `reports/epic_attack_concentration_exact_r10_resume_20260721`；复跑为 `scheduled_shards=0`、`reused_shards=1185`，汇总和闸门 JSON 完全一致。
- `attack_p90_balanced` 相对当前正式策略为 `-0.008644 [-0.016554, -0.000734] / 100体力`；Heroic 产出 `-20%/+20%` 区间也全为负，正式淘汰。
- `attack_p95_balanced` 为 `-0.006555 [-0.012780, -0.000331] / 100体力`；Heroic 产出 `-20%/+20%` 区间也全为负，正式淘汰。
- `attack_p975_balanced` 为 `-0.002397 [-0.007909, 0.003114] / 100体力`；Heroic 产出 `-20%/+20%` 下仍跨零，状态为效应未决，不能建立 Holdout 或发布。
- 主场景每 100,000 总体力的新增攻击集中装备分别为 P90 `54.1552`、P95 `31.9889`、P97.5 `13.3057` 件；这些产量不抵消正式百里分损失或不确定性，不能单独作为采用依据。
- 所有 scenario × seed × candidate 行均满足裂缝体力加圣女体力 `=100000`；候选只执行 `stop -> continue`，候选诱发的新停止数为 `0`。原生/转换 `75+`、22速、循环、金币/材料资源均已写入正式报告和 JSON。
- 验证：攻击/生命 v4/基线审计相关回归 `29` 项通过；语法检查和 `git diff --check` 通过。未创建 Holdout，未修改正式策略、DP、资源模型、评分、跳值、GUI、OCR、自动化或真实游戏状态。
- 正式产物：[报告](reports/epic_attack_concentration_exact_r10_20260721.md)、[原始 JSON](reports/epic_attack_concentration_exact_r10_20260721.json)、[研究器](tools/research_epic_attack_concentration.py)、[测试](tests/test_research_epic_attack_concentration.py)。
- 下一步：攻击集中不再是 V1 的发布候选；需先由主 agent 按总计划决定是否启动独立的策略覆盖与完整口径审计。不得因 P97.5 跨零而收集或消耗真实强化样本。
- 使用建议：当前不启用攻击集中救回，继续使用 `baili-formal-dp-v1-epic-balanced`。P90/P95 是明确负收益，P97.5 仅为效应未决，不能作为实际操作门槛。
