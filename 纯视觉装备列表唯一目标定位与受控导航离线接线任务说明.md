# 纯视觉装备列表唯一目标定位与受控导航离线接线任务说明

## 目标

实现并离线验证纯视觉受控导航的前置契约：在装备列表页唯一定位目标、验证列表到详情与详情到强化页的页面跃迁、从已验证按钮锚点派生归一化点击点，并把两次单次强化的后验与资源账本连续性接线为可测试的 fail-closed 接口。

## 范围

- 仅修改 `src/e7_enhance` 内与视觉导航/运行时直接相关的最小模块和对应公开单元测试。
- 使用目标期望指纹：武器、红装/传说、85级、`+3`、生命值套装、攻击主属性、攻击力 `13%`/暴击率 `3%`/速度 `2`/效果抗性 `8%`、装备分数 `37`。
- 仅使用合成/公开 fixture 进行离线验证；完成后更新本任务说明和总计划。

## 禁止修改项

- 禁止真实 ADB、截图、OCR 推理、窗口输入、页面导航、点击、强化、选材、确认或资源消耗。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout、新鲜快照契约、真实 ADB 帧源或既有离线识别语义。
- 禁止硬编码真实屏幕坐标、将用户文字描述当作视觉证据、降低 `0.98` 门槛或绕过稳定帧/唯一性/后验验证。

## 输入与权威口径

- 本文件是本轮唯一权威任务来源；同时遵守根目录 `AGENTS.md` 和 `e7-gear-enhance-plan.md`。
- 所有视觉结论保持 `mode=visual_only`、`verification=unverified` 与 fail-closed；每个字段需唯一且 `>=0.98`。
- 用户已批准本离线接线阶段；该批准不构成真实设备、截图或输入授权。

## 执行步骤

1. 审阅现有视觉运行时、稳定帧、归一化点击器和公开测试，确认最小扩展边界。
2. 增加列表页唯一目标证据、已验证锚点派生点击点、页面跃迁及后验验证的纯内存接口。
3. 为目标不唯一、字段不足/低置信度、锚点缺失、页面签名不变、后验旧帧、两节点账本断裂及重复点击补充 fail-closed 测试。
4. 运行定向视觉测试、Python 语法检查和 `git diff --check`，记录真实退出码。
5. 同步任务说明和总计划；不申请或执行真实操作。

## 产物

- 最小离线导航契约与公开单元测试。
- 本任务说明、总计划中的结果、风险和下一步记录。

## 验证要求

- 不存在唯一目标、页面/锚点/后验任一不通过或低于 `0.98` 时，必须在调用点击器前 fail-closed。
- 点击点必须由当前已验证按钮锚点和当前视口派生，不能接受裸真实像素坐标。
- 第二次单节点操作必须显式使用第一节点的通过后验账本；后验失败或未知结果不得进入下一节点。

## 停止条件

- 需要真实页面、真实截图、真实 OCR、ADB、窗口输入或任何资源操作时立即停止。
- 实现会改变既有正式策略、OCR 门槛、真实采集或自动化语义时立即停止并另建任务。

## 当前状态

`offline_controlled_navigation_wiring_completed_20260729`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口未提供 Luna，本轮实际执行模型为 `gpt-5.6-terra + high`。

## 完成记录（2026-07-29）

- 新增 `src/e7_enhance/visual_navigation.py`。`NavigationEvidenceGate` 在列表页仅接受唯一且每个期望字段置信度 `>=0.98` 的目标；稳定帧、双局部锚点、页面签名、当前视口和目标可见性任一缺失均 fail-closed。
- 点击点只从当前页唯一通过的按钮锚点归一化边界中心派生，输出 `NormalizedClickTarget`；未加入真实像素坐标、窗口后端、ADB 或任何输入调用。
- 页面跃迁要求同一 `operation_id`、后验时间严格递增、后验页面类型正确、页面签名变化、前后稳定帧哈希不重叠、目标仍可见且视觉指纹唯一连续。
- `VisualNodeChain` 仅在前一节点返回通过的后验与 `expected_after` 账本时允许下一节点；操作 ID、节点顺序或资源前值不一致均在调用底层节点执行器前停止。
- 新增 `tests/test_visual_navigation.py`，覆盖目标唯一性/低置信度、锚点与稳定帧、页面跃迁新鲜性，以及两节点账本连续性。

## 验证结果

- 首次以 `tests.test_*` 形式调用 `unittest` 时因 `tests` 不是 Python 包而未加载测试；该调用不作为验证结果。
- 已改用仓库既有 discovery 方式：新导航 `6/6`、既有运行时 `7/7`、点击器 `4/4` 均通过，退出码 `0`。
- `python -m unittest discover -s tests -p 'test_visual_*.py'`：`75/75` 通过，退出码 `0`。
- Python 3.9 `py_compile` 与 `git diff --check`：退出码 `0`；后者仅报告既有 `e7-gear-enhance-plan.md` LF/CRLF 警告。

## 剩余风险与下一步

- 本产物只定义离线契约，尚未接入列表页真实 OCR/模板、真实按钮锚点、窗口/ADB 输入后端或真实截图采样；不能据此执行真实导航或点击。
- 若要进入真实只读校准，必须另建任务说明并取得明确授权，且只验证当前列表页的稳定帧、唯一目标与锚点，不发送输入。真实受控导航/点击还须在后续独立任务中单独授权。
- 本轮未连接 ADB、截图、OCR 推理、窗口输入、页面导航、点击、强化、选材、确认或资源消耗。
