# 已重铸库存套装权重与百里分体力效率对比任务说明

## 状态

`completed_formal_r50_absolute_cost_unstable_expansion_pending`

本任务使用玩家已重铸库存的套装比例作为“账号保留/需求权重”，重算不同早期策略每获得 1 点正式百里分需要的总体力。装备获取成本直接使用已确认的来源批次成本，不反推副本自然掉率。

前置任务：[Epic非速度PhaseB-C体系映射修复与坦克门槛复核任务说明.md](Epic非速度PhaseB-C体系映射修复与坦克门槛复核任务说明.md) 已完成。当前研究用于在 holdout 前比较修正候选，不预先冻结最终候选。

## 输入冻结

- 源文件：`D:\VScode\E7-tools\gear_data\0橙U子0\json\gear_fribbels_20260610_182000.json`
- SHA-256：`2BB32F384FBE3D926F5274D50AB24DC8766C2AF87AA1ABC0B6946A47EEAE1DB7`
- 文件只读，不在源目录写入任何内容。
- 重铸口径：`level == 90` 且 `enhance == 15`。
- 当前识别：765 件，其中 Epic 616、Heroic 149。
- 套装使用 `items[*].raw.f` 的内部代码，禁止依赖显示名称自行翻译。

主权重为全部 765 件的套装占比。另报告 Epic-only 与 Heroic-only 权重敏感性，但维度裂缝实际品质产出仍使用资源模型的 Epic/Heroic 联合比例，不能用库存品质比例替代掉率。

## 已确认套装计数

| 套装代码 | 数量 |
|---|---:|
| `set_speed` | 235 |
| `set_cri_dmg` | 98 |
| `set_immune` | 63 |
| `set_max_hp` | 63 |
| `set_torrent` | 50 |
| `set_counter` | 42 |
| `set_penetrate` | 34 |
| `set_chase` | 27 |
| `set_acc` | 26 |
| `set_cri` | 25 |
| `set_opener` | 22 |
| `set_shield` | 22 |
| `set_res` | 17 |
| `set_riposte` | 12 |
| `set_rage` | 8 |
| `set_revenant` | 8 |
| `set_def` | 6 |
| `set_vampire` | 6 |
| `set_att` | 1 |

## 权重含义与限制

- 该比例经过玩家刷取选择、强化、出售、保留和重铸，属于后选择分布。
- 主用途是近似“账号实际重视哪些套装”，不是估计维度裂缝每套自然掉率。
- 模拟装备必须重新按合法自然生成规则产生；不得把已重铸装备的副属性分布当成新掉落分布，否则会产生幸存者偏差。
- 维度裂缝单次选择四套且池内等概率。库存权重场景只表示玩家长期轮换选套装的需求比例，不改变单次来源批次成本或品质产出。
- 不生成“每套自然掉率”、不把库存数量乘进装备获取体力，也不输出账号刷取规划。

## 对照场景

使用相同随机路径、资源模型和策略，至少比较：

1. `uniform_observed_sets`：19 个已观察套装等权，作为机制对照。
2. `reforged_inventory_all`：765 件全部重铸库存权重，主场景。
3. `reforged_inventory_epic`：616 件 Epic 权重敏感性。
4. `reforged_inventory_heroic`：149 件 Heroic 权重敏感性。
5. `top4_equal`：库存数量最高四套 `set_speed/set_cri_dmg/set_immune/set_max_hp` 等权，作为单次维度裂缝四套示例。

不得输出虚假的“账号最优刷取方案”，只比较给定权重下的强化策略效率。

## 策略对照

- 当前正式策略；
- 全局 `10/14` 历史对照；
- 全局 `12/17` 均衡对照；
- 修正后的输出 `8/13`、纯坦 `10/17` 保守均衡候选；
- 修正后的输出 `8/13`、纯坦 `12/17` 较高效率候选。

研究完成后由用户确认最终候选；完成前不冻结或开始128件holdout。

## 统一生成与资源口径

- 每个套装独立条件生成合法 `+0` Epic/Heroic 装备，再按场景权重汇总。
- 部位均匀；右三主属性按合法池抽取；副属性遵守部位禁用、主副属性不重复和品质词条数规则。
- 跳值使用 STOVE 官方离散概率，不使用均匀 `roll_range()` 代替。
- 装备获取成本使用固定联合来源批次：每批维度裂缝 `85` 体力，按当前确认的 Epic/Heroic 联合产出、来源金币和下级强化石结算；圣女 3-7 只补资源缺口。
- 套装需求权重只改变每批产出落入各套装后的终局价值和强化流，不改变 `85` 体力来源批次、联合品质比例或圣女瓶颈补充。
- 计入强化材料与金币、出售回收、合法转换成本；来源下级石只抵扣一次。
- 22 速独立报告，不折算为正式百里分。

## 样本要求

- 每个套装、每个品质至少覆盖 5 个独立 seed。
- 条件生成必须让低库存权重套装也获得足够模拟量，再按目标权重回加，不能按库存数量直接少量抽样。
- 正式报告前检查每套正式百里分非零事件、ESS、seed 稳定性和最大单样本贡献；不足则对该套扩样。
- 候选配对差异使用共同随机路径和成对 95% 区间。
- 绝对每点体力成本若 95% 相对半宽大于 10%，标记为 `absolute_cost_unstable`，不得给单一发布常量。

## 核心指标

主指标必须直接计算：

`stamina_per_baili = sum(85体力来源批次 + 圣女补充体力) / sum(正式体系百里分)`

同时报告：

- 每 100,000 总体力正式百里分；
- 每 1 点正式百里分所需总体力；
- 维度裂缝体力、圣女 3-7 体力和来源循环；
- 原生/转换 75+、22 速和输出 60；
- 按套装、品质、体系拆分的分子和分母贡献；
- 相对当前正式策略的配对差值。

## 与旧指标的同口径审计

必须单列比较以下历史值，不能直接混排：

- 发布 Epic DP 常量 `799.2 体力/百里分`；
- 用户记忆的“约 700 体力/百里分”；
- Phase C 错误分组下 `68.8965 分/10万体力`，折合约 `1451.45 体力/分`，仅作失效历史记录。

报告逐项说明是否包含：装备获取、Heroic 联合产出、圣女补充、出售回收、转换成本、22速价值、套装权重和候选分布。只有分子、分母与资源边界完全相同的结果才允许宣称效率提高或下降。

另外以当前代码重新运行旧 `cost_per_baili_score=sum(total_stamina)/sum(score)` 公式，分别在 uniform 与库存需求权重下生成“DP 可比口径”，用于判断 `799.2` 差异来自资源模型、策略、终局价值定义还是套装需求权重；不得把库存权重解释成掉率修正。

## 产物

- 派生套装权重 JSON，包含源路径、SHA-256、过滤条件和计数；
- 可续跑的按套装/品质/seed 原子研究工具；
- 人类可读报告和原始结果 JSON；
- 与旧 `799.2/约700/1451` 的口径对照表；
- 回归测试和更新后的总计划。

## 禁止修改

- 外部 Fribbels JSON；
- 正式策略、DP lambda、资源默认值、评分和跳值表；
- GUI、OCR、ADB、Airtest 和自动点击；
- 既有 Phase A/B/C 报告和 resume。

## 验证

- 测试重铸过滤、原始套装代码计数、权重和为 1、源哈希变化拒绝复用。
- 测试等权与库存权重在相同每套结果下只改变汇总权重。
- 测试每点体力与每10万百里分互为倒数换算。
- 运行定向测试、语法检查、`git diff --check` 和 `tools/run_all_tests.py`，报告真实退出码。

## 完成记录（2026-07-18）

- 已验证指定外部文件SHA-256，派生765件重铸装备的`raw.f`套装权重；仅使用套装代码、品质、等级和强化等级，未读取或输出其他外部私有数据。
- 完成190/190个`set × rank × seed`原子分片，每片50个条件合法非速度胚子，所有强化事件使用本研究器的STOVE离散采样。
- 绝对每点体力成本在五个权重场景均触发`absolute_cost_unstable`，不得据此更新799.2、约700或发布候选。已生成完整报告和JSON，后续只能追加新的样本块扩样。
- R50已完成库存权重下的初步策略比较，但样本不足，不能冻结排序。扩样后必须把四策略全部重新聚合；库存全部权重为主场景，19套等权只作机制对照。
- 主 agent 已补跑语法检查、`git diff --check`和`tools/run_all_tests.py`，真实退出码均为`0`；当前研究器尚无专用测试文件。
- 后续任务：[已重铸库存套装权重绝对成本扩样与旧口径桥接任务说明.md](已重铸库存套装权重绝对成本扩样与旧口径桥接任务说明.md)。
