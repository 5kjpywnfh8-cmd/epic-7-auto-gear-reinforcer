# 纯视觉套装局部裁剪与强化证据锚点修正任务说明

## 目标

在右侧装备详情面板候选区域已由一次真实 ADB 原始帧确认后，离线收窄套装字段的局部裁剪与锚点，并补齐只读强化等级证据的解析契约。保持 `0.98` 字段置信度门槛和 fail-closed，不因用户指纹推断缺失字段。

## 唯一权威来源

本文件是本轮唯一权威任务来源。ADB 帧源、设备发现和禁止输入继承《纯视觉 ADB 截图与受控点击迁移任务说明.md》；右侧详情布局和真实帧审查继承《纯视觉右侧装备详情面板裁剪与 ADB 原始帧审查任务说明.md》；字段解析继承 `src/e7_enhance/ocr_paddle.py`、`src/e7_enhance/ocr_regions.py` 及现有 `0.98` 门槛。

## 范围

- 只读审阅真实帧审查记录和现有套装/强化字段解析。
- 离线调整套装候选区域、套装锚点命名或局部裁剪接口，使真实帧中已观察到的 `命中套装(0/2)` 有明确的局部识别入口。
- 离线补充强化等级证据的区域/文本契约测试；优先接受唯一且通过门槛的明确 `+N`，只有没有 `+N` 时才接受唯一 `exp0/...`，不从目标描述、其它字段或页面上下文推断 `+0`。
- 补充 fake/公开 PNG 和解析器测试，覆盖区域边界、锚点缺失、低置信度、缺失强化和字段矛盾的 fail-closed 行为。
- 不执行真实 ADB、截图、OCR、页面导航或任何输入；若离线验证通过，下一次真实单帧复验必须另建授权记录。

## 禁止修改与禁止操作

- 不修改正式策略、DP、评分、资源模型、GUI、Holdout、自动化规则、帧源或点击执行器。
- 不降低或绕过 `0.98` 字段置信度门槛，不把人工截图或用户描述写成机器确认。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、外部上传和私人归档操作。
- 禁止 ADB `input tap`、`swipe`、`keyevent`，禁止导航、强化、选材、资源消耗和重启游戏。
- 不触碰 `refs/heads/main - 副本`，不使用 `git add -A`；只暂存本任务文件和本任务代码/测试文件。

## 输入与权威口径

- 真实帧已确认右侧详情候选区可读出英雄武器、85、攻击力 `100`、暴击率 `5%`、攻击力 `8%`、效果抗性 `8%`、分数 `28`。
- 真实帧中套装文本 `命中套装(0/2)` 最高置信度为 `0.971498`，强化文本未出现；这两项当前均为未通过证据，不得降阈值或推断。
- 输出继续标记 `mode=visual_only`、`verification=unverified`。

## 执行步骤

1. 完整阅读本文件、`AGENTS.md`、`e7-gear-enhance-plan.md` 及相关前置任务说明，审阅实现和公开测试。
2. 设计最小的套装局部区域/锚点和强化证据表示，保持现有公开常量兼容；不得加入点击坐标或重试规则。
3. 修改实现并补充离线测试，覆盖通过和 fail-closed 分支。
4. 精确审阅 diff，运行定向测试、语法检查和 `git diff --check`；不得连接设备。
5. 将完成项、验证结果、未完成项、风险和下一步写回本文件与总计划。

## 产物

- 套装局部区域/锚点和强化证据的离线实现。
- 公开 fake/解析器测试及验证记录。
- 不产生真实截图、游戏数据、点击或强化结果。

## 停止条件

任一区域、锚点、PNG、字段、置信度、矛盾检查或测试失败，立即 fail-closed；不得自动重试或越过离线验证进入真实读取、点击或强化。

## 当前状态

`offline_set_crop_enhance_evidence_verified_pending_real_frame_calibration_20260726`

执行模型：`gpt-5.6-terra + high`。

## 完成项与验证记录（2026-07-26）

- `ocr_paddle.py` 将套装候选裁剪收窄为 `1280x720` 的 `(819,547)-(1216,619)`，新增强化证据候选 `(819,115)-(1216,180)`；保留旧公开常量兼容别名，不增加点击坐标。
- `ocr_regions.py` 同步套装/强化局部区域并增加 `set_anchor`、`enhance_anchor` 入口；仍使用既有 `0.98` 字段门槛。
- 解析器只接受明确且通过置信度门槛的唯一 `+N`，或没有 `+N` 时唯一的 `exp0/...`；缺失、非零经验条、不同 `+N` 或不同经验条均 fail-closed。同屏经验条仅作兼容上下文，不覆盖明确 `+N`。
- `recognize_backpack_enhance()` 改为只对强化局部区域做 OCR，不再进行全帧经验条扫描；结果附带 `enhance_bounds` 和 `experience_context=enhance_local_crop`。
- 公开测试覆盖套装 `0.971498` 低置信度拒绝、明确 `+N`、`exp0`、缺失/非零/矛盾强化证据、局部 PNG 边界和完整适配链路；定向测试 `67/67`，`py_compile` 和 `git diff --check` 通过。
- 本轮未执行真实 ADB、截图、PaddleOCR、页面导航、ADB 输入、点击、强化、选材、资源消耗、底层读取或外部上传。

## 未完成项、风险与下一步

- 新套装/强化坐标是基于已确认布局的离线候选，尚未由新的真实 ADB 原始帧校准；当前不能声称真实套装或强化识别已通过。
- 下一步需另建任务说明并取得一次单独的只读 ADB 单帧授权，在内存中校准候选区域并运行局部 OCR；若任一锚点、字段或 `0.98` 门槛失败，继续 fail-closed。
- 受控点击、页面导航、强化、选材和资源操作仍未授权，不得由本任务推进。
