# 外部红紫阈值表策略对比任务说明

## 目标

将用户截图中的红装/紫装分节点阈值作为冻结外部策略，在与当前 A/B/C 相同的合法强化轨迹、显式维度裂缝+圣女资源池和终局评价下做离线对比。本任务只研究，不修改正式策略、lambda、评分、跳值表或GUI。

## 冻结阈值

每个节点满足“速度、套装分、输出分”任一路线即通过该节点。

| 品质 | 当前强化 | 速度 | 套装分 | 输出分 |
|---|---:|---:|---:|---:|
| Epic | +0 | 2 | 25 | 19 |
| Epic | +3 | 3 | 31 | 24 |
| Epic | +6 | 5 | 37 | 30 |
| Epic | +9 | 9 | 43 | 37 |
| Heroic | +0 | 2 | 22 | 19 |
| Heroic | +3 | 3 | 28 | 24 |
| Heroic | +6 | 5 | 34 | 30 |
| Heroic | +9 | 10 | 40 | 37 |

外部表的速度阈值按字面执行，仅用于候选对比；不得覆盖当前正式 Epic `+0速度>=2` 和 Heroic `+0速度>=4` 硬路线。

## 已确认术语

- “强化石”指下级强化石，1个提供1,500基础强化经验，使用金币14,400。
- “传家宝”指终局重铸后全部副属性官方总GS `>=75` 的装备，不按体系过滤，不考虑“根源”。
- “根源”指标完全忽略，不进入分子、分母、策略或报告排序。
- 75+传家宝只作为本对比的终局产出指标，不反向修改当前早期策略中“75+不自动救回”的规则。

## 分数映射

用户已确认固定口径，不再运行多映射敏感性：

- `套装分 = total_official_gs`：当前全部副属性的官方总GS，不按体系过滤。
- `输出分 = output_effective_gs`：装备在合法普通输出体系下的有效GS，只统计该输出体系认可的有效副属性。不得改用最佳正式体系，不得将输出（必爆）自动取高，也不得用全部副属性官方GS替代。

实现时必须调用现有结构化评分/体系接口，避免另写一套属性字符串加总。若项目中的“合法普通输出体系有效GS”与用户确认的输出口径无法一一对应，先在报告中列出具体字段差异，不得静默改定义。

## +9后的路径

用户已确认：`+9` 达标后直接强化到 `+15`，中途不在 `+12` 再次审核止损。Heroic在强化经过 `+12` 时仍按真实规则补第4条词条，Epic在 `+12/+15` 仍分别跳既有副属性；这些强化事件必须进入终局模拟。

## 共同研究口径

- 使用与联合资源池研究相同的5个seed、每件5,000条公共轨迹和断点分片；外部策略与A/B/C必须复用相同底层路径。
- normal_85 Epic/Heroic跳值、Heroic +12补词条、Epic五次跳既有词条保持权威规则。
- 显式资源池固定为每批85维度裂缝体力、1 Epic、`85/23.81` Heroic、0.425来源下级石、127,500来源金币，再用一次圣女3-7瓶颈补净金币/经验缺口。
- 金币、经验不得直接相加体力；来源石和来源金币只记一次。
- 终局85级默认重铸；90级不二次重铸。
- “传家宝”主指标使用原生终局总GS>=75；合法满值转换后的75+另列，不能混入原生数量。
- 终局速度>=22、正式体系百里分和输出有效GS>=60分别报告；22速价值映射未完成前只报告数量/概率，不混入本任务的传家宝定义。

### 联合策略与隔离对照

- 主结果 `D_full`：Epic 与 Heroic 均执行外部阈值表，表示截图整套红紫策略的真实联合结果。
- 隔离结果 `D_epic_only`：Epic 执行外部阈值表，Heroic分别固定为现有基础回退/全停，与A/B/C使用相同Heroic背景；该结果用于判断Epic阈值本身的增量贡献。
- A/B/C 的 Heroic 路径不得调用 D 的阈值函数。主结果与隔离结果不得混为同一排序结论。
- 必须复用已完成22速研究的速度价值锚点、研究lambda、gear集合、seed和随机种子派生规则。允许确定性重放相同轨迹并只计算D，不要求把旧聚合分片伪装成包含D。
- D使用独立可续跑目录写入D专用原子分片；A/B/C可读取冻结的22速研究聚合结果，禁止重复覆盖其分片。
- `+9`通过后虽然没有`+12`审核门槛，但路径实际经过`+12`并发生强化事件；节点到达率必须把`+12`记为到达，材料和跳值也必须完整计入。

## 输出指标

按每100个联合来源批次和每100,000总投入体力归一化，至少输出：

- 总金币消耗、维度裂缝金币使用/盈余、圣女补充体力；
- 下级强化石需求、来源0.425石覆盖、剩余石需求；
- 传家宝（原生总GS>=75）数量与每件体力；
- 满值转换后75+数量，单独列示；
- 终局速度>=22数量；
- 输出有效GS>=60数量；
- 正式体系终局价值、全链路价值/100体力；
- 各强化节点到达率、停止率和平均止损成本；
- 相对A/B/C的B-C/外部表-B成对95%区间。

成对区间必须基于相同seed、相同装备和确定性重放的相同随机轨迹；不得用两个候选各自独立置信区间是否重叠代替成对差值区间。

截图底部的绝对结果缺少原始装备数量、总体力或批次数，不能直接复现 `2467w金币/868强化石/31传家宝`。先报告标准化指标；只有取得原表样本规模后才换算绝对值。

## 验证与边界

- 先补阈值边界、OR逻辑、Heroic +12补词条、固定套装分/输出分映射、+9直达+15路径、传家宝定义及资源守恒测试。
- 额外补回归测试，确保A/B/C的Heroic不误用D阈值、完整资源流不遗漏粉末/出售回收/转换、+9直达+15仍统计经过+12、22速价值使用冻结锚点，以及D专用分片可续跑且不覆盖既有分片。
- 不接OCR、ADB、Airtest或自动点击。
- 不把外部表结果写入正式建议。
- 更新计划，运行相关研究/资源/策略测试和全量测试入口。

最终报告必须明确：外部表与B相比节省/损失了什么、传家宝与22速/输出装备产量如何变化，以及该策略是否值得进入后续验证。
