# 纯视觉真实列表页目标卡片整卡模板只读采集任务说明

## 目标

从当前武器列表页取得目标装备卡片的真实整卡视觉参考，生成可审计的 full-card fingerprint template，供后续列表页三帧视觉字段校准使用。

## 范围

- 仅运行官方 MuMu `adb.exe devices -l`，接受唯一状态为 `device` 的设备。
- 仅读取恰好三帧内存 PNG，在固定 `1280x720` 列表区域裁切目标卡片和必要的上下文。
- 比较三帧的卡片版式、字段排列、视觉指纹和目标字段一致性；仅在目标唯一且字段置信度 `>=0.98` 时登记模板。
- 模板 manifest 必须记录来源、视口、边界、SHA-256、阈值和 `reference_only`/生产候选状态。

## 禁止操作

- 禁止 `input`、点击、滚动、导航、进入详情页或强化页、选材、确认和资源消耗。
- 禁止保存全屏截图、使用历史 crop、用户描述或人工绘图冒充当前目标卡片模板。
- 设备或字段门槛失败时不重试、不追加采样、不上传、不联网、不下载模型。

## 目标字段

武器、红装/传说、85 级、强化 `+3`、生命值套装、主属性攻击力；副属性攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`；分数 `37`。

## 当前状态

`target_card_capture_succeeded_stable_card_reference_obtained_blocked_on_icon_semantics_and_region_calibration_20260801`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口不可用 Luna，实际执行模型为 `gpt-5.6-terra + high`。

## 停止条件

设备不唯一、目标卡片不可见、字段不完整/不唯一、视觉指纹跨帧漂移或任一置信度低于 `0.98` 时立即 fail-closed，不生成生产模板。

## 授权要求

执行前需要用户明确批准本文件定义的一次性只读采集；该授权不包含任何点击或导航。

## 执行记录（2026-08-01）

- 用户已批准一次性只读采集。
- 前置检查发现字段映射 manifest 为 `incomplete`、整卡指纹 manifest 为 `missing`，无法满足目标唯一、字段完整和 `>=0.98` 置信度条件。
- 按停止条件在 ADB 前 fail-closed，未运行 `adb devices -l`、未读取三帧、未截图、未执行 OCR、未裁切模板、未输入、未点击、未滚动、未导航、未强化、未选材、未确认、未消耗资源、未重试、未联网或上传。授权不可复用。
- 详细结果见[模板采集报告](reports/visual_list_target_card_template_capture_20260801.md)。

## 追加成功采集与证据复核（2026-08-01）

- 用户授权后重新采集三帧内存 PNG：均 `1280x720`，SHA-256 分别为 `157ee69d…`、`dae6199c…`、`5f1b25ff…`；目标卡片区域（x143-313, y451-568）与右侧详情面板区域（x820-1280, y300-480）三帧像素完全一致（mean_diff=0.00），仅列表头部存在极小差异（0.03，顶栏数字）。
- 右侧详情面板 OCR 高置信确认目标字段真值：`传说武器`、`暗黑钢铁剑`、`攻击力 160`、`攻击力 13%`、`暴击率 3%`、`速度 2`、`效果抗性 8%`、`装备分数 37`、`生命值套装（0/2）`、`85`、`+3`。列表卡片副属性数值（`13%`/`3%`/`2`/`8%`，y=483/504/524/541）与详情面板副属性文字标签顺序完全一致，确认为同一帧内同一件选中装备。
- 已提取目标卡片整卡 crop（170x117，x143-313 y451-568），三帧字节完全一致（sha256 `13531f05…`），保存为 `assets/visual/list_reference/capture_20260801/target_card_full_20260801.png`（PNG sha256 `acf1d02e…`），标记 `reference_only`。
- 证据复核仍有两项阻断，故 `field_mapping_manifest.json` 继续 `incomplete`、`card_fingerprint_manifest.json` 继续 `missing`：
  1. 三个缺失字段（攻击%/暴击率%/效果抗性%）在列表卡片上靠图标识别，但目标卡片图标仅约 9-16px，且与详情面板图标模板匹配分数低（NCC/IoU 约 0.05-0.62，均低于 `0.98`）；详情面板图标与列表卡片图标不是同一套可匹配渲染。字段语义映射无可靠图标证据，继续 fail-closed。
  2. 注册区域 `candidate_card:1`（x102-614, y158-317, 512x160）与当前真实列表布局目标卡片（x143-313, y451-568, 170x117）不匹配；即使登记整卡模板，运行时 SHA-256 也无法命中当前注册区域产生的 crop。整卡指纹需先校准列表区域注册。
- 本轮仅执行官方 MuMu ADB 设备发现与三次 `exec-out screencap -p` 内存读取，未输入、未点击、未滚动、未导航、未强化、未选材、未确认、未消耗资源、未落盘全屏截图、未重试、未联网、未上传或下载模型。整卡 crop 仅作 `reference_only` 审计资产。
