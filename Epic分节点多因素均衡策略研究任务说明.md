# Epic分节点多因素均衡策略研究任务说明

## 目标

修正当前B候选在`normal_85 Epic +0/+3`共用同一有效GS门槛的问题，研究一个可解释、分节点、受误停风险约束的均衡候选。本任务只做离线研究；不得修改正式策略、正式lambda、Heroic正式策略、评分、跳值表、GUI或现有前瞻批次预测。

## 已确认问题

- 当前B规则为有效GS`>=30`继续、`18--30`谨慎继续、`<18`停止，+0与+3完全共用。
- 模拟中的继续和谨慎继续都表示强化到下一节点，因此B的资源动作本质上是有效GS`>=18`继续。
- 有效GS不会随强化下降。训练路径中B在+0仅`19.53125%`通过，但这些路径到+3后的停止率为`0`；即使+3未命中有效体系，也会继续到+6。
- +0到+3的条件增量成本约`2.6221`体力，+3到+6约`11.4101`体力。+3缺少独立止损会放大资源消耗。
- 旧45件研究lambda Oracle中，B误停7件正效用状态，正效用召回约`69.57%`、total regret约`12.4629`；C误停1件、召回约`95.65%`、regret约`0.103`。B的联合批次平均效率第一不代表单件风险已解决。

## 研究原则

1. 分别学习/搜索+0和+3规则，不允许继续使用同一固定门槛作为唯一候选。
2. 不把所有特征直接堆进一个不可解释模型。使用逐级消融，只有对独立数据有稳定增益的因素才保留。
3. 本任务继续把速度硬路线排除在非速度门槛训练之外；速度规则是否正确由独立的《22速优先强化路线重校准任务说明.md》处理。非速度候选不得用自身结果宣称整体速度策略最优。
4. +6/+9/+12仍使用现有正式后续策略，本任务只改变离线候选的+0/+3动作。
5. 继续/谨慎继续在资源评估中都按“强化到下一节点”处理；三分类展示与二元资源动作分别报告。
6. 当前空前瞻批次`epic_b_balanced_prospective_20260713`不得改写。若新候选通过研究，只能将旧空批次标记为被新任务替代并创建新批次ID；不得在同一批次中静默更换哈希。

## 候选阶梯

至少并行冻结以下可解释候选，并复用相同底层轨迹：

1. `B_global_current_gs`：现有+0/+3共用18/30，作为基线。
2. `B_stage_gs`：+0和+3分别搜索有效GS的继续/复核/停止门槛。
3. `B_stage_hit`：在分节点GS上增加+3是否命中当前合法体系有效副属性；不得依赖必须保存前一截图的字段，应优先使用现有rolls/强化命中分析可从当前状态推导的结构化结果。
4. `B_stage_structure`：增加当前有效词条数、可行有效词条数、部位最多三条有效路径和白字右三主属性限制。
5. `B_stage_category`：增加体系优先层（纯输出/纯坦克、半肉、双效），纯输出与纯坦克同层按终局概率/效用排序；不得因匹配多个体系额外加分。
6. `B_stage_probability`：增加终局正式达标概率和下一节点增量资源成本。
7. `B_stage_conversion`：只在合法满值转换确实改变属性、体系、终局GS和utility时加入转换收益，并计100,000金币。

每一级都必须与前一级做成对差值和独立验证。若新增因素没有稳定改善，最终候选不得保留该因素。

## 特征口径

允许使用：

- 强化节点（+0/+3）；
- 合法体系有效GS和全部官方GS，必须分开；
- 套装、部位、合法主属性约束；
- 当前有效/无效副属性；
- 当前有效词条数、可行有效词条数；
- +3命中属性及是否命中目标体系；
- normal_85 Epic独立跳值分布；
- 终局正式达标概率；
- 下一节点增量粉末、下级石、金币和出售回收；
- 合法满值转换增益与成本；
- 终局速度>=22价值。

不得使用玩家库存、活动加成、OCR置信度、人工标签泄漏或未来强化结果作为当前决策特征。

## 优化目标与约束

本轮研究“均衡档”，不得只最大化某一个产量：

- 主目标：显式维度裂缝+圣女联合资源池下的非速度正式终局价值/总体力；完整联合排序必须等待独立速度路线研究完成，再合并速度优先结果，不得继续用旧固定速度路线替新候选背书。
- 风险约束：分别报告Oracle正效用召回不低于85%/90%/95%的Pareto候选，不预先偷偷选定一个召回门槛。
- 尾部约束：误停数量、total regret、P90 regret、max regret和错过终局价值必须报告。
- 产量护栏：原生75+、合法满值转换75+、22速、输出有效GS>=60按每100批和每100,000总体力报告，但不得未经校准直接相加成一个分数。
- 资源指标：总体力、金币、粉末、下级石、各节点到达率/停止率和每个目标产出所需总体力。

外部D保留为高投入对照。当前每100,000总体力中，D_epic_only的原生75+和转换75+高于B，D_full的22速产量也高于B；因此报告必须区分“正式综合价值效率”和“单一目标产量效率”，不得继续用一个结论覆盖所有目标。

## 数据隔离

- 开发/搜索：现有299件Epic开发集合及其确定性公共轨迹。
- 机制覆盖：条件生成样本只用于合法性、单调性和稀疏组合覆盖，不作为真实掉落权重。
- 旧45件Oracle/人工样本：只作已知审计集，禁止反向调门槛后再称独立验证。
- 新前瞻验证：当前批次为0/48，研究期间暂停入批。新候选和发布闸门冻结后创建新的至少48件真实`normal_85 Epic +0/+3`非速度盲测批次。
- rift_85、Heroic和速度硬路线不得混入门槛训练。

## 执行要求

1. 先复现现有B的+0通过率、+3停止率0和两个节点成本，不先改策略。
2. 先增加最小测试，证明同门槛导致+0通过者+3无新增止损，并验证分节点候选能对+3无效命中止损。
3. 复用5 seed、每件5,000轨迹、确定性random_seed、原子分片、断点续跑和多进程；新研究使用独立resume目录。
4. 门槛搜索只能在开发集完成；搜索范围、步长、选择标准和并列规则必须写入原始JSON。
5. 对每个候选做特征消融、公共轨迹成对95%区间、Heroic基础回退/全停、Heroic产出率±20%和22速锚点敏感性。
6. 使用研究lambda`0.0022851391`复核旧45件Oracle，同时报告其非独立性质；不得写入正式配置。
7. 不修改或覆盖现有B、D、ABC终局计数分片和报告原始数据。
8. 运行定向测试、`tools/run_all_tests.py`、语法检查和`git diff --check`。

## 发布闸门

本任务本身不发布策略。只有候选同时满足以下条件，才允许新建前瞻批次：

- 在主资源口径的价值率不劣于B，或以明确可接受的小幅效率损失换取显著更高的Oracle召回/更低regret；
- +3确实产生独立止损，不再机械通过所有+0入选路径；
- 结论在速度锚点、Heroic背景和产出率敏感性下稳定；
- 规则可由当前装备状态计算，GUI/JSON路径无需依赖不存在的历史快照；
- 新候选、比较指标和解盲标准在收集任何新样本前冻结。

最终报告必须说明：最终保留了哪些因素、删除了哪些无增益因素、+0/+3各自规则、相对B/C/D的资源和产量变化、Oracle误停风险，以及是否有资格创建新的48件前瞻批次。
