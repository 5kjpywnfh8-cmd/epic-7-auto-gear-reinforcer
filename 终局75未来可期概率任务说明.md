# 终局 75+ 未来可期概率任务说明

## 目标

补齐早期装备终局概率中的 R61“未来可期”目标。当前候选只计算达到正式“体系 x 部位”最低档 GS 的概率，没有计算终局重铸后全部副属性官方 GS 达到 75+ 的概率。

本任务新增独立的 75+ 后备终局路线、debug/GUI/真实样本字段和测试。不得把所有正式体系门槛统一改成 75，不得修改 OCR、ADB、Airtest 或自动点击。

## 根因

`lightweight_calibration.py` 当前使用：

```text
threshold = min(formula[0] for formula in formulas)
terminal_reach_probability = P(候选体系有效 GS >= 该体系/部位低档门槛)
```

该门槛随分类和部位变化，可能为 59、64、70、72 等；它不是 R61 的 75 分门槛。`score_engine.py` 虽然在终局静态评分中支持“副属性官方分 >=75 -> 未来可期”，但早期终局概率和真实样本候选 debug 没有对应字段。

## 权威口径

1. 正式体系概率和未来可期概率必须并存，不得互相覆盖。
2. 正式体系概率：保持现有“候选体系有效 GS 达到该分类/部位最低档门槛”的定义。
3. 未来可期目标：终局重铸后全部副属性官方 GS `>=75`，对应 R61。
4. 未来可期 GS 计算全部副属性，不按某个候选体系过滤，不包含主属性。
5. 85 级终局默认重铸，不受旧 `reforgeEligible` 影响；90 级不得再次重铸。
6. normal_85 与 rift_85 的剩余强化跳值分布继续独立；Heroic `+12` 补第 4 条，Epic 所有节点跳既有属性。
7. 正式分类优先于未来可期。只有没有形成合格正式路线时，“未来可期”才作为后备终局目标；不得让混合高总分路线覆盖合格的输出/坦克等正式体系。
8. 未来可期仍是辅助/后备价值，不进入正式百里分。未完成概率校准前，不得仅凭该概率发布新的 `+0/+3` 自动继续规则。

## 新增字段

全局早期 debug 至少输出：

- `expected_final_total_substat_gs_native`：未转换的终局预期全部副属性官方 GS。
- `future_75_gs_threshold`：固定为 `75.0`。
- `terminal_future_75_probability_native`：未转换路径终局达到 75+ 的概率。
- `expected_final_total_substat_gs_after_max_conversion`：按当前选定合法转换路线满值转换后的终局全部副属性官方 GS；没有转换路线时为 `null`。
- `terminal_future_75_probability_after_max_conversion`：对应转换路线达到 75+ 的概率；不适用时为 `null`。
- `terminal_future_75_probability`：当前实际后备路线使用的 75+ 概率。
- `terminal_goal_type`：`formal_category` 或 `future_75_fallback`。
- `terminal_goal_probability`：当前目标对应的概率。

每个正式候选仍保留原有低档门槛和正式达标概率。GUI 中文不得继续把单一“终局达标概率”同时代表两种概念。

## 转换规则

1. 未转换 75+ 概率必须始终计算。
2. 若当前选定正式候选存在合法转换路线，额外计算该同一转换路线对全部副属性官方 GS 和 75+ 概率的影响，转换值使用已确认的 Fribbels Greater 100% 满值表。
3. 不得为了 R61 单独发明不符合现有“唯一无效词条、rolls <=2、目标不重复”约束的新转换路线。
4. 原生值和转换后值必须同时展示，不能只显示乐观转换结果。

## 建议边界

- 存在合格正式候选时，继续按正式候选层级和正式达标概率排序；75+ 作为辅助展示。
- 不存在合格正式候选时，可以把“未来可期 75+”显示为后备目标，并进入轻量 debug。
- 本任务不自行编造“75+ 概率达到多少就自动继续”的阈值。
- `+0/+3` 后备未来路线在没有已发布校准规则时最多为谨慎继续/待 +6 复核，不能自动继续。
- 精确 DP 的正式百里 utility 和 R61 辅助价值边界保持不变；若以后要让 R61 进入 DP utility，必须单独立项。

## 执行要求

1. 先新增失败测试，证明当前正式候选概率使用低档门槛且没有独立 75+ 概率。
2. 复用评分引擎的官方 GS 权重、标准化和重铸逻辑，不得在 GUI 复制公式。
3. 概率计算必须使用当前装备、剩余合法强化事件、来源/品质跳值和 85 重铸；不得用终局均值与 75 直接比较冒充概率。
4. 新字段贯通策略 debug、`debug_view_model`、真实样本 JSON/CSV、详情窗口和旧记录兼容重算。
5. GUI 全中文，明确显示“正式体系低档达标概率”和“终局 75+ 未来可期概率”。
6. 用第一轮 29 件人工样本重新生成建议/debug，但保留人工判断、分歧原因和备注。
7. 重点复核目标体系为“淘汰/提取”的 8 件分歧样本，报告 75+ 概率是否解释人工继续/停止差异。
8. 更新 README、规则审阅、人工验收说明和计划文档。
9. 运行相关测试及全量测试。

## 最低回归

- 正式低档门槛小于 75 时，两种概率分别展示且数值口径不混淆。
- 当前/确定终局全部副属性 GS 已达到 75 时，未来可期概率为 1。
- 合法理论上界低于 75 时，未来可期概率为 0。
- 未命中正式体系但存在 75+ 可能性的装备能显示“未来可期”后备路线。
- 合格正式输出/坦克候选不会被未来可期覆盖。
- 85 级 `reforgeEligible=true/false` 的 GS、概率和建议一致；90 级不重复重铸。
- normal_85/rift_85 概率保持各自跳值分布；rift_85 + Heroic 仍被拒绝。
- 转换候选同时显示未转换和满值转换后的 75+ GS/概率。
- `建议结果/14.json` 仍为谨慎继续/输出候选；`建议结果/07.json` 保持原速度路线。
- 第一轮 29 件人工记录在重算后不丢失。
- 相关测试和全量测试通过。

## 最终报告

- 根因与代码证据；
- 正式低档概率和 75+ 未来可期概率的公式/差异；
- 转换前后 75+ 概率口径；
- 29 件人工样本重算结果，特别是 8 件淘汰/提取分歧；
- 14/07 回归；
- GUI/持久化验证；
- 测试结果；
- 后续是否需要校准自动继续概率门槛。
