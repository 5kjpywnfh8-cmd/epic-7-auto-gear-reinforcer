# 纯视觉 OCR 缓存与套装强化证据离线修复任务说明

## 目标

在不连接 MuMu、不读取或上传游戏数据、不执行截图或点击的前提下，修复并验证纯视觉离线识别的三个已知缺口：PaddleOCR 模型缓存路由、套装图标局部模板的确定性预处理/锚点评估，以及 `+0` 强化证据的来源边界。完成后仍必须保持 `mode=visual_only`、`verification=unverified` 和 fail-closed。

## 唯一权威来源与输入

本文件是本轮唯一权威任务来源。执行必须同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md`、`纯视觉 ADB 局部模板与 PaddleOCR 只读识别任务说明.md`、`纯视觉套装图标模板补充任务说明.md`、`纯视觉局部套装模板匹配器任务说明.md`、`纯视觉套装局部裁剪与强化证据锚点修正任务说明.md` 及其已记录的真实校准结果。

输入仅限现有源代码、公开测试、已提交的本地模板清单和合成 PNG/文本 fixture。不得读取任何私人归档、真实截图文件或游戏数据作为本轮输入。

## 范围

1. 审阅并修复 `src/e7_enhance/ocr_paddle.py` 的缓存配置，使检测、识别、方向分类模型目录显式且稳定地指向传入 ASCII 缓存根下的已存在模型；缺文件、路径漂移、非 ASCII 或引擎参数不一致必须 fail-closed。
2. 审阅 `src/e7_enhance/visual_set_template_matcher.py` 的局部模板比较，增加最小、确定性且可审计的输入预处理/锚点评估能力；不得降低固定 `0.98` 阈值，不得把候选锚点写成字段确认。
3. 审阅强化证据解析和区域绑定，明确只从 `enhance` 局部读取唯一、置信度达到 `0.98` 的 `+N`，或在没有 `+N` 时读取唯一 `exp0/...`；不得从用户描述、全屏其它文字或缺失字段推断 `+0`。
4. 补充公开离线测试，覆盖缓存目录绑定/缺失模型、模板预处理命中与歧义拒绝、区域越界、`+N`/`exp0`/冲突/非零经验和完整适配链路。
5. 同步本文件与 `e7-gear-enhance-plan.md` 的完成项、验证结果、未完成项、风险和下一步。

## 禁止修改与操作

- 禁止 ADB `devices`、`exec-out screencap -p`、`input tap`、`swipe`、`keyevent`、页面导航、点击、强化、选材、重启游戏和资源消耗。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器和任何外部上传；禁止下载模型或联网。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR `0.98` 门槛、Holdout、ADB 帧源、点击执行器和自动化规则。
- 禁止把真实帧、私人截图或 OCR 原始图像写入磁盘；不得触碰 `refs/heads/main - 副本` 或私人归档；不得使用 `git add -A`。

## 执行步骤

1. 在全新 Python 进程中只读检查当前缓存布局和依赖接口，不下载、不重试；确认实现所需的模型文件名和 PaddleOCR 构造参数。
2. 先写测试，再对缓存路由和模型目录显式绑定做最小实现；引擎初始化必须能证明使用传入缓存而非用户默认目录。
3. 对套装模板比较设计预处理和锚点评估，保持现有注入接口与证据 schema；用合成 PNG 验证唯一命中、低分、并列和尺寸漂移均按契约处理。
4. 对强化解析补充来源/区域约束测试；任何缺失或矛盾保持拒绝。
5. 精确审阅 diff，运行定向测试、Python 3.9 语法检查和 `git diff --check`。
6. 只暂存本任务涉及的 Markdown、源代码和测试文件，精确提交并推送当前分支；不得暂存既有用户改动。

## 产物

- 本任务涉及的最小源代码与公开测试改动。
- 本文件与 `e7-gear-enhance-plan.md` 的同步记录。
- 测试、语法检查、diff 检查和提交/推送结果。

## 停止条件

依赖缺失、模型文件无法只读验证、缓存路径仍会漂移、模板输入无法确定性解码、任何区域越界、置信度低于 `0.98`、强化证据缺失/冲突、测试失败或触及禁止操作时立即停止并 fail-closed；不得进入真实 ADB 校准、点击执行器或自动强化。

## 当前状态

`offline_ocr_cache_template_enhance_evidence_verified_20260726`

执行模型：`gpt-5.6-terra + high`。

## 完成项与验证记录（2026-07-26）

- `src/e7_enhance/ocr_paddle.py` 现在以固定 `PP-OCRv4` 参数显式传入检测、识别和方向分类三个模型目录；所有目录必须位于传入的 ASCII 缓存根 `userprofile/.paddleocr/whl/` 下，并具备 `inference.pdmodel` 与 `inference.pdiparams`。缺根目录、非 ASCII、路径逃逸或任一模型缺失均在引擎初始化前 fail-closed；不再依赖或改写默认用户目录环境变量。
- 套装匹配器只增加固定 `(0.5, 1.0, 2.0)` 的 RGBA 最近邻缩放预处理，输出局部 `anchor_bounds`、`template_scale` 和预处理标记；固定 `0.98` 阈值未变，跨模板、跨尺度或跨位置并列仍拒绝。
- 强化解析仅消费 `region=enhance` 的唯一 `+N`，或无 `+N` 时唯一 `exp0/...`；区域外、缺失、重复、非零经验和低于 `0.98` 的证据均拒绝。ADB 局部适配链同时要求实际存在 `enhance` 局部区域。
- 新增公开合成测试覆盖显式缓存绑定/缺模型、缩放锚点、区域外强化、重复与低置信度证据，以及完整视觉未确认链路。
- 验证退出码均为 `0`：`test_ocr_paddle.py` `14/14`、`test_visual_set_template_matcher.py` `7/7`、`test_visual_adb_recognition.py` `8/8`、`test_visual_platform.py` `7/7`、`test_visual_templates.py` `5/5`、`test_visual_*.py` `65/65`；Python 3.9 `py_compile` 与 `git diff --check` 通过。
- 本轮未执行真实 ADB、截图、OCR 推理、页面导航、输入、点击、强化、选材、资源消耗、底层读取、外部上传、联网或模型下载，也未读取私人截图。实现与测试已精确提交为 `cf7ce61` 并推送当前分支；提交时 Git 仍报告既有 `refs/heads/main - 副本` 异常引用的 geometric repack 错误，但该引用未被修改。

## 未完成项、风险与下一步

- 本轮只验证本地模型布局和构造参数，不执行模型初始化或真实 OCR；尚未证明实际缓存模型、真实页面模板或强化局部可达到 `0.98`。
- 最近邻缩放仅是确定性的离线候选预处理，不构成套装字段确认；所有输出仍是 `mode=visual_only`、`verification=unverified`。
- 后续真实只读校准、页面导航、受控点击和自动强化均不属于本任务，必须新建任务说明并取得单独授权；在此之前继续 fail-closed。
