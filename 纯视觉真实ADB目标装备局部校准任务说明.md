# 纯视觉真实 ADB 目标装备局部校准任务说明

## 目标

对用户当前已打开的目标装备详细页执行一次真实、只读、内存内的 ADB 截图校准，检查红装命中武器页面的右侧详情区域、套装图标/文字局部、等级、属性、分数和可强化状态是否能形成视觉证据。输出只表示本地视觉观测，不表示服务器确认。

## 唯一权威来源

本文件是本轮唯一权威任务来源。同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md` 和现有纯视觉/OCR接口。聊天内容不能替代或扩大本文件范围。

## 目标装备口径

- 红装/传说、85级、命中套武器。
- 主属性攻击力 100。
- 副属性暴击率 5%、攻击力 8%、效果抗性 8%。
- 官方装备分数 28。
- 装备名称不是强化策略输入，本轮不要求名称识别或绑定。
- 用户已说明未强化装备没有 `+0` 文字；本轮记录页面是否存在可强化/未满 15 的明确视觉证据，不把 OCR 缺失单独推断为 +0。

## 范围

1. 完整读取本文件后，运行一次 MuMu 官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`。
2. 只接受自动发现且状态为 `device` 的唯一设备；不得写死、复用或主动传入任何 `192.168.x.x:5555` 地址。
3. 在设备闸门通过后，仅执行一次 `exec-out screencap -p`。PNG 只存在于当前进程内存，不落盘、不上传、不进入私人归档。
4. 在内存中校验 PNG 结构、CRC、视口、方向、非黑屏和 SHA-256；根据现有红装详情区域裁剪 `set_icon`、`set_text`、`level`、`enhance`、主属性、副属性和分数区域。
5. 复用现有局部模板匹配、PaddleOCR 字段门槛和 fail-closed 规则，记录候选文本、字段置信度、模板分数、区域边界和拒绝原因。因本轮只取一帧，结果是校准观测，不宣称正式稳定帧通过。

## 明确禁止

- 禁止 `adb input tap`、`swipe`、`keyevent`，禁止页面导航、点击、强化、选材、资源消耗、重启游戏或任何 GUI 操作。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、外部上传和模型下载。
- 禁止降低或绕过 `0.98` 字段/模板置信度门槛，禁止以装备名称缺失、套装文字缺失或 `+0` OCR 缺失臆测字段。
- 禁止读取、恢复、暂存或上传私人归档、私人截图；不得触碰或删除 `refs/heads/main - 副本`；不得使用 `git add -A`。
- 本轮不得为了满足稳定帧而重复执行 ADB 截图。正式运行时的多帧稳定采样必须另行按既有稳定帧契约验证。

## 执行步骤

1. 完整阅读本文件及其列出的上位文档，确认当前分支和工作区，不修改代码。
2. 运行一次 `adb devices -l`，保存命令输出摘要；若设备不是唯一 `device`，立即停止。
3. 由自动发现的序列号派生一次 `exec-out screencap -p`；不手工指定 IP/端口。
4. 在内存中完成 PNG/视口/方向/非黑屏/哈希校验，并使用现有区域与识别器做一次局部观测。
5. 精确记录通过字段、低于 `0.98` 的字段、缺失证据、区域漂移、模板并列和任何异常；结果固定标记 `mode=visual_only`、`verification=unverified`、`click_performed=false`。
6. 将完成项、验证结果、未完成项、风险和下一步写回本文件与 `e7-gear-enhance-plan.md`。本轮不提交代码；若文档发生修改，后续按项目规则单独精确审阅和提交。

## 产物

- 一次真实 ADB 只读截图的内存校验摘要（不含截图文件）。
- 红装详情局部区域、模板/OCR 结果、置信度和 fail-closed 原因。
- 本文件与总计划的状态记录。

## 验证要求

- 设备发现失败、设备不唯一、PNG 损坏、旋转/视口漂移、黑屏、区域越界、模板缺失、锚点缺失、低于 `0.98`、字段矛盾或页面不匹配均必须停止并 fail-closed。
- 本轮真实观测与离线测试分开报告；单帧校准不得写成稳定帧或服务器确认。
- 执行模型记录为 `gpt-5.6-terra + high`。

## 停止条件

任一前置条件或只读校验失败立即停止，不重试、不截图第二次、不进入点击接线。只有局部证据和后续稳定帧契约均通过后，才可另建受控 ADB 点击任务并重新取得明确授权。

## 执行记录（2026-07-27）

- 设备发现通过：官方 MuMu `adb.exe devices -l` 返回唯一 `emulator-5554 device`，未使用网络序列号。
- 唯一一次截图通过：`exec-out screencap -p` 返回 `921775` 字节，视口 `1280x720`，PNG 结构/CRC/像素流/非黑屏校验通过，SHA-256 为 `36503de6d8d3cb4e97d998d8488497d14b9ce8ad73f6f4e0d9e15f4035b9df94`。PNG 未落盘、未上传，未执行第二次截图。
- 现有红装区域在内存中完成解析：`set_icon=(819,550)-(900,616)`、`set_text=(900,550)-(1120,616)`、`enhance=(819,115)-(1216,180)`，其余详情区域均在视口边界内。
- 本地套装图标模板没有唯一候选：`set_icon_anchors=[]`。
- PaddleOCR 只读观测返回少量非目标页面文本，主要为“持有点数”“选择套装”；`85` 仅得 `0.797087`，低于 `0.98`。解析结果 `accepted=false`，拒绝原因为 `low_confidence:level`、`missing:enhance`、`unrecognized:gear_score`、`unrecognized:main_and_substats`、`unrecognized:rank_slot`、`unrecognized:set`。
- 结果固定为 `mode=visual_only`、`verification=unverified`、`click_performed=false`；未执行 ADB 输入、导航、点击、强化、选材、资源消耗、底层读取或外部上传。

## 当前状态

`real_adb_target_local_calibration_completed_fail_closed_page_mismatch_20260727`

执行模型：`gpt-5.6-terra + high`。

## 未完成项、风险与下一步

- 当前帧不能确认目标红装详细页，也不能确认命中套武器、等级、套装、属性、分数或未满 15 强化状态；不能进入 ADB 点击接线。
- 阻断原因是页面/证据不匹配和局部识别缺失，不是将 `0.98` 门槛调低即可解决的问题。
- 下一步必须另建任务说明并取得新的单次只读授权；用户需在 MuMu 中确认已进入目标装备的右侧详细信息页后再重新校准。重新校准前不执行任何 ADB 输入或自动重试。
