# 纯视觉真实武器列表页参考资产三帧只读采集任务说明

## 目标

在用户已进入武器列表页的前提下，采集一次固定三帧只读内存 PNG，并仅从其中生成标记为 `reference_only` 的武器列表版式/卡片 crop，补齐离线字段映射和模板资产准备所需的参考输入。

## 范围

- 只运行官方 MuMu `adb.exe devices -l` 并接受唯一状态为 `device` 的设备。
- 只读取固定三帧 `exec-out screencap -p`，内存校验后仅保存固定裁切 crop，不保存全屏截图。
- 只校验 PNG、非黑帧、`1280x720` 视口、来源和时间单调；采集结果不作为当前目标装备通过证据。
- 生成武器列表网格、代表性卡片和必要详情面板的 `reference_only` 资产与 manifest。

## 禁止操作

- 禁止 `input`、`tap`、`swipe`、`keyevent`、点击、导航、强化、选材、确认和资源消耗。
- 禁止实时 OCR/模板推理、目标唯一性结论、自动重试、第二批采样、联网、上传、模型下载和底层数据读取。
- 禁止把本次参考 crop 写成当前目标字段或真实导航授权。

## 输入与权威口径

- 本文件为本轮唯一权威任务来源，同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md` 和列表资产准备任务说明。
- 用户已说明当前已进入武器列表页；该描述只作为采集前置，不替代视觉证据。
- 实际执行模型：`gpt-5.6-terra + high`；用户偏好的 `gpt-5.6-luna + max` 当前接口不可用。

## 执行步骤

1. 执行一次官方 `adb devices -l`，确认唯一 `device`。
2. 对唯一设备读取恰好三帧内存 PNG，校验 PNG、非黑帧、`1280x720` 和时间单调。
3. 仅从最后一帧生成固定 `reference_only` crop，不保存全屏图；记录源哈希和每个 crop 的哈希/边界。
4. 不运行生产 OCR/模板读取器，不输出目标字段通过结论。
5. 同步本任务说明、资产 manifest、列表资产准备任务和总计划。

## 产物

- 武器列表 `reference_only` crop 与 manifest。
- 本任务说明和总计划的采集记录。

## 停止条件

- 设备不唯一、PNG/视口无效、时间倒退或任何输入/导航需求立即 fail-closed。
- 需要保存全屏截图、实时 OCR、目标识别或第二批采样时立即停止。

## 当前状态

`reference_crops_captured_manifest_reviewed_waiting_offline_asset_build_20260801`

## 授权记录（2026-08-01）

- 用户明确表示已进入武器列表并允许采集；本授权仅覆盖本文件定义的一次三帧只读参考资产采集，不包含点击或导航。

## 执行记录（2026-08-01）

- 官方 MuMu `adb devices -l` 退出码为 `0`，发现唯一 `emulator-5554`，状态为 `device`。
- 恰好读取三帧内存 PNG；三帧均通过 PNG、非黑帧、`1280x720` 和时间单调校验，哈希相同。
- 仅从第三帧保存四个固定 `reference_only` crop，未保存全屏截图；manifest 为 `assets/visual/list_reference/weapon_capture_manifest_20260801.json`。
- 右侧详情参考可交叉确认武器主属性攻击力、生命值、速度、暴击伤害和分数 `18`；这些不是当前目标通过证据。
- 未执行 OCR、点击、导航、强化、选材、确认或资源操作。

## 下一步

把武器参考 crop 合并到列表资产准备任务，继续补齐攻击%、暴击率%、效果抗性图标和整卡指纹模板；完成后另建真实三帧视觉字段校准任务并重新授权。
