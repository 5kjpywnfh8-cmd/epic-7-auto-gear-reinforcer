# 纯视觉套装多裁切方案离线对比任务说明

## 目标

针对红装基准的装备详情页，增加可审计的套装图标/文字多裁切候选方案，在同一离线截图上并行比较候选区域、预处理和识别结果，寻找达到固定 `0.98` 字段置信度且唯一的套装证据。该任务不进入 ADB 点击或自动强化。

## 当前状态

`offline_set_crop_comparison_text_wide_pass_icon_low_fail_closed_20260727`

执行模型：`gpt-5.6-terra + high`。

当前执行范围：仅实现显式套装裁切候选与纯内存离线比较器，并补充公开合成测试；不接入实时 OCR、ADB 或点击链路。套装匹配器仅增加受控首个 `IEND` 和 8-bit 调色板 PNG 解码，候选已按红装图标标注基准 `(875,540)-(925,596)`（`1280x720`）调整，保留宽裁切供离线比较。

## 完成记录（2026-07-27）

- `src/e7_enhance/ocr_regions.py` 增加四个固定候选：红装图标宽/紧裁切、套装文字宽/紧裁切；候选不进入默认 parser manifest，未改变默认 PaddleOCR 入口。套装匹配器增加受控首个 `IEND` 和 8-bit 调色板 PNG 解码。
- `src/e7_enhance/set_crop_comparison.py` 在同一输入帧中依次提取全部候选并分别调用注入的图标/文字识别器，输出每个候选的像素边界、置信度、唯一性和拒绝原因。仅当两通道各自唯一达到 `0.98` 且值一致时接受；结果固定为 `visual_only`、`unverified`、`click_performed=false`。
- 合成测试覆盖固定边界、越界、重复候选、低置信度和图文矛盾；定向 4/4、相邻 OCR 15/15、AST 语法检查和 `git diff --check` 均通过。报告：[reports/visual_set_crop_comparison_20260727.md](reports/visual_set_crop_comparison_20260727.md)。

## 风险与下一步

离线截图实测显示，未标注 `before_page.png` 的宽文字候选为 `速度套装（0/4） @ 0.981642`，紧文字候选为 `0.961027`；标注图宽候选为 `0.977467` 且受到绿色人工标注额外 OCR 行干扰。图标候选经受控模板解码后最高约 `0.853229` / `0.792352`，均低于 `0.98`，因此完整套装证据继续 fail-closed。后续如需真实只读校准，必须另建任务说明并重新获得授权。

## 权威输入

- 根目录 `AGENTS.md`。
- 根目录 `e7-gear-enhance-plan.md`。
- 既有纯视觉任务说明及公开测试。
- 用户提供的离线参考截图：`C:\Users\orangine\Downloads\before_page(1).png`、`C:\Users\orangine\Downloads\before_page.png`。截图只允许内存读取，不复制到仓库、不上传。

## 范围

1. 审阅现有 `ocr_regions.py`、套装模板识别器、PaddleOCR 适配器和公开测试。
2. 为套装图标和套装文字定义有限、显式、可审计的候选裁切方案；候选不得越界、不得动态猜测或自动重试。
3. 在同一帧并行提取所有候选，分别运行图标模板匹配和套装文字 OCR，输出候选名称、像素边界、置信度、唯一性和拒绝原因。
4. 选择结果时保持字段 `0.98` 门槛、唯一候选要求和 fail-closed；不能用低置信度文字由图标缺失补齐，也不能反向推断。
5. 增加合成/公开回归测试，覆盖多方案比较、越界、重复最佳候选、低置信度和字段矛盾。
6. 任务完成后精确审阅 diff，运行定向测试、语法检查和 `git diff --check`，并把结果同步回本任务说明和总计划。

## 禁止修改与操作

- 禁止 ADB `devices`、截图、`input tap`、页面导航、强化、选材、资源消耗和游戏重启。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层装备读取器、外部上传、模型下载和联网。
- 禁止降低或绕过 `0.98`、稳定帧、视口、哈希、唯一性和 fail-closed 规则。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。
- 禁止把截图写入仓库或私人归档；禁止使用 `git add -A`；禁止触碰 `refs/heads/main - 副本`。

## 预期产物

- 套装候选区域配置/比较器的最小实现（如现有抽象无法承载，才新增模块）。
- 对应公开测试及离线比较报告，报告每个候选的结果和拒绝原因。
- 本任务说明和 `e7-gear-enhance-plan.md` 的状态、验证、风险和下一步记录。

## 停止条件

任何输入截图损坏、视口不一致、候选区域越界、模板无法完整解析、任一必需字段低于 `0.98`、候选不唯一或字段矛盾时，立即保持 fail-closed；不得进入真实 ADB 校准、点击接线或自动强化。真实只读校准必须另建任务说明并重新取得授权。
