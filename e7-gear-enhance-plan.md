# 第七史诗装备强化判断工具计划

## Summary

## 早期轻量预测与转换候选（2026-07-11）

状态：已完成策略与回归测试；自动继续校准规则仍未发布。

- 权威任务说明：[早期轻量预测与转换策略任务说明.md](早期轻量预测与转换策略任务说明.md)。
- `+0/+3` 改为逐个“体系 x 部位”候选使用第 6.4 节终局低档 GS 阈值、来源/品质跳值、85 级重铸、理论区间和终局达标概率评估；不再用统一副属性总分决定候选方向。
- 三条命中加一条可转换偏离属性保留为候选，前台仍为“谨慎继续，待 +6 精确复核”；转换不计入数值达标概率。
- 高优先级候选（输出、输出必爆、抗坦、纯肉、命坦）优先于双效和半肉；同层按终局达标概率排序。
- `建议结果/14.json` 已固定为“谨慎继续 / 输出候选”，`建议结果/07.json` 仍为“继续 / 多跳速度高价值路线”。
- 离线校准记录：[reports/early-lightweight-conversion-20260711.md](reports/early-lightweight-conversion-20260711.md)。当前仅完成 6 条定向精确 DP 标签，规则表保持为空，未覆盖分组固定转入 +6 精确 DP 复核。
- 主 agent 复核：策略测试 28 项、GUI 支持测试 16 项、全量测试 133 项均通过；14 的 `0.0012` 终局达标概率只参与候选排序，不会绕过“四条完整命中 + 已发布校准规则”的自动继续门槛。

### 下一阶段：校准数据与人工验收

当前阶段只扩充证据，不发布新的早期自动继续规则。

操作说明：[早期校准与人工验收操作说明.md](早期校准与人工验收操作说明.md)。

1. 先扩充离线精确 DP 校准：每个拟发布“`+0/+3` 四条完整命中自动继续”的具体分组，至少需要 8 条训练继续标签和独立保留集；训练集与保留集不得再出现 `6 / 0` 这种没有独立验证的切分。
2. 分组必须保持来源、品质、部位、主属性约束、体系、套装组、当前有效副属性数和可行有效副属性数；normal/rift 绝不合并。
3. 优先校准高优先级体系的代表性分组：输出/输出必爆、抗坦/纯肉/命坦；再覆盖双效和半肉。输出衣服三条合法有效副属性必须作为独立组验证。
4. 每次拟写入 `lightweight_calibration_rules.json` 的规则，必须先报告训练集、保留集、误继续数、误停止数、样本种子和覆盖范围；证据不足时继续保守复核。
5. 收集真实 `+0/+3` 人工验收样本，特别记录三条命中一条偏离、输出衣服、输出/坦克概率排序、双效/半肉竞争候选。14 当前只验证候选方向正确，不证明其 0.12% 原生终局概率值得自动继续。
6. 转换石结果分布、成本和实际资源价值仍是独立分支；未得到权威规则前，转换继续仅作为 debug 候选，不进入数值概率或早期自动继续。
7. 当前 GUI 仅支持单件编辑、导入和保存，不支持从第三方导出批量筛选未强化装备、追加样本集合或自动填写人工记录。若真实样本来自导出文件，先确认字段兼容；不兼容或需要批量截取时另立只读转换/筛选工具任务。
8. 输出（必爆）衣服在正式矩阵中没有单独公式，不发布必爆衣服自动继续规则；它应按实际属性竞争普通输出或半肉候选，而非被直接淘汰。

## 历史讨论：+0 / +3 三条命中与转换优先级（2026-07-10，已由 2026-07-11 实现记录覆盖）

状态：已完成实现与回归；具体结果见本文顶部“早期轻量预测与转换候选”及校准记录。

任务说明：[`早期轻量预测与转换策略任务说明.md`](早期轻量预测与转换策略任务说明.md)。该说明已合并此前“体系匹配、部位差异化门槛与转换优先级”指令，并以最新讨论为准：`+0 / +3` 单条未命中时仍为“谨慎继续”、转换仅为候选路径；终局 90 级 `GS` 门槛用于概率校准而非当前分数直接比较。

子 agent 交接规则：只发送“请完整阅读并执行根目录《早期轻量预测与转换策略任务说明.md》；该文档覆盖此前所有同主题指令，以其中最新规则为唯一标准。”不得将旧版指令与新指令拼接，以免恢复已废弃的 `+0 / +3 -> convert` 预期。

复现输入：`建议结果/14.json`。

当前观察：

- 该样本为 `+3` 普通 85 红装暴击套武器，副属性为攻击%、暴率、爆伤、命中。
- 当前工具给出“谨慎继续”，但目标体系选择为“半肉(通用)”。
- 输出体系实际命中攻击%、暴率、爆伤三条，命中为唯一未命中副属性；因此需要验证“体系优先级”和“转换石路径”是否被错误地退化为半肉路线。

待确认的用户规则：

1. `+0 / +3` 时，四条副属性均命中同一体系且分数达到该体系门槛，建议“继续”。
2. `+0 / +3` 时，三条副属性命中同一体系且副属性总分达到门槛，建议“谨慎继续”。
3. 仅有一条副属性未命中目标体系的装备，应保留可通过转换石修正的候选路径；不能因未形成四条完整分类而自动退化为低优先级体系。
4. 当多个转换候选同时成立时，优先级应为：输出、坦克体系优先；双效、半肉靠后。原因是双效和半肉的高分装备相对更容易取得、需求量相对更小。

已确认的补充口径：

1. “副属性总分”不得使用全局统一阈值，也不得仅以 `official_parts.substats` 单独作全局判定。阈值必须随体系、部位、主属性、可合法出现的副属性数及白字需求变化。
2. 输出衣服等受部位副属性禁用规则限制、最多只能形成三条有效副属性的情形，三条有效副属性必须是完整的正常路径，不能机械地按“四条才完整”降级。
3. `+0 / +3` 时，恰有一条未命中、且满足优先体系的转换资格，前台建议仍为“谨慎继续”；转换只作为 debug 中的候选终局路径，不得提前显示为“转换/过渡”。
4. 转换候选排序固定为：输出、坦克体系优先；双效、半肉靠后。排序只能在候选均满足各自体系/部位门槛时生效，不能用低分装备虚构转换价值。
5. 输出与坦克同属高优先级候选时，按各自终局达标概率从高到低排序；概率相同才使用稳定的既有分类顺序打破平局。

门槛证据与校准结论：

1. 《装备强化与评分规则审阅.md》第 6.4 节已经提供静态正式分类基础门槛表。表中 `GS` 是“该分类有效副属性官方分”，并按“分类 × 部位”给出高/中/低档门槛；它不是统一的副属性总分。
2. 该静态表应作为体系、部位和白字需求差异的权威基础。例如输出武器低档门槛为 `GS=70`，输出衣服低档门槛为 `GS=64`；衣服的较低门槛正是其合法有效副属性受限的体现。
3. 第 6.4 节的门槛是终局正式评分门槛，不能把 `+0 / +3` 当前 `GS` 直接拿来判继续。早期规则需要运行精确 DP/合法跳值数据，校准为“预期终局 GS、理论上界/下界或终局达标概率”的分组门槛。
4. 子 agent 不得自行编造新的统一数值。若某个“体系 × 部位”没有静态公式（表内为 `-`）或没有足够校准数据，只能给出人工复核，不能自动继续。
5. `+0 / +3` 的终局达标概率必须以“当前副属性 + 剩余强化事件 + 来源/品质跳值分布 + 85 级默认重铸”计算 90 级终局 `GS` 是否达到第 6.4 节对应格的低档门槛。禁止把当前 `GS` 按强化等级线性放大，或将 90 级门槛直接作为低强化装备的当前分数门槛。
6. 转换石尚未有权威的结果分布模型时，不得把未经验证的转换结果并入数值概率。`+0 / +3` 仅将其记录为候选终局路径，前台仍为“谨慎继续”；未来若补充转换石规则，再单独校准“含转换”的概率。

本轮子 agent 任务的最低验收：

- 使用 `建议结果/14.json` 复现当前建议、基础建议、轻量预测路径、完整分类诊断和候选体系排序。
- 为每个“体系 × 部位 × 主属性约束 × 可行有效副属性数”明确门槛口径，禁止用未定义的全局分数替代。
- 先写最小失败回归测试，再实施最小修复。
- 修复后，14 在 `+3` 的建议仍为“谨慎继续”，但 debug 的首选转换候选必须为输出、且能解释输出优先于半肉；不得改变 `07.json` 的既有“继续”结果。
- 覆盖 `+0`、`+3`、四条命中、三条命中、单条未命中可转换、双效/半肉低优先级的回归样本。
- 运行相关测试和全量测试，并更新本计划与必要的分支记录文档。

主 agent 约定：每次讨论或子 agent 结论到达后，必须更新本计划；若出现独立的策略分支、规则假设或验收结论，新增单独的 Markdown 记录并在此处链接。

当前已从“策略校准 / 模拟验证”推进到“`dp_assisted` 默认策略接入阶段”。

总体节奏仍是：先稳定强化判断，再接 OCR，最后再考虑自动化。

核心结论：

- OCR 是实用化必需项，但不和评分逻辑第一阶段混在一起调。
- `R2-R58` 正式分类百里分是主目标；`R61` 未来可期只做辅助展示。
- 普通 85 红装默认策略改为 `normal_epic_dp_assisted`。
- 普通 85 紫装默认策略改为 `normal_heroic_dp_assisted`。
- 异界 85 红装继续保持 round2 推荐策略，不启用 DP 默认。
- DP 只在 `+9 / +12` 节点影响建议，`+0 / +3 / +6` 保持 round2 策略。
- 当前不需要继续扩大样本，下一步重点是 PySide6 桌面可视化操作台。
- 桌面主程序使用 PySide6；Airtest 只作为后续可选操作层，不作为主界面和决策层。
- 开发阶段保留完整调试输出；真实强化时默认只给简洁建议。

## Current Status

截至 2026-07-10：

- `normal_85 Epic`：
  - baseline cost 均值：`932.82`
  - DP cost 均值：`817.24`
  - `1M x 5 seeds` 中 `5/5` 胜出
  - 建议默认启用 `normal_epic_dp_assisted`
- `normal_85 Heroic`：
  - baseline cost 均值：`22838.96`
  - DP cost 均值：`5864.86`
  - `1M x 5 seeds` 中 `5/5` 胜出
  - 建议默认启用 `normal_heroic_dp_assisted`
- `rift_85 Epic`：
  - DP assisted 在验证中表现变差
  - 不接入默认策略，只保留人工复核工具
- 已完成验证：
  - round2 深度验证
  - route_solver / DP 精确强化树复核
  - dp_assisted `100k x 3` 与 `1M x 5` 复验
  - 可视化报告
  - 转换成本敏感性分析
  - 单元测试：`66 tests OK`
- 默认策略接入已完成：
  - 核心接入文件：`src/e7_enhance/strategy_defaults.py`
  - 核心接入文件：`src/e7_enhance/enhance_policy.py`
  - `--disable-dp-assist` 已接入 `suggest / batch`
  - 非 debug 前台保持简洁
  - 验证：`73 tests OK`

## Desktop App Direction

桌面端采用分层方案：

```text
PySide6 桌面主程序
├─ 装备 JSON 导入 / 手动编辑
├─ 后续截图 / OCR 人工确认
├─ 调用现有 suggest / enhance_policy
├─ 显示简洁强化建议
├─ debug 折叠面板
├─ 保存装备与建议结果
└─ Airtest adapter 预留
   ├─ 截图
   ├─ 模拟器窗口定位
   ├─ 画面元素识别
   └─ 人工确认后的点击
```

技术选择：

- 主 GUI：`PySide6`
- 核心算法：继续复用现有 Python 模块，不迁移、不重写。
- Airtest：后续可选 adapter，只做截图、定位、点击等操作层能力。
- 第一版不做安卓 App。
- 第一版不接 Airtest 点击。
- 第一版不接 ADB 自动化。
- 第一版不允许 OCR 或自动化结果直接触发资源消耗。

桌面版第一阶段目标：

- 不需要命令行即可完成一次装备判断。
- 支持导入装备 JSON。
- 支持手动编辑装备字段。
- 调用当前 `baili-formal-dp-v1` 策略。
- 前台只显示建议、下一检查点、目标体系、关键理由。
- debug 面板展示策略、DP、评分和命中分析。
- 预留截图 / OCR 按钮，但可以先置灰或显示“未接入”。
- 预留 Airtest adapter 接口，但不调用。

## Implementation Changes

规则来源优先级：

1. `C:\Users\orangine\Downloads\套装属性与装等计算表.xlsx`
2. `D:\VScode\E7-tools\Epic-7-tools.v1.1\e7_gear_rating.html`
3. `D:\VScode\E7-tools\Epic-7-tools.v1.1\gear2_orange_rating_updated.csv`

阶段安排：

- `v1`：用 JSON / CSV 样本验证评分、套装契合、强化止损，不接 OCR。已完成。
- `v1.1`：接入 `dp_assisted` 默认强化策略，不接 OCR，不做自动点击。已完成。
- `v1.2`：PySide6 桌面可视化操作台。已完成，进入人工验收。
- `v1.3`：真实样本 JSON 回放与人工审核。
- `v1.5`：加入 OCR 截图识别，但只读装备，必须人工确认和修正。
- `v2`：OCR 识别 + 强化后复检，每个节点重新判断。
- `v2.5`：可选 Airtest / ADB 操作辅助，每次消耗前仍需人工确认。
- `v3`：可选半自动流程，不做无确认全自动强化。

强化节点：

- 固定在 `+0 / +3 / +6 / +9 / +12 / +15` 重新计算。
- `+0 / +3 / +6` 使用 round2 策略判断。
- `+9 / +12` 对普通 85 红装和紫装启用 DP assisted 判断。
- 异界 85 红装不启用 DP assisted 默认判断。
- `+12` 是强制止损节点：
  - 核心属性没中，建议停。
  - 只剩理论期待，建议停或谨慎继续。
  - 仍命中目标体系且价值足够，才建议继续到 `+15`。

## Strategy Defaults

新增集中配置 `strategy_defaults`：

- `normal_85` + `Epic`：`normal_epic_dp_assisted`
- `normal_85` + `Heroic`：`normal_heroic_dp_assisted`
- `rift_85` + `Epic`：保持 round2 推荐策略，`enable_dp_assist=false`

新增策略版本：

- `strategy_version`: `baili-formal-dp-v1`

DP assisted 接入规则：

- 只影响 `+9 / +12`。
- 只对普通 85 红装 / 紫装默认开启。
- 对异界 85 红装默认关闭。
- 保留 `enable_dp_assist` 开关，方便回退。
- debug 必须输出：
  - baseline 建议
  - DP 建议
  - DP utility
  - 是否覆盖 baseline
  - DP 使用的 `lambda_value`
  - 转换成本口径

## Output Design

PySide6 前台布局：

- 左侧：装备输入区
  - JSON 导入
  - 手动编辑
  - 后续截图 / OCR 入口
- 中间：装备字段确认区
  - 套装
  - 部位
  - 主属性
  - 强化等级
  - 装备等级
  - 品质
  - 副属性
- 右侧：强化建议区
  - 建议
  - 下一检查点
  - 目标体系
  - 关键理由
- 底部：debug / 日志折叠面板
  - 策略版本
  - 策略名称
  - DP 详情
  - 百里分 / target_score
  - 命中分析
  - 当前输入 JSON

开发阶段保留完整内部字段：

- `official_score`
- `effective_score`
- `baili_score`
- `target_score`
- `strategy_version`
- `strategy_name`
- `enable_dp_assist`
- `dp_recommendation`
- `dp_utility`
- `baseline_recommendation`
- `dp_override_applied`
- `rating_level`
- `fit_status`
- `valid_substats`
- `invalid_substats`
- `roll_hit_analysis`
- `ocr_raw_result`

真实强化前台默认只显示：

- `建议`：继续 / 停止 / 谨慎继续
- `下一检查点`：例如强化到 `+9` 后复检
- `目标体系`：例如一速、输出、抗坦、半肉等
- `关键理由`：1-3 条，不展示完整评分构成

调试详情放到详情面板或日志，不作为默认界面。

默认前台不展示完整 DP 计算过程，只在详情 / 日志中展示。

## Data Interfaces

统一装备输入结构：

```json
{
  "set": "Speed",
  "slot": "Weapon",
  "mainStat": { "type": "Attack", "value": 525 },
  "enhance": 12,
  "level": 85,
  "rank": "Epic",
  "substats": [
    { "type": "Speed", "value": 12 },
    { "type": "CritChance", "value": 8 },
    { "type": "CritDamage", "value": 14 },
    { "type": "AttackPercent", "value": 12 }
  ],
  "rollHistory": []
}
```

统一判断输出结构分两层：

```json
{
  "summary": {
    "recommendation": "stop | continue | cautious_continue | keep | convert | uncertain",
    "next_check_at": 12,
    "target_profile": "一速",
    "reasons": ["速度命中不足", "+12 后继续收益偏低"]
  },
  "debug": {
    "official_score": 0,
    "effective_score": 0,
    "baili_score": 0,
    "target_score": 0,
    "strategy_version": "baili-formal-dp-v1",
    "strategy_name": "normal_epic_dp_assisted",
    "enable_dp_assist": true,
    "baseline_recommendation": "",
    "dp_recommendation": "",
    "dp_utility": 0,
    "dp_override_applied": false,
    "rating_level": "",
    "fit_status": "",
    "valid_substats": [],
    "invalid_substats": [],
    "roll_hit_analysis": []
  }
}
```

没有 `rollHistory` 时按当前总值保守判断；有强化历史时额外分析每次强化是否命中有效属性。

## Test Plan

已完成验证：

- 用 `gear2_orange_rating_updated.csv` 做回归样本。
- 对比旧工具的官方分、百里分、评级、保留体系。
- 覆盖 `+0 / +3 / +6 / +9 / +12 / +15` 判断。
- 专门测试 `+12` 止损场景。
- round2 普通红装 / 紫装 / 异界深度验证。
- DP route_solver 复核。
- dp_assisted `100k x 3` 与 `1M x 5` 复验。

v1.1 默认策略接入验证：

- `strategy_defaults` 路由正确。
- `normal_85 Epic` 默认使用 `normal_epic_dp_assisted`。
- `normal_85 Heroic` 默认使用 `normal_heroic_dp_assisted`。
- `rift_85 Epic` 默认不启用 DP assisted。
- DP 只在 `+9 / +12` 影响建议。
- `+0 / +3 / +6` 保持 round2 策略。
- `R61` 不进入正式收益和 DP utility。
- 前台输出保持简洁。
- debug 输出 DP 字段。
- 运行少量 smoke 样本，不再跑 `1M` 大样本。
- 单元测试通过。

v1.2 PySide6 桌面验证：

- 能启动桌面主窗口。
- 能导入示例装备 JSON。
- 能手动编辑装备字段。
- 能调用现有 `suggest / enhance_policy`。
- 能显示简洁建议。
- 能展开 debug 面板查看 DP 字段。
- 能保存当前装备 JSON 和建议结果。
- 截图 / OCR 入口存在但不触发真实识别也可接受。
- Airtest adapter 入口存在但第一版不调用。
- 不需要命令行即可完成一次判断。
- 不接 OCR / ADB / Airtest 点击。
- 运行相关单元测试和一次 GUI smoke。

v1.5 验证：

- 用手动截图测试 OCR。
- OCR 结果必须可人工确认和修正。
- 修正后的结构必须复用 v1 评分逻辑。

v2 验证：

- 模拟完整强化流程：识别、确认、建议、强化后复检、再次止损。
- 验证 OCR 错误不会直接触发资源消耗。
- 验证默认前台输出足够简洁，调试字段仍可追踪问题。

## Assumptions

- 第一版不做 ADB 自动点击。
- OCR 放在 `v1.5`，不是取消。
- 当前不需要继续扩大模拟样本。
- 粗略 Score 只作为参考，不作为最终决策。
- 最终判断以 `R2-R58` 正式分类百里分、套装、部位、主属性、副属性有效性、强化命中方向、DP 复核共同决定。
- `R61` 未来可期只作为辅助展示和人工参考。
- 转换石成本已做敏感性分析，但实际玩家价值仍需校准。
- 真实掉落模型尚未按玩家实际掉落分布校正。
- 紫装 DP 提升明显，但仍需要真实使用观察。
- 异界 85 红装暂不接入 DP 默认策略。
- 当前阶段使用 PySide6 做桌面主程序。
- Airtest 可以后续接入，但只作为操作层，不作为主 GUI 和策略层。
- OCR / ADB / Airtest 点击 / 真实点击仍不进入当前阶段。
- 所有计划和实现文件放在 `C:\Users\orangine\Documents\第七史诗强化装备脚本`。

## Remaining Risks

- 真实掉落模型未校正。
- 转换石成本仍需按玩家实际价值校准。
- 紫装成功率低，模拟提升明显但需要真实使用观察。
- 异界 85 红装策略保留 round2 推荐，DP 只作为人工复核工具。
- PySide6 桌面 GUI 已实现，当前处于人工验收阶段。
- OCR 未接入。
- Airtest adapter 未接入。
- ADB 和真实自动点击未接入。

## Plan Maintenance

- 后续凡是已经验证通过并决定接入的项目，都必须同步更新本计划文档。
- 计划状态必须区分：已验证、当前阶段、下一步、剩余风险。
- 不再把已收口的模拟验证任务作为默认下一步重复执行。

## Manual Acceptance Stage

当前进入人工验收阶段。

本阶段目标：
- 使用 PySide6 GUI v1 对真实装备做人工验收。
- 通过 `samples/manual_acceptance_gears.json` 录入 20-50 件真实装备。
- 使用 GUI 导入样本后逐件查看、判断、保存建议结果。
- 使用 `manual_acceptance/manual_test_records.csv` 记录工具建议与人工判断是否一致。
- 分歧先记录，不立即改核心策略；只有发现明显 bug 才修策略。

本阶段产物：
- `manual_acceptance/README.md`：人工验收流程。
- `manual_acceptance/manual_test_records.csv`：人工测试记录模板。
- `samples/manual_acceptance_gears.json`：20 件样本录入文件，可替换为真实装备。

GUI 验收关注点：
- 前台建议保持简洁：建议、下一检查点、目标体系、关键理由。
- debug 可查看：策略版本、策略名、DP 状态、DP utility、是否覆盖、百里分、目标分。
- 保存建议结果包含：原装备 JSON、完整 suggestion、summary_view、debug_view、manual_review 占位字段。

边界：
- 不接 OCR。
- 不接 Airtest。
- 不接 ADB。
- 不做自动点击。
- 不迁移评分 / DP 算法。
