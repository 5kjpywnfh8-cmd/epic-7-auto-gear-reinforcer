# 第七史诗装备强化判断工具计划

### Heroic 中后期 22速差异分层与指标修正（2026-07-13，进行中）

- 研究器升级为独立 v3 schema/resume；旧 v1/v2 smoke 不读入正式统计。
- M1 改为“可达性全救回参考”，不再作为速度效率 Oracle。
- 200 事件闸门改用原始唯一轨迹事件；每十万加权产量与 ESS 分开记录。
- 已确认合法差异见证：normal_85 Heroic 非鞋 +12、14速、4 次速度命中，基础回退 stop、P22>0，M1 返回 continue。
- v3 smoke 仅验证分片与账本，尚未进行正式扩样；不能创建前瞻批次。
- 两次隔离 `tests/test_gui_app.py` 均因既有 `test_gui_support` 顶层导入失败，未修改 GUI。

## Heroic +0/+3 离线 Pareto 研究（2026-07-12）

状态：已完成离线证据收集，**未修改正式策略**。

- 训练/留出严格按实例划分为 64 / 16 件真实 `normal_85 Heroic +0`；16 件 Heroic 资源回退人工批次未参与门槛选择。
- 每件训练装备使用固定种子 `20260712` 的 10,000 条完整合法轨迹，共 640,000 条；Heroic 在 `+12` 补第 4 条词条，胚子成本作为沉没成本排除，只计算从当前节点开始的 50/50 材料强化成本与出售回收。
- 当前基础回退对训练集的 `+0` 通过率为 100%，平均增量 103.10 体力/件，高价值终局召回为全继续的 69.06%。终局正式达标、75+、速度和满值转换救回均独立统计。
- 候选方案只作为下一独立策略任务的输入：终局达标概率 `>=10%` 为保守省资源，`>=5%` 为平衡，全继续为高召回对照。16 件留出集的混淆矩阵已记录，但未用于反调门槛。
- 详情、按部位结果、Wilson 区间和原始指标见 `reports/heroic_early_policy_pareto_20260712.md` 与同名 JSON。下一步需先确认可接受的召回损失和人工留出集的误停容忍度，再建立只改 `+0/+3 Heroic` 门槛的独立正式策略任务。

## Summary

## 真实样本身份核验（2026-07-11）

状态：已修复 Fribbels 装备实例身份归并；可开始正式人工验收。

- `samples/real_acceptance_fribbels_20260610_plus0_plus3.json` 共 425 件，均为 85 级、`+0/+3`、Epic/Heroic；实际策略导入为 425 成功、0 错误。
- 分布为 417 件 `+0`、8 件 `+3`；345 件 Epic、80 件 Heroic。所有来源当前按 `normal_85`，因此不能用于验证异界来源策略。
- 425 件的 `ingameId` / `id` 全部唯一，但只有 41 个不同 `code`；33 组 `code` 重复，涉及 417 件装备。`code` 是装备类型编码，不是实例唯一编号。
- 修复前 `ManualSampleStore` 以 `code` 作为样本身份，实测会把 425 件错误归并为 41 个样本，单个样本最多包含 70 件互不相同的装备。
- 现已改为 Fribbels 优先使用稳定的 `ingameId`（兼容 `id`），并完整保留到表单、标准装备 JSON、样本 JSON/CSV 和 GUI；相同实例才允许归入不同强化快照。实际重新导入结果为 425 个独立样本、425 个快照、0 错误。
- 旧版 `identity_key=code:*` 记录无法安全拆分，加载时明确拒绝；备份后删除旧 JSON/CSV，再从原始导出重新导入。现在可按 `manual_acceptance/README.md` 先抽查字段和来源，再填写人工判断、一致性、分歧原因与备注。

### 人工验收启动顺序

1. 重新导入原始 425 件子集，先确认显示为 425 个独立样本、425 个快照、0 条错误；若存在旧版 `code:*` 记录，按提示备份后重新建库。
2. 第一轮先审核全部 8 件 `+3`，再从 `+0` 中按红/紫品质、六个部位和主要目标体系分层抽取 30 至 50 件，不需要立即审核全部 425 件。
3. 人工审核的对象是“工具建议、目标体系和关键理由是否符合实际装备价值”。每件只填写人工判断、一致性、分歧原因和备注，工具结果与 debug 由程序自动保存。
4. 单件分歧先记录，不立即修改策略。第一轮结束后按“建议方向错误、目标体系错误、门槛过严/过松、部位规则错误、来源或输入错误”分类汇总。
5. 只有同类分歧重复出现，或发现确定性规则错误时，才建立独立策略修复任务并先补回归测试。人工结论用于验收真实资源价值，不直接替代精确 DP 校准标签。

### 第一轮人工验收结果（2026-07-11）

报告：[reports/manual-acceptance-first-batch-20260711.md](reports/manual-acceptance-first-batch-20260711.md)。

- 已审核 29 件：`+0` 21 件、`+3` 8 件；全部 8 件 `+3` 已覆盖。
- 工具与人工操作完全相同 15 件；工具谨慎继续而人工继续 7 件；工具谨慎继续而人工停止 7 件。
- 修正后“一致性”为一致 15、不完全一致 7、不一致 7，矛盾记录已清零；14 件实际差异均已有分歧原因，13 件另有具体备注，首轮审核数据已完整。
- 初步分支为：基础 4/5 速赌速度、`+3` 未命中体系且低分、紫装非速度白字主属性鞋、衣服等部位受限体系。方向并不一致，禁止用全局统一门槛一次性调整。
- 当前重铸前 GS、满值转换终局 GS 和终局 75+ 概率均已写入真实样本。14 件差异中 8 件走未来可期后备、6 件走正式体系；速度赌博与 75+ 后备概率必须独立处理。
- 分歧 01 已完成并经主 agent 复核：定向 62 项、全量 166 项测试通过；3 件 `+0` 正向样本进入早期赌速度，`+3` 未跳速度样本停止，07 与 14 均未回归。
- 下一步执行 [真实样本分歧02-非速度主属性鞋任务说明.md](真实样本分歧02-非速度主属性鞋任务说明.md)，只处理仍为“工具谨慎继续、人工停止”的两个紫装鞋样本；随后再处理部位受限体系，不继续盲目扩大随机审核量。

## 真实样本管理工具（2026-07-11）

状态：已完成真实样本导入、筛选、人工记录、JSON 持久化与 CSV 自动导出；未修改任何策略、评分、轻量预测或精确 DP 规则。

- 主窗口新增“真实样本管理”入口，打开独立对话框，不改变现有主分栏。
- 支持单件、`items` / `gears` / `equipment` 集合和已保存建议结果包；逐条校验，单条非法输入不会阻止同批合法样本导入。
- JSON 为 `manual_acceptance/real_sample_records.json`，CSV 为自动生成的 `manual_acceptance/real_sample_records.csv`；有实例编号的装备按同一实例保存不同强化快照，`code` 仅保留为类型编号；无实例编号时仅对完全相同状态去重。
- 可筛选 `+0/+3`、全部未满强化、来源、品质、部位、套装、审核状态和工具/人工不一致；人工只需记录判断、一致性、分歧原因和备注。
- 主窗口已按实际 Fribbels `items` 样本增加只读适配，自动筛选 `85级 +0/+3 Epic/Heroic`；筛选子集可继续导入真实样本管理。其他第三方格式仍不做猜测解析。

### 性能与强化等级筛选（2026-07-11，已完成）

任务说明：[真实样本管理性能与筛选优化任务说明.md](真实样本管理性能与筛选优化任务说明.md)。

- Qt 心跳回归已先复现同步导入时心跳为 `0` 的阻塞，再改为后台串行导入/保存；所有 QWidget 更新保留在主线程，任务期间禁用冲突按钮并显示中文进度和错误。
- 实测 425 件导入为 425 个样本、425 个快照、0 错误。采样总耗时约 `854 ms`，期间 2 ms 心跳为 `60` 次；单条审核保存总耗时约 `445 ms`，期间心跳为 `26` 次。耗时仍主要来自建议生成和完整 JSON 写入，但不再阻塞窗口。
- 强化筛选已拆为 `+0/+3/+6/+9/+12/+15`，另保留“全部未满强化”和“全部”。本批数据计数为 `+0=417`、`+3=8`、其余节点为 `0`、未满强化为 `425`。
- 保存审核后仅更新受影响行；若审核状态改变导致当前筛选不再匹配，才移除该行并选择相邻项。批量表格刷新关闭重绘和信号，避免逐格重算。
- 优化仅涉及真实样本管理 GUI、存储、文档和测试；未修改策略、评分、DP、样本身份或 debug 内容。

### 当前 GS 与满值转换（2026-07-11，已完成）

任务说明：[真实样本GS与满值转换任务说明.md](真实样本GS与满值转换任务说明.md)。

- 新增当前重铸前有效 GS，并同时保留当前重铸后、原生终局和满值转换后终局 GS，全部明确为候选体系有效分。
- 85/90 级转换满值使用 Fribbels `Constants.modValues.reforged.greater` 的 Greater、100% 品质上端值；它是独立转换石表，不与 normal/rift 普通强化跳值表混用。
- 待转换副属性的终局强化次数使用小型精确分布：Epic 每个节点等概率跳既有副属性，Heroic `+12` 仅补第 4 条、不当作跳值。
- 满值转换后的 GS 进入转换候选达标概率和同层排序；`+0/+3` 三条命中、一条可转换仍为谨慎继续，不能绕过自动继续校准门槛。
- 真实样本 JSON/debug/CSV 均保存这些字段；旧记录重新导入时保留人工结论并补齐新 debug 字段。

### 终局 75+ 未来可期概率（2026-07-11，已完成）

任务说明：[终局75未来可期概率任务说明.md](终局75未来可期概率任务说明.md)。

- 正式候选继续使用第 6.4 节“体系 x 部位”低档 GS 门槛；新增的 R61 概率固定以终局重铸后全部副属性官方 GS `>=75` 计算，二者不再共用“终局达标概率”名称。
- 原生路径与当前已选合法转换路径均输出总 GS、75+ 概率和目标类型。转换值仍来自 Fribbels `Constants.modValues.reforged.greater` Greater、100% 品质上端值，绝不使用 normal/rift 强化跳值表。
- 合格正式候选时 `terminal_goal_type=formal_category`，按正式达标概率排序；没有合格正式候选才显示 `future_75_fallback`。75+ 不参与早期自动继续校准，后备路线最多谨慎继续并在 +6 复核。
- 已重算第一轮 29 件人工记录，人工判断、分歧原因和备注均保留。报告：[reports/manual-acceptance-future75-20260711.md](reports/manual-acceptance-future75-20260711.md)。

## Fribbels +0/+3 GUI 适配（2026-07-11）

状态：已完成实际导出识别、只读筛选、中文报告、集合导航和独立验收子集；未修改策略、评分或 DP。

- 原始 2467 件导出筛选为 425 件：337 件 `+0 Epic`、80 件 `+0 Heroic`、8 件 `+3 Epic`；非 `+0/+3` 跳过 2035 件，剩余非 85 级跳过 7 件。
- GUI 导入后自动判断首件并显示 `1 / 425`；上一件/下一件切换不改变窗口与 splitter 尺寸。
- Fribbels 套装、部位、主属性、副属性、强化次数和 `raw.code` 已转换为现有表单字段；逆袭、夹攻等本次出现的值在 GUI/debug 中使用中文。
- 独立子集：`samples/real_acceptance_fribbels_20260610_plus0_plus3.json`。原文件只读，来源无法可靠识别时固定按 `normal_85` 导入并要求人工确认异界来源。

## 早期轻量预测与转换候选（2026-07-11）

状态：已完成策略与回归测试；自动继续校准规则仍未发布。

- 权威任务说明：[早期轻量预测与转换策略任务说明.md](早期轻量预测与转换策略任务说明.md)。
- `+0/+3` 改为逐个“体系 x 部位”候选使用第 6.4 节终局低档 GS 阈值、来源/品质跳值、85 级重铸、理论区间和终局达标概率评估；不再用统一副属性总分决定候选方向。
- 三条命中加一条可转换偏离属性保留为候选，前台仍为“谨慎继续，待 +6 精确复核”；转换满值进入转换候选的终局 GS、达标概率和同层排序，但不绕过自动继续校准门槛。
- 高优先级候选（输出、输出必爆、抗坦、纯肉、命坦）优先于双效和半肉；同层按终局达标概率排序。
- `建议结果/14.json` 已固定为“谨慎继续 / 输出候选”，`建议结果/07.json` 仍为“继续 / 多跳速度高价值路线”。
- 离线校准记录：[reports/early-lightweight-conversion-20260711.md](reports/early-lightweight-conversion-20260711.md)。当前仅完成 6 条定向精确 DP 标签，规则表保持为空，未覆盖分组固定转入 +6 精确 DP 复核。
- 主 agent 复核：策略测试 28 项、GUI 支持测试 16 项、全量测试 133 项均通过；14 的 `0.0012` 终局达标概率只参与候选排序，不会绕过“四条完整命中 + 已发布校准规则”的自动继续门槛。

### 下一阶段：校准数据与人工验收

当前阶段只扩充证据，不发布新的早期自动继续规则。

操作说明：[早期校准与人工验收操作说明.md](早期校准与人工验收操作说明.md)。

真实样本管理工具任务：[真实样本管理工具任务说明.md](真实样本管理工具任务说明.md)，当前已完成；离线精确 DP 校准可独立继续。

1. 先扩充离线精确 DP 校准：每个拟发布“`+0/+3` 四条完整命中自动继续”的具体分组，至少需要 8 条训练继续标签和独立保留集；训练集与保留集不得再出现 `6 / 0` 这种没有独立验证的切分。
2. 分组必须保持来源、品质、部位、主属性约束、体系、套装组、当前有效副属性数和可行有效副属性数；normal/rift 绝不合并。
3. 优先校准高优先级体系的代表性分组：输出/输出必爆、抗坦/纯肉/命坦；再覆盖双效和半肉。输出衣服三条合法有效副属性必须作为独立组验证。
4. 每次拟写入 `lightweight_calibration_rules.json` 的规则，必须先报告训练集、保留集、误继续数、误停止数、样本种子和覆盖范围；证据不足时继续保守复核。
5. 收集真实 `+0/+3` 人工验收样本，特别记录三条命中一条偏离、输出衣服、输出/坦克概率排序、双效/半肉竞争候选。14 当前只验证候选方向正确，不证明其 0.12% 原生终局概率值得自动继续。
6. 转换候选按满值进入转换后终局 GS、达标概率和同层排序；转换石成本和实际资源价值仍是独立分支。三条命中、一条可转换仍不进入早期自动继续。
7. 当前 GUI 已支持经实际样本验证的 Fribbels `+0/+3` 只读筛选和真实样本记录；其他第三方导出仍需先确认字段，不能复用 Fribbels 适配器猜测解析。
8. 输出（必爆）衣服在正式矩阵中没有单独公式，不发布必爆衣服自动继续规则；它应按实际属性竞争普通输出或半肉候选，而非被直接淘汰。

## 历史讨论：+0 / +3 三条命中与转换优先级（2026-07-10，已由 2026-07-11 实现记录覆盖）

状态：已完成实现与回归；具体结果见本文顶部“早期轻量预测与转换候选”及校准记录。

任务说明：[`早期轻量预测与转换策略任务说明.md`](早期轻量预测与转换策略任务说明.md)。该说明已合并此前“体系匹配、部位差异化门槛与转换优先级”指令，并以最新讨论为准：`+0 / +3` 单条未命中时仍为“谨慎继续”、转换仅为候选路径；终局 90 级 `GS` 门槛用于概率校准而非当前分数直接比较。

子 agent 交接规则：只发送“请完整阅读并执行根目录《早期轻量预测与转换策略任务说明.md》；该文档覆盖此前所有同主题指令，以其中最新规则为唯一标准。”不得将旧版指令与新指令拼接，以免恢复已废弃的 `+0 / +3 -> convert` 预期。

复现输入：`建议结果/14.json`。

当前观察：

- 该样本为 `+3` 普通 85 红装暴击套武器，副属性为攻击%、暴率、爆伤、命中。
- 当前工具给出“谨慎继续”，但目标体系选择为“半肉(通用)”。
- 输出体系实际命中攻击%、暴率、爆伤三条，命中为唯一未命中副属性；因此需要验证“体系优先级”和“转换石路径”是否被错误地退化为半肉路线。

待确认的用户规则：

1. `+0 / +3` 时，四条副属性均命中同一体系且分数达到该体系门槛，建议“继续”。
2. `+0 / +3` 时，三条副属性命中同一体系且副属性总分达到门槛，建议“谨慎继续”。
3. 仅有一条副属性未命中目标体系的装备，应保留可通过转换石修正的候选路径；不能因未形成四条完整分类而自动退化为低优先级体系。
4. 当多个转换候选同时成立时，优先级应为：输出、坦克体系优先；双效、半肉靠后。原因是双效和半肉的高分装备相对更容易取得、需求量相对更小。

已确认的补充口径：

1. “副属性总分”不得使用全局统一阈值，也不得仅以 `official_parts.substats` 单独作全局判定。阈值必须随体系、部位、主属性、可合法出现的副属性数及白字需求变化。
2. 输出衣服等受部位副属性禁用规则限制、最多只能形成三条有效副属性的情形，三条有效副属性必须是完整的正常路径，不能机械地按“四条才完整”降级。
3. `+0 / +3` 时，恰有一条未命中、且满足优先体系的转换资格，前台建议仍为“谨慎继续”；转换只作为 debug 中的候选终局路径，不得提前显示为“转换/过渡”。
4. 转换候选排序固定为：输出、坦克体系优先；双效、半肉靠后。排序只能在候选均满足各自体系/部位门槛时生效，不能用低分装备虚构转换价值。
5. 输出与坦克同属高优先级候选时，按各自终局达标概率从高到低排序；概率相同才使用稳定的既有分类顺序打破平局。

门槛证据与校准结论：

1. 《装备强化与评分规则审阅.md》第 6.4 节已经提供静态正式分类基础门槛表。表中 `GS` 是“该分类有效副属性官方分”，并按“分类 × 部位”给出高/中/低档门槛；它不是统一的副属性总分。
2. 该静态表应作为体系、部位和白字需求差异的权威基础。例如输出武器低档门槛为 `GS=70`，输出衣服低档门槛为 `GS=64`；衣服的较低门槛正是其合法有效副属性受限的体现。
3. 第 6.4 节的门槛是终局正式评分门槛，不能把 `+0 / +3` 当前 `GS` 直接拿来判继续。早期规则需要运行精确 DP/合法跳值数据，校准为“预期终局 GS、理论上界/下界或终局达标概率”的分组门槛。
4. 子 agent 不得自行编造新的统一数值。若某个“体系 × 部位”没有静态公式（表内为 `-`）或没有足够校准数据，只能给出人工复核，不能自动继续。
5. `+0 / +3` 的终局达标概率必须以“当前副属性 + 剩余强化事件 + 来源/品质跳值分布 + 85 级默认重铸”计算 90 级终局 `GS` 是否达到第 6.4 节对应格的低档门槛。禁止把当前 `GS` 按强化等级线性放大，或将 90 级门槛直接作为低强化装备的当前分数门槛。
6. 2026-07-11 最新规则已将转换结果固定为合法满值：转换候选可用满值转换后 GS 计算达标概率和同层排序；`+0 / +3` 前台仍为“谨慎继续”，不能仅凭转换满值自动继续。

本轮子 agent 任务的最低验收：

- 使用 `建议结果/14.json` 复现当前建议、基础建议、轻量预测路径、完整分类诊断和候选体系排序。
- 为每个“体系 × 部位 × 主属性约束 × 可行有效副属性数”明确门槛口径，禁止用未定义的全局分数替代。
- 先写最小失败回归测试，再实施最小修复。
- 修复后，14 在 `+3` 的建议仍为“谨慎继续”，但 debug 的首选转换候选必须为输出、且能解释输出优先于半肉；不得改变 `07.json` 的既有“继续”结果。
- 覆盖 `+0`、`+3`、四条命中、三条命中、单条未命中可转换、双效/半肉低优先级的回归样本。
- 运行相关测试和全量测试，并更新本计划与必要的分支记录文档。

主 agent 约定：每次讨论或子 agent 结论到达后，只要形成或改变了范围、假设、规则、指标、结论、状态、阻断或下一步，必须在同一轮更新本计划；即使结论是维持现状也要记录原因。每个可执行任务在下发前必须创建或更新独立的 `*任务说明.md`，聊天指令不能作为唯一任务来源；任务说明必须在本计划中链接。子 agent 返回后先回写完成项、验证、风险和下一步，再下发后续任务。每次给出明确下一任务时，最终回复必须包含固定标题 `给子 agent 的续作指令：` 和可直接发送的完整交接指令，且一次只下发最先执行的任务。完整强制规则见 [AGENTS.md](AGENTS.md)。

当前已从“策略校准 / 模拟验证”推进到“`dp_assisted` 默认策略接入阶段”。

总体节奏仍是：先稳定强化判断，再接 OCR，最后再考虑自动化。

核心结论：

- OCR 是实用化必需项，但不和评分逻辑第一阶段混在一起调。
- `R2-R58` 正式分类百里分是主目标；`R61` 未来可期只做辅助展示。
- 普通 85 红装默认策略改为 `normal_epic_dp_assisted`。
- 普通 85 紫装默认策略改为 `normal_heroic_dp_assisted`。
- 异界 85 红装继续保持 round2 推荐策略，不启用 DP 默认。
- DP 只在 `+9 / +12` 节点影响建议，`+0 / +3 / +6` 保持 round2 策略。
- 当前不需要继续扩大样本，下一步重点是 PySide6 桌面可视化操作台。
- 桌面主程序使用 PySide6；Airtest 只作为后续可选操作层，不作为主界面和决策层。
- 开发阶段保留完整调试输出；真实强化时默认只给简洁建议。

## Current Status

截至 2026-07-10：

- `normal_85 Epic`：
  - baseline cost 均值：`932.82`
  - DP cost 均值：`817.24`
  - `1M x 5 seeds` 中 `5/5` 胜出
  - 建议默认启用 `normal_epic_dp_assisted`
- `normal_85 Heroic`：
  - baseline cost 均值：`22838.96`
  - DP cost 均值：`5864.86`
  - `1M x 5 seeds` 中 `5/5` 胜出
  - 建议默认启用 `normal_heroic_dp_assisted`
- `rift_85 Epic`：
  - DP assisted 在验证中表现变差
  - 不接入默认策略，只保留人工复核工具
- 已完成验证：
  - round2 深度验证
  - route_solver / DP 精确强化树复核
  - dp_assisted `100k x 3` 与 `1M x 5` 复验
  - 可视化报告
  - 转换成本敏感性分析
  - 单元测试：`66 tests OK`
- 默认策略接入已完成：
  - 核心接入文件：`src/e7_enhance/strategy_defaults.py`
  - 核心接入文件：`src/e7_enhance/enhance_policy.py`
  - `--disable-dp-assist` 已接入 `suggest / batch`
  - 非 debug 前台保持简洁
  - 验证：`73 tests OK`

## Desktop App Direction

桌面端采用分层方案：

```text
PySide6 桌面主程序
├─ 装备 JSON 导入 / 手动编辑
├─ 后续截图 / OCR 人工确认
├─ 调用现有 suggest / enhance_policy
├─ 显示简洁强化建议
├─ debug 折叠面板
├─ 保存装备与建议结果
└─ Airtest adapter 预留
   ├─ 截图
   ├─ 模拟器窗口定位
   ├─ 画面元素识别
   └─ 人工确认后的点击
```

技术选择：

- 主 GUI：`PySide6`
- 核心算法：继续复用现有 Python 模块，不迁移、不重写。
- Airtest：后续可选 adapter，只做截图、定位、点击等操作层能力。
- 第一版不做安卓 App。
- 第一版不接 Airtest 点击。
- 第一版不接 ADB 自动化。
- 第一版不允许 OCR 或自动化结果直接触发资源消耗。

桌面版第一阶段目标：

- 不需要命令行即可完成一次装备判断。
- 支持导入装备 JSON。
- 支持手动编辑装备字段。
- 调用当前 `baili-formal-dp-v1` 策略。
- 前台只显示建议、下一检查点、目标体系、关键理由。
- debug 面板展示策略、DP、评分和命中分析。
- 预留截图 / OCR 按钮，但可以先置灰或显示“未接入”。
- 预留 Airtest adapter 接口，但不调用。

## Implementation Changes

规则来源优先级：

1. `C:\Users\orangine\Downloads\套装属性与装等计算表.xlsx`
2. `D:\VScode\E7-tools\Epic-7-tools.v1.1\e7_gear_rating.html`
3. `D:\VScode\E7-tools\Epic-7-tools.v1.1\gear2_orange_rating_updated.csv`

阶段安排：

- `v1`：用 JSON / CSV 样本验证评分、套装契合、强化止损，不接 OCR。已完成。
- `v1.1`：接入 `dp_assisted` 默认强化策略，不接 OCR，不做自动点击。已完成。
- `v1.2`：PySide6 桌面可视化操作台。已完成，进入人工验收。
- `v1.3`：真实样本 JSON 回放与人工审核。
- `v1.5`：加入 OCR 截图识别，但只读装备，必须人工确认和修正。
- `v2`：OCR 识别 + 强化后复检，每个节点重新判断。
- `v2.5`：可选 Airtest / ADB 操作辅助，每次消耗前仍需人工确认。
- `v3`：可选半自动流程，不做无确认全自动强化。

强化节点：

- 固定在 `+0 / +3 / +6 / +9 / +12 / +15` 重新计算。
- `+0 / +3 / +6` 使用 round2 策略判断。
- `+9 / +12` 对普通 85 红装和紫装启用 DP assisted 判断。
- 异界 85 红装不启用 DP assisted 默认判断。
- `+12` 是强制止损节点：
  - 核心属性没中，建议停。
  - 只剩理论期待，建议停或谨慎继续。
  - 仍命中目标体系且价值足够，才建议继续到 `+15`。

## Strategy Defaults

新增集中配置 `strategy_defaults`：

- `normal_85` + `Epic`：`normal_epic_dp_assisted`
- `normal_85` + `Heroic`：`normal_heroic_dp_assisted`
- `rift_85` + `Epic`：保持 round2 推荐策略，`enable_dp_assist=false`

新增策略版本：

- `strategy_version`: `baili-formal-dp-v1`

DP assisted 接入规则：

- 只影响 `+9 / +12`。
- 只对普通 85 红装 / 紫装默认开启。
- 对异界 85 红装默认关闭。
- 保留 `enable_dp_assist` 开关，方便回退。
- debug 必须输出：
  - baseline 建议
  - DP 建议
  - DP utility
  - 是否覆盖 baseline
  - DP 使用的 `lambda_value`
  - 转换成本口径

## Output Design

PySide6 前台布局：

- 左侧：装备输入区
  - JSON 导入
  - 手动编辑
  - 后续截图 / OCR 入口
- 中间：装备字段确认区
  - 套装
  - 部位
  - 主属性
  - 强化等级
  - 装备等级
  - 品质
  - 副属性
- 右侧：强化建议区
  - 建议
  - 下一检查点
  - 目标体系
  - 关键理由
- 底部：debug / 日志折叠面板
  - 策略版本
  - 策略名称
  - DP 详情
  - 百里分 / target_score
  - 命中分析
  - 当前输入 JSON

开发阶段保留完整内部字段：

- `official_score`
- `effective_score`
- `baili_score`
- `target_score`
- `strategy_version`
- `strategy_name`
- `enable_dp_assist`
- `dp_recommendation`
- `dp_utility`
- `baseline_recommendation`
- `dp_override_applied`
- `rating_level`
- `fit_status`
- `valid_substats`
- `invalid_substats`
- `roll_hit_analysis`
- `ocr_raw_result`

真实强化前台默认只显示：

- `建议`：继续 / 停止 / 谨慎继续
- `下一检查点`：例如强化到 `+9` 后复检
- `目标体系`：例如一速、输出、抗坦、半肉等
- `关键理由`：1-3 条，不展示完整评分构成

调试详情放到详情面板或日志，不作为默认界面。

默认前台不展示完整 DP 计算过程，只在详情 / 日志中展示。

## Data Interfaces

统一装备输入结构：

```json
{
  "set": "Speed",
  "slot": "Weapon",
  "mainStat": { "type": "Attack", "value": 525 },
  "enhance": 12,
  "level": 85,
  "rank": "Epic",
  "substats": [
    { "type": "Speed", "value": 12 },
    { "type": "CritChance", "value": 8 },
    { "type": "CritDamage", "value": 14 },
    { "type": "AttackPercent", "value": 12 }
  ],
  "rollHistory": []
}
```

统一判断输出结构分两层：

```json
{
  "summary": {
    "recommendation": "stop | continue | cautious_continue | keep | convert | uncertain",
    "next_check_at": 12,
    "target_profile": "一速",
    "reasons": ["速度命中不足", "+12 后继续收益偏低"]
  },
  "debug": {
    "official_score": 0,
    "effective_score": 0,
    "baili_score": 0,
    "target_score": 0,
    "strategy_version": "baili-formal-dp-v1",
    "strategy_name": "normal_epic_dp_assisted",
    "enable_dp_assist": true,
    "baseline_recommendation": "",
    "dp_recommendation": "",
    "dp_utility": 0,
    "dp_override_applied": false,
    "rating_level": "",
    "fit_status": "",
    "valid_substats": [],
    "invalid_substats": [],
    "roll_hit_analysis": []
  }
}
```

没有 `rollHistory` 时按当前总值保守判断；有强化历史时额外分析每次强化是否命中有效属性。

## Test Plan

已完成验证：

- 用 `gear2_orange_rating_updated.csv` 做回归样本。
- 对比旧工具的官方分、百里分、评级、保留体系。
- 覆盖 `+0 / +3 / +6 / +9 / +12 / +15` 判断。
- 专门测试 `+12` 止损场景。
- round2 普通红装 / 紫装 / 异界深度验证。
- DP route_solver 复核。
- dp_assisted `100k x 3` 与 `1M x 5` 复验。

v1.1 默认策略接入验证：

- `strategy_defaults` 路由正确。
- `normal_85 Epic` 默认使用 `normal_epic_dp_assisted`。
- `normal_85 Heroic` 默认使用 `normal_heroic_dp_assisted`。
- `rift_85 Epic` 默认不启用 DP assisted。
- DP 只在 `+9 / +12` 影响建议。
- `+0 / +3 / +6` 保持 round2 策略。
- `R61` 不进入正式收益和 DP utility。
- 前台输出保持简洁。
- debug 输出 DP 字段。
- 运行少量 smoke 样本，不再跑 `1M` 大样本。
- 单元测试通过。

v1.2 PySide6 桌面验证：

- 能启动桌面主窗口。
- 能导入示例装备 JSON。
- 能手动编辑装备字段。
- 能调用现有 `suggest / enhance_policy`。
- 能显示简洁建议。
- 能展开 debug 面板查看 DP 字段。
- 能保存当前装备 JSON 和建议结果。
- 截图 / OCR 入口存在但不触发真实识别也可接受。
- Airtest adapter 入口存在但第一版不调用。
- 不需要命令行即可完成一次判断。
- 不接 OCR / ADB / Airtest 点击。
- 运行相关单元测试和一次 GUI smoke。

v1.5 验证：

- 用手动截图测试 OCR。
- OCR 结果必须可人工确认和修正。
- 修正后的结构必须复用 v1 评分逻辑。

v2 验证：

- 模拟完整强化流程：识别、确认、建议、强化后复检、再次止损。
- 验证 OCR 错误不会直接触发资源消耗。
- 验证默认前台输出足够简洁，调试字段仍可追踪问题。

## Assumptions

- 第一版不做 ADB 自动点击。
- OCR 放在 `v1.5`，不是取消。
- 当前不需要继续扩大模拟样本。
- 粗略 Score 只作为参考，不作为最终决策。
- 最终判断以 `R2-R58` 正式分类百里分、套装、部位、主属性、副属性有效性、强化命中方向、DP 复核共同决定。
- `R61` 未来可期只作为辅助展示和人工参考。
- 转换石成本已做敏感性分析，但实际玩家价值仍需校准。
- 真实掉落模型尚未按玩家实际掉落分布校正。
- 紫装 DP 提升明显，但仍需要真实使用观察。
- 异界 85 红装暂不接入 DP 默认策略。
- 当前阶段使用 PySide6 做桌面主程序。
- Airtest 可以后续接入，但只作为操作层，不作为主 GUI 和策略层。
- OCR / ADB / Airtest 点击 / 真实点击仍不进入当前阶段。
- 所有计划和实现文件放在 `C:\Users\orangine\Documents\第七史诗强化装备脚本`。

## Remaining Risks

- 真实掉落模型未校正。
- 转换石成本仍需按玩家实际价值校准。
- 紫装成功率低，模拟提升明显但需要真实使用观察。
- 异界 85 红装策略保留 round2 推荐，DP 只作为人工复核工具。
- PySide6 桌面 GUI 已实现，当前处于人工验收阶段。
- OCR 未接入。
- Airtest adapter 未接入。
- ADB 和真实自动点击未接入。

OCR 字段命名审阅：[OCR字段命名与规范化说明.md](OCR字段命名与规范化说明.md)。当前内部英文键保持稳定；未来必须以真实截图/导出语料建立独立 OCR 规范化层，不能把现有 JSON/Fribbels 英文别名当作游戏画面权威词表。

## Plan Maintenance

- 后续凡是已经验证通过并决定接入的项目，都必须同步更新本计划文档。
- 计划状态必须区分：已验证、当前阶段、下一步、剩余风险。
- 不再把已收口的模拟验证任务作为默认下一步重复执行。

## 2026-07-11 真实样本分歧 01：早期赌速度

- 已完成：为 `+0/+3` 非鞋速度副属性新增独立早期赌速度路线。红装速度 `>=5`、紫装速度 `>=4` 时，+0 继续至 +3；+3 以真实速度 `rolls` 判定是否命中，命中继续至 +6，未命中退出该路线。
- 已完成：策略 debug 与中文 GUI debug 展示路线资格、部位资格、品质阈值、当前速度、速度跳数、+3 命中、结束原因和下一检查点。
- 已验证：07 保持“多跳速度高价值路线 / 继续”，14 保持“候选体系待 +6 精确复核 / 谨慎继续”。
- 未合并：正式体系 GS 门槛、75+ 未来可期概率、重铸与转换满值计算、普通/异界跳值表、白字主属性鞋和部位受限体系。
- 剩余人工验收：红装低于速度 5、其他紫装阈值，以及非速度鞋“停止”分歧必须由独立的鞋子评分规则任务处理，不能借非鞋赌速度路线覆盖。

### 当前下一步（2026-07-11）

- 分歧 01 的早期赌速度分支已收口，报告为 [reports/manual-acceptance-early-speed-gamble-20260711.md](reports/manual-acceptance-early-speed-gamble-20260711.md)。
- 两个鞋子反例仍输出“谨慎继续”，尚未达到人工 `stop`，因此不能计为第一轮全部分歧已修复。
- 下一任务为 [真实样本分歧02-非速度主属性鞋任务说明.md](真实样本分歧02-非速度主属性鞋任务说明.md)；不得借该任务调整早期赌速度、正式体系门槛或 75+ 概率算法。
- 会话 `019f5023-60e2-7c33-98c5-752649bacc2f` 已收到部分“资源成本基线与 DP 审核”规则。已确认当前资源模型存在实际偏差，需要独立修复并重新校准；不得混入非速度鞋策略任务。

### 资源成本基线与 DP 审核（2026-07-11）

完整任务：[资源成本基线与DP审核任务说明.md](资源成本基线与DP审核任务说明.md)。

- 当前代码默认资源产出为 `72,450` 金币和 `200` 强化经验/8体力；权威日常基线为 `58,889.177` 金币和 `1,161.1` 基础强化经验/8体力，两者均不一致。
- [历史口径，已被 2026-07-25 后文覆盖] 当前没有计算普通 89%、Good 5.5%、Great 5.5% 的互斥期望，也没有应用宠物强化经验 `+6.6%`；旧的含宠物单一倍率曾记为 `1.15395`。现行修复要求先取 `floor(基础经验 * 1.166)`，再叠加随机层 `1.0825`。
- 强化粉末最小基础经验单位为 `100`；Good/Great 对一次投入的整批材料判定，溢出经验返还为 100 经验粉末。金币与实际使用的材料绑定，正式模型必须按材料组合计算，不能用单一倍率替代逐批取整和材料成本。
- 当前默认红装获取来源为 `rift_new_1_32=13.64` 体力，和日常常用“一刀龟裂 Buff 约 85 体力/红装”不一致。
- 当前精确 DP 与校准策略的转换成本默认为 `0`，且接口把它直接当作体力；权威值为 `100,000` 金币，必须先通过统一资源模型换算并进入终局转换 utility。
- 出售回收已经进入模拟总成本与 DP 的继续边际成本，但当前紫装仍通过红装回收乘 `0.95` 推算，并沿用红装强化需求；与权威紫装累计成本和回收表不符。
- DP 的 `lambda` 来自旧资源模型生成的 round2 成本报告。修正资源常量后必须重跑相关模拟/校准并更新推荐值，不能只改 `resource_model.py` 后继续使用旧 `cost_per_baili_score`。
- 当前只完成只读审查，未修改代码。资源修复必须独立执行，并在修改资源模型后重跑相关模拟/校准；未确认的紫装获取体力和红装新表不得猜测。
- 资源任务口径已冻结：最新截图逐级经验为权威，旧紫装累计表作废；默认只使用 `100经验/1,600金币` 的强化粉末，“强化石优先、粉末补尾差”仅为默认关闭的可选预设。可以下发 [资源成本基线与DP审核任务说明.md](资源成本基线与DP审核任务说明.md) 执行。

### 资源成本基线与 DP 审核实施结果（2026-07-11）

- [历史口径，倍率部分已被 2026-07-25 后文覆盖] 已完成红/紫逐级经验、紫装独立出售回收、100 经验/1,600 金币粉末模型和 100,000 金币转换成本；Good/Great/页面基础/宠物倍率正在按分层规则修复。
- 已完成：默认资源来源改为圣女 3-7 与一刀龟裂 Buff 85 体力红装；金币与材料经验体力等价仍取瓶颈。
- 已完成：精确 DP 的继续边际成本按装备品质选择独立资源表，转换成本按金币统一折算；不再使用无单位转换体力常量。
- 已完成：普通红/异界红重跑后发布 `792.9 / 0.00126119` 与 `328.5 / 0.00304414`；旧 `932.82`、`22,838.96`、`411.68` 及其 lambda 已失效。
- 已完成：紫装胚子获取体力无权威数据，明确标记未确认；`normal_heroic_dp_assisted` 保留既有接口以避免影响早期轻量预测，但其 `280.3` 仅为强化路径暂定值，获得可靠掉率后必须重校准。
- 报告：[reports/resource-baseline-dp-20260711.md](reports/resource-baseline-dp-20260711.md)。

### 装备来源公开数据补查（2026-07-11）

- 已完成：审阅用户提供的龟裂/讨伐收益文章及 11 个 Reddit 数据链接，建立 [装备来源收益公开数据审阅.md](装备来源收益公开数据审阅.md)。
- 已确认：官方打铁品质概率为 Rare 35%、Heroic 53%、Epic 12%；第一季龟裂 1000 次样本将 Epic 比例修正为接近 20%，但样本中的宠物亲密度和控制等级发生变化。
- 用户已确认：公开资料难以进一步完整统计，允许将默认一刀龟裂 Heroic `23.81` 体力/件、讨伐 Buff 打铁 Heroic `65.69` 体力/件作为来源明确的 `estimated` 工程基线；两者禁止平均或跨来源复用。
- 保持不变：日常圣女 3-7 金币/强化经验基线和一刀龟裂 Buff 约 85 体力/Epic。后续资源模型任务需要用 `23.81` 替换 Heroic 未确认/猜测胚子成本并重跑校准，但主 agent 本轮不修改代码。
- 新增待审：通用装备强化经验（武器/头盔/衣服/鞋子）与饰品强化经验（项链/戒指）日常获得量约为 `13:5`。按服务部位数归一化，饰品强化材料暂定相对稀缺系数 `1.3`，只影响强化经验机会成本，不影响金币、标称经验或终局价值。
- 实现前必须确认 `1,161.1` 基础强化经验/8体力是两类材料合计还是仅通用材料；未确认前只使用 `1.3` 相对系数，不按 `13/18`、`5/18` 拆绝对产出。
- 后续可继续收集第二季原图提升证据等级，但不再作为发布工程基线的前置阻塞项。
- 来源宠物副产品权威修正：用户确认现有讨伐、异界之隙、维度裂缝装备获取计算已经考虑宠物额外装备，后续不得重复加入装备收益。
- 下级强化石规则：讨伐与异界之隙每次结算 `0.11` 概率额外 1 个，维度裂缝每次结算 `0.20` 概率额外 1 个；按下级强化石 `1,500` 基础经验，分别为 `165 / 165 / 300` 期望基础经验/次。
- 下级强化石当前仍未显式进入来源资源产出、强化材料池或 DP utility，属于待实现偏差；使用时仍支付 `14,400` 金币/个，Good/Great/宠物强化倍率只在使用批次应用。
- DP 影响边界：来源胚子常量主要进入模拟总成本和重跑后的 `lambda`；当前精确 DP 的节点边际强化成本只读取统一强化资源表，不会直接获得来源宠物下级强化石收益。
- 体力信用确认：讨伐/异界之隙/维度裂缝分别为 `20 / 160 / 40` 体力/次。按 `1,161.1/8` 基础经验近似，强化石抵扣 `1.13685 / 1.13685 / 2.06701` 体力，来源调整系数为 `0.943157 / 0.992895 / 0.948325`。
- 初步净胚子估算：维度裂缝 Epic `85 → 80.61`、Heroic `23.81 → 22.58`；讨伐 Buff 打铁 Heroic `65.69 → 61.96`。异界之隙具体旧常量必须先确认来源 ID 和毛/净口径，再统一乘 `0.992895`。
- 实现要求：用下级强化石替代粉末的真实材料组合计算最终信用，计入 `14,400` 金币/个的使用成本；副产品只能在胚子净成本或强化材料成本一处抵扣，随后必须重跑资源模拟、`cost_per_baili_score` 和 `lambda`。

### 来源副产品、紫装胚子与材料池实施结果（2026-07-11）

- 已完成：来源从单一体力浮点改为可审计元数据。`riftslash_20_buff` 明确为维度裂缝一刀 Buff；新增 `hunt_buff_craft_heroic` 和 `rift_collapse` 分别承载已确认的讨伐 Buff 打铁、异界之隙规则。历史 `rift_new_*`、`rift_hunt_*`、`riftslash`、`hunt_*` 不猜测映射，保留兼容但不加副产品。
- 已完成：宠物额外装备保持“已包含”标记，不重复增加；下级强化石按来源概率记为胚子净成本信用，并比较等经验粉末与 14,400 金币下级石的统一资源等价，后续强化节点不再抵扣同一批石头。
- 已完成：维度裂缝 Epic/Heroic 的 85/23.81 毛成本和讨伐 Buff 打铁 Heroic 的 65.69 毛成本均带来源与状态标签。正式净成本分别为 83.7、22.5、65.0；Heroic 估算状态仍保留。
- 已完成：项链/戒指强化材料机会成本为通用部位的 1.3 倍，已进入模拟、校准和精确 DP 的下一节点成本；不改变标称经验、材料数量、强化金币、Good/Great、宠物倍率、GS、终局价值或跳值。
- 已完成重校准：`normal_85 Epic=836.4 / 0.00119560`，`rift_85 Epic=324.7 / 0.00307977`，`normal_85 Heroic=8648.1 / 0.00011563`。已替换 `DP_ASSIST_CONFIGS`；紫装不再是 upgrade-only 暂定值，但仍依赖维度裂缝 Heroic 毛成本的 estimated 假设。
- 报告已更新：[reports/resource-baseline-dp-20260711.md](reports/resource-baseline-dp-20260711.md)。

### 资源模型剩余待确认项处置（2026-07-11）

- `23.81` 维度裂缝 Heroic 毛成本：按用户确认继续作为带 `estimated` 标签的默认工程基线，不阻塞当前使用；不得改写为 `confirmed`，也不得跨来源复用。后续仅在取得可追溯原始统计或更可靠公开样本时替换。发布资源策略前需做该参数的敏感性检查，确认合理波动不会大面积改变建议。
- 异界之隙装备毛成本：当前只确认单次 160 体力和宠物下级强化石概率，尚不足以得到“每件候选装备”的净成本。保持仅备案，不进入默认胚子成本、来源校准或 DP；取得结算次数、实际装备产量、品质及筛选口径后再单独校准。
- 历史来源 ID：`rift_new_*`、`rift_hunt_*`、`riftslash`、`hunt_*` 继续只作旧 JSON 兼容，不注入新副产品、不映射到新来源。只有 GUI/旧导出样本、旧配置说明或原始实现证据能唯一对应实际玩法时，才新增映射回归测试并启用。
- 校准合理性待审：`normal_85 Heroic=8648.1 / lambda 0.00011563` 与 Epic 数量级差异很大。全量测试通过不能代替数值合理性验证；在把该值用于稳定日常建议前，需只读核对模拟样本量、随机种子、终局合格样本数、分母定义、胚子成本记账次数和置信区间，并与修复前结果及至少一次独立种子重跑对照。
- 处置顺序：先审计 Heroic 重校准异常；若合理则保留并记录证据，若不合理则由独立子任务按“失败测试 -> 最小修复 -> 重校准 -> 全量测试”处理。其余三项不会阻塞当前普通/维度裂缝资源模型，暂不为缺失数据编造默认值。
- 下一任务：执行 `normal_85 Heroic` 重校准合理性只读审计；不得修改代码、配置或覆盖当前校准产物。审计报告确认根因和数值可信度后，再决定是否建立独立修复任务。
- 强化材料权威口径修正：日常强化时，下级强化石与经验粉末按“提供的基础强化经验量 `1:1`”混合，不是材料个数 `1:1`。因此 1 个下级强化石提供 1,500 基础经验时，应配 15 份粉末提供另外 1,500 基础经验；该完整组合共 3,000 基础经验、38,400 金币。Good/Great 对整批材料判定，宠物经验加成作用于最终获得经验，三者均不降低金币。Heroic 校准审计必须检查现实现是否仍把两者当作二选一替代材料，并以经验贡献各 50% 的口径做敏感性核对；本轮审计不得直接修改默认配置。

### normal_85 Heroic 资源重校准审计结果（2026-07-11）

- 已完成只读审计，报告：[reports/heroic-resource-calibration-audit-20260711.md](reports/heroic-resource-calibration-audit-20260711.md)。`8648.1` 可被同参数精确复现：seed `20260711` 的分子/分母为 `233,497.45 / 27.0`；`lambda=1/8648.1=0.0001156323`，发布值取八位小数。
- 审计结论为 **B. 校准噪声**：三个独立 10,000 样本 seed 得到 `8648.05 / 8729.08 / 15768.69`，每个 seed 只有 6 个非零终局样本（0.06%），总体力稳定而终局百里分分母波动。当前 `0.00011563` 不得继续作为稳定默认 Heroic DP 常量。
- 未发现 Heroic `23.81` 毛胚子与 `22.538682` 净胚子重复记账、品质概率重复折算、金币/经验体力相加、出售回收符号错误或转换石遗漏。旧 `280.3` 是不含胚子成本的 upgrade-only 暂定口径；以现行轨迹改为胚子 0 得 `300.39`，不能与新值直接判定回归。
- 发现独立资源口径偏差：当前材料函数仍全粉末，下级石仅作为“替代粉末”的来源信用，未实现用户确认的按基础经验 50/50 混合。临时同轨迹核对影响仅 `8648.05 -> 8661.12`（+0.15%），不是十倍差异主因，但修复与重校准前也不允许重新发布 Heroic lambda。
- 下一独立任务：先补 50/50 混合材料、离散单位、整批 Good/Great、溢出粉末和来源额外下级石单次记账的失败测试；最小修改限定资源模型及直接调用者。随后用多个独立 seed、合并分子/分母和足够非零终局样本重跑 Heroic 校准，再决定是否更新 `DP_ASSIST_CONFIGS`。
- 下一任务实施口径：50/50 按大量日常强化的“基础经验贡献长期占比”处理，不要求每个单独强化节点强制凑成 1 个下级石 + 15 份粉末；否则低强化节点会因 3,000 经验组合单位产生不合理溢出。节点仍按 100 基础经验粒度处理，材料机会成本按经验贡献各 50% 分摊；下级石 1,500 经验离散性在累计材料池/期望用量中处理，来源赠送石只能抵扣石头侧一次。
- Heroic 重校准发布闸门：按独立 seed 的固定批次累计，正式估计必须使用 `sum(total_stamina) / sum(terminal_baili_score)`，不得平均各 seed 的比值。至少累计 200 个非零终局样本，并要求比值的 95% 置信区间相对半宽不超过 10%、后半样本估计与全量估计偏差不超过 10%；未达标则继续采样或保持 Heroic 资源 DP 未发布，不得写入新的单点常量。
- 当前 `normal_85 Heroic lambda=0.00011563` 已判定不可作为稳定默认值。下一实现任务必须提供明确的未校准降级行为；不得回退使用不同口径的旧 `0.00356761`，也不得在新校准未通过发布闸门时继续沿用当前值。

### 50/50 强化材料修复与重校准（2026-07-12，已完成）

- 报告：[reports/resource-material-mix-recalibration-20260712.md](reports/resource-material-mix-recalibration-20260712.md)，原始多 seed 汇总：[reports/resource-material-mix-recalibration-20260712.json](reports/resource-material-mix-recalibration-20260712.json)。
- 资源模型已改为按基础经验贡献粉末/下级石各 `50%`：总输入先按 `100` 经验粒度取整，单节点允许期望下级石分数数量，长期经验贡献保持 1:1；Good/Great/宠物只影响输入经验与粉末返还，不降低金币。
- 来源赠送下级石仅在胚子端抵扣石头经验机会成本一次；石头使用金币仍在强化节点扣除。项链/戒指 `1.3` 仍仅作用于材料机会成本。
- 30,000 样本合并发布：`normal_85 Epic=799.2 / lambda=0.0012512513`，`rift_85 Epic=332.6 / lambda=0.0030066146`；均通过 95% 相对半宽与后半样本稳定性门槛。
- `normal_85 Heroic` 合并估计 `9865.2432937 / lambda=0.0001013660`，但仅 18 个非零终局、95% 相对半宽 41.86%，未发布。默认回退 `baili_marginal_low`，`+0/+3` 轻量预测保留，`+6` 后不调用精确 DP；debug 明示未发布原因。
- 下一步仅在 Heroic 收集到至少 200 个非零终局且置信区间达标后，建立独立发布任务；不得恢复 `8648.1` 或 `0.00356761`。
- 这里的“至少 200 个非零终局”指模拟校准产生的有效终局，不要求收集 200 件真实 Heroic 装备。按当前约 0.06% 有效率，预计至少需要约 33 万次模拟，并继续采样直至置信区间和前后半稳定性同时达标。
- 真实 Heroic 仅用于人工验收未校准降级与建议方向。现有 80 件 `+0 Heroic` 足够按部位、主属性、三条命中情况抽取 12–20 件；重点确认基础策略继续工作、未调用无效 lambda、debug/GUI 中文说明清楚，不用等待更多真实紫装。
- 下一子任务分两路：先用可续跑的 Heroic-only 多 seed 批次扩展模拟并做 `23.81` 毛成本 ±20% 敏感性检查；同时对上述 12–20 件真实 Heroic 保存人工验收记录。只有模拟发布闸门与估算成本敏感性均通过，才建立 Heroic lambda 发布任务。
- 扩展校准结论：45 个 seed、450,000 条模拟得到 206 个非零终局，合并候选 `cost=12834.5324 / lambda=0.000077914798`；非零数和后半稳定性通过，但 95% 相对半宽仍为 39.32%，不得发布。`23.81` 毛成本 ±20% 使候选 lambda 变化 `+26.90% / -17.49%`，输入估算不确定性同样超过发布容忍度。
- 主 agent 处置：停止继续盲目扩样，将 Heroic 资源 DP 标记为“数据不足，延期发布”，保持基础策略回退。按理想 `1/sqrt(N)` 粗估，达到 10% 相对半宽可能需要约 696 万条模拟，且仍无法消除 `23.81 estimated` 的来源误差；只有取得更可靠 Heroic 胚子成本或决定采用有正确权重的方差缩减方法时才重启该分支。
- 人工验收待办：现有 16 件子集已自动确认全部触发未发布回退，但这只验证机制。用户仍需逐件判断 14 件“谨慎继续”和 2 件“轻量继续”的方向、目标体系和理由是否合理；重点核对 2 件继续的触发依据，以及 14 件中是否存在应停止或应直接继续的样本。人工结论完成前不修改策略。
- 人工验收阻塞：Heroic 子集导入真实样本管理器后无法与旧数据区分。现有记录虽有单一 `source_file`，但重复装备导入时直接跳过，不会保存“同一样本属于新的验收子集”关系；因此必须新增独立于装备玩法来源的“验收批次/样本来源”多值元数据和动态筛选。
- 分类边界：`itemSource=normal_85/rift_85` 继续表示游戏内装备来源；原始数据集表示装备最初来自哪个 Fribbels/JSON 文件；验收批次表示该装备被选入哪个审核任务。一件装备可属于多个验收批次，不能通过覆盖单一来源字段实现。
- 下一子任务：测试先行修复真实样本管理器的批次元数据、重复导入追加批次成员关系、中文批次筛选/表格标识/详情展示和 CSV 导出；安全迁移旧记录并重新导入 Heroic 子集。当前 `acceptance_subset` 中文元数据已出现问号，需重新生成可读的批次名称、用途和标签。不得修改装备策略。
- Heroic 16 件人工审核已完成：工具为 14 件“谨慎继续”与 2 件“继续”；人工为 10 件“停止”、4 件“谨慎继续”、2 件“继续”。2 件继续与 4 件谨慎继续一致，10 件全部是工具谨慎继续但人工停止，属于单方向过度保留。该子集为边界覆盖抽样，不能把 62.5% 直接外推到总体，但已足以证明 Heroic 未校准基础回退过宽，存在强化资源浪费风险。
- 下一策略研究：以现有 80 件真实 `+0 Heroic` 为起点，保留已审核 16 件作为独立人工验收集，其余样本用于构造真实起始分布；按 normal_85 Heroic 跳值规则模拟 +3 至 +15。早期决策只比较从当前节点开始的增量强化成本、停止回收和高价值终局概率，胚子获取成本视为沉没成本，不使用未发布 Heroic lambda。
- 本轮只输出 +0/+3 候选策略的成本-收益 Pareto 表和建议门槛，不直接改策略。至少比较当前基础回退、速度硬路线、体系命中/部位合法有效词条、当前有效 GS、白字主属性与转换候选等组合；报告每件平均增量体力、终局正式体系/75+/速度成功率、成功召回率、每个成功的增量成本，以及对 16 件人工集的混淆矩阵。用户确认取舍后再建立实现任务。
- Heroic 离线研究已完成但证据不足发布：当前基础回退 `+0` 通过率 100%、平均增量 103.10 体力/件，仅保留全继续 69.06% 的高价值终局；5%/10% 概率门槛虽节省资源，但在 16 件人工留出集仍误停 2/3 件，因此继续保持现状，不建立正式修改任务。
- 下一研究优先级转向 Epic。现有普通红装为 337 件 `+0`（323 待审核、14 已审核）和 8 件已审核 `+3`，具备比 Heroic 更充足的真实起始分布和留出样本；先做 normal_85 Epic 的 +0/+3 分层 Pareto 研究。
- 异界红装稀有度暂按用户表述理解为“异界数量约为普通红装的 1/40–1/60”。稀有度不能直接乘入单件终局价值，也不能覆盖属性合法性和体系匹配；同状态下 rift_85 因独立高跳值表可采用更高召回/多给一个节点的候选策略，但必须用 rift_85 分布单独模拟。当前没有真实异界样本，故只允许生成候选策略和与 normal_85 的同状态差异，不得直接发布异界早期门槛。
- 红装资源优先级：先按部位/主属性合法性和体系匹配排除错误胚子，再按当前 GS、速度路线、终局达标概率和增量成本决定投入；高分普通红装与合法异界红装可提高继续预算，低分或属性方向错误的异界红装仍应停止。胚子已获得后的 +0/+3 决策不把 40–60 倍稀有度作为沉没成本重复计入。
- 红装早期速度权威修正：normal_85 Epic 与 rift_85 Epic 的 `+0` 非鞋装备，只要初始速度 `>=2` 就进入独立赌速度硬路线并继续到 `+3`；此前 `>=5` 门槛作废。`+3` 若命中速度则继续到 `+6`，未命中时退出速度保护，再交由普通体系/评分判断，不等于无条件停止。速度硬路线不得被 GS、终局概率或资源 Pareto 门槛覆盖。Heroic `+0` 非鞋速度 `>=4` 的既有规则保持不变。
- Epic 离线研究必须把 `速度>=2` 作为固定基线而非候选阈值，并分别报告这条硬路线的资源消耗、+3 命中率和高速度终局召回；任何候选省资源策略都不得停止该类 +0 胚子。正式代码若仍使用 `>=5`，需先建立独立最小回归修复任务，不能借 Pareto 分析静默修改。
- Epic 速度硬路线修复后复核：当前 22 件已审核 Epic 的严格一致为 13/22（59.1%）；`+0` 为 9/14，另有 4 件人工要求直接继续、1 件人工停止；`+3` 为 4/8，另外 4 件人工停止。工具对这 22 件全部输出“谨慎继续”，说明主要剩余问题是非速度 +0/+3 缺少继续/停止分层，不是速度路线。
- 历史 `reports/calibration-strategy-summary.md` 虽有每组 100,000 样本的五类预设策略模拟，但其目标是总体资源/终局策略排序，不包含当前真实 +0/+3 Epic 起始分布、非速度体系分层和人工 holdout 混淆矩阵，不能作为非速度早期策略已验收的证据。
- 下一子任务按既定 Epic Pareto 研究执行，但必须先冻结速度 `>=2` 硬路线，将速度样本从非速度门槛训练与混淆矩阵中单独报告；重点搜索能把当前单一“谨慎继续”拆成继续/谨慎继续/停止的体系×部位×主属性×有效 GS/终局概率规则。本轮仍只研究不改正式策略。
- Epic 非速度离线研究已完成，但主 agent 复核发现成本指标存在关键简化：工具只缓存 `+0/+3/+15`，`+3` 的“谨慎继续”和“继续”都会直接走到 `+15`，没有执行 +6/+9/+12 的实际后续策略，因此“当前全谨慎”和“全部继续”均为 214.06 体力。“节省体力”只是相对该简化基线的增量强化体力差，不是实际端到端策略节省。
- 报告的“每个高价值终局体力”定义为 `所有模拟路径的增量强化净体力总和 / 二元高价值终局数量`；不包含已获得胚子成本。F 的 1218.48 约等于 `38.98 / 3.20%`。该指标不区分 75 分与更高分终局，也不反映每点终局 GS 的体力效率。
- 当前 K 分类策略确有输出/坦克/双效/半肉的概率门槛，但只是分类级固定概率（如输出 12%/3.5%、纯肉 8%/2.5%），没有结合第 6.4 节分类×部位 GS 门槛、主属性限制和当前分数；F/L 又是全局 GS 30/18、34/22。因此用户提出的“分类结合门槛 + 分数效率”尚未被完整研究。
- 下一研究必须先修正离线工具口径：逐节点执行 +6/+9/+12/+15 正式后续策略，分别报告早期下一节点成本和实际端到端成本；新增 `期望终局价值/GS 增量 ÷ 增量体力`、`每点期望终局 GS 所需体力`。强化决策主指标继续排除沉没的胚子成本；另列全链路敏感性指标，normal Epic 仅使用明确 gear_source 成本，rift 获取毛成本未确认前不得给出全链路定值。
- 修正后指标解释：例如“每个高价值终局体力 580.20”是 `所有已获得胚子的后续强化净体力 / 高价值终局数量`，近似表示长期每投入 580.20 强化等价体力得到一个高价值终局；不包含刷取胚子的体力，不能解释为每 580 体力从副本获得一件高价值装备。
- 操作语义修正：现行 `summary_to_continue()` 将 `continue` 与 `cautious_continue` 都映射为继续到下一强化节点，二者在实际资源动作上相同，仅表示置信度/人工复核级别。后续自动强化策略评估必须拆为两层：`action=enhance_to_next_checkpoint/stop` 二分类，以及 `execution=auto/manual_review` 风险标签；不能再用三分类准确率直接代表自动强化动作准确率。
- 样本补充方案：先完成人工审核现有 24 件盲测批次；同时可自动生成合法分层合成状态，覆盖稀疏的分类×部位×主属性×可行词条数组合和低/中/高初始跳值。合成样本必须标记 synthetic、单独成批，只用于机制覆盖、单元格概率和单调性研究；总体资源效率仍按真实样本频率加权，合成样本不得伪装成真实分布或替代人工验收。
- 稀疏组合不建立独立硬门槛，优先研究可共享的层级规则：分类基础门槛 + 部位/主属性可行性修正 + 当前 GS/终局概率修正。只有真实盲测动作二分类与人工复核标签均达到约定标准后，才建立正式实现任务。
- DP 与离线指标口径澄清：正式资源校准的 `cost_per_baili_score=sum(total_stamina)/sum(score)` 中，`total_stamina` 包含装备获取与强化；DP 节点边只扣后续增量强化成本，但获取成本已通过发布 lambda 影响资源价值。故离线报告排除胚子成本的结果只能称“已获得装备的后续强化边际效率”，不能称完整高价值装备成本，也不能直接替代 DP utility。
- 后续报告必须同时给出两层：一是用于当前装备动作的边际强化成本；二是按明确 `gear_source` 计入所有候选胚子获取成本的全链路成本/高价值终局。`itemSource=normal_85/rift_85` 不是资源来源，不能据此猜 gear_source；normal 需按已确认来源分别出场景，rift 获取毛成本未确认时只给参数化公式/敏感性，不给固定值。
- 套装职责收缩（2026-07-12）：强化器只处理一件已经获得的装备，套装是 `Gear.set` 的正式输入字段，由 JSON/GUI 提供；未来 OCR 只填写同一字段。策略、DP 和路径模拟不得重新随机套装。
- Epic 离线研究的合成接口为 `generate_conditional_gear(set_code, rank, seed, slot=None, main_stat=None)`：`set_code` 必填；仅支持 `normal_85` 的 Heroic/Epic；左三固定主属性，右三仅从合法主属性生成；副属性遵循主副不重、彼此不重和部位禁用规则。Rare 被明确拒绝。
- 条件样本带 `synthetic=true`、`generation_mode=conditional_set`、套装、品质、部位、主属性、seed 和生成规则元数据。它们只补齐稀疏的分类×部位×主属性×可行有效词条数，用于机制覆盖、单调性和给定装备状态的后续概率；不得混入真实训练频率、22 件 holdout、24 件盲测或发布门槛。
- 不再要求 `selected_sets`，不再生成四套混合掉落，不建立套装池、套装需求权重、副本总体价值或账号刷取/体力分配规划。现有 `enhance_simulator.generate_gear()` 的随机套装仅保留为独立通用模拟能力，不是本 Epic 离线研究的输入分布。
- 核心边际强化成本排除已获得装备成本，套装获取概率不进入自动强化建议、DP 或默认资源常量。维度裂缝各品质单件净成本只作粗略来源参考；若未来研究指定套装刷取，四套等概率下可将普通单件成本约乘以 4 单列为敏感性，不得与当前单件决策口径混合。
- 真实样本按原始套装逐件模拟，条件样本按明确 `set_code` 独立展示，绝不把不同套装强制混成一个总体分布。24 件盲测仍需人工审核；合成样本不能替代人工标签。
- 条件覆盖样本共 `1,092` 条，结构为 `21 套装 × 26 个合法部位/主属性组合 × 2 品质`；Heroic/Epic 各 `546` 条，52 个品质×部位×主属性组合均覆盖且无重复编号。该数量足以验证结构遍历和合法性，但每个单元格只有 1 个初始副属性实例，不能估计初始词条组合、初始跳值或策略边界的统计分布。
- 复检分层：1,092 条全部执行自动合法性、重复和单调性检查；人工不逐条审核，只按套装、品质、六部位、右侧主属性及有效词条边界抽查 30–50 条。24 件真实盲测必须全部人工标注，优先级高于继续扩大合成样本。
- 扩样闸门：先取得 24 件盲测动作标签。若候选策略在真实盲测有改善且仍需研究稀疏边界，再只扩 Epic 条件样本，每个重要 `set_code×slot×main_stat` 单元格生成 10–20 个独立初始副属性/低中高跳值变体；这些样本仍不替代真实标签或直接发布门槛。
- 实施完成（2026-07-12）：`EARLY_SPEED_GAMBLE_MINIMUMS` 的 Epic 固定阈值已由 `5` 改为 `2`，normal_85 与 rift_85 共用该门槛且不合并跳值表。`+3` 未命中速度已移除直接停止分支，改为继续执行普通体系/评分决策并在中文 debug 中展示结果和路线；鞋子与 Heroic `>=4` 规则未改。Epic 离线研究现已按 `速度>=2` 固定基线执行。

### Epic 非速度 +0/+3 三分类 Pareto 离线研究（2026-07-12，已完成，未发布策略）

- 产物：[报告](reports/epic_non_speed_early_policy_pareto_20260712.md)、[原始指标](reports/epic_non_speed_early_policy_pareto_20260712.json)、[冻结盲测批次](samples/epic_non_speed_blind_acceptance_20260712.json) 与工具 `tools/epic_non_speed_early_policy_pareto.py`。本研究未修改 `enhance_policy`、DP 配置、资源模型、跳值表或 GUI。
- 当前低一致率的根因已确认：固定速度路线之外，现行早期非速度回退几乎把所有 Epic `+0/+3` 均归为“谨慎继续”，没有将直接继续、下一节点复核和停止三类投入分开。
- 数据冻结：人工已审核 Epic `22` 件全部为 holdout（`+0=14`、`+3=8`），不参与候选规则选择；从剩余普通红装 `+0` 状态剥离 `43` 件速度硬路线（非鞋速度 `>=2`），再固定抽取 `24` 件盲测，训练 `256` 件，四个实例 ID 集合无交叉。
- 模拟：每件训练状态 `5,000` 条公共完整轨迹，固定种子 `20260712`、`8` 进程；Epic 始终模拟五次既有副属性跳值。胚子为沉没成本，只计算当前节点后的 50/50 材料、饰品 `1.3` 系数、出售回收和满值转换 `100,000` 金币。JSON 记录了正式体系、75+、速度、转换和任一高价值终局的 Wilson 95% 区间，以及部位/主属性/体系拆分。
- 正式 normal_85 候选 Pareto 只作研究输入：省资源 `F_current_gs` 为 `38.98` 平均增量体力、保留当前基线 `56.89%` 高价值终局；平衡 `K_category` 为 `99.85` 体力、`71.87%` 召回；高召回对照仍是当前全谨慎策略。三者对 22 件 holdout 的准确率分别为 `45.45% / 45.45% / 63.64%`，不足以直接发布新规则。
- 盲测批次尚未有人工标签；在完成三分类验收并明确允许的人工继续误停与高价值召回损失前，不建立 normal_85 Epic 正式策略修改任务。rift_85 只使用 `256` 件合法量化映射状态做独立高跳值研究，没有真实异界样本，因此不得发布 rift 硬门槛；低分或属性方向错误的异界装备仍可停止。
- 24 件 Epic 非速度盲测已全部完成人工标签：人工为继续 `2`、谨慎继续 `8`、停止 `14`；当前正式回退 24 件全部谨慎继续，三分类仅 `8/24`，按实际强化/停止动作仅 `10/24`，有 `14` 件人工停止被过度强化。
- 冻结候选初步对照：`B_global_current_gs` 在新盲测三分类 `19/24`、动作 `21/24`，误强化 `0`、误停 `3`，显著优于当前全谨慎；分类概率/GS效率高召回类候选动作多为 `15/24`，主要仍有 `8` 件误强化。该对照需由子任务使用稳定实例/指纹关联重新计算后写入正式报告，不能依赖数组顺序直接发布。
- 下一研究任务：对旧 22 件 holdout、新 24 件盲测及合并 46 件分别输出三分类和动作二分类矩阵、置信区间、误强化/误停样本明细与终局价值损失；重点分析 B 的 3 件新盲测误停能否由分类完整命中、合法转换或部位受限路径救回。盲测标签现已揭示，后续若据此调参，必须先从剩余真实样本冻结新的独立验证批次。
- 人工标签定位修正：人工判断只表示当前经验、资源风险偏好和可解释性验收，不是数学效率最优真值。不得以提高人工分类准确率作为唯一训练目标，也不得把人工 stop 直接定义为模型误强化。
- 当前证据：`B_global_current_gs` 虽在新 24 件上最符合人工动作，但报告的 `GS/100体力=32.757` 低于当前全谨慎的 `34.699`，且高价值召回为 `92.76%`；省总资源不等于单位资源转化效率更高。候选选择必须同时比较期望终局价值、DP utility、增量成本和人工偏好。
- 下一主判据改为离线精确 DP/oracle：对每件已审核样本计算 `continue_utility-stop_utility`、终局正式/速度价值、增量体力和不确定性；正效用为强化、负效用为停止、效用接近零或置信区间跨零为人工复核。人工标签作为第二套对照，报告“DP建议×人工判断”分歧，不直接反向修改 DP。
- 后续候选必须分别报告：相对精确 DP 的动作一致率/效用遗憾、相对人工的动作一致率，以及总体 GS/100体力和高价值召回。只有能近似 DP 且不产生不可接受人工风险的轻量规则，才允许进入正式实现任务。

#### 分节点成本与连续终局价值复核（2026-07-12，已完成，仍未发布策略）

- 原报告口径已纠正：旧工具只缓存 `+0/+3/+15`，把 +3 的“继续/谨慎继续”都直接算为冲到 `+15`，没有执行 `+6/+9/+12` 的现行后续复核，故平均 `214.06` 体力不是实际策略成本。
- 新工具逐节点执行 `+0→+3→+6→+9→+12→+15`：+0/+3 的候选“继续/谨慎继续”只到下一节点，+6/+9/+12 调用当前正式基础后续策略；任何节点停止均按该节点出售/回收结算，合法转换只在 +15 终局路径计入一次。`A_current_review` 的实际端到端均值为 `24.06` 体力，较旧口径少 `190.00`，其中 `87.5%` 在 +6 止损。
- 每个策略现在同时输出下一节点条件成本、实际端到端体力/金币、各节点停止/到达率、终局目标体系有效 GS、全部副属性官方 GS、正式价值、速度潜力、两者最大值、满值转换价值及相对当前停止的价值增量。Pareto 不再仅按二元高价值成功率，而以体力、GS/100体力和高价值召回共同支配。
- 已将第 6.4 节低档 GS 与 `分类×部位×主属性×可行/当前有效词条` 组合为离线候选维度；输出/必爆、抗坦、纯肉、命坦、双效、半肉均保留拆分。`分类×部位` 组合最多只有少数真实训练样本，仍只作为研究，不发布独立门槛；输出衣服等三条受限路径独立记录。
- 两套资源口径已分离：强化决策效率排除已获得胚子；全链路效率仅在输入含明确 `gear_source` 时才允许计入。当前 Fribbels 样本只有 `normal_85`，不能把它当作获取来源；rift 胚子毛成本未确认，JSON 仅标记敏感性状态。盲测批次的 24 件现已附带每个冻结候选策略的预测动作。
- 重新跑完 normal/rift 独立跳值研究：每件 `5,000` 条、seed `20260712`、8 进程。候选结果、分类×部位低档 GS 表、连续 GS/体力、holdout 矩阵和盲测预测见 `reports/epic_non_speed_early_policy_pareto_20260712.md` 与同名 JSON。22 件 holdout 的最高准确率仍为当前全谨慎基线 `63.64%`，无足够证据建立正式策略修改任务。

### normal_85 Heroic 扩展校准与未发布验收（2026-07-12）

- 已完成扩展模拟，报告：[reports/heroic-resource-calibration-expanded-20260712.md](reports/heroic-resource-calibration-expanded-20260712.md)，原始合并结果：[reports/heroic-resource-calibration-expanded-20260712.json](reports/heroic-resource-calibration-expanded-20260712.json)。工具现在支持 Heroic-only、断点续跑、seed 去重与逐 seed 原子保存；普通校准和已发布 Epic 常量未修改。
- 累计 `45` 个独立 seed、`450,000` 条模拟、`206` 个非零终局。合并候选为 `12,834.5324 / 0.000077914798`，但仅作为诊断值：95% 相对半宽 `39.3209%` 未达 `<=10%`，后半偏差 `0.9652%` 合格，故总体不发布。
- `23.81` 毛胚子成本的 ±20% 同轨迹敏感性使候选 cost 为 `10,113.7351 / 15,555.3298`，lambda 相对基线为 `+26.90% / -17.49%`。来源仍为 `estimated`；即使以后统计 CI 合格，也须以临界 DP 建议复核成本敏感性后才可审议发布。
- 新增 [samples/heroic_resource_fallback_acceptance_20260712.json](samples/heroic_resource_fallback_acceptance_20260712.json)：16 件真实 `+0 Heroic` 验收子集覆盖六部位、左/右三、速度有无、固定值、非速度鞋及输出/坦克/双效/半肉候选。16/16 均显示“Heroic 资源校准未发布，已回退基础策略”，未启用 DP lambda；14 件谨慎继续、2 件轻量继续，供人工只审核退化行为和方向。
- 下一步：继续使用未挑选的新 seed 扩展 Heroic-only 统计，保留所有已有 45 个 seed；未同时满足非零数、CI、后半稳定性前不得建立 lambda 发布任务，也不得改写 `DP_ASSIST_CONFIGS`。

### 真实样本验收批次管理（2026-07-12，已完成）

- 根因：真实样本记录只有单一 `source_file`，重复快照导入会直接跳过，无法记录同一装备状态被选入新的人工验收任务。
- 存储已升级为版本 `3`：顶层 `batches` 保存批次元数据；快照保存 `acceptance_batch_ids` 与 `original_source_files`。旧记录会按原 `source_file` 建立兼容批次，且保留人工判断、备注、审核状态、样本编号和快照编号。
- GUI 已将“来源”明确为“装备来源”，新增“验收批次”动态筛选、表格列与详情展示；导入后自动切换到刚导入批次。CSV 增加原始数据来源、验收批次、批次用途和批次标签。
- [samples/heroic_resource_fallback_acceptance_20260712.json](samples/heroic_resource_fallback_acceptance_20260712.json) 已恢复可读批次元数据：`heroic_resource_fallback_20260712`、Heroic 资源回退验收（2026-07-12）。端到端验证为：先导入原始 425 件，再导入子集，快照总数仍为 425，Heroic 批次为 16 件。
- 后续人工验收可直接在真实样本管理中导入该子集，逐件复核未发布资源校准的回退提示、建议方向、目标体系和中文 debug；此流程不触及装备策略。

## Manual Acceptance Stage

当前进入人工验收阶段。

本阶段目标：
- 使用 PySide6 GUI v1 对真实装备做人工验收。
- 通过 `samples/manual_acceptance_gears.json` 录入 20-50 件真实装备。
- 使用 GUI 导入样本后逐件查看、判断、保存建议结果。
- 使用 `manual_acceptance/manual_test_records.csv` 记录工具建议与人工判断是否一致。
- 分歧先记录，不立即改核心策略；只有发现明显 bug 才修策略。

本阶段产物：
- `manual_acceptance/README.md`：人工验收流程。
- `manual_acceptance/manual_test_records.csv`：人工测试记录模板。
- `samples/manual_acceptance_gears.json`：20 件样本录入文件，可替换为真实装备。

GUI 验收关注点：
- 前台建议保持简洁：建议、下一检查点、目标体系、关键理由。
- debug 可查看：策略版本、策略名、DP 状态、DP utility、是否覆盖、百里分、目标分。
- 保存建议结果包含：原装备 JSON、完整 suggestion、summary_view、debug_view、manual_review 占位字段。

边界：
- 不接 OCR。
- 不接 Airtest。
- 不接 ADB。
- 不做自动点击。
- 不迁移评分 / DP 算法。

### Epic 非速度人工盲测后的效率主判据（2026-07-12）

- 24 件盲测的人工标签已经完成，但人工判断只用于玩家偏好、风险和可解释性验收，不代表数学效率最优。
- 不直接发布最贴近人工标签的 `B_global_current_gs`；先对旧 22 件与新 24 件共 46 件运行稳定指纹关联的 DP oracle 审计。
- 主判据为 `continue_utility - stop_utility` 与候选策略的 utility regret；人工一致率、GS/100 体力、高价值召回、误强化成本和误停价值同时作为辅助指标。
- 本轮只做离线研究，不修改正式策略。若根据 46 件调整候选，必须先冻结至少 48 件新的真实 normal_85 Epic 独立验证批次。
- 完整执行说明：[Epic非速度早期策略精确DP审计任务说明.md](Epic非速度早期策略精确DP审计任务说明.md)。

### Epic 非速度精确 DP Oracle 审计（2026-07-12，已完成，未发布策略）

- 产物：[审计报告](reports/epic_non_speed_dp_oracle_audit_20260712.md)、[逐件原始结果](reports/epic_non_speed_dp_oracle_audit_20260712.json) 与工具 `tools/epic_non_speed_dp_oracle_audit.py`。本轮仅新增离线审计、测试与文档；未修改正式强化策略、DP 常量、资源模型、跳值表或 GUI。
- 关联不依赖数组顺序：旧 holdout 的 21 件非速度样本以保存的 `instanceId` 关联；盲测导入时丢失实例 ID 的 24 件，以套装、部位、主属性、等级、品质、强化等级和排序后副属性值/跳数生成的规范化指纹唯一关联。重复指纹不猜测，直接列为异常并排除。
- 目标 46 件中，1 件为已发布 Epic 速度硬路线，按规则单列，不进入非速度门槛审计；45 件（旧 21、盲测 24）使用 normal_85 Epic 独立跳值与发布的 `799.2` 体力/百里分、`lambda=0.0012512513` 做精确离散 DP 枚举。另有 7 件同源历史审核记录不在 Epic +0/+3 任务范围，独立记录而不混入 46 件统计。
- Oracle 结果为继续 23、停止 22、数值边界复核 0。停止 utility 规范化为 0；当前装备获取与当前出售状态均为共同沉没项，不在 continue-stop 中重复扣除。继续分支计入下一节点净资源成本、后续出售回收、速度潜力、合法满值转换和一次 100,000 金币转换成本。
- 当前正式全谨慎策略动作一致率为 `51.11%`，但保持 Oracle 正效用召回 `100%`，总 regret `0.0746`。`B_global_current_gs` 虽有较高人工动作一致率 `73.33%`，却误停 7 件 Oracle 正效用状态、总 regret `12.9089`，不可因人工贴合而发布。`C_category_probability` 总 regret `0.0778`、误停 1 件、误强化 14 件，是低 regret 的研究候选，但仍未通过独立验证。
- 人工与 Oracle 对照：人工偏保守但 DP 正效用 6 件、人工偏激进但 DP 负效用 9 件、资源动作一致 30 件。人工标签保留为风险和可解释性验收，不覆盖 Oracle。
- 发布结论：不建立正式策略修改任务。若要以本 46 件结果调整任一候选，必须先从剩余真实 normal_85 Epic 冻结至少 48 件新的独立验证批次，再读取其人工标签并复跑同一审计口径。

### Epic 早期综合效能口径复核（2026-07-12，下一研究任务）

- 当前策略动作一致率只有 `51.11%`、人工复核覆盖 `100%`，说明自动化与资源动作仍很粗；但总 regret `0.0746` 且正效用召回 `100%`，不能仅凭误强化件数判断数学效能很差。
- 当前 Oracle 终局价值是正式达标 GS 与速度潜力取高，正式门槛以下按 0；现有低 regret 只对这套阈值化价值函数成立。下一步审核部位受限正式体系、满值转换实际增值、速度/GS量纲和均衡档风险分位数；75+ 及一般过渡装不进入早期自动价值，多体系匹配数量不加分。
- 下一步先冻结至少 48 件新的真实 normal_85 Epic 非速度验证样本，然后审计价值函数覆盖、建立多指标 Pareto，并基准测试 `+0/+3` 精确 DP 是否可直接用于单件 GUI 判断；只有性能不足时才蒸馏轻量规则。
- 不建立账号刷取规划，不把装备获取成本重复计入已获得单件的继续/停止，不猜测套装或账号需求权重。
- 完整执行说明：[Epic早期综合效用与可部署策略研究任务说明.md](Epic早期综合效用与可部署策略研究任务说明.md)。

#### 综合效能权威口径补充（2026-07-12）

- `total utility regret=0.0746` 是 45 件上当前动作相对逐件 Oracle 最优动作的净效用损失总和，越接近 0 越好；按当前 lambda 等价约 `59.63` 体力。它不是准确率，也不等于 `84.83` 毛误强化体力，后者未扣除误强化路径仍可能产生的终局价值。
- DP 的 `max(正式 GS, 速度潜力)` 是对每个 +15 终局分别取两条价值路线的较高者，再对强化分支求期望，不是把两种分数相加。速度量纲新增权威锚点：终局 22 速度至少等于正式 GS 第二档，同时保留最终速度与速度跳数的高速度、多跳优势。
- 75+ 未来可期不进入 `+0/+3` 自动继续价值；仅在装备已完成强化后作为人工使用剩余资源寻找过渡装的提示，偶尔使用，不得救回早期低效装备。
- 多体系匹配不额外加分。价值优先级为速度最高；纯输出与纯坦克同属第二层并按达标概率排序；半肉低于纯体系；双效低于半肉。输出与坦克互斥，纯坦克最多允许一种效果属性。
- 满值转换后的实际属性、终局 GS、目标体系和 100,000 金币成本必须进入终局 utility。
- 风险档位预留高召回、均衡、省资源三档，但下一研究只做均衡档，使用当前已确认的日常收入与强化成本。人工复核率不是游戏资源成本，不进入 DP，只作为未来自动化操作便利性指标。
- 当前报告的“终局价值/100体力”准确名称应为“后续强化终局价值/100增量体力”：分子是各强化装备的期望 `max(正式百里分, 校准后速度潜力值)` 总和，分母是从当前节点起的后续强化净体力，不直接包含装备获取成本。它不是纯百里分/100体力，也不是完整刷取效率。
- 获取成本通过包含全链路资源的发布 lambda 间接影响 DP 定价，但对已经获得的单件装备属于 continue/stop 共有沉没项，不在节点分母中重复扣除。若报告“全链路终局价值/100总投入体力”，必须在明确 `gear_source` 后把所有进入筛选的胚子（包括立即停止者）获取成本与后续强化成本共同计入，不能由 `normal_85` 猜来源。

#### 均衡档完整产出口径修正（2026-07-12）

- 下一轮策略模拟改以“全链路正式终局价值/100总投入体力”为主指标：所有进入筛选的 Epic 胚子都计获取成本，包括立即停止者；随后只给策略选择强化的装备增加强化、出售回收差额和转换成本。这样直接反映有限体力在胚子获取与强化材料之间的平衡。
- 默认研究场景明确为 `riftslash_20_buff` 维度裂缝一刀 Buff Epic：毛成本 `85.0` 体力/件，资源模型当前净成本 `83.7`，宠物额外装备已包含。样本缺少 `gear_source` 时仅作统一场景假设，不回写原始来源；其他来源只能独立敏感性报告。
- 均衡档终局分子默认只使用正式体系百里分。唯一独立例外为终局速度 `>=22` 的一速装备：22 速度至少映射为正式 GS 第二档，22 以上再保留最终速度与速度跳数优势；22 以下速度不产生独立终局价值，只能走正式体系百里分。
- 单件 continue/stop debug 仍保留后续强化边际口径，用于解释当前装备动作；它不作为策略整体性价比主指标。完整模拟与单件 debug 必须并列，禁止把胚子成本重复扣在 DP 节点边。
- 因终局计值与策略候选发生变化，全链路研究必须重新求 `cost_per_terminal_value` 和候选 lambda 稳定性；不得把现有发布 lambda 与胚子获取成本直接相加，也不得在研究阶段覆盖正式 DP 常量。

#### 维度裂缝 Epic/Heroic 联合产出修正（2026-07-12）

- 维度裂缝同一批体力同时产出红装和紫装，联合全链路模拟不得把 Epic `83.7` 与 Heroic `22.5` 按件数相加，否则同一副本体力和同一来源强化石信用会重复记账。
- 以毛 `85.0` 体力批次归一化：期望 Epic 为 `1` 件；按现有 Heroic 毛成本 `23.81 estimated`，同批期望 Heroic 为 `85/23.81≈3.57` 件。来源宠物装备已包含，来源下级强化石信用只在批次端抵扣一次，当前批次净成本按资源模型为 `83.7`。
- 联合主指标为 `100 × (Epic终局价值 + Heroic产出率×Heroic终局价值) / (批次净获取体力 + Epic强化净体力 + Heroic产出率×Heroic强化净体力)`；两种品质立即停止后的出售/经验回收也必须进入。
- 本轮只优化 Epic 策略，Heroic 固定当前未发布 lambda 的基础回退，并另做 Heroic 全停止敏感性。不得借联合模拟发布 Heroic lambda 或修改紫装策略；`23.81` 和 Heroic 产出率必须做 ±20% 敏感性。
- 报告需并列联合价值率与 Epic 在固定 Heroic 基线下的增量贡献。只有联合排序和 Heroic 敏感性下均稳定，才允许把 Epic 均衡候选送入新 48 件独立验证。

#### 联合批次 pilot 结果与下一阻塞（2026-07-12）

- 已实现联合研究层，100轨迹 smoke 为 `pilot_not_for_release`：固定 Heroic 基础回退、Heroic 全停及产出率 ±20% 时，第一名在 `B_global_current_gs` 与 `C_category_probability` 之间变化，因此当前无候选进入48件验证。
- 100轨迹只证明小样本排序不稳定，不能证明 B/C 的真实优劣。5,000轨迹单次运行超过120秒，下一工具任务必须支持分片、原子保存、断点续跑、seed去重和合并原始分子/分母；以候选成对价值率差的95%区间是否跨0作为排序稳定条件。
- 新发现成本冲突：联合工具硬编码 `83.7`，当前单品质资源接口返回约 `82.9`。必须先按40体力/次、85体力/Epic、0.20石头/次、50/50经验混合及14,400金币使用成本逐单位审计，确认来源信用是否按 `85/40=2.125` 次结算缩放以及是否重复记账。
- 在成本唯一化和排序统计稳定前，不冻结新48件验证集，不修改正式策略或DP常量。完整下一任务：[维度裂缝联合批次成本与排序稳定性复核任务说明.md](维度裂缝联合批次成本与排序稳定性复核任务说明.md)。
- 当前联合报告仍拼接旧 Pareto 的边际口径与“盲测尚未标注”结论，属于过期文案；下一任务必须输出口径一致的独立联合报告或清理冲突段落。

### 维度裂缝联合批次成本与排序稳定性复核（2026-07-12，已完成，未发布策略）

- 资源模型已修正来源下级强化石信用的结算次数：以 85 体力的 Epic 批次为锚点，`85/40=2.125` 次结算、`2.125×0.20=0.425` 个期望下级石。唯一采用的批次净获取成本为 `85 - 0.425×1500/(1161.1/8) = 80.607613` 体力；`83.7` 是旧联合工具硬编码，`82.9` 是旧单件路径漏乘结算次数。石头使用的 14,400 金币仍在强化材料端计入，来源石信用只在批次获取端记一次。
- 新增 `joint_source_batch_metadata()` 作为资源模型的显式联合来源批次接口；联合工具不再使用 83.7 常量，也不将 Epic 与 Heroic 单件获取成本相加。Heroic 产出 `85/23.81≈3.57` 仍为 estimated，并在基线及 ±20% 敏感性中报告。
- 独立研究产物：[联合报告](reports/riftslash_joint_batch_stability_20260712.md)、[原始汇总](reports/riftslash_joint_batch_stability_20260712.json)、断点分片 `reports/riftslash_joint_batch_resume_20260712/` 与工具 `tools/research_riftslash_joint_batch.py`。5 个独立 seed、299 件 Epic、80 件 Heroic、每件 5,000 轨迹，共 3,790 个原子分片；按原始分子/分母合并，不平均分片价值率。
- 在 Heroic 基础回退/全停及 Heroic 产出率基线/±20% 的六个场景中，`B_global_current_gs` 均为第一，且对第二名 `C_category_probability` 的 95% 成对价值率区间均大于 0。因此 B 仅获得进入新的 48 件独立 Epic 验证集的资格，不覆盖现有正式策略，也不覆盖 Epic/Heroic lambda。
- 成本口径变化意味着已发布 lambda 需要在独立任务中重校准；本轮没有写回 DP 常量、Heroic 策略、正式装备策略、评分规则、跳值表或 GUI。

#### B 候选进入前瞻验证前的剩余闸门（2026-07-13）

- 联合稳定报告的分子只含正式体系价值，明确未映射终局速度 `>=22` 的一速价值。现有299件开发样本含43件固定速度路线；共享速度价值虽不改变候选动作，也可能改变价值率比值排序。因此 B 只能视为“正式体系模型下稳定”，尚不能直接解盲验证。
- 一速研究基线采用现有 `22~24:5S-105、25~26:10S-225、>=27:20S-490` 曲线，同时执行用户权威锚点“22速至少正式第二档”。因各分类/部位第二档百里分并不完全相同，需做第二档低位/中位/高位数值敏感性；若候选排序翻转，再请求用户选择唯一锚点。
- 新成本 `80.607613` 使已发布 Epic/Heroic lambda 需要另行研究重校准。新值在发布前不得写回正式配置，但必须用于新验证集的 Oracle 对照，并报告旧45件动作是否变化。
- 现有345件真实 Epic 已全部使用：299件进入开发/联合研究，46件进入旧holdout/盲测。不存在可直接抽取的独立48件；必须从冻结时点之后的新导出中前瞻收集，按实例ID/指纹与旧集合去重。可以先收集封存，但在速度/重校准/联合排序通过前不得读取人工标签。
- 全量测试仍受 `test_gui_app.py` 顶层导入与 Qt 复用阻断。正式策略任务前必须修复测试入口或建立逐文件独立进程的统一全量测试脚本，不能以分散测试代替长期可复跑入口。
- 完整下一任务：[Epic均衡候选前瞻验证准备任务说明.md](Epic均衡候选前瞻验证准备任务说明.md)。

#### 圣女3-7与维度裂缝强化资源综合口径（2026-07-13）

- 当前实现是经济等价综合，不是显式联合库存：维度裂缝每85体力的0.425个期望下级石先折成4.392387体力信用，使来源条件成本为80.607613；强化路径再按圣女3-7的金币/基础经验产率折算50/50材料和金币瓶颈。
- 圣女3-7只提供金币与基础强化经验的日常机会成本基线，当前没有证据表明它实际掉落本模型所需的下级石；剩余石头需求使用基础经验等价代理，不得描述成实际石头来源。
- 现行标量公式可能与真实资源平衡不同：各节点分别取金币/经验瓶颈后相加，不一定等于汇总Epic/Heroic全部强化、出售回收、转换和来源石后只取一次总体瓶颈。完整产出研究需新增显式资源流对照。
- 显式主口径为 `85维度裂缝体力 + max(净金币缺口/金币产率, 净经验缺口/经验产率)`；来源0.425石头先抵扣实际石头需求，不再从85额外减体力。另保留允许跨批石头盈余的长期机会成本敏感性。
- B的48件前瞻批次可以继续盲收，但在显式资源池与后续22速价值敏感性均保持排序稳定前不得解盲。完整任务：[维度裂缝与圣女材料联合资源池复核任务说明.md](维度裂缝与圣女材料联合资源池复核任务说明.md)。
- 新增权威来源产出：维度裂缝每次40体力结算获得60,000金币；按85体力联合批次为2.125次、共127,500金币。显式资源池必须先用这127,500金币及装备出售回收抵扣红紫装强化和转换金币，再用圣女3-7补剩余金币/经验瓶颈；该来源金币只记一次，不能在来源成本和强化成本两端重复信用。

### 维度裂缝与圣女材料联合资源池复核（2026-07-13，已完成，未发布策略）

- 当前正式资源路径此前并未显式汇总维度裂缝来源金币、红紫装材料、出售回收和转换；它是“0.425来源石折体力信用 + 各节点独立圣女瓶颈”的条件标量模型。新研究新增独立显式资源账本，不改变该正式条件视图或 DP 常量。
- 来源元数据补充确认值：维度裂缝每40体力结算产出60,000金币；85体力联合批次产出127,500金币。联合资源池固定为 85 体力、1 Epic、`85/23.81≈3.57` Heroic（estimated）、0.425下级石及127,500来源金币，所有来源金币和石头均只记一次。
- 显式账本先以0.425石头抵扣实际下级石需求，再汇总粉末、剩余下级石、材料金币、石头使用金币、转换金币、出售金币和经验回收。只对汇总后的金币/经验缺口使用一次圣女3-7瓶颈：`max(金币缺口/金币产率, 经验缺口/经验产率)`；主分母为 `85 + 圣女补充体力`，不再显式扣80.607613。
- 产物：[报告](reports/riftslash_saint_pool_20260713.md)、[原始汇总](reports/riftslash_saint_pool_20260713.json)、分片 `reports/riftslash_saint_pool_resume_20260713/`、工具 `tools/research_riftslash_saint_pool.py`。5 seed、299 Epic、80 Heroic、每件5,000轨迹、3,790原子分片；旧标量、本批资源池、长期跨批石头机会价值及来源金币开/关均使用相同路径。
- 主场景中来源127,500金币先抵扣后仍有金币盈余，经验缺口为唯一瓶颈；关闭来源金币虽产生金币缺口，但仍不超过经验瓶颈，因此不改变 B/C 排序。来源0.425石头全部被使用，无本批石头盈余，故本批与长期跨批口径相同。B在 Heroic 基础回退/全停、产出率基线/±20% 及两种显式资源池口径下均第一，95%成对区间不跨0，保留48件前瞻验证资格。
- 圣女3-7仍仅是金币与基础强化经验的机会成本基线，模型不声称其实际掉落下级石。未修改正式策略、Heroic策略、lambda、评分、跳值表或GUI；实际圣女下级石来源数据仍缺失且未被猜测。

#### 显式资源池完成后的当前下一步（2026-07-13）

- 显式资源池闸门已通过，不再重复研究：B在全部Heroic/产出率和本批/长期资源口径下保持第一，最窄B-C 95%区间为 `[0.000464, 0.001364]`。后续联合排序固定使用 `85 + 一次圣女瓶颈`、0.425来源石和127,500来源金币。
- 仍未完成：终局22速价值映射与第二档敏感性、新资源口径下研究lambda及旧45件Oracle变化、可靠全量测试退出码、48件前瞻真实样本收集。
- 当前工作区未发现 `collecting_blind` 前瞻批次，进度为0/48。必须从冻结后新导出中收集，旧299开发件和46审核件均不得复用。
- `unittest discover` 虽输出 `Ran 228 tests ... OK`，PowerShell仍因Qt原生子进程返回非零；在统一入口正确返回成功码前，只能记为“测试内容通过、全量执行器未验收”。
- 下一任务继续执行 [Epic均衡候选前瞻验证准备任务说明.md](Epic均衡候选前瞻验证准备任务说明.md)，不再执行已完成的资源池复核。

### Epic 均衡候选前瞻验证准备（2026-07-13，已完成，未发布策略）

- 已冻结 `A_current_review`、`B_global_current_gs`、`C_category_probability` 的规则/预测函数/代码哈希，并新建 `samples/epic_b_balanced_prospective_20260713.json`。批次状态为 `collecting_blind`，只接受冻结后的真实 `normal_85 Epic +0/+3` 非速度导出；当前进度 `0/48`，不得用旧 345 件补足或读取人工判断。
- 显式联合资源池保持为唯一主口径：`85` 维度裂缝体力、`0.425` 来源下级石、`127,500` 来源金币、红紫统一资源流和一次圣女 3-7 瓶颈。没有回退 `80.607613` 标量分母。
- 离线研究已把终局速度 `>=22` 作为独立一速价值接入。速度 `<22` 为 0；22 速保留曲线斜率并以第二档低/中/高 `1/9/11` 做敏感性。5 seed、每件 5,000 轨迹、3,790 原子分片下，B 在 24 个速度/紫装/产出率场景均第一，最窄 B-第二名 95% CI 为 `[0.000452, 0.001400]`。
- 主场景研究值为 `437.6101` 体力/终局价值、lambda `0.0022851391`；只用于研究 Oracle。旧 45 件精确 Oracle 重算后动作变化 0，空前瞻批次已锁定该口径；未写回正式 lambda、Heroic 策略或正式策略。
- 详细记录：[前瞻准备报告](reports/epic_b_balanced_prospective_preparation_20260713.md)。下一步仅是继续盲收独立真实样本；未收满 48 件不得解盲。

#### 外部红紫阈值表对比需求（2026-07-13）

- 新增冻结外部策略：Epic在+0/+3/+6/+9的“速度/套装分/输出分”阈值为 `2/25/19、3/31/24、5/37/30、9/43/37`；Heroic为 `2/22/19、3/28/24、5/34/30、10/40/37`。任一路线达标视为通过节点，仅作离线对比，不覆盖正式速度硬路线。
- 用户确认“强化石”是下级强化石；“传家宝”是终局重铸后全部副属性官方总GS>=75，不按体系过滤；“根源”完全忽略。75+仅作产出指标，不反向启用早期自动救回。
- 用户已确认固定映射：套装分就是全部副属性官方总GS；输出分就是合法普通输出体系有效GS。不得改用最佳正式体系、必爆输出取高或其他多映射敏感性。
- 用户已确认截图策略在+9达标后直接强化到+15，中途不在+12审核止损。Heroic经过+12时仍补第4条，Epic的+12/+15仍按既有副属性跳值事件模拟。
- 对比复用显式联合资源池与公共轨迹，按每100批次和每100,000总体力报告金币、下级石、原生75+传家宝、转换后75+、22速、输出有效GS>=60、正式价值和节点止损。截图底部绝对数缺少样本规模，暂不能直接复现。
- 完整任务：[外部红紫阈值表策略对比任务说明.md](外部红紫阈值表策略对比任务说明.md)。该研究可与48件前瞻盲收并行，但不得读取新人工标签或延迟22速/lambda主闸门。
- 并行边界：若另一子任务只负责48件前瞻样本盲收、导入或人工记录整理，可与外部阈值表离线对比并行；若另一子任务正在修改 `tools/epic_non_speed_early_policy_pareto.py`、联合资源池研究工具、`resource_model.py`、共用测试或本计划文档，则等待其完成后再启动，或使用独立 worktree，禁止在同一工作区并发写同一批文件。外部对比必须读取前一任务最终产物后再冻结公共轨迹与资源口径。

### 外部阈值表D研究中间审计（2026-07-13，已由下方正式对比替代）

- 已有 `tools/external_threshold_strategy.py`、`tools/research_external_threshold_d.py`、基础边界测试与1轨迹 smoke；尚未完成5 seed×每件5,000轨迹、完整资源/节点汇总、正式报告和计划闭环，D不得进入验证或正式策略。
- smoke研究器当前存在实际口径偏差：A/B/C的Heroic分支调用了D的 `simulate_path`；显式资源池传入 `powder_base_exp=0` 且未完整汇总出售经验等资源流；价值率只使用正式价值，未复用冻结22速价值锚点；+9直达+15没有在到达率中记录实际经过的+12；输出的是候选独立CI而非要求的成对差值CI。
- 续作应复用 `research_riftslash_saint_pool.py` 的原子分片、断点续跑、多进程、随机种子派生和完整 `FLOW_FIELDS` 资源账本。D用独立resume目录确定性重放同轨迹，只补D分片；A/B/C读取冻结的22速研究产物，禁止覆盖原3,790分片。
- 联合对比同时报告 `D_full`（红紫均使用截图阈值）和 `D_epic_only`（仅Epic使用D，Heroic与A/B/C保持同一基础回退/全停背景），避免把Heroic策略变化误归因于Epic阈值。48件前瞻盲收可并行，仍不得解盲。

### 外部红紫阈值表 D 对比完成（2026-07-13，未发布策略）

- 已完成：`D_full`（Epic/Heroic 均使用截图阈值）与 `D_epic_only_base` / `D_epic_only_all_stop`（仅 Epic 使用 D，Heroic 保持冻结基础回退/全停背景）的独立离线对比。D 采用 `reports/external_threshold_d_resume_20260713/` 的 3,790 个专用原子分片；A/B/C 只读 `reports/riftslash_saint_pool_22speed_resume_20260713/` 的冻结 22速产物，未覆盖其任何分片。
- 已修复研究器偏差：A/B/C Heroic 不再调用 D；显式联合资源池完整传入粉末、下级石、材料金币、转换金币、出售金币/经验并只结算一次圣女瓶颈；价值率复用冻结22速锚点；D 在 +9 通过直达 +15 时仍记录实际到达 +12；排序使用相同 seed 的成对价值率95%区间；D 分片支持原子保存、断点续跑、去重和多进程。
- 规模：5 seed（20260712--20260716）、299 Epic、80 Heroic、每件累计 5,000 轨迹、研究 lambda `0.0022851391` 仅备案，未写回正式配置。联合批次固定为 85 体力、1 Epic、`85/23.81` Heroic（estimated）、0.425 下级石、127,500 金币和一次圣女3-7瓶颈。
- 主场景（curve_baseline / Heroic基础回退 / 基线产出）中，B 的含22速价值率为 `0.228514/100体力`，C 为 `0.224783`，D_epic_only_base 为 `0.170794`，D_full 为 `0.108350`。成对95%区间：`D_epic_only_base - B=[-0.059553,-0.055887]`、`D_full - B=[-0.122263,-0.118064]`、`B-C=[0.003340,0.004122]`。
- 结论：所有冻结22速锚点、Heroic基础回退/全停及产出率基线/±20%场景下，D 均不优于 B；损失已可隔离为 Epic 阈值本身（D_epic_only 仍显著落后），Heroic 同时使用截图表进一步扩大损失。D 不进入新的独立验证，48件前瞻批次保持 `collecting_blind` 且不读取人工标签。
- 产物：[正式报告](reports/external_threshold_d_comparison_20260713.md)、[原始JSON](reports/external_threshold_d_comparison_20260713.json)、[D研究器](tools/research_external_threshold_d.py)、[阈值模块](tools/external_threshold_strategy.py)。冻结 A/B/C 旧分片没有原生75+/转换75+/22速/输出60的逐路径字段，报告明确标为不可反推，不以零代替。

#### D对比后的下一步

- 策略排序已闭环：D在全部敏感性场景显著落后B，不进入验证；不得为了补描述性指标重新打开D发布判断。主线继续收集冻结后的48件B前瞻盲样本，收满前不解盲。
- 原任务中特别要求的原生75+传家宝、转换75+、22速和输出60产量目前只能从D专用分片获得，A/B/C旧冻结分片缺字段，因此横向产量表尚未闭环。若需要完整回答截图中的“传家宝数量”问题，应新增一次独立的A/B/C指标专用确定性重放。
- 指标补全必须写入新的resume目录和报告，使用与冻结22速研究完全相同的装备、seed、random_seed和策略哈希；只能新增计数字段，不得覆盖原3,790分片、修改冻结预测、重算门槛或改变D/B结论。按每100联合批次和每100,000总体力报告A/B/C/D的原生75+、转换75+、22速、输出60及成对差值。

#### D对比数据的用户解释口径

- `含22速价值率/100体力`不是GS或装备数量，而是每100体力获得的统一终局价值点；主场景B为`0.228514`，等价于平均约`437.6`体力获得1个终局价值点。D_epic_only为`0.170794`（约`585.5`体力/价值点），D_full为`0.108350`（约`922.9`体力/价值点）。
- `D_epic_only-B`区间整体小于0，说明即使Heroic保持与B相同，仅Epic采用截图阈值也稳定更差；`D_full-B`进一步下降，说明Heroic也套用截图阈值会增加无效强化消耗。`B-C`优势较小但区间仍整体大于0，表示在当前模型与样本中B稳定略优于C，而非巨大优势。
- 22速锚点、Heroic基础回退/全停及Heroic产出率±20%均未改变B第一，故D淘汰结论对已研究假设稳健；这不等于B已发布，仍需完成48件冻结后真实前瞻盲测。
- A/B/C旧分片的75+/转换75+/22速/输出60字段缺失；报告或JSON中的占位0不得解释为实际产量为0。在指标专用重放完成前，只能比较统一终局价值效率，不能完整比较各策略的传家宝件数。

### A/B/C 终局产量补全（2026-07-13，已完成，不改变策略结论）

- 已完成：使用冻结 A/B/C 预测函数哈希、相同 299 Epic/80 Heroic、5 seed 和每件 5,000 轨迹，在 `reports/abc_terminal_metrics_resume_20260713/` 生成 3,790 个独立原子分片。D 只读取既有 `external_threshold_d_resume_20260713/` 分片；未覆盖旧冻结分片、未读前瞻人工标签，`epic_b_balanced_prospective_20260713` 保持 `collecting_blind 0/48`。
- 统计口径：仅实际到达 +15 才计产量；原生75+为重铸后全部副属性官方GS>=75；转换75+为合法满值转换实际应用后重新计算；22速为终局非鞋速度>=22；输出60为合法普通输出体系有效GS>=60。红紫资源流继续统一进入一次圣女瓶颈。
- 主场景（Heroic基础回退、基线产出）每100联合批次：A 为原生75+ `0.4030` / 转换75+ `0.5493` / 22速 `0.0204` / 输出60 `0.2529`；B 为 `0.3987` / `0.5454` / `0.0198` / `0.2529`；C 为 `0.3994` / `0.5458` / `0.0203` / `0.2498`；D_epic_only 为 `1.7721` / `1.6363` / `0.0412` / `0.2563`；D_full 为 `1.7748` / `1.6363` / `0.2741` / `0.2563`。
- 解释：D 的终局产量更多，但代价是更高总体力（D_epic_only `156.135`、D_full `305.818`，B `104.278` 体力/批），故不推翻既有 B 效率第一、D 淘汰结论。A 相比 B 的75+/22速略多但同样消耗更多资源；B-C 的原生/转换75+成对区间跨0，B 的输出60略多、22速略少，不能把这些解释性产量差异夸大为新策略排序证据。
- 产物：[补全原始数据](reports/abc_terminal_metrics_20260713.json)、[统计工具](tools/abc_terminal_metrics.py)、[已追加章节的D报告](reports/external_threshold_d_comparison_20260713.md)。

### B候选规则与证据边界说明（2026-07-13）

- B不是完整策略或精确DP，而是仅用于`normal_85 Epic +0/+3`非速度早期节点的固定三档规则：使用`evaluate_gear(...).effective_score`，`>=30`继续、`18--30`谨慎继续、`<18`停止。速度硬路线先于B覆盖；到+6/+9/+12后交回现有正式基础策略。模拟中继续/谨慎继续均表示只强化到下一节点。
- 这里的有效GS不是全部副属性总GS。它先按套装、部位、主属性和正式体系合法性建立有效属性候选，优先选有效词条数更多的合法体系，再按有效GS和体系优先级选择；无效词条不计入。因此B间接使用结构合法性，但不使用分类独立门槛、部位难度、主属性限制强弱、终局达标概率、当前有效词条数、转换候选或风险尾部来改变18/30门槛。
- `18/30`是冻结的全局对照阈值，不是由第6.4节分类×部位表或精确DP逐组拟合出的最优门槛。B之所以成为当前候选，是其在299件Epic、80件Heroic、5 seed、每件5,000轨迹的显式联合资源池中，平均含22速终局价值/总体力最高；研究同时纳入85体力来源批次、红紫共同产出、来源金币/石头、圣女瓶颈、强化/出售/转换成本及Heroic产量±20%等敏感性。
- “联合批次平均第一”不等于单件判断全面。使用研究lambda重算旧45件精确Oracle时，B仍误停7件正效用状态，Oracle正效用召回约69.57%，total regret约12.4629；C仅误停1件、召回约95.65%、regret约0.103。B在联合平均效率上仅小幅领先C，说明它可能通过大量早停换效率，同时承担少数高价值装备被错停的长尾风险。
- 因此前瞻48件的用途不是确认B“已经正确”，而是独立检查其误停率、正效用召回、regret尾部和相对C的联合效率。批次仍为0/48时，B只能称为待验证均衡候选，不得发布到正式强化建议。

### B分节点与多因素修正方向（2026-07-13，待研究）

- 用户指出B不应把+0/+3混为一个门槛。该问题成立：B在+0通过`19.53125%`，这些路径到+3后的停止率为`0`；有效GS单调不降导致+3即使未命中体系也机械通过。+0到+3条件成本约`2.6221`体力，+3到+6约`11.4101`，后者约4.35倍，缺少+3独立止损可能显著影响效率。
- 资源轮次关系需要纠正：D_epic_only每批`156.135`体力、B每批`104.278`，所以约`2轮D_epic_only≈3轮B`，不是3轮D只能换2轮B；D_full一批`305.818`约等于`2.93`轮B。
- 不能仅凭D总体力更高宣布其所有目标效率更低。每100,000总体力：B/D_epic_only/D_full的原生75+为`3.8233/11.3500/5.8035`，转换75+为`5.2302/10.4800/5.3507`，22速为`0.1901/0.2636/0.8963`，输出60为`2.4253/1.6412/0.8379`。D在原生75+及部分速度目标上单位体力更高，但正式综合价值与输出60效率更低；D只可在对应单目标下描述，不能用B综合价值第一覆盖。
- 下一研究应分别校准+0/+3，并对命中体系、结构限制、分类、终局概率、下一节点成本和合法转换做逐级消融，而不是把所有因素无验证地堆进一个公式。主目标仍为正式价值/22速价值的均衡效率，同时以Oracle召回和regret约束误停长尾。
- 当前B前瞻批次仍为`0/48`。在分节点新候选研究完成前暂停收集；不得修改空批次内冻结哈希。若新候选通过离线闸门，标记旧空批次被替代并创建新的冻结批次。
- 完整任务：[Epic分节点多因素均衡策略研究任务说明.md](Epic分节点多因素均衡策略研究任务说明.md)。

### 22速最高优先级对B结论的修正（2026-07-13，待研究）

### Epic 非速度分节点消融（2026-07-13，已完成，未发布）

- 已完成 `normal_85 Epic` 非速度开发集的 5 seed、每件 5,000 条公共轨迹重放；独立分片目录为 `reports/epic_stage_balanced_non_speed_resume_20260713/`，未覆盖 B/D/ABC 既有分片，前瞻批次保持 `collecting_blind 0/48`。
- 主口径只使用正式体系终局价值/总体力，不再使用 `max(正式价值, 22速有限价值)` 作整体排序。`B_stage_conversion` 为非速度阶段最高候选（`0.174469/100体力`），但它不构成速度或整体策略发布结论。
- 分节点 GS、命中和结构层的成对区间为负或零；分类层跨敏感性不稳定，因此均不保留。概率层和合法满值转换层在所有 Heroic 回退/全停、产出率敏感性下为正；转换层 Oracle regret 从 `14.484977` 降至 `2.018629`，但 Oracle 正效用召回仍为 `69.57%`，不能创建或恢复前瞻批次。
- 产物：[正式报告](reports/epic_stage_balanced_non_speed_20260713.md)、[原始结果](reports/epic_stage_balanced_non_speed_20260713.json)、[研究器](tools/research_epic_stage_balanced.py)。后续必须按《22速优先强化路线重校准任务说明.md》建立独立速度研究和 resume 目录，再讨论组合流程。
- 主agent复核：`B_stage_conversion`只能视为当前非速度开发集的最高研究项，不能称为可用新策略。其Oracle正效用召回与旧B同为`69.57%`、仍误停7件；平均价值率提升很小，未解决长尾风险。分节点GS/命中/结构未获稳定增益，说明当前数据不支持强行加入这些因素，而不是证明这些因素在所有未来样本中永久无用。
- 测试归因已完成：官方逐文件隔离入口`tools/run_all_tests.py`退出码0；`test_manual_sample_performance.py`串行重复通过。直接模块方式运行`tests.test_gui_app`两次均以`0xC0000409`原生终止且无Python断言堆栈，属于GUI/Qt残余风险；完整日志位于`reports/test_logs_20260713/`。速度研究可继续，但不得宣称所有GUI调用方式均稳定，也不得在本任务修改GUI业务代码。
- 下一执行顺序：按22速任务说明运行独立速度研究。非速度报告保持封存，旧空前瞻批次继续暂停，完整速度/非速度组合策略和新前瞻批次均不得提前生成。

- 用户确认22速优先级最高后，不能继续以`max(正式价值, 有限速度价值)`的单一平均值宣布B为整体最优。每100,000总体力的22速为B`0.1901`、D_epic_only`0.2636`、D_full`0.8963`；后两者约为B的1.39倍和4.71倍，且成对差值区间为正。B只能称为正式综合价值/输出效率候选，不能称为速度最优。
- 当前速度硬路线只明确覆盖Epic/Heroic的+0初始门槛和+3命中后继续到+6；+6以后回到通用策略。初始门槛为Epic 2、Heroic 4。D_full的速度增量主要来自Heroic，说明Heroic门槛和后续节点链需要单独校准。
- 当前B前瞻批次明确排除速度硬路线，即使收满48件也不能验证速度策略。该空批次继续暂停；非速度分节点研究可独立进行，但完整联合候选排序必须等待速度优先路线研究结果。
- 速度研究采用分层目标：第一目标最大化22速/总体力，第二目标在速度效率近似相同的候选中最大化正式体系价值，第三目标节约资源；同时保留旧第二档有限权重作为敏感性，不再混为唯一目标。
- 完整任务：[22速优先强化路线重校准任务说明.md](22速优先强化路线重校准任务说明.md)。
- 执行顺序：若《Epic分节点多因素均衡策略研究任务说明.md》已在子任务中运行，不中断或丢弃已完成分片；直接追加范围修正，要求其把现有结果限定为“非速度候选”，停止整体排序/前瞻批次创建。当前原子写入或测试结束后，再完整读取22速任务说明并继续；不得同时由两个子任务并发修改共用研究工具、测试和计划文档。
- 官方概率表修正（2026-07-13）：STOVE官方公告《(概率) - 装备制作》在“Lv.72~85装备强化时副属性提升概率”中给出的传说（Epic）速度跳值为`2=33.223%`、`3=33.223%`、`4=33.223%`、`5=0.332%`，来源：https://page.onstove.com/EpicSeven/kr/view/10060905 。此前Epic`2/3/4各33.1%、5为0.7%`的临时研究假设立即作废；normal_85 Epic速度离线研究改用官方分布，但本轮不修改正式代码或跳值配置。
- 同一公告的11张强化概率表全部标记稀有度“传说（Epic）”，没有英雄（Heroic）概率表或品质切换入口。用户进一步确认normal_85 Heroic速度概率为`1=0.332%`、`2/3/4各33.223%`且无5速；该规则在项目内具有权威性，但报告必须注明它不是上述公告直接列出的Heroic表。此前Heroic`1为0.7%、2/3/4各33.1%`的临时分布作废。初始速度生成概率仍不得从强化跳值反推，rift_85继续使用独立分布。
- 用户确认normal_85 Heroic除速度稀有端点外，其余固定值、百分比、双效、暴击和暴伤强化跳值及概率均与normal_85 Epic官方表相同。该规则可用于后续normal_85模拟器对照，但本轮速度研究不得顺带扩大为正式跳值实现修改。
- 当前速度子任务补充指令：暂停任何基于0.7%尾部的实现或正式研究；仅在离线研究层使用Epic官方概率与Heroic用户确认概率，并增加两种品质离散概率及normal/rift隔离测试。正式跳值表、正式策略、lambda与GUI保持不变。
- STOVE资料核对记录：[STOVE装备概率资料核对记录.md](STOVE装备概率资料核对记录.md)。官方Epic表还覆盖固定攻击/防御/生命、百分比、双效、暴击和暴伤；本轮只记录证据，不扩大到正式实现。
### 22速路线新概率重校准（2026-07-13，已完成，未发布策略）

- 已按 STOVE 官方 Epic `2/3/4=33.223%、5=0.332%` 和用户确认 Heroic `1=0.332%、2/3/4=33.223%` 重跑独立离线研究；此前 `0.7%` 临时尾部假设产生的速度研究分片与结论均标记失效，未续跑或混用。rift Epic 继续使用独立异界表。
- 新目录为 `reports/speed_priority_22_official_v2_resume_20260713/` 与 `reports/speed_priority_22_rare_removed_v2_resume_20260713/`，分别覆盖 normal Epic 51 件、normal Heroic 53 件、rift Epic 51 件，5 seed、每件每 seed 5,000 条轨迹、500 条原子分片。
- 已废弃的历史结论：旧联合 normal Epic/Heroic 批次曾将`speed_reachable_22_start_4`列为第一并报告`0.002530个22速/100总体力`；它错误使用Heroic=2的当前基准，并把条件速度胚子混入85体力完整批次。该数字、相对当前区间和“稀有端点不影响排序”的结论均不得再使用，见后续v3口径修正记录。
- 历史产物保留仅供追溯：`reports/speed_priority_22_probability_recalibration_20260713.md`、`reports/speed_priority_22_official_20260713.json`、`reports/speed_priority_22_rare_removed_20260713.json`。唯一可用的本轮离线结论见`reports/speed_priority_22_methodology_correction_20260713.md`；本轮未修改正式策略、正式lambda、评分、跳值表、GUI或48件前瞻批次。

#### 主agent口径复核（2026-07-13，需修正后再判断）

- STOVE Epic与用户确认Heroic概率层本身已按新值重跑，旧0.7%分片未复用；这部分可保留。但`speed_reachable_22_start_4`暂时不能进入正式策略或前瞻验证。
- 研究器中的`speed_current`在+0统一传入初始门槛2，导致normal Heroic也按2继续；正式现状实际为normal/rift Epic门槛2、normal Heroic门槛4。因此“相对当前路线”的联合差值没有使用真实当前基准。
- 联合候选只按相同名称取Epic/Heroic交集，强制两种品质共用同一个`start_2/3/4`。它没有评估正式当前组合`Epic=2、Heroic=4`，也没有评估各品质独立最优组合。分来源点估计实际为：normal Epic的可达性门槛2最高、normal Heroic门槛3最高、rift Epic门槛2/3最高；不能据联合`start_4`反推三种来源都应采用4。
- 项目权威规则已冻结normal/rift Epic非鞋初始速度>=2进入赌速路线；`start_4`会停止2/3速Epic胚子，与该规则冲突，不能作为Epic发布候选。
- 当前研究只从真实库存筛选“非鞋且已有速度”的51 Epic和53 Heroic，却在联合分母中把它们当作每个85体力批次必然获得的1 Epic与3.57 Heroic。没有加入非鞋、初始带速度和初始速度值的自然出现概率，因此`0.002530个/100总体力`不是可解释的真实副本产量。该指标只能在重新定义后作为条件样本比较，不能与此前完整装备池产量直接比较。
- 修正后必须分开两种口径：已获得速度胚子的边际强化效率排除获取成本；包含获取成本的完整装备池效率必须同时保留无速度装备及其固定策略，或有可追溯自然掉落权重。真实库存筛选比例不得伪装成自然掉率。
- `tools/run_all_tests.py`本轮未取得完整成功码；即使离线排序修正，也必须先完成全量验证，才能创建速度前瞻批次。
- 下一任务：[22速研究口径复核与修正任务说明.md](22速研究口径复核与修正任务说明.md)。旧速度报告保留为概率重跑记录，但策略结论状态改为`methodology_blocked`。

### 22速研究口径修正（2026-07-13，已完成，未发布）

- 已修复离线`speed_current`将Heroic误按2速起步的问题，新的`speed_current_exact`冻结为normal/rift Epic=2、normal Heroic=4；+3命中速度到+6，之后读取正式后续策略。新增回归测试覆盖三个来源/品质的+0门槛及+3/+6行为。
- 已废除相同候选名强制联合：Epic只保留已确认2速硬路线，Heroic独立研究2/3/4；联合组合改为显式映射，含`current_epic2_reachable_heroic4`。不再存在Epic的`start_3/start_4`发布候选。
- 条件速度胚子研究现在排除85体力装备获取成本，输出每100速度胚子、每100增量体力、每件22速的增量体力、各节点行为、正式价值、原生/转换75+及输出60。完整装备池效率明确为`not_estimated`，因为输入是已筛选库存速度胚子，缺少自然初始速度/掉落权重与固定非速度分支；不得再称为副本每100体力产量。
- 官方主分布与稀有端点移除敏感性均完成5 seed、每件每seed 5,000轨迹的新v3分片。normal Epic可达性Epic2在主分布较真实当前为负`-0.000632`，但在移除稀有端点后转正`+0.002865`，排序不稳；命中链虽稳定提高条件22速效率，却显著降低正式价值、75+和输出60。Heroic可达性4速度效率稳定提高但仍有正式价值代价。详见[综合报告](reports/speed_priority_22_methodology_correction_20260713.md)。
- 发布闸门未通过：完整装备池未估计、normal Epic可达性对稀有端点不稳，且显著速度增益存在未授权的正式价值/75+/输出60牺牲。不得创建或解盲速度前瞻批次，不得修改正式策略。
- 验证：新增定向研究测试8项通过，语法检查和`git diff --check`通过。`tools/run_all_tests.py`第一次仅`test_manual_sample_performance.py`失败；该Qt性能文件单独通过后，第二次完整入口输出`ALL TEST FILES PASSED`并以退出码0结束。本轮未修改GUI。

### Heroic 紫装 +0 速度门槛与十万体力产量（2026-07-13，待研究）

- 用户要求比较两组 `normal_85 Heroic` 非鞋 `+0` 速度门槛：仅速度套/两件套从当前4降到3或2，以及所有套装从4降到3或2。Epic `>=2`硬路线与+3以后行为保持冻结，本任务只做离线研究，不修改正式策略。
- 上一轮条件速度胚子结果不能回答真实产量。本轮必须纳入完整 Epic/Heroic 联合装备池、无速度装备、鞋子、合法部位/主副属性、Heroic +12补词条、出售/转换和全部后续策略。
- 人类主报告统一使用完整线路 `100,000` 总体力：一次循环为85维度裂缝体力加该策略所需圣女3-7瓶颈补充；明确展示循环数、维度裂缝/圣女体力与次数、红紫产出、节点到达、资源账本、22+件数、各正式体系百里分合计及总百里分。
- 22+速度不折算成百里分；候选按Pareto交换关系报告“多多少22+、少多少GS、多多少圣女体力”，不得用未经确认的单一权重替用户决定取舍。
- 套装按单套装条件结果计算。实际维度裂缝所选四套等概率，四套结果等于四个单套装结果算术平均；不建立通用副本套装池或账号体力规划。
- 初始副属性数值精确概率仍缺直接证据。若审计后仍无证据，必须把现有区间均匀与强化离散概率代理初始值分成两种敏感性；排序不一致则标记`initial_distribution_blocked`，不得进入策略任务。
- 完整执行说明：[Heroic紫装速度门槛与十万体力产量研究任务说明.md](Heroic紫装速度门槛与十万体力产量研究任务说明.md)。

#### 与旧 B 缺项和现有优化量的关系

- 截至本记录，没有任何新候选写入正式强化策略，因此正式脚本相对旧规则的实际优化为 `0`。22速v3和Epic非速度分节点结果都仍是离线研究。
- `B_stage_conversion`在研究层确实读取了+0/+3分节点、命中体系、当前/可行有效词条、受限三词条部位、体系优先级、终局概率和合法满值转换；但分节点GS、命中、结构和分类层没有稳定提升，只有概率和转换层在开发集消融中保留。
- 平均正式体系价值率仅由`B_global_current_gs`约`0.174254`提高到`B_stage_conversion`约`0.174469/100体力`，相对约`+0.12%`。主要改善在尾部：旧45件非独立Oracle的总regret由`12.462881`降至`2.018629`，约`-83.8%`，最大regret由`10.445017`降至`0.943169`；但误停仍为7件、正效用召回仍为`69.57%`，不能称为已解决误停问题。
- 分类独立精确门槛、部位取得高分难度、玩家库存需求、rift_85及Heroic非速度早期策略仍未形成发布规则。新Heroic速度门槛任务只研究速度专项和完整十万体力产量，不得被描述为补齐全部因素。

### Heroic 紫装速度门槛第一版结果复核（2026-07-13，需修正后重跑）

- 第一版报告状态原为`initial_distribution_blocked_and_22_ci_unresolved`，但主agent进一步发现其H4正式后续基准存在实际偏差：研究器在所有节点用`advise_gear(..., enable_dp_assist=False).recommendation != stop`作为二元动作，导致约551.22件装备从+3开始全部到+15，+3/+6/+9/+12停止数均为0。
- 该偏差把每个85体力来源循环的圣女补充放大到约594.28体力，使十万总体力中圣女占87,405.1、维度裂缝仅12,594.9。账本加总虽然等于十万，但强化路径不是现有正式+6/+9/+12后续策略，因此第一版`1.351件22+`、`42.84总百里分`及候选差值全部降级为无效基准，禁止用于门槛决策。
- 第一版全部22+均来自normal Epic，normal Heroic为0。H2/H3没有观察到任何Heroic 22+收益，只体现了额外资源消耗和循环数下降；默认每套每seed 200条不足以研究该稀有事件，不能据此判断降低Heroic门槛无效。
- 终局计数还需分离原生正式GS、合法满值转换后可达GS与实际承担转换成本的策略GS；未知/一速分类不得强制写为半肉(通用)。
- 下一步先补失败测试并恢复正式二元后续路线，再用按部位/主属性/速度出现/初始1--4速的自然概率分层放大Heroic稀有事件。禁止直接续用第一版resume或只机械增加相同全池样本。
- 完整续作说明：[Heroic紫装速度门槛研究基准与稀有事件修正任务说明.md](Heroic紫装速度门槛研究基准与稀有事件修正任务说明.md)。

### Heroic 紫装速度门槛完整池研究（2026-07-13，已完成，未发布）

- 新增独立研究器 `tools/research_heroic_speed_threshold_100k.py` 和定向回归测试；没有修改正式策略、跳值表、lambda、评分、DP、资源默认值、GUI或前瞻批次。
- 研究使用完整 `normal_85` Epic/Heroic 联合池：每个85体力来源批次为1 Epic和3.569929 Heroic，包含鞋子、无速度装备、合法部位/主属性/副属性、Heroic +12补词条、出售和合法满值转换。来源金币127,500与下级石0.425只记一次；每项主指标归一到100,000总体现力。
- 冻结的Heroic候选为H4当前、仅速度套/确认两件套的H3/H2、全套装H3/H2；弱化套只作为分离敏感性。Epic非鞋速度>=2及+3后速度链没有被候选削弱。
- 20个套装、两种初始值口径、5个seed、每套每seed20个完整联合批次均已完成，原始分片在 `reports/heroic_speed_threshold_100k_resume_20260713/`，结果见 `reports/heroic_speed_threshold_100k_20260713.md` 与 `.json`。
- 发布/人工确认闸门未通过：初始副属性数值没有直接概率证据，合法均匀与“强化概率代理初始值”对四套示例的22+候选排序不同；且当前20样本/seed下候选相对H4的22+置信区间跨零。状态为 `initial_distribution_blocked_and_22_ci_unresolved`，不得创建速度前瞻批次或修改Heroic门槛。
- 研究释放节点使用正式分类建议的非DP基线；Heroic资源DP lambda仍未发布，未在研究层伪造替代值。该限制已列入报告，不能把本轮称为完整策略发布验证。
# 2026-07-13 Heroic 速度研究基准修正（进行中）

- 已完成动作等价性收口：2,560 个确定性状态确认 H2/H3 仅改变 +0 前台标签，不改变自动 `enhance/stop` 路径；状态为 `no_binary_action_difference_under_current_policy`，暂停 Heroic 22+ 稀有事件扩样。
- 第一版 `heroic_speed_threshold_100k_20260713` 已标记 `superseded_due_to_followup_baseline_error`：其将展示层“谨慎继续”错误扩展为后续节点继续，导致 +6/+9/+12 无止损。
- 新 v2 研究器保持 Epic 已发布 DP，Heroic 在 +6/+9/+12 明确使用 `baili_marginal_low` 基础回退；H2/H3 仅能覆盖授权套装的 Heroic 非鞋 +0 初始 2/3 速。
- v2 smoke 已观察到 +6/+9 止损，并使用独立 schema/resume。尚未达到每主候选、每初始口径 200 个 Heroic 22+ 有效终局的分层稀有事件门槛，不能创建速度前瞻批次或修改正式门槛。

### Heroic +0 初始速度门槛动作等价性复核（2026-07-13，优先于正式扩样）

- v2正式后续基准修正有效：H4单循环圣女补充由第一版约594降到约137体力，且+6/+9出现止损。该部分可以保留。
- 但H2/H3当前只在Heroic非鞋+0初始2/3速把研究动作标为`continue`，+3以后全部交回released；正式`early_speed_gamble_status`在+3只检查速度rolls>=2，不保存+0初始门槛资格。
- 当前自动强化把前台`continue`与`cautious_continue`都解释为“强化至下一复核节点”。smoke中H4、H3/H2速度套两件套和H3/H2全套装的循环数、圣女体力、到达/停止+6及22+完全相同，说明候选尚未改变二元强化动作。
- 因此暂停“每候选200个Heroic 22+事件”的正式扩样。先穷举+0/+3/+6动作矩阵；若只存在标签差异，状态应收口为`no_binary_action_difference_under_current_policy`，无需继续随机长跑。
- 当前“满值转换可达GS”仍复用只救回原生未正式命中的conversion标志，需补充原生已正式但转换能进一步提高GS的独立可达值；可达敏感性不得自动计10万金币。
- 完整任务：[Heroic紫装初始速度门槛动作等价性收口任务说明.md](Heroic紫装初始速度门槛动作等价性收口任务说明.md)。若确认动作等价，下一项优先研究+6/+9/+12的Heroic 22速可达性路线，而不是继续调整仅影响+0标签的门槛。

### Heroic 中后期22速可达性路线（2026-07-13，待研究）

- H2/H3初始门槛已以`no_binary_action_difference_under_current_policy`收口，不再扩样或讨论发布。下一策略变量转为真正改变自动动作的Heroic +6/+9/+12速度救回。
- 基准固定为：+0/+3完全沿用正式路线；Heroic +6以后使用`baili_marginal_low`。候选只能在基础回退原本stop时，按22可达性/P22概率/概率成本/速度命中链救回；基础回退本来continue的装备不得被候选停止，Epic不受影响。
- 主目标为Heroic 22+/十万总体力，正式GS和资源作为独立代价，不合成单一分数。比较速度套+确认两件套范围与全部套装范围；弱化套单列敏感性。
- 使用当前+6状态的精确Heroic速度概率枚举，并以自然权重分层扩大“基础回退stop且候选救回”的差异strata。每个进入正式比较的候选至少需要200个由救回产生的Heroic22+事件，不能用候选共享事件凑数。
- 完整十万体力继续分解维度裂缝/圣女循环、节点救回数、22--27+、原生/转换可达/实际策略GS及资源账本。初始值均匀与强化概率代理仍分开；排序变化则保持`initial_distribution_blocked`。
- 完整任务：[Heroic紫装中后期22速可达性路线研究任务说明.md](Heroic紫装中后期22速可达性路线研究任务说明.md)。本任务仅离线研究，不修改正式策略或创建前瞻批次。

#### 中后期研究器 smoke 后复核（2026-07-13，需修指标并实现差异分层）

- 可保留：研究器已冻结+0/+3、Epic和Heroic正式基础回退，P22正确覆盖+12补词条/+15/一次重铸；20条完整池smoke出现+6/+9止损且十万总体力账本守恒。
- 当前全0结果只说明随机完整池未抽到“基础回退stop、P22>0、候选救回并终局22+”的交集，不能解释为候选无收益。`_simulate_shard()`仍是完整池直接抽样，尚未实现任务要求的差异strata和自然权重过采样。
- 指标偏差一：`M1_reachable_22`只是不计资源地救回全部P22>0状态，不能称为最大化22/十万总体力的速度Oracle；正式报告应改称可达性全救回参考，除非另行实现真正资源效率Oracle。
- 指标偏差二：当前“每多1件22+额外总体力”用两个固定归一为100,000的`ledger.total_stamina`相减，必然为0。应改报圣女体力变化、维度裂缝体力/循环损失、GS变化及100000/Heroic22。
- 指标偏差三：当前`candidate_unique_rescued_22_events`来自已经换算为每十万体力的数值跨seed求和，不是原始独立事件。200事件闸门必须使用原子分片原始唯一事件，并同时约束加权ESS。
- 下一步先做可行状态审计，再按套装/部位/主属性/节点/当前速度/rolls/历史命中和基础回退动作建立自然概率差异分层。每候选要求原始独立救回22事件>=200、ESS>=100且至少5 seed非零；公共状态仍进入完整联合资源与GS账本。
- 完整续作：[Heroic中后期22速差异分层与指标修正任务说明.md](Heroic中后期22速差异分层与指标修正任务说明.md)。正式扩样前先补上述失败测试；本轮不修改正式策略或创建前瞻批次。

#### v3 合法差异状态审计修正（2026-07-13）

- 用户指出：Heroic +12只补第4条，不强化速度；因此+12为14速/rolls=4时，+9必须已经是14速/rolls=4，且+3/+6/+9三次都命中速度。
- 当前`enumerate_legal_rescue_states()`手工构造的witness在+12只有3条副属性，roll_history也只记录一次速度命中，却标成14速/rolls=4。该对象违反Heroic +12四词条及强化历史一致性，不能证明合法差异状态存在。
- v3已完成的M1命名、边际公式、原始事件/加权产量/ESS分离和schema升级可以保留；“合法差异状态已确认”撤回，正式差异扩样继续暂停。
- 修正必须从合法+0 Heroic装备逐节点生成+3/+6/+9/+12完整path，并断言+9到+12速度值和rolls不变、+12新增合法非重复第4条、roll_history含全部速度命中。
- 若M0在+9已stop而候选救回，则+12是候选后续状态，不是M0自然到达状态。终局22+按`first_rescue_node`只归因一次，后续救回节点仅作诊断，禁止+9/+12重复计产量。
- 上述修正已补入[Heroic中后期22速差异分层与指标修正任务说明.md](Heroic中后期22速差异分层与指标修正任务说明.md)，完成失败测试和合法路径审计后才能实现自然权重过采样。
- 合法路径审计已修复：witness 现在从 `+0` Heroic 三词条出发，`+3/+6/+9` 均记录合法的4速命中，`+12` 只补非重复第四词条；`+9/+12` 均为14速、4次速度命中。审计器会拒绝任何三词条 `+12`、缺失三次速度命中或速度/rolls 与 `+9` 不一致的路径。当前该 witness 的 M0 在 `+12` 停止、`P22=0.083057`，M1继续。另有确定性归因测试：若 M0 在 `+9` 停止，`+12` 标记为候选专属后续，终局22+仅按首次救回 `+9` 计一次，后续救回只作诊断。
- 正式自然权重差异扩样仍暂停；下一步才是实现过采样与自然权重估计，不能复用旧手工 +12 三词条 witness 或将其分片纳入统计。

#### v5 自然权重差异分层正式研究（2026-07-13，未通过闸门）

- 新分层器保留完整自然公共池作为 Epic、鞋子、无速度、基线继续/停止、出售、资源和正式GS背景；定向 proposal 只处理非鞋初始含速度的 `+3/+6/+9` 八类命中模式，并增加 `2+4+4+4` 且 `+15` 速度4的重叠高值 proposal。`+12` 第四词条和其他 `+15` 分支仍按自然分布抽样，因此失败强化成本没有被删除。
- 每个候选事件保存唯一 `seed:targeted:plan:path`、自然概率分解、proposal 概率、真实 importance weight、首次/后续救回节点；原始事件按候选内轨迹去重，不跨候选合并。v5 resume 与 v3/v4 完全隔离；v4 因高值 proposal 将代理初始值概率错误写死为 `0.25` 而作废。
- v5 正式规模：官方概率、稀有1速移除、强化概率代理初始值三组各 5 seed（20260802--20260806）；每 seed 公共池64、定向池256。报告与原始数据：`reports/heroic_midgame_speed22_v5_stratified_formal_20260713.{md,json}`，resume 为 `reports/heroic_midgame_speed22_v5_stratified_formal_resume_20260713/`。
- 闸门状态 `offline_only_gate_not_met`：M1/M2 0.5--5%/M4/M5 的 all_sets 在官方模型仅96个原始候选救回22+，虽覆盖5个非零seed，但真实重要性权重 ESS 仅 `1.018`（代理初始值为96），均未达到原始事件>=200、ESS>=100的双重要求；M2 10%与全部M3无救回事件。不得创建前瞻批次、解盲或修改正式策略。
- 后续仅可在新任务中重新设计 proposal 分配以降低普通 h111 与高值重叠 strata 的权重失衡，并保持公共池账本不变；不能用更多同一 proposal 的样本把 ESS 问题掩盖为数量问题。
- 验证：研究器定向19项、速度/资源/策略相关97项、语法检查和`git diff --check`通过。两次隔离`tests/test_gui_app.py`均为`test_gui_support`顶层导入失败（日志在`reports/test_logs_20260713/test_gui_app_v5_stratified_run{1,2}.log`）；`tools/run_all_tests.py`因此退出1，尽管其余逐文件测试通过。本轮未修改GUI，不能宣称全量测试成功。

#### 主agent审计：v5 估计器口径失效（2026-07-13，必须修正后重跑）

- v5 的 `proposal_probability` 声明八个普通命中模式各占 `0.0625`、高值重叠 proposal 占 `0.5`；实际抽样却使用 `plans[path_index % len(plans)]`，9个 plan 每 seed 各获得约28--29次，即实际分配接近各 `1/9`。balance weight 仍按声明的 `0.0625/0.5` 计算，抽样分配与权重密度不一致。
- `_record_stratified_delta()` 将候选相对 M0 的终局产量、GS、出售/强化/转换成本和节点账本只乘 `common_runs / targeted_runs`，没有乘 `evidence["importance_weight"]`。只有 `weighted_rescued_22_count` 单独使用了 importance weight，因此正式表的主指标与事件证据不在同一估计口径。
- 直接证据：`M1_reachable_22/all_sets` 报告主表为 `114.1220 Heroic22+/10万体力`，而同一行真实加权救回字段仅为 `0.196815`，相差约 `579.84` 倍；前者及其 GS、资源和边际指标全部禁止用于策略判断。
- 96个原始救回事件的权重和为 `0.1644363`；其中唯一普通 `h111` 事件权重 `0.162963`，占总权重 `99.104%`，其余95个高值重叠事件合计仅占约 `0.896%`，所以事件 ESS 为 `1.018`。这不是单纯样本量不足，而是 proposal/估计器设计失衡。
- v5 报告和 JSON 保留为失败审计证据，状态升级为 `invalid_estimator_not_for_policy_comparison`；不得续跑 v5 resume、不得引用其候选产量/GS/成本/CI、不得创建前瞻批次或修改正式策略。
- 下一步建立 v6：先增加失败回归测试，统一所有候选增量使用同一个自然权重；再将速度签名做互斥自然分层，按精确分层质量乘条件均值汇总。若保留重叠 proposal，必须使用与实际样本分配一致的 balance heuristic，并报告全部样本设计 ESS、正贡献 ESS、最大单样本贡献占比及逐 seed 加权区间。
- 完整执行说明：[Heroic中后期22速v5估计器审计与v6修正任务说明.md](Heroic中后期22速v5估计器审计与v6修正任务说明.md)。

#### 主agent复核：合法 witness 通过，进入差异分层实现（2026-07-13）

- 已复核完整代码和回归测试：witness从Heroic +0三词条、初始2速开始，+3/+6/+9分别合法命中速度4，故+9为14速/rolls=4；+12只新增不重复的生命4%，保持14速/rolls=4和原三次roll_history；+15再执行合法速度4。
- `enumerate_legal_rescue_states()`在该完整path上只返回+12差异状态，表示此具体装备M0在+9仍continue、到+12才stop；+12的`P22=0.083057`且M1 continue，因此+12救回是合法真实二元差异，不是候选专属伪状态。
- first/later rescue归因测试同时覆盖另一构造：若M0在+9已stop、候选从+9救回，则+12只标记为candidate-only，终局22+仅归因first_rescue_node=9一次。该逻辑可以保留。
- 下一步可以实现正式自然权重过采样。必须对差异strata强制条件事件并保存完整自然概率分解，同时生成成功/失败未来分支；不能只强制成功路径而漏算失败资源成本。
- 当前`_candidate_event_summary()`的smoke仍用`raw_weights=[1.0]`计算ESS。正式重要性抽样时每个raw event必须携带真实importance weight，raw count取唯一轨迹数，ESS取真实权重；候选/范围共享同一底层轨迹但不得跨候选合并成独立事件。
- 正式分层实现继续以[Heroic中后期22速差异分层与指标修正任务说明.md](Heroic中后期22速差异分层与指标修正任务说明.md)为唯一执行说明。完成至少200原始救回22、ESS>=100、5 seed非零及成对区间前，不得生成发布结论或前瞻批次。

### Heroic 中后期22速可达性路线（2026-07-13，进行中）

- 已新增独立研究器 `tools/research_heroic_midgame_speed22.py` 与失败回归测试。它不读取第一版速度分片；schema/resume 独立为 `heroic_midgame_speed22_v1`。
- 已冻结 `+0/+3` 为 released route、Epic 为 released DP、Heroic `+6/+9/+12` 为 `baili_marginal_low`。候选永远不能停止基线继续状态，只能在 normal_85 Heroic 非鞋已有速度副属性且基线停止时救回。
- 已实现精确 Heroic P22 枚举：速度 `1=0.332%`、`2/3/4=33.223%`、无5；公布值的 100.001% 舍入误差先归一化；+12 补第4词条；+15仍是强化事件；重铸速度只加一次。M1、P22网格、P22/下一节点体力、真实速度命中链、外部速度参考和两种套装范围均已冻结。
- 20条完整池 smoke 已确认 +6/+9 出现 `baili_marginal_low` 节点止损，联合账本严格等于十万总体力，且没有候选改变 Epic、鞋子、无速度或+0/+3。样本中尚未观察到候选救回22+，因此没有候选进入正式比较，也没有创建速度前瞻批次。
- 待续：按自然状态权重扩展基线 stop 且P22可达的差异 strata，只有单候选独立救回 Heroic 22+事件达到200且相对M0的成对95%区间为正，才可生成完整正式比较和人工确认闸门结论。
- 验证：新研究及速度/资源/策略相关测试86项通过，语法检查与`git diff --check`通过。`tools/run_all_tests.py`已运行但被既有`tests/test_gui_app.py` Qt原生测试失败中断；本任务未修改GUI，不能据此宣称全量成功。

### Heroic 中后期22速 v6 估计器审计与 pilot（2026-07-13，未通过闸门）

- v5 已确认为失效估计器：实际轮询等额抽取9个 proposal，却按 `0.0625/0.5` 混合比例计算权重；候选的22+才乘 importance weight，而正式GS、强化经验/金币、出售回收、转换成本和节点账本只乘普通缩放，不能用于策略比较。v3/v4/v5 分片、resume 和结论均不读取。
- v6 升级为 `heroic_midgame_speed22_v6_mutually_exclusive_speed_signatures`。Heroic 初始速度、+3/+6/+9 是否命中速度及各自跳值、+15 是否命中速度及跳值构成2,500个互斥签名；每个签名保存精确自然质量，并以 `sum(P(signature) * mean(metric | signature))` 汇总。签名内仍自然生成+12第四词条及全部非速度未来分支，失败强化成本不会遗漏。
- 新增失败回归覆盖：签名总自然质量守恒且无 proposal 重叠；初始值口径改变会改变对应自然质量；候选22+差值恒等于加权救回22+；v3/v4/v5 resume 均被拒绝。所有候选差异指标共用同一个签名质量。
- 新 schema pilot：seed `20260723`，公共完整池64、每个签名1条，分别运行官方概率、移除稀有1速端点、初始值分布代理。官方 `M1_reachable_22/all_sets` 为 `0.1862739` Heroic22+/10万总体力，且等于 `weighted_rescued_22_per_100k`；原始救回22事件14、设计ESS 61.54、正贡献ESS 14、最大单样本贡献7.14%。
- 结论：`pilot_only_gate_not_met`。未达到原始救回22>=200、正贡献ESS>=100、至少5个非零seed、成对区间及最大单样本<=5%的联合发布闸门；不得创建前瞻批次、解盲或修改任何正式策略。该结论不等于永久禁止后续研究扩样。
- 产物：[v6 pilot报告](reports/heroic_midgame_speed22_v6_pilot_20260713.md)、[原始结果](reports/heroic_midgame_speed22_v6_pilot_20260713.json)、[v6 resume](reports/heroic_midgame_speed22_v6_pilot_resume_20260713/)。后续可依据该pilot制定新的v7方差分配并用全新seed独立扩样；不得沿用v5，也不得把v6 pilot分片直接并入正式结果。
- 验证：研究器定向19项、研究/速度/策略/资源相关97项通过；语法检查与`git diff --check`通过。`tests/test_gui_app.py`隔离两次均在`test_fribbels_import_shows_filter_report_and_keeps_layout_stable`因`ModuleNotFoundError: test_gui_support`失败，日志位于`reports/test_logs_20260713/test_gui_app_v6_run{1,2}.log`；`tools/run_all_tests.py`因此退出1，不能宣称全量测试成功。本轮未修改GUI。

#### 主agent复核：v6 方法通过，转入独立 v7 方差分配（2026-07-14）

- v6 已消除 v5 的结构性失效：2,500个速度签名互斥且自然质量合计 `0.275`，等于非鞋初始含速度的自然质量；候选主表 `Heroic22+ - M0` 与加权救回字段均为 `0.1862738996/10万体力`，权重口径一致。
- 当前 pilot 是每签名仅1条条件样本、单一seed。21个终局可达22+签名中仅14条样本同时满足基础回退stop和候选救回；41条候选救回中另有27条终局未达22，说明成功与失败资源路径均已被保留，但签名内部由部位、主属性、其余副属性和+12补词条造成的条件方差尚不能估计。
- 14个正贡献来自14个等质量签名，故正贡献ESS=14、最大单样本占比=1/14=7.14%。这属于最小pilot样本不足，不是新的权重错误；可以制定独立正式扩样，不能机械续跑或把pilot并入正式估计。
- 正式扩样前先穷举合法+6/+9/+12动作矩阵并合并完全等价候选。pilot显示 `M1/M2 0.5--5%/M4/M5` 同结果、`M2 10%/M3 0.002` 同结果、`M3 0.005/0.01` 与M0同结果；只有全状态动作序列完全相同后才能按别名组只跑一个代表。
- v7 使用全新schema、resume和seed。pilot仅用于冻结分配：终局22+且可能产生候选差异的签名提高条件样本数，终局未达22但候选会救回的签名仍需抽样以保留失败强化成本；可证明候选增量恒为0的签名不进入定向层。公共自然池需独立扩充，不能继续用64条估计完整联合账本。
- 完整执行说明：[Heroic中后期22速v7方差分配与正式扩样任务说明.md](Heroic中后期22速v7方差分配与正式扩样任务说明.md)。

### Heroic 中后期 22 速 v7 方差分配正式扩样（2026-07-14，进入人工确认）

- 先穷举合法 `normal_85 Heroic` 非鞋速度装备的 `+6/+9/+12` 完整二元动作序列，读取当前 `baili_marginal_low` 回退；得到三组完全等价候选：`M1/M2 0.5--5%/M4/M5`、`M2 10%/M3 0.002`、`M0/M3 0.005/0.01`。正式模拟只运行每组代表，别名保留在报告中。
- 新 study 为 `heroic_midgame_speed22_v7_variance_allocated_signatures`，schema `7`；v3--v6 分片均拒绝。v6 pilot 仅用于冻结分配，不并入正式估计。使用新 seed `20260724--20260728`，官方概率、移除稀有 1 速端点和初始值分布代理分别独立运行。
- 每 seed 公共自然池由64扩至128；2,500个互斥速度签名中，21个终局22+签名各64条条件样本，其余签名各2条失败成本样本，共6,302条定向样本。所有22+、GS、强化/出售/转换资源及节点账本使用同一签名质量。
- 正式主模型中，`M1_reachable_22/all_sets` 相对M0增加 `0.132990` Heroic22+/10万总体力，95% CI `[0.132022, 0.133958]`；原始救回22事件3,356、正贡献ESS 3,356、最大单样本0.03%、五个seed均非零。`M2_p22_10pct/all_sets` 为 `0.053294`，事件1,344、ESS1,344、最大单样本0.07%。两种初始值口径均通过同一离线闸门。
- 状态为 `offline_statistical_gate_passed_pending_full_suite`：统计闸门已通过，但全量测试成功码前不得进入人工确认、发布正式策略、修改前瞻批次或把22速折算进正式体系百里分。报告：[v7报告](reports/heroic_midgame_speed22_v7_formal_20260714.md)，原始结果：[v7 JSON](reports/heroic_midgame_speed22_v7_formal_20260714.json)，resume：[v7分片](reports/heroic_midgame_speed22_v7_formal_resume_20260714/)。
- 验证：研究、速度、策略、资源与联合账本相关113项测试通过；语法检查和`git diff --check`通过。`tools/run_all_tests.py`已运行但在既有Qt真实样本性能测试`test_manual_sample_performance.py`中止（隔离复跑在第二个Qt测试异常退出、无Python断言），本任务未修改GUI或真实样本管理器，故不能声明全量测试成功。

#### v7 后续收口：报告口径、全量测试与人工选择（2026-07-14，待执行）

- v7 候选只改变 `normal_85 Heroic` 中后期路线，但 `100,000总体力` 分母是维度裂缝红紫联合产出加圣女3-7补充的完整线路。当前主表将紫装救回增量、红紫联合GS和采样不足的绝对联合22+放在同一表中，容易误读。
- `Heroic22+` 必须明确标为紫装候选新增量；`联合22+` 只有在红装绝对基线精度足够时才能称为联合绝对产量。当前公共池仅每seed 128条、红装22+观测为0，不得解释为红装不会产22+；若不追加兼容的高精度基线，应把联合绝对22+标为`not_estimated`。
- 十万总体力账本继续明确显示：M0约38,974.255维度裂缝体力、61,025.745圣女体力、458.520653个来源循环，期望458.520653件Epic和1,636.885993件Heroic。候选增量按该联合分母解释，不是“十万紫装专用体力”。
- 当前两档待人工选择：均衡参考 `M2_p22_10pct/all_sets` 增加 `0.053294` 紫装22+/十万联合总体力，每新增1件约需137.469圣女体力、策略GS变化 `-0.013515`；激进参考 `M1_reachable_22/all_sets` 增加 `0.132990`，每新增1件约需213.783圣女体力、策略GS变化 `-0.058901`。不得由子agent代替用户选择。
- 上述件数不能与旧B方案的 `0.228514统一终局价值/100体力`直接比较，单位与分母均不同。B是Epic早期方案，v7是Heroic中后期附加路线，二者应组合而非互相替代。人工选择前必须在同一显式资源池和同一22速锚点下比较 `B+M0`、`B+M2 10% all_sets`、`B+M1 all_sets` 的联合价值率及成对区间。
- 仅作边际检查：若每件22速在统一价值尺度中至少值1点，M2的盈亏平衡仅需 `0.2536`点/件、M1仅需 `0.4429`点/件，因此两者相对各自M0均可能为正；但该近似没有重新结算B联合分母，不能替代同池重放结论。
- 隔离日志 `test_manual_sample_performance_run2/run3` 实际均为5项通过且 `EXIT_CODE=0`；统一全量入口的异常更像Qt进程/顺序波动，需在新任务中复现并收口。只允许修复测试生命周期、导入或隔离入口，不借机修改GUI业务逻辑。
- 完整任务：[Heroic中后期22速v7全量测试与人工确认准备任务说明.md](Heroic中后期22速v7全量测试与人工确认准备任务说明.md)。完成后由用户确认均衡/激进/维持M0，随后才可建立正式策略实现任务。

#### v7 人工确认准备完成（2026-07-14，等待用户选择）

- 报告已改为明确的完整联合线路口径：每`100,000`总体力均为维度裂缝红紫联合产出加圣女3-7补充。Heroic22+只报告为紫装候选相对M0新增；本轮Epic公共池未观测到22+，红紫联合绝对22+固定标为`not_estimated`，未把紫装新增量伪装成联合绝对量。
- 已在同一来源、同一Epic公共路径、同一Heroic公共池、同一显式资源结算和同一22速价值锚点下重放冻结的`B_global_current_gs`：B+M0 / B+M2 10% / B+M1。curve_baseline统一终局价值/100体力依次为`0.095374`、`0.095932`、`0.096434`；M2-M0成对95%区间`[0.000539, 0.000576]`，M1-M0为`[0.000967, 0.001152]`。
- 三档的紫装22+新增/10万联合总体力为：M0 `0`；均衡M2 `0.077158`（95% CI `[0.074966, 0.079351]`）；激进M1 `0.192582`（95% CI `[0.184550, 0.200615]`）。对应联合策略GS变化为`0`、`-0.015354`、`-0.071389`。22速仍未折算进正式体系GS。
- 新报告：[v7人工确认报告](reports/heroic_midgame_speed22_v7_formal_20260714.md)，原始结果：[v7 JSON](reports/heroic_midgame_speed22_v7_formal_20260714.json)。新study `heroic_midgame_speed22_v7_joint_value_confirmation`拒绝旧v7 resume，避免缺少B同池字段的分片混入。
- Qt收口：修复测试侧的延迟Qt对象清理，不改GUI或真实样本管理器业务逻辑。`test_manual_sample_performance`隔离连续两次通过；`test_gui_app`隔离连续两次通过。统一入口首次仅`test_gui_app.py`原生退出、无Python断言，后续两次均输出`ALL TEST FILES PASSED`且退出码0，日志：[run1](reports/test_logs_20260714/run_all_tests_v7_run1.log)、[run2](reports/test_logs_20260714/run_all_tests_v7_run2.log)、[run3](reports/test_logs_20260714/run_all_tests_v7_run3.log)。首次记录为未稳定复现的Qt环境波动。
- 研究器21项定向测试、语法检查和`git diff --check`通过；正式策略、跳值表、lambda、评分、DP、GUI业务逻辑和前瞻批次均未修改。现在只等待用户在“维持M0 / 均衡M2 / 激进M1”间作出人工选择；选择前不得发布正式策略。

#### Heroic 中后期22速 M1 正式策略实施（2026-07-14）

- 用户已人工确认选择激进档 `M1_reachable_22/all_sets`。正式规则只覆盖 `normal_85 Heroic` 非鞋、已有速度副属性的 `+6/+9/+12`；当 `baili_marginal_low` 基础策略停止且精确 `P22>0` 时救回继续。基础策略已经继续时，M1永不覆盖为停止。
- `+0/+3`、Epic、`rift_85`、鞋子、无速度副属性、跳值表、评分、DP lambda、转换规则和 Epic B 前瞻批次均不在发布范围内。
- 正式P22逻辑迁入 `src/e7_enhance/heroic_speed22_rescue.py`，不得从生产代码导入离线 `tools/`。后续记录验证结果及发布完成状态。

#### Epic 非速度 +0/+3 高置信度止损预检（2026-07-14，样本阻断）

- 新研究 `epic_high_confidence_stop_20260714` 已在调用精确 DP Oracle 前冻结 epsilon=`6.4e-09`（`64 * NUMERIC_ZERO_TOLERANCE`）、+0/+3 独立浅规则、SHA256记录级开发/冻结分割，以及每节点冻结验证至少24件、明确正效用误停必须为0的闸门。
- 已排除旧 Oracle/旧盲测的45个实例、旧0/48批次、旧resume和旧预测哈希；条件生成样本未加载。剩余真实范围为：+0开发174件、+0冻结99件；+3开发0件、+3冻结0件。
- 因+3没有独立真实样本，预检直接失败。未执行Oracle、五seed维度裂缝Epic/Heroic联合资源池或每10万体力指标；未将未计算指标写零，未创建48件批次，正式Epic策略保持不变。
- 产物：[报告](reports/epic_high_confidence_stop_20260714.md)、[预检JSON](reports/epic_high_confidence_stop_20260714.json)、[可复跑工具](tools/research_epic_high_confidence_stop.py)。新增回归测试验证epsilon边界、+0/+3独立规则及四个必要条件的安全覆盖语义。
- 后续只能先独立收集至少64件新的真实`normal_85 Epic`非鞋、初速<2、+3快照；固定同一分割后预计提供至少24件冻结验证。禁止用旧45件、旧盲测、合成样本或已+6状态回填该缺口。收满并冻结前不再调阈值，也不运行联合资源池。

#### Epic 非速度 +3 前瞻采集器（2026-07-14，开始收集）

- 旧记录已作废：此前“64--127允许Oracle初筛”的表述不再有效，统一以128件manifest冻结隔离规则为准。
- 每个配对保留+0/+3完整快照、实例ID、规范化指纹、套装/部位/主属性/副属性、+3命中与增量、roll history、各副属性roll数、gear_source、策略规则hash、带时区采集时间、`sample_source`与`collected_blind`。无法可靠关联、缺少ID/命中历史、旧来源、重复、鞋子、rift或速度硬路线均记录排除原因，不混入有效样本。
- JSON以原子替换写入，可中断续收；实例ID+状态指纹双重去重。采集器不导入旧Oracle、旧盲测或旧0/48批次，不调用点击/ADB/Airtest，不改变GUI和正式策略动作。
- 0--127有效配对始终仅收集；64只作进度显示，禁止Oracle初筛、候选预测和阈值研究；128后方可先生成一次性manifest，按`SHA256(sample_id)`固定为64开发/64冻结验证，包含样本ID、分组、规则hash、数据SHA与纳入/排除记录，之后不可移动样本分组。
- 操作说明：[采集报告](reports/epic_plus3_prospective_collection_20260714.md)。本轮不运行Oracle或联合资源池。

#### Epic 非速度 +3 前瞻样本采集（2026-07-14，待执行）

- 当前研究阻断已转为数据采集任务，完整说明：[Epic非速度+3前瞻样本采集任务说明.md](Epic非速度+3前瞻样本采集任务说明.md)。
- 只收录新的真实 `normal_85 Epic` 非鞋、`+0` 初速不存在或小于2、由当前正式策略到达 `+3` 的成对快照；所有合格装备连续盲收，实例 ID 与规范化指纹双重去重。
- 64件有效 `+3` 快照仅为进度里程碑；推荐且唯一允许研究准备的门槛为128件，以获得先冻结的开发64件、冻结验证64件。达到128前不允许 Oracle 初筛。
- 本任务不运行 Oracle、五 seed 联合资源池或每十万体力研究，不创建新48件策略验证批次，不修改正式 Epic 策略、DP、资源、跳值、GUI 主流程、OCR、ADB、Airtest或自动点击。

#### Epic 非速度 +3 采集运行时接线任务说明（2026-07-14，已由后文完成记录覆盖）

- 复核发现当前仅完成采集核心：`record_observation` 的正式调用者为0，只有手工 `--record-json` CLI和测试会写入数据集；仅运行 `--progress` 不会自动增加样本。
- 下一任务说明：[Epic非速度+3运行时采集接线任务说明.md](Epic非速度+3运行时采集接线任务说明.md)。先确认带稳定实例ID和完整状态的真实装备输入路径；若不存在则报告阻断，不得用手工表单、模拟或旧样本替代。
- 推荐把合格 `+0` 保存为未计数 pending，由同实例真实 `+3` 的后续出现证明实际强化并形成有效 pair，禁止在仅展示建议时伪造 `actual_enhancement=true`。
- 接线只允许增加不可见观察钩子；采集失败不得影响正式建议，不修改GUI布局、业务动作、OCR、ADB、Airtest或自动点击。本阶段仍不运行Oracle或策略研究。

#### Epic 非速度 +3 样本冻结隔离修正任务说明（2026-07-14，已由后文完成记录覆盖）

- 复核发现64件时允许Oracle初筛会污染未来128件哈希分组：最初64件中的已查看样本可能被重新分入`frozen_validation`。
- 权威修正为128件前始终`collecting_blind`；64件只作进度里程碑。达到128件后先生成一次性manifest，再只对64件`development`运行Oracle；冻结验证组在候选和预测哈希冻结前不得读取。
- 修正任务：[Epic非速度+3样本冻结隔离修正任务说明.md](Epic非速度+3样本冻结隔离修正任务说明.md)。此前所有“64--127件允许Oracle初筛”的计划和报告表述由本节覆盖。
- 本修正只改采集状态机、manifest约束、文档和测试，不查看样本效用，不修改正式策略或GUI业务逻辑。

#### Epic 非速度 +3 样本冻结隔离修正（2026-07-14，完成）

- 状态机已收口：`0--127`有效pair始终为`collecting_blind`；`64`只显示进度；`>=128`且manifest缺失为`ready_to_freeze_manifest`；manifest存在后为`manifest_frozen`并显示固定64开发/64冻结验证数量。
- `freeze_manifest()`写一次后复用原文件。超过128件时按固定哈希选入前128，剩余pair逐项写入`not_included_pairs`及`outside_initial_freeze128`原因，禁止静默遗漏。
- 新增后续研究入口约束：`development_pairs_from_manifest()`必须显式提供有效manifest且只返回64件开发样本；`frozen_validation_pairs_from_manifest()`在候选和预测哈希冻结前固定拒绝。当前没有Oracle工具接入该数据集。
- 回归测试覆盖64件仍盲收、64--127不可冻结、128冻结64/64、write-once、溢出排除、仅开发集读取与冻结验证拒绝。本轮未运行Oracle、候选研究或联合资源池，未修改正式策略或GUI业务逻辑。

#### Epic 非速度 +3 采集运行时接线（2026-07-14，完成）

- 可信输入已确认并接入：主窗口的 `load_forms_from_path()` 仅在 `load_gear_collection_with_report()` 判定为完整 Fribbels 导出时启用观察。该路径由原始导出保留 `ingameId -> instance_id`、完整四副属性、强化等级、roll history 和装备来源；手工表单、验收样本、回放和原生 JSON 会清空可信指纹集合，不触发采集。
- `+0` 不再要求或写入 `actual_enhancement`，只根据已经生成的正式建议保存 pending。后来同一实例的真实 `+3` 状态在下一次建议前出现，且+3命中可唯一由两快照推导时，才形成有效 pair；pending 不计入64/128门槛或manifest。
- 观察发生在原建议结果生成之后；适配器只读取该结果，不重算`advise_gear`，异常被吞掉为内部诊断，建议、`next_check_at`、debug、GUI布局、OCR/ADB/Airtest和点击流程均不变。当前导入状态必须与原始Fribbels状态指纹一致，手工修改后不会被采集。
- CLI 现在只允许进度与manifest操作，移除手工`--record-json`写入路径。当前有效pair进度仍为0；本轮没有运行Oracle、策略研究、联合资源池或创建48件批次。

#### Epic 非速度 +3 真实盲收（2026-07-14，转为长期实测复核：0/128）

- 用户确认旧`+0`装备已出售，无法再补采对应`+3`。采集器只对未来新装备继续盲收，用作长期实测一致性复核，不再作为当前Epic研究硬阻断。
- `64`件只作为进度里程碑，不运行Oracle；收满`128`件后先生成一次性manifest，随后只对64件开发组启动下一项Oracle研究。
- 采集期间冻结`advise_gear`和`lightweight_prediction`的实现及其源码哈希；若正式早期策略确需修改，必须关闭当前批次并建立新collection，禁止混用不同规则哈希。
- 进度命令：`python -B tools/collect_epic_plus3_prospective.py --progress`。权威操作说明：[采集报告](reports/epic_plus3_prospective_collection_20260714.md)。

#### Epic 非速度 +3 官方概率精确分支研究（2026-07-17，已完成，详见下节联合结果）

- 当前研究改用保存的真实`+0`自然胚子分布。旧预检错误地将规范化`boot`与`Boots`比较，误计24件鞋子；按本任务明确非鞋范围修正为开发157件、冻结92件。对每件装备按四条副属性目标和STOVE官方离散跳值概率精确枚举`+3`全部合法分支，装备是否已出售不影响条件期望。
- 精确分支必须按概率加权，不能当成等权合成装备；报告明确称为“真实+0条件下的模型精确+3分支”，未来真实pair只作长期实测复核。
- 权威任务：[Epic非速度+3官方概率精确分支研究任务说明.md](Epic非速度+3官方概率精确分支研究任务说明.md)。先开发组研究并冻结候选，再一次性读取冻结组；通过节点闸门后才运行五seed联合资源池。
- 现有模拟器均匀`roll_range()`和旧离线`OFFICIAL_ROLL_DISTRIBUTIONS`不得直接复用；后者对固定攻击33/46概率处理错误。正式跳值表和生产策略本轮不修改。

#### Epic 非速度 +0/+3 精确分支联合资源池（2026-07-17，等待人工确认）

- 开发157件、冻结验证92件均已完成。冻结候选哈希为`78567df462a783ba739d9fcb56735ec4ecaf3797b576b83975293045d95a3966`；`+0/+3`明确正效用误停均为0、召回均为100%，通过节点安全闸门。
- 五seed联合资源池已完成，共1,645个原子分片：每个seed 249件Epic和80件Heroic。续跑最终`scheduled=0`，没有重算已完成分片。
- Heroic产出基线下，正式体系价值率均值从`0.069678`提高至`0.070542/100体力`，候选减当前95%区间`[0.000662, 0.001067]`；产出`-20%/+20%`区间分别为`[0.000802, 0.001243]`和`[0.000562, 0.000935]`，全部大于0。
- 首seed十万总体力条件示例：正式体系百里分`81.107 -> 82.115`，22速`2.752 -> 2.786`，圣女体力`76173.149 -> 75876.766`，来源循环`280.316 -> 283.803`。绝对数条件于真实非速度Epic样本，只用相对差值作发布排序。
- 报告：[联合报告](reports/epic_exact_plus3_joint_pool_20260717.md)；原始结果：[联合JSON](reports/epic_exact_plus3_joint_pool_20260717.json)。相关11项测试与统一全量测试通过。
- 当前状态：`eligible_for_human_confirmation_not_released`。用户确认前不得修改正式策略。若确认发布，严格执行：[Epic非速度+0+3安全止损正式发布任务说明.md](Epic非速度+0+3安全止损正式发布任务说明.md)。

#### Epic 非速度 +0/+3 门槛矩阵控制变量研究（2026-07-17，已完成，未发布）

- 用户提出应比较`+0`与`+3`门槛间隔，例如均衡组合`12/17`。该意见成立：改变`+0`会改变进入`+3`的状态集合，必须同时做固定+0单节点敏感性和完整路线矩阵。
- 主矩阵预注册`T0={10,12,14}`、`T3={14,16,17,18,20}`，优先比较满足`T3>=T0+4`的组合；固定其他有效词条、终局概率、转换和资源参数。
- 上一候选已读取冻结92件，不能将其作为新矩阵的独立发布验证。矩阵筛选后需收集新的真实`+0` holdout，至少48件、建议64件；不要求实际强化到`+3`，按官方概率精确分支验证即可。
- 任务说明：[Epic非速度+0+3门槛矩阵控制变量研究任务说明.md](Epic非速度+0+3门槛矩阵控制变量研究任务说明.md)。本任务未修改正式策略。
- 新增独立研究器 `tools/research_epic_threshold_matrix.py`：开发组只读157件真实 `+0` 胚子，冻结 `T0={10,12,14}`、`T3={14,16,17,18,20}`；`+3`使用同一套STOVE官方离散分支并按概率加权。节点风险结果先于联合资源池，未通过节点闸门的路线不运行五seed资源池。
- 完整15组矩阵、`T0=12` 与 `T3=18` 控制变量及五seed联合资源池已完成。`12/17`在开发组精确节点审计中存在 `+0=2`、`+3=16` 个明确正效用误停，未进入资源池；唯一通过零误停节点闸门的是保守`10/14`。它在Heroic基线产出下相对当前正式策略的正式体系价值率差为`[0.000492, 0.000798]/100体力`，但本轮仅冻结候选，仍需新的独立 `+0` holdout后才可讨论发布。产物：[报告](reports/epic_threshold_matrix_20260717.md)、[JSON](reports/epic_threshold_matrix_20260717.json)。本任务未修改正式策略。

#### Epic 非速度 +0/+3 门槛矩阵 Phase B（2026-07-18，已完成正式研究，未发布）

- Phase A `epic_threshold_matrix_20260717` 已标记为 `global_uniform_phase_a`，保留原报告与分片，不覆盖、不重跑；`10/14`仅作为全局保守对照。
- Phase B 使用候选体系一致的 `current_pre_reforge_gs`、category、terminal probability、conversion value；采用新schema/new resume，所有15组全局门槛及四个体系组的 `-2/0/+2` 单因素行均进入离线联合资源池。节点闸门只限制发布，不限制比较；新holdout收集暂停。
- formal `runs=10` 已完成 `22,380/22,380` 个哈希一致分片。候选层只读取 `joint_v2/base/<rank>/seed-*/gear-*.json` 公共基础缓存，不重复正式路径；pilot `runs=1` 与正式结果完全隔离。
- 正式结果显示：当前策略每10万总体力正式价值 `68.382`；全局 `10/14` 为 `68.807`，全局 `12/17` 为 `69.191`。修正口径后所有研究候选均存在明确正效用误停，不能沿用 Phase A 的“10/14 零误停”发布结论。
- 产物：[Phase B 正式报告](reports/epic_threshold_matrix_phase_b_formal_r10_20260718.md)、[正式 JSON](reports/epic_threshold_matrix_phase_b_formal_r10_20260718.json)。Phase B 定向测试、语法检查、`git diff --check` 与 `tools/run_all_tests.py` 全量测试真实退出码均为 `0`。

#### Epic 非速度 +0/+3 门槛矩阵 Phase C（2026-07-18，已完成离线比较，未冻结候选）

- 仅复用 Phase B formal runs=10 的基础分片，未重新模拟 +3 后正式路径，未修改正式策略、DP、资源模型、评分或 GUI，且未开始 holdout。
- 固定比较 current、全局 10/14、12/17、14/18，以及输出/高优先级小范围组合。离线效率排序与发布闸门分离；用户确认 Pareto 风险取舍前，不冻结候选哈希，也不设计新的分层 holdout。
- 完整任务与收口规则：[Epic非速度+0+3门槛矩阵PhaseC组合收口任务说明.md](Epic非速度+0+3门槛矩阵PhaseC组合收口任务说明.md)。结果见 [Phase C 报告](reports/epic_threshold_matrix_phase_c_20260718.md) 与 [原始 JSON](reports/epic_threshold_matrix_phase_c_20260718.json)。
- `balanced_output_m2/m4` 分别保留全局 `12/17` 的 `84.14%/63.58%` 收益；`balanced_high_priority_m2` 保留 `66.80%`，且消除 `+0` 明确正效用误停，但 `+3` 仍有概率质量 `1.8` 的明确正效用误停。当前状态为 `completed_offline_pending_user_confirmation`。
- 主 agent 风险复核推荐 `balanced_output_m4`：它相对 `balanced_high_priority_m2` 每10万总体力只少约 `0.026` 正式百里分，但误停 utility loss 从 `0.042477` 降至 `0.005320`，regret 也更低。`balanced_output_m2` 被全局 `12/17` 以相同误停风险和更高效率支配，不再作为首选。该推荐尚待用户确认，未冻结候选。
- Phase C 复跑、语法检查、`git diff --check` 与 `tools/run_all_tests.py` 全量测试均通过，真实退出码 `0`。
- 用户于 2026-07-18 确认选择 `balanced_output_m4`：默认 `+0/+3=12/17`，纯输出 `+0/+3=8/13`。当前只批准进入独立 holdout，不代表正式发布。
- 下一任务已落盘：[Epic非速度+0+3均衡候选独立Holdout验证任务说明.md](Epic非速度+0+3均衡候选独立Holdout验证任务说明.md)。先冻结候选、关键代码哈希、固定64件盲收协议和验证闸门，再开始采集；收满前不得读取 Oracle 或效率指标。
- 本研究线不再新增 Phase D。若独立 holdout 未通过预注册闸门，则维持当前正式策略；不得使用该 holdout 回调门槛。

#### Epic Phase B/C 体系映射错误与纯坦门槛复核（2026-07-18，已完成离线研究，未发布）

- 在用户确认 `balanced_output_m4` 后复核发现，`system_group()` 将正式分类“输出(必爆)”写成“输出(必暴)”，导致开发集6件必爆输出装备进入 `unknown`，没有应用纯输出保护。current 与全局门槛对照不受影响；所有输出/高优先级组合、Phase C Pareto 和 `68.8965` 条件总百里分结论失效。
- 157件开发样本按正式分类为：输出61、输出(必爆)6、抗坦35、纯肉18、命坦15、半肉21、双效1。纯坦合组68件可用于粗门槛研究，但不足以发布三个坦克子体系或体系×部位门槛；半肉和双效同样不足。
- `system_group("输出(必爆)")` 已修复；新增正式分类全集、同音字/简繁体/括号差异未知分类测试。开发样本实际 `unknown=0`，正式分类进入unknown会使研究失败。
- 新 schema/candidate hash 已只用于修正研究。Phase B formal runs=10 的 `1,185` 个公共基础分片与对应 runs=10 对照分片经正式路径、跳值、资源模型哈希验证后只读复用；没有重跑正式后续路径。
- 修正后开发组为pure_output `67`、pure_tank `68`、bruiser `21`、dual `1`。输出组合和“固定输出8/13，纯坦T0={8,10,12}、T3={13,15,17}”的9组均已完成五seed联合资源池比较。纯坦 `8/13`、`8/15`、`8/17`均为总明确正效用误停概率质量`0.3`；其正式百里分/10万总体力分别为`68.697`、`68.723`、`68.769`，全局12/17为`69.191`且风险`3.7`。不自动选择候选。
- 结果：[修正报告](reports/epic_threshold_matrix_phase_bc_mapping_repair_20260718.md)、[原始JSON](reports/epic_threshold_matrix_phase_bc_mapping_repair_20260718.json)、[研究器](tools/research_epic_threshold_matrix_phase_c.py)。holdout任务状态为`blocked_pending_user_selection_after_mapping_repair`；用户确认前不冻结、采集或读取holdout，正式策略不变。
- 能力审计结论保持：现有官方分支枚举、加权统计、成对区间、原子缓存和资源池工具足以完成当前研究。当前风险是开发胚子分布代表性和纯坦仅68件，不是工具能力不足。

#### 已重铸库存套装权重与每点百里分体力对比（2026-07-18，前置已完成，待执行）

- 用户提供只读 Fribbels 导出 `D:\VScode\E7-tools\gear_data\0橙U子0\json\gear_fribbels_20260610_182000.json`，SHA-256 为 `2BB32F384FBE3D926F5274D50AB24DC8766C2AF87AA1ABC0B6946A47EEAE1DB7`。按 `level=90/enhance=15` 识别765件已重铸装备，其中Epic 616、Heroic 149。
- 套装保留分布以 `raw.f` 计数：速度235、爆伤98、免疫63、生命63、激流50，其余14套合计256。该分布只作为账号保留/需求权重，不解释为自然掉率；模拟副属性仍使用合法自然生成分布。
- Phase C 错误分组下 `68.8965分/10万体力` 折合约 `1451.45体力/分`，与发布 DP 常量 `799.2` 和用户记忆约700不是同一口径，当前不得据此判断策略效率减半。
- 完整任务：[已重铸库存套装权重与百里分体力效率对比任务说明.md](已重铸库存套装权重与百里分体力效率对比任务说明.md)。前置体系映射与纯坦门槛复核已完成；按库存需求权重、19套等权、Epic/Heroic权重敏感性和top4等权场景，使用固定 `85` 体力联合来源批次和同一分子分母重算每1点正式百里分所需总体力，并与旧 `799.2/约700` 逐项审计；不计算或反推副本自然掉率。
- 比较候选固定为当前正式策略、全局12/17、输出8/13+纯坦10/17及输出8/13+纯坦12/17；完成前不冻结128件holdout。

#### 已重铸库存套装权重 R50 正式研究（2026-07-18，绝对成本不稳定）

- R50完成190/190个`set×rank×seed`分片。库存总权重下四策略每点百里分体力约为当前8631.07、全局12/17为8570.43、输出8/13+纯坦10/17为8593.88、输出8/13+纯坦12/17为8591.28。
- 五个场景绝对成本均为`absolute_cost_unstable`，不得更新799.2、约700或冻结候选。R50只作扩样前参考。
- 当前联合线路把Heroic强化资源纳入圣女补充，而旧799.2是Epic-only历史DP常量；下一步必须先做五层旧口径桥接，再追加独立seed/block，不能只机械增加同五seed样本。
- 本轮主 agent 已补跑全量测试、语法检查和`git diff --check`，真实退出码均为0；研究器专用测试待后续任务补齐。
- 下一任务：[已重铸库存套装权重绝对成本扩样与旧口径桥接任务说明.md](已重铸库存套装权重绝对成本扩样与旧口径桥接任务说明.md)。128件holdout继续暂停。
- 用户确认策略评价应以已重铸库存代表的常用套装需求为主。765件库存中前4套占60%、前5套约66.5%、前10套约86.7%，偏门后9套仅约13.3%；扩样后必须重跑四策略聚合，库存全部权重为主排序、top4等权为主要敏感性、19套等权只作机制对照。
- 用户再次确认不能沿用偏门套装等权样本的策略排序。处理结论是：扩样后重新聚合四套策略属于硬闸门；主结论使用`reforged_inventory_all`，`top4_equal`只做常用套装敏感性，`uniform_observed_sets`不得参与候选选择。R50已经按已重铸库存分布加权，当前正式策略的19套等权与库存权重结果分别为`8643.51`和`8631.07`体力/分，仅差约`0.14%`，故当前绝对成本异常不能归因于偏门套装；应由五层旧口径桥接和独立seed/block扩样解决。新增条件样本仍按套装等量覆盖，汇总时再施加库存权重，避免因直接少抽偏门套装而增加方差。

#### 已重铸库存套装权重绝对成本扩样与旧口径桥接（2026-07-18，完成，未发布策略）

#### 已重铸库存套装权重 R400 收口审计修复（2026-07-18，R800 已触发）

- 权威任务说明：[已重铸库存套装权重R400收口审计修复任务说明.md](已重铸库存套装权重R400收口审计修复任务说明.md)。新审计器只读合并 R50 的 5 个 seed cluster 与 R400 的 20 个 block cluster；R200 是 R400 前缀，未读取、未计数。R50/R400 分片及历史报告均未覆盖。
- 合并后库存主权重当前正式策略为`7849.35`体力/正式百里分，正值约束 cluster bootstrap 95% 区间`[6707.59, 9118.88]`，相对半宽`15.36%`，超过10%绝对成本闸门。此前 R400 单独稳定的“停止R800”结论已被后文覆盖，不能继续使用。
- 按预注册阶梯恢复 R800。执行前已记录预计规模`20 block × 19 set × 2 rank = 760`个新 `r800` 分片、每个分片800条条件样本；R800 只作为同一block的累积阶段读取，禁止与R200/R400重复相加。完成后重新审计 R50+R800。
- R800 已完成`760/760`，新审计器只读合并 R50+R800，R200/R400均未计入。库存主权重当前策略为`7811.16 [6674.63,9031.69]`体力/正式百里分，相对半宽`15.09%`；`top4_equal`为`6605.05 [5747.09,7607.74]`、相对半宽`14.09%`。均未通过10%闸门。报告已补齐 top4、原生75+、转换75+、22速、资源体力和循环。
- 当前任务仅授权至 R800，未授权 R1600；因此停止生成新轨迹。旧 R400 报告仅作修正前历史对照，详见 [R800收口审计报告](reports/reforged_inventory_set_weight_r800_closure_audit_20260718.md) 与 [JSON](reports/reforged_inventory_set_weight_r800_closure_audit_20260718.json)。
- 收口验证完成：审计/桥接/资源相关定向测试共 28 项通过；语法检查和 `git diff --check` 均为退出码 `0`；`tools/run_all_tests.py` 真实退出码 `0`、输出 `ALL TEST FILES PASSED`。一项 GUI 用例首次 Qt 原生退出后由既有隔离重试机制通过，未修改 GUI。R1600 仍未获授权，不得继续生成轨迹。

- 权威任务说明：[已重铸库存套装权重绝对成本扩样与旧口径桥接任务说明.md](已重铸库存套装权重绝对成本扩样与旧口径桥接任务说明.md)。R50 的 190 个旧分片和历史报告保留只读；新研究使用独立 `block/seed/set/rank` 原子分片，未覆盖 R50。
- R200 的完整联合线路相对半宽为`10.8%`，因此按预注册阶梯执行 R400。R400 完成 20 个独立 seed/block cluster、每套每品质每 cluster 400 条条件样本、`760/760` 分片；库存总权重完整联合线路当前正式策略为`8564.05`体力/正式百里分，正值约束 cluster bootstrap 95%区间`[7861.09, 9312.08]`，相对半宽`8.5%`，达到绝对成本稳定闸门，不继续 R800。
- 五层桥接（当前策略）：发布 Epic-only 回放`612.60 [586.55,642.11]`；仅换库存权重`488.26 [461.68,519.06]`；同旧资源分母改用当前终局价值`591.20 [548.30,639.86]`；85体力联合来源仅计Epic价值`1695.71 [1565.19,1843.75]`；Heroic全停不变；完整 Heroic M1 联合线路`8564.05 [7861.09,9312.08]`。22速未折算进正式百里分。
- 结论：库存套装需求权重不是绝对成本巨大差异主因；终局价值定义、装备获取/圣女资源边界和 Heroic M1 强化资源均有影响，其中 Heroic M1 是联合线路的最大额外成本项。`799.2`未复现，状态`historical_constant_not_reproduced`，不得把此差异归因于策略效率下降，也不更新正式DP常量或“约700”。
- 候选仍是离线对照：库存总权重下全局12/17相对当前策略的共同路径成对增量为`0.000088 [0.000081,0.000095]`正式百里分/100总体力；其余候选也在报告中保留。未冻结候选、未启动128件holdout，未修改正式策略、DP、资源模型、评分、跳值、GUI、OCR或自动化。
- 产物：[R400报告](reports/reforged_inventory_set_weight_bridge_r400_20260718.md)、[R400原始JSON](reports/reforged_inventory_set_weight_bridge_r400_20260718.json)、[研究器](tools/research_reforged_inventory_set_weight_bridge.py)、[回归测试](tests/test_research_reforged_inventory_set_weight_bridge.py)。
- 验证：专用研究器 7 项与相关资源/Phase C 共 24 项通过；语法检查、`git diff --check` 通过；`tools/run_all_tests.py` 真实退出码 `0`、输出 `ALL TEST FILES PASSED`。一项 GUI 单例曾原生退出一次，既有隔离重试后通过，未修改 GUI。
- 主 agent 收口审计将本节状态降为“R400 数值完成、预注册收口未完成”：研究器仅把 R50 记为只读引用，没有把 R50 的 5 个 seed cluster 合并进最终四策略聚合；人类报告遗漏已存在于 JSON 的 `top4_equal`，也未输出预注册的原生75+、转换75+和22速。`8564.05` 暂仅代表 R400 新分片结果，停止 R800 需等待 R50+R400 合并后的相对半宽复核。
- 下一任务：[已重铸库存套装权重R400收口审计修复任务说明.md](已重铸库存套装权重R400收口审计修复任务说明.md)。先只读重聚合并补报告，不生成新轨迹；只有合并后相对半宽重新超过10%才恢复R800阶梯。正式策略与holdout继续不变。
- 用户新增子 agent 指令格式要求：今后固定标题 `给子 agent 的续作指令：` 后必须使用单独的 `text` 代码块承载全部可执行内容，首句统一写“请完整阅读并执行根目录《明确任务说明.md》。”；代码块外只作解释，不得混入需要复制的指令。该规则已同步到 `AGENTS.md` 与 `README.md`。
- R800 二次收口审计发现聚合错误：R50/R800 每 cluster 分别为50/800条，但旧审计先归一化后将25个cluster等权，使R50权重从应有的1.53846%放大到20%；同时每十万裂缝/圣女体力重复乘入25个cluster，报告合计错误为2,500,000。故`7811.16 [6674.63,9031.69]`、15.09%半宽及“需要下一扩样阶段”全部作废，R800分片本身保留有效。
- 只读诊断中，R800-only库存主权重为`8507.19 [7910.44,9131.16]`、相对半宽7.17%；按路径质量的R50+R800点估计约8449.15。这些仅用于确认错误影响，不是最终发布值。下一任务：[已重铸库存套装权重R800聚合权重与资源缩放修复任务说明.md](已重铸库存套装权重R800聚合权重与资源缩放修复任务说明.md)。先修正不等样本质量与十万体力守恒，禁止生成新轨迹或运行R1600。

#### 已重铸库存套装权重 R800 聚合权重与资源缩放修复（2026-07-18，已完成，未发布策略）

- 权威任务说明：[已重铸库存套装权重R800聚合权重与资源缩放修复任务说明.md](已重铸库存套装权重R800聚合权重与资源缩放修复任务说明.md)。先补失败测试后，以 schema `2` 只读合并 R50 五个 `sample_mass=50` seed cluster 与 R800 二十个 `sample_mass=800` block cluster；R200/R400均未读取、未计入，未生成新轨迹。
- 全部分子、分母、资源和终局产量均按实际 sample_mass 汇总；绝对成本与成对差值均使用来源分层 cluster bootstrap，保持 R50/R800 的设计质量。库存主权重当前正式策略为`8449.15 [7879.12,9083.25]`体力/正式百里分，相对半宽`7.13%`；`top4_equal`为`6769.57 [6325.45,7284.80]`、相对半宽`7.08%`。两者均通过`<=10%`绝对成本闸门。
- 每个场景和策略的裂缝体力加圣女体力均为`100000`（仅浮点误差）；新报告已补齐四策略、top4、原生75+、转换75+、22速、质量/方差与资源守恒诊断：[报告](reports/reforged_inventory_set_weight_r800_weighted_closure_audit_20260718.md)、[JSON](reports/reforged_inventory_set_weight_r800_weighted_closure_audit_20260718.json)。旧R800报告状态为`superseded_due_to_unequal_cluster_weight_and_resource_scaling_error`，不得再用于扩样决策。

### 超高攻击/生命集中属性体系 v3 兼容重算（2026-07-20）

- 任务说明：[超高攻击生命集中属性体系兼容修复与v3重算任务说明.md](超高攻击生命集中属性体系兼容修复与v3重算任务说明.md)；研究器：[research_epic_concentration_rescue_exact.py](tools/research_epic_concentration_rescue_exact.py)。
- v2 因候选救回路径未执行体系兼容过滤而失效；v3 使用 schema `3` 和全新 resume，完成 `1185/1185` 分片（157 Epic、80 Heroic、5 seed、每件10条主路径），不读取 Holdout，不修改正式策略及其他业务模块。
- v3 在每个 `+6/+9/+12` 节点读取当前 selected candidate 的正式分类、合法性与有效词条；生命集中阈值为 `37.7363`，官方概率 DP 采用 `P>=10%`。
- 五 seed 成对正式价值增量 95% 区间为 `[0.000111, 0.001693]` 百里分/100体力；区间下界大于零，当前状态为 `health_p975_balanced_positive_requires_independent_validation`。这不是发布结论，下一步只能另建独立验证任务。
- 每100,000总体力候选相对基线的正式百里分增量为 `0.9019 [0.1110, 1.6927]`，新增生命集中装备为 `2.2455 [0.8541, 3.6370]` 件；均为固定研究池条件值，不代表账号全装备效率。
- 五个 seed 均满足裂缝体力 + 圣女体力 = `100000`；同一 resume 零新增续跑为 `scheduled_shards=0` 且汇总一致。报告：[v3报告](reports/epic_concentration_rescue_v3_system_compatible_20260720.md)，JSON：[v3原始结果](reports/epic_concentration_rescue_v3_system_compatible_20260720.json)。
- v2 的正式淘汰结论标记为“已被 v3 覆盖，仅作历史对照”；第三项策略覆盖审计、Holdout 和正式策略修改均保持暂停。
- 本任务到此收口，不运行R1600、不冻结候选、不启动或读取holdout，正式策略、DP、资源模型、评分、GUI、OCR和自动化均未修改。
- 验证：新增审计器专用回归 `12` 项通过；语法检查与`git diff --check`退出码均为`0`；`tools/run_all_tests.py`最终工作区真实退出码`0`并输出`ALL TEST FILES PASSED`。GUI 用例由统一入口隔离运行，未修改 GUI。

#### 超高攻击/生命集中属性有效词条绑定 v4 重算（2026-07-20）

- 唯一权威任务说明：[超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md](超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md)；研究器：[research_epic_concentration_rescue_exact.py](tools/research_epic_concentration_rescue_exact.py)。
- v3 结果因有效生命词条未绑定 DP/剪枝/缓存/终局命中而作废。v4 使用 schema `4`、全新 study/resume，完成 `1185/1185` 分片；v2/v3 分片、区间和独立验证资格未复用。
- v4 在每个 `+6/+9/+12` 节点按 selected candidate 的 exact `CATEGORY_RULES`、sourceRow、setGroup、validGroup 读取 `health_valid_keys`，并将同一上下文传入初始值、概率、剪枝、缓存键、Heroic +12 和终局命中。无效 `hpFlat` 保留为命中槽位但不计集中 GS。
- v3 无效词条额外救回质量为 `0.001901 [0.001785, 0.002017]`/联合批次；v4 每十万总体力新增生命集中装备 `1.9997 [0.6424, 3.3570]` 件。
- 五 seed 成对正式价值增量 95% 区间为 `[0.000031, 0.001715]` 百里分/100体力，状态仍为 `health_p975_balanced_positive_requires_independent_validation`。按本轮限制不设计独立验证、不进入第三项策略覆盖审计、不修改正式策略。
- 五个 seed 均满足裂缝体力 + 圣女体力 = `100000`；同一 v4 resume 零新增续跑为 `scheduled_shards=0` 且汇总一致。报告：[v4报告](reports/epic_concentration_rescue_v4_bound_health_20260720.md)，JSON：[v4原始结果](reports/epic_concentration_rescue_v4_bound_health_20260720.json)。
- 验证：v4 专用回归 `15` 项通过；`tools/run_all_tests.py` 最终真实退出码 `0` 并输出 `ALL TEST FILES PASSED`；语法检查、`git diff --check` 和资源不变量检查均通过。

#### Epic 非速度 +0/+3 均衡候选独立 Holdout 验证（2026-07-18，盲收中）

- 权威任务说明：[Epic非速度+0+3均衡候选独立Holdout验证任务说明.md](Epic非速度+0+3均衡候选独立Holdout验证任务说明.md)。候选已只写一次冻结为`output_8_13_tank_10_17`：默认`+0/+3=12/17`、`pure_output=8/13`、`pure_tank=10/17`；冻结同时记录候选、体系映射、Phase B/C、官方+3概率、正式DP、资源和评分哈希及预注册闸门。
- 新的独立采集模块与CLI不复用旧+0/+3成对采集器或旧manifest，也不接入GUI、OCR、ADB、Airtest或自动化。它只接收新完整 Fribbels `+0` 快照，按实例ID与规范化指纹双重排除历史与批内重复，且不计算候选动作、Oracle、风险或效率。
- 冻结文件：[freeze](samples/epic_output_8_13_tank_10_17_holdout_freeze_20260718.json)；独立collection：[collection](samples/epic_output_8_13_tank_10_17_holdout_collection_20260718.json)。当前状态`collecting_blind 0/128`，64件仍只显示进度；尚未创建manifest、未读取任何验证输入、未生成验证报告。128件后才允许原子生成64/64 manifest。
- 正式策略、DP、资源模型、评分、跳值、GUI业务逻辑、OCR、ADB、Airtest、自动化和holdout历史数据均未修改。
- 验证：冻结/隔离/去重/状态机失败回归及相关 Phase B/C 定向测试共13项通过；语法检查通过；`tools/run_all_tests.py`真实退出码`0`并输出`ALL TEST FILES PASSED`。当前仍为`collecting_blind 0/128`，不得读取或计算任何holdout指标。
- 主 agent 最终审计接受本轮统计收口：sample_mass、来源分层bootstrap和十万体力守恒一致，库存/top4相对半宽均小于10%，不运行R1600。候选效率方向在两种权重下相同，但仍须结合Phase B/C误停风险由用户重新选择。
- 三档对照：全局12/17每10万总体力增加约0.0878正式百里分、误停质量3.7；输出8/13+纯坦10/17增加约0.0499、误停质量0.3；输出8/13+纯坦12/17增加约0.0539、误停质量约2.2。10/17相对12/17仅少约0.0040分，却减少约1.9误停质量，主agent推荐`output_8_13_tank_10_17`作为128件独立holdout候选。
- 上述等待选择状态已被用户确认覆盖：用户选择`output_8_13_tank_10_17`进入独立holdout，规则为默认12/17、纯输出8/13、纯坦10/17。Holdout任务状态更新为`ready_to_freeze_output_8_13_tank_10_17`；旧`balanced_output_m4`与旧64件冻结表述仅作历史对照。下一步先生成候选冻结文件，再按128件盲收和64/64开发/验证隔离执行；冻结前不得读取指标或运行Oracle，正式策略仍不变。
- 用户询问8449体力/分是否由无速度范围导致。结论：研究器对Epic/Heroic均条件生成非鞋、初始四条副属性无速度的胚子，非速度范围会降低正式终局价值分子，但8449同时包含85体力联合获取、Heroic M1强化与圣女补充，不能只归因于无速度。该值是非速度条件池完整联合成本，不是包含速度硬路线、鞋子和其他来源的账号全装备平均效率；报告22速为0是范围结构结果。
- Holdout准备审计发现输入入口未真正验证完整Fribbels：CLI会给任意JSON注入`full_fribbels_export`标记；真实导出的时间键为无时区`export_time`，当前采集器要求`exported_at`，首个真实文件会失败。collection仍为0/128，无样本污染；修复前暂停导入。下一任务：[Epic非速度Holdout完整Fribbels入口修复任务说明.md](Epic非速度Holdout完整Fribbels入口修复任务说明.md)。
- Holdout入口修复已完成：CLI 先用`is_fribbels_export()`验证完整Fribbels元数据和item结构，再将真实`export_time`规范化为`+08:00`的`exported_at`；普通JSON不再可被升格。collection保持`collecting_blind 0/128`，未导入真实样本。修复任务：[Epic非速度Holdout完整Fribbels入口修复任务说明.md](Epic非速度Holdout完整Fribbels入口修复任务说明.md)。
- 入口修复最终验证：Holdout/GUI support定向31项通过；真实Fribbels文件只读预检识别2467件并正确规范化时间，未写collection；语法、`git diff --check`与全量测试真实退出码均为0。Holdout状态恢复为`collecting_blind 0/128`，当前唯一允许动作是导入冻结后生成的新完整Fribbels导出并查看进度。
- 用户指定最新`player_data.json`要求导入。只读核对确认它是`fribbels_mumu_reader`在冻结后生成的完整容器快照，2565件items/equipment均保留Fribbels raw字段；需先增加可信wrapper验证、使用`metadata.imported_at`并补既有`normal_85`默认来源，再执行首次导入。任务：[Epic非速度Holdout可信PlayerData快照适配与首次导入任务说明.md](Epic非速度Holdout可信PlayerData快照适配与首次导入任务说明.md)。
- 可信player snapshot适配与首次导入完成：严格验证reader元数据、2565件items/equipment数量和身份、完整raw结构及带时区imported_at；定向9项、语法、diff与全量测试均通过。正式导入接受6件，当前`collecting_blind 6/128`、剩余122；排除计数为历史实例277、非+0 692、范围外1514、鞋21、速度硬路线55。未生成manifest或读取任何holdout指标。

#### 新套装全力/弱化映射与 Holdout 影响核对（2026-07-18，已完成）

- 任务说明：[新套装全力弱化映射与Holdout影响核对任务说明.md](新套装全力弱化映射与Holdout影响核对任务说明.md)。已核对名称别名、运行时 `SET_GROUPS` 和基础百里表：全力进入输出/必爆输出/半肉（血防），弱化进入命坦；不存在“新套装未识别”导致的当前样本减少。
- 可信快照共2565件，Holdout有效接受6件；全力总数15、弱化总数0，但符合 `normal_85 Epic` 非鞋、初始速度<2、`+0` 四副属性范围的子集两者均为0。6件偏少来自历史/强化等级/范围/鞋子/速度路线排除，不得用该快照补入新套装样本。
- 新增规则加载回归，未修改正式策略、DP、资源模型、评分、GUI、OCR、自动化或 Holdout 冻结哈希。后续若修改套装映射，必须新建独立 Holdout 批次，不得污染当前 `collecting_blind 6/128`。

#### 新 +0 真实装备档案与 Holdout 隔离（2026-07-19，已实现）

- 任务说明：[新+0真实装备档案与Holdout隔离任务说明.md](新+0真实装备档案与Holdout隔离任务说明.md)。新增独立追加式档案，仅接收冻结后可信 `normal_85 Epic +0`，保留鞋子、速度路线和其他 Holdout 排除项，并标记 `holdout_candidate`。
- 档案与 `epic_output_8_13_tank_10_17_holdout_collection_20260718.json` 完全隔离，不记录策略动作或效率，也不改变当前候选哈希。旧 `+15`、Heroic 和非 `normal_85 Epic +0` 不进入档案。
- 模块：[epic_plus0_archive.py](src/e7_enhance/epic_plus0_archive.py)；CLI：[collect_epic_plus0_archive.py](tools/collect_epic_plus0_archive.py)；默认档案：`samples/epic_plus0_real_archive_20260719.json`。
- 已从可信快照导入22件新`normal_85 Epic +0`：6件同时符合当前Holdout、5件鞋子、11件速度硬路线。当前Holdout仍为`collecting_blind 6/128`，档案数据只作为后续研究原始样本，不回填验证集。

- 2026-07-19 03:10:33 新PCAP已通过现有reader生成快照并分别导入档案与Holdout；两边新增均为0。新旧快照装备ID均2565件，新增/删除/内容变化均为0，仅`imported_at`更新，游戏数据`export_time`仍为2026-06-14；档案保持22件，Holdout保持6/128。
- 2026-07-19 22:10:33 新完整Fribbels导出共2653件：真实档案新增355件、累计377件；Holdout新增122件、累计128/128，已生成不可变64/64 manifest，状态为`ready_for_frozen_validation`。当前只允许进入冻结哈希确认和验证准备，不得提前读取标签或效率指标。
- 当前“强化决策”状态：正式策略仍可运行，Heroic M1 已是正式实现；`output_8_13_tank_10_17` 只是已冻结候选，尚未通过独立 Holdout 验证，不能替换正式 Epic `+0/+3` 决策。

#### 当前全流程百里分效率口径（2026-07-19）

- 口径说明：[当前全流程百里分效率口径说明.md](当前全流程百里分效率口径说明.md)。最新稳定的库存主权重/current正式策略为每10万总体力`11.8355`正式百里分，即`8449.15 [7879.12,9083.25]`体力/分；裂缝/圣女体力约`14324.94/85675.06`，循环约`168.53`。
- 该数值是“非速度条件池的完整资源流程”：包含装备获取、Epic/Heroic、强化、回收、转换和圣女补充，但不包含速度硬路线、鞋子和其他来源。账号全部装备的总百里分效率仍为`not_estimated`，不得把`8449.15`称为账号全装备平均。
- 当前Holdout候选`output_8_13_tank_10_17`离线为`11.89`分/10万总体力、`8413.71`体力/分，但尚未通过128件独立验证，不替代current正式数据。

#### 超高攻击/生命集中属性额外决断（2026-07-19，待离线研究）

- 当前正式评分已将`atkPct/atkFlat/hpPct/hpFlat`按官方GS权重计入对应体系，普通高攻击/高生命已经受到纯输出、必爆输出、纯坦及相关坦克/半肉体系保护；但没有“单方向极端集中”独立价值或救回规则。
- 这类额外决断有研究价值，但不得直接发布。权威任务说明：[超高攻击生命集中属性救回研究任务说明.md](超高攻击生命集中属性救回研究任务说明.md)。先用已重铸库存P90/P95/P97.5定义集中GS阈值网格，再按官方自然跳值研究`+6/+9/+12`基线停止状态的10%概率救回和可达性参考。
- 集中值只计算副属性官方GS，不含主属性、套装加成或英雄面板；攻击和生命分别报告，OR只作组合敏感性。必须报告每10万总体力新增件数、每件体力、正式百里分损益、资源和五seed成对区间。
- 当前正式策略、DP、评分、资源模型、GUI、Holdout与新+0真实档案均不修改。研究未通过总体价值及分布稳健性闸门时，只允许增加debug提示，不允许自动救回。

#### MuMu 抓包读取与真实装备导入流程（2026-07-20，历史记录，已被 2026-07-21 新入口覆盖）

- 权威任务说明：[MuMu抓包读取与真实装备导入任务说明.md](MuMu抓包读取与真实装备导入任务说明.md)。读取器位于 `D:\VScode\E7-tools\Epic-7-tools.v1.2\game_read\fribbels_mumu_reader.py`，先通过 `adb devices` 获取当前实际 MuMu 地址，再传入 `--device`；禁止继续使用失效的 `<local-adb-endpoint-redacted>`。
- 标准在线参数为 `--seconds 150 --restart`；抓包结束后先检查 `gear_fribbels_*.json`、`player_data.json` 和 summary 是否落盘，再判断外层超时是否真实失败。已有 PCAP 必须先经读取器解码，不能直接导入项目。
- 2026-07-19 22:10:33 成功输出 `D:\VScode\E7-tools\gear_read\gear_fribbels_20260719_221033.json`：`item_count=2653`、`hero_count=372`、2,653 个唯一实例 ID且全部保留 `raw`，SHA-256 为 `A7D33AF99F1488AEC7CE49E01AED6AF67EBE04ED79D55F8CA35C0CAA7D4FE797`。本次真实档案新增355件（累计377），Holdout新增122件（累计128/128）；Holdout已满，后续只进入真实档案或新批次。
- MuMu规则已同步至 [AGENTS.md](AGENTS.md) 和 [MuMu任务说明](MuMu抓包读取与真实装备导入任务说明.md)：禁止硬编码旧ADB地址，先执行 `adb devices`；外层命令超时必须先检查落盘文件和完整性再决定重跑。
- 解码后的完整 JSON 先导入 `tools/collect_epic_plus0_archive.py`；只有新 Holdout 处于 `collecting_blind` 才允许导入 `collect_epic_balanced_holdout.py`。当前候选已 `128/128`、`ready_for_frozen_validation`，后续抓取不得写入旧 Holdout。
- 本流程只记录真实装备，不执行强化点击、不读取 Oracle、不修改正式策略；导入结果和异常原因必须同步写入本计划。

#### 距离 OCR 自动强化的当前评估（2026-07-19）

- 决策层已有标准Gear模型、规则/DP、资源模型、真实样本、PySide6 GUI与Fribbels导入；但OCR按钮仍禁用，没有截图语料、区域定位、OCR引擎、字段置信度或人工确认视图。Airtest仍是`NullAirtestAdapter`，ADB截图、窗口定位、点击、点击后回读、资源保护和紧急停止均未实现。
- 按子系统粗略估计，决策层完成七成以上，端到端安全自动强化约三成；距离只读OCR建议还有“截图/字段识别”和“影子真值验证”两个主要里程碑，距离无人值守还有人工确认单节点执行及受限自动循环共四个里程碑。
- 权威路线：[OCR自动强化实施路线与第一阶段只读识别任务说明.md](OCR自动强化实施路线与第一阶段只读识别任务说明.md)。只读OCR可与128件Holdout验证并行，因为它只负责截图转标准Gear；任何点击必须等待策略哈希冻结、整件字段一致闸门、`next_check_at`节点控制和点击后回读闭环通过。

#### 策略优先、GUI/OCR 延期（2026-07-19，用户确认）

- 用户决定先完成强化策略V1，再继续GUI、OCR、ADB或Airtest。现有PySide6 GUI只保留为手工输入、debug、样本管理和验收工具，不删除也不扩展自动化。
- 权威任务说明：[强化策略V1收口与GUI延期任务说明.md](强化策略V1收口与GUI延期任务说明.md)。收口顺序固定为：128件Holdout验证 -> 超高攻击/生命集中属性研究 -> 品质/来源/部位/节点覆盖与完整口径审计 -> 策略V1规则/哈希/回归冻结。
- 《OCR自动强化实施路线与第一阶段只读识别任务说明.md》状态改为暂停。只有V1冻结、`next_check_at`节点契约和全部回归通过后才恢复；当前不得启用OCR/Airtest按钮、安装自动化依赖或开发点击功能。

#### 强化策略 V1 Holdout 正式运行（2026-07-20，运行阻断待修复）

- 128 件 Holdout 已冻结为 `development 64 / frozen_validation 64`，候选仍为 `output_8_13_tank_10_17`；未读取人工标签，正式策略和候选哈希未修改。
- 验证器已完成 development 的 `320` 个五 seed 分片；两次长跑在冻结分区整批返回前达到外层时限，未生成最终 JSON/报告，不能声称通过或失败。
- 根因是验证器按分区等待全部逐件状态/流量作业后才落盘，导致已完成工作无法及时续跑。下一步仅修复逐件状态与逐作业原子分片写入和续跑调度，然后按原命令完成冻结分区与预注册闸门。
- 联合汇总附加阻断：Phase B formal Heroic M1 共享缓存 5 seed×80 片完整，但分片 `input_hash` 对应 formal 报告保存的历史 Phase B 代码哈希；验证器不得把它误判为当前源码缓存。下一步按 formal 报告哈希做只读兼容审计，仍不重写 Phase B 缓存。
- 资源汇总另发现验证器把 `expected_lower_stone_units` 写成不存在的键；修复仅限验证器字段映射，资源模型、联合批次和候选不变。
- 首份 Holdout 报告已生成且闸门通过；展示层修复为每10万总体力正式百里分，并补齐原生75+、转换75+与22速，避免把每100体力率当成最终产量。

#### Epic 非速度均衡候选 Holdout 完成（2026-07-20）

- 128 件已按不可变 manifest 完成 `development 64 / frozen_validation 64`；候选为 `output_8_13_tank_10_17`（默认12/17、纯输出8/13、纯坦10/17）。
- 验证器完成逐件 Oracle、官方 `+3` 全分支、五 seed `runs=10` 联合资源池及 Heroic 产出 `-20%/基线/+20%`；所有分片哈希通过。Phase B formal Heroic 共享缓存按其正式报告保存的历史代码哈希做只读兼容校验，未重写历史缓存。
- frozen_validation 预注册闸门 8/8 通过：`+0` 召回100%、`+3`加权召回100%、纯输出+0误停0、候选regret不高于当前、无高损失误停、联合价值和产出敏感性区间均为正。
- 人类报告按每10万总体力展示正式百里分、原生75+、转换75+、22速、裂缝/圣女体力和循环；绝对产量明确限定为 Holdout 分布条件，不代表账号自然掉落全局效率。
- 状态锁定为 `validation_passed_pending_user_release`。正式 Epic 策略、DP、资源模型、评分、跳值、GUI/OCR/Airtest 均未修改；下一步等待用户确认，确认后另建发布任务说明并执行冻结与全量回归。
- 产物：[Holdout 报告](reports/epic_output_8_13_tank_10_17_holdout_validation_20260719.md)、[逐件 JSON](reports/epic_output_8_13_tank_10_17_holdout_validation_20260719.json)、[验证器](tools/validate_epic_balanced_holdout.py)、[任务说明](Epic非速度+0+3均衡候选独立Holdout验证任务说明.md)。
- 任务说明：[强化策略V1收口与GUI延期任务说明.md](强化策略V1收口与GUI延期任务说明.md)、[Holdout任务说明](Epic非速度+0+3均衡候选独立Holdout验证任务说明.md)。
## 2026-07-20：用户确认发布 Epic 非速度均衡候选

- 用户已明确确认发布 `output_8_13_tank_10_17`；128 件 Holdout 的八项预注册闸门全部通过是本次发布的唯一验证依据。
- 已建立独立权威任务说明：[Epic非速度均衡候选正式发布任务说明.md](Epic非速度均衡候选正式发布任务说明.md)。
- 发布范围仅为 `normal_85 Epic` 非鞋、非速度路径的 `+0/+3` 单向止损；速度硬路线、鞋子、`+6` 后 Epic DP、Heroic M1、资源、评分、跳值、GUI/OCR/自动化保持冻结。
- 当前状态：`released_20260720`。`output_8_13_tank_10_17` 已发布为正式 `normal_85 Epic` 非速度 `+0/+3` 单向止损，策略版本为 `baili-formal-dp-v1-epic-balanced`。
- 首次全量入口发现两个发布阻断并进入修复：转换目标并列导致原始转换值显示随哈希种子漂移，但 4,610 个冻结动作无差异；历史 R50/R400/R800 审计错误引用当前策略哈希。处理口径已写入正式发布任务说明，修复后必须重新运行完整测试入口。
- 两个阻断均已修复：正式转换门槛使用同义且确定的转换 GS 增益零/正判定；历史只读审计使用历史报告保存的代码哈希。4,610 个动作在三个固定哈希种子下全部一致。
- 统一全量测试入口输出 `ALL TEST FILES PASSED`，真实退出码 0；语法检查和 `git diff --check` 通过。
- 发布报告：[epic_output_8_13_tank_10_17_release_20260720.md](reports/epic_output_8_13_tank_10_17_release_20260720.md)。强化策略 V1 尚未收口，下一项按[超高攻击生命集中属性救回研究任务说明.md](超高攻击生命集中属性救回研究任务说明.md)执行；该文档已将新正式版本固定为研究基线，GUI/OCR 继续延期。

#### 超高攻击/生命集中属性救回研究（2026-07-20，正式研究待执行）

- 权威任务说明：[超高攻击生命集中属性救回研究任务说明.md](超高攻击生命集中属性救回研究任务说明.md)。本轮唯一研究范围是已发布 `baili-formal-dp-v1-epic-balanced` 下 `+6/+9/+12` 基线停止节点的一向救回。
- 已新增独立研究器与测试：[research_epic_concentration_rescue.py](tools/research_epic_concentration_rescue.py)、[test_research_epic_concentration_rescue.py](tests/test_research_epic_concentration_rescue.py)。
- 阈值仅来自冻结重铸库存 P90/P95/P97.5；攻击、生命、OR 分别输出；`balanced` 使用终局概率 `>=10%`，`aggressive` 仅作 `>0` 敏感性，OR 不得直接发布。
- 已完成 smoke（单 seed、1 条主路径/装备、2 条终局概率路径/状态），主流程成功但不可用于结论。正式五 seed、资源池、节点救回统计和 CI 尚未完成。
- 在正式 JSON/报告生成前，不得冻结候选、创建新 Holdout、修改正式策略或恢复 GUI/OCR/自动化任务。

#### 超高攻击/生命集中属性救回研究收口（2026-07-20）

- 正式离线研究已完成：五 seed、每件10条主路径、每状态4条官方离散终局路径；产物为[报告](reports/epic_concentration_rescue_20260720.md)、[原始 JSON](reports/epic_concentration_rescue_20260720.json)、[研究器](tools/research_epic_concentration_rescue.py)和[测试](tests/test_research_epic_concentration_rescue.py)。
- 结果：攻击/生命 P90、P95、P97.5 及 OR 的成对正式价值增量均未形成正区间；生命 P97.5 balanced 最接近零但仍为负，不满足发布闸门。
- 研究阻断：概率路径数4导致 aggressive 与 balanced 不可区分，绝对百里分区间仍较宽；本轮只作为离线否决证据和方法学收口，不发布任何集中属性救回规则。
- 后续若重启，必须先在新的独立任务说明中提高终局概率分辨率并保持五 seed 配对；在此之前正式策略继续冻结，Holdout 不读取、不复用。

#### 超高攻击/生命集中属性概率精度主审计（2026-07-20）

- 主审计确认 p4 结果足以阻止发布，但不足以正式淘汰真实 `P>=10%` 均衡档。每状态4条终局路径只能产生 `0/25/50/75/100%`，导致 balanced 与 aggressive 在所有非零状态上动作完全相同。
- 研究器 `_official_paths()` 是官方离散跳值随机采样，不是精确枚举；旧报告“精确概率”口径已更正。计数类对称 t 区间的负下界也不得继续作为负产量展示。
- `health_p975_balanced` 的旧成对价值区间为 `[-0.001180,-0.000046]/100体力`，最接近零。更精细的10%概率筛选可能排除低概率负效用救回，因此当前只能表述为“没有候选获得发布证据”。
- 当时下一任务为[超高攻击生命集中属性救回概率精度收口任务说明.md](超高攻击生命集中属性救回概率精度收口任务说明.md)，现已被后文覆盖。
- 正式策略、DP、资源模型、评分、跳值、GUI、OCR、自动化和 Holdout 均保持冻结；精度收口完成前不进入策略覆盖审计。

#### 超高攻击/生命集中属性概率精度收口执行（2026-07-20）

- 已按唯一权威任务说明 [超高攻击生命集中属性救回概率精度收口任务说明.md](超高攻击生命集中属性救回概率精度收口任务说明.md) 建立 v2 精确研究器、测试和独立 resume；旧 p4 结果仅作历史对照。
- v2 固定正式基线、157 Epic 开发样本、80 Heroic 联合池、五个 seed、每件10条主路径和联合资源口径。生命 P97.5 概率使用官方离散跳值状态级精确 DP，终局价值沿同池主路径配对。
- 小状态精确对拍与性能预检通过；最重单 Epic 分片约7.5秒。当前状态 `formal_r10_pending`，正式分片尚未全部完成；不创建 Holdout、不修改正式策略、不进入覆盖审计。
- 产物：[v2研究器](tools/research_epic_concentration_rescue_exact.py)、[v2测试](tests/test_research_epic_concentration_rescue_exact.py)，正式报告与 JSON 完成后由该任务说明唯一授权。

#### 超高攻击/生命集中属性概率精度收口完成（2026-07-20）

- v2 正式 `1,185/1,185` 分片完成，续跑 `scheduled_shards=0`；官方离散跳值状态级精确概率、同池十条主路径配对、Heroic `+12` 补第四词条、一次重铸及资源守恒均已验证。
- `health_p975_balanced` 五 seed 成对正式百里分差为 `-0.000665 [-0.001082, -0.000248]/100体力`，区间上界小于零，状态锁定为 `health_p975_balanced_offline_rejected`，不创建 Holdout、不改正式策略。
- 人类报告按每10万总体力展示：当前 `84.4864`、候选 `83.8214` 正式百里分；候选新增生命集中件 `6.7239 [2.1059,11.3418]`，约 `14872.4` 体力/件；裂缝+圣女体力严格为 `100000`。
- 旧 p4 仅作历史对照；其“接近零但精度不足”结论已被 v2 的正式淘汰结论覆盖。策略覆盖与完整口径审计仍按后续任务说明决定，GUI/OCR/自动化继续延期。
- 产物：[v2报告](reports/epic_concentration_rescue_v2_exact_20260720.md)、[v2原始JSON](reports/epic_concentration_rescue_v2_exact_20260720.json)、[v2研究器](tools/research_epic_concentration_rescue_exact.py)、[v2测试](tests/test_research_epic_concentration_rescue_exact.py)、[任务说明](超高攻击生命集中属性救回概率精度收口任务说明.md)。

#### 超高攻击/生命集中属性 v2 主审计阻断（2026-07-20）

- 主审计保留 `health_p975_balanced` 正式价值差区间上界小于0的淘汰方向；该指标来自五 seed 同池配对。
- 解释性产量尚未正式收口：报告生成器将五 seed 的新增集中装备每批均值/区间统一除以第一个 seed 的候选总资源，而不是逐 seed 先换算每10万总体力再汇总。因此 `6.7239件/10万体力` 与 `14872.4体力/件` 暂待只读复核。
- 预注册测试未全部实现：现有4项只覆盖概率质量自洽、Heroic +12、+15/重铸和旧 schema 拒绝；缺少独立手算概率、五 seed 合并、同池拒绝、十万体力守恒、三态发布闸门、非负展示和0分片续跑证据。
- 当时下一任务为[超高攻击生命集中属性v2正式收口审计补强任务说明.md](超高攻击生命集中属性v2正式收口审计补强任务说明.md)，现已被后文覆盖。
- 正式策略、DP、评分、资源模型、跳值、GUI、OCR、自动化和 Holdout 继续冻结；第二项状态为 `pending_v2_closure_audit`，第三项策略覆盖审计不得启动。

#### 集中属性体系兼容缺失与 v3 重算（2026-07-20）

- 用户复核指出集中攻击/生命必须属于对应体系。代码审计确认旧 p4 的 `_compatible()` 原本限制攻击为 pure_output/bruiser、生命为 pure_tank/bruiser，但 v2 精确研究仅导入该函数，从未在候选 `_outcome()` 中调用；生命候选实际对所有基线停止状态计算并执行救回。
- 因非生命体系错误救回会改变候选动作、资源和正式价值，v2 `health_p975_balanced_offline_rejected` 结论、`-0.000665` 差值及解释性产量全部降级为失效历史结果，不能只读后处理。原 v2 收口审计补强任务在执行前被覆盖。
- 新体系契约要求按每个救回节点当时的 selected candidate 判定。生命仅允许抗坦、纯肉、命坦及三类半肉，拒绝输出、必爆输出、双效、一速、unknown；集中值只能计入该正式规则认可的生命词条。攻击未来只允许输出/必爆输出及明确认可攻击词条的半肉，本轮不运行攻击候选。
- 当时下一任务为[超高攻击生命集中属性体系兼容修复与v3重算任务说明.md](超高攻击生命集中属性体系兼容修复与v3重算任务说明.md)，现已被后文覆盖。
- 正式策略及 GUI/OCR/自动化仍不变；强化策略 V1 第二项状态为 `pending_system_compatible_v3_rerun`，第三项不得启动。

#### 集中属性 v3 有效词条绑定缺失与 v4 重算（2026-07-20）

- v3 正确按节点 selected candidate 做生命体系兼容布尔过滤，但 `health_valid_keys` 没有传入精确概率 DP、缓存键、剪枝上界或终局命中；这些路径仍统计全部 `hpPct+hpFlat`。
- 半肉血防/通用的 `hpFlat` 可能因此抬高生命集中概率，v3 正向区间 `[0.000111,0.001693]`、`117.2607` 百里分/10万体力及 `2.2455` 件产量均降级为失效历史结果，不进入独立验证。
- 当时下一任务为[超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md](超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md)，现已被后文覆盖。
- v3 分片、动作、汇总、正向结论不得复用；正式策略、DP、资源模型、跳值、GUI/OCR/自动化和 Holdout 继续冻结，V1 第二项状态为 `pending_valid_stat_bound_v4_rerun`。

#### 集中属性 v4 基线漂移与独立验证前置审计（2026-07-20）

- v4 有效词条绑定、精确概率、缓存/剪枝/终局命中和15项专用回归通过；五 seed 成对价值差 `[0.000031,0.001715]/100体力` 只能暂作为离线信号。
- 同研究池基线出现未解释漂移：v2 `current_formal=84.4864`，v3/v4 `current_formal=116.3588`。候选不应改变基线，必须先逐 seed 对比 Epic +3 分支、路径、Heroic、资源和 flow；确认是否为旧分支循环修复，不能凭猜测放行。
- 唯一下一任务为[超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md](超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md)。在桥接通过前不建立独立验证、不修改正式策略、不进入第三项。
- V1 第二项状态为 `pending_v4_baseline_bridge`；GUI/OCR/自动化继续冻结。
- 当前集中属性研究范围仅为生命 P97.5 `health_p975_balanced`；攻击 P90/P95/P97.5 尚未运行正式概率 DP、联合资源池或门槛比较。攻击目前只有体系兼容契约测试，不存在可发布的攻击门槛结论；须在基线桥接完成后另建独立攻击研究任务。
- 生命门槛 `37.7363` 的口径为：冻结重铸库存中 `level=90、+15、已重铸` 装备的生命集中副属性官方 GS 的 P97.5；研究终局也在一次重铸后比较。它不是未重铸 +15 GS、装备总 GS、主属性或英雄面板值；若正式体系不认可 `hpFlat`，该词条不计入候选集中值，但门槛仍沿用该重铸库存分位数。

#### v4 基线漂移审计收口（2026-07-21）

- 唯一权威任务说明：[超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md](超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md)；审计器：[audit_epic_concentration_baseline_drift.py](tools/audit_epic_concentration_baseline_drift.py)；测试：[test_audit_epic_concentration_baseline_drift.py](tests/test_audit_epic_concentration_baseline_drift.py)。
- 审计逐 seed 对比 v2/v3/v4 `current_formal`、Epic/Heroic flow、资源池、分支质量和每十万换算。v2 Epic 加权路径质量为 `40.245236/785`，v3/v4 为 `785/785`；Heroic 三版均 `400/400`，每个官方 Epic `+3` 分支概率和均为 `1`。
- `84.4864` 的可复现原因是 v2 Epic `+3` 分支循环只保留一个分支，属于欠权重历史结果；`116.3588` 是完整分支后的 v3/v4 共同基线。三版策略/资源模型哈希一致，候选兼容性、阈值和有效键不会污染 `current_formal`。
- 固定 gear/seed 的 baseline-only、`1e9` 不可救回候选、v4 重放通过：前两者 flow 与动作序列完全相同，v4 只在救回动作改变时产生差异；五 seed 裂缝体力加圣女体力均为 `100000`。修复前后哈希、允许比较的结果和禁止复用的 v2 结果见审计 JSON/报告。
- 状态：`baseline_bridge_complete_no_independent_validation`。本轮未建立独立验证、未修改正式策略/DP/资源/评分/跳值/GUI/OCR/自动化/Holdout，也未进入第三项策略覆盖审计；后续若继续，必须先新建独立任务说明并同步本计划。

#### 超高攻击/生命集中属性独立中期验证准备（2026-07-21）

- 已建立唯一权威任务说明：[超高攻击生命集中属性独立中期验证任务说明.md](超高攻击生命集中属性独立中期验证任务说明.md)。候选仅为 `health_p975_balanced`，门槛为重铸库存生命集中副属性 GS `P97.5=37.7363`，只在 `normal_85` 非鞋、非速度装备的 `+6/+9/+12` 基线停止节点按体系兼容条件救回。
- 验证协议固定为新收集 `128` 个独立真实中期状态，`64 development / 64 frozen_validation`；可信完整 Fribbels 导出、实例 ID/规范化指纹去重、冻结后解盲，禁止读取 v4 开发样本、旧 Holdout、旧 Oracle 或历史已强化档案。
- 当前状态：`ready_to_collect_user_authorization_20260721`。只能被动收集；若要取得冻结验证组终局标签，必须先得到用户明确授权，再继续强化至 `+15` 并进行一次重铸。授权前不得启动消耗游戏资源的验证、建立发布任务或修改正式策略。
- 攻击集中门槛仍未正式研究；现有攻击内容仅为体系兼容契约，不构成攻击候选结论。第三项策略覆盖审计、GUI/OCR/自动化继续暂停。

#### 攻击集中基础策略与 OCR 只读采样优先级调整（2026-07-21）

- 用户确认当前正式策略可作为实际使用基线；生命集中独立验证仍保持待授权，不再阻塞攻击集中离线研究和 OCR 只读/影子采样，但生命救回不得发布。
- 新的下一项权威任务说明：[超高攻击集中属性基础策略研究任务说明.md](超高攻击集中属性基础策略研究任务说明.md)。攻击只允许输出、必爆输出和明确认可攻击词条的半肉体系；先做 P90/P95/P97.5 精确 DP 与五 seed 联合资源池，结果未通过独立验证不得修改正式策略。
- OCR 路线恢复到第一阶段只读/影子模式：[OCR自动强化实施路线与第一阶段只读识别任务说明.md](OCR自动强化实施路线与第一阶段只读识别任务说明.md)。当前可采集真实截图、OCR 字段、建议一致性、耗时和失败率；自动点击、无人值守强化和资源消耗仍暂停。
- 手工与软件效率分开记录：游戏资源/正式百里分仍按统一经济口径，OCR 识别耗时、等待、重试、人工确认和执行失败率单列，不用软件样本替代策略研究样本。
- 当时执行状态：攻击集中研究交给子智能体；用户要求的 `gpt-5.6-luna + max` 在当前接口不可用，按最新调整实际使用 `gpt-5.6-terra + high`，不得误记为 Luna。最小 smoke 已通过（2/2 分片、续跑 scheduled=0、三种 Heroic 产出情景资源守恒）；该进行中状态已被后文正式研究收口覆盖。

#### 攻击集中正式研究收口（2026-07-21）

- 权威任务说明：[超高攻击集中属性基础策略研究任务说明.md](超高攻击集中属性基础策略研究任务说明.md)。正式产物：[报告](reports/epic_attack_concentration_exact_r10_20260721.md)、[JSON](reports/epic_attack_concentration_exact_r10_20260721.json)、[研究器](tools/research_epic_attack_concentration.py)、[测试](tests/test_research_epic_attack_concentration.py)。
- 新 schema `1` 的五 seed、157 Epic、80 Heroic、每件10路径联合池完整结束 `1185/1185`；新 resume 复跑 `scheduled=0/reused=1185`，旧生命 v2/v3/v4 分片未读入。
- P90 balanced：`-0.008644 [-0.016554,-0.000734] /100体力`；P95 balanced：`-0.006555 [-0.012780,-0.000331] /100体力`。二者在 Heroic 产出 `-20%/基线/+20%` 下均为负，已作为离线候选淘汰。
- P97.5 balanced：`-0.002397 [-0.007909,0.003114] /100体力`，三种 Heroic 产出情景都跨零，状态 `effect_unresolved`，不得建立 Holdout、发布或修改正式策略。
- P90/P95/P97.5 的新增攻击集中件数为 `54.1552/31.9889/13.3057` 每十万总体力；增加数量不是策略价值正收益证据。候选不产生新的 stop 动作，所有 scenario × seed × candidate 均通过裂缝+圣女体力 `=100000` 守恒。
- 相关回归 `29` 项、语法检查、`git diff --check` 通过。攻击集中不再是 V1 发布候选；下一步仍需单独任务说明后才可启动策略覆盖与完整口径审计。GUI/OCR/自动化和真实强化仍不因本结论改变。
- 使用结论：当前不启用攻击集中救回，继续使用 `baili-formal-dp-v1-epic-balanced`。P90/P95 已明确淘汰；P97.5 仅标记效应未决，不足以创建 Holdout、发布门槛或改变正式建议。

#### OCR 第一阶段只读研发启动（2026-07-21）

- 用户授权组织 OCR 研发，当前唯一权威任务说明为：[OCR自动强化实施路线与第一阶段只读识别任务说明.md](OCR自动强化实施路线与第一阶段只读识别任务说明.md)。状态为 `stage1_read_only_implementation_authorized`。
- 本轮只实现截图采集接口、区域定义、字段规范化、结构化 schema、字段置信度、低置信度拒绝和影子对拍基础；不接 Airtest、不点击、不消耗游戏资源、不修改正式策略。
- 当前接口无 `gpt-5.6-luna`，代码子任务使用 `gpt-5.6-terra + high`；主 agent 只审阅和决定闸门。未经用户授权不得安装 OCR/ADB/Airtest 新依赖或启动模拟器抓图。
- 若仓库无真实截图语料，第一轮只允许完成基础设施和合成测试，并以 `waiting_for_real_screenshot_corpus` 停止；不得声称 OCR 准确率或进入自动点击。

#### OCR 第一阶段只读基础设施收口（2026-07-21）

- 权威任务说明：[OCR自动强化实施路线与第一阶段只读识别任务说明.md](OCR自动强化实施路线与第一阶段只读识别任务说明.md)。实际执行模型为 `gpt-5.6-terra + high`，因为当前协作接口无 Luna；未安装依赖、联网、打开模拟器、ADB 抓图或调用 Airtest。
- 审计未发现真实游戏截图语料或 OCR 引擎，历史状态为 `waiting_for_real_screenshot_corpus`（已被后文“第一批真实截图配对收口”覆盖）。现有 GUI OCR/Airtest 按钮继续禁用，`NullAirtestAdapter` 未修改。
- 已实现只读 [capture](src/e7_enhance/ocr_capture.py)、[regions](src/e7_enhance/ocr_regions.py)、[normalize](src/e7_enhance/ocr_normalize.py)：保留截图 SHA-256、原文、候选、字段置信度、规范化值、拒绝原因、耗时与重试；任何未知、低置信度或 Gear 非法输入均拒绝。结构化 Gear 只经现有 `build_gear_dict`/`Gear.from_dict` 后调用统一 `advise_gear`，可与同件 JSON 建议哈希对拍。
- 合成夹具回归：[test_ocr_stage1_readonly.py](tests/test_ocr_stage1_readonly.py) 6 项通过，覆盖别名/噪声、拒绝、哈希、区域、schema、建议对拍及无点击。它不是 OCR 准确率。下一步必须先取得真实截图与同件结构化真值，另建只读影子对拍任务；不得进入点击或无人值守阶段。
- 截图提供协议已写入 OCR 任务说明：装备详情页原始 PNG、同件 Fribbels/人工 JSON、`screenshot_id -> instanceId` manifest，首批建议 10-20 张多样样本；首轮只做字段级影子对拍，不修改策略或启用点击。
- 用户确认采用手动截图；agent 负责与同期完整 Fribbels 导出匹配、生成 manifest、处理歧义和完成字段级对拍，用户无需手工录入 OCR 字段。

#### OCR 第一批真实截图配对启动（2026-07-21）

- 已收到 14 张真实装备页面 PNG；源文件均存在。待核对结构化参考为 `D:\VScode\E7-tools\gear_read\gear_fribbels_20260719_221033.json`。
- 当前唯一权威任务说明：[OCR第一批真实截图配对与只读影子核对任务说明.md](OCR第一批真实截图配对与只读影子核对任务说明.md)。状态为 `real_corpus_received_pairing_ready`。
- 本批先完成原图持久化、SHA-256、装备/页面分组、Fribbels `instanceId` 唯一匹配和最小字段真值；不能唯一匹配的项目必须标记 `ambiguous` 或 `unmatched`，不能猜测。
- 当前没有 OCR 引擎，因此本批不能产生 OCR 准确率结论；Airtest、自动点击、资源消耗、正式策略和 GUI 业务逻辑继续冻结。

#### OCR 第一批真实截图配对收口（2026-07-21）

- 权威任务说明：[OCR第一批真实截图配对与只读影子核对任务说明.md](OCR第一批真实截图配对与只读影子核对任务说明.md)。实际使用 `gpt-5.6-terra + high`；未安装依赖、联网或接触任何游戏控制接口。
- 14 张原始 PNG 已持久化，`ocr-0006=ocr-0003`、`ocr-0008=ocr-0007` 为原字节重复但均保留。产物：[manifest](manual_acceptance/ocr_stage1/batch_001/manifest.json)、[最小参考](manual_acceptance/ocr_stage1/batch_001/references/matched_gears.json)、[报告](reports/ocr_stage1_batch_001_pairing_20260721.md)、[只读工具](tools/pair_ocr_stage1_batch.py)。
- 8 组中仅生命套 `+15` 头盔（`ocr-0009/0010`）唯一匹配 `instanceId=2565468393`；其余 12 张为 `unmatched`，原因均记录为 `stale_reference_possible`，无 `ambiguous`、无猜测匹配。最小参考不含 `raw`、英雄、账号资源或无关装备。
- 定向 8 项、语法检查、14 条 manifest/哈希/无点击校验通过。当前状态 `paired_reference_waiting_for_ocr_engine`：真实语料已具备，但尚无 OCR 引擎，因此不得报告准确率、置信度或建议对拍，更不得进入 GUI 点击、Airtest 或无人值守阶段。
@@
#### MuMu 同账号实时装备数据读取与 OCR 影子刷新（2026-07-21，历史流程，已被后文新入口覆盖）

- 用户确认 MuMu 始终登录同一账号，并授权在需要时直接调用外部 Fribbels 读取器获取最新装备数据，不必每次手工提供 JSON。
- 唯一权威任务说明：[MuMu同账号实时装备数据读取与OCR影子刷新任务说明.md](MuMu同账号实时装备数据读取与OCR影子刷新任务说明.md)。执行前仍必须先运行 `adb devices`，使用实际在线地址；不得复用历史 `<local-adb-endpoint-redacted>`。
- 本授权仅覆盖只读抓取、解码、校验、真实样本导入和 OCR 影子配对准备；不覆盖点击、强化、出售、转换、Airtest、无人值守或正式策略修改。Holdout 已冻结时不得写入旧批次。
- 输出超时必须先检查 Fribbels JSON、`player_data.json`、PCAP、summary 是否已落盘；只有输出缺失或校验失败才允许重跑。需记录输入路径、时间、设备地址、读取器版本、SHA-256、装备数量、实例 ID 和排除原因。
- 当前状态：`authorized_pending_read`；OCR 第一批配对报告继续作为历史对照，未报告 OCR 准确率，也未启动自动点击。
- 读取尝试记录：实际 ADB `<local-adb-endpoint-redacted>`、Python39/scapy 读取器重启后运行 150 秒和 180 秒，分别落盘 570,504 与 382,486 字节 PCAP，但均解码为 `0 items`；未导入旧 JSON。读取器源码确认 `--restart` 不会自动进入主页，必须额外传 `--auto-enter`；后续按“重启 + 自动进入主页 + 抓包”流程重试。读取器的自动导航仅用于主页同步，不执行强化、出售或转换。
- 追加记录：使用 `--auto-enter` 后仍得到 393,976 字节 PCAP 和 `0 items`，未导入。已确认 `Epic-7-tools.v1.2\game_read` 与此前成功生成 20260719 快照的 `D:\VScode\E7-tools\gear_read` 读取器哈希不同；下一步切换到已验证入口。读取器自动导航仅用于主页同步，不执行强化、出售或转换。
- 安全阻断：该读取器会把抓到的账号同步包分组发送到第三方 Fribbels `/getItems` API；三次 v1.2 尝试均未得到新 JSON。继续使用此前成功的 `gear_read` 入口前，必须取得用户对外部传输的明确同意；在此之前不再抓取或解码。
- 用户已明确同意向 Fribbels `/getItems` API 发送本账号同步包；本次授权仅用于只读装备读取，继续保持不强化、不出售、不转换、不修改正式策略。
- 后续诊断：v1.2 三次及此前成功入口 `D:\VScode\E7-tools\gear_read` 一次均完成 ADB/重启/自动导航和 PCAP 落盘，但都解码为 `0 items`。前台仍为 Epic Seven；只读截图已保存为 `manual_acceptance/mumu_current_screen.png`。当前状态为 `waiting_for_manual_home_or_equipment_page`，下一次应在用户确认完整主页或装备页后再以不重启模式抓取，或先另建页面导航任务，不再盲目重复抓包。
- 用户补充并授权 v1.2 页面流程：重启 Epic Seven 后进入主页，点击右上角背包图标，再点击左下角仓库，让游戏发送全量装备同步包；本轮按此流程重试。页面导航仅用于只读同步，不进行强化、出售或转换。
- 后台编排首次尝试因 `--adb` 路径空格未正确引用而立即退出，未启动读取器、未生成有效 PCAP，不计入读取失败；下一次修正参数引用后重试。
- 修正参数后 v1.2 抓包正常运行并执行了导航点击，但解码仍为 `0 items`；抓包后及额外等待 30 秒的只读截图均只有角色动画，未出现可确认的完整主页/背包入口。为避免误触其他游戏功能，当前状态改为 `waiting_for_visible_home_navigation`，等待用户手动进入主页或仓库后再以不重启模式读取。
- 用户已关闭加速器，授权再次尝试 v1.2 读取；执行前重新检测 ADB 地址，再按重启、主页、背包、仓库流程抓取。
- 已确认实际页面路径：主页顶栏箱包入口约 `(1067,30)` -> 背包；左下角仓库入口约 `(54,670)` -> “装备保管箱”，页面显示 `1033/1100`。下一步在该页面保持不重启抓包，仅滚动仓库列表触发同步请求。
- 不重启抓包仅得到 7,157 字节 PCAP、17 组数据和 `0 items`，确认完整同步需要重启/登录流。下一步改为“先抓包，再 ADB 强制停止/启动游戏，主页 -> 背包 -> 装备保管箱”，避免读取器退出确认框。
- 成功读取：加速器关闭后按“先抓包、强制重启、关闭公告、点击开始、主页 -> 背包 -> 装备保管箱”流程，生成 `D:\VScode\E7-tools\Epic-7-tools.v1.2\game_read\gear_fribbels_20260721_165440.json`。校验通过：`export_time=2026-07-21 16:54:40`、`2668` 件装备、`383` 名英雄、`4` 件神器、唯一实例 ID `2668/2668`、完整 `raw` `2668/2668`、SHA-256 `db9d15eab50ce84bc4c55d7524f85a4258c0a086fd1eadc14c09fb13a094fb09`。读取器仅在玩家名称后处理阶段报 `NoneType`，装备 JSON 已可安全导入；下一步只写真实样本档案，不写冻结 Holdout。
- 导入结果：真实档案新增 `5` 件，累计 `382` 件；排除 `archive_fingerprint=6`、`archive_instance_id=355`、`known_instance_id=13`、`not_plus0=804`、`outside_normal_85_epic_scope=1485`。Holdout 保持冻结，未写入；下一步执行档案定向测试、语法检查和 `git diff --check`。
#### MuMu 同账号实时读取验证收口补充（2026-07-21，历史结果，仅作对照）
- 新快照 `gear_fribbels_20260721_165440.json` 已完成校验和真实档案导入；档案定向测试 `3/3`、相关 `py_compile`、`git diff --check` 均通过。

#### OCR 第一批截图使用最新同账号快照复配（2026-07-21，待执行）
- 最新可信参考为 `D:\VScode\E7-tools\Epic-7-tools.v1.2\game_read\gear_fribbels_20260721_165440.json`，SHA-256 为 `db9d15eab50ce84bc4c55d7524f85a4258c0a086fd1eadc14c09fb13a094fb09`。
- 唯一权威任务说明：[OCR第一批真实截图与最新同账号快照复配及影子验证任务说明.md](OCR第一批真实截图与最新同账号快照复配及影子验证任务说明.md)。先严格复配现有 14 张截图，再决定是否有足够真值进入 OCR 字段级影子验证；不报告未经匹配支持的 OCR 准确率。
- 本轮不修改策略、GUI、OCR 点击逻辑或 Holdout；状态为 `ready_for_latest_snapshot_repair`。
- 用户已批准从当前 MuMu 同账号补采只读截图，辅助 OCR 页面区域和字段验证。截图必须保存原始 PNG、采样时间、设备地址、页面类型和 SHA-256，并与同期 Fribbels 快照独立配对；不允许强化、出售、转换或把截图直接当成 OCR 真值。任务状态更新为 `authorized_for_readonly_screenshot_sampling`。
- 截图采样首次诊断：当前 MuMu 显示“与服务器的连接已中断”，已保存 `manual_acceptance/ocr_stage1/batch_002_current.png`，仅作为错误态样本，不计入 OCR 真值；任务状态改为 `waiting_for_game_connection_before_sampling`，恢复连接并进入装备保管箱后再采样。
- 用户修正页面路径：进入背包后先点击左下角图标，再点击“管理”进入装备保管箱；后续截图采样按“主页 -> 背包 -> 左下角图标 -> 管理 -> 装备保管箱”执行。
- 截图采样补充：保管箱和选择状态截图已保存，但 ADB 触摸未稳定打开装备详情；任务状态为 `waiting_for_manual_detail_page`，需用户手动打开一件装备详情后再继续字段级 OCR 采样，避免把选择界面当成详情真值。
- 用户确认游戏有两种详情页：`warehouse_detail`（仓库详情）与 `backpack_detail`（背包详情）。当前用户已打开仓库详情，下一步先保存并配对该页面，再单独采集背包详情；不得跨页面复用区域模板或准确率。

#### OCR 第一批截图最新快照复配结果（2026-07-21）

- 使用冻结 SHA-256 的 2026-07-21 同账号快照严格复配后，结果为 `2 matched / 0 ambiguous / 12 unmatched`。唯一 matched 是 `ocr-0009/0010 -> instanceId=2565468393`，但两张只是同一件装备的不同页面，独立真值仅 1 件。
- 当前 [manifest](manual_acceptance/ocr_stage1/batch_001/manifest.json) 已替换为最新参考并记录每图候选、哈希和无点击校验；旧 2026-07-19 结果仅作历史对照。12 张零候选均维持 `unmatched + stale_reference_possible`，没有猜测匹配。
- `ocr_shadow_gate=pairing_insufficient_for_ocr`，本轮没有 OCR 引擎输出、置信度、准确率或建议哈希对拍。用户已授权后续只读补采，但这不是策略、GUI、点击或资源操作授权；必须在明确采样交接后才开始。

#### OCR 第二批仓库/背包详情分流采样（2026-07-21）

- 用户已手动打开仓库详情页，截图 `manual_acceptance/ocr_stage1/batch_002/screenshots/ocr-0006.png` 已保存并标记 `page_type=warehouse_detail`；游戏的仓库详情与背包详情必须分别统计，不能共用模板或准确率。
- 当前截图为 +13；匹配允许强化等级阶梯向下归一（+13 按 +12 比较），但套装、部位、品质、主属性和副属性仍须完全一致。按该规则仍无唯一候选，故 `unmatched`，不猜测 `instanceId`，不计入 OCR 真值；批次 manifest：[batch_002/manifest.json](manual_acceptance/ocr_stage1/batch_002/manifest.json)。
- 当前状态：`warehouse_detail_sample_saved_unmatched_after_floor_level_match`。截图设备地址未在现有证据中记录，已显式标记未通过采样校验。下一步先重新获取同时间 Fribbels 快照，再分别采集多个 `warehouse_detail` 和 `backpack_detail` 装备；在多个唯一 matched 装备前保持 `pairing_insufficient_for_ocr`，不报告 OCR 准确率、不接入自动点击。

#### 同会话 Fribbels 快照刷新阻断（2026-07-21，旧读取器结果，已被后文覆盖）

- 已检测实际 MuMu ADB 为 `<local-adb-endpoint-redacted>`。两次只读抓包均未得到完整装备同步：第一次重启抓包手动拉回 `531574` 字节 PCAP，解码 `0 items`；第二次不重启抓包得到 `9535` 字节 PCAP，解码 `0 items`。
- 读取器只自动进入大厅，不自动完成“背包 -> 管理 -> 装备保管箱”；横屏 ADB 坐标点按未可靠命中目标，已停止自动导航。未生成新可信 JSON，未导入档案，未读取 Holdout。
- 当前状态：`waiting_for_manual_backpack_navigation_before_capture`。用户手动进入背包后再启动抓包；在新 JSON 校验通过前，不更新 `ocr-0006` 配对、不报告 OCR 准确率。

#### MuMu 读取器迁移与真实数据统一口径（2026-07-21）

- 当前唯一权威说明为：[MuMu抓包读取与真实装备导入任务说明.md](MuMu抓包读取与真实装备导入任务说明.md)；OCR 刷新边界见：[MuMu同账号实时装备数据读取与OCR影子刷新任务说明.md](MuMu同账号实时装备数据读取与OCR影子刷新任务说明.md)。
- 读取器根目录已迁移为 `D:\VScode\Epic-7-tools.v1.2`；Python 入口为 `local_tools\read_mumu.py`，CMD 入口为 `scripts\read_mumu.cmd`。旧 `D:\VScode\E7-tools\...\game_read\fribbels_mumu_reader.py`、手工 PCAP 拉取、固定 IP 和 `gear_fribbels_*.json` 方法仅作历史对照。
- 标准真实读取命令使用 Python 3.9，参数固定为 `--player-id mumu_live --seconds 240 --auto-enter-seconds 105`。执行前仅检查 `adb devices -l`；读取器自动选择在线 `device`，不得写死或主动传入 `192.168.x.x:5555`。
- MuMu 必须启用 Root，并允许模拟器内 `su -c tcpdump`；Epic Seven 包名固定为 `com.zlongame.cn.epicseven`。不安装 Npcap、不修改系统网络设置、不修改 FribbelsE7Optimizer。
- 用户已授权把本账号捕获的 TCP 载荷发送到 `https://krivpfvxi0.execute-api.us-west-2.amazonaws.com/dev/getItems`。当前可信输出固定为 `data\players\mumu_live\current\player_data.json`，原始 PCAP/Fribbels 响应和不可变快照分别保存在同时间 `imports` 与 `snapshots`。
- 失败必须报告读取器完整输出，并区分 ADB、Root/tcpdump、空抓包、未抓到同步、Fribbels 解码、加速器导致 UDP 或其他接口流量。外层超时先检查三类输出是否落盘，不得盲目重跑。
- 当前状态：`reader_migration_documented_ready_for_fresh_read`。本轮仅更新规则并核验新入口存在，尚未使用新入口生成新 `mumu_live/current/player_data.json`；此前 `waiting_for_manual_backpack_navigation_before_capture` 状态已被本节覆盖。

#### 子 agent 续作指令临时禁用（2026-07-21，已被 2026-07-26 内部自动执行规则覆盖部分口径）

- 用户明确要求暂时禁用自动发布子 agent 续作指令；本规则覆盖计划前文“存在明确下一任务时必须自动附带续作指令”的旧要求，旧要求当前仅作历史对照。
- 禁用期间，即使存在可执行下一步，也只在任务说明与本计划中记录，不在最终回复自动生成 `给子 agent 的续作指令：` 或 `text` 代码块。
- 只有用户在当前轮明确索要子 agent 指令或续作指令时，才按原固定格式提供；用户明确恢复自动发布前，本临时规则持续有效。
- 文档同步义务保持不变；本规则不授权创建、启动、续跑或派发子 agent，也不影响正式策略、研究器、GUI、OCR 或 Holdout。
- 权威说明：[子agent续作指令临时禁用规则任务说明.md](子agent续作指令临时禁用规则任务说明.md)。当前状态为 `completed_rule_active`。

#### OCR 仓库详情同期快照刷新与继续配对（2026-07-21，执行中）

- 用户明确要求继续此前 `warehouse_detail` 配对；目标仍是 `manual_acceptance/ocr_stage1/batch_002/screenshots/ocr-0006.png`，不新增策略、GUI、点击或资源操作范围。
- 本轮先按新权威入口 `D:\VScode\Epic-7-tools.v1.2\local_tools\read_mumu.py --player-id mumu_live --seconds 240 --auto-enter-seconds 105` 自动发现在线 MuMu 并生成同期 `player_data.json`；禁止固定 ADB IP。
- 读取后必须校验同时间 `current/imports/snapshots`、SHA-256、装备数量、实例 ID 唯一性和 `raw` 完整性；通过后只写真实样本档案，不写已冻结 Holdout。
- `ocr-0006` 继续允许 `+13 -> +12` 阶梯强化等级匹配，但套装、部位、品质、等级、主属性和全部副属性必须严格一致；无唯一候选时保持 `unmatched`，不得猜测 `instanceId`。
- `warehouse_detail` 与 `backpack_detail` 仍独立采样、模板和统计。当前状态为 `fresh_snapshot_read_authorized_pairing_in_progress`。

- 新入口已成功完成批次 `20260722_000656`：标准 `player_data.json` 为 `2,589,289` 字节，SHA-256 `FBC7C69160356C1679473D6A3FC4E38ED87F15A1A4ED0DAE29C20A19DD9D821E`，装备 `2668`、英雄 `387`，实例 ID `2668/2668` 唯一；PCAP、Fribbels 原始响应、不可变快照和 reader result 同批落盘。
- 新标准 `player_data.items` 为规范化字段，不内嵌旧式 raw；同批 `snapshots/20260722_000656/reader_result.json` 的 `data.items` 保留 `2668/2668` 完整 raw。旧档案入口和配对器仅接受上一代容器，不能绕过校验直接使用。
- 新适配任务：[MuMu新player_data结构档案与OCR配对适配任务说明.md](MuMu新player_data结构档案与OCR配对适配任务说明.md)。当前状态为 `implementation_authorized_pending_terra_high`；只允许增加严格 schema/raw 证据适配、真实档案导入和 batch 002 只读配对，不写 Holdout、不修改策略或自动化。

- 进一步审计发现新读取器的 `2668` 件装备强化等级全部为 `0`，而主属性/副属性包含明显已强化值。根因是 `readers/mumu_capture/fribbels_decode.py` 读取原始装备不存在的 `raw.enhance`，未沿用已验证的 `op` 事件数与品质偏移推导。
- 错误快照禁止导入真实 `+0` 档案，也不能作为强化等级配对真值。先执行 [MuMu读取器强化等级推导修复与同PCAP回放任务说明.md](MuMu读取器强化等级推导修复与同PCAP回放任务说明.md)，用同一 PCAP 离线回放，不重复抓包或控制游戏。
- 当前状态更新为 `reader_fix_authorized_pending_terra_high`；只有修复后回放的强化等级分布与旧已验证共同实例一致，才恢复档案适配和 `ocr-0006` 配对。

- 外部读取器修复子任务因 `D:\VScode\Epic-7-tools.v1.2` 当前不可写而停止；`apply_patch` 未落盘，外部代码、测试和数据均未改变。状态为 `blocked_by_external_write_permission`，未使用其他写入方式绕过权限。
- 本轮改用项目内严格 raw 证据桥接：以同批 `reader_result.data.items[].raw.op`、品质最大事件数和初始偏移推导 `0/3/6/9/12/15`，只覆盖内存复制件的错误 `enhance=0`。不得按主属性数值猜测，raw 推导也全0或身份不一致时继续拒绝。
- 项目适配任务状态改为 `raw_evidence_bridge_authorized_in_progress`；真实档案导入和 `ocr-0006` 配对仍等待桥接回归通过后执行。

#### 背包装备 OCR 与人工确认强化优先（2026-07-22）

- 外部读取器已修复强化等级并离线重建批次 `20260722_011424`。当前标准 `player_data.json` SHA-256 为 `490211CF73E7EA34A22422FC8068D643716A1B417EA12E2BA18F6CA628C0025D`，`2668/2668` 实例 ID 唯一；强化等级分布为 `+0:481、+3:2、+6:8、+9:24、+12:3、+15:2150`。
- 用同批 raw 证据对仓库 `ocr-0006` 只读预演后仍为 `0` 个候选，且全量快照不存在其四条副属性组合。仓库配对暂停，旧 batch 002 manifest 不强配、不计入 OCR 准确率；真实档案导入本轮未执行。
- 用户决定优先完成背包中的强化。唯一当前任务为[背包装备详情OCR与人工确认强化闭环任务说明.md](背包装备详情OCR与人工确认强化闭环任务说明.md)，本轮诊断见[背包优先与仓库配对阻断记录](reports/ocr_stage1_backpack_priority_20260722.md)；先完成 `backpack_detail/enhance` 同件唯一配对、字段识别和正式建议哈希对拍。
- 第一阶段仍为只读建议加人工确认，不执行强化点击、不消耗资源、不接 Airtest、不修改正式策略/GUI/Holdout。只有至少 10 件独立背包样本通过字段和建议闸门后，才可另行请求单件实际强化授权。
- 当前状态：`epic_ocr_shadow_rejected_low_confidence_set`。已安装 `paddleocr==2.10.0 / paddlepaddle==2.6.2`，ASCII 模型缓存为 `D:\VScode\cultivation\e7_ocr_cache`。batch 004 Epic 强化页唯一匹配 `4181490939`；OCR 正确识别等级、部位、主属性、四副属性、分数及 +0，但套装置信度 `0.976382 < 0.98`，因此 fail closed，不生成 OCR 建议。JSON 影子建议仅为对照：`cautious_continue / +3复核`。详见[batch 004 报告](reports/ocr_stage1_backpack_batch_004_ocr_shadow_20260722.md)。相关定向 21 项通过；全量入口唯一失败为既有 Qt 原生用例，隔离连续两次通过。下一步继续独立 Epic/Heroic 样本和套装字段识别改进；不自动点击、不消耗资源。子 agent 续作指令临时禁用规则继续生效。

#### 背包 OCR batch 004 影子闭环收口（2026-07-22，覆盖前文低置信度状态）

- 本轮重跑 PaddleOCR 后，套装独立裁剪置信度 `0.993683`，所有必需字段通过 `0.98` 闸门；`exp0/525` 以 `0.993024` 确认 `+0`。
- 新增 OCR 字段兼容与只读影子收口工具：[ocr_backpack_shadow.py](src/e7_enhance/ocr_backpack_shadow.py)、[finalize_ocr_backpack_shadow.py](tools/finalize_ocr_backpack_shadow.py)；原始 OCR 与策略投影分开保存，未执行点击。
- batch 004 唯一实例 `4181490939` 的 OCR 建议与同件 JSON 建议哈希均为 `1f9d66361c7a84f727007850fb764dc6e29e28d9ef48f0ccbe9334a403584887`，`matched=true`；当前样本量 `1/10`，状态 `backpack_ocr_shadow_passed_insufficient_samples`。
- 产物：[batch 004 报告](reports/ocr_stage1_backpack_batch_004_ocr_shadow_20260722.md)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_004/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_004/ocr_shadow.json)、[manifest](manual_acceptance/ocr_stage1/batch_004/manifest.json)。
- 验证：相关定向测试 27 项、语法检查、`git diff --check` 通过；`tools/run_all_tests.py` 输出 `ALL TEST FILES PASSED`，真实退出码 `0`。
- 下一步：继续采集至少 9 件独立 Epic/Heroic 背包样本，覆盖不同部位、品质和强化节点；达到 10 件并通过闸门前，不进入人工确认强化、自动点击或 GUI 接线。子 agent 续作指令临时禁用规则继续生效。

#### 背包 OCR batch 005 采样准备（2026-07-22，进行中）

- 任务说明：[背包装备详情OCR与人工确认强化闭环任务说明.md](背包装备详情OCR与人工确认强化闭环任务说明.md)。
- batch 004 已通过只读 OCR 影子闸门但仅有 `1/10` 独立样本；本轮仅扩充 Epic/Heroic 背包样本，不进入人工强化或自动点击。
- 先按《MuMu抓包读取与真实装备导入任务说明.md》执行自动发现 ADB 的新鲜读取，校验 `player_data.json`、同批 `reader_result.json`、PCAP、原始响应、装备数量、实例 ID 唯一性和 `raw` 完整性；通过后才建立 batch 005 截图配对。
- 标准读取器已完成批次 `20260722_132045`：装备 `2670`、英雄 `387`；`player_data.json` 实例 ID `2670/2670` 唯一，同批 `reader_result.json` 保留 `raw=2670/2670`，PCAP 与 Fribbels 原始响应齐全。
- 初次 ADB 列表为空；使用当前运行实例桥接日志实时上报的地址恢复连接。没有把地址写死到读取器或规则；后续仍先运行 `adb devices -l`。
- 新快照已导入真实 Epic +0 档案，累计接受 `382` 件，未写冻结 Holdout。当前截图为登录背景页，不是装备详情，因此 OCR 样本仍为 `1/10`。
- 报告：[batch 005 新鲜读取与采样前置报告](reports/ocr_stage1_backpack_batch_005_reader_20260722.md)。
- 当前状态：`waiting_for_manual_backpack_epic_or_heroic_detail`；等待用户手动打开新的 Epic/Heroic 背包装备详情或强化页，不自动导航、不点击、不消耗资源。

#### 背包 OCR batch 005 `+3` 主属性配对实现（2026-07-22，进行中）

- 用户已打开新的 Epic `+3` 鞋子详情；截图 SHA-256 为 `F692FDE3D2EB65A882041D48A71877E2A39CB4653EB54B011931CE4754BFE6FF`。
- 现有配对器只接受 `+0` raw 主属性，无法合法处理本件 `+3`；不得把它降级成 `+0` 或手工指定实例 ID。
- 已在[背包 OCR 任务说明](背包装备详情OCR与人工确认强化闭环任务说明.md)授权最小节点映射实现：支持 `0/3/6/9/12/15` 可见主属性严格配对，保持既有唯一性、fail-closed 和无点击边界。
- 当前状态：`batch_005_plus3_main_value_pairing_implementation_authorized`；实现和回归通过后继续本件 OCR/JSON 建议对拍。

#### 背包 OCR batch 005 详情页与 `+3` 节点阻断（2026-07-22）

- `ocr-0002.png` 是 `backpack_detail`，现有强化页模板识别失败并 fail closed；未计入独立样本，当前仍 `1/10`。
- 同批真值唯一匹配 `instanceId=4156959864`（Epic、Boots、+3、Speed），但 raw 仅有 base/终局主属性值，没有官方中间节点表；禁止插值或把终局值当作截图值。
- Terra 审核未改代码；需要官方节点表后才实现配对与 normalize 共用映射。详情报告：[batch 005 详情页与 +3 配对审计](reports/ocr_stage1_backpack_batch_005_detail_20260722.md)。
- 当前状态：`batch_005_detail_template_and_plus3_node_mapping_blocked`。用户可手动进入强化页继续页面模板验证，但在节点表确认前不生成 `+3` 建议。

#### 背包 OCR batch 005 `+0` 套装与副属性标签修复（2026-07-22，进行中）

- `ocr-0003.png` 为新 Epic `+0` 武器强化页，伤口套、攻击 100、四条副属性和 GS 25 均可见；截图 SHA-256 为 `4E65269A1CB0DF75307DF6BE9354160F9E4E98DA3D91D95FC3722E3B5B14317F`。
- OCR 仅因 `伤口` 未映射及暴伤标签最小置信度 `0.949667` 被 fail closed；不得降低 `0.98` 全局门槛。
- 已在[背包 OCR 任务说明](背包装备详情OCR与人工确认强化闭环任务说明.md)授权最小修复：复用正式套装别名，增加局部副属性标签重识别并补回归；修复后再对本件做唯一配对和建议影子对拍。
- 当前状态：`batch_005_plus0_ocr_set_and_substat_label_fix_authorized`。

#### 背包 OCR batch 005 `+0` 影子闭环收口（2026-07-22）

- 新 Epic `+0` 伤口武器 `instanceId=4164602184` 已唯一配对，OCR 字段全部通过 `0.98`；暴伤局部重识别由 `0.949667` 提升至 `0.983678`。
- OCR 与 JSON 建议哈希一致：`54270bd5dee28ff06853eb64078ebe2d8e76e106e05ed8bdb7b784d86bbfa933`，建议谨慎继续、+3 复核、半肉通用。
- 当前独立通过样本 `2/10`，报告：[batch 005 只读影子闭环报告](reports/ocr_stage1_backpack_batch_005_shadow_20260722.md)。继续至少 8 件独立 Epic/Heroic 样本；未达 10 件前不进入人工强化或 GUI 接线。
- `+3` 主属性中间节点表阻断仍有效；不因本件 `+0` 通过而放宽节点匹配。
- 当前状态：`backpack_ocr_shadow_passed_insufficient_samples`。

- 验证收口：OCR/配对/规则定向 `37` 项通过；`tools/run_all_tests.py` 真实退出码 `0`，输出 `ALL TEST FILES PASSED`；语法检查与 `git diff --check` 通过。

#### 背包 OCR 剩余 8 件采集口径（2026-07-22）

- 用户手动切换到未采样 Epic/Heroic 装备并进入强化页，采集端只执行截图、唯一配对、OCR 和建议哈希对拍；不自动点击、不实际强化。
- 当前 `20260722_132045` 快照可供连续切换装备使用；只有装备状态变化或快照不存在实例时才重新抓包。每个 `instanceId` 只计一次，详情页、重复实例、低置信度和非唯一匹配不计数。
- 剩余样本优先 Heroic、缺失部位和新套装；当前阶段优先 `+0`。`+3/+6/+9/+12` 主属性显示值暂不阻断 `+0` 样本采集，后续实际人工强化时逐节点保存截图建立真实记录。
- 中间主属性记录不改变正式策略；官方表或足够真实节点证据形成前，不猜测插值，不生成依赖中间显示值的建议。
- 当前状态保持 `backpack_ocr_shadow_passed_insufficient_samples`，进度 `2/10`。

#### 背包 OCR batch 006 配对阻断（2026-07-22）

- 任务说明：[背包装备详情OCR与人工确认强化闭环任务说明.md](背包装备详情OCR与人工确认强化闭环任务说明.md)。本轮沿用同一 `20260722_132045` 快照，不重新抓包。
- 新截图：[batch 006](manual_acceptance/ocr_stage1/batch_006/screenshots/ocr-0001.png)，OCR 原始结果：[ocr_raw.json](manual_acceptance/ocr_stage1/batch_006/ocr_raw.json)，报告：[配对记录](reports/ocr_stage1_backpack_batch_006_pairing_20260722.md)。
- 结果为 `ambiguous_and_low_confidence`：两个候选实例 `4182710946`、`4182731599` 的所有可见字段和 raw `+0` 主属性相同；暴击率数值置信度 `0.779728`。不计样本、不生成建议，进度保持 `2/10`。
- 下一步：用户手动切换另一件未采样 `Epic/Heroic +0` 装备并进入强化页，继续只读截图与严格配对；不点击强化、不重新抓包。

#### 背包 OCR batch 007 影子拒绝（2026-07-22）

- 沿用 `20260722_132045` 快照，未重新抓包；`Epic +0` 暴击套铠甲唯一配对到 `instanceId=4181507121`。
- OCR 未识别“传说铠甲”的品质/部位，效果抗性 `5%` 数值置信度仅 `0.803892`，所以 `ocr_gate=rejected_ocr_shadow`，未生成建议对拍。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_007/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_007/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_007/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_007_shadow_20260722.md)。
- 本件保留为 OCR 修复语料，不计入通过样本，进度仍为 `2/10`；下一步继续手动切换另一件未采样 `Epic/Heroic +0` 装备。

#### 背包 OCR batch 008 影子拒绝（2026-07-22）

- 沿用 `20260722_132045` 快照，未重新抓包；`Epic +0` 速度套头盔唯一配对到 `instanceId=4181502338`。
- OCR 仅防御 `4%` 数值置信度 `0.944295` 未达 `0.98`，所以 `ocr_gate=rejected_ocr_shadow`，未生成建议对拍。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_008/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_008/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_008/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_008_shadow_20260722.md)。
- 本件保留为失败语料，不计入通过样本，进度仍为 `2/10`；下一步继续手动切换另一件未采样 `Epic/Heroic +0` 装备。

#### 新会话交接与强化页导航授权（2026-07-22）

- 用户要求在新会话继续当前 OCR 只读采集；唯一权威说明为[背包装备详情OCR与人工确认强化闭环任务说明.md](背包装备详情OCR与人工确认强化闭环任务说明.md)，本节记录交接状态。
- 用户已明确授权自动点击当前已选装备的“强化”入口，仅用于进入 `enhance` 页面；禁止实际强化、材料、自动登记、出售、转换、锁定、重铸、替换装备和其他游戏操作。页面不明确立即停止。
- 新会话执行前须完整读取 `computer-use` 技能并先截图确认页面；先运行 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`，只选择状态为 `device` 的 MuMu，不得写死或复用 `192.168.x.x:5555`。
- 当前沿用可信快照 `20260722_132045`，不重新抓包；仅当装备状态变化、实例不在快照或快照校验失败时重新读取。有效通过样本 `2/10`，batch 006–008 均拒绝且不计数；下一步采集未采样 Epic/Heroic `+0`，优先 Heroic、缺失部位和新套装。
- 当前阶段仍是 `backpack_ocr_shadow_passed_insufficient_samples`：保持 `0.98` 置信度、唯一配对和 fail-closed；未满 10 件通过样本前，不进入人工确认强化、自动强化或 GUI 接线。

#### 背包 OCR batch 009 采集前置阻断（2026-07-22）

- 权威任务说明：[背包装备详情OCR与人工确认强化闭环任务说明.md](背包装备详情OCR与人工确认强化闭环任务说明.md)。已完整读取 `computer-use` 技能及其必读运行、确认和 API 文档，没有执行任何 MuMu 输入。
- 两次运行 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l` 均未返回状态为 `device` 的 MuMu；未连接或复用历史 `192.168.x.x:5555` 地址。
- `computer-use` 唯一枚举到 `MuMu安卓设备` 窗口，但两次只读截图均以 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)` 失败，无法确认页面类型和已选装备。
- 本轮按 fail-closed 停止，未点击“强化”，未重新抓包，未创建 batch 009 采集产物，未运行唯一配对、OCR 或建议哈希对拍。可信快照保持 `20260722_132045`，有效通过样本保持 `2/10`。
- 当前状态：`batch_009_blocked_by_adb_and_window_capture`。下一步先恢复 MuMu 的 ADB `device` 可见性，再重试只读页面截图；窗口捕获仍失败时继续停止，不试探点击。

#### batch 009 ADB 设备发现只读诊断（2026-07-22，进行中）

- 用户确认 MuMu 已启动并要求解释无法读取的原因；诊断范围已写入[背包装备详情OCR与人工确认强化闭环任务说明.md](背包装备详情OCR与人工确认强化闭环任务说明.md)。
- 只读区分 ADB server 生命周期、MuMu 设备桥接、客户端/端口冲突和实例未就绪；不执行 `adb connect`，不传入历史 IP，不修改配置、网络或游戏状态。
- 当前状态：`batch_009_adb_discovery_diagnosis_in_progress`。诊断结论形成前，batch 009 采集与强化入口点击继续阻断。

- 诊断已收口：同一 PowerShell 进程内 ADB server 存活时连续两次 `devices -l` 仍为空；MuMu 进程已启动约 56 分钟，因此不是实例未启动，也不只是命令边界清理 server。
- ADB 日志显示上一条历史 transport 在当前 MuMu 实例启动前约两分钟已经终止，新实例启动后没有新 transport 注册。根因是当前实例的 ADB 桥接未注册到指定 MuMu ADB server；命令结束后 server 被执行环境清理只是附加问题。
- MuMu 自带 ADB 版本和路径正确，ADB 端口/密钥环境变量没有覆盖；未发现客户端或环境变量冲突证据。历史 batch 005 也曾需要从当前实例桥接日志派生地址后显式注册，但本轮禁止 `adb connect`，所以未执行修复。
- `computer-use` 窗口截图的 `0x80004002` 仍是独立阻断。当前状态更新为 `batch_009_adb_transport_not_registered_diagnosed`；不创建 batch 009 采集产物，有效进度保持 `2/10`。

#### 当前实例 ADB 恢复授权（2026-07-22）

- 用户明确授权恢复当前 MuMu 实例的 ADB 注册；仅允许使用当前运行实例的动态发现信息，不得写死、复用、输出或持久化历史 `192.168.x.x:5555` 地址。
- 恢复只验证 `adb devices -l` 出现状态为 `device` 的 MuMu，不启动读取器、不重新抓包、不执行游戏 UI 操作。动态信息缺失或出现多设备时立即停止。
- 当前状态：`batch_009_adb_transport_restore_authorized`；OCR 样本仍为 `2/10`。

- ADB 恢复完成：MuMu 官方管理器对当前 `vmindex=0` 的动态注册命令在沙箱外退出码 `0`；随后指定 `adb.exe devices -l` 返回恰好一个 `device`（产品 `aurora`、型号 `24031PN0DC`），未记录或复用 IP。
- 本轮只恢复并验证 ADB，未启动读取器、未重新抓包、未截图或执行游戏输入；batch 009 采集产物尚未创建，有效样本保持 `2/10`。
- 当前状态更新为 `batch_009_adb_transport_restored_pending_page_capture`。继续 OCR 前仍需按 `computer-use` 要求先截图确认页面。

#### batch 009 当前强化页采集授权（2026-07-22）

- 用户确认已手动进入 `enhance` 页面并允许继续只读采集；本轮不自动点击入口。
- 只保存原始 PNG 与元数据，随后执行唯一配对、PaddleOCR、`0.98` 闸门和 OCR/JSON 建议哈希对拍；禁止实际强化、资源操作、重新抓包或策略修改。
- 当前状态：`batch_009_enhance_capture_authorized`，有效样本仍为 `2/10`。

#### 背包 OCR batch 009 只读影子闭环结果（2026-07-22）

- Computer Use 窗口捕获仍为 `0x80004002`；ADB 恢复后使用无 serial/IP 的原始 `screencap`，确认当前页面为 `enhance`，未发送 UI 输入。
- 新 Heroic `+0` 暴击套 Helmet 唯一配对到 `instanceId=4181486277`，未重复既有通过或拒绝批次；PaddleOCR 局部重识别后全字段通过 `0.98`。
- OCR 与同件 JSON 建议哈希一致：`72f632bd828d3cf7e193db538365ddddd94938abe7dbf9b90e3f33443181dce2`；建议谨慎继续、+3 复核、半肉通用。
- 报告：[batch 009 只读影子闭环报告](reports/ocr_stage1_backpack_batch_009_shadow_20260722.md)。`click_performed=false`，有效独立样本更新为 `3/10`；继续采集至少 7 件，未满 10 件前不进入人工确认强化或 GUI 接线。
- 当前状态恢复为 `backpack_ocr_shadow_passed_insufficient_samples`。

#### 背包 OCR 自主连续采集授权（2026-07-22，进行中）

- 唯一权威说明仍为[背包装备详情OCR与人工确认强化闭环任务说明.md](背包装备详情OCR与人工确认强化闭环任务说明.md)。用户已授权采集端自主循环退出强化页、在背包选择未采样装备、进入明确的强化页并只读采集，直到有效独立样本达到 `10/10`。
- 授权只覆盖导航和只读采集；实际强化、材料、自动登记、出售、转换、锁定、重铸、替换装备及任何资源消耗仍严格禁止。每次输入前后均须以最新截图确认页面，页面不明确立即停止。
- 每批先确认指定 MuMu ADB 只有一个 `device`，不传入或记录 IP；继续复用可信快照 `20260722_132045`，不重新抓包。后续从 batch 010 递增，保持严格唯一配对、`0.98` 字段闸门和 OCR/JSON 建议哈希对拍。
- 拒绝批次只保留为 OCR 修复语料并继续采下一件，不降低阈值、不人工指定实例；每批结果同步任务说明、总计划和报告。达到 `10/10` 后立即停止界面输入，状态最多进入 `ready_for_single_item_manual_confirmation`。
- 当前有效进度 `3/10`，通过批次为 004、005、009；006、007、008 仍拒绝。当前状态：`autonomous_backpack_ocr_collection_authorized_3_of_10`。

#### 背包 OCR batch 010 自主采集拒绝（2026-07-22）

- 已执行授权的退出、选择和进入强化页导航，采集新的 Heroic `+0` 守护套 Armor；实际强化及其他资源操作点击为 `0`，装备状态未变化，继续复用 `20260722_132045` 快照。
- 原始截图 SHA-256 `9edd522ff0d05af950646a0dfa2f435a2a35020586877a786b5e104e2f04c0f9`；严格唯一配对到 `instanceId=4183341692`，候选数 `1`，未人工指定实例编号。
- PaddleOCR 因品质/部位未识别及暴击率 `3%` 置信度 `0.954320 < 0.98` fail closed；不生成建议、不计入通过样本，只保留为修复语料。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_010/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_010/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_010/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_010_shadow_20260722.md)。有效进度仍为 `3/10`，下一批为 011。

#### 背包 OCR batch 011 自主采集拒绝（2026-07-22）

- 已执行授权的退出、选择和进入强化页导航，采集新的 Heroic `+0` 抵抗套 Necklace；实际强化及其他资源操作点击为 `0`，装备状态未变化，继续复用 `20260722_132045` 快照。
- 原始截图 SHA-256 `cf78467178eb7b4d7409faa7acb26c7150f1c5b6b2ebf405825ee2aea1a82205`；严格唯一配对到 `instanceId=4184397469`，候选数 `1`，未人工指定实例编号。
- PaddleOCR 因效果抗性 `5%` 值置信度 `0.975677 < 0.98` fail closed；不生成建议、不计入通过样本，只保留为修复语料。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_011/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_011/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_011/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_011_shadow_20260722.md)。有效进度仍为 `3/10`，下一批为 012。

#### 背包 OCR batch 012 自主采集拒绝（2026-07-22）

- 已执行授权的退出、选择和进入强化页导航，采集新的 Epic `+0` 速度套 Weapon；实际强化及其他资源操作点击为 `0`，装备状态未变化，继续复用 `20260722_132045` 快照。
- 原始截图 SHA-256 `a7185c809f1785df1178274d6015672c2ea8fac99035320824c21014436340d4`；严格唯一配对到 `instanceId=4182729560`，候选数 `1`，未人工指定实例编号。
- PaddleOCR 因等级 `85` 置信度 `0.973843` 和暴击率字段置信度 `0.970604` 低于 `0.98` fail closed；不生成建议、不计入通过样本，只保留为修复语料。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_012/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_012/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_012/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_012_shadow_20260722.md)。有效进度仍为 `3/10`，下一批为 013。

#### 背包 OCR batch 013 自主采集通过（2026-07-22）

- 新 Epic `+0` 速度套 Necklace 唯一配对到 `instanceId=4182712419`；PaddleOCR 全字段通过 `0.98`，建议哈希与同件 JSON 一致：`db11dd2e4cb91d7e1243956a0ea518e46accb965644a39fa31732bb550776c03`。
- 建议为 `continue / +3复核 / 速度潜力`，仅作只读影子输出。资源操作点击为 `0`，快照仍为 `20260722_132045`。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_013/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_013/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_013/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_013_shadow_20260722.md)。有效进度更新为 `4/10`，下一批为 014。

#### 背包 OCR batch 014 自主采集拒绝（2026-07-22）

- 新 Epic `+0` 速度套 Armor 唯一配对到 `instanceId=4184252756`；数值字段全部过 `0.98`，但品质/部位未识别，fail closed，不生成建议、不计入样本。
- 原始截图 SHA-256 `6a8c4439d05998b83090f6e30db649ca12df8b2422cbcf5e4fe4e8d9ee39c1ab`；资源操作点击为 `0`，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_014/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_014/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_014/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_014_shadow_20260722.md)。有效进度保持 `4/10`，下一批为 015。

#### 背包 OCR batch 015 自主采集通过（2026-07-22）

- 向下翻页后采集新的 Epic `+0` 速度套 Ring，唯一配对到 `instanceId=4182709045`；PaddleOCR 全字段过 `0.98`。
- OCR/JSON 建议哈希一致：`9de2eeef53ee0169b7505c31add8d4a0ef3c948ae03f3ae16e4dfc1657734259`，建议 `continue / +3复核 / 速度潜力`；仅作影子输出，资源操作点击为 `0`。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_015/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_015/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_015/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_015_shadow_20260722.md)。有效进度更新为 `5/10`，下一批为 016。

#### 背包 OCR batch 016 自主采集通过（2026-07-22）

- 从背包 `+15` 区域回翻到 `+0` 区域后采集新的 Epic `+0` 命中套 Helmet，唯一配对到 `instanceId=4169516003`；未重新抓包，继续复用 `20260722_132045` 快照。
- PaddleOCR 全字段过 `0.98`，最低必需字段置信度 `0.981290`；OCR/JSON 建议哈希一致：`d030dd4d970640ae7fbf36a4180cb57ffaeb85dbceda2c4afa6b7c84635bd7db`。
- 建议 `cautious_continue / +3复核 / 未来可期`，仅作影子输出，资源操作点击为 `0`。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_016/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_016/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_016/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_016_shadow_20260722.md)。有效进度更新为 `6/10`，下一批为 017；继续按每次翻页后截图确认的口径提高新装备覆盖率。

#### 背包 OCR batch 017 自主采集拒绝（2026-07-22）

- 新 Epic `+0` 命中套 88 级 Armor 唯一配对到 `instanceId=4185427540`；原始截图 SHA-256 `0c9b0052e1524d55fefc2285bea118b9b7d1e815d0bb92c81c7c899e0bb099ea`。
- OCR 数值字段均过 `0.98`，但等级及品质/部位组合未识别；正式建议模型不支持等级 `88`，finalizer 返回 `Unsupported equipment level: 88`。
- fail closed，不生成建议或 `ocr_shadow.json`，不修改正式策略，不计入样本；资源操作点击为 `0`，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_017/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_017/ocr_raw.json)、[报告](reports/ocr_stage1_backpack_batch_017_shadow_20260722.md)。有效进度保持 `6/10`，下一批为 018，后续只选等级 `85`。

#### 背包 OCR batch 018 自主采集通过（2026-07-22）

- 新 Heroic `+0` 速度套 Boots 唯一配对到 `instanceId=4181506046`；PaddleOCR 全字段过 `0.98`，最低必需字段置信度 `0.993683`。
- OCR/JSON 建议哈希一致：`cfcc2e030d3811e1731027dda89da5dcf636cff3891b393faf1c4e86a9bcef1b`，建议 `cautious_continue / +3复核 / 抗坦`，仅作影子输出。
- 资源操作点击为 `0`，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_018/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_018/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_018/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_018_shadow_20260722.md)。有效进度更新为 `7/10`，下一批为 019。

#### 背包 OCR batch 019 自主采集拒绝（2026-07-22）

- 新 Heroic `+0` 吸血套 Weapon 唯一配对到 `instanceId=4182203590`；原始截图 SHA-256 `29ba97ea6813e4a608c80e6e9a55c1696cdca03e5ce69d0936ddc4184641ba4d`。
- 已识别字段均过 `0.98`，但 OCR 漏掉“速度 3”整条副属性，触发 `gear_validation_failed:Heroic gear must have 3 substats at +0`。
- fail closed，不生成建议、不计入样本；资源操作点击为 `0`，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_019/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_019/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_019/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_019_shadow_20260722.md)。有效进度保持 `7/10`，下一批为 020。

#### 背包 OCR batch 020 自主采集拒绝（2026-07-22）

- 新 Epic `+0` 伤口套 Armor 唯一配对到 `instanceId=4182203589`；原始截图 SHA-256 `aecebbda0cb4238ea15fc104fb6f9423eb6144fdd97fa0adad7551587f5313c0`。
- 数值与等级字段过 `0.98`，但品质/部位 OCR 为空，触发 `low_confidence/unrecognized:slot` 与 `rank`；fail closed，不生成建议、不计入样本。
- 资源操作点击为 `0`，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_020/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_020/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_020/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_020_shadow_20260722.md)。有效进度保持 `7/10`，下一批为 021。

#### 背包 OCR batch 021 自主采集通过（2026-07-22）

- 新 Heroic `+0` 速度套 Weapon 唯一配对到 `instanceId=4181488027`；PaddleOCR 全字段过 `0.98`，最低必需字段置信度 `0.983678`。
- OCR/JSON 建议哈希一致：`0a21d7493a22e92a10615285bf68202e12e019c24292be25063564491cd3bc11`，建议 `cautious_continue / +3复核 / 半肉(血防)`，仅作影子输出。
- 资源操作点击为 `0`，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_021/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_021/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_021/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_021_shadow_20260722.md)。有效进度更新为 `8/10`，下一批为 022。

#### 背包 OCR batch 022 自主采集拒绝（2026-07-22）

- 新 Epic `+0` 速度套 Necklace 唯一配对到 `instanceId=4181507120`；原始截图 SHA-256 `f0beb90708a74172754ca59c0b6b5f38945f29a771a866bd6b9bed2bf6e8f8df`。
- 暴击伤害字段 `0.931981`、暴击率值 `0.945029` 低于 `0.98`，fail closed，不生成建议、不计入样本。
- 资源操作点击为 `0`，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_022/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_022/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_022/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_022_shadow_20260722.md)。有效进度保持 `8/10`，下一批为 023。

#### 背包 OCR 采集暂停（2026-07-22）

- 用户要求暂停以便手动操作游戏；有效进度保持 `8/10`，batch 023 未开始，当前状态 `paused_for_user_gameplay_8_of_10`。
- 暂停后不再发送任何模拟器或 ADB 输入。恢复时重新校验唯一设备并截图确认当前页面；不得复用暂停前坐标或默认装备状态未变化。

#### 背包 OCR batch 023 自主采集拒绝（2026-07-22）

- 用户恢复采集后，重新完成 Computer Use/ADB 闸门；Windows 截图仍为 `0x80004002`，按既有授权使用无 serial/IP 的 ADB 截图。新的 `backpack_detail` 与明确强化入口均经截图确认，只执行一次进入 `enhance` 的导航点击，资源操作点击为 `0`。
- 新 Epic `+0` 暴击套 Weapon 唯一配对到 `instanceId=4181478587`，候选数 `1`；原始强化页截图 SHA-256 `7273a40ed19763e7142cb11d1c3cd46fefeeb271a3ffb59fe46dd896c8db9e30`。
- 等级 `85` 的 OCR 置信度 `0.973843 < 0.98`，触发 `low_confidence:level`；fail closed，不生成建议、不计入样本。
- 未发现装备状态变化或资源消耗证据，未重新抓包，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_023/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_023/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_023/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_023_shadow_20260722.md)。有效进度保持 `8/10`，当前状态 `autonomous_backpack_ocr_collection_continuing_8_of_10`，下一批为 024。

#### 背包 OCR batch 024 自主采集拒绝（2026-07-23）

- 重新完成唯一 ADB 闸门和截图确认；退出旧强化页、选择新 85 级装备、点击明确强化入口均为导航操作，资源操作点击为 `0`。最终截图 SHA-256 `2ea8053d9ee01dc3ac249cf28a5ee2892c21a5ea490fc2eee3375e16dc2867c6`。
- 新 Epic `+0` 速度套 Armor 唯一配对到 `instanceId=4184252756`，候选数 `1`。初次部位误记导致零候选，修正为 Armor 后配对通过；未人工指定实例编号。
- OCR 触发 `low_confidence:slot`、`unrecognized:slot`、`low_confidence:rank`、`unrecognized:rank`，fail closed，不生成建议、不计入样本。
- 未发现装备状态变化或资源消耗证据，未重新抓包，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_024/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_024/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_024/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_024_shadow_20260723.md)。有效进度保持 `8/10`，当前状态 `autonomous_backpack_ocr_collection_continuing_8_of_10`，下一批为 025。

#### 背包 OCR batch 025 自主采集拒绝（2026-07-23）

- 完成唯一 ADB 闸门和只读导航，未执行资源操作；新 Heroic `+0` 抵抗套 Necklace 自动唯一配对到 `instanceId=4184397469`，但与 batch 011 重复，跨批唯一性失败。
- 效果抗性数值 OCR 置信度 `0.975840 < 0.98`，触发 `low_confidence:substats[2].value`；fail closed，不生成建议、不计入样本。
- 未发现装备状态变化或资源消耗证据，未重新抓包，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_025/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_025/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_025/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_025_shadow_20260723.md)。有效进度保持 `8/10`，当前状态 `autonomous_backpack_ocr_collection_continuing_8_of_10`，下一批为 026；后续增加进入强化页前的只读历史实例预检。

#### 背包 OCR batch 026 自主采集通过（2026-07-23）

- 重新完成唯一 ADB 闸门、只读预检和背包向下翻页；历史重复与多候选均在进入强化页前拦截，未执行资源操作。
- 新 Epic `+0` 速度套 Necklace 唯一配对到 `instanceId=4181502333`，候选数 `1`；PaddleOCR 全字段通过，最低字段置信度 `0.989018`。
- OCR/JSON 建议哈希一致：`3d5c040e8ca7186d5a8b5c2c1e4fd92165edb0d50989a267bd4fb127c9524912`；建议 `cautious_continue`、`next_check_at=3`，仅作只读影子输出。
- 未发现装备状态变化或资源消耗证据，未重新抓包，继续复用 `20260722_132045` 快照。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_026/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_026/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_026/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_026_shadow_20260723.md)。有效进度更新为 `9/10`，当前状态 `autonomous_backpack_ocr_collection_continuing_9_of_10`，下一批为 027。

#### 背包 OCR batch 027 通过与 10 样本收口（2026-07-23）

- 重新完成唯一 ADB 闸门、背包翻页和进入强化页前只读预检；新 Heroic `+0` 速度套 Boots 唯一配对到未采样 `instanceId=4181504137`，候选数 `1`。
- PaddleOCR 全字段通过，最低字段置信度 `0.993683`；OCR/JSON 建议哈希一致 `4fad1a5dcf256a4d4ff0f44f478ee6219c405ca5c0f3b269ea48e9a3cc740f01`。
- 通过批次 004、005、009、013、015、016、018、021、026、027 共 `10/10`，实例 ID 全部唯一；达到目标后已立即停止所有游戏界面和 ADB 输入。
- 未执行实际强化或其他资源操作，未重新抓包，继续保留可信快照 `20260722_132045`。当前状态更新为 `ready_for_single_item_manual_confirmation`；不扩展为自动强化授权，不修改正式策略/DP/评分/资源模型/GUI/Holdout/自动化规则。
- 产物：[manifest](manual_acceptance/ocr_stage1/batch_027/manifest.json)、[OCR 原始结果](manual_acceptance/ocr_stage1/batch_027/ocr_raw.json)、[影子结果](manual_acceptance/ocr_stage1/batch_027/ocr_shadow.json)、[报告](reports/ocr_stage1_backpack_batch_027_shadow_20260723.md)。

#### 背包 OCR 10 样本收口验证（2026-07-23）

- batch 023 至 027 原始 PNG 哈希、manifest、OCR 结果、`click_performed=false` 与通过实例唯一性核对通过；有效样本 `10/10`、唯一实例 `10/10`。
- OCR/配对/只读闸门定向测试 `25` 项、MuMu 快照测试 `7` 项、批次配对测试 `2` 项全部通过，退出码 `0`；`git diff --check` 退出码 `0`，仅有既有 LF/CRLF 提示。
- 全量 `tools/run_all_tests.py` 两次均因工具时间上限退出，超时前已执行项全部 `PASSED`，但没有完整退出码；当前验证状态为 `targeted_passed_full_suite_timed_out`，不得写成全量通过。
- 当前状态保持 `ready_for_single_item_manual_confirmation`，不继续游戏输入，不执行实际强化，不修改正式策略或自动化范围。

#### 当前答复与后续边界（2026-07-23）

- 背包 OCR 只读采集已完成 `10/10`，通过实例全部唯一；当前没有待执行采集批次，达到收口后保持停机。
- 后续若进入单件人工确认，必须先建立或更新独立 `*任务说明.md`、同步本总计划，并重新取得针对实际确认操作的明确授权；不得把当前 `ready_for_single_item_manual_confirmation` 状态解释为自动强化授权。
- 在上述前置条件满足前，保留可信快照 `20260722_132045` 和全部 batch 产物，不重新抓包、不发送模拟器/ADB 输入、不执行资源操作，也不修改正式策略、DP、评分、资源模型、GUI、Holdout 或自动化规则。

#### 人工审查流程（待新任务授权，2026-07-23）

- 先建立独立单件人工确认任务，明确目标样本、允许页面、停止条件和资源边界；当前 OCR 任务不自动进入该阶段。
- 无消耗审查顺序：读取 `computer-use` 技能 -> 运行 MuMu `adb.exe devices -l` 并确认唯一 `device` -> 截图确认页面和装备 -> 保存时间、设备信息、页面类型、PNG SHA-256 -> 用可信快照及同批 `reader_result.json` 唯一配对并逐字段核对。
- `0.98` 闸门、候选唯一性、页面明确性、实例去重和 OCR/JSON 建议哈希必须全部通过；否则 fail closed，不生成建议，不人工指定 `instanceId`。
- 只有实际验证强化结果时才允许单件、单复核节点的资源操作；必须在新任务中逐项授权材料来源和资源上限，并记录操作前后截图及资源变化。未逐项授权的材料选择、自动登记、出售、转换、锁定、重铸或替换仍默认禁止。

#### 人工审查填写完成记录（2026-07-23）

- [人工审查包](reports/ocr_stage1_manual_review_packet_20260723.md) 已整理全部 10 个 OCR 通过样本，用户已完成逐件填写。
- 人工接受 4 件：004、013、015、016；人工拒绝 6 件：005、009、018、021、026、027。该人工筛选仅作为审查记录，不改写 OCR 技术通过数 `10/10`，不改变正式策略、DP、评分、资源模型或 Holdout。
- 第二项实际强化验证仍为 `pending_new_task_and_explicit_authorization`。在独立任务说明和明确授权完成前，不发送模拟器/ADB 输入，不执行强化或任何资源操作。

#### 会话边界与启动条件（2026-07-23）

- 新会话不是技术必需，但推荐用于隔离只读 OCR 采集与实际强化验证的审计上下文；是否换会话不改变前置闸门。
- 实际验证必须先有新的独立任务说明和新的明确授权，明确装备、复核节点、材料来源、资源上限及停止条件；不得复用本轮只读采集或导航点击授权。
- 开始操作前仍须重新读取 `computer-use` 技能、运行唯一 MuMu `device` 检查并截图确认当前页面。

#### 单件人工确认强化结果验证任务已建立（2026-07-23）

- 独立任务说明：[单件人工确认强化结果验证任务说明](单件人工确认强化结果验证任务说明.md)。
- 当前状态：`awaiting_explicit_operation_authorization`。该任务只完成新会话交接准备，尚未取得实际强化、材料来源和资源上限的逐项授权。
- 任务范围限定为一件人工审查接受样本（batch `004/013/015/016` 中择一）、一个复核节点（默认 `+3`），先完成唯一 ADB、截图和 JSON 配对闸门；授权前不得发送模拟器/ADB 输入。
- 达到一个复核节点后立即停止；结果只写入独立验证报告，不回写正式策略、DP、评分、资源模型、GUI、Holdout 或自动化规则。
- 新会话交接状态：`handoff_ready_awaiting_explicit_operation_authorization`。初始化阶段只允许读取文档、读取技能、唯一设备检查、截图和只读配对预检；实际强化前仍需逐项明确授权。

#### 单件人工确认只读前置检查（2026-07-23）

- 新会话已完成任务说明、`AGENTS.md`、本计划和 `computer-use` 技能读取；唯一 MuMu `device=1`，未保存设备序列或地址。
- Windows 截图接口仍返回已知错误 `0x80004002`，按既有规则使用不指定设备的 ADB 只读截图。当前页面为游戏主城，不显示目标装备或强化页，故状态推进为 `fail_closed_current_page_not_target_equipment_awaiting_explicit_operation_authorization`；此前 `handoff_ready_awaiting_explicit_operation_authorization` 仅作历史交接状态。
- 可信快照 `20260722_132045` 的 `player_data.json` 与同批 `reader_result.json` 哈希核对一致，装备/唯一实例/原始 `raw` 数量均为 `2670`。人工接受 batch `004/013/015/016` 使用正式配对器重配后均为单候选，最低 OCR 置信度均不低于 `0.98`，建议哈希均一致。
- 本轮只读截图 `1` 次；界面输入、状态变更型 ADB 输入、资源操作点击均为 `0`。文件侧配对通过不代表目标已选择，也不构成强化授权。
- 产物：[只读预检报告](manual_acceptance/single_item_confirmation/preflight_20260723_102914/preflight_report.md)、[预检 manifest](manual_acceptance/single_item_confirmation/preflight_20260723_102914/preflight_manifest.json) 和同目录四份脱敏配对结果。
- 下一步仅等待用户逐项确认目标 batch/装备、复核节点、材料来源和资源上限；四项齐全后先重新执行唯一设备与目标页面截图闸门，再决定是否允许最小操作。不得修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化规则。

#### 用户人工进入背包后的只读续检（2026-07-23）

- 用户已人工进入背包并允许继续下一步，当前状态更新为 `user_positioned_backpack_awaiting_readonly_recheck_and_explicit_operation_authorization`。
- 本次新增授权仅覆盖技能读取、唯一设备检查、截图和只读配对；不覆盖导航点击、实际强化、材料选择或任何资源消耗。
- 下一步重新截图确认背包页面与当前装备，并仅使用可信快照 `20260722_132045` 及同批 `reader_result.json` 进行严格唯一配对。通过后仍须取得目标、复核节点、材料来源和资源上限四项授权。

#### 背包页面续检结果（2026-07-23）

- 用户人工进入背包后，唯一设备检查通过；Windows 截图接口仍失败，已保存新的只读 ADB 截图。
- 当前选中装备为 `88` 级戒指；本任务接受范围 `004/013/015/016` 的可信快照候选均为 `85` 级，严格配对候选为 `0`，因此状态更新为 `fail_closed_current_selection_not_accepted_target_awaiting_explicit_operation_authorization`。
- 本轮未执行界面点击、导航、实例 ID 人工指定、强化或资源操作。证据见 `manual_acceptance/single_item_confirmation/preflight_20260723_102914/`。
- 下一步仅能在当前选中装备属于授权目标后重新截图配对；实际强化仍需目标、复核节点、材料来源和资源上限四项明确授权。

#### 004 页面重检设备闸门失败（2026-07-23）

- 用户表示已打开人工接受样本 batch `004`，但本次只读复检先执行唯一 MuMu ADB 闸门，结果为 `device_count=0`、其他状态设备 `0`。
- 不指定设备的 ADB 截图随后因 `no devices/emulators found` 失败，输出为 `0` 字节；该输出不是有效页面证据，不替换既有预检截图，也不进行页面或装备配对。
- 未执行任何界面点击、导航、强化、材料选择或资源操作；状态更新为 `fail_closed_device_unavailable_awaiting_readonly_recheck_and_explicit_operation_authorization`。
- 继续条件是恢复唯一 `device` 后重新截图并完成 004 的严格唯一配对；实际强化前仍需目标、复核节点、材料来源和资源上限四项授权。正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛和自动化规则保持不变。

#### batch 004 只读详情核验通过（2026-07-23）

- 用户重启 MuMu 后人工打开背包并查看 batch `004` 详情；唯一设备闸门重新通过，结果为 `device=1`、其他状态设备 `0`。
- Computer Use 两次截图均返回 `SetIsBorderRequired ... 0x80004002`，未产生输入；使用不指定设备的 ADB 二进制流回退截图成功，页面明确为背包装备详情。有效截图 SHA-256 为 `60FF6694896BF279EE239C98E289A3E28F628A241CCD4558A7AB2BA0E3D06497`。
- 截图字段与正式 `pair_batch_004.json` 一致：85 级速度套戒指、生命值 `12%` 主属性、防御力 `5%`、暴击率 `5%`、攻击力 `6%`、攻击力 `38`、GS `27`；可信快照同批双文件哈希和配对闸门通过，候选数 `1`，实例由配对器确定为 `4181490939`，最低 OCR 置信度 `0.983459`，建议哈希一致。
- 状态更新为 `target_004_readonly_verified_awaiting_explicit_operation_authorization`。只读核验不等于强化授权；仍需目标、复核节点、材料来源和资源上限四项明确授权。正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛和自动化规则保持不变。

#### batch 004 +3 操作授权与操作前闸门通过（2026-07-23）

- 用户已明确授权 batch `004`、复核节点 `+3`，材料仅限最多 2 个饰品强化粉末和 1 个下级饰品强化石，金币上限 `17600`。
- 操作前唯一设备、页面、装备、可信快照双文件哈希、唯一候选、`0.98` OCR 门槛和建议哈希均通过；当前强化页材料预览与授权上限完全一致。
- 状态推进为 `authorized_preflight_passed_awaiting_single_enhance_click`；只允许一次最小强化操作，到达 `+3` 后立即停止。独立记录目录为 `manual_acceptance/single_item_confirmation/operation_004_plus3_20260723_153606/`。
- 正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛和自动化规则保持不变。

#### batch 004 +3 操作完成并停止（2026-07-23）

- 操作前闸门通过后仅发送一次授权强化输入，目标装备到达 `+3`，立即停止；未执行第二次强化或其他资源操作。
- 结果为生命值 `12% -> 19%`、攻击力副属性 `6% -> 13%`、GS `27 -> 37`，其余已核验副属性保持不变。
- 材料消耗为粉末 `2`、下级强化石 `1`；金币消耗 `17600`，均未超过本轮授权上限。
- 独立结果见 `manual_acceptance/single_item_confirmation/operation_004_plus3_20260723_153606/operation_report.md` 与 `operation_manifest.json`；当前状态为 `completed_target_004_plus3_stopped`。
- 本次人工验证结果不回写正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化规则。
- 004 的影子建议为 `cautious_continue`、下次复核 `+3`、目标“半肉(通用)”；实际结果到达同一节点，作为单件验证一致性记录，仍不进入正式策略。

#### 单件人工确认强化结果离线汇总器开发（2026-07-23）

- 实际 batch `004` / `+3` 验证已完成并停止；后续阶段限定为本地离线证据汇总，不再发送模拟器或 ADB 输入。
- 独立任务说明：[单件人工确认强化结果离线汇总器任务说明](单件人工确认强化结果离线汇总器任务说明.md)。
- 实现目标：读取 `operation_manifest.json` 及其同批 `pair_batch_004.json` 基线，校验唯一配对、可信快照哈希、`0.98` OCR 门槛、影子建议、基线/结果属性变化、授权资源、实际消耗、单次动作、复核节点和停止原因，输出 JSON 与 Markdown 离线报告。
- 输出仅属于人工验证证据整理，不是正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化规则的变更；`verified` 只表示该证据包内部一致。
- 当前状态：`completed_verified_batch_004_offline_summary`。定向测试 `5/5`、Python 语法检查和 CLI smoke 均通过；报告为 [JSON](reports/single_item_confirmation_summary_20260723.json) 与 [Markdown](reports/single_item_confirmation_summary_20260723.md)。下一步只记录为：若继续人工强化验证，先为 batch `013/015/016` 逐件建立新的独立任务说明并重新取得目标、节点、材料来源和资源上限授权。

#### batch 013 +3 设备发现诊断与操作授权（2026-07-24）

- 新独立任务段见：[单件人工确认强化结果验证任务说明](单件人工确认强化结果验证任务说明.md)“batch 013 +3 设备发现诊断与操作授权记录”。
- 用户已明确授权目标为 `batch 013` / 85 级 Epic 速度套项链，当前只执行 `+0→+3`；材料来源允许粉末、下级强化石、上级强化石，资源上限为粉末 `2`、下级强化石 `1`、上级强化石 `0`、金币 `17600`。`+6` 不属于本次一次性操作，必须在 `+3` 停止后重新授权。
- 由于识别反复失败，本轮先建立只读诊断回路：MuMu 进程状态 -> 强制 ADB 唯一 `device` -> 当前窗口/截图 -> 可信快照 `20260722_132045` 双文件严格唯一配对。未定位根因前不修改 OCR 方法、配对器、`0.98` 门槛、正式策略、DP、评分、资源模型、GUI、Holdout 或自动化规则。
- 最近一次强制 ADB 检查结果为 `device_count=0`、其他状态设备 `0`，故状态为 `fail_closed_device_unavailable_awaiting_process_and_readonly_recheck`；未截图、未配对、未选材、未强化、未消耗资源。下一步先检查 MuMu 相关进程，恢复后重新运行设备闸门。

#### batch 013 设备发现诊断结果（2026-07-24）

- 已先检查 MuMu 进程；主进程、虚拟机进程和相关服务均存在且响应正常。
- 按强制顺序重新运行 MuMu ADB `devices -l`，仍为 `device_count=0`、其他状态设备 `0`。因此当前红灯位于 Android 实例/ADB 桥接，而不是 OCR 字段识别；桌面进程存在不能证明 ADB 设备已上线。
- 尚未获得新的截图，不能进入页面识别、严格配对或强化操作；未选材、未点击、未消耗资源。OCR 方法、配对器、`0.98` 门槛和正式规则保持不变。
- 当前状态更新为 `fail_closed_mumu_processes_alive_but_adb_device_unavailable`。下一步仅为恢复唯一 `device` 后重新做 `batch 013` 页面截图和只读配对。

#### MuMu ADB 连接模式处置（2026-07-24）

- 当前设置为“本地和远程连接”；本项目只需要 MuMu 自带 ADB 的本机连接，建议用户手动改为“仅本地连接”并完整重启 MuMu，使 ADB 桥接重新初始化，同时避免不必要的远程暴露。
- 该设置不是 OCR 识别方法，也不能单独解释 `device_count=0`。重启后必须重新执行唯一 `device` 闸门；通过后才允许截图和 `batch 013` 只读配对，未通过则继续 `fail_closed`。

#### 本地连接切换后的复检结果（2026-07-24）

- 用户已将 MuMu ADB 设置改为“仅本地连接”；复检时 MuMu 相关主进程、虚拟机和服务均存在且响应正常。
- 强制 ADB 复检仍为 `device_count=0`、其他状态设备 `0`，说明仅切换模式尚未完成 Android 实例的 ADB 注册。尚未截图、配对或进入游戏操控。
- 下一步必须完整退出并重新启动 MuMu 实例，再重新执行唯一 `device` 闸门；OCR 方法和正式规则继续保持不变。

#### batch 013 +3 操作完成并停止（2026-07-24）

- 完整重启后，MuMu 进程与唯一 ADB `device` 闸门恢复正常；Computer Use 仍因已知 `0x80004002` 无法截图，按既有授权使用无 serial/IP 的 ADB 二进制流截图和从当前截图派生的输入坐标。
- 新页面截图、可信快照 `20260722_132045` 双文件哈希、正式 `pair_batch_013.json` 唯一候选、实例 `4182712419`、最低 OCR 置信度 `0.990471` 和建议哈希全部通过。操作前材料精确为粉末 `2`、下级强化石 `1`、上级强化石 `0`、金币 `17,600`，预览目标 `+3`。
- 仅发送一次授权强化确认；结果为主属性生命值 `12% -> 19%`、副属性速度 `3 -> 7`（`+4`），其余基线副属性不变。游戏官方展示装备分数 `30 -> 41` 仅作结果记录，不作为本次强化策略输入，也不改变正式评分模型。金币 `346,670,175 -> 346,652,575`，消耗 `17,600`；材料消耗与授权一致。
- 到达 `+3` 后立即停止，未继续 `+6`，未发送第二次强化或其他资源操作。证据见 [操作报告](manual_acceptance/single_item_confirmation/operation_013_plus3_20260724_112042/operation_report.md) 与 [operation manifest](manual_acceptance/single_item_confirmation/operation_013_plus3_20260724_112042/operation_manifest.json)。
- 当前状态：`completed_target_013_plus3_stopped; offline_summary_verified`。`+6` 不在本次授权内；不修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化规则。

#### batch 013 离线汇总复核（2026-07-24）

- 013 单包离线汇总为 `verified`，退出码 `0`，`1/1` 操作包通过且 `fail_closed=0`；全部设备、页面、OCR、配对、可信快照、属性、资源、单次动作、复核节点和停止闸门为 `true`。
- 根目录合并汇总包含 batch `004` 与 `013`，结果为 `manifest_count=2`、`verified_count=2`、`fail_closed_count=0`；004 旧证据保持通过。
- `tests/test_single_item_confirmation.py` 的旧单 manifest 断言已最小更新为 004/013 两个操作包，并同时验证合并 Markdown 包含两个 batch；最终定向测试 `5/5` 通过、退出码 `0`。生产汇总器未修改。
- 报告见 [013 单包 JSON](reports/single_item_confirmation_summary_batch_013_20260724.json)、[013 单包 Markdown](reports/single_item_confirmation_summary_batch_013_20260724.md)、[合并 JSON](reports/single_item_confirmation_summary_20260724.json) 和 [合并 Markdown](reports/single_item_confirmation_summary_20260724.md)。该 `verified` 仅表示证据包内部一致，不进入正式策略或自动化发布。

#### batch 013 Git 保存与远程推送阻断（2026-07-24）

- 分支 `codex/batch-013-plus3-verification-20260724` 已创建本地提交 `ad8e983`，保存任务文档、测试、013 操作截图/manifest/报告和 20260724 离线汇总；其他工作区既有代码改动未暂存、未回退。
- Git 提交后的自动 geometric repack 因既有无效引用 `main - 副本` 报错，但提交对象和 HEAD 校验正常；为避免破坏文件，未删除或修复该引用。
- 推送到 GitHub `origin` 被安全策略拦截：提交包含游戏截图和工作区文档，远程仓库的私有/可信状态未建立。不得绕过，需先向用户说明外传范围并取得新的显式授权。
- 用户已明确授权将本轮截图、文档、测试和报告推送至 `origin`；分支已成功推送，草稿 PR 已创建：[PR #1](https://github.com/5kjpywnfh8-cmd/epic-7-auto-gear-reinforcer/pull/1)。当前发布状态：`git_published_draft_pr_open`。本地证据、提交和分支完整保留。

#### batch 013 工作区待提交项与扫描卡顿审计（2026-07-25）

- 审计开始时，分支本地 HEAD 与远程 `origin/codex/batch-013-plus3-verification-20260724` 均为 `7896713`；本段文档同步后以远程分支最新提交为准。batch `013` 本轮授权范围内的截图、文档、测试和报告已完整发布，没有需要补交到当前草稿 PR 的文件。
- 工作区另有 `31` 个已跟踪文件改动，约 `3013` 行新增、`312` 行删除，覆盖既有策略、资源模型、GUI、校准、测试和旧文档；这些内容与 batch `013` 无关，保持未暂存、未回退、未覆盖，后续必须通过独立任务审阅并在单独分支发布，不得混入当前 PR。
- 根目录可见 `82` 个名称含“`- 副本`”的顶层文件，且既有重复引用仍存在；大量副本和未跟踪内容会触发工作区索引。检查时 OpenCode 为主要高 CPU 进程，Git 仅短时运行只读状态命令。
- 本轮未删除、移动或清理文件，未修改 Git 配置或忽略规则。当前只使用 `git status --untracked-files=no` 避免全量未跟踪扫描；任何副本整理必须先形成独立清单、逐项哈希比对和可恢复备份，再取得用户明确授权。

#### 工作区整理优先级与只读盘点任务（2026-07-25）

- 当前决定为先整理工作区边界，再继续功能开发；原因是大量副本、持续索引和 `31` 个来源未审的既有代码改动会增加卡顿、误提交及覆盖风险。
- 独立任务说明：[工作区副本盘点与非破坏性整理任务说明](工作区副本盘点与非破坏性整理任务说明.md)。当前状态为 `task_defined_awaiting_readonly_inventory_start; destructive_cleanup_not_authorized`。
- 第一阶段只允许分批、限速的只读目录清点、大小核对、SHA-256 比对和 Git 状态分类；不删除、不移动、不改引用、不改 Git 配置、不结束用户进程，也不暂存现有 `31` 个改动。
- 只读盘点完成后必须先向用户提交“保留 / 候选隔离 / 内容分叉 / 无法判断”清单。实际隔离或删除需要新的独立任务和逐项目标、目标目录、恢复方式授权；完成该闸门后才恢复新功能开发。

#### 工作区直接副本分批只读盘点结果（2026-07-25）

- 前述“`task_defined_awaiting_readonly_inventory_start`”状态已被本节覆盖；当前状态为 `partial_direct_inventory_complete_full_workspace_untracked_scan_deferred; destructive_cleanup_not_authorized`。
- 已完成根目录、`e7_enhance`、`manual_acceptance`、`reports` 的一级直接文件盘点，共 `418` 个候选：`415` 个 SHA-256 完全一致，`3` 个大小分叉，`0` 个哈希分叉，`0` 个缺少原件。大小分叉均为根目录任务计划/任务说明副本，不能覆盖当前原件。
- 一级直接统计：根目录 `165` 文件/`82` 副本；`e7_enhance` `6`/`3`；`manual_acceptance` `62`/`31`；`reports` `608`/`302`；未发现目录联接或重解析点。
- 已发现候选中 Git 跟踪 `0` 个、`.gitignore` 命中 `126` 个、未跟踪且未忽略 `292` 个；全工作区未跟踪扫描因卡顿风险未执行，因此本结果不是完整仓库盘点。
- Git 前后 HEAD 与远程均为 `5f755af`，既有已跟踪改动保持 `31` 个，暂存区保持 `0`；`.git` 内 `7` 个副本命名条目保持不动。报告见 [JSON](reports/workspace_duplicate_inventory_20260725.json) 与 [Markdown](reports/workspace_duplicate_inventory_20260725.md)。
- 本轮仍未删除、移动、重命名、覆盖文件，未修改 Git 配置、引用、索引或正式策略。`415` 个精确副本仅进入“可恢复隔离候选”，下一步须先由用户审核清单，再建立独立隔离任务并逐项授权。

#### 415 个精确副本可恢复隔离任务已授权（2026-07-25）

- 用户已明确授权只处理盘点 JSON 中 `415` 个 `exact_match` 候选，移至工作区外 `C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\`，保留相对路径和恢复清单，不删除源文件。
- 独立任务说明：[415 个精确副本可恢复隔离任务说明](415个精确副本可恢复隔离任务说明.md)。实际移动前必须验证目标目录不存在/为空、源文件和对应原件均存在且双侧哈希与报告一致、无冲突和联接点。
- 3 个分叉文档、`.git` 内部条目、31 个既有代码/文档改动和未盘点深层文件均排除。任何预检失败、目标冲突、权限/空间异常或哈希变化都 `fail_closed`，不进入移动。
- 当前阶段状态：`authorized_preflight_pending; move_not_started`。移动后需逐项验证目标哈希、源路径消失、原件未变，并将结果写回任务说明和总计划。

#### 415 个精确副本隔离移动前预检通过（2026-07-25）

- 目标目录 `C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\` 尚不存在且位于工作区外；manifest 中 `415` 个候选和 `415` 个对应原件均存在，源/原件共 `830` 个 SHA-256 全部匹配，目标冲突 `0`。
- 预检阶段未创建目录、未移动、未删除、未覆盖文件；Git 既有 `31` 个改动和暂存区 `0` 保持不变。Manifest 见 [workspace_isolation_manifest_20260725.json](reports/workspace_isolation_manifest_20260725.json)，状态为 `planned`。
- 当前状态更新为 `preflight_passed_move_pending`。下一步仅允许按 manifest 逐项移动并验证目标哈希、源路径消失和原件不变；任何异常立即 `fail_closed`。

#### 415 个精确副本隔离完成并验证（2026-07-25）

- 按已授权 manifest 将 `415` 个 exact-match 副本移至 `C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\`，实际移动 `415/415`，无冲突和错误。
- 移动后 `415/415` 目标哈希一致、`415/415` 源路径消失、`415/415` 原件未变化；结果见 [JSON](reports/workspace_isolation_result_20260725.json)、[Markdown](reports/workspace_isolation_result_20260725.md) 和 [manifest](reports/workspace_isolation_manifest_20260725.json)。
- 未删除、覆盖文件，未修改 Git 配置、引用、索引、正式策略或用户进程。3 个分叉副本、`.git` 内部条目、31 个既有改动及未盘点深层文件继续排除。
- 当前整理状态为 `partial_direct_inventory_complete_isolation_completed; deeper_unscanned`；整理闸门完成，下一步可恢复功能开发，不自动继续处理剩余文件。

#### 功能开发恢复边界（2026-07-25）

- 工作区整理闸门已完成，允许恢复代码开发；未盘点深层未跟踪文件、3 个内容分叉副本和 `.git` 内异常引用继续保持不处理。
- 当前分支仍有 `31` 个用户既有未提交改动，涉及策略、资源模型、GUI、校准、测试和旧文档；本轮不暂存、不回退、不覆盖，也不混入 batch `013` 草稿 PR。
- 单件人工确认离线汇总器已完成并验证 batch `004/013`；人工验证 `+3` 的实际操作不再扩展为自动化功能，正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛和自动化规则保持不变。
- 状态更新为 `development_unblocked_awaiting_scoped_code_task`。下一项代码开发必须先建立独立任务说明，明确目标文件、禁止修改项和验证命令，再使用独立精确提交；当前不自动选择或启动新的功能。

#### 工作区未提交数量复核（2026-07-25）

- 当前 Git 跟踪 `202` 个文件，真实已跟踪修改为 `31` 个、暂存区为 `0`；差异约为 `3013` 行新增和 `312` 行删除。IDE 显示的一万多个更改不能解释为一万多个代码修改。
- 折叠未跟踪目录后共有 `400` 个状态条目，其中 `31` 个为已跟踪修改、`369` 个为未跟踪文件/目录；完整 `--untracked-files=all` 递归统计在 `60` 秒内超时，说明深层未跟踪内容仍是扫描卡顿来源。
- 当前决定为“需要继续整理，但不直接清空”。31 个既有改动必须先独立审阅并决定分组提交；未跟踪内容需另建只读分类任务，再区分提交、保留、隔离、忽略和无法判断项。
- 状态更新为 `cleanup_recommended_full_untracked_scan_timed_out; destructive_cleanup_not_authorized`。本轮未删除、移动、回退或暂存任何用户文件，不执行 `git clean`，不修改 `.git` 异常引用。

#### 工作区完整清理与 Git 收口（2026-07-25，阶段一进行中）

- 用户已授权执行完整收口，最终闸门为 `git status --porcelain` 无输出、本地与远程同步、无暂存和未推送提交；未知文件删除和私人数据外传仍必须在精确清单后重新确认。
- 唯一权威任务说明：[工作区完整清理与Git收口任务说明](工作区完整清理与Git收口任务说明.md)。禁止 `git reset`、`git restore`、`git checkout` 回退、`git clean`、宽泛忽略和 `git add -A`；不得修复 `.git` 内异常引用。
- 起始分支为 `codex/batch-013-plus3-verification-20260724`，HEAD `0c41890d8f064c8fdd4af40c3f512cdde80e2708` 与远程同步；已创建专用分支 `codex/worktree-cleanup-20260725`。
- 起始已跟踪修改为 `31` 个文件、约 `3013` 行新增和 `312` 行删除，暂存区 `0`；这些既有改动必须按任务来源和功能分组审阅、验证、独立提交，不能整体提交或回退。
- 前次折叠未跟踪状态为 `369` 个文件/目录，完整递归扫描曾在 60 秒超时；本轮改用目录级、限时、可续跑分类。415 个已隔离精确副本保持恢复来源，3 个分叉副本和 `.git` 异常项继续保留。
- 阶段一安全基线已完成：[Markdown](reports/worktree_cleanup_baseline_20260725.md) / [JSON](reports/worktree_cleanup_baseline_20260725.json)。完整未跟踪枚举为 `255840` 个文件；工作区排除 `.git` 后为 `256449` 个文件、`2379545803` 字节。
- `reports` 是主要卡顿来源：`255347` 个文件、`1972226214` 字节，主体为研究 resume 分片；`manual_acceptance` 含私人游戏截图和操作证据，未经本轮外传授权不得推送。现有 `reports/*.md` 忽略规则过宽，必须在逐份分类后移除或替换。
- 当前状态：`phase_1_complete_phase_2_tracked_review_in_progress_no_cleanup_performed`。尚未删除、忽略、移动、暂存、提交或上传任何待分类内容；下一步审阅全部 31 个既有已跟踪修改并按任务来源/功能分组。
- 深层副本盘点已完成：[报告](reports/worktree_cleanup_deep_duplicate_inventory_20260725.md)、[SHA-256 manifest](reports/worktree_cleanup_deep_duplicate_manifest_20260725.csv)、[JSON 汇总](reports/worktree_cleanup_deep_duplicate_summary_20260725.json)。排除 `.git` 后共 `127762` 个候选，`127759` 个 exact-match、3 个内容分叉、原件缺失 `0`；目标冲突 `0`。
- `127759` 个 exact-match 仅允许可恢复隔离到 `C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\`，不删除源的语义之外不覆盖目标；3 个内容分叉、`.git` 条目、已跟踪修改和其他未跟踪内容均排除。当前状态：`exact_duplicate_isolation_preflight_passed_move_pending`。
- `127759/127759` 个 exact-match 已按 manifest 可恢复移动并逐项验证；结果 manifest 位于工作区外隔离目录。工作区现只剩 3 个内容分叉副本，均保留原位。深层副本导致的 Heroic `160`/`80` 审计污染已消除，历史审计回归 `1/1` 通过，未修改审计器或测试。
- 当前状态更新为 `phase_2_tracked_review_in_progress_deep_duplicate_isolation_completed`。下一步审阅 31 个既有已跟踪修改，并逐步替换过宽的 `reports/*.md` 忽略规则。

#### 生成分片忽略规则精确化（2026-07-25）

- 只读分类确认 `reports/` 当前有 `127855` 个文件、`1100733173` 字节；其中 `60` 个 `*_resume_*`、测试日志、调试和可视化目录贡献了约 `12.78` 万个扫描入口，是 IDE 卡顿的主要来源。
- 已将过宽的 `reports/*.md` 替换为精确规则：`reports/*_resume_*/`、`reports/test_logs_*/`、`reports/epic_threshold_matrix_single_debug/`、`reports/visual/`、`runtime_cache/` 和 `.vscode/`。规则只隐藏可再生的续跑/诊断/本地缓存内容；顶层权威报告和未分类报告保持可见，文件均未删除。
- 当前状态：`phase_2_generated_resume_noise_ignored_tracked_review_pending`。31 个已跟踪修改仍未暂存，98 个源码/测试/工具未跟踪文件、316 个顶层报告文件和 276 个 `manual_acceptance` 入口继续按来源审阅；其中私人操作证据未经本轮新外传授权不得推送。

#### 已跟踪功能改动分组收口（2026-07-25）

- 已完成并推送资源校准组：`32` 项定向测试通过，提交 `fc4e2e9`。
- 已完成并推送策略/DP/资源模型组：`92` 项定向测试通过，提交 `ede8e5e`；路线复核输出显示最大分歧率 `0.0%`，未改变正式策略发布边界。
- 已完成并推送 GUI 组：支持真实样本管理的 GUI 支持层和 Qt 应用测试共 `33` 项通过，提交 `77baf88`。
- 三个提交均已推送到 `origin/codex/worktree-cleanup-20260725`。剩余已跟踪修改仅为 6 个说明文档，下一步完成文档校对和提交；源码、测试和工具新增文件、顶层报告、私人操作证据继续单独审核，不能用 `git add -A` 收口。

#### 未跟踪代码与项目文档收口（2026-07-25）

- 已补齐已发布代码的运行时依赖：5 个策略/GUI 模块与 6 个测试提交为 `04bb9af`；43 项相关测试通过。
- 只读 OCR、MuMu 新结构配对和单件离线汇总代码共 21 个文件提交为 `4cad58d`；34 项纯代码/合成夹具测试通过，真实截图、PCAP、玩家数据和操作证据未进入提交。
- 其余可复现研究代码、测试和工具共 67 个文件提交为 `cef92cc`；统一全量测试 `tools/run_all_tests.py` 真实退出码 `0`，耗时 `292.8` 秒，输出 `ALL TEST FILES PASSED`。两个 Qt 用例各由 runner 自动重试一次后通过。
- 上述提交均已推送到 `origin/codex/worktree-cleanup-20260725`。根目录未跟踪文档共 68 个，其中 3 个内容分叉副本继续保留，2 个含具体本地连接标识的历史任务文档不推送、不静默改写，其余 63 个进入项目文档提交候选。
- 当前状态：`phase_3_safe_root_docs_pending_reports_samples_private_evidence_review`。后续分别审核顶层报告、9 个样本文件和 `manual_acceptance` 私人证据；不得用宽泛忽略或批量上传绕过外传闸门。

#### 安全文档提交与历史端点脱敏（2026-07-25）

- 63 个已核验根目录项目文档与任务/计划同步形成本地提交 `aa00b55`；3 个内容分叉副本和 2 个含具体本地连接标识的未跟踪历史文档未纳入。
- 首次推送 `aa00b55` 被外传安全闸门拒绝：总计划既有历史段落仍保留 4 处具体本地端点。未绕过拒绝，远程分支继续停在 `cef92cc`。
- 已将总计划的 4 处历史端点机械替换为 `<local-adb-endpoint-redacted>`；验证为具体端点命中 `0`、占位符 `4`、UTF-8 可读、`git diff --check` 通过，脱敏后 SHA-256 为 `763affa2bafa27f223e01326a7ec79077db4b2b9548c679e188ed25043fb3503`。
- 当前状态：`phase_3_endpoint_redaction_verified_safe_docs_push_pending`。下一步只提交并推送脱敏与状态同步，不混入报告、样本或私人操作证据。

#### 未跟踪内容复核与 smoke 分片精确忽略（2026-07-25）

- 脱敏文档提交已改写为 `3b44695` 并成功推送到 `origin/codex/worktree-cleanup-20260725`；当前已跟踪修改和暂存项均为 `0`，本地与远程同步。
- 精确枚举得到 `2166` 个未跟踪且未忽略文件：`reports` 1876、`manual_acceptance` 276、`samples` 9、根目录 5。IDE 显示的一万多个项目不代表一万多个代码改动。
- 5 个明确可再生成的 smoke 分片目录共 `1651` 个文件、`5194317` 字节，已按精确路径加入 `.gitignore`；文件原位保留，未删除、移动或上传。清单见 [Markdown](reports/worktree_cleanup_untracked_inventory_20260725.md) 与 [JSON](reports/worktree_cleanup_untracked_inventory_20260725.json)。
- 本次规则与清单提交后仍保留 `515` 个待决文件：顶层研究/生成报告 189、顶层私人/真实报告 36、`manual_acceptance` 私人证据 276、样本 9、根目录分叉副本 3、含具体本地连接标识的历史文档 2。
- 当前状态更新为 `phase_3_inventory_recorded_generated_smoke_ignored_private_decision_pending`。下一步审阅 189 个研究/生成报告；未经精确新授权不得上传私人证据或 7 个命中真实/实例/Fribbels 标记的样本，不处理 3 个内容分叉副本和 `.git` 异常引用。

#### 安全合成样本收口（2026-07-25）

- 9 个未跟踪样本中，`samples/epic_b_balanced_prospective_20260713.json` 是空的盲采集控制 manifest；`samples/epic_conditional_set_coverage_20260712.json` 是固定 seed `20260712` 生成的 1092 条合成覆盖样本。
- 两个文件均通过 JSON 解析，账号、玩家、实例、Fribbels、MuMu、OCR、PCAP、具体端点和外部解码服务标记命中均为 `0`；SHA-256 已写入未跟踪分类清单，允许独立纳入版本控制。
- 其余 7 个样本均命中真实、实例或 Fribbels 标记，继续 fail closed，不纳入本提交。安全样本提交后待决未跟踪数由 `515` 更新为 `513`。
- 当前状态更新为 `phase_3_safe_synthetic_samples_verified_commit_pending`。下一步独立提交并推送安全样本与状态同步，再继续审阅 189 个顶层研究/生成报告。

#### 顶层报告分类完成（2026-07-25）

- 只读分类覆盖 `reports/` 顶层未跟踪文件 `225` 个、`108157699` 字节，A/B/C/D 四类互斥完整覆盖；分类报告见 [Markdown](reports/worktree_cleanup_reports_classification_20260725.md) 与 [JSON](reports/worktree_cleanup_reports_classification_20260725.json)。
- A 类权威研究摘要/manifest 为 `76` 个、`45922614` 字节，22 个 JSON 全部解析通过，主 agent 敏感模式扫描为 `0`；先提交 54 个 Markdown 摘要，再提交 22 个 JSON manifest。
- B 类可再生成原始输出/日志为 `98` 个、`40156240` 字节；下一步只按分类清单加入精确 `.gitignore`，不删除原文件。
- C 类私人/真实操作或研究数据为 `49` 个、`22063344` 字节；D 类含具体本地路径或真实库存源路径为 `2` 个、`15501` 字节。两类均不提交、不静默改写，等待精确授权或脱敏决定。
- `manual_acceptance` 未跟踪文件为 `276` 个、`197821247` 字节；剩余 7 个真实样本为 `3041714` 字节；根目录 5 个待决文件为 `379353` 字节。
- A 类 54 个 Markdown 摘要已通过 `git diff --cached --check`、具体敏感值扫描并提交推送，提交为 `64639eb`；未包含 A 类 JSON、B 类原始输出或私人证据。
- A 类 22 个 JSON manifest 已通过 JSON 解析、`git diff --cached --check` 和具体敏感值扫描，提交 `bdde672` 已推送；A 类 76 个文件现已全部安全纳入版本控制。
- B 类 98 个可再生成顶层原始输出/日志已逐个加入 `.gitignore`，不使用宽泛目录规则，原文件均保留未删除。规则生效后待决未跟踪数应为 `339`。
- 当前状态更新为 `phase_3_reproducible_reports_ignored_private_decision_pending`。下一步复核实际 `339` 个待决文件，并形成私人外传、脱敏和内容分叉副本的最终用户决定清单。

#### 私人数据与内容分叉副本授权闸门（2026-07-25）

- A 类 Markdown、A 类 JSON 和 B 类精确忽略规则已分别由 `64639eb`、`bdde672`、`5c21038` 提交并推送；当前本地/远程仍为 `0/0`。
- 最终剩余 `339` 个未跟踪文件、`223321159` 字节，最大单文件 `26483765` 字节：报告 51、`manual_acceptance` 276、真实样本 7、根目录 5。
- 只读边界扫描记录：5 个报告含本地 IPv4 端点模式，4 个含绝对路径，1 个含 PCAP/原始载荷字段，37 个含账号/实例字段；具体值不输出、不上传。
- 51 个报告、276 个人工证据、7 个真实样本和 5 个根目录待决文件均保持可见、未忽略、未删除；3 个内容分叉副本和 2 个含具体本地连接标识的历史任务文档继续原位保留。
- 当前状态更新为 `phase_4_private_externalization_authorization_pending`。达到 `git status --porcelain` 为空前，必须由用户明确授权精确外传到 `origin`，或授权可恢复移至工作区外私有归档；D 类文件还需单独授权脱敏副本策略。在授权前不上传、移动、删除、整体忽略或静默改写。

#### 私人归档只读预检（2026-07-25）

- 拟议归档目录为工作区同级的 `%USERPROFILE%\Documents\第七史诗强化装备脚本_私人证据归档_20260725\`；目标当前不存在、位于工作区外，父目录存在。
- 339 个源文件、`223321159` 字节全部存在；不安全相对路径 `0`、重解析点 `0`，集合 SHA-256 为 `1b33e73a3cfdb243bb1cf91bb58d0300ae727c9152a7698463f192fd94f9b76a`。
- C 盘可用空间 `226711515136` 字节，超过两倍源集合空间要求。预检报告见 [Markdown](reports/worktree_cleanup_private_archive_preflight_20260725.md) 与 [JSON](reports/worktree_cleanup_private_archive_preflight_20260725.json)。
- 本轮未创建目录、移动、复制、删除、忽略或上传文件。当前状态更新为 `private_archive_preflight_passed_awaiting_explicit_user_choice`；只有用户明确选择私有归档方案后才允许执行。

#### 私人归档执行授权（2026-07-25）

- 用户已明确授权将预检覆盖的 `339` 个未跟踪文件可恢复移动至 `%USERPROFILE%\Documents\第七史诗强化装备脚本_私人证据归档_20260725\`，并生成逐文件 SHA-256 恢复清单。
- 授权不扩展到删除、覆盖、内容改写、私人数据上传或 `.git` 异常引用处理；移动前仍须重新验证文件集合、路径安全、重解析点、目标冲突和磁盘空间，任一变化立即 `fail_closed`。
- 当前状态更新为 `private_archive_authorized_revalidation_pending`。

#### 私人归档完成与工作区收口（2026-07-25）

- 移动前二次验证通过：未跟踪集合仍为 `339` 个文件、`223321159` 字节，最大文件 `26483765` 字节，集合 SHA-256 仍为 `1b33e73a3cfdb243bb1cf91bb58d0300ae727c9152a7698463f192fd94f9b76a`；不安全路径、重解析点和目标冲突均为 `0`。
- 已将 `339/339` 个文件按原相对路径可恢复移动至 `%USERPROFILE%\Documents\第七史诗强化装备脚本_私人证据归档_20260725\`，目标逐文件大小和 SHA-256 全部通过；源文件剩余 `0`，未上传、删除、覆盖或改写私人内容。
- 归档内已生成 JSON、CSV 和校验文件；移动后 Git 未跟踪文件为 `0`。最终汇总见 [Markdown](reports/worktree_cleanup_closure_20260725.md) 与 [JSON](reports/worktree_cleanup_closure_20260725.json)。
- 收口提交 `d2f60fa` 已推送至 `origin/codex/worktree-cleanup-20260725`；最终 `git status --porcelain` 为空，本地与远程 `ahead/behind=0/0`，工作区清理任务状态更新为 `worktree_cleanup_complete`。

#### 功能开发恢复：策略 V1 冻结清单与覆盖矩阵（2026-07-25）

- 工作区清理闸门已完成，用户已明确要求恢复功能开发。当前最先执行的功能任务为[策略V1冻结清单与覆盖矩阵生成器任务说明](策略V1冻结清单与覆盖矩阵生成器任务说明.md)。
- 本任务只新增确定性的只读 manifest/报告生成能力：记录正式策略版本、行为输入文件哈希、支持范围、默认策略和各节点决策路由；不会修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化。
- 对无法证明满足的冻结条件必须输出 `not_ready` 和明确缺口，不得把生成 manifest 解释为策略已经发布或冻结。
- 已实现只读 manifest 模块与 CLI，输出 `10` 个行为输入哈希、`3` 个来源/品质组合和 `21` 条节点路由；报告见 [JSON](reports/policy_v1_manifest_20260725.json) 与 [Markdown](reports/policy_v1_manifest_20260725.md)。
- 核心 manifest SHA-256 为 `565ecd81d9f1354129bbc91ebbd54f291c33249b83371e0be7a5f3316d6c2adb`；Python 3.9 定向测试 `7/7`、语法检查和 CLI smoke 均通过。
- 当前冻结状态为 `not_ready`，明确缺口为核心 `advise_gear` 未规范拒绝未知套装，以及本清单尚未附着完整回归矩阵证据；本轮不修改策略。
- 当前状态更新为 `policy_v1_manifest_implemented_targeted_validation_passed_full_suite_pending`。
- 扩大策略回归发现 `tests/test_enhance_policy.py` 的既有用例直接依赖已归档私人记录，产生 5 个 `FileNotFoundError`；其余策略断言未失败。为保持工作区清理边界，本轮不读取或恢复私人归档，只把该用例替换为语义等价的最小合成夹具后继续全量验证。
- 自包含修复后该策略文件 `43/43` 通过；统一全量入口真实退出码 `1`，新 manifest 测试通过，但另有 `13` 个既有测试组继续依赖已归档的少数真实样本、Holdout 和 batch `004` 证据。不得跳过测试或静默恢复私人文件。
- 当前状态更新为 `policy_v1_manifest_targeted_passed_full_suite_blocked_awaiting_temporary_private_fixture_authorization`。推荐在用户明确授权后临时复制精确测试依赖、运行全量测试，再删除临时副本并验证归档哈希和 Git 状态；不暂存、不提交、不上传私人文件。
- 用户已明确授权只临时恢复统一全量测试所需的精确私人夹具：恢复前按归档清单逐文件校验 SHA-256 且目标必须不存在，测试后删除临时副本并复核归档原件及集合 SHA-256 不变；这些文件不得暂存、提交或上传。当前状态更新为 `policy_v1_manifest_private_fixture_restore_authorized_preflight_in_progress`。
- 首轮临时恢复 9 个文件后，全量入口真实退出码 `1`、耗时 `270.2` 秒；除 `test_single_item_confirmation.py` 外全部测试组通过。唯一新增依赖是 batch `004` 操作包引用的 `preflight_20260723_102914/pair_batch_004.json`；其缺失造成该组 2 个失败和 2 个错误，不是 manifest 模块回归。
- 首轮 9 个临时副本已全部删除；归档 `339/339` 个文件、总字节数和逐文件 SHA-256 复核通过，集合 SHA-256 仍为 `1b33e73a3cfdb243bb1cf91bb58d0300ae727c9152a7698463f192fd94f9b76a`，Git 未出现私人夹具。当前状态更新为 `policy_v1_manifest_second_full_suite_preflight_pending_with_pairing_baseline`。
- 第二轮补入 batch `004` 配对基线后，单件组底层验证通过，聚合用例仍因已跟踪 batch `013` 操作包缺少其归档配对基线而 `1/5` 失败。归档清单中 `pair_batch_013.json` 唯一存在，因此临时依赖扩展为共 11 个文件；batch `013` 操作包本身无需恢复。当前状态更新为 `policy_v1_manifest_batch_013_pairing_baseline_preflight_pending`。
- 补入唯一匹配的 batch `013` 配对基线后，单件组 `5/5` 通过；统一全量入口真实退出码 `0`、耗时 `270.3` 秒并输出 `ALL TEST FILES PASSED`。一个 Qt 用例两次以退出码 `3221226505` 失败后由既有 runner 第三次重试通过。
- 全量测试后已删除全部 `11` 个临时私人夹具；归档 `339/339` 个文件、`223321159` 字节和逐文件 SHA-256 最终复核通过，集合 SHA-256 仍为 `1b33e73a3cfdb243bb1cf91bb58d0300ae727c9152a7698463f192fd94f9b76a`。暂存区为空，Git 未跟踪项只剩本任务 6 个非私人产物。
- 当前状态更新为 `policy_v1_manifest_implementation_and_validation_complete_commit_push_pending`。全量 runner 通过不等于机器可读回归证据已附着，manifest 继续正确输出 `not_ready`；未知套装拒绝与回归证据附着仍是后续独立缺口。
- 功能、测试、报告和任务文档已由提交 `335dc6e` 推送至 `origin/codex/policy-v1-manifest-20260725`。提交后的 geometric repack 仅受既有异常引用 `refs/heads/main - 副本` 影响，提交对象和远程推送均成功，本轮未修改该引用。
- 当前状态更新为 `policy_v1_manifest_task_complete`；后续最先处理的仍是独立的未知套装 fail-closed 与机器可读回归证据附着缺口，不得在本任务中静默改变正式策略。

#### 策略 V1 剩余冻结闸门收口启动（2026-07-25）

- 用户已明确要求修复 V1 manifest 对 Epic `+0/+3` 内部分组规则的可见性，并继续完成未知套装 fail-closed 与机器可读完整回归矩阵证据两个剩余闸门。
- 新的唯一权威任务说明为[策略V1剩余冻结闸门收口任务说明](策略V1剩余冻结闸门收口任务说明.md)。唯一允许的正式行为变化是统一入口拒绝未知套装；Epic 三组阈值、四项联合条件和其他正式策略数值保持不变。
- 回归证据必须由可重跑工具产生并绑定策略版本、行为输入、测试输入和真实测试结果；证据缺失、失败、覆盖不完整或哈希漂移时继续输出 `not_ready`，不得手工把闸门改为通过。
- 当前状态更新为 `policy_v1_remaining_gates_authorized_implementation_in_progress`；GUI、OCR、模拟器操作和自动强化均不在本任务范围。
- 固定回归矩阵第二轮已通过 `9` 个文件、`123` 项测试和 `13/13` 覆盖要求，manifest 定向测试 `10/10` 通过并达到五闸门 `ready`。
- 首次统一全量入口在外层 `600` 秒达到真实退出码 `124`；终止前发现 `UnitySet` 是 GUI 已公开支持并显示为“夹攻”的既有套装，但正式别名/代码表遗漏，统一未知套装拒绝后导致真实导入少两件。当前只允许补齐 `UnitySet -> set_unity -> 夹攻` 别名，不新增体系分组、权重或门槛。
- 本次全量测试的 `11` 个临时私人夹具已全部删除，归档 `339/339` 个文件、`223321159` 字节及恢复清单哈希复核不变。当前状态更新为 `policy_v1_remaining_gates_unity_alias_compatibility_fix_in_progress`。

#### 策略 V1 五闸门收口完成（2026-07-25）

- 未知套装现在由统一结构校验入口 fail closed；为保持既有公开兼容性，只补齐 `UnitySet -> set_unity -> 夹攻`，未把夹攻套加入任何体系分组、权重或门槛。
- V1 manifest 已显式展开 Epic 非速度 `+0/+3` 的三组规则和四项联合止损条件，不再只显示外层 `lightweight_prediction`。正式门槛仍为 `pure_output=8/13`、`pure_tank=10/17`、默认组 `12/17`，策略动作未修改。
- 机器可读回归证据见 [policy_v1_regression_evidence_20260725.json](reports/policy_v1_regression_evidence_20260725.json)：`9` 个文件、`123` 项测试、`13/13` 覆盖要求通过，证据核心 SHA-256 为 `db625509456c59ab53dd1bd7cd217a43eed248760a7923fd7003986cc680393c`。
- 当前 [V1 manifest JSON](reports/policy_v1_manifest_20260725.json) / [Markdown](reports/policy_v1_manifest_20260725.md) 为 `5/5 ready`、缺口 `0`，核心 SHA-256 为 `050041e19c7626d20deb73e95e2894ce48a5c0e7cd685a4ae471914d7d97423b`。旧任务中的 `not_ready` 已明确标记为历史状态并由本段覆盖。
- manifest 定向测试 `11/11`、固定策略矩阵 `123/123`、Python 3.9 语法检查和 CLI `--require-ready` 通过。统一全量入口真实退出码 `0`、耗时 `278.7` 秒、输出 `ALL TEST FILES PASSED`；两项 Qt 用例首次原生异常退出，均由既有 runner 第二次重试通过。
- 全量测试后 `11/11` 个临时私人夹具已删除，归档 `339/339` 个文件、`223321159` 字节及恢复清单哈希复核不变，工作区私人测试文件剩余 `0`。
- 当前状态更新为 `policy_v1_five_gates_ready_commit_push_pending`。唯一权威任务说明为[策略V1剩余冻结闸门收口任务说明](策略V1剩余冻结闸门收口任务说明.md)；`ready` 只表示策略 V1 契约冻结就绪，不授权模拟器点击、材料选择或资源消耗。
- 功能、测试、机器证据、报告和任务文档已由提交 `b1233c3` 推送至 `origin/codex/policy-v1-manifest-20260725`；提交后的 geometric repack 仍只受既有异常引用影响，提交与推送成功且未修改该引用。
- 当前状态更新为 `policy_v1_five_gates_ready_complete`。策略 V1 冻结契约已达到本阶段完成定义；任何模拟器强化、材料选择或资源消耗仍需独立任务和逐项操作授权。

#### 策略与自动强化执行边界报告审阅（2026-07-25）

- 用户提供《策略与自动强化执行边界报告》并要求主 agent 审阅。独立任务说明为[策略与自动强化执行边界报告审阅任务说明](策略与自动强化执行边界报告审阅任务说明.md)，审阅结果见[策略与自动强化执行边界审阅报告](reports/策略与自动强化执行边界审阅_20260725.md)。本轮只修改文档，不修改正式策略、DP、评分、资源模型、GUI、OCR、Holdout 或自动化，不操作模拟器。
- 报告中的三类装备决策路由、Epic 非速度 `+0/+3` 三组阈值、四项联合止损、未知候选体系/未知套装边界、Heroic 未发布资源 DP 与 M1 救回方向均与当前 V1 基本一致；`rift_85` 的用户侧显示统一建议使用“异界红装”，内部标识保持不变。
- 报告所列自动点击前闸门第 1、2 项已经由当前 manifest 完成：`5/5 ready`、缺口 `0`、核心 SHA-256 `050041e19c7626d20deb73e95e2894ce48a5c0e7cd685a4ae471914d7d97423b`。该 `ready` 只冻结策略契约，不表示自动执行就绪。
- 当前资源模型提供期望资源/体力研究口径，允许分数单位且未建模上级强化石；现有单件汇总器只做操作后资源证据校验。离散材料组合、材料优先级、事前硬上限、多节点余额和自动预算均未实现，不得直接用于自动点击。
- 多节点流程新增阻断：实际强化后旧可信快照与旧建议哈希对当前装备状态失效，必须获取新鲜权威状态并重新唯一配对；当前策略入口只接受 `0/3/6/9/12/15`，非标准落点必须 fail closed；点击还需要持久化幂等状态机，未知结果不得自动重发。
- “一次审核总预算后尝试至 `+15`”会改变现有“一件、一个节点、到点重新授权”的操作契约，只能由新的独立任务和明确用户授权引入。即使未来采用总预算，仍需每段派生预算并逐节点重新核验，不能一次性选满到 `+15` 的材料。
- 一次转换限制已在实现中体现：早期候选要求恰好一条未命中属性，终局转换只替换一条；但冻结矩阵尚缺直接固定“两条无效属性最多修一条”的显式回归断言，后续只需补防回归测试，不改变策略数值。
- 当前状态更新为 `automation_boundary_review_complete_offline_budget_task_pending_authorization`。下一步最先应建立“自动预算与硬上限离线规划器”独立任务，只做整数材料计划、授权约束和 fail-closed 测试；未取得授权前不创建执行器、不接 GUI/OCR/ADB、不进行模拟器试点。

#### 策略与自动强化执行边界报告修订完成（2026-07-25）

- 用户已明确批准按审阅结论修订原报告；独立任务说明为[策略与自动强化执行边界报告修订任务说明](策略与自动强化执行边界报告修订任务说明.md)，修订结果见[策略与自动强化执行边界报告修订稿](reports/策略与自动强化执行边界报告_修订稿_20260725.md)。原审阅报告继续作为审计记录，不被覆盖或删除。
- 用户指定后续所有代码实现和代码测试任务使用 `gpt-5.6-terra + high`；本轮只做 Markdown 修订，没有执行代码。后续启动代码子任务时必须记录实际模型与 reasoning effort，指定模型不可用时停止并报告，不得静默替换。
- 修订稿已将 V1 manifest `5/5 ready`、未知套装统一 fail closed、Epic `+0/+3` 三组阈值/四项联合条件可见性和机器可读回归矩阵移入“已完成前置条件”；`ready` 仍不表示自动执行就绪。
- 当前有效操作契约继续固定为“一件装备、一个复核节点、到点停止并重新授权”。“一次人工审核累计硬预算后以 `+15` 为目标上限”仅作为未来候选契约，未替代当前规则，也不构成模拟器操作或资源消耗授权。
- 尚未实现的执行闸门明确为：离散整数预算与事前硬上限、多节点新鲜权威状态、非标准节点契约、持久化幂等状态机与故障恢复、单件目标上限 `+15` 受控试点。上级强化石在模型验证前必须 fail closed。
- 当前状态更新为 `automation_boundary_revision_complete_offline_budget_task_pending_authorization`。下一步仍是等待用户单独授权“自动预算与硬上限离线规划器”；本轮不修改正式策略、DP、评分、资源模型、GUI、OCR、Holdout 或自动化，不操作模拟器。

#### 自动预算与硬上限离线规划器启动（2026-07-25）

- 用户已明确授权“自动预算与硬上限离线规划器”；唯一权威任务说明为[自动预算与硬上限离线规划器任务说明](自动预算与硬上限离线规划器任务说明.md)。本阶段只实现纯 Python 离线整数规划、硬上限核对、结构化 fail-closed 和可复跑测试，不接 GUI、OCR、ADB、MuMu 或模拟器。
- 模块支持标准节点分段、普通装备/饰品材料池、允许材料集合、库存、材料优先级、预计消耗、建议硬上限和用户审核硬上限。上级强化石在没有已发布离散模型前必须返回 `unsupported_material`，不得猜测或静默忽略。
- 任何成功计划必须证明不超过库存、分段/累计材料硬上限和金币硬上限；非标准节点、跨材料池、硬上限不完整、无可行整数组合或真实规则无法证明时均 fail closed。规划结果不构成实际操作授权。
- 代码实现和代码测试按用户指定使用 `gpt-5.6-terra + high`；主 agent 只负责审阅、验证和文档同步。当前状态更新为 `offline_budget_planner_authorized_pending_terra_high_implementation`。
- 首次 Terra high 实现已产出规划模块、CLI、18 项定向测试和验证报告，但独立 Terra high 只读审阅发现 3 个高优先级缺口：非法分段硬上限/预览类型会被静默当成未提供，仅提供一类硬上限即可误标为完整验证，以及五段计划的库存/分段/累计越限性质证据不足；另有 CLI/Markdown 原始错误详情安全化缺口。当前状态更新为 `offline_budget_planner_implementation_review_failed_corrections_in_progress`，未提交、未推送，不得进入状态机开发。
- Terra high 已完成上述修复：非映射硬上限/预览严格 fail closed，仅完整分段与累计硬上限可进入验证模式；材料常量改为直接引用 `resource_model`；新增五段库存、逐段、累计和 81 组有界矩阵安全测试；CLI/Markdown 错误输出已安全化。规划器 `27/27`、资源模型 `15/15`、CLI `1/1`、五段矩阵越限 `0`，当前状态更新为 `offline_budget_planner_corrections_complete_second_review_pending`。
- 二次 Terra high 只读审阅未发现新的实现类 P1/P2，但指出 81 组矩阵尚未分别改变五个分段的粉末/下级强化石上限，也未独立改变累计下级强化石上限；该 P2 测试证据缺口必须补齐。当前状态更新为 `offline_budget_planner_second_review_test_coverage_gap_correction_in_progress`，仍不提交、不推送、不进入状态机开发。
- Terra high 已补齐测试维度：五段分段材料上限 `40` 组、累计两种材料 `8` 组、两种材料库存 `8` 组，总计 `56` 组，成功 `30`、fail closed `26`、越限 `0`；规划器测试更新为 `29/29`。当前状态更新为 `offline_budget_planner_test_coverage_gap_fixed_final_review_pending`，等待最终独立复核与公开定向测试复跑。
- 最终 Terra high 复核未发现实现类 P1，但发现两项 P2 证据精度问题：计划量为 `0` 的分段边界存在重复输入却按独立标签计数；五段矩阵成功断言未强制完整五段和固定端点。当前状态更新为 `offline_budget_planner_final_review_evidence_precision_correction_in_progress`，修复前不提交、不推送。
- Terra high 已修复两项证据问题：56 个标签候选中 3 个重复 alias 不计入独立证据，最终独立输入为 `53`（分段 `37`、累计 `8`、库存 `8`）；成功 `28`、fail closed `25`、结构或越限违规 `0`。每个成功结果均强制五段固定路线和唯一端点，规划器测试更新为 `30/30`。当前状态更新为 `offline_budget_planner_evidence_precision_fixed_final_gate_review_pending`。
- 聚焦 Terra high 复核发现 `53` 实为覆盖轴案例数，跨维度全局规范化请求为 `40`、重复请求案例 `13`；报告必须区分这些口径。成功结果 oracle 也尚未精确拒绝额外、未知或跨池材料键。当前状态更新为 `offline_budget_planner_final_gate_oracle_and_global_dedup_correction_in_progress`，仍不提交、不推送。
- Terra high 已修复全局统计与 oracle：报告输出 raw `56`、alias `3`、coverage `53`、global unique `40`、duplicate `13`，coverage 成功/失败 `28/25`，unique 成功/失败 `15/25`；oracle 精确校验材料池、允许集合和所有材料键，synthetic 回归覆盖缺失/额外/未知/跨池/错误材料池，违规 `0`。规划器测试 `34/34`，当前状态更新为 `offline_budget_planner_global_dedup_oracle_fixed_final_review_pending`。
- 最终 Terra high 独立只读审阅未发现新的 P1/P2，离线规划器任务完成闸门通过。实现提供单一 `plan_budget()` 入口、稳定 JSON/Markdown、`budget-plan` CLI、完整硬上限验证和结构化 fail closed；报告见 [JSON](reports/offline_budget_planner_validation_20260725.json) 与 [Markdown](reports/offline_budget_planner_validation_20260725.md)。
- 最终验证：规划器 `34/34`、资源模型回归 `15/15`、CLI 回归 `1/1`、Python 3.9 语法、报告生成、成功/失败 CLI smoke 和 `git diff --check` 均通过；未读取或恢复私人归档，未运行依赖私人夹具的全量测试。
- [历史状态，已被后文覆盖] 当前状态曾为 `offline_budget_planner_complete_validated_commit_push_pending`；当时饰品离散材料和上级强化石为 `unsupported_material`。规划器只提供离线计划，不验证实时库存、页面或实际消耗，也不构成材料选择或强化授权。
- 下一步候选为“多节点新鲜快照与重新配对契约”，必须另建独立任务说明并取得用户授权；不得自动开始 GUI、OCR、ADB、MuMu 或执行状态机开发。
- 本任务产物已由提交 `c5b2379` 推送至 `origin/codex/policy-v1-manifest-20260725`；当前状态更新为 `offline_budget_planner_complete_validated_published`。下一步候选仍为“多节点新鲜快照与重新配对契约”，未取得新的独立任务授权前不开始。

#### 饰品与上级强化石离散材料证据复核（2026-07-25）

- 用户要求解释饰品材料和上级强化石为何 fail closed，并只读查看饰品材料获取数据。独立任务说明为[饰品与上级强化石离散材料证据复核任务说明](饰品与上级强化石离散材料证据复核任务说明.md)，报告见[饰品与上级强化石离散材料证据复核](reports/饰品与上级强化石离散材料证据复核_20260725.md)。本轮未操作模拟器、未读取私人归档、未修改代码或正式资源模型。
- 已更正旧结论：仓库已跟踪 batch `013` 强化页截图直接显示饰品粉末 `100`、下级饰品强化石 `1,500`、上级饰品强化石 `4,500`、传说饰品强化石 `13,500` 基础经验；下级石与粉末选择预览及实际操作闭环确认金币分别为 `14,400`、`1,600`。因此饰品粉末/下级石并非缺少公开离散证据，当前规划器的全饰品池 `unsupported_material` 是安全但过度保守的支持缺口。
- 上级饰品强化石仍只确认 `4,500` 基础经验，没有已跟踪的单独选择金币预览，不得按比例猜测金币；其 fail-closed 继续有效。传说石不在当前规划器范围，且同样缺金币闭环。
- 获取数据边界保持不变：`13:5` 是用户确认的日常总量近似，按服务部位 `4:2` 得到 `1.3` 相对机会成本；当前没有原始逐日获取日志。batch `013` 库存 `23,353/514/201/214` 只是时点库存，不是获取率。
- [已被本节后文覆盖] 当前页面实际预览经验为基础经验约 `1.166` 倍（`1,500 -> 1,749`，`1,600 -> 1,865`，`1,700 -> 1,982`）；旧记录曾因来源不明而禁止硬编码，现按用户授权作为当前账号页面基础层，并在执行前重新读取页面预览、异常时 fail closed。
- 当前状态更新为 `accessory_discrete_evidence_review_complete_code_fix_pending_authorization`。下一步优先级改为“饰品粉末/下级石离散规划支持修复”，需另建代码任务并取得用户授权，使用 `gpt-5.6-terra + high`；上级石证据采集和多节点新鲜快照契约在其后，未经授权不得开始。

#### 饰品材料与账号页面倍率规划口径修订（2026-07-25）

- 用户明确澄清：装备与饰品强化石使用相同的离散经验和金币规则，只是使用部位及材料池不同。此前“上级饰品石缺金币证据”的结论已被本节覆盖；权威常量为粉末 `100/1,600`、下级石 `1,500/14,400`、上级石 `4,500/43,200`，两套材料 ID 仍必须严格隔离。
- 用户授权当前账号离线执行预算按页面已验证的整数规则 `floor(基础经验 * 1166 / 1000)` 计算。三组已跟踪预览 `1,500 -> 1,749`、`1,600 -> 1,865`、`1,700 -> 1,982` 均一致；并明确 Good/Great 期望层应建立在该整数页面结果上，随机层期望为 `1.0825`。旧 `1.15395` 不再作为页面基础之上的最终总倍率；派生连续近似为 `1.262195`，逐批仍优先使用整数结果。实际执行前仍须新鲜页面预览复核。
- 独立代码任务为[饰品材料与账号页面倍率离线规划支持修复任务说明](饰品材料与账号页面倍率离线规划支持修复任务说明.md)。范围扩展为两类材料池三种材料、整数页面倍率、随机期望分层、规划输出和回归证据，不接 GUI/OCR/ADB/MuMu/自动点击，不构成实际强化授权。
- 随机层的可复现整数定义补充为：`N=floor(基础经验*1166/1000)`，普通/Good/Great 分别为 `N/floor(1.5N)/2N`，再按 `0.89/0.055/0.055` 加权；所有溢出返还也必须从对应整数结果计算，不能先使用连续总倍率再取整。
- 主审阅补充执行/研究边界：离线执行规划器要求普通页面结果 `N` 已满足节点需求；长期资源模型继续按期望成本语义，在 `100` 基础经验网格上选择分层期望足额的最小投入。两条路径不得共用确定性门槛，也不得用连续 `1.262195` 替代逐批整数期望。
- 代码与代码测试按用户指定使用 `gpt-5.6-terra + high`。当前状态为 `accessory_material_and_account_multiplier_planner_fix_authorized_pending_implementation`；完成前离线规划器现有 `unsupported_material` 行为仍是实际代码状态。
- [已被本条覆盖] 本轮已在授权的既有实现差异上完成该代码任务：普通与饰品材料池均支持粉末、下级石、上级石，常量统一为 `100/1,600`、`1,500/14,400`、`4,500/43,200`，材料 ID、库存和硬上限键仍按池严格隔离。可达性按 `floor(base*1166/1000)` 判定并输出基础/页面有效经验；Good/Great 仅在资源模型中按整数页面结果叠加期望。旧 `1.15395` 路径已移除，连续元数据为 `1.262195`。
- 上级石的选择遵循既有目标顺序：`minimum_gold` 第一，因此同时可用时 `17` 粉末（`27,200`）正确优先于 `1` 上级石（`43,200`）；测试改为验证上级石受支持、`4,500/43,200` 常量、仅允许或硬上限迫使时的选择，以及跨池库存/硬上限 fail closed。资源模型旧回归期望已同步。
- 验证：Python 3.9 资源模型 `16/16`、离线规划器（含 CLI smoke）`39/39`，临时目录报告生成确认普通与饰品各 5 个相邻分段成功，Python 3.9 语法和 `git diff --check` 通过。未读取私人归档，未修改正式策略、DP、GUI/OCR/ADB/MuMu/自动化；按用户指令未提交、未推送。当前状态为 `accessory_material_and_account_multiplier_planner_fix_completed_validated_not_committed`。
- 主审阅发现并已修正资源模型与执行规划器共用确定性门槛的错误。执行规划器仍要求普通页面结果 `N` 足额，Epic `+0 -> +3` 保持 `1,700 -> 1,982`；`material_cost_for_level()` 则在 `100` 基础经验网格用 `178*N + 11*floor(1.5*N) + 11*(2*N) >= 200*标称经验` 选择长期期望最小投入。标称 `1,969` 的长期结果为 `1,600 -> 1,865 -> 2,018.835`，并从三个整数结果计算返还。资源模型 `16/16`、规划器 `39/39` 与 `git diff --check` 均通过；状态仍为 `accessory_material_and_account_multiplier_planner_fix_completed_validated_not_committed`，未提交、未推送。
- 后续主审阅发现三项收口缺口：生成报告的五段硬上限矩阵仍只覆盖 `common`，需要为 `accessory` 生成同等独立矩阵；旧倍率导出常量被删除，需保留映射到新分层口径的兼容别名；规划结果需新增显式页面溢出字段并保留旧键兼容。当前状态回退为 `accessory_material_and_account_multiplier_planner_fix_main_review_gaps_in_progress`，修正和复跑前不得提交。
- 主审阅收口修正完成：`PET_ENHANCE_EXP_MULTIPLIER=1.066` 仅作为历史兼容组件保留，`EXPECTED_ENHANCE_EXP_MULTIPLIER` 映射为当前连续近似 `1.262195`，测试明确拒绝旧 `1.15395`。规划结果 JSON 新增 `page_effective_experience_overflow`，并保留同值的 `experience_overflow` 兼容键。
- 报告生成器保留 common 的既有五段矩阵字段，并新增完整的 `accessory_*` 五段 proposal、exact limit 与 bounded matrix JSON 字段；Markdown 单列饰品五段矩阵。两池均独立覆盖 `+3/+6/+9/+12/+15`、库存、分段硬上限和累计硬上限，饰品矩阵材料键仅为 `accessory_*`。资源模型 `16/16`、规划器 `39/39`、临时报告生成、Python 3.9 语法、矩阵/溢出 smoke 与 `git diff --check` 均通过；未读取私人文件，未提交、未推送。当前状态为 `accessory_material_and_account_multiplier_planner_fix_main_review_gaps_corrected_validated_not_committed`。
- 主工作区最终补充验证通过：共享研究桥接 `7/7`，另一个直接研究测试退出码 `0`。公开验证 JSON/Markdown 已刷新，common/accessory 五段矩阵各 `69` 个覆盖案例、`49` 个全局唯一请求、违规 `0`；CLI smoke 已刷新为 `offline_budget_planner/v2`，`+0 -> +3` 为 `1` 下级石加 `2` 粉末、基础/页面 `1,700/1,982`、金币 `17,600`。当前状态为 `accessory_material_and_account_multiplier_planner_fix_completed_validated_commit_push_pending`。
- 提交 `54a64a6 feat: support accessory enhancement budget planning` 已成功推送至 `origin/codex/policy-v1-manifest-20260725`，当前状态更新为 `accessory_material_and_account_multiplier_planner_fix_completed_validated_published`。提交后的 Git 自动 geometric repack 因已知异常引用 `refs/heads/main - 副本` 报警，但未修改该引用。

#### 多节点新鲜快照与重新配对契约阶段建立（2026-07-25）

- 用户已同意建立新的工程阶段任务说明并提交推送。独立任务说明为[多节点新鲜快照与重新配对契约任务说明](多节点新鲜快照与重新配对契约任务说明.md)，本阶段唯一权威来源为该任务说明、根目录 `AGENTS.md` 和本总计划。
- 离线预算规划器及饰品材料支持已经完成并发布；当前下一阶段聚焦强化后 `+3/+6/+9/+12` 节点的新鲜权威状态、同批双文件严格配对、唯一装备身份连续性和旧证据失效契约。
- 本阶段初始范围仅包含离线 schema/验证器、fail-closed 规则、公开回归测试和不含私人证据的验证报告；不接 GUI、OCR、ADB、MuMu、自动点击、材料选择或资源消耗。
- 新节点必须重新取得独立采集标识和同批 `player_data.json`/`reader_result.json`，并重新校验页面、装备字段、`0.98` 置信度、哈希、实例 ID、节点顺序和建议哈希；零/多候选、旧快照复用、跨批、哈希不一致、低置信度、节点跳跃或未知结果继续 fail closed。
- 可信快照 `20260722_132045` 仅作历史只读配对证据，不能充当强化后任何新节点的当前状态。当前阶段不改变正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。
- [已被后文覆盖] 代码实现和代码测试按用户指定使用 `gpt-5.6-terra + high`。任务说明已建立并推送，但尚未授权开始代码实现；当前状态为 `phase_established_ready_for_implementation_authorization`。
- 用户已明确授权实现，实际代码与测试由 `gpt-5.6-terra + high` 子 agent 执行。新增纯离线节点快照契约验证器，仅最小扩展背包配对器的非 `+0` 当前主属性匹配，保持旧单件只读报告和 `+0` 配对兼容。
- 新契约覆盖 `0 -> 3 -> 6 -> 9 -> 12 -> 15`、同批双 JSON 规范化内容 SHA-256、采集标识/时间顺序、页面明确性、`0.98` 置信度、唯一实例连续性、raw 节点校验和旧快照/旧建议哈希失效。零/多候选、跨批、哈希不一致、页面不明确、低置信度、节点跳跃、实例漂移、未知结果和非 `+0` 起始链均 fail closed，不生成继续建议。
- 公开 `not_ready` JSON/Markdown 报告已生成，不含私人路径、设备地址或原始载荷。Python 3.9 定向验证：新契约 `9/9`、配对器 `6/6`、MuMu 数据适配器 `7/7`，语法检查与 `git diff --check` 通过。旧单件测试因私人归档样本不在工作区而未通过，按任务边界未读取或恢复该归档。
- 主 agent 审阅发现并修复了“非 `+0` 节点可独立启动验证链”的连续性缺口；修正后重跑通过。本轮未修改正式策略、DP、评分、资源模型、GUI/OCR/ADB/MuMu/Holdout/自动化，未读取私人归档。当前状态为 `fresh_snapshot_contract_implemented_validated_commit_push_pending`。
- 实现提交 `0ceabc1 feat: add fresh snapshot pairing contract` 已成功推送至 `origin/codex/policy-v1-manifest-20260725`，当前状态为 `fresh_snapshot_contract_implemented_validated_published`。提交后的 Git 自动 geometric repack 因已知异常引用 `refs/heads/main - 副本` 报警，但未修改该引用。

#### 真实多节点新鲜快照采集与契约验收阶段建立（2026-07-25）

- 用户已明确授权按顺序进入真实多节点采集与契约验收阶段，但首先只建立独立任务说明；本阶段唯一权威来源为[真实多节点新鲜快照采集与契约验收任务说明](真实多节点新鲜快照采集与契约验收任务说明.md)、根目录 `AGENTS.md`、[多节点新鲜快照与重新配对契约任务说明](多节点新鲜快照与重新配对契约任务说明.md)和[MuMu抓包读取与真实装备导入任务说明](MuMu抓包读取与真实装备导入任务说明.md)。
- 当前尚未提供四项执行参数：用户可识别的目标装备及可复核指纹、连续节点计划、允许材料来源、每段及累计材料/金币资源硬上限。用户已明确授权后续只读预检，但该预检必须等待四项参数齐全；目标 `item_instance_id` 必须由正式配对器唯一确定，不得由用户或执行者人工填写。参数齐全前不得连接 MuMu、ADB、OCR，不得抓包、读取真实快照、导入档案、强化、选材或消耗资源。
- 任务说明已固定顺序闸门：先补齐四项参数，再按已授权范围执行只读预检，之后才可按节点逐次取得真实读取授权、运行离线契约，并在每段人工操作前再次取得明确授权。每个节点均要求新鲜同批 `player_data.json`/`reader_result.json`、唯一实例连续性、预期 `raw` 等级、页面明确性和 `0.98` OCR 闸门；任一异常继续 fail closed。
- 后续代码实现和代码测试必须使用 `gpt-5.6-terra + high`；实际模型和 reasoning effort 必须如实记录。当前状态为 `read_only_preflight_authorized_awaiting_execution_parameters`。本轮只更新 Markdown，未操作 GUI/OCR/ADB/MuMu，未读取私人归档，未改动正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。

#### 真实多节点新鲜快照采集参数记录（2026-07-26）

- 用户已提供并写入[真实多节点新鲜快照采集与契约验收任务说明](真实多节点新鲜快照采集与契约验收任务说明.md)的四项执行参数：目标为当前 `+0` 的 85 级红装/Epic 武器、爆伤套、主属性攻击力 `100`、副属性生命 `4%`/爆伤 `4%`/暴击 `5%`/速度 `2`；节点仅 `+0 -> +3`；材料池仅普通装备粉末和下级强化石；每段及累计硬上限均为粉末 `2`、下级强化石 `1`、上级强化石 `0`、金币 `17,600`。
- 目标 `item_instance_id` 尚未且不得人工填写，必须由后续正式配对器唯一确定。上级强化石硬上限为 `0`，未列材料、跨池材料或任何超限输入均 fail closed。
- 用户已授权的范围仍仅为只读预检。当前不得执行 `+0` 真实读取、抓包、OCR 页面操作、导入档案、强化、选材或资源消耗；这些操作均须后续阶段逐项获得明确授权。当前状态更新为 `execution_parameters_recorded_read_only_preflight_authorized`。
- 本轮仅同步两份 Markdown；实际模型记录要求保持 `gpt-5.6-terra + high`。未操作 GUI/OCR/ADB/MuMu，未读取私人归档，未修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。

#### 真实多节点新鲜快照只读预检通过（2026-07-26）

- 在已记录四项执行参数和只读预检授权的范围内，ADB 检查退出码为 `0`，`device_count=1`、`other_state_count=0`；Root 检查通过。未记录、复用或传入固定设备地址。
- 读取器源码实际 `tcpdump` 探针 `su -c 'tcpdump --help >/dev/null 2>&1 && echo READY'` 返回 `tcpdump_probe_exit=0`、`tcpdump_ready=True`，因此 `tcpdump` 前置通过。此前 `command -v tcpdump` 的退出码 `2` 是不兼容探针方式，不代表读取器失败；读取器路径均存在。
- 标准 `current/player_data.json` 与 `gear_fribbels.json` 存在，但时间仅为 `2026-07-22 05:20:45/46 UTC`，只作旧输出，未被作为本任务新鲜节点证据；目标唯一配对未开始。
- 本轮未执行抓包、重启游戏、页面操作、OCR、导入、强化、选材或资源消耗。当前状态更新为 `read_only_preflight_passed_awaiting_fresh_read_authorization`，下一步仅等待用户对 `+0` 新鲜真实读取的明确授权；保留既有四项参数和 `gpt-5.6-terra + high` 模型要求。

#### 真实多节点历史快照文件侧唯一配对（2026-07-26）

- 只读预检保持通过：ADB `device_count=1`、`other_state_count=0`，Root=`True`，读取器源码实际 `tcpdump --help ... && echo READY` 探针返回 `exit=0`、`READY=True`，标准路径存在。
- 在不抓包、不重启游戏、不操作页面/OCR、不导入、不强化、不选材和不消耗资源的范围内，使用 `current/player_data.json` 与 `snapshots/20260722_132045/reader_result.json` 完成历史文件侧配对：`player_items=2670`、`raw_items=2670`、`candidate_count=1`、`candidate_enhance=0`；脱敏候选身份哈希为 `36465dfb10980dc0f50293f10813e60777199cb50a6e4fe88d8774627b309b93`。
- `player_sha256=3d74e2d69147d5a1053f0ebccc217ed2c64842fa2ce91cce6413e5fea364185b`，`reader_sha256=3831f40814c015166794bf44fbd55d153668532d549e612aff431c24e42f900b`。标准 `current/player_data.json` 与 `gear_fribbels.json` 时间为 `2026-07-22T05:20:45Z/2026-07-22T05:20:46Z`；未记录原始实例 ID。
- 上述结果仅为 `2026-07-22` 历史快照的文件侧唯一配对，不是本任务新鲜 `+0` 证据，不能替代同批新鲜双文件、页面/OCR 核验或离线契约验收。当前状态保持 `read_only_preflight_passed_awaiting_fresh_read_authorization`；下一步仅等待用户单独明确授权 `+0` 新鲜真实读取。

#### `+0` 新鲜真实读取授权冲突（2026-07-26）

- 用户已授权 `+0` 新鲜真实读取和向指定 Fribbels API 上传本账号 TCP 载荷，但授权文字同时禁止页面点击。只读核对标准固定读取命令确认，`--auto-enter-seconds 105` 在启动 `tcpdump` 后会 force-stop/monkey 重启 Epic Seven、在 105 秒内循环 ADB `input tap` 登录/公告位置，并在前三轮发送 `KEYCODE_BACK`。
- 因此标准命令与“零页面点击”约束冲突，不能执行；当前状态更新为 `fresh_read_authorized_blocked_by_standard_reader_page_input_conflict`。本轮未启动抓包、未重启、未上传、未点击、未强化、未选材或消耗资源。
- 下一步仅等待用户明确二选一：A（推荐）允许标准读取器仅为登录/公告执行自动点击和重启，仍禁止装备/材料/强化页面输入；或 B 保持零页面点击，另行授权偏离固定标准命令并评估 `--auto-enter-seconds 0`，不保证同步或读取成功。保留既有四项参数和 `gpt-5.6-terra + high` 模型要求。

#### `+0` 新鲜真实读取 A 方案授权（2026-07-26）

- 用户已选择 A 方案，授权标准读取器为登录/公告执行 force-stop/monkey 重启、自动点击和前三轮 `KEYCODE_BACK`；`+0` 新鲜真实读取现可按固定标准命令执行，并已授权向指定 Fribbels API 上传本账号 TCP 载荷。
- 授权严格限于读取器的登录/公告流程。装备、材料、强化页面输入，强化、选材、导入和任何资源消耗仍禁止；出现超出此范围的行为必须停止。
- 当前状态更新为 `fresh_read_authorized_ready_for_standard_reader_execution`。本轮仅同步 Markdown，未运行读取器、ADB 或 GUI，未抓包、重启、上传、点击、强化、选材或消耗资源。执行模型记录为 `gpt-5.6-terra + high`。

#### `+0` 标准新鲜读取未抓到账号同步包（2026-07-26）

- A 方案固定标准命令已运行一次，读取器完整输出结论为“Fribbels 解出 `0` 件装备，账号同步包可能未抓到”。批次 `20260726_012048` 仅产生 `1312188` 字节 PCAP；新的 `player_data.json`、`reader_result.json` 和不可变 snapshot 均未生成，`current` 仍为 2026-07-22 历史文件，不得作为本任务新鲜 `+0` 证据。
- 读取器诊断：`packet_count=2969`；协议为 `udp=812`、`tcp=2144`、`ip_other=5`、`other=8`；`tcp_payload_bytes=1100894`、`udp_payload_bytes=2880`、`tcp_payload_groups=478`；`observed_ports=[443,9997,51390,5138,80,39518,442,8081,53,32946,39542,56248]`。
- 当前状态更新为 `fresh_read_fail_closed_no_account_sync_20260726_012048`。停止重试、目标配对、导入和任何游戏内操作，直至用户另行授权且环境发生可解释变化；本轮没有强化、选材或资源消耗。执行模型记录为 `gpt-5.6-terra + high`。

#### MuMu 新鲜读取 0 件装备离线诊断与修复阶段建立（2026-07-26）

- 用户已要求定位“为什么读取不了”并修复读取功能；独立任务说明为 [MuMu新鲜读取0件装备诊断与修复任务说明](MuMu新鲜读取0件装备诊断与修复任务说明.md)，本阶段唯一权威来源为该说明、根目录 `AGENTS.md`、[MuMu抓包读取与真实装备导入任务说明](MuMu抓包读取与真实装备导入任务说明.md) 和本总计划。
- 权威失败事实维持不变：批次 `20260726_012048` 的 `packet_count=2969`、`tcp_payload_bytes=1100894`、`tcp_payload_groups=478`，但 Fribbels 解出 `0` 件；仅落盘 PCAP，未生成新的 `player_data.json`、`reader_result.json` 或 snapshot。此前“未抓到账号同步包”仅为读取器当前诊断，尚不是已验证根因。
- 执行顺序固定为 diagnosing-bugs 离线反馈回路：先以失败/成功 PCAP 的非敏感结构建立实际运行过的确定性红灯回放，再复现最小化、提出至少三项可证伪假设并逐项探测；只有根因定位且红灯回归测试存在后，才允许对 `D:\VScode\Epic-7-tools.v1.2` 作最小修复并用原失败 PCAP 离线回放验证。
- 本阶段明确不重传载荷、不运行 MuMu/ADB/OCR/GUI、不点击、不强化、不选材、不导入；不修改正式策略、DP、评分、资源模型、Holdout 或自动化规则。不得把旧快照作为新鲜读取成功证据；离线修复完成也不得自动触发真实读取。
- 实际模型记录为 `gpt-5.6-terra + high`。当前状态为 `offline_diagnosis_task_established_awaiting_execution`：尚未建立或运行红灯测试、尚未验证任何根因假设、尚未修改外部读取器；本轮仅创建/同步 Markdown，未提交。

#### MuMu 新鲜读取 0 件装备离线根因确认与授权阻断（2026-07-26）

- 离线红灯探针 `tools/mumu_capture_sync_probe.py` 已实际运行。失败 PCAP 在 `60--120` 秒仅有 `759` 字节 TCP 载荷，退出码 `1`，连续 `3` 次一致；成功批次 `20260722_132045` 同窗为 `405198` 字节，退出码 `0`。探针结果是公开的非敏感结构统计，不记录或重传原始载荷。
- ACK 跨流碰撞在成功/失败样本均为 `0`；失败去重损失 `11014`，小于成功 `44761`；协议、分帧与可打印比例相近。因此 TCP 分组、ACK 碰撞和协议/分帧变化均不是本次 `0 items` 的已证根因，旧的“账号同步包可能未抓到”诊断现已收敛为可证结论。
- 根因结论：读取器固定登录/公告驱动无法保证触发账号同步。要恢复读取，须状态感知地进入大厅后只读导航装备/英雄页面，或使用等价同步触发；当前 A 方案禁止装备页面输入，此行为超出既有授权，不能实施。
- 当前状态更新为 `root_cause_verified_blocked_by_sync_trigger_page_authorization`。禁止盲改外部读取器、盲目重跑真实读取或重传 API 载荷；等待用户明确授权同步触发页面输入后，才可另行建立最小实现任务。实际模型为 `gpt-5.6-terra + high`；本轮仅同步 Markdown，未修改代码、未运行外部工具、未提交。

#### MuMu 同步触发页面导航授权扩大（2026-07-26）

- 用户已明确同意扩大 A 方案：允许最小状态感知地确认进入大厅后，只读导航装备或英雄页面以触发账号同步；修复完成后，允许一次标准真实读取及向既有指定 Fribbels API 上传本账号 TCP 载荷验证。
- 授权仍严格排除强化页输入、材料选择、装备变更、导入、任何资源消耗、OCR，以及正式策略、DP、评分、资源模型、Holdout 和自动化规则改动。实现必须保持最小范围，不能借此盲改读取器或扩大真实操作。
- 仓库离线红灯探针 `tools/mumu_capture_sync_probe.py` 的权威基线为：失败 `759` 字节、退出码 `1`；成功批次 `20260722_132045` 为 `405198` 字节、退出码 `0`。修复必须先使用该探针验证，再执行获准的一次真实读取。
- 当前状态更新为 `sync_trigger_page_authorized_ready_for_implementation`。实际模型记录为 `gpt-5.6-terra + high`；本轮仅同步 Markdown，未修改代码、未提交。

#### MuMu 同步触发最小实现与离线验证完成（2026-07-26）

- 外部读取器 `D:\VScode\Epic-7-tools.v1.2\readers\mumu_capture\reader.py` 已最小实现：`capture_pcap` 的登录驱动后调用 `trigger_read_only_sync`。该函数以 `dumpsys window windows` 确认 Epic Seven 为前台；仅此前置通过时，单次点击 `HERO_LOBBY_TAP=(0.10,0.89)` 触发同步，非前台 fail-closed。未使用 OCR，未增加强化、材料或装备变更操作。
- 新增 `tests/test_mumu_sync_trigger.py`。修复前红灯为 `AttributeError`、退出码 `1`；修复后 `unittest 3/3`、退出码 `0`，Python 3.9 内存语法检查退出码 `0`。`tools/mumu_capture_sync_probe.py` 的失败 `759` 字节/退出码 `1`、成功 `405198` 字节/退出码 `0` 基线保持不变，供下一次读取结果判定。
- 尚未执行真实 MuMu、ADB、标准读取或 Fribbels API 调用，未产生新 `player_data.json`、`reader_result.json` 或 snapshot，因而不得宣称读取功能已在真实环境恢复。仍禁止强化页输入、材料选择、装备变更、导入、资源消耗、OCR 和正式模型相关改动。
- 当前状态更新为 `implementation_verified_awaiting_real_read`。下一步为主 agent 精确审阅后，按已获授权执行一次标准真实读取；实际模型为 `gpt-5.6-terra + high`。本轮仅同步 Markdown，未修改代码、未提交。

#### MuMu 同步触发候选真实验证失败并撤销（2026-07-26）

- 标准读取命令按授权仅执行一次，批次 `20260726_015751` 仍为 Fribbels `0` 件：`packet_count=2814`、`udp/tcp/ip_other/other=802/2003/5/4`、`tcp_payload_bytes=1083301`、`udp_payload_bytes=2856`、`tcp_payload_groups=448`、`observed_ports=[443,9997,51390,5138,80,40098,442,33538,8081,53,40118,52600]`。仅生成 `1283593` 字节 PCAP，无新 `player_data.json`、`reader_result.json` 或 snapshot，`current` 仍是 `2026-07-22` 历史文件。
- 本次 `tools/mumu_capture_sync_probe.py` 为 `814` 字节、退出码 `1`。可证结论为：前台包确认不能区分 Unity 内部页面，单次英雄入口点击候选未触发账号同步，读取功能尚未恢复。
- 候选已精确撤销：外部 `reader.py` 恢复原长度 `8874`，候选测试已删除，Python 3.9 内存语法检查退出码 `0`。禁止继续盲点点击、自动重跑或重传 API 载荷；不得将本次失败 PCAP 或旧 `current` 当作新鲜成功输出。
- 需要用户新的明确授权并另建任务后，才能研究只读截图/模板状态识别，或用户手动确认页面后再触发读取。当前状态为 `real_validation_failed_candidate_reverted_awaiting_visual_state_authorization`，实际模型 `gpt-5.6-terra + high`；本轮仅同步 Markdown，未修改代码、未提交。
#### MuMu 状态感知只读导航修复任务（2026-07-26）

- 用户已批准建立并执行 [MuMu状态感知只读导航修复任务说明](MuMu状态感知只读导航修复任务说明.md)：允许只读截图/模板状态识别，确认大厅后导航装备或英雄页面，并在离线修复验证后执行一次标准真实读取及既有 Fribbels API 验证。
- 本阶段只允许读取器状态识别、只读导航接线、公开回归测试和文档同步；强化、选材、装备变更、导入、资源消耗、OCR 字段采集、正式策略/DP/评分/资源模型/GUI/Holdout/自动化规则均禁止。
- 执行模型固定记录为 `gpt-5.6-terra + high`。当前状态：`task_established_awaiting_reader_audit`。下一步先审计读取器入口、状态/ADB 封装和公开测试，建立修复前红灯后再修改。
- 读取器审计后确认旧固定坐标未处理 MuMu `720x1280` 物理尺寸与 `1280x720` 逻辑截图的旋转映射；已在外部读取器加入标准库 PNG 状态模板匹配、旋转坐标转换、导航前后 fail-closed 和 5 项模拟 ADB 回归测试。Python 3.9 语法检查及既有数据写入定向测试均通过。
- 只读设备预检发现 1 个在线 `device`，截图成功；现有参考截图仍包含登录奖励/活动遮罩或错误页面，未形成干净目标装备/英雄模板，因此本轮未启动标准抓包、Fribbels API 或产生新鲜双文件。当前状态：`offline_fix_verified_awaiting_target_template_and_real_validation`。下一步等待用户提供/确认干净目标页模板后再执行一次真实读取。

#### MuMu 截图入口链路与只读导航模板任务（2026-07-26）
- 用户已提供大厅、背包、装备保管箱、英雄页面及入口参考截图，并明确批准在只读范围内按“大厅 -> 右上角四方格控制界面 -> 背包或英雄页面”导航采集；玩家名参考为 `O橙U子O`，不进入 OCR 或身份输入。
- 独立任务说明为 [MuMu截图入口链路与只读导航模板任务说明](MuMu截图入口链路与只读导航模板任务说明.md)，本轮唯一新增执行口径。模板使用用户临时截图路径，模板不提交为账号数据。
- 现有读取器仅支持一次固定导航点击，无法覆盖四方格和目标入口链路；下一步先实现状态确认、分步只读点击、目标模板确认及 fail-closed 回归，再按既有 MuMu 读取说明执行一次真实验证。
- 授权仍排除强化、选材、装备变更、导入、资源消耗、OCR、正式策略/DP/评分/资源模型/GUI/Holdout/自动化规则改动；真实读取失败不得重试或复用历史输出。执行模型记录为 `gpt-5.6-terra + high`。
- 当前状态更新为 `visual_entry_chain_authorized_ready_for_implementation`。
- 只读 ADB 截图核对（未发送输入）显示当前页面为用户图八个人资料弹窗，模板相似度约 `0.9863`，大厅图四约 `0.6196`；当前不满足大厅前置条件，禁止盲点或启动真实抓包。状态更新为 `visual_entry_chain_authorized_implementation_in_progress`。
- 外部读取器链路实现已完成并经主 agent 复核：大厅 -> 四方格 -> 控制界面 -> 目标页；新增 `--menu-template`、`--menu-tap`、`--entry-tap`，模板/坐标预检在 ADB 与抓包前执行，缺失或不匹配 fail-closed。导航回归 `8/8`、既有数据写入 `6/6`、Python 3.9 语法检查通过；未调用真实 MuMu/ADB/Fribbels。
- 用户未提供控制界面整屏模板，入口小图不作为整屏模板。下一步仅允许在确认大厅后单次点击四方格并采集临时菜单截图；菜单状态未确认则停止，不抓包。状态更新为 `visual_entry_chain_authorized_awaiting_readonly_menu_template`。
- 单次获准大厅探针确认大厅相似度 `0.9705`，但四方格点击后仍为大厅。只读证据显示 `wm size=720x1280`，活动 `dumpsys input` Viewport 的逻辑/物理帧均为 `1280x720`；现有 rotation=1 映射把目标点送到屏外约 `(685,1239)`。根因收敛为输入帧映射错误，状态更新为 `visual_entry_chain_authorized_input_frame_mapping_fix_required`；修复与回归通过前禁止继续点击或抓包。
- 修复后当前大厅截图受频道提示、资源数字和角色动画影响，相似度为 `0.874`，而资料弹窗为 `0.6196`；`0.92` 整屏阈值会误拒真实大厅。下一步将阈值收敛为 `0.85` 并记录回归，继续要求模板存在、PNG 有效和目标页二次确认。
- 输入帧解析与动态阈值修复已完成：活动 Viewport 归一化映射、重复等价 Viewport 去重、缺失帧 fail-closed；导航回归 `10/10`、Python 3.9 语法通过。大厅确认后只读尝试 `(1239,35)`、`(1248,24)`、`(35,1245)` 三种有依据坐标，均未打开控制界面，最后一次大厅相似度 `0.8686`。已停止，不抓包、不上传、不进入目标页；状态更新为 `visual_entry_chain_authorized_menu_navigation_blocked_fail_closed`。

#### 控制界面整屏模板已补齐（2026-07-26）

- 用户提供并确认大厅闭合截图与四方格展开后的控制界面整屏截图；控制面板截图对应最初序列的“图四”，而最初“图二”仍仅是装备保管箱参考；此前“缺少菜单整屏模板”的阻断已被后文覆盖。
- 菜单截图中左下方公文包图标确认是背包入口，按参考分辨率暂用归一化坐标 `(0.735, 0.820)`；新鲜目标背包模板仍使用用户图一，英雄入口不在本次读取路径内。
- 当前状态更新为 `visual_entry_chain_menu_template_received_ready_for_readonly_validation`。下一步只做模板/坐标预检、导航回归和精确审阅，之后才可按《MuMu抓包读取与真实装备导入任务说明.md》执行一次标准真实读取；不强化、不选材、不改装备、不导入、不采集 OCR 字段。

#### 只读导航后真实读取被 Fribbels 413 拒绝（2026-07-26）

- 导航回归 `10/10`、三份 PNG 模板预检通过；ADB 自动发现 `emulator-5554`，读取前大厅相似度 `0.9754`。按授权执行唯一一次 240 秒读取，批次 `20260726_115345` 的 PCAP 为 `18,823,101` 字节，`tcp_payload_bytes=18,283,414`、`tcp_payload_groups=534`。
- Fribbels API 返回 HTTP `413 Request Entity Too Large`，因此没有同批 `fribbels_raw_response.json`、`reader_result.json`、snapshot 或新 `player_data.json`；`current` 仍是 2026-07-22 历史输出，不能充当新鲜节点证据。
- 本轮未强化、选材、改装备、导入、OCR 或资源消耗；已停止重试。后续若继续，必须先另建“Fribbels 请求体上限与载荷分批”任务说明并完成离线验证，不能直接重传本批 TCP 载荷。当前状态：`real_validation_failed_fribbels_413_oversized_payload_20260726_115345`。

#### Fribbels 载荷上限与分批解码任务建立（2026-07-26）

- 代码审计确认当前 `fribbels_decode.py` 不含本地 Fribbels 解密算法，只能提取 TCP groups 并调用既有 API；本地无法替代服务端解码。
- 用户已明确要求：若不能本地解码，则对同一批 TCP groups 进行大小受限分批。已建立独立任务说明 [MuMu Fribbels载荷上限与分批解码任务说明](MuMu%20Fribbels载荷上限与分批解码任务说明.md)，本轮先做离线预算审计，不直接重传。
- 当前状态更新为 `task_established_awaiting_offline_payload_budget_audit`；执行模型记录为 `gpt-5.6-terra + high`。

#### Fribbels 分批实现与离线预算验证完成（2026-07-26）

- 已确认没有本地 Fribbels 解密算法，仍必须调用既有接口；新增确定性请求体预算、分批发送、批次去重合并和任一批失败 fail-closed。
- 对失败批次 `20260726_115345` 离线审计：排除 `1` 条超大 TLS 流（`16,820,767` 字节，明确记录在诊断中），剩余 `525` 个 group，请求体 `2,901,695` 字节，预算 `7,000,000` 字节，预计 `1` 批。
- 新增分批公开测试后，读取器全量测试 `20/20`、Python 3.9 语法检查通过；尚未进行新的 API 请求。当前状态：`implementation_verified_awaiting_batched_api_validation`。

#### 分批 API 验证仍为 0 件装备（2026-07-26）

- 使用同一批 `20260726_115345` 新鲜 PCAP 执行一次受限分批请求；接口接受请求，未再返回 `413`，但 Fribbels 解出 `0` 件装备。
- 未生成新的 `player_data.json`、`reader_result.json` 或 snapshot；`current` 仍为 2026-07-22 历史输出，不得作为新鲜节点证据。
- 分批只解决请求体上限，未证明这批流量包含可解码账号同步包；已停止重传、TLS 流调整、重新抓包、配对和导入。当前状态：`batched_api_validation_failed_zero_items_20260726_115345`。

#### MuMu 账号同步触发与分批读取修复任务建立（2026-07-26）

- 用户已明确“完全授权，并处理问题”，允许在只读范围内继续诊断、修改读取器、进行截图/ADB 导航、抓包和调用既有 Fribbels API；强化、选材、装备变更、导入、资源消耗、OCR 和正式模型改动仍禁止。
- 已建立独立任务说明 [MuMu账号同步触发与分批读取修复任务说明](MuMu账号同步触发与分批读取修复任务说明.md)，本轮先做失败/成功 PCAP 离线差分和红灯回放，再修复并执行一次新鲜读取。
- 已列出五项可证伪假设，当前状态更新为 `task_established_awaiting_offline_sync_differential`；执行模型记录为 `gpt-5.6-terra + high`。

#### 局部页面锚点识别授权（2026-07-26）

- 用户指出整屏阈值可能受动态背景、角色动画和资源数字影响，并明确批准改为局部稳定锚点识别；验收目标从“整屏高相似度”收敛为“可靠确认已进入背包或英雄页面”。
- 允许只读截图/局部模板识别和页面导航，仍禁止强化、选材、装备变更、导入、OCR 字段采集和资源消耗。
- 当前状态更新为 `local_region_recognition_authorized_awaiting_template_region_audit`；下一步先审计模板区域并建立局部匹配红灯/绿灯回归。

#### 局部页面锚点识别实现完成（2026-07-26）

- 读取器新增局部区域采样和轮询确认：控制面板使用左下入口锚点，背包/英雄目标页使用顶部返回箭头与帮助图标双锚点；整屏目标模板不再作为唯一判据，局部不匹配仍 fail-closed。
- 新增超时红灯回归；读取器全量测试 `21/21`，Python 3.9 语法检查通过。
- 正式读取硬条件保持：先 `force-stop + monkey` 重启 Epic Seven，再执行大厅 -> 控制面板 -> 背包/英雄只读导航；当前状态更新为 `local_region_recognition_implemented_awaiting_fresh_restart_read`。

#### 网络错误弹窗误判已收紧（2026-07-26）

- 上一次正式读取批次 `20260726_124528` 实际停留在“网络错误”弹窗，宽局部区域误判为目标页；导航后窗口仅 `30,374` 字节，Fribbels `0 items`。
- 已将局部锚点收紧为菜单入口图标区域阈值 `0.82`，目标页顶部返回箭头与帮助图标两个窄区域阈值 `0.80`；该错误弹窗的离线得分 `[0.795, 0.769]`，现在会 fail-closed。
- 当前状态更新为 `local_region_recognition_hardened_awaiting_fresh_restart_read`；下一步必须重启游戏后再做一次真实验证，不能复用本批结果。

#### 目标页图标亮度约束完成（2026-07-26）

- 颜色相似度仍可能被暗色网络错误弹窗抬高，已增加返回箭头/帮助图标的亮像素比例约束；错误弹窗 `[0.052, 0.000]` 低于目标模板期望 `[0.161, 0.201]`，并保留颜色阈值 `0.72`。
- 读取器全量测试 `22/22`、Python 3.9 语法检查通过；正式读取仍必须重启游戏。当前状态更新为 `target_icon_brightness_guard_implemented_awaiting_fresh_restart_read`。

#### 白屏/网络 UI 被正确拒绝，读取 fail-closed（2026-07-26）

- 最新一次必须重启的真实读取在目标页确认阶段得到相似度 `[0.209, 0.266]`、亮度比例 `[1.000, 1.000]`；临时截图确认是白屏加右上角关闭按钮，不是背包或英雄页。
- 读取器在抓包结果处理前停止，没有新 PCAP、Fribbels 请求或新鲜快照；此前批次 `20260726_124528` 的网络错误弹窗已被记录为 `0 items`。
- 局部识别修复已达到“不误把网络错误/白屏当目标页”的安全目标，但 MuMu/游戏网络页面仍未稳定加载；当前状态更新为 `target_guard_rejected_blank_network_ui_fail_closed_20260726`。不得继续盲目点击或重复读取，需环境发生可解释变化后再授权下一次真实验证。

#### 用户人工重启后环境恢复（2026-07-26）

- 用户已人工重启 Epic Seven 并确认本次无网络错误/白屏异常，满足继续读取的环境变化条件。
- 当前状态更新为 `environment_recovered_awaiting_readonly_preflight_20260726`；下一步先检查 ADB、当前大厅模板和局部锚点，再执行一次必须重启游戏的标准真实读取。

#### 公告页关闭与大厅早停修复完成（2026-07-26）

- 最新失败画面确认是公告展示页；旧 `drive_game_entry()` 未覆盖右上角关闭按钮 `(0.965, 0.065)`，且未在大厅确认后早停，导致后续导航可能误触。
- 已加入公告关闭点和大厅模板状态感知早停；新增回归后读取器全量测试 `23/23`、Python 3.9 语法检查通过。
- 当前状态更新为 `announcement_close_stateful_fix_verified_awaiting_fresh_restart_read_20260726`；下一步执行一次必须重启的真实读取。

#### 背包入口触发网络错误，转验英雄入口（2026-07-26）

- 公告页处理修复后，读取已通过大厅和控制面板；背包入口点击后出现网络错误弹窗，目标锚点得分 `[0.788, 0.780]`、亮度 `[0.054, 0.000]`，被正确 fail-closed，没有新快照。
- 按既有“背包或英雄页”授权，下一步切换控制面板英雄入口和用户英雄模板，作为单一替代同步触发假设；当前状态更新为 `backpack_entry_network_error_awaiting_hero_entry_validation_20260726`。

#### 英雄页局部模板区域修复完成（2026-07-26）

- 英雄入口临时截图确认已真实进入英雄页；失败原因是英雄页帮助图标比背包模板更靠右，已增加英雄专用顶部区域集合，任一背包/英雄集合通过即可。
- 读取器全量测试保持 `23/23`、Python 3.9 语法检查通过；当前状态更新为 `hero_region_match_fixed_awaiting_fresh_restart_read_20260726`。

#### 英雄入口真实导航通过但无账号同步（2026-07-26）

- 英雄入口批次 `20260726_133902` 已通过大厅、控制面板和英雄页局部识别，完成 240 秒抓包；PCAP `1,301,692` 字节，`tcp_payload_bytes=1,092,035`、`tcp_payload_groups=465`，分批请求 `1` 批。
- Fribbels 返回 `0 items`；60--120 秒导航后窗口仅 `1,242` 字节，没有新 `player_data.json`、`reader_result.json` 或 snapshot，旧 `current` 未被使用。
- 由此确认导航/输入/局部模板链路已通过，剩余问题不是整屏阈值或 413，而是游戏未产生可解码账号同步包；当前状态：`navigation_and_trigger_verified_no_account_sync_20260726_133902`。停止继续点击和重复读取，等待游戏同步环境或协议发生可解释变化。

#### 重启后复验仍为无界面背景（2026-07-26）

- 用户人工重启 Epic Seven 后确认无异常。按现行读取器执行一次英雄入口只读复验；固定命令先因安全校验要求显式模板与入口坐标而在抓包前退出，随后补齐用户提供的 PNG 模板执行一次 240 秒读取。
- ADB 自动发现 `emulator-5554`，读取器完成启动和重启，但目标页锚点在 12 秒窗口内 fail-closed：相似度 `[0.619, 0.564]`、亮度比例 `[0.196, 0.870]`。失败后只读截图及 8 秒后复查均显示只有场景背景、界面控件未加载。
- 本次没有新鲜 `player_data.json`、`reader_result.json`、snapshot 或有效 Fribbels 解码结果；未继续点击、未强化、未选材、未导入。当前状态更新为 `restart_recheck_ui_not_loaded_fail_closed_20260726`，等待游戏 UI/账号同步环境发生可解释变化后再建立独立复验任务。

#### 自动强化请求的边界确认（2026-07-26）

- 用户询问是否跳过当前读取任务直接进入自动强化；结论为不允许跳过新鲜快照闸门，也不得把历史 `current` 作为自动强化输入。
- 如继续推进，必须另建独立自动强化任务说明，先完成新鲜同批双 JSON、页面、实例 ID、`raw` 和资源上限校验，再逐节点取得明确强化授权；当前没有启动强化、选材、装备变更或资源消耗。
- 读取职责明确分为两层：开发期读取用于验证读取器/契约；自动强化运行期仍必须自动读取。初始 `+0` 读取确认目标装备，每次实际强化后都要新鲜读取并重新配对，只有节点、实例 ID、`raw`、页面状态和资源账本连续时才允许下一节点；旧快照不能作为继续依据。
- 用户提出以识别采样替代每次读取以降低延迟；结论为采样只能做轻量预检，不能证明服务端结果、材料扣除或实例连续性。可评估的后续方向是“稳定 UI 采样 + 缩短窗口新鲜读取”的混合方案，必须另建任务验证后才能改变运行时契约。

#### 纯视觉采样强化原型任务建立（2026-07-26）

- 用户要求在开发环境暂不支持游戏数据读取时，先实现纯视觉采样强化方案。
- 已建立独立任务说明：[纯视觉采样强化执行器任务说明.md](纯视觉采样强化执行器任务说明.md)。本轮仅实现 dry-run 原型、状态机和测试；不连接 MuMu/ADB/Fribbels/Airtest，不发送点击，不强化、不选材、不消耗资源。
- 目标固定为用户提供的红装 85 武器、爆伤套、攻击 100、生命 4%/爆伤 4%/暴击 5%/速度 2，`+0 -> +3`，资源上限粉末 2、下级强化石 1、上级强化石 0、金币 17600。真实点击执行需另建任务并重新取得授权。
- 已新增 `src/e7_enhance/visual_sampling.py` 与 `tests/test_visual_sampling.py`；目标匹配、页面/候选/节点/资源 fail-closed 和点击器零调用覆盖均通过，定向测试 `6/6`、Python 3.9 语法检查和 `git diff --check` 通过。
- 原型仅接受结构化视觉采样证据并输出 dry-run `+0 -> +3` 建议，不调用点击器；真实截图/OCR/ADB 接线仍未完成，当前状态为 `visual_sampling_prototype_dry_run_verified_runtime_integration_pending`。

#### 纯视觉采样方案设计拆分（2026-07-26）

- 用户要求当前会话继续保留 MuMu/Fribbels 底层读取方案，将纯视觉方案设计推送到新会话。
- 已创建独立任务说明：[纯视觉采样方案设计任务说明.md](纯视觉采样方案设计任务说明.md)。新会话仅做离线设计、接口审阅和验证矩阵，不连接 ADB/MuMu、不上传数据、不点击、不强化。
- 当前会话主线保持为底层读取与 Fribbels `413 Request Entity Too Large` 载荷上限问题；纯视觉设计结论不得覆盖新鲜快照契约或正式自动强化边界。
- 新会话已完成离线设计报告：[纯视觉采样方案设计_20260726.md](reports/纯视觉采样方案设计_20260726.md)。设计确认纯视觉只能做页面/目标预检和稳定等待，不能替代每个实际强化节点的新鲜同批双文件、raw 强化等级、实例连续性或资源对账。
- 已审阅现有 `visual_sampling.py`、`fresh_snapshot_contract.py`、`ocr_backpack_pair.py`、`mumu_player_data.py`、`single_item_confirmation.py` 及公开测试；保留现有 dry-run 与新鲜契约，不修改代码。
- 离线定向回归基线为视觉 `6/6`、新鲜契约 `9/9`、MuMu 数据适配 `7/7`、背包配对 `6/6`；未读取私人归档、未接 ADB/MuMu、未采集真实截图、未上传数据。
- 后续实现必须另建任务说明，依次拆为视觉证据 schema/稳定帧、混合节点编排器、外部读取器适配和单节点受控操作；当前不自动下发或开始这些任务。
- 状态：`design_complete_offline_only_runtime_integration_pending_20260726`；本会话仍保留底层读取主线。

#### 用户要求改为纯视觉闭环自动强化候选模式（2026-07-26）

- 用户明确拒绝“每节点底层读取”的运行时方案，要求纯视觉进行自动强化，以降低读取延迟。
- 前一节关于“视觉只能预检、每次实际强化后必须新鲜读取”的结论已被本节覆盖，仅保留为底层可验证模式的历史对照；本节不修改 MuMu/Fribbels 契约，也不把纯视觉结果伪装为服务端确认。
- 已将 [纯视觉采样方案设计任务说明.md](纯视觉采样方案设计任务说明.md) 和 [reports/纯视觉采样方案设计_20260726.md](reports/纯视觉采样方案设计_20260726.md) 修订为纯视觉闭环候选口径：`bootstrap -> pre_action -> single_action -> post_action -> next_node`，每节点要求连续稳定帧、目标指纹、节点变化和资源数字一致。
- 纯视觉输出必须携带 `mode=visual_only`、`verification=unverified`；视觉上的强化等级、材料/金币变化不能证明服务端状态、实例 ID 连续性或真实扣款。任何不稳定、节点跳跃、资源矛盾、页面漂移、超时或点击后未知结果均停止且禁止自动重试。
- 当前仍只完成离线设计和原型审阅，未实现视觉采样器、点击器接线或真实多节点运行；实现前必须另建独立任务说明并取得明确实现授权。当前状态：`pure_visual_closed_loop_design_revised_offline_implementation_pending_20260726`。

#### 纯视觉运行时接线实现授权（2026-07-26）

- 用户已明确批准开始纯视觉运行时接线；已建立唯一实现任务说明：[纯视觉运行时接线实现任务说明.md](纯视觉运行时接线实现任务说明.md)。
- 本轮实现范围限定为离线 `VisualEvidence`、稳定帧/局部锚点校验、纯视觉单节点状态机和可注入 fake sampler/fake clicker；不连接 MuMu/ADB，不读取或上传游戏数据，不执行真实页面点击、强化、选材或资源消耗。
- 实现输出必须携带 `mode=visual_only`、`verification=unverified`，视觉结果不得转换为新鲜快照或服务端确认；点击后未知结果、节点跳跃、资源矛盾和不稳定证据必须停止且禁止自动重试。
- 执行模型记录为 `gpt-5.6-terra + high`。当前状态：`implementation_authorized_pending_terra_high`；代码修改和测试尚未开始。

#### 纯视觉运行时接线离线实现完成（2026-07-26）

- 已完成 [纯视觉运行时接线实现任务说明.md](纯视觉运行时接线实现任务说明.md) 授权范围内的代码接线：新增 `src/e7_enhance/visual_runtime.py`，提供 `VisualEvidence`、稳定帧/双锚点/字段置信度/唯一目标/资源预览闸门，以及 `pre_action -> single_action -> post_action -> next_node` 单节点编排。
- `VisualOnlyEnhanceExecutor` 只调用注入式 sampler/clicker；每节点至多一次点击，点击异常、post 采样失败、旧帧复用、时间戳不递增、节点跳跃和资源矛盾均 fail-closed 且禁止自动重试。结果显式携带 `mode=visual_only`、`verification=unverified`。
- 现有 `PureVisualSamplingExecutor` 保持兼容，决策结果新增 `mode` 和 `verification` 字段；未修改 MuMu/ADB/Fribbels、正式策略、DP、评分、资源模型、GUI/OCR/Holdout 或正式自动化规则。
- 主审补充并确认边界校验：候选数拒绝布尔值、锚点数值限制在 `[0,1]`、资源键与预期账本一致、post-action 时间戳必须晚于 pre-action。
- 验证：运行时公开测试 `7/7`、旧 dry-run 公开测试 `6/6`、Python 3.9 语法检查通过、`git diff --check` 通过；未连接设备、未读取或上传游戏数据、未采集真实截图、未发送游戏点击、未强化或消耗资源。
- 提交 `eecbca7 feat: wire offline pure visual runtime` 已成功推送至 `origin/codex/policy-v1-manifest-20260725`；提交后的 Git geometric repack 仍因既有异常引用 `refs/heads/main - 副本` 报警，但未修改该引用。当前状态：`implementation_verified_offline_published_20260726`。真实截图/OCR/ADB/游戏点击仍需另建任务并重新授权。

#### 纯视觉点击执行器开发任务建立（2026-07-26）

- 用户已明确批准开发纯视觉点击执行器；独立任务说明为[纯视觉点击执行器开发任务说明.md](纯视觉点击执行器开发任务说明.md)。
- 本阶段只实现可注入窗口后端的归一化坐标单次点击器和离线回归，不连接 MuMu/ADB，不运行真实截图/OCR，不执行真实页面点击、强化、选材或资源消耗。
- 点击器必须要求显式单次授权，校验坐标和视口，调用前记录尝试，后端异常按未知结果处理并禁止内部重试；上层仍由 `VisualOnlyEnhanceExecutor` 负责视觉证据闸门和 `unknown_result` 停止。
- 已完成 `src/e7_enhance/visual_click.py` 与 `tests/test_visual_click.py`：归一化坐标换算、视口/授权校验、一次性尝试锁定、后端异常未知结果和极大数坐标 fail-closed 均有覆盖。
- 定向验证 `tests.test_visual_click`、`tests.test_visual_runtime`、`tests.test_visual_sampling` 共 `17/17` 通过；Python 语法检查和 `git diff --check` 通过。仅使用 fake backend，未连接 MuMu/ADB，未采集截图/OCR，未执行真实点击、强化、选材或资源消耗。
- 执行模型记录为 `gpt-5.6-terra + high`。提交 `2a071f3 feat: add fail-closed visual click executor` 已推送至 `origin/codex/policy-v1-manifest-20260725`；当前状态：`implementation_verified_offline_published_20260726`。真实窗口后端仍需另建任务和明确运行授权。

#### 纯视觉截图与窗口适配层开发任务建立（2026-07-26）

- 用户已批准进入下一步纯视觉适配层开发；独立任务说明为[纯视觉截图与窗口适配层开发任务说明.md](纯视觉截图与窗口适配层开发任务说明.md)。
- 本阶段只实现 `VisualFrame`、稳定帧收集器、证据解析器 seam 和 callback/file-backed fake 适配，不连接 MuMu/ADB，不采集真实截图/OCR，不执行真实页面点击、强化、选材或资源消耗。
- 适配层必须校验帧哈希、视口、时间戳和解析结果；任何漂移、异常或未知状态 fail-closed，禁止自动重试；现有单节点状态机和点击器接口保持兼容。
- 已完成 `src/e7_enhance/visual_adapter.py` 与 `tests/test_visual_adapter.py`：`VisualFrame`、callback/file-backed source、稳定帧收集器、证据解析器 seam 和 `VisualAdapterSampler`；帧哈希、视口、来源、时间戳和证据绑定均有 fail-closed 校验。
- 定向验证 `tests.test_visual_adapter`、`tests.test_visual_runtime`、`tests.test_visual_click`、`tests.test_visual_sampling` 共 `24/24` 通过；Python 语法检查通过；本轮目标文件的 `git diff --check` 通过。仅使用 callback/file-backed fake，未连接 MuMu/ADB，未采集真实截图/OCR，未执行真实窗口点击、强化、选材或资源消耗。
- 执行模型记录为 `gpt-5.6-terra + high`。提交 `a75f5ff feat: add offline visual frame adapters` 已推送至 `origin/codex/policy-v1-manifest-20260725`；当前状态：`implementation_verified_visual_adapter_offline_published_20260726`。真实平台驱动仍需另建任务与运行授权。

#### 纯视觉真实截图与窗口后端接线任务建立（2026-07-26）

- 用户已批准下一步纯视觉接线；已建立独立任务说明：[纯视觉真实截图与窗口后端接线任务说明.md](纯视觉真实截图与窗口后端接线任务说明.md)。
- 本轮仅实现可注入的平台窗口/截图后端协议及离线 fake 回归，保持真实后端惰性；不连接或控制 MuMu/ADB，不采集真实截图，不运行 OCR，不点击页面，不强化、不选材、不消耗资源。
- 后端必须复用 `FrameSource`/`VisualAdapterSampler` 的 fail-closed 约束，记录窗口、视口、来源和时间戳；真实平台运行需另行授权。
- 执行模型记录为 `gpt-5.6-terra + high`。当前状态：`implementation_authorized_pending_terra_high`。

#### 纯视觉真实截图与窗口后端接线离线实现完成（2026-07-26）

- 已完成 [纯视觉真实截图与窗口后端接线任务说明.md](纯视觉真实截图与窗口后端接线任务说明.md) 授权范围内的离线实现：`visual_adapter.py` 新增 `PlatformWindow`、`PlatformWindowBackend`、`PlatformFrameSource` 和 `LazyPlatformFrameSource`。
- 真实后端保持惰性构造；窗口缺失、后端异常、非字节截图载荷、时间戳异常和既有视口/来源漂移均 fail-closed，未增加自动重试或设备依赖。
- 主 agent 复核验证：视觉适配、运行时、点击器和采样定向测试 `27/27` 通过；Python 语法检查通过；本轮目标文件 `git diff --check` 通过。
- 仅使用 offline fake 后端，未连接 MuMu/ADB，未采集真实截图，未运行 OCR，未执行真实窗口输入、游戏点击、强化、选材或资源消耗。
- 未完成项为真实平台后端实例化、真实截图/OCR、只读页面识别和人工监督单节点验证；这些仍需独立任务和明确运行授权。当前状态：`implementation_verified_offline_pending_real_runtime_authorization`。

#### 纯视觉只读截图与局部识别接入任务建立（2026-07-26）

- 用户已批准下一步纯视觉只读接入；已建立独立任务说明：[纯视觉只读截图与局部识别接入任务说明.md](纯视觉只读截图与局部识别接入任务说明.md)。
- 本轮范围为可配置、惰性的 Windows 只读截图后端和局部区域识别 seam，复用现有 `PlatformWindowBackend`、`ocr_regions`、`ocr_normalize` 与 `ocr_paddle` 只读接口；不启动或控制游戏，不点击、不强化、不选材、不消耗资源，不读取或上传底层数据。
- 现有 OCR `0.98` 门槛、新鲜快照契约、正式策略、DP、评分、资源模型、GUI、Holdout 和自动化规则保持不变。
- 执行模型记录为 `gpt-5.6-terra + high`。当前状态：`implementation_authorized_pending_terra_high`。

#### 纯视觉只读截图与局部识别离线接线完成（2026-07-26）

- 已完成 [纯视觉只读截图与局部识别接入任务说明.md](纯视觉只读截图与局部识别接入任务说明.md) 授权范围内的离线接线：新增 `src/e7_enhance/visual_platform.py`，提供惰性只读 Windows 驱动协议、PNG/窗口失败闸门、局部区域提取、模板识别和 OCR 证据绑定。
- 证据继续复用现有 `ocr_regions`、`ocr_paddle` 和 `MIN_FIELD_CONFIDENCE=0.98`；目标指纹现在包含套装、部位、品质、强化等级、等级、主属性和副属性，避免仅凭页面锚点误配目标。
- 主 agent 串行复核验证：视觉适配、运行时、点击器、采样和平台接线定向测试 `32/32` 通过；Python 语法检查和目标文件 `git diff --check` 通过。
- 仅使用 fake/offline 输入，未连接 MuMu/ADB，未采集真实截图，未运行真实 OCR，未执行任何窗口输入、游戏点击、强化、选材或资源消耗。
- 具体 Windows 截图驱动实例化、真实只读截图/OCR、页面识别和人工监督单节点验证仍未完成，不能写成真实运行结论。当前状态：`implementation_verified_offline_pending_real_windows_driver`。

#### 纯视觉真实 Windows 只读预检（2026-07-26）

- 用户已确认人工打开背包中的一件装备详情页，并授权进入真实只读预检；独立任务说明为[纯视觉真实 Windows 只读预检任务说明](纯视觉真实 Windows 只读预检任务说明.md)。
- 本轮仅允许通过 Computer Use 定位已存在的唯一窗口并读取一张内存截图，执行局部页面/装备详情识别；不激活、不点击、不导航、不发送输入，不读取或上传底层数据。
- 识别仍使用现有 `0.98` 字段门槛，输出必须标记 `mode=visual_only`、`verification=unverified`；截图失败、窗口不唯一、区域/字段不确定时 fail-closed 且禁止自动重试。
- `list_apps()` 已唯一返回 MuMu Android 设备窗口 `MuMu安卓设备`（窗口 ID `3276970`）；调用 `get_window_state({ include_screenshot: true })` 时失败，原始错误为 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`。
- 因未获得 PNG/视口/时间戳，本轮未执行局部模板/OCR、装备指纹绑定或字段判定；未激活、点击、导航、底层读取、上传或资源操作。当前状态更新为 `real_windows_capture_failed_fail_closed_20260726`。
- 下一步需另建 Windows 只读截图驱动兼容修复任务，先离线实现并验证不依赖该接口的捕获后端，再重新授权真实预检；不得在当前任务内自动重试或换窗口。

#### Windows 只读截图驱动兼容修复（2026-07-26）

- 用户已批准进入下一步；已建立独立任务说明：[Windows 只读截图驱动兼容修复任务说明](Windows只读截图驱动兼容修复任务说明.md)。
- 本轮范围仅为离线实现不依赖 `SetIsBorderRequired` 的只读捕获后端/适配 seam、fake 测试和回归验证；不得连接、激活或输入 MuMu/Epic Seven，不读取或上传底层数据。
- 已新增 `WindowsClientWindow`、`WindowsClientAreaApi`、`WindowsClientAreaDriver`，并覆盖唯一窗口、视口/来源漂移和 PNG 失败闸门；相关视觉/OCR 回归 `52/52` 通过，Python 语法检查和 `git diff --check` 通过。
- 该实现仍是离线注入 seam，尚未绑定具体 Windows 系统 API；未执行真实截图/OCR。执行模型记录为 `gpt-5.6-terra + high`，当前状态更新为 `implementation_verified_offline_pending_system_api_binding`。
- 下一步需另建“Windows 客户区 API 系统绑定与只读预检”任务并取得明确授权；不得把本轮离线结果写成真实截图已修复。
#### 会话子 agent 自动派发策略说明（2026-07-26，已被后文覆盖）
- 本节“新会话中默认不自动启动子 agent”及“派发需要当前轮明确授权”的结论已被用户于同日的后续澄清覆盖，仅作历史对照。

#### 子 agent 自动执行恢复（2026-07-26）
- 用户明确恢复内部子 agent 的默认自动执行分工：简单代码实现、测试补充、研究器改动和文档机械同步，按 `AGENTS.md` 的既有分工自动派发；每项实际工作仍须先建立或更新独立的 `*任务说明.md`，并遵守同一文件范围不得由多个子 agent 并行修改等约束。
- 继续禁止自动生成面向人类的 `给子 agent 的续作指令：` 或 `text` 代码块。只有用户当轮明确要求给出该类指令时，才使用既有固定格式；该限制不影响内部子 agent 的创建、启动、续跑或派发。
- 旧《子agent续作指令临时禁用规则任务说明.md》关于“不得自动派发”的解释已被本节覆盖，仅保留其聊天交接文本禁用规则。当前权威任务说明：[子agent自动执行恢复规则任务说明.md](子agent自动执行恢复规则任务说明.md)。
- 验证：规则关键词检索确认内部自动派发与人类交接文本限制已分离；`git diff --check` 通过。
- 当前状态：`subagent_internal_auto_dispatch_enabled_human_handoff_text_still_opt_in_20260726`；本轮仅更新执行规则文档，不改变现有功能、正式策略或运行时任务范围。
#### 纯视觉 ADB 截图与受控点击迁移（2026-07-26）
- 本轮唯一权威任务说明为[纯视觉 ADB 截图与受控点击迁移任务说明](纯视觉%20ADB%20截图与受控点击迁移任务说明.md)。用户正式选择 MuMu 主路线为“ADB 截图 -> 局部模板/PaddleOCR 识别 -> 视觉决策 -> 单次受控 ADB 点击 -> 新截图后验”；视觉结果固定标记 `mode=visual_only`、`verification=unverified`，不得表述为服务器确认。
- 此前“Windows 只读截图预检”及其 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)` 结论仅作历史对照，已被本条 ADB 主路线覆盖，不再作为纯视觉主路线阻断。Windows 截图实现仍仅保留为非 ADB 场景备用，不在本轮修改。
- 当前第一阶段只实现和验证只读 ADB PNG 帧源：先运行 MuMu 官方 `adb.exe devices -l`，只接受自动发现且状态为 `device` 的唯一设备；随后仅使用 `adb exec-out screencap -p` 获取 PNG，并接入既有 `VisualFrame`、`PlatformFrameSource`、`VisualAdapterSampler`、稳定帧、帧哈希、视口、时间戳、局部区域、PaddleOCR 和 `0.98` 字段置信度门槛。
- ADB 不可用、设备不唯一、命令失败、PNG 损坏、旋转/视口漂移、黑屏、旧帧、区域越界、锚点缺失、低置信度和字段矛盾均必须 fail-closed，禁止自动重试。不得调用 `input tap`、`swipe`、`keyevent`，不得导航、强化、选材、重启游戏或消耗资源；不得使用 Fribbels、PCAP、TCP 载荷、`player_data.json`、底层读取或外部上传。
- 按用户要求记录实际执行模型为 `gpt-5.6-terra + high`。离线实现已完成：新增 `src/e7_enhance/visual_adb.py` 与 `tests/test_visual_adb.py`，覆盖唯一 `device` 自动发现、只读命令白名单、禁止 `192.168.x.x:5555` 序列号、完整 PNG chunk/CRC、内置像素解码黑屏拒绝、设备身份、旋转/视口漂移、稳定帧和 `VisualAdapterSampler` 全链路；定向 ADB 测试 `6/6`、全部视觉测试 `40/40`、语法检查和 `git diff --check` 均通过。全量测试 `534` 项为 `504` 通过、`7` 失败、`23` 错误，失败均落在缺失私人归档/历史 holdout 或既有研究矩阵/策略清单状态，未读取或恢复归档，不归因于本次 ADB 改动。随后按授权完成一次真实只读预检：官方 MuMu ADB 自动发现唯一 `emulator-5554`（`device`），一次 `exec-out screencap -p` 在内存中通过 PNG/黑屏校验，视口 `1280x720`、字节数 `879645`、帧哈希 `45b852ca53abc3776c54f55a18a11c6da2bb2b981001aa1a604531b0ad43aa97`；未落盘、未 OCR、未输入、未导航、未强化、未选材、未消耗资源、未读取底层数据、未上传。当前状态为 `real_readonly_adb_preflight_passed_pending_post_preflight_commit`：真实局部 OCR、页面识别和受控点击仍未开始。

#### 纯视觉 ADB 局部模板与 PaddleOCR 只读识别阶段建立（2026-07-26）
- 用户已批准进入下一阶段；本阶段唯一权威任务说明为[纯视觉 ADB 局部模板与 PaddleOCR 只读识别任务说明](纯视觉%20ADB%20局部模板与%20PaddleOCR%20只读识别任务说明.md)。
- 阶段范围冻结为 ADB `VisualFrame` 的内存局部裁剪、局部模板锚点、PaddleOCR 行解析和 `VisualEvidence` 证据绑定；保持 `mode=visual_only`、`verification=unverified` 和 `0.98` 字段置信度门槛。
- 本阶段禁止任何 ADB 输入、页面导航、点击、强化、选材、资源消耗、底层读取和外部上传；真实只读识别预检仅在离线实现和定向测试通过后执行一次，截图不落盘。
- 执行模型记录为 `gpt-5.6-terra + high`；当前状态：`implementation_verified_offline_real_readonly_ocr_blocked_dependencies_20260726`。

#### 纯视觉 ADB 局部模板与 PaddleOCR 只读识别完成（2026-07-26）
- 新增 `src/e7_enhance/visual_adb_recognition.py` 与 `tests/test_visual_adb_recognition.py`：完成 ADB PNG 内存局部裁剪、模板锚点/Paddle 行源注入、证据 provenance 绑定和 fail-closed 解析；未改变既有 ADB 帧源、视觉状态机、OCR 门槛、正式策略或自动化规则。
- 离线定向回归 `70/70` 通过，语法检查和 `git diff --check` 通过；执行模型记录为 `gpt-5.6-terra + high`。
- 按本阶段允许的真实只读预检自动发现唯一 `emulator-5554`，一次截图 `1280x720`、`878846` 字节、PNG/内容校验通过，内存局部裁剪 `1152x533` 通过；未保存、未上传、未输入、未导航、未强化、未选材、未消耗资源、未读取底层数据。
- 真实 OCR/模板识别未执行：当前环境缺少 `PIL`、`paddleocr`、`paddle`，按 fail-closed 停止。当前结果是“帧源和局部裁剪真实预检通过、OCR 依赖阻断”，不是页面或装备识别成功，也不是服务器确认。
- 下一步先建立依赖审计/真实局部识别只读复验任务；受控点击仍必须另建任务说明并单独授权。链接：[纯视觉 ADB 局部模板与 PaddleOCR 只读识别任务说明](纯视觉%20ADB%20局部模板与%20PaddleOCR%20只读识别任务说明.md)。

#### 纯视觉 OCR 依赖审计与真实局部识别只读复验阶段建立（2026-07-26）
- 用户已批准进入下一步；本阶段唯一权威任务说明为[纯视觉 OCR 依赖审计与真实局部识别只读复验任务说明](纯视觉%20OCR%20依赖审计与真实局部识别只读复验任务说明.md)。
- 首先只读审计 `PIL`、`paddleocr`、`paddle`，不安装、不联网、不下载模型；仅当依赖审计通过才执行一次真实只读 ADB 局部识别。
- 继续禁止 ADB 输入、页面导航、点击、强化、选材、资源消耗、底层读取和外部上传；结果保持 `mode=visual_only`、`verification=unverified`。
- 执行模型记录为 `gpt-5.6-terra + high`；当前状态：`dependency_audit_authorized_terra_high_in_progress`。

#### 纯视觉 OCR 依赖审计 fail-closed（2026-07-26）
- 只读审计结果：默认 Python `3.13.13` 不可见 `PIL`、`paddleocr`、`paddle`；项目 Python `3.9.2` 可导入 `PIL 11.3.0`，但 `paddleocr` 因 `.cache\\paddle` 权限拒绝、`paddle` 因循环初始化错误而无法导入。
- 依赖导入闸门未通过，本轮未执行 ADB、截图、OCR、模板识别、页面导航或任何输入；未安装依赖、未联网、未下载模型、未修改游戏环境。
- 当前状态更新为 `dependency_audit_failed_import_fail_closed_20260726`。下一步需另建“纯视觉 OCR 运行环境修复与依赖复验”任务并取得明确授权，不能在当前任务中擅自修复环境。

#### 纯视觉 OCR 运行环境修复与依赖复验阶段建立（2026-07-26）
- 用户已批准进入下一步并询问 Paddle 初始化失败原因；本阶段唯一权威任务说明为[纯视觉 OCR 运行环境修复与依赖复验任务说明](纯视觉%20OCR%20运行环境修复与依赖复验任务说明.md)。
- 当前先做只读最小复现和根因诊断，不安装/卸载/升级依赖，不修改缓存权限，不联网，不下载模型，不执行 ADB、截图、OCR、页面导航或任何游戏操作。
- 执行模型记录为 `gpt-5.6-terra + high`；当前状态：`diagnosis_authorized_terra_high_in_progress`。

#### Paddle 导入失败根因诊断完成（2026-07-26）
- Python 3.9.2 干净进程导入 `paddlepaddle 2.6.2` 时，在 `paddle/dataset/common.py:62` 创建 `C:\Users\orangine\.cache\paddle\dataset`，被当前运行环境工作区外写入闸门拒绝，报 `PermissionError: [WinError 5]`。
- `paddleocr 2.10.0` 先导入 `paddle`，因此同样在缓存目录创建阶段失败；尚未进入 OCR 引擎或模型下载。版本与 `requirements-ocr.txt` 一致，普通 ACL 读数显示父目录对用户有完整控制。
- 之前的 `partially initialized module 'paddle'` 只是在同进程第一次导入失败后的二次症状，干净进程未复现为独立根因。
- 当前状态更新为 `diagnosis_complete_environment_write_gate_blocked_repair_authorization_pending_20260726`。尚未修复环境、安装依赖、联网、执行 ADB 或真实 OCR；最小修复需要单独授权可写 Paddle 缓存根目录或受控环境写入。

#### Paddle 工作区缓存导入复验授权（2026-07-26）
- 用户明确批准下一步，授权将 Paddle 缓存重定向到已批准的本地可写 ASCII 工作区路径做一次全新 Python 3.9 导入复验；中文仓库路径会被现有 `_configure_cache()` 拒绝。不安装/升级依赖、不修改用户目录 ACL、不联网、不下载模型、不执行 ADB/OCR/游戏操作。
- 全新 Python `3.9.2` 进程在 ASCII 工作区缓存下成功导入 `paddle 2.6.2` 与 `paddleocr 2.10.0`，退出码 `0`；未构造 OCR 引擎、未下载模型、未执行 ADB/OCR/游戏操作。
- 根因闭环为默认用户缓存写入被环境阻断，中文仓库路径又被现有 ASCII 保护拒绝；当前状态更新为 `repair_verified_ascii_cache_imports_ready_for_readonly_ocr_20260726`。
- 下一步是建立/执行真实 ADB 局部识别只读复验，仍禁止任何输入和强化。

#### 真实 ADB 局部识别只读复验任务（2026-07-26）
- 已创建并完整阅读唯一权威任务说明：[纯视觉真实 ADB 局部识别只读复验任务说明](纯视觉真实ADB局部识别只读复验任务说明.md)。
- 用户已批准进入下一步；授权仅覆盖一次真实只读 ADB 截图及内存局部模板/PaddleOCR 识别，不覆盖点击、页面导航、强化、选材、资源消耗、底层读取或外部上传。
- 执行顺序固定为：ASCII 工作区缓存中的 Python 3.9.2 依赖导入闸门 -> 官方 `adb.exe devices -l` 唯一 `device` -> 单次 `exec-out screencap -p` -> 内存 PNG/稳定帧/视口/哈希/区域校验 -> 局部模板/PaddleOCR -> `VisualEvidence`。
- 结果必须继续标记 `mode=visual_only`、`verification=unverified`；任一依赖、设备、PNG、视口、稳定帧、区域、锚点、OCR 模型或 `0.98` 字段门槛失败都 fail-closed 且禁止自动重试。
- 任务当前状态：`real_adb_readonly_capture_failed_probe_validator_fail_closed_20260726`；执行模型记录为 `gpt-5.6-terra + high`。
- 本轮依赖导入和唯一设备发现通过；按单次截图约束调用一次 `exec-out screencap -p`，但内存内容校验回调因探针未定义 `_nonblack` 抛出 `NameError`，链路以 `FrameCaptureError` fail-closed 停止。截图未落盘，未重试，未执行 OCR、模板识别、证据生成或任何游戏操作。
- 该停止原因是本地只读探针 wiring 错误，不是设备不唯一或 PNG 已证损坏；不能把本轮写成真实页面/装备识别成功。
- 下一步必须先另行更新任务说明并取得新授权，修复探针内容校验后再进行一次单次真实只读截图；在新证据通过前不进入受控点击任务。

#### 真实 ADB 只读探针校正与局部识别复验（2026-07-26）
- 用户已批准进入下一步；已创建并完整阅读唯一权威任务说明：[纯视觉真实 ADB 只读探针校正与局部识别复验任务说明](纯视觉真实ADB只读探针校正与局部识别复验任务说明.md)。
- 本轮范围仅修正一次性只读探针的 `_nonblack(payload, viewport)` 回调 wiring，并在批准的 ASCII 缓存中检查 Paddle 包和本地模型；不修改生产代码、不安装依赖、不联网、不下载模型。
- 执行顺序固定为：依赖导入/模型存在闸门 -> 官方 ADB 唯一 `device` -> 定义完整校验回调 -> 单次 `exec-out screencap -p` -> 内存 PNG/视口/哈希/区域校验 -> （模型可用时）局部模板/PaddleOCR。
- 真实截图最多一次；任何回调异常、设备/PNG/视口/区域失败、模型缺失、低置信度或字段矛盾都 fail-closed，禁止重试；结果继续标记 `mode=visual_only`、`verification=unverified`。
- 当前状态：`probe_validator_corrected_model_cache_missing_fail_closed_20260726`；执行模型记录为 `gpt-5.6-terra + high`。
- 依赖导入通过：全新 Python 3.9.2 进程导入 `paddle 2.6.2` 与 `paddleocr 2.10.0`，退出码 `0`。批准 ASCII 缓存递归文件数为 `0`，没有本地 OCR 模型；按任务禁止下载模型的规则，本轮未构造引擎、未调用 ADB、未截图、未 OCR、未生成视觉证据。
- `_nonblack` 探针 wiring 已按任务要求完成离线校正方案，但未取得真实截图验证；前次截图失败后没有新的 ADB 重试。
- 下一步必须另行授权本地模型提供或受控模型下载，并新建/更新任务说明；在此之前保持视觉识别和受控点击停止。

#### PaddleOCR 模型受控下载与本地引擎复验阶段建立（2026-07-26）
- 用户已明确授权采用 PaddleOCR 引擎并允许受控联网下载模型；本轮唯一权威任务说明为[纯视觉 PaddleOCR 模型受控下载与本地引擎复验任务说明](纯视觉%20PaddleOCR%20模型受控下载与本地引擎复验任务说明.md)。
- 本轮只允许将 PaddleOCR 中文识别所需模型写入批准的 ASCII 工作区缓存，并在全新 Python 3.9.2 进程验证本地引擎初始化；禁止 ADB、截图、OCR 识别、页面导航、点击、强化、选材、资源消耗、底层读取、Fribbels/PCAP/TCP、外部上传和私人归档操作。
- 此前“模型缺失即停止”的结论仅作历史对照，已被本授权覆盖；`0.98` 字段置信度门槛、`mode=visual_only` 和 `verification=unverified` 保持不变。
- 执行模型记录为 `gpt-5.6-terra + high`；当前状态：`authorized_model_download_pending_execution_20260726`。

#### PaddleOCR 模型下载与本地引擎复验完成（2026-07-26）
- 已按[纯视觉 PaddleOCR 模型受控下载与本地引擎复验任务说明](纯视觉%20PaddleOCR%20模型受控下载与本地引擎复验任务说明.md)执行用户授权范围内的模型准备。
- 从 PaddleOCR 官方资源下载 PP-OCRv4 中文检测/识别模型和中文方向分类模型，共 9 个文件、`18,032,845` 字节，写入批准的 ASCII 工作区缓存；未写入默认用户缓存，未读取或上传游戏数据。
- 模型缓存清单（相对路径、大小、SHA-256）摘要为 `94ddad6c342e222483498b8ab6a88286393a3b3071746156477d0016979bdc31`。
- 全新 Python `3.9.2` 进程导入 `paddle 2.6.2`、`paddleocr 2.10.0` 成功，并在仅允许缓存命中的离线探针下构造 `PaddleOCR`、`TextDetector` 和 `TextRecognizer` 成功，退出码 `0`。
- 本轮未执行 ADB、截图、OCR 识别、页面导航、点击、强化、选材或资源消耗；“模型缺失即停止”仅作为前一探针历史对照，已被本授权覆盖。
- 执行模型记录为 `gpt-5.6-terra + high`；当前状态：`model_downloaded_offline_engine_verified_pending_real_adb_ocr_20260726`。

#### 真实 ADB 模型就绪局部 OCR 只读复验阶段建立（2026-07-26）
- 用户批准进入下一步；本轮唯一权威任务说明为[纯视觉真实 ADB 模型就绪局部 OCR 只读复验任务说明](纯视觉真实ADB模型就绪局部OCR只读复验任务说明.md)。
- 本轮仅允许自动发现唯一 `device`、执行一次 `exec-out screencap -p`、内存 PNG/装备区域校验和一次本地 PaddleOCR 局部识别；禁止第二次截图、稳定帧包装、VisualEvidence 稳定证据、ADB 输入、页面导航、点击、强化、选材、资源消耗、底层读取和外部上传。
- 结果必须保持 `mode=visual_only`、`verification=unverified`；单帧局部 OCR 通过不等于服务器确认、装备底层确认或强化成功。
- 执行模型记录为 `gpt-5.6-terra + high`；当前状态：`authorized_real_adb_single_frame_local_ocr_pending_20260726`。

#### 真实 ADB 模型就绪局部 OCR 只读复验完成（2026-07-26）
- 已按[纯视觉真实 ADB 模型就绪局部 OCR 只读复验任务说明](纯视觉真实ADB模型就绪局部OCR只读复验任务说明.md)执行一次真实只读单帧探针。
- 自动发现唯一 `emulator-5554 device`；一次 PNG 内存校验通过，视口 `1280x720`、`858918` 字节、帧哈希 `5df31a9f02817c90c7e7e636489c338415411c5f65650906fc2c8e29e5b1fef2`；局部裁剪 `1152x533`、`701013` 字节通过，均未落盘。
- 本地 PaddleOCR 对局部图像返回 92 行并运行现有解析，但 fail-closed：`unrecognized:set`、`missing:enhance`、`low_confidence:substats[2]`（`0.927478`）。部分结果显示英雄武器、85、攻击力 100、暴击率 5%、攻击力 8%，不能视为目标装备或服务器确认。
- 未执行第二次截图、稳定帧采样、`VisualEvidence` 包装、模板点击、页面导航、ADB 输入、强化、选材、资源消耗、底层读取或外部上传；结果保持 `mode=visual_only`、`verification=unverified`。
- 执行模型记录为 `gpt-5.6-terra + high`；当前状态：`real_adb_single_frame_local_ocr_completed_fail_closed_20260726`。下一步需目标装备详情页就绪后另行授权新单帧复验；稳定帧证据需另行授权。

#### 纯视觉目标装备详情页单帧字段复验任务建立（2026-07-26）
- 用户已将画面切换到目标装备详情页，并提供本次视觉目标指纹：命中套武器、红装/传说、85级、`+0`、主属性攻击力 `100`、副属性暴击率 `5%`/攻击力 `8%`/效果抗性 `8%`、官方分数 `28`。
- 已创建并完整阅读唯一权威任务说明：[纯视觉目标装备详情页单帧字段复验任务说明](纯视觉目标装备详情页单帧字段复验任务说明.md)。本轮只授权一次自动发现唯一设备后的 ADB 内存截图和局部 PaddleOCR 字段比较；不点击、不导航、不强化、不选材、不消耗资源、不落盘、不上传。
- 结果继续固定为 `mode=visual_only`、`verification=unverified`；单帧通过不等于服务器确认，也不构成稳定帧或自动强化授权。执行模型记录为 `gpt-5.6-terra + high`。
- 当前状态：`authorized_target_detail_single_frame_pending_20260726`。

#### 纯视觉目标装备详情页单帧字段复验 fail-closed（2026-07-26）
- 已按[纯视觉目标装备详情页单帧字段复验任务说明](纯视觉目标装备详情页单帧字段复验任务说明.md)执行一次真实只读 ADB 单帧和内存局部 PaddleOCR；未执行第二次截图、稳定帧、任何输入或游戏操作。
- 自动发现唯一 `emulator-5554 device`；PNG 视口 `1280x720`、`858918` 字节，帧哈希 `5df31a9f02817c90c7e7e636489c338415411c5f65650906fc2c8e29e5b1fef2`，局部裁剪原始 `370x430`、送 OCR `740x860`，均未落盘。该哈希与上一轮混合背包画面完全相同，存在旧帧/页面未更新风险。
- OCR 返回 `20` 行，仅识别到 `85`（`0.999722`）；缺失套装、部位、强化、主/副属性和分数，原因：`missing:enhance`、`unrecognized:gear_score`、`unrecognized:main_and_substats`、`unrecognized:rank_slot`、`unrecognized:set`。目标指纹比对为 `false`，结果保持 `mode=visual_only`、`verification=unverified`。
- 当前状态：`target_detail_single_frame_failed_stale_or_wrong_page_fail_closed_20260726`。下一次读取前必须在目标详情页实际刷新后另行更新任务说明并取得新的单次只读授权；受控点击、强化和资源操作仍未授权。

#### 纯视觉目标装备详情页新授权单帧复验仍为旧帧（2026-07-26）
- 用户确认已刷新到目标装备详情页并再次授权一次只读 ADB 单帧复验；任务说明已补充本次执行记录，结果继续标记 `mode=visual_only`、`verification=unverified`。
- 自动发现唯一 `emulator-5554 device`；只执行一次 `exec-out screencap -p`，内存 PNG 校验通过，视口 `1280x720`、`858918` 字节，帧哈希 `5df31a9f02817c90c7e7e636489c338415411c5f65650906fc2c8e29e5b1fef2`。
- 哈希与上一轮旧帧完全相同，按旧帧/页面未更新 fail-closed；本次未调用 OCR，未落盘、未上传、未输入、未导航、未强化、未选材、未消耗资源。此前 `20` 行 OCR 仅属于上一轮记录，不归入本次复验。
- 当前状态更新为 `target_detail_single_frame_recheck_failed_same_stale_frame_20260726`。在 ADB 返回不同哈希的新鲜详情页帧并完成新的授权前，不进入稳定帧、点击执行器或自动强化。

#### 用户截图覆盖“页面未刷新”判断并定位详情裁剪区错误（2026-07-26）
- 用户提供人工截图确认目标装备已在背包中选中，右侧详情面板显示命中套武器、85级、攻击力 `100`、暴击率 `5%`、攻击力 `8%`、效果抗性 `8%`、装备分数 `28`，与目标指纹一致；该证据仅为人工观察，仍标记 `mode=visual_only`、`verification=unverified`。
- 旧帧哈希不变不能单独证明页面未刷新。结合截图与现有 `BACKPACK_ENHANCE_PANEL`（左侧背包网格裁剪），更可能的根因是 OCR 读取了错误区域，导致只识别 `85`；此前“旧帧/页面未更新”结论改作历史对照，不再作为当前根因。
- 当前状态更新为 `visual_detail_panel_crop_mismatch_diagnosed_20260726`。下一步先创建右侧详情局部区域/锚点修正任务说明并完成离线测试；实现授权和新的单次只读 ADB OCR 授权分别保留，期间不截图、不输入、不点击、不强化。
@@
#### 纯视觉真实ADB模型就绪局部OCR只读复验完成（2026-07-26）
