# 纯视觉目标装备详情页单帧字段复验任务说明

## 目标

在用户已将 MuMu 停留于目标装备详情页的前提下，执行一次真实、只读、内存内的 ADB PNG 截图和局部 PaddleOCR 字段复验。仅验证视觉证据是否匹配用户提供的目标指纹，不执行任何游戏操作。结果固定标记 `mode=visual_only`、`verification=unverified`，不得表述为服务器确认或强化结果。

## 唯一权威来源

本文件是本轮唯一权威任务来源。ADB 帧源、设备发现和禁止输入继承《纯视觉 ADB 截图与受控点击迁移任务说明.md》；局部区域和 `0.98` 字段门槛继承《纯视觉 ADB 局部模板与 PaddleOCR 只读识别任务说明.md》；模型缓存和引擎前置条件继承《纯视觉 PaddleOCR 模型受控下载与本地引擎复验任务说明.md》。

## 本次目标指纹

- 部位：武器；套装：命中套；品质：红装/传说；等级：85；强化：`+0`
- 主属性：攻击力 `100`
- 副属性：暴击率 `5%`、攻击力 `8%`、效果抗性 `8%`
- 用户提供官方分数：`28`

## 授权范围

用户已批准下一步。本轮仅覆盖：自动发现唯一 MuMu `device`、执行一次由该设备派生的 `exec-out screencap -p`、在内存校验 PNG/视口/非黑屏/帧哈希和装备区域、调用一次本地 PaddleOCR 并与目标指纹做字段比较。

## 明确禁止

- 禁止 ADB `input tap`、`swipe`、`keyevent`，禁止页面导航、点击、强化、选材、重启和资源消耗。
- 禁止第二次截图、自动重试、稳定帧包装、全屏盲识别、降低 `0.98` 字段置信度门槛或把单帧结果写成 `VisualEvidence` 稳定证据。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、外部上传和截图落盘；不读取或恢复私人归档。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout、自动化规则和既有 ADB/Windows 生产代码；不得触碰 `refs/heads/main - 副本` 或使用 `git add -A`。

## 执行步骤

1. 在全新 Python 3.9.2 进程中将 Paddle/PaddleOCR 缓存指向已批准的 ASCII 模型缓存，并确认本地模型命中；失败立即停止。
2. 运行官方 MuMu `adb.exe devices -l`，只接受自动发现且状态为 `device` 的唯一设备；设备不唯一或状态异常立即停止且不重试。
3. 只执行一次由自动发现结果派生的 `exec-out screencap -p`，在内存中完成 PNG 结构、CRC、视口、非黑屏和哈希校验。
4. 以内存局部区域裁剪调用一次 PaddleOCR，复用现有行解析和 `0.98` 门槛；缺失、矛盾、页面不匹配或任一字段低于门槛均 fail-closed。
5. 只报告元数据、解析字段、目标指纹比对和停止原因，不保存截图或输出游戏数据载荷。

## 产物与验证要求

- 记录模型、Python、唯一设备、一次 PNG 的视口/字节数/哈希、局部区域尺寸、OCR 行数和解析结果或停止原因。
- 离线定向测试、语法检查和 `git diff --check` 与真实单帧结果分开报告。
- 本任务不授权受控点击；点击执行器、稳定多帧、自动强化和资源操作必须另行任务说明并取得明确授权。

## 停止条件

依赖、模型、设备、PNG、视口、非黑屏、区域、OCR、字段、置信度或目标指纹任一环节失败立即停止，不重试；任何需要输入或第二次截图的步骤另行授权。

## 执行记录

- 执行模型：`gpt-5.6-terra + high`；Python `3.9.2`；本地 Paddle/PaddleOCR 模型缓存命中。
- 官方 MuMu ADB 自动发现唯一设备 `emulator-5554`（状态 `device`），未使用固定 IP/端口；只执行一次 `exec-out screencap -p`，未执行任何 ADB 输入。
- PNG 仅在内存中校验：视口 `1280x720`，`858918` 字节，帧 SHA-256 `5df31a9f02817c90c7e7e636489c338415411c5f65650906fc2c8e29e5b1fef2`；装备局部裁剪在内存中为 `740x860`（原始区域 `370x430`），未落盘截图。
- 本次哈希与上一轮混合背包画面的单帧哈希完全相同，属于旧帧/页面未更新风险，不能证明当前屏幕已进入目标详情页。
- PaddleOCR 在装备局部内存图上返回 `20` 行；解析仅识别到 `85`（置信度 `0.999722`），fail-closed 原因：`missing:enhance`、`unrecognized:gear_score`、`unrecognized:main_and_substats`、`unrecognized:rank_slot`、`unrecognized:set`。目标指纹比对为 `false`。
- 结果保持 `mode=visual_only`、`verification=unverified`；未执行第二次截图、稳定帧、模板点击、页面导航、ADB 输入、强化、选材、资源消耗、底层读取或外部上传。

## 当前状态

`target_detail_single_frame_failed_stale_or_wrong_page_fail_closed_20260726`

## 结论、风险与下一步

- 本轮仅证明 ADB 单帧和局部 PaddleOCR 可运行；没有得到目标装备详情页的完整高置信度视觉证据，也没有证明服务器或游戏状态。
- 由于帧哈希与上一轮完全相同且字段大量缺失，当前证据按旧帧/页面未更新处理；禁止自动重试和任何受控点击。
- 下一次截图必须在用户确认画面已实际刷新到目标装备详情页后，另行更新任务说明并取得新的单次只读授权；在此之前保持点击、强化和资源操作停止。

## 新授权复验记录（2026-07-26）

- 用户再次确认已刷新到目标装备详情页并批准一次新的单帧只读复验；本次执行模型仍为 `gpt-5.6-terra + high`，Python `3.9.2`。
- 自动发现仍为唯一 `emulator-5554 device`；仅执行一次由该设备派生的 `exec-out screencap -p`。PNG 在内存中通过校验，视口 `1280x720`、字节数 `858918`，帧 SHA-256 仍为 `5df31a9f02817c90c7e7e636489c338415411c5f65650906fc2c8e29e5b1fef2`。
- 该哈希与上一轮旧帧逐字节相同，因此按旧帧/页面未更新立即停止；本次没有调用 PaddleOCR，也没有生成新的字段结果。截图未落盘、未上传、未输入、未导航、未强化、未选材、未消耗资源。
- 本次状态：`target_detail_single_frame_recheck_failed_same_stale_frame_20260726`。下一次截图仍须在 ADB 实际返回不同于该哈希的新帧后，重新更新任务说明并取得新的单次只读授权；禁止自动重试。
