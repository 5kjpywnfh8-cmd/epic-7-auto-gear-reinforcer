# 真实多节点新鲜快照采集与契约验收任务说明

## 目标

在用户逐项提供执行参数并授权只读预检后，为**一件已明确授权的装备**按标准节点 `+0 -> +3 -> +6 -> +9 -> +12 -> +15` 采集新鲜权威快照，并以已发布的离线新鲜快照契约进行逐节点重新配对与验收。

本任务的每个节点都必须由当次新采集的同批 `player_data.json` 和 `reader_result.json` 证明。任一节点不满足契约即停止；不得将旧快照、旧配对结果或旧建议用作后续节点证据。

## 当前状态

`fresh_read_fail_closed_no_account_sync_20260726_012048`

四项执行参数、只读预检和 `+0` 新鲜真实读取授权均已记录，且用户已授权读取器向指定 Fribbels API 上传本账号 TCP 载荷。用户选择的 A 方案标准读取已运行一次，但 Fribbels 解出 `0` 件装备，未抓到账号同步包；本批次未形成新鲜双文件，已 fail closed。不得重试、配对、导入或继续任何游戏内操作。

## 范围

- 仅限一个目标装备实例；不得扩展到批量装备、背包扫描或替换目标。
- 仅限标准节点 `0/3/6/9/12/15`；不得跳过节点，`+15` 后结束。
- 每个节点先采集新鲜同批双文件，再运行离线契约验证；通过前不得进入下一节点。
- 真实读取只允许使用《MuMu抓包读取与真实装备导入任务说明.md》规定的读取器、自动发现设备和标准输出路径。
- 强化本身、材料选择、资源扣减和页面点击不在本任务的自动化范围内。即使后续单独获得操作授权，也必须由用户在每个节点确认后再继续。

## 执行前置输入

开始任何只读预检前，用户必须一次性明确以下四项参数。缺少任一项即保持当前状态：

| 参数 | 必填内容 | 验收用途 |
| --- | --- | --- |
| 目标装备 | 用户可识别的 batch/装备描述，以及可复核的部位、套装、品质、等级、主属性与副属性指纹 | 只读预检时由正式配对器唯一确定 `item_instance_id`；不得由用户或执行者人工填写实例 ID |
| 节点计划 | 本轮允许覆盖的连续节点范围，例如仅 `+0` 采集，或 `+0 -> +3` | 禁止跳跃、隐含全程强化或超过授权节点 |
| 材料来源 | 允许材料池、允许材料 ID/名称及其库存来源 | 禁止跨池、猜测或自动选材 |
| 资源硬上限 | 每段和累计的材料与金币硬上限；未提供的维度明确为不允许 | 离线预算核对与超限 fail-closed |

用户已明确授权本任务的**只读预检**，但该授权仅在四项参数齐全后生效。只读预检仅可检查设备发现结果、读取器前置条件、现有输出时间与任务输入完整性，并由正式配对器唯一确定目标实例；不抓包、不重启游戏、不上传载荷、不导入档案、不操作页面、不强化。

### 已记录执行参数（2026-07-26）

| 参数 | 已授权值 | 限制 |
| --- | --- | --- |
| 目标装备 | 当前 `+0`、85 级、红装/Epic、武器、爆伤套；主属性攻击力 `100`；副属性生命 `4%`、爆伤 `4%`、暴击 `5%`、速度 `2` | `item_instance_id` 仍必须由正式配对器唯一确定；不得人工填写、替换或扩展目标 |
| 节点计划 | 仅 `+0 -> +3` | 本轮不授权 `+6` 及之后节点；`+0` 真实读取和到 `+3` 的人工强化均仍待后续单独授权 |
| 材料来源 | 普通装备材料池；仅普通装备粉末与下级强化石 | 上级强化石为 `0`，不得使用；不得跨材料池、猜测库存或自动选材 |
| 每段硬上限 | 粉末 `2`、下级强化石 `1`、上级强化石 `0`、金币 `17,600` | 未列材料和费用均不允许 |
| 累计硬上限 | 粉末 `2`、下级强化石 `1`、上级强化石 `0`、金币 `17,600` | 仅覆盖本轮唯一的 `+0 -> +3` 分段 |

### 只读预检结果（2026-07-26）

| 检查项 | 结果 | 处理 |
| --- | --- | --- |
| ADB 设备发现 | 退出码 `0`；`device_count=1`，`other_state_count=0` | 设备发现通过；未记录或使用固定设备地址 |
| Root | 通过 | 不构成继续读取授权 |
| `tcpdump` | 读取器实际探针通过：`tcpdump_probe_exit=0`、`tcpdump_ready=True` | 通过；此前 `command -v tcpdump` 的退出码 `2` 为不兼容探针方式，不代表读取器失败 |
| 读取器路径 | 均存在 | 仅证明本地前置路径可用，不启动读取器 |
| 既有输出 | 标准 `current/player_data.json` 与 `gear_fribbels.json` 存在，时间为 `2026-07-22 05:20:45/46 UTC` | 仅作旧输出；不得作为本任务任何新鲜节点证据 |

本轮未执行抓包、重启游戏、页面操作、OCR、导入、强化、选材或资源消耗；目标唯一配对也未开始。下一步仅等待用户对 `+0` 新鲜真实读取的明确授权。

### 历史快照文件侧唯一配对（2026-07-26）

在不读取私人归档、不执行采集或页面操作的前提下，已使用标准 `current/player_data.json` 与 `snapshots/20260722_132045/reader_result.json` 完成文件侧配对核对。标准 `current/player_data.json` 与 `gear_fribbels.json` 的时间分别为 `2026-07-22T05:20:45Z` 和 `2026-07-22T05:20:46Z`，均为历史旧输出。

| 字段 | 结果 |
| --- | --- |
| `player_items` / `raw_items` | `2670` / `2670` |
| `candidate_count` | `1` |
| `candidate_instance_sha256` | `36465dfb10980dc0f50293f10813e60777199cb50a6e4fe88d8774627b309b93` |
| `candidate_enhance` | `0` |
| `player_sha256` | `3d74e2d69147d5a1053f0ebccc217ed2c64842fa2ce91cce6413e5fea364185b` |
| `reader_sha256` | `3831f40814c015166794bf44fbd55d153668532d549e612aff431c24e42f900b` |

本表仅证明历史 `2026-07-22` 双文件在文件侧唯一配对到目标指纹，未记录原始实例 ID。它不是本任务的 `+0` 新鲜节点证据，不得用于跳过 `+0` 新鲜真实读取、同批采集、页面/OCR 核验或离线契约验收。

### `+0` 新鲜读取授权冲突（2026-07-26）

用户已授权 `+0` 新鲜真实读取及向《MuMu抓包读取与真实装备导入任务说明.md》指定 Fribbels API 上传本账号 TCP 载荷，但同时明确禁止页面点击。对读取器源码的只读核对确认，固定标准命令的 `--auto-enter-seconds 105` 会在启动 `tcpdump` 后：

- force-stop 并通过 monkey 重启 Epic Seven；
- 在 105 秒内循环发送 ADB `input tap`，用于登录/公告位置；
- 在前三轮发送 `KEYCODE_BACK`。

用户已于 2026-07-26 选择 A 方案，原冲突已在限定范围内解除：

| 选项 | 所需授权与边界 |
| --- | --- |
| A（已选） | 允许标准读取器仅为登录/公告执行自动点击和重启；仍禁止任何装备、材料、强化页面输入，以及强化、选材、导入和资源消耗 |
| B（未选） | 维持零页面点击；另行授权偏离固定标准命令并评估 `--auto-enter-seconds 0`，不保证账号同步或读取成功 |

本次文档同步未启动抓包、未重启、未上传、未点击、未强化、未选材或消耗资源。

### `+0` 标准新鲜读取失败（2026-07-26）

已按 A 方案运行一次固定标准读取命令。读取器完整输出的结论为“Fribbels 解出 `0` 件装备，账号同步包可能未抓到”。批次 `20260726_012048` 仅产生 `1312188` 字节 PCAP；未生成新的 `player_data.json`、`reader_result.json` 或不可变 `snapshot`，标准 `current` 仍为 2026-07-22 历史文件，不能充当本任务新鲜 `+0` 证据。

| 诊断项 | 结果 |
| --- | --- |
| `packet_count` | `2969` |
| 协议计数 | `udp=812`、`tcp=2144`、`ip_other=5`、`other=8` |
| 载荷字节 | `tcp_payload_bytes=1100894`、`udp_payload_bytes=2880` |
| `tcp_payload_groups` | `478` |
| `observed_ports` | `[443,9997,51390,5138,80,39518,442,8081,53,32946,39542,56248]` |

状态为 `fresh_read_fail_closed_no_account_sync_20260726_012048`。在用户另行授权且环境发生可解释变化前，停止重试、目标配对、导入、页面操作、强化、选材和资源消耗。

## 权威口径

1. 根目录 `AGENTS.md`：项目级边界、文档同步、模型与验证规则。
2. [多节点新鲜快照与重新配对契约任务说明](多节点新鲜快照与重新配对契约任务说明.md)：已发布离线契约、节点顺序、同批双文件、`0.98` OCR 门槛和 fail-closed 语义。
3. [MuMu抓包读取与真实装备导入任务说明](MuMu抓包读取与真实装备导入任务说明.md)：真实读取器、自动设备发现、输出校验和失败处理。
4. [e7-gear-enhance-plan.md](e7-gear-enhance-plan.md)：阶段状态、历史结论和后续闸门。
5. 已发布代码与报告仅用于离线验收：`src/e7_enhance/fresh_snapshot_contract.py`、`src/e7_enhance/ocr_backpack_pair.py`、`reports/fresh_snapshot_contract_validation_20260725.json` 和 `.md`。

聊天内容不得替代或扩大本说明。与上述口径冲突时停止并先更新本说明与总计划。

## 禁止项

- 不修改正式策略、DP、评分、资源模型、策略 manifest、Holdout、GUI、OCR 门槛或自动化规则。
- 不修改离线契约的 `0.98` 门槛、零/多候选、跨批、哈希不一致、旧证据复用和节点跳跃的 fail-closed 语义。
- 不自动点击强化、不自动选材、不自动扣减资源、不实现或启用无人值守状态机。
- 未获得对应阶段明确授权时，不连接 MuMu/ADB/OCR，不抓包、不重启游戏、不上传解码载荷、不导入真实档案。
- 不读取、恢复、暂存或上传私人归档；不直接修改真实 JSON、PCAP 或 Fribbels 响应。
- 不写死、记录、复用或输出任何 `192.168.x.x:5555` 地址；设备必须由读取器自动发现。
- 不修改或删除既有异常引用 `refs/heads/main - 副本`。

## 顺序执行步骤

1. 四项执行参数已记录；目标实例 ID 必须在后续正式只读配对中产生，不得人工填写。仅允许的节点范围为 `+0 -> +3`。
2. 本轮只读预检已通过；历史快照的文件侧唯一配对已完成，但不是新鲜节点证据。A 方案标准读取已因未抓到账号同步包 fail closed；不得重试、目标配对、导入或执行任何游戏内操作，直到用户另行授权且环境发生可解释变化。
3. `+0` 真实读取及 A 方案授权已取得；本次固定标准命令已执行但未抓到账号同步包，未形成新鲜双文件，已触发 `fail_closed`。不得重试、导入档案或继续任何游戏内操作，除非用户另行授权且环境发生可解释变化。
4. 只有未来一次获准且成功形成同批双文件的读取，才可为 `+0` 生成不含私人载荷的节点元数据并运行离线契约；必须唯一重配对到目标实例并通过页面、同批、哈希、`raw` 和 `0.98` 闸门。
5. 只有 `+0` 验收通过且用户明确授权下一段人工操作时，才允许用户将目标装备强化至已授权的下一标准节点。系统不得点击、选材或消耗资源。
6. 到达每个后续节点后停止，等待该节点的新鲜读取授权；重新采集同批双文件并重新运行契约。验证实例连续性、预期强化等级、新采集 ID、时间顺序和旧证据失效。
7. 重复步骤 5-6，直至用户授权的终点；到达 `+15` 时结束。每个阶段均须更新本说明、总计划和脱敏报告。

## 失败与停止条件

以下任一情况必须结构化记录 `fail_closed` 并停止当前链路，不得自动重试或给出继续强化建议：

- 缺少节点采集授权或下一段人工操作授权；四项执行参数和只读预检授权已于本轮记录。
- 标准读取器所需的登录/公告页面输入未获得 A 方案明确授权，或其行为扩展到装备、材料、强化页面输入时，必须停止。
- Fribbels 解出 `0` 件装备、未生成新鲜 `player_data.json`/`reader_result.json`/snapshot，或无法证明账号同步包已抓到时，必须 fail closed。当前批次已触发本条件。
- ADB 无 `device`、MuMu Root 或读取器实际 `tcpdump` 探针不可用、读取器/API 解码失败，或同一环境无变化下重复失败。
- 输出非新鲜、缺少同批双文件、时间/采集 ID 不能证明顺序、哈希不符、缺少实例 ID、装备数组、`raw`、PCAP 或原始响应。
- 目标实例零候选、多候选、实例漂移、等级不等于预期节点、节点跳跃、页面不明确、必需 OCR 字段低于 `0.98`。
- 材料池、库存、资源硬上限不足、跨池、未知材料，或任何预算验证不通过。
- 需要修改禁止项、需要读取私人归档、需要手工指定设备 IP，或需要把历史快照当作当前节点证据。

## 产物

- 本说明中逐项记录的执行参数、授权记录、节点状态和停止原因。
- 每个已授权节点的脱敏元数据：采集 ID、时间、规范化 SHA-256、目标实例 ID、节点、契约结果和 fail-closed 原因（如有）。
- 不含私人路径、设备地址、原始载荷或 PCAP 内容的 JSON/Markdown 节点验收报告。
- 同步更新后的 `e7-gear-enhance-plan.md`。

## 验证要求

- 任一真实节点采集前，先完成并记录只读预检结果；未授权预检不得执行。
- 每个节点都必须证明同批双文件、新鲜采集 ID、唯一目标实例、预期 `raw` 强化等级、页面明确性和所有必需 OCR 字段 `>= 0.98`。
- 离线契约必须对每个节点产生稳定、脱敏的结果；任何 fail-closed 均不得有继续建议或自动化副作用。
- 若涉及资源计划，只可使用已发布的离线预算规划器验证用户给定的材料与硬上限；通过不构成材料选择或强化授权。
- 每个阶段结束后精确审阅本次修改，运行与改动相称的定向验证、`git diff --check`，并按项目规则提交、推送；不得使用 `git add -A`。

## 模型与执行分工

- 后续代码实现和代码测试必须使用 `gpt-5.6-terra + high`；实际模型和 reasoning effort 必须写入阶段记录、报告和总计划。若该配置不可用，停止并如实记录，不得静默替换。
- 主 agent 负责范围确认、用户授权闸门、产物审阅、文档同步和是否进入下一步骤的决定。
- 本说明为本阶段唯一任务权威来源；在更新为可执行状态前，不得启动真实操作或后续子任务。
