# Epic 非速度 Phase B/C 体系映射修复与坦克门槛复核任务说明

## 状态

`completed_offline_pending_user_confirmation_holdout_paused`

已完成体系分组修复、缓存验证复用、输出组合重聚合和纯坦9组控制变量研究。`balanced_output_m4` 的旧选择已撤销；新的候选仍待用户确认，独立 holdout 保持暂停。

## 能力与工具审计

- 当前仓库已具备官方分支枚举、共同随机路径、加权汇总、ESS、五 seed 成对区间、原子缓存和显式资源池，足以完成本任务。
- 当前数据规模为157件开发样本及既有正式基础分片，不需要额外安装数据分析 skill、数据库或大数据框架。
- 本轮优先复用 Python 标准库和项目现有研究器。新增 skill 不能替代体系映射修复、样本隔离或统计闸门，不作为执行前置条件。
- 只有后续明确需要交付 Excel 工作簿、交互式可视化或现有工具无法表达的高级统计模型时，才单独评估对应 skill；不得在本任务中安装依赖扩大范围。

## 已确认根因

`tools/research_epic_threshold_matrix_phase_b.py::system_group()` 将项目正式分类“输出(必爆)”错误写成了“输出(必暴)”。当前 157 件开发样本中：

- 输出：61；
- 输出(必爆)：6，错误进入 `unknown`；
- 抗坦：35；
- 纯肉：18；
- 命坦：15；
- 半肉：21；
- 双效：1。

因此：

- current 和全部 global T0/T3 行不依赖体系组映射，仍可保留为历史全局对照；
- `pure_output`、`balanced_output_*`、`balanced_high_priority_*` 及其 Pareto 结论失效，必须修正后重聚合；
- 旧 `balanced_output_m4=68.8965` 百里分/10万总体力是错误分组下的条件估计，不得用于冻结或发布。

## 目标

1. 修复“输出(必爆)”体系映射并增加回归测试。
2. 复用 Phase B formal `runs=10` 公共基础缓存，重新生成全部体系组和 Phase C 组合结果。
3. 在修正后的输出保护候选上，独立研究纯坦 `+0/+3` 门槛。
4. 重新给出每10万总体力总百里分、配对增量、体系风险和样本充分性结论。

## 修复要求

- `system_group("输出(必爆)")` 必须返回 `pure_output`。
- 增加正式分类名称全集测试，防止同音字、简繁体或括号差异静默进入 `unknown`。
- `unknown` 必须在报告中列出实际分类名称和数量；已知正式分类进入 `unknown` 时研究直接失败。
- 候选哈希和 schema 升级，旧体系候选分片不得复用；公共正式路径基础缓存可在哈希验证一致后复用。

## 纯坦独立控制变量

修正输出分组后，以纯输出 `8/13` 作为固定保护基线，只改变纯坦门槛：

- 纯坦 `T0 ∈ {8, 10, 12}`；
- 纯坦 `T3 ∈ {13, 15, 17}`；
- 报告完整 9 组，但优先比较保持 `T3 >= T0 + 3` 的合法路线。
- 半肉和双效暂用全局 `12/17`，不因当前稀疏样本发布独立门槛。

每组必须重新决定 `+0` 进入 `+3` 的状态集合，不能固定 `+0` 后只替换 `+3`。

## 样本与统计

- 当前纯坦组共 68 件，只足以研究“纯坦合组”的粗门槛，不足以分别发布抗坦、纯肉、命坦或体系×部位门槛。
- 当前双效仅 1 件、半肉 21 件，不得据此发布独立规则。
- 修正研究只用于选择新 holdout 候选。最终独立 holdout 固定扩大为 128 件真实 `+0`，不在 48/64 件时提前查看结果。
- holdout 应分别报告 pure_output 与 pure_tank 的有效样本数；若任一高优先级组少于 40 件，不发布组独立门槛，只继续盲收至满足预注册数量。

## 报告要求

- 五 seed `runs=10` 成对价值率与 95% 区间；
- 每100,000总体力正式百里分总量及其绝对区间；
- 相对 current 的配对百里分增量及区间；
- 原生/转换75+、22速、裂缝体力、圣女3-7体力和循环数；
- `+0/+3` 按 pure_output、pure_tank、bruiser、dual、unknown 拆分的误停数、概率质量和 utility loss；
- 开发样本按体系、部位的数量表；
- 明确区分“条件于现有胚子分布的绝对总量”和“同轨迹候选差值”。

## 产物

- 修正后的 Phase B/C 研究器、测试、新 schema 与候选结果；
- 人类可读修正报告和原始 JSON；
- 更新后的 Phase C 任务说明、holdout 任务说明和总计划。

## 禁止修改

- 正式策略、DP、资源模型、评分和跳值表；
- GUI、OCR、ADB、Airtest 和自动点击；
- Phase A/B/C 既有报告和 JSON。旧结果只标记失效，不覆盖。
- 在修正候选获得用户重新确认前，不得冻结 holdout 候选或开始采集。

## 验证

- 新增“输出(必爆)”映射失败回归测试，修复前必须失败、修复后通过。
- 增加正式分类全集不得进入 `unknown` 的测试。
- 运行 Phase B/C 定向测试、语法检查、`git diff --check` 和 `tools/run_all_tests.py`，报告真实退出码。

## 完成记录（2026-07-18）

- `system_group("输出(必爆)")` 已修复为 `pure_output`；正式分类全集测试和同音字、简繁体、全角括号的未知分类回归测试已加入。正式分类进入 `unknown` 会在研究中失败，本轮实际 `unknown=[]`。
- 新研究 schema 为 `3`，候选哈希包含正式分类映射和规则；旧 Phase C 候选分片不复用。已验证并只读复用 Phase B formal runs=10 的 `1,185` 个公共基础分片及对应 runs=10 对照分片，正式路径、跳值表、资源模型哈希一致。
- 修正后开发组为 pure_output `67`、pure_tank `68`、bruiser `21`、dual `1`。输出组合与9组纯坦控制变量均已按同胚子、同官方+3分支、同五seed重新聚合。
- Heroic基线下，全局 `12/17` 为 `69.191` 正式百里分/10万总体力；固定输出 `8/13` 且纯坦 `8/13`、`8/15`、`8/17` 分别为 `68.697`、`68.723`、`68.769`。三者总明确正效用误停概率质量均为 `0.3`，而固定输出 `8/13`、纯坦 `12/17` 为 `2.2`。这些是离线风险/效率对照，不构成发布选择。
- 产物：[修正报告](reports/epic_threshold_matrix_phase_bc_mapping_repair_20260718.md)、[原始JSON](reports/epic_threshold_matrix_phase_bc_mapping_repair_20260718.json)、[研究器](tools/research_epic_threshold_matrix_phase_c.py)、[回归测试](tests/test_research_epic_threshold_matrix_phase_c.py)。正式策略、DP、资源模型、评分、GUI、OCR、自动化和holdout均未修改或启动。
