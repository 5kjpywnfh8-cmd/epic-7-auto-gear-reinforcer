# 纯视觉采样方案设计任务说明

## 目标

在不修改当前 MuMu/Fribbels 底层读取主线的前提下，修订并设计一套“纯视觉闭环自动强化”候选方案：每个节点仅以截图/局部识别证据驱动页面确认、单次操作和结果确认，不把底层读取作为运行时必经步骤；同时明确视觉方案不能证明服务端状态和真实扣款的风险边界，以及后续实现所需的最小接口。

## 会话分工

- 本任务在新 Codex 会话中执行；当前会话继续保留 MuMu/Fribbels 底层读取、413 载荷上限诊断和新鲜快照契约主线。
- 本任务是设计与离线审阅任务；用户已明确要求纯视觉闭环，但本轮仍不把候选方案提升为正式自动强化运行时契约，也不开始代码或真实操作。
- 原“视觉预检 + 每节点底层新鲜读取”的混合结论仅作历史对照，已被本说明后文的纯视觉闭环候选口径覆盖。
- 执行模型：由新会话按项目规则选择并如实记录；当前偏好为 `gpt-5.6-luna + max`，不可用时回退 `gpt-5.6-terra + high`。

## 范围

- 设计截图采样证据协议：页面身份、目标装备唯一性、强化等级、资源账本、节点连续性和失败状态。
- 设计整屏与局部模板识别的分层策略、稳定等待、采样频率和证据保留格式。
- 设计纯视觉闭环状态机：视觉预检、单次操作、操作后稳定确认、节点推进和未知结果停止。
- 明确纯视觉资源显示只能作为计划/视觉对账证据，不能声称证明服务端真实扣款；为该模式定义显式风险标记和 fail-closed 条件。
- 审阅现有 `src/e7_enhance/visual_sampling.py` 及公开测试，提出最小接口改进建议；只有任务说明明确授权时才修改代码。
- 输出离线设计报告、风险清单、验证矩阵和后续实现拆分建议。

## 禁止修改项

- 不修改 MuMu/Fribbels 读取器、Fribbels 请求分批策略或底层新鲜快照契约。
- 不修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。
- 不连接 ADB/MuMu，不读取或上传游戏数据，不启动真实截图采样，不点击页面，不强化、不选材、不消耗资源。
- 不把历史 `current`、PCAP、私人归档或旧 Fribbels 响应当作设计输入；不修改 `refs/heads/main - 副本`。

## 权威输入

- 本文件是新会话本轮唯一权威任务来源。
- 设计输入限于现有纯视觉原型、公开测试、项目总计划及用户已给出的装备与资源参数。
- 底层读取流程以《MuMu抓包读取与真实装备导入任务说明.md》为边界参考，不在本任务中执行。

## 执行步骤

1. 完整阅读本说明、`AGENTS.md`、`e7-gear-enhance-plan.md` 和现有纯视觉原型/测试。
2. 只读梳理现有接口、证据字段、fail-closed 分支和未覆盖风险。
3. 形成纯视觉闭环状态机、局部识别分层、视觉结果闸门和最小实现拆分设计；将旧混合读取闸门降为历史对照。
4. 编写设计报告与验证矩阵；如发现必须改代码，先停在报告中，不越权实现。
5. 精确审阅产物并回写本说明和总计划，报告完成项、未完成项、风险与下一步。

## 产物

- 纯视觉采样方案设计报告。
- 证据协议/状态机/纯视觉结果闸门的离线验证矩阵，并记录与底层读取契约的不可等价点。
- 后续实现任务拆分建议（不自动下发、不执行真实操作）。

## 停止条件

- 任何需要真实截图、ADB/MuMu、网络上传、GUI 接线、页面点击或游戏资源操作时立即停止并报告。
- 若设计结论会改变底层读取契约或正式策略，必须另建任务并等待明确授权。

## 当前状态

`pure_visual_closed_loop_design_revised_offline_implementation_pending_20260726`

- 已完成纯视觉证据协议、分层识别、稳定等待、验证矩阵和最小实现拆分设计；按用户新要求，已将运行时口径修订为纯视觉闭环候选方案，旧混合读取闸门仅作历史对照；报告见 [reports/纯视觉采样方案设计_20260726.md](reports/纯视觉采样方案设计_20260726.md)。
- 定向回归基线：视觉原型 `6/6`、新鲜快照契约 `9/9`、MuMu 数据适配 `7/7`、背包配对 `6/6`；未读取私人归档，未连接设备。
- 未完成：真实截图模板/时序校准、纯视觉运行时采样器、视觉点击器接线、单节点和多节点执行；这些必须另建任务说明并取得实现/真实操作授权。
- 纯视觉模式不能证明服务端强化结果、实例 ID 连续性或真实资源扣除；未知结果、视觉不稳定、资源显示矛盾和节点跳跃必须停止且禁止自动重试。
- 当前会话继续保留底层读取主线；本任务只记录纯视觉候选运行模式，不修改新鲜快照契约、正式策略或自动化规则。
