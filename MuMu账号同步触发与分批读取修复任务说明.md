# MuMu 账号同步触发与分批读取修复任务说明

## 目标

在用户完整授权的只读范围内，定位并修复“截图导航后 Fribbels 仍返回 `0 items`”问题，最终通过一次新鲜 MuMu 读取生成同批、可校验的装备快照。读取过程中不强化、不选材、不改变装备、不导入、不消耗资源。

## 范围与授权

- 允许修改 `D:\VScode\Epic-7-tools.v1.2\readers\mumu_capture\reader.py`、`fribbels_decode.py`、`local_tools\read_mumu.py` 及公开测试；允许只读截图、局部稳定锚点识别、ADB 页面导航、抓包和向既有 Fribbels 接口上传本账号 TCP 载荷。
- 允许为诊断增加短期、带唯一前缀的非敏感日志；完成后必须移除或保留必要的结构化诊断字段。
- 不读取、恢复、暂存或上传私人归档；不修改正式策略、DP、评分、资源模型、GUI、OCR、Holdout 或自动化规则。
- 使用模型：`gpt-5.6-terra + high`。

## 权威输入

- 既有失败新鲜 PCAP：`D:\VScode\Epic-7-tools.v1.2\data\players\mumu_live\imports\20260726_115345\mumu_capture.pcap`。
- 已知成功基线 PCAP：`D:\VScode\Epic-7-tools.v1.2\data\players\mumu_live\imports\20260722_132045\mumu_capture.pcap`，仅用于脱敏结构对比，不作为本次新鲜结果。
- 既有截图入口任务、抓包读取任务和分批解码任务说明继续有效；本说明只覆盖同步触发与验证顺序。

## 假设与可证伪预测

1. 点击坐标或时机错误：目标页模板确认失败，或点击后同步窗口无新增流量。
2. 背包页面本身不触发同步：目标页模板确认成功，但 60--120 秒窗口仍低于成功基线。
3. 分组过滤误删账号同步流：原始分组存在候选而过滤后为 `0 items`。
4. API 不兼容分组边界/批次合并：已知成功基线在同一离线提取/合并路径上无法复现装备。
5. 游戏协议或网络出口变化：新鲜 PCAP 的候选端口、流量结构与成功基线显著不同。

## 执行步骤

1. 建立并运行红灯回放：同一失败 PCAP 经当前分批路径稳定得到 `0 items`；记录非敏感统计。
2. 对失败/成功 PCAP 做流、端口、时间窗、TLS 排除和 group 边界的离线差分，不发送网络。
3. 为确认的根因先写回归测试，再做最小读取器修复；不盲目增加点击次数或重试。
4. 运行公开测试、Python 3.9 语法检查和离线回放；精确审阅后才进行一次新鲜真实读取。
5. 核对同批 PCAP、Fribbels 原始响应、`reader_result.json`、`player_data.json`、snapshot、SHA-256、装备数量、实例 ID 和 `raw`；任一项失败立即停止。

## 产物

- 最小读取器修复、公开回归测试和非敏感诊断。
- 本说明、总计划和最终验证报告。
- 仅真实读取成功时生成新的标准快照；失败不覆盖历史 `current`。

## 停止条件

- 无法区分上述假设、需要扩大到强化/材料/装备变更、需要 OCR 字段采集、需要私人归档或需要新的外部服务时停止并报告。
- 新鲜读取返回 `0 items`、页面状态不明、双文件非同批、实例不唯一或 `raw` 缺失时 fail-closed。

## 当前状态

`restart_recheck_ui_not_loaded_fail_closed_20260726`

## 2026-07-26 重启后复验

- 用户人工重启 Epic Seven 后确认无异常；按当前唯一读取器执行标准只读命令时，固定命令因读取器安全校验要求显式模板与入口坐标而在抓包前退出，未产生副作用。
- 随后使用用户提供的大厅、控制界面和英雄页 PNG 模板，并沿已验证的英雄入口执行一次 240 秒读取。ADB 自动发现 `emulator-5554`，读取器启动并重启游戏，但目标页确认在 12 秒窗口内 fail-closed：相似度 `[0.619, 0.564]`、亮度比例 `[0.196, 0.870]`。
- 失败后只读截图显示仍为无界面控件的场景背景；等待 8 秒再次截图仍未加载大厅控件。未继续点击、未进入解码、未上传 TCP 载荷、未生成新 `player_data.json` 或 snapshot。
- 本次结果不改变既有结论：当前阻断是游戏 UI/账号同步未稳定加载，不能归因于 Fribbels 分批预算；停止重复读取，等待可解释的游戏同步环境变化后再建立新任务验证。

用户已完整授权处理问题，并补充批准采用局部稳定锚点识别：只要可靠确认进入背包或英雄页面即可，不要求整屏高相似度。首次局部实现的宽区域把“网络错误”弹窗误判为目标页，随后图标级相似度仍可能偏高；现已增加返回箭头/帮助图标的亮像素比例约束（目标模板期望约 `0.161/0.201`，错误弹窗仅 `0.052/0.000`），控制面板入口阈值 `0.82`，目标页颜色阈值 `0.72` 加亮度下限。正式读取固定先重启游戏。公开测试 `22/22`、Python 3.9 语法检查通过。

最新一次重启读取在目标页确认阶段得到相似度 `[0.209, 0.266]`、亮度比例 `[1.000, 1.000]`，临时截图为白屏加右上角关闭按钮；读取器已 fail-closed，没有抓包落盘、Fribbels 请求或新鲜快照。此前 `20260726_124528` 仍确认过网络错误弹窗并返回 `0 items`。用户已人工重启 Epic Seven 并确认当前无异常，环境状态发生可解释变化。后续审阅发现公告页关闭按钮位于 `(0.965, 0.065)`，旧驱动未覆盖；已加入状态感知关闭点和大厅确认早停，公开测试 `23/23` 通过。背包入口点击后触发网络错误弹窗并被拒绝；英雄入口已确认真实进入英雄页，原失败仅因复用了背包帮助图标区域，已增加英雄专用区域集合。

最新英雄入口批次 `20260726_133902` 已通过大厅、控制面板和英雄页局部识别并完成 240 秒抓包；PCAP `1,301,692` 字节，`tcp_payload_bytes=1,092,035`、`tcp_payload_groups=465`、请求 `1` 批，60--120 秒窗口仅 `1,242` 字节。Fribbels 返回 `0 items`，没有生成新 `player_data.json`、`reader_result.json` 或 snapshot；当前 `current` 仍为历史文件。导航与输入链路现已验证，剩余根因是游戏未产生可解码账号同步包，当前 fail-closed 停止。
