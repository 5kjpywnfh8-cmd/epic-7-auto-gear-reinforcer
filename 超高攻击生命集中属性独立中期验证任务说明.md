# 超高攻击/生命集中属性独立中期验证任务说明

## 状态

`ready_to_collect_user_authorization_20260721`

本文件是本轮唯一权威任务来源。v4 有效词条绑定和基线桥接均已通过，`health_p975_balanced` 仅获得离线正向信号；未完成本独立验证前不得发布或写入正式策略。

## 候选冻结

- 候选：`health_p975_balanced`，仅在基线 `stop` 的 `normal_85` `+6/+9/+12` 节点救回。
- 门槛：重铸库存生命集中 GS `P97.5=37.7363`。
- 兼容：按节点实时 selected candidate 的 `CATEGORY_RULES/sourceRow/validGroup/health_valid_keys` 判定；生命只允许抗坦、纯肉、命坦及三类半肉，未知和不兼容分类 fail closed。
- 概率：官方离散跳值状态级精确 DP，`P(终局有效生命集中 GS >= 37.7363) >= 10%`。
- 冻结哈希：候选、v4 schema/研究器、分类规则、跳值、资源模型、来源和验证器哈希必须写入不可变 manifest。

## 样本协议

- 新收集 `128` 个独立真实中期状态，固定 `64 development / 64 frozen_validation`；不读取旧 v4 开发样本、旧 `+0/+3` Holdout、旧 Oracle 或历史已强化档案。
- 每个状态必须来自可信完整 Fribbels 导出，保留稳定 instance ID、原始 `raw`、品质、等级、来源、套装、部位、主属性、全部副属性、强化等级和 roll history。
- 范围必须与 v4 一致；接受 `normal_85 Epic/Heroic` 的非速度、非鞋、`+6/+9/+12` 节点状态，并记录实际节点。节点、品质和允许分类必须分层统计；任何一层没有样本只能标记未估计。
- 同一装备在多个节点只计一个预注册状态，优先最早符合节点；实例 ID 与规范化指纹双重去重。开发/冻结分割后不可移动。
- 当前任务只允许被动采集和读取，不自动点击强化；若要取得终局标签，必须由用户明确授权验证组继续强化和重铸。

## 盲收与解盲

1. 收满128件前只记录数据质量、分层数量和进度，不记录候选动作、预测概率、人工判断或效率指标。
2. 生成不可变 `64/64` manifest 后冻结候选、代码、输入和验证闸门；冻结组在解冻前不得被研究器读取。
3. development 组只用于检查字段完整性、分层覆盖和验证器流程，不得调节门槛。
4. 用户授权后，对 frozen_validation 中每件状态继续到 +15 并进行一次重铸，记录每个节点的实际资源、终局有效生命 GS、正式终局价值、75+、22速、出售回收和转换结果。候选停止状态也必须继续，避免只留下成功样本。
5. 解盲只读取冻结组一次，并按预注册 v4 验证器计算基线与候选的成对资源动作和终局价值。

## 验证闸门

- `+6/+9/+12` 每节点、Epic/Heroic 每品质和允许体系均有预注册最小覆盖；不足时状态为 `coverage_insufficient`，不能发布。
- 候选与 v4 精确预测的分类、有效键和 `next_check_at` 一致；未知分类必须拒绝自动救回。
- frozen_validation 中正效用状态召回至少 `95%`，误停正效用概率质量为 `0`；不得出现高损失误停。
- 候选相对当前正式策略的联合正式价值差 95% 区间下界大于 `0`，并在 Heroic 产出率基线及 `±20%` 敏感性下保持为正。
- 裂缝体力 + 圣女体力每个 seed/策略等于 `100,000`；每10万报告正式百里分、生命集中新增件、原生/转换75+、22速、资源和循环。
- 以上闸门任一失败：维持当前正式策略，不发布救回；只有区间正且全部风险闸门通过才允许另建正式发布任务。

## 禁止项

- 不修改正式策略、DP、评分、资源模型、跳值、GUI、OCR、自动化或现有 Holdout。
- 不使用验证组反向调节 `37.7363`、10% 门槛、体系兼容或候选哈希。
- 不把未完成的实际强化标签用模拟结果替代，不把条件样本产量冒充账号全流程效率。
- 在用户授权继续强化前，不运行需要消耗游戏资源的验证步骤。

## 产物与完成条件

- 独立 collection/freeze/manifest、验证器、逐件 JSON、人类报告和不可变哈希记录；
- 更新本任务、`e7-gear-enhance-plan.md` 和《强化策略V1收口与GUI延期任务说明.md》；
- 定向测试、全量测试、语法检查和 `git diff --check` 通过后，由主 agent 审核是否允许正式发布任务。
