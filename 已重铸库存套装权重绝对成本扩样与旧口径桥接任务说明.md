# 已重铸库存套装权重绝对成本扩样与旧口径桥接任务说明

## 状态

`r400_numeric_run_complete_closeout_audit_incomplete`

R50正式研究已完成，但五个权重场景的绝对每点百里分体力均未达到稳定闸门。本任务保留R50原始分片，追加独立样本块，并建立旧`799.2`到当前联合线路的同口径桥接。

## 已知阻断

- R50覆盖190/190个`set×rank×seed`分片，每片50件条件合法非速度胚子。
- 库存总权重下绝对每点体力约8631，绝对区间相对半宽远大于10%，状态为`absolute_cost_unstable`，不得更新799.2或约700。
- 候选成对差值比绝对值稳定，但不能替代绝对成本闸门。
- 当前全量测试通过，但研究器尚无专用测试文件；本任务必须先补测试。

## 第一阶段：专用测试

覆盖外部源SHA、重铸过滤、19套权重和为1、固定85体力不受套装权重影响、block/seed/set/rank分片哈希、R50只读复用、权重倒数换算和非负比值区间。

## 第二阶段：旧口径桥接

使用同一随机输入分层复跑并逐层列出装备获取、Heroic产出、强化经验、金币、出售回收、转换、圣女补充和正式百里分：

1. `published_epic_replay`：复现发布`normal_85 Epic`的`sum(total_stamina)/sum(score)`。
2. `epic_inventory_weighted`：只替换库存需求权重。
3. `joint_source_epic_value_only`：加入85体力联合来源但只计Epic正式价值。
4. `joint_source_all_stop_heroic`：加入Heroic成本与回收但不强化Heroic。
5. `joint_source_released_heroic`：加入Heroic M1，形成完整联合线路。

无法复现799.2时标记`historical_constant_not_reproduced`，不得直接宣称策略导致差异。

## 第三阶段：独立扩样

- R50五个旧seed和分片不可覆盖。
- 新schema按`seed×block×set×rank`保存独立原子分片。
- 先扩至至少20个独立seed cluster、每套每品质每seed累计R200，再重新评估。
- 若绝对成本95%相对半宽仍大于10%，按R400、R800、R1600、R3200逐级追加；达到R3200仍不稳定则停止并标记`absolute_cost_not_publishable`。
- 每套等量条件抽样后再按需求权重汇总，报告ESS和最大单cluster贡献。

## 策略重新对比要求

- 扩样后必须使用R50旧分片与全部新增独立分片，重新聚合全部四套策略；不能只扩绝对成本而复用R50排序。
- 本项重跑是发布前硬要求，不是可选敏感性分析。R50虽然已经使用已重铸库存权重重聚合四套策略，但其绝对成本区间未稳定，不能把R50排序直接带入扩样后的结论。
- 主排序场景固定为`reforged_inventory_all`。765件已重铸库存中前4套占60%、前5套约66.5%、前10套约86.7%，偏门后9套合计约13.3%，按其真实低权重保留。
- `top4_equal`是常用四套主要敏感性；Epic-only和Heroic-only是品质敏感性。
- `uniform_observed_sets`只作机制对照，不得用于选择最终候选或宣称账号实际效率。
- R50中当前正式策略的`uniform_observed_sets=8643.51体力/分`、`reforged_inventory_all=8631.07体力/分`，库存加权仅改善约0.14%。因此当前绝对成本偏高不能归因于偏门套装占比；优先排查绝对方差和旧`799.2`与联合资源线的口径差异。
- 新增样本继续按`set×rank`等量条件生成，再在汇总时应用已重铸库存权重；不得按库存权重减少偏门套装的原始覆盖，否则会放大低权重套装的方差。偏门套装不删除，只在主结果中按其真实低权重计入。
- 每个场景重新比较当前正式、全局12/17、输出8/13+纯坦10/17、输出8/13+纯坦12/17，并使用共同随机路径输出成对95%区间。
- 最终候选只有在库存主权重和top4等权下方向一致、配对区间和风险指标均达标后，才提交用户确认。

## 统计与报告

- 分子分母先按独立seed/block求和，再计算总体比值，禁止平均各seed的比值。
- 绝对比值使用按seed/block聚类的非参数bootstrap或正值约束区间，不得输出负体力下界。
- 候选差异继续使用共同随机路径成对95%区间。
- 报告每10万总体力正式百里分、每点百里分体力、裂缝/圣女体力、循环数、原生/转换75+、22速和五层旧口径桥接。

## 约束与验证

- 套装权重只表示需求比例，不计算或反推自然掉率；装备获取成本固定为85体力联合来源批次。
- 策略固定为当前正式、全局12/17、输出8/13+纯坦10/17、输出8/13+纯坦12/17。
- 库存全部权重是主场景，top4等权是主要敏感性；19套等权仅作机制对照。
- 不修改正式策略、DP、资源、评分、跳值、GUI、OCR、自动化或holdout。
- 先新增专用测试，再运行定向测试、语法检查、`git diff --check`和`tools/run_all_tests.py`，报告真实退出码。

## 完成记录（2026-07-18）

- 新增专用研究器 `tools/research_reforged_inventory_set_weight_bridge.py` 与 7 项回归测试。R50 的 190 个旧分片和旧报告只读保留；新数据使用 `stage/block/seed/set/rank` 原子分片，R200 和 R400 分别保留。
- R200 的完整联合线路相对半宽为 10.8%，按停止规则继续 R400；R400 使用 20 个独立 seed/block cluster、每套每品质每 cluster 400 条条件样本，共 760/760 个分片，完整联合线路相对半宽降至 8.5%，因此不运行 R800。
- 当前正式策略的五层桥接：发布 Epic-only 回放（19套等权）612.60 [586.55, 642.11]；仅换库存需求权重 488.26 [461.68, 519.06]；同旧资源分母改用当前终局价值 591.20 [548.30, 639.86]；85体力联合来源但只计 Epic 价值 1695.71 [1565.19, 1843.75]；Heroic 全停相同；完整联合 Heroic M1 为 8564.05 [7861.09, 9312.08] 体力/正式百里分。
- 结论：套装需求权重不是巨大差异主因。库存权重影响旧口径，终局价值定义与 85体力/圣女资源边界继续抬高成本；Heroic M1 的强化资源消耗是联合线路中最大的额外成本来源。旧 799.2 未复现，状态为 `historical_constant_not_reproduced`，不得据此归因于策略变差或更新正式 DP 常量。
- 四策略在库存总权重完整联合线路下均达到绝对成本稳定闸门；共同路径成对结果中，全局12/17 相对当前策略增加 0.000088 [0.000081, 0.000095] 正式百里分/100总体力。此为离线比较，不冻结候选、不启动128件 holdout，也不修改正式策略、DP、资源、评分、GUI、OCR 或自动化。
- 最终报告与原始结果：`reports/reforged_inventory_set_weight_bridge_r400_20260718.md`、`reports/reforged_inventory_set_weight_bridge_r400_20260718.json`；R200 仅作扩样阶段记录。
- 验证：专用研究器 7 项与相关资源/Phase C 共 24 项通过；语法检查、`git diff --check` 通过；`tools/run_all_tests.py` 真实退出码 `0`、输出 `ALL TEST FILES PASSED`。一项 GUI 单例曾原生退出一次，既有隔离重试后通过，未修改 GUI。

## 主 agent 收口审计（2026-07-18）

- R400 数值与 760/760 新分片本身保留，但当前研究器的 `r50_reference` 只记录路径和只读状态，没有读取 R50 分片参与四策略最终聚合，不满足“R50 旧分片与全部新增独立分片重新聚合”的硬要求。
- 原始 JSON 已包含 `top4_equal`，其四策略方向与库存主权重一致，但 Markdown 报告遗漏该主要敏感性；终局原生75+、转换75+和22速也未进入最终 ledger 与报告。
- 因此 `8564.05` 只能称为 R400 新分片口径的稳定估计，暂不能作为完成全部预注册收口要求后的最终值；原“停止 R800”结论等待 R50+R400 合并区间复核。
- 修正任务：[已重铸库存套装权重R400收口审计修复任务说明.md](已重铸库存套装权重R400收口审计修复任务说明.md)。修复初始阶段不生成新轨迹、不运行 R800、不冻结候选或启动 holdout。

## 后续审计覆盖（2026-07-18）

- 本文的 R400 报告、`8564.05` 和“停止 R800”仅为修正前历史对照，已被 [R400收口审计修复任务说明](已重铸库存套装权重R400收口审计修复任务说明.md) 覆盖。
- 修复审计只读合并 R50+R400 后触发 R800；R800 完成后只读合并 R50+R800 的当前策略为 `7811.16 [6674.63, 9031.69]` 体力/正式百里分，相对半宽 `15.09%`，仍未达到绝对成本稳定闸门。本文不得再被用作“绝对成本已稳定”或“无需 R800”的依据。
