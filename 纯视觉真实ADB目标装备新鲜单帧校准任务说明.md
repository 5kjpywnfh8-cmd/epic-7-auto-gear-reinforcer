# 纯视觉真实 ADB 目标装备新鲜单帧校准任务说明

## 目标

在用户已打开目标装备详细页的前提下，使用一次自动发现的 MuMu ADB 截图完成右侧详情面板、命中套装图标和强化证据的只读校准。结果只允许标记 `mode=visual_only`、`verification=unverified`，不得表述为服务器确认。

## 本轮唯一权威来源

本文件是本轮唯一权威任务来源；同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md`、`纯视觉右侧装备详情面板裁剪与ADB原始帧审查任务说明.md`、`纯视觉套装与强化证据真实ADB单帧校准任务说明.md` 及现有视觉接口。

## 目标指纹

- 部位：武器；套装：命中套；品质：红装/传说；等级：85；强化：`+0`
- 主属性：攻击力 `100`
- 副属性：暴击率 `5%`、攻击力 `8%`、效果抗性 `8%`
- 官方分数：`28`

## 授权范围

- 运行一次官方 MuMu `adb.exe devices -l`，只接受自动发现且状态为 `device` 的唯一设备。
- 由该设备派生一次 `exec-out screencap -p`；PNG 只在内存中校验、裁剪和哈希，不落盘、不上传。
- 在批准的 ASCII PaddleOCR 模型缓存中执行一次右侧局部 OCR，并使用本地套装模板匹配器。
- 仅记录设备、视口、PNG 字节数、帧哈希、局部区域尺寸、OCR/模板结果和停止原因。

## 明确禁止

- 禁止 ADB `input tap`、`swipe`、`keyevent`，禁止页面导航、点击、强化、选材、重启游戏和资源消耗。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器和任何外部上传。
- 禁止第二次截图、自动重试、全屏盲 OCR、降低或绕过 `0.98` 门槛。
- 禁止读取、恢复、暂存或上传私人归档；禁止触碰 `refs/heads/main - 副本`；禁止 `git add -A`。

## 执行步骤

1. 在全新 Python 3.9.2 进程中导入 `paddle`/`paddleocr`，并确认批准 ASCII 缓存中的三类本地模型均存在；失败立即停止。
2. 运行官方 `adb.exe devices -l` 一次；设备不唯一、状态不是唯一 `device` 或命令失败立即停止。
3. 使用自动发现的设备执行一次 `exec-out screencap -p`，在内存完成 PNG、CRC、视口、非黑屏和帧哈希校验。
4. 使用右侧详情候选区和 `equipment_regions()` 做内存局部裁剪；执行一次套装模板匹配和一次 PaddleOCR 行解析。
5. 只有套装、部位、品质、等级、强化、主属性、副属性、分数全部唯一且字段置信度均 `>=0.98`，并且帧哈希不是已知旧帧时，才记录“只读校准通过”。否则 fail-closed，记录具体原因并停止。

## 验证要求

- 真实校准与 fake/offline 测试分开报告；不得把人工截图观察或旧帧结果写成真实识别通过。
- 运行与本轮代码无关的公开测试前，先精确审阅工作区 diff；测试失败、语法错误或 `git diff --check` 失败立即停止。
- 若校准通过，下一步仅建立“真实单次受控 ADB 点击接线”任务说明；本文件不执行点击。

## 停止条件

依赖/模型缺失、ADB 设备不唯一、PNG/视口/非黑屏/旧帧校验失败、区域越界、模板未达 `0.98`、强化证据缺失、任一字段低置信度或矛盾、需要第二次截图或任何输入时，立即 fail-closed 并等待用户重新确认下一次只读授权。

## 本轮只读执行记录（2026-07-26）

- 依赖闸门通过：Python `3.9.2`、`paddle 2.6.2`、`paddleocr 2.10.0`；批准 ASCII 缓存中的检测/识别/分类模型均存在。
- ADB 自动发现通过：唯一设备 `emulator-5554`，状态为 `device`；未使用固定 IP/端口。
- 只执行一次 `exec-out screencap -p`，PNG 在内存校验通过；视口 `1280x720`，字节数 `808663`，帧 SHA-256 `5da516911e79a92aad5709b0cc1ee44bcd9f4915d5063a97add7127cabd29ea8`，未命中已知旧帧集合。
- 右侧详情候选区 `(768,58)-(1242,619)`、套装区 `(819,547)-(1216,619)`、强化区 `(819,115)-(1216,180)` 均在内存裁剪；未落盘截图。
- 本地 PaddleOCR 返回 `35` 行；字段识别到部位武器、等级 `85`、主属性攻击 `100`、副属性暴击 `5%`/攻击 `8%`/效果抗性 `8%`、分数 `28`。
- 实际品质识别为“英雄武器”（`Heroic`），与本任务目标“红装/传说”（`Epic`）不一致；套装 `命中套装(0/2)` 最高置信度 `0.906965`，独立套装局部 `0.905730`，均低于 `0.98`；套装模板无唯一锚点；强化局部没有唯一 `+0` 或 `exp0/...`。
- 结果：`accepted=false`，停止原因 `target_rank_mismatch`、`low_confidence:set`、`template_anchor_missing`、`missing:enhance`；结果仍为 `mode=visual_only`、`verification=unverified`、`click_performed=false`。

## 当前状态

`readonly_fresh_frame_completed_fail_closed_waiting_target_page_confirmation`

## 模型记录

本轮执行模型：`gpt-5.6-terra + high`。
