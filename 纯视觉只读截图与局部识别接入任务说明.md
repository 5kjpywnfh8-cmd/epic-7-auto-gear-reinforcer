# 纯视觉只读截图与局部识别接入任务说明

## 目标

在已发布的惰性平台帧源之上，接入可控的 Windows 只读截图后端和局部页面识别 seam，为纯视觉强化运行时提供真实画面输入；本任务只确认页面/目标/字段状态，不执行任何游戏输入或资源操作。

## 范围

- 审阅并复用 `PlatformWindowBackend`、`PlatformFrameSource`、`VisualAdapterSampler` 和现有 OCR 只读模块。
- 新增一个可配置、惰性构造的 Windows 桌面截图后端：只定位已经存在的目标窗口、读取客户区视口并返回 PNG 字节；不得启动、激活、移动或点击窗口。
- 新增只读局部识别编排 seam：按 `ocr_regions.equipment_regions()` 提取规范化区域，允许注入模板识别/OCR 适配器，并把来源、帧哈希、视口、时间戳和字段置信度绑定到 `VisualEvidence`。
- 复用现有 OCR 解析器和 `0.98` 门槛；不得调低、绕过或重定义 OCR 门槛，不得改正式策略或新鲜快照契约。
- 补充 fake 窗口、fake 截图、稳定帧和识别失败的离线公开测试。
- 真实只读预检仅在任务文档完成并由用户明确要求执行时进行；即使执行也只允许截图/局部识别，不允许点击。

## 禁止修改与禁止操作

- 不启动、重启、控制或点击 Epic Seven、MuMu、ADB 或任何游戏窗口；不发送键盘/鼠标输入。
- 不强化、不选材、不改装备、不消耗资源，不读取或上传游戏底层数据，不调用 Fribbels 或其他外部接口。
- 不修改 MuMu/Fribbels 读取器、载荷分批、新鲜快照契约、正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。
- 不读取、恢复、暂存或上传私人归档；不修改或删除 `refs/heads/main - 副本`；不得使用 `git add -A`。

## 权威输入

- 本文件是本轮唯一任务来源。
- 设计与边界依据：`纯视觉采样方案设计任务说明.md`、`纯视觉运行时接线实现任务说明.md`、`纯视觉真实截图与窗口后端接线任务说明.md`。
- 代码依据：`src/e7_enhance/visual_adapter.py`、`src/e7_enhance/visual_runtime.py`、`src/e7_enhance/ocr_capture.py`、`src/e7_enhance/ocr_regions.py`、`src/e7_enhance/ocr_normalize.py`、`src/e7_enhance/ocr_paddle.py` 及对应公开测试。

## 执行步骤

1. 只读审阅现有平台帧源、OCR 区域/解析接口和公开测试，冻结兼容边界。
2. 先补充失败测试，再实现惰性 Windows 后端和局部识别编排；真实依赖缺失时必须构造失败而不是隐式安装或启动设备。
3. 对窗口消失、客户区变化、截图失败、非 PNG/非字节载荷、来源漂移、时间戳异常、区域越界、OCR 低置信度和字段矛盾统一 fail-closed，禁止自动重试。
4. 运行定向测试、Python 语法检查和 `git diff --check`，精确审阅 diff。
5. 将完成项、验证结果、未完成项、风险和下一步同步回本文件与 `e7-gear-enhance-plan.md`，再精确提交并推送。

## 产物

- Windows 只读截图后端及其依赖注入 seam。
- 局部区域识别/证据绑定适配及公开离线测试。
- 本文件与总计划的状态记录。

## 验证要求

- 既有视觉适配、运行时、点击器、采样和 OCR 只读测试不回归。
- 新增惰性构造、窗口定位失败、视口变化、截图失败、区域越界、OCR 低置信度和证据绑定失败测试。
- 定向测试退出码为 `0`；Python 语法检查和 `git diff --check` 通过。
- 报告必须区分 fake/offline 验证与真实只读预检；不得把截图识别写成服务端确认或强化成功。

## 停止条件

- 任何步骤需要启动/重启游戏、点击或发送输入、ADB/MuMu 控制、网络上传、底层数据读取、强化、选材或资源操作时立即停止并报告。
- 无法唯一确认目标窗口、稳定帧、局部区域、字段置信度或页面状态时 fail-closed，禁止自动重试。

## 当前状态

`implementation_verified_offline_pending_real_windows_driver`

- 已新增 `src/e7_enhance/visual_platform.py`：`WindowsCaptureConfig`、惰性 `LazyWindowsReadOnlyBackend`、只读驱动协议、局部区域提取/模板识别/OCR 注入 seam，以及 `LocalRecognitionEvidenceParser`。
- 局部证据复用 `equipment_regions()` 和现有 `parse_backpack_enhance_lines()`，继续使用 `0.98` 字段门槛；目标指纹绑定套装、部位、品质、强化等级、等级、主属性和副属性。
- 已新增 `tests/test_visual_platform.py`，覆盖惰性构造、窗口/PNG 失败、区域越界、OCR 低置信度、模板缺失和完整适配链路。
- 主 agent 串行复核：`python -B -m unittest tests.test_visual_adapter tests.test_visual_runtime tests.test_visual_click tests.test_visual_sampling tests.test_visual_platform` 为 `32/32` 通过；Python 语法检查和目标文件 `git diff --check` 通过。
- 执行模型记录为 `gpt-5.6-terra + high`。
- 仅使用 fake/offline 驱动和结构化 OCR 行；未连接或控制 MuMu/ADB，未采集真实截图，未运行真实 OCR，未执行窗口输入、游戏点击、强化、选材或资源消耗。
- 未完成项：具体 Windows 截图驱动实例化、真实只读截图、真实局部模板/OCR、页面识别和人工监督单节点验证；这些必须在后续任务中单独授权和验证。
