# MuMu新鲜读取0件装备诊断与修复任务说明

本文件是本轮“为什么读取不了，修复读取功能”的唯一权威任务说明。它仅授权离线诊断与在满足前置闸门后的最小读取器修复；根目录 `AGENTS.md`、[MuMu抓包读取与真实装备导入任务说明](MuMu抓包读取与真实装备导入任务说明.md) 和 [e7-gear-enhance-plan.md](e7-gear-enhance-plan.md) 同时适用。

## 目标

定位批次 `20260726_012048` 已抓到大量 TCP 载荷但 Fribbels 解出 `0` 件装备的可复现根因，并在先建立红灯反馈回路和回归测试后，以最小改动修复 `D:\VScode\Epic-7-tools.v1.2` 的读取/载荷处理功能。修复成功的判据是：同一类真实同步流量经读取器离线处理能产生非空、结构合格的结果；不得把旧快照或人工 JSON 冒充新读取成功。

## 范围

- 第一阶段仅离线比较失败 PCAP 与已知成功 PCAP 的非敏感结构：包/流方向、协议与端口分布、TCP 会话与重组完整性、HTTP/TLS/压缩/分帧特征、载荷分组边界、读取器过滤与提交前的计数。
- 将失败批次构造成可重复的本地回放或最小 fixture，优先在读取器的载荷提取/解码请求接口建立确定性红灯测试。
- 生成假设、逐项验证、只为区分假设添加最小且带唯一前缀的临时诊断；结论形成后删除临时诊断。
- 仅在根因已定位、红灯测试已实际运行且确认正确 seam 后，最小修改外部读取器 `D:\VScode\Epic-7-tools.v1.2`；修改后运行回归测试和原失败输入的离线回放。
- 用户已明确扩大授权：允许以最小状态感知方式确认进入大厅后，只读导航至装备或英雄页面以触发账号同步；修复后允许执行一次标准真实读取，并向既有指定 Fribbels API 上传本账号 TCP 载荷验证。

## 禁止修改项

- 本阶段不得重传任何 TCP 载荷，不得调用 Fribbels API 或其他外部服务。
- 除上述获准的大厅确认、装备/英雄页面只读导航、一次标准真实读取和既有 Fribbels API 验证外，不运行 MuMu、ADB、OCR、GUI，不点击、不重启游戏、不强化、不选材、不导入、不消耗资源。
- 即使在扩大授权下，仍禁止强化页输入、材料选择、装备变更、导入和任何资源消耗；不得执行 OCR，亦不得修改正式策略、DP、评分、资源模型、Holdout 或自动化规则。
- 不直接改写 PCAP、`player_data.json`、`reader_result.json`、snapshot 或任何真实读取输出；不得以旧 `20260722_132045` 快照作为新鲜读取成功证据。
- 不修改正式策略、DP、评分、资源模型、Holdout、GUI/OCR 门槛或自动化规则。
- 不安装 Npcap、不改系统/模拟器网络设置、不改 FribbelsE7Optimizer 安装目录；不得写死或复用 `192.168.x.x:5555` 地址。
- 根目录仓库除本任务说明、总计划、必要的公开测试/报告外不作改动；外部读取器在根因未定位和红灯测试未建立前不得修改。

## 输入与权威口径

- 失败批次：`20260726_012048`。读取器已报告 `packet_count=2969`、`tcp_payload_bytes=1100894`、`tcp_payload_groups=478`，但 Fribbels 解出 `0` 件；仅落盘 `1312188` 字节 PCAP，未生成新的 `player_data.json`、`reader_result.json` 或不可变 snapshot。
- 仓库红灯探针为 `tools/mumu_capture_sync_probe.py`：失败样本为 `759` 字节/退出码 `1`，成功批次 `20260722_132045` 为 `405198` 字节/退出码 `0`。它是同步触发是否恢复的离线前置反馈回路。
- 同批协议诊断：`udp=812`、`tcp=2144`、`ip_other=5`、`other=8`、`udp_payload_bytes=2880`；观测端口仅作结构对照，不得据此写死过滤规则。
- 成功对照仅可使用现有成功批次的非敏感统计结构和本地离线输入；原始账号载荷、实例 ID、装备字段及个人数据不得写入报告、测试名称或提交信息。
- 新鲜读取可信输出口径仍以 `D:\VScode\Epic-7-tools.v1.2\data\players\mumu_live\current\player_data.json`、同批 `imports\<时间戳>` 和 `snapshots\<时间戳>` 为准；本轮不产生新的真实读取。
- 实际执行模型：`gpt-5.6-terra + high`。

## 执行步骤

### 1. 建立离线反馈回路

1. 先读取读取器相关 `CONTEXT.md`（若存在）、本任务说明、读取器入口及其现有测试/ADR；不先凭代码阅读下结论。
2. 选择正确 seam：优先将已保存 PCAP 或经脱敏的最小载荷 fixture 回放到“PCAP 重组 -> 载荷分组 -> Fribbels 请求构建/响应规范化”的真实路径。
3. 写出并实际运行一条无人值守、数秒级、确定性的命令。它必须断言用户的精确症状：失败 fixture 的有效账号同步候选被错误遗漏或形成空解码输入，而不是只断言“程序未崩溃”。
4. 记录命令、退出码、实际失败输出和 fixture 的非敏感摘要。未获得能稳定变红的回路前，停止在本阶段，不进入假设或修复。

### 2. 复现与最小化

1. 反复运行反馈回路，确认它稳定复现 `0 items` 的实际路径。
2. 每次只移除一个会话、方向、分组或 framing 条件，缩到仍为红灯的最小输入；保留每个仍有负载的元素及其理由。
3. 对成功/失败样本仅比较元数据和结构特征，不打印、存档或上传原始载荷。

### 3. 假设与验证

1. 在任何修复前提出至少三个按概率排序、可证伪的假设。每项使用“若 X 是根因，改变/观察 Y 将导致 Z”的预测格式。
2. 将假设、预测和最小探针写入公开诊断报告并先同步总计划；用户未及时反馈时可按排序继续离线验证。
3. 每个探针只验证一个预测，优先断点/REPL，其次带唯一 `[DEBUG-<id>]` 前缀的边界日志；不得“全量打日志”。
4. 可能方向包括但不限于：TCP 重组遗漏、过滤规则过窄、协议/压缩/分帧变化、请求分组/序列化错误、解码响应处理错误。它们仅为待证伪假设，不是结论。

### 4. 最小修复与测试

1. 确认根因后，先把最小复现固化为会失败的回归测试；若不存在正确 seam，记录架构阻断并停止请求下一步授权，不用浅层测试替代。
2. 仅修改必要的外部读取器文件，保持既有命令、自动设备发现和 fail-closed 输出契约兼容；不扩大为抓包流程重写。
3. 依次运行：红灯回归测试（修复前）、同一测试（修复后）、原失败 PCAP 离线回放、相关既有定向测试、Python 3.9 语法检查和 `git diff --check`。
4. 只有离线回放验证恢复候选/非空解码输入且没有泄漏载荷时，才可将状态记为“离线修复已验证”；真实新鲜读取仍需用户另行明确授权。

## 产物

- 公开、脱敏的失败/成功结构对照与根因诊断报告，含反馈回路命令、退出码、最小化结果、假设预测和验证结果。
- 读取器侧最小回归测试及必要的最小修复（仅在前置闸门通过时）。
- 本任务说明和 [e7-gear-enhance-plan.md](e7-gear-enhance-plan.md) 的状态、证据链接、未完成项与下一步同步记录。

## 验证要求

- 反馈回路须已经实际运行，确定、快速、无人值守，且能在修复前对“0 件装备/空有效解码输入”稳定变红。
- 修复前后均报告真实命令、退出码和不含载荷的摘要；不得用“无异常退出”替代症状断言。
- 修复后回归测试和原失败 PCAP 的离线回放必须通过；临时 `[DEBUG-<id>]` 日志须删除并检索确认。
- 不运行任何真实读取和外部请求；因此本阶段不得宣称已获得新鲜 `player_data.json`、已导入、已配对或已可强化。

## 停止条件

- 无法构造实际运行过的红灯、确定性、无人值守反馈回路时，停止并报告已尝试的 seam、缺失的非敏感 fixture/日志或所需授权。
- 需重传载荷、运行 MuMu/ADB/OCR/GUI、点击、导入、强化、选材或消耗资源时立即停止，等待用户单独授权。
- 根因未定位、回归测试未红、拟议改动超出读取器最小范围，或发现需要修改正式策略/DP/评分/资源模型/Holdout/自动化时停止并另建任务。
- 离线修复完成后不得自动触发真实新鲜读取；仅记录下一步为等待用户授权并确认环境有可解释变化。

## 当前状态

`real_validation_failed_candidate_reverted_awaiting_visual_state_authorization`。实际执行模型为 `gpt-5.6-terra + high`。

离线红灯探针 `tools/mumu_capture_sync_probe.py` 已实际运行：失败 PCAP 在 `60--120` 秒 TCP 载荷仅 `759` 字节，退出码 `1`，连续 `3` 次结果一致；成功批次 `20260722_132045` 为 `405198` 字节，退出码 `0`。ACK 跨流碰撞均为 `0`；失败样本去重损失 `11014`，小于成功样本 `44761`；协议、分帧和可打印比例相近。因此 TCP 分组、ACK 跨流碰撞或协议/分帧变化不是本故障已证根因。

可证结论是：现读取器固定的登录/公告驱动不能保证触发账号同步。用户已授权状态感知地进入大厅后只读导航装备/英雄页面，或采用等价同步触发方式，并允许修复后一次标准真实读取及既有 Fribbels API 验证。仍禁止强化页输入、材料选择、装备变更、导入、资源消耗、OCR 和正式模型相关改动。

前台包确认与单次英雄入口点击候选已完成真实验证，但未恢复同步。批次 `20260726_015751` 标准命令仅运行一次，Fribbels 仍解出 `0` 件；`packet_count=2814`，`udp=802`、`tcp=2003`、`ip_other=5`、`other=4`，`tcp_payload_bytes=1083301`、`udp_payload_bytes=2856`、`tcp_payload_groups=448`，`observed_ports=[443,9997,51390,5138,80,40098,442,33538,8081,53,40118,52600]`。仅落盘 `1283593` 字节 PCAP，无新 `player_data.json`、`reader_result.json` 或 snapshot，`current` 仍为 `2026-07-22`；`tools/mumu_capture_sync_probe.py` 结果为 `814` 字节、退出码 `1`。

结论：仅凭前台包确认无法区分 Unity 内部页面，单次英雄入口点击候选真实验证失败，不能作为同步触发修复。外部候选已精确撤销：`reader.py` 已恢复原长度 `8874`，候选测试已删除，Python 3.9 内存语法检查退出码 `0`。不得继续盲点点击、自动重跑或重传 API 载荷。后续需要用户新的明确授权并建立新任务，方向仅限只读截图/模板状态识别，或由用户手动确认已到达目标页面后再触发读取。

本轮真实验证已结束且 fail-closed；不得将其称为读取恢复。等待视觉状态识别或用户手动页面确认的明确授权前，禁止继续真实读取。本文仅同步结果，未修改代码、未提交。
