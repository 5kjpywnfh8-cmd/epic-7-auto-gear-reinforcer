# 超高攻击/生命集中属性救回概率精度收口任务说明

## 状态

`v2_invalid_missing_system_compatibility_pending_v3_20260720`

本文件是概率精度阶段的历史权威记录，现已被体系兼容 v3 任务覆盖。上一轮 `probability_paths_per_state=4` 的结果只证明“当前没有发布证据”，不能作为真实 `P>=10%` 均衡档的最终淘汰依据。

## 目标

在不改变正式基线、样本池、主路径和资源口径的条件下，提高终局达标概率的分辨率，先复核最接近零的 `health_p975_balanced`。只有完成本任务的精度与回归闸门，才能决定集中属性救回是正式淘汰，还是进入后续独立验证。

## 已知问题

- 上一轮每状态仅抽 4 条终局路径，概率只能取 `0/25/50/75/100%`，因此 `P>=10%` 与 `P>0` 在所有非零状态上动作完全相同。
- 研究器 CLI 默认 `--probability-runs=32`，正式结果却使用 4；旧结果不得表述为已完成真实 balanced 候选比较。
- `_official_paths()` 是按官方离散跳值随机采样，不是概率精确枚举；报告不得再称其为“精确概率”。
- `health_p975_balanced` 的旧成对区间为 `[-0.001180,-0.000046]` 正式百里分/100体力，贴近零，最可能被概率筛选精度改变。
- 计数类指标使用未截断对称 t 区间时可能出现负下界；人类报告必须截断到 0，或明确标注为未截断统计区间。

## 固定输入与控制变量

- 正式基线固定为 `baili-formal-dp-v1-epic-balanced`。
- Epic 开发分区固定 157 件，Heroic 联合研究池固定 80 件；禁止读取 Holdout `frozen_validation`。
- 外部重铸库存及 SHA-256、生命 P97.5 阈值 `37.74`、5 个 seed、每件 10 条主路径、Heroic 产出率、裂缝/圣女资源池、评分与转换规则全部保持不变。
- 第一阶段只比较 `current_formal` 与 `health_p975_balanced (P>=10%)`；旧 `health_p975_aggressive` 只作历史对照，不参与发布。
- 旧 schema v1、p4 seed 文件和合并结果不得作为新正式结果复用；允许只读引用旧数值做差异说明。

## 实现要求

1. 优先把状态到 `+15`/重铸后的攻击与生命集中 GS 分布改为官方概率加权精确枚举。必须覆盖 Epic/Heroic、`+6/+9/+12`、Heroic `+12` 只补第四词条、合法副属性去重、官方离散跳值和一次重铸。
2. 若精确枚举经性能预检确认不可行，才允许使用分层或重要性抽样；不得退回 4 条路径。替代估计器至少要能分辨 10% 门槛，保存自然概率、proposal 概率、importance weight、ESS 和状态级概率误差。
3. 若仍使用普通独立抽样，每状态至少 32 条只能作为最低 smoke，不得自动视为正式精度；正式运行前必须证明 10% 附近动作误分类风险受控。
4. 新建 schema 与独立 resume/output 路径，保存研究器、正式策略、跳值表、资源模型、输入、候选及估计器哈希；哈希不一致时拒绝复用。
5. 保持同池配对：基线和候选必须复用同一主路径与终局分布。候选层不得改变基线路径、样本权重或资源口径。
6. 报告使用“官方概率精确枚举”或“官方离散分层估计”中的真实实现名称，不得把随机采样写成精确概率。

## 必须新增的回归测试

- 构造 `0<P<10%` 的合法状态，验证 balanced 停止而 aggressive 可救回，证明两档已可区分。
- 对至少一个小状态手工概率和研究器结果做精确对拍。
- 验证 `+12` Heroic 只补第四词条，`+15` 正常命中已有词条，重铸只计一次。
- 验证基线/候选同池配对、五 seed 合并、resume 哈希拒绝和裂缝体力加圣女体力等于 100,000。
- 验证发布闸门不允许由概率分辨率不足、单 seed 或区间跨零的结果通过。
- 验证 22速、75+及其他非负计数在人类报告中不会显示误导性的负产量下界。

## 人类报告

至少报告：

- 新旧估计器、概率分辨率、状态数、精确分支数或 ESS；
- `health_p975_balanced` 相对基线的正式价值/100体力及成对 95% 区间；
- 每 100,000 总体力的正式百里分、集中生命新增件、原生75+、转换75+、22速、裂缝/圣女体力和循环数；
- `+6/+9/+12` 首次救回数、被 10% 门槛排除的低概率状态质量、Epic/Heroic 与套装/部位覆盖；
- 与旧 p4 结果的动作差异、资源差异和结论差异；
- 绝对百里分仅条件于 157 Epic + 80 Heroic 研究池，不得与账号库存加权的全流程效率直接比较。

## 决策闸门

- 若 `health_p975_balanced - current_formal` 的五 seed 成对 95% 区间上界仍小于 0，正式淘汰本轮集中生命 P97.5 救回；其余旧候选继续保持“不发布”，并在总计划中记录不再为 V1 阻断项。
- 若区间跨 0，状态为精度已修复但效能未决；不得发布，需另建扩样任务。
- 若区间下界大于 0，才允许在新任务中扩展其他 balanced 阈值并设计独立验证；不得直接发布。
- aggressive、OR、`+0/+3` 集中属性救回、正式策略、DP、资源模型、评分、跳值、GUI、OCR、自动化和 Holdout 均禁止修改。

## 完成与停止条件

- 生成新的原始 JSON、人类报告、回归测试和总计划记录，且定向测试、`tools/run_all_tests.py`、语法检查、`git diff --check` 均通过，才算完成。
- 若精确枚举或替代估计器在性能预检中超过单分片时限，先实现原子分片与续跑；不得以旧 p4 结果代替。
- 本任务完成前，不得进入“策略覆盖与完整口径审计”，GUI/OCR 继续延期。

## 2026-07-20 执行记录（v2）

- 已建立独立研究器 `tools/research_epic_concentration_rescue_exact.py`、独立测试 `tests/test_research_epic_concentration_rescue_exact.py`、schema `2` 与独立 resume/output 路径；旧 p4 schema、resume 和结果拒绝复用。
- 概率方法固定为官方离散跳值的生命集中状态级精确概率质量 DP；终局价值使用同一批每件十条官方主路径做基线/候选配对。
- 已覆盖 Epic/Heroic、`+6/+9/+12/+15`、合法副属性去重、Heroic `+12` 仅补第四词条、一次重铸和 `P>=10%` 救回门槛。
- 小状态回归已通过，包含 `0<P<10%`、Heroic `+12`、`+15`、重铸、旧 schema/resume 拒绝。单个最重 Epic 分片约 7.5 秒，已满足原子分片续跑要求。
- 当前状态：`formal_r10_pending`。正式五 seed、157 Epic、80 Heroic、每件10条主路径尚未全部完成；未生成正式结论、未进入策略覆盖审计、未修改正式策略或 Holdout。

## 2026-07-20 正式收口

- v2 正式分片已完成 `1185/1185`：五个 seed、157 件 Epic、80 件 Heroic、每件10条主路径；续跑复核 `scheduled_shards=0` 且聚合结果一致。
- 正式报告：[epic_concentration_rescue_v2_exact_20260720.md](reports/epic_concentration_rescue_v2_exact_20260720.md)；原始 JSON：[epic_concentration_rescue_v2_exact_20260720.json](reports/epic_concentration_rescue_v2_exact_20260720.json)。
- `health_p975_balanced` 相对 `current_formal` 的五 seed 成对正式百里分差为 `-0.000665 [-0.001082, -0.000248]/100体力`，区间上界小于零，正式淘汰本候选。
- 每10万总体力：当前基线正式百里分 `84.4864`，候选 `83.8214`；候选新增生命集中件 `6.7239 [2.1059, 11.3418]`，每件约 `14872.4` 体力；裂缝+圣女体力均守恒为 `100000`。
- `0<P<10%` 状态自然质量为 `0.3504`，证明 v2 已能区分 10% 门槛；旧 p4 结果只作历史对照，不复用动作、分片或结论。
- 闸门状态：`health_p975_balanced_offline_rejected`。正式策略、DP、评分、资源模型、跳值、GUI、OCR、自动化和 Holdout 均未修改；本任务完成后仍不得直接进入策略覆盖审计，须按总计划审核是否还有其他前置项。

## 主审计补充（2026-07-20）

- 五 seed 成对正式价值区间的淘汰方向保留；该指标按同池 seed 配对，不受下述展示问题直接影响。
- 人类报告的新增生命集中件/10万体力使用第一个 seed 的资源分母统一缩放，未按各 seed 的候选总资源先归一再汇总；`6.7239` 和 `14872.4` 暂作为待复核解释性数值。
- 新增测试文件实际只有4项，未覆盖任务预注册的五 seed 合并、同池配对、资源守恒、三态发布闸门和非负展示。
- 当前唯一下一任务改为[超高攻击生命集中属性v2正式收口审计补强任务说明.md](超高攻击生命集中属性v2正式收口审计补强任务说明.md)。不得重跑正式分片或进入第三项。

## 体系兼容后续审计（2026-07-20）

- v2 研究器只导入 `_compatible`，候选 `_outcome()` 并未调用；所有基线停止状态均直接进入生命集中概率判断。
- 因输出、双效、未知等非生命体系可能被错误救回，v2 的正式价值差和淘汰结论全部降级为失效历史结果，不能通过只读重新聚合修复。
- 上述 v2 只读审计补强任务已被覆盖。当前唯一下一任务为[超高攻击生命集中属性体系兼容修复与v3重算任务说明.md](超高攻击生命集中属性体系兼容修复与v3重算任务说明.md)，必须使用新 schema/resume 重算。
