# 真实样本分歧 02：非速度主属性鞋任务说明

## 目标

修复 `+0` 紫装非速度主属性鞋在没有合法体系价值时仍被统一建议“谨慎继续”的问题。只处理鞋子早期停止边界，不修改早期赌速度、正式体系 GS 门槛、终局 75+ 概率、转换满值、跳值表或 DP。

## 权威样本

唯一复现数据源为 `manual_acceptance/real_sample_records.json`，按 `snapshot_id` 读取。

负向样本：

- `snapshot-b6abed008bef3ff6`：`+0`、普通 85、Heroic、先手套鞋，防御固定值主属性；副属性为攻击 8%、抵抗 8%、速度 4。工具为 `cautious_continue / 未来可期`，终局 75+ 概率 `0.0010`；人工为 `stop`，依据是白字主属性鞋直接放弃。
- `snapshot-639d421f7a018374`：`+0`、普通 85、Heroic、追击套鞋，生命百分比主属性；副属性为攻击 8%、速度 4、暴击率 3%。工具为 `cautious_continue / 淘汰或提取`，终局 75+ 概率 `0`，没有合格正式体系；人工为 `stop`，依据是非速度主属性紫鞋、未命中体系且分低。

必须保持的正向边界：

- `snapshot-1bf671e8bb50b263`：`+0`、普通 85、Heroic、先手套速度主属性鞋；工具与人工均为 `continue / 输出`，不得回归。
- `建议结果/07.json`：保持 `continue / 多跳速度高价值路线`。
- `建议结果/14.json`：保持 `cautious_continue / 输出候选待 +6 精确复核`。

## 已确认规则

1. 鞋子不适用“非鞋早期赌速度”路线；不得因为副属性速度 4 而自动保留。
2. 固定攻击、生命或防御主属性的 `+0` 紫装鞋属于明确停止路径。当前真实证据只覆盖普通 85 Heroic；若要推广到 Epic 或其他来源，必须先给出独立证据和测试，不得在本任务中推断。
3. 百分比非速度主属性的 `+0` 紫装鞋不能统一停止。仅当同时满足以下条件时，本任务允许输出停止：
   - 没有满足套装、部位、主属性和至少三条有效副属性要求的正式体系候选；
   - 终局 75+ 后备概率为 0；
   - 不具备其他已发布的硬继续路线。
4. 转换满值只可解释合法候选的潜力。主属性或体系不匹配、当前有效副属性不足的被拒绝候选，不能仅凭转换后理论值阻止停止。
5. 速度主属性鞋、已经命中合法正式体系的鞋、75+ 后备概率大于 0 的百分比主属性鞋，均不得被第 3 条直接停止；保持现有策略并等待更多人工样本。
6. 不得用全局 GS 阈值实现，不得修改所有紫装或所有鞋子的默认建议。

## 执行要求

1. 先复现 3 件鞋子样本，记录主属性类别、正式候选及拒绝原因、当前重铸前/后 GS、终局正式体系概率、终局 75+ 概率、转换候选和当前建议。
2. 明确当前“没有强负证据则统一谨慎继续”的代码路径，以及两个负向样本为何没有停止。
3. 先新增失败回归测试，再修改代码；测试必须直接读取真实快照，不重新手写样本属性。
4. 建立显式的鞋子早期停止诊断，debug 至少展示：鞋子路线资格、主属性类别、固定值主属性停止资格、合格正式候选数、75+ 后备概率、其他硬路线、停止条件是否全部满足及未满足原因。
5. 不修改真实样本记录中的人工结论，不改 GUI 样本存储结构，不接 OCR、ADB、Airtest 或自动点击。
6. 更新 `e7-gear-enhance-plan.md`，并新建独立修复报告到 `reports/`。
7. 运行策略与 GUI 相关测试，再运行全量测试。

## 最低验收

- `snapshot-b6abed008bef3ff6`：`stop`，debug 明确为固定值主属性紫鞋停止。
- `snapshot-639d421f7a018374`：`stop`，debug 明确为百分比非速度主属性、无合格正式候选、75+ 概率为 0 且无其他硬路线。
- `snapshot-1bf671e8bb50b263`：保持 `continue / 输出`。
- 07 保持 `continue / 多跳速度高价值路线`；14 保持 `cautious_continue / 输出候选待 +6 精确复核`。
- 未经真实样本确认的 Epic 鞋、异界鞋和“75+ 概率大于 0 的百分比鞋”不被新规则扩大处理。
- 相关测试与全量测试全部通过。

## 最终报告

- 根因与代码证据；
- 三件鞋子修复前后建议和 debug 差异；
- 固定值主属性与百分比主属性采用不同边界的原因；
- 07、14 回归结果；
- 修改文件和测试结果；
- 仍需人工采样的 Epic、其他套装/百分比主属性鞋边界。
