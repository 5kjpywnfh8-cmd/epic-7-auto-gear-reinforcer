# 超高攻击/生命集中属性有效词条绑定修复与 v4 重算任务说明

## 状态

`completed_v4_positive_pending_baseline_bridge_20260720`

本文件是本轮唯一权威任务来源。v3 已在节点执行体系兼容布尔过滤，但返回的 `health_valid_keys` 没有传入精确概率 DP 或终局命中判断；v3 正向结果因此失效。

## v4 执行记录

- 研究器：[research_epic_concentration_rescue_exact.py](tools/research_epic_concentration_rescue_exact.py)；study `epic_concentration_rescue_v4_bound_health_20260720`；schema `4`；全新 resume `reports/epic_concentration_rescue_v4_bound_health_resume_20260720`。
- 正式分片：`1185/1185`，保持 `157` 件 Epic、`80` 件 Heroic、5 个 seed、每件 10 条主路径和联合资源口径；v2/v3 分片、区间、动作和独立验证资格均未复用。
- 每个救回节点按 selected candidate 的 exact `CATEGORY_RULES`、`sourceRow`、`setGroup`、`validGroup` 绑定 `health_valid_keys`；同一上下文进入初始值、官方概率 DP、剪枝上界、缓存键、Heroic +12 新词条和终局命中。
- 半肉(血防)/半肉(通用) 不认可的 `hpFlat` 不计集中 GS，但仍保留在 `all_keys` 中占用命中槽位；v3 因该缺陷产生的额外救回质量为 `0.001901 [0.001785, 0.002017]`/联合批次。
- 五 seed 成对正式价值差 95% 区间为 `[0.000031, 0.001715]` 百里分/100体力，状态为 `health_p975_balanced_positive_requires_independent_validation`。按本轮边界不设计独立验证、不进入第三项审计、不修改正式策略。
- 每 100,000 总体力候选正式百里分为 `117.2315 [60.3235, 174.1394]`，新增生命集中装备 `1.9997 [0.6424, 3.3570]` 件；固定研究池条件值，不代表账号全装备效率。
- 每个 seed 裂缝体力 + 圣女体力 = `100,000`；零新增续跑为 `scheduled_shards=0` 且汇总一致。
- 报告：[v4 人类报告](reports/epic_concentration_rescue_v4_bound_health_20260720.md)；原始数据：[v4 JSON](reports/epic_concentration_rescue_v4_bound_health_20260720.json)。
- 验证：v4 专用回归 `15` 项通过；`tools/run_all_tests.py` 真实退出码为 `0` 并输出 `ALL TEST FILES PASSED`；语法检查、`git diff --check` 和资源不变量检查均通过。

## 根因

- `health_compatibility_from_snapshot()` 能得到当前正式分类认可的 `hpPct/hpFlat` 集合。
- `_outcome()` 随后仍调用不接收有效键集合的 `exact_health_probability(state, threshold, cache)`。
- `_health_state()`、精确分布、剪枝上界及终局 `_concentration_value(..., "health")` 固定统计全部 `hpPct+hpFlat`。
- 因此 `半肉(血防)`、`半肉(通用)` 等不认可 `hpFlat` 的体系，仍可能被白字生命跨过 P97.5 门槛。现有测试只断言返回列表不含 `hpFlat`，没有验证概率与终局命中真正忽略它。

## 目标

把同一个 selected candidate 的正式规则、有效生命词条、精确概率和终局命中绑定为一个不可拆分的评估上下文，使用 schema 4 和全新 resume 重算 `health_p975_balanced`。

## 规则绑定

1. 在每个 `+6/+9/+12` 节点读取当前 `selected_candidate_snapshot(state)`。
2. 按 exact category 从 `CATEGORY_RULES` 唯一定位正式规则，并校验 `sourceRow`；从该规则的 `validGroup` 读取 `VALID_STATS`。不得把名称含混的 `candidate.set_group` 直接当作权威 `validGroup`。
3. 生命允许/拒绝分类保持 v3 契约不变；未知、重复或 sourceRow 不一致时 fail closed。
4. 当前规则认可的 `health_valid_keys` 必须同时传入初始集中值、未来状态 DP、Heroic +12 新词条、剪枝上界、缓存键、终局命中和报告。
5. 不认可的生命词条不贡献集中 GS，但仍作为装备已有副属性保留在命中槽位中，正确稀释未来命中概率。
6. 攻击集中使用相同的规则绑定帮助函数并增加契约测试，但不运行攻击正式研究。

## 固定控制变量

- 正式基线、生命 P97.5 阈值 `37.7363`、157 Epic、80 Heroic、5 seed、每件10条主路径、官方概率、Heroic +12、一次重铸和联合资源池保持不变。
- 只比较 `current_formal` 与 `health_p975_balanced (P>=10%)`。
- 不读取 Holdout，不扩展其他阈值、OR、aggressive 或 `+0/+3`。

## 隔离与重算

- 使用 schema 4、新 study、新 resume 和新哈希；v2/v3 分片、动作、差值、汇总和结论均不得复用。
- 可以复用相同 seed 形成控制变量，但必须生成新的1,185个正式分片。
- 报告逐分类列出 v3 因无效生命词条而多救回的质量、v4 实际救回质量和正式价值差。
- 继续逐 seed 先换算每10万总体力，再汇总新增集中装备及区间。

## 必须测试

- `半肉(血防)` 与 `半肉(通用)` 的高 `hpFlat`、低 `hpPct` 状态不能靠 `hpFlat` 达标；`半肉(白字)`、纯坦体系可按规则计入 `hpFlat`。
- 同一 Gear、不同 `health_valid_keys` 的精确概率和缓存结果必须不同，缓存不得串用。
- 不认可的生命词条仍占命中槽位并稀释目标概率。
- selected category、`CATEGORY_RULES`、sourceRow、validGroup 必须绑定一致；未知或不一致 fail closed。
- 概率 DP、剪枝版与小状态手工枚举对拍；终局命中使用同一有效键集合。
- v3 schema/resume 拒绝；v4 零新增续跑、五 seed 同池、逐 seed 资源归一、十万体力守恒、非负展示和三态 gate。

## 决策闸门

- 成对95%区间上界小于0：淘汰 v4 候选。
- 区间跨0：效能未决，另建扩样任务。
- 区间下界大于0：才允许建立独立验证任务，不得直接发布。

## 禁止项与完成条件

- 不修改正式策略、DP、评分、资源模型、正式跳值、GUI、OCR、自动化或 Holdout。
- 不进入第三项策略覆盖审计，不设计验证集，不并行研究攻击候选。
- 完成 v4 JSON、报告、1,185个新分片、回归测试及所有文档同步，并通过定向/全量测试、语法检查和 `git diff --check` 后交由主 agent 审核。

## 主审计补充（2026-07-20）

- v4 有效词条绑定、精确概率 DP 和15项回归通过；正向成对区间暂保留为离线信号。
- 发现基线漂移：v2 `current_formal=84.4864`，v3/v4 `current_formal=116.3588`。v4 任务未解释该差异，不能直接进入独立验证。
- 当前唯一下一任务为[超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md](超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md)。
