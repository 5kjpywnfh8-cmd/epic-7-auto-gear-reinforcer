# Epic 非速度 +3 前瞻样本采集任务说明

> 状态更新（2026-07-14）：旧真实 `+0` 装备已出售，无法补采对应 `+3`。本采集流程保留为未来运行时实测复核，但不再阻断当前策略研究。当前研究改用 [Epic非速度+3官方概率精确分支研究任务说明.md](Epic非速度+3官方概率精确分支研究任务说明.md) 的“真实 +0 + 官方概率精确分支”口径。

## 目标

为 `normal_85 Epic` 非速度早期策略提供长期真实 `+3` 观测，用于复核官方概率精确分支模型；它不再承担解除当前研究阻断的职责。

本任务只建立样本采集、配对、去重、冻结和进度报告机制；不运行 DP Oracle、不调整候选门槛、不运行五 seed 联合资源池、不创建新的 48 件策略验证批次，也不修改正式 Epic 策略。

## 当前阻断

- 已排除旧 Oracle、旧盲测、旧 `0/48` 批次及其候选哈希和 resume。
- 可用独立真实样本中，`+0` 已有开发 174 件、冻结 99 件。
- `+3` 开发和冻结样本均为 0，不能评估分节点规则。
- 旧样本、合成状态和已经强化至 `+6` 以上的状态不得回填该缺口。

## 收录范围

只收录同时满足以下条件的装备：

- `normal_85`；
- `Epic`；
- 非鞋；
- `+0` 初始速度不存在或小于 2；
- 由当前正式策略真实强化到 `+3`；
- 在下一次策略判断前保存 `+3` 快照；
- 来源为真实游戏装备，不是条件生成或模拟轨迹。

必须按实际到达顺序连续收录所有合格装备，不得只选择高分、低分、争议或人工关注的装备。

## 快照与配对字段

同一装备应关联保存：

- `+0` 强化前快照；
- `+3` 强化完成、下一次策略判断前快照；
- 实例 ID 和规范化指纹；
- 套装、部位、主属性及全部副属性；
- `+3` 命中的词条和实际增量；
- 可取得的副属性命中历史或重铸计数；
- `gear_source`；
- 当前正式策略版本和规则哈希；
- 采集时间；
- `sample_source=prospective_runtime`；
- `collected_blind=true`。

无法可靠关联 `+0` 与 `+3` 时，必须记录排除原因，不得作为有效样本。

## 采集实现约束

优先复用现有真实样本导入、导出或记录接口。若现有接口不足，只允许增加隔离且可关闭的被动 JSON 记录器：

- 不改变强化或停止动作；
- 不改变 GUI 布局和主流程；
- 不触发额外点击；
- 不接入或修改 OCR、ADB、Airtest；
- 原子写入并支持中断续收；
- 按实例 ID 和规范化指纹双重去重；
- 不读取旧 Oracle、旧盲测、旧批次或旧候选预测。

## 样本量与冻结

- 进度里程碑：64 件有效 `+3` 快照；该数量只用于显示采集进度，仍不得运行 Oracle。
- 最低冻结验证量：同一预先固定分割下至少 24 件。
- 推荐目标：128 件，以提高开发和冻结验证的稳定性；推荐开发 64 件、冻结验证 64 件。
- 达到 128 件后必须先生成一次性 manifest；只有 manifest 中的 64 件开发组允许进入 Oracle 初筛。
- manifest 中的 64 件冻结验证组在候选规则和预测哈希冻结前不得读取 Oracle、预测或人工标签。
- 分割算法、随机种子或哈希规则必须在读取 Oracle 和候选预测前冻结。

生成不可变 manifest，至少包含：

- 样本 ID；
- 开发或冻结分组；
- 策略与规则哈希；
- 数据文件 SHA256；
- 纳入或排除原因。

manifest 冻结后不得移动样本分组，不得根据研究结果替换样本。

## 进度报告

提供可复跑命令并报告：

- 已发现的候选数量；
- 有效 `+0/+3` 配对数量；
- 重复数量；
- 缺少命中历史或无法配对的数量；
- 不符合范围的数量及原因；
- 距离 64 件最低量和 128 件推荐量还差多少。

未收满 128 件并生成 manifest 时只报告采集进度，不运行 Oracle、五 seed 联合研究或每十万总体力指标。

## 验证要求

新增最小回归测试，覆盖：

- `+0/+3` 正确关联；
- 范围过滤；
- 实例 ID 与规范化指纹双重去重；
- 原子写入和中断续收；
- 固定哈希分割；
- 旧样本不会混入；
- 采集器不会改变正式策略动作。

完成后报告修改文件、当前有效样本进度、测试结果和仍无法可靠采集的字段。不得把“采集工具完成”写成“策略研究完成”。
