# Heroic 中后期 22 速差异分层与指标修正任务说明

更新日期：2026-07-13

## 目标

在 `tools/research_heroic_midgame_speed22.py` 已通过方法 smoke 的基础上，先修正三个统计/命名偏差，再实现“基础回退 stop 且候选可救回”的自然概率差异分层，完成正式多 seed 研究。

本任务只修改离线研究器、对应测试、报告、独立 resume 和计划。不得修改正式策略、跳值表、lambda、评分、DP、资源默认值、GUI、OCR、ADB、Airtest、自动点击或前瞻批次。

## 当前可保留部分

- `+0/+3`、Epic 和正式 Heroic 基础回退均已冻结；候选只在 Heroic `+6/+9/+12` 基线 stop 时可能救回。
- P22 精确枚举已覆盖 Heroic 速度跳值、+12补词条、+15强化和一次重铸。
- 完整联合资源池和十万总体力账本 smoke 守恒。
- 候选、套装范围和测试骨架已建立。
- smoke 出现 +6/+9 基线止损，说明正式基准路径不再是第一版错误的全冲+15。

这些部分不得无理由重写。

## 必须先修正的偏差

### 1. M1 不是速度效率 Oracle

当前 `M1_reachable_22/all_sets` 救回全部 `P22>0` 状态，只能称为“每件胚子的可达性救回上界”。它没有比较额外资源消耗、圣女补充增加和来源循环减少，不能保证最大化 `Heroic 22+/100,000总体力`。

修正要求：

- 将 M1 显示名改为 `reachability_rescue` 或“可达性全救回参考”；
- 删除或改名当前 `speed_oracle_gap_22`，不得把候选与M1的差叫作速度效率Oracle差距；
- 若实现真正效率Oracle，必须在动作中考虑后续22概率、节点净资源成本及固定总预算下循环数变化，并单独证明；否则不输出Oracle。

### 2. “每多1件22+额外总体力”公式无效

当前所有候选的 `ledger.total_stamina` 都按定义归一为100,000，候选间相减恒为0。该值不能代表边际成本。

修正后同时报告：

```text
平均总体力/件Heroic22 = 100000 / Heroic22件数
每新增1件Heroic22的圣女体力变化 = (候选圣女体力 - M0圣女体力) / Heroic22增量
每新增1件Heroic22减少的维度裂缝体力 = (M0维度裂缝体力 - 候选维度裂缝体力) / Heroic22增量
每新增1件Heroic22减少的来源循环 = (M0循环数 - 候选循环数) / Heroic22增量
每新增1件Heroic22的正式GS变化 = (候选GS - M0GS) / Heroic22增量
```

增量<=0时显示“不适用”。不得使用固定总预算之差。

### 3. 200事件闸门必须使用原始独立事件

当前 `_row_per100k()` 已把 `rescued_22_events` 换算成每十万体力值，`_summarize()` 再跨 seed 求和并命名为 `candidate_unique_rescued_22_events`。这不是原始事件数，也没有唯一轨迹证明，不能用于 `>=200` 闸门。

修正要求：

- 每个分片保存原始 `raw_rescued_22_count`；
- 保存可去重的 `trajectory_id/seed/stratum/chunk/path_index`，或至少用不重叠原子分片保证唯一；
- `weighted_rescued_22_per_100k` 与原始计数分开；
- gate 使用原始独立事件数和加权有效样本量，禁止使用归一化产量；
- resume 重跑不得重复计数。

## 扩样前可行状态审计

### 紫装 +9/+12 状态修正（最高优先级）

用户确认并由强化事件规则直接推出：Heroic `+12` 只补第4条副属性，不强化已有速度。因此：

- 若 `+12` 是14速、速度 `rolls=4`，则 `+9` 必须已经是14速、`rolls=4`；
- 这表示初始速度 `rolls=1`，且 `+3/+6/+9` 三次强化全部命中速度；
- `+12` 状态必须有4条副属性，新增第4条不能是已有的速度；
- `roll_history` 必须完整记录三次速度命中及各自合法跳值，不能只记录一次；
- 不能直接手工构造一个 `+12` 三词条对象并称为合法可达 witness。

当前 v3 第一阶段 `enumerate_legal_rescue_states()` 使用的 `+12/14速/rolls=4/3条副属性/仅一次速度历史` witness 无效。保留指标修正，但撤回“合法差异状态已确认”。先增加从合法 `+0` 装备逐事件构造完整 path 的验证器：

1. 在 +0 创建合法 Heroic 三词条装备且包含初始速度；
2. 依次应用 +3/+6/+9 的真实命中目标和合法跳值；
3. +12 只添加合法且不重复的第4条；
4. 同时保存 +9 和 +12 状态，断言两者速度值和速度 rolls 相同；
5. 逐节点执行 M0 与候选，确认该路径实际能到达被审计节点。

如果 M0 在 +9 已经 stop，而候选在 +9 救回，则 +12 只属于候选后续状态，不能称为“M0自然到达的+12救回”。终局22+事件按首次动作分歧节点归因一次：

- `first_rescue_node`：首次从M0 stop变为candidate continue的节点；
- `later_rescue_nodes`：候选路径后续再次遇到的救回节点，仅作过程诊断；
- 一件终局22+只能计入一个首次救回节点，禁止在+9和+12重复计两次贡献。

在正式长跑前，先确定性/条件生成合法 `+6/+9/+12` 状态，证明真实存在：

- normal_85 Heroic；
- 非鞋且已有速度；
- `baili_marginal_low` 当前返回 stop；
- `P22>0`；
- 至少一个候选返回 continue；
- 该状态可由合法 +0 装备和真实强化事件到达。

按套装、节点、当前速度、速度 rolls、副属性数输出状态数量和示例。如果某节点不存在合法差异状态，应从候选研究中删除该节点，而不是等待随机样本。

## 自然概率差异分层

当前 `_simulate_shard()` 仍是完整池均匀随机抽样，没有实现任务要求的差异 strata。新增独立的分层路径，保留完整池 smoke 作为对照。

### 分层键

至少包括：

- initial mode；
- 套装；
- 部位与主属性；
- 初始是否含速度及初始速度值；
- 到达当前节点前的速度命中模式和速度跳值；
- 当前节点 `+6/+9/+12`；
- 当前速度与速度 rolls；
- 当前副属性数；
- 基础回退动作；
- 候选救回集合。

### 权重

每个 stratum 保存自然概率分解：

```text
部位概率
* 主属性概率
* 合法副属性包含速度的概率
* 初始速度值概率
* 历史命中目标概率
* 历史跳值概率
* 其他被条件化事件概率
```

- 副属性类型使用STOVE合法候选 `1/n`；
- 鞋速度主属性不得生成速度副属性；
- Heroic强化速度使用确认离散概率；
- 初始速度分别保留`legacy_initial_uniform`和`roll_distribution_as_initial_proxy`，后者只作代理；
- strata权重之和与完整自然池一致；被排除的公共状态必须在公共基准层保留，不能丢失其资源、出售和正式GS贡献。

### 抽样

- 对“基线stop且至少一个候选救回”的差异 strata 过采样；
- 对候选共享的公共状态低采样或复用同一估计；
- 所有候选使用公共未来轨迹；
- 分层估计为 `sum(stratum_weight * conditional_mean)`；
- 报告原始样本数、原始救回22数、加权贡献和有效样本量ESS；
- 置信区间使用独立seed的分层估计或分层bootstrap，不能把重要性样本当普通同分布二项样本。

## 事件和统计闸门

每个进入正式候选比较的策略必须同时满足：

- 至少200个原始、唯一、由该候选救回后产生的 Heroic22+终局；
- 加权ESS至少100；
- 至少5个独立seed都有非零差异贡献；
- 相对M0的Heroic22+/十万总体力成对95%区间可计算；
- 结果给出相对半宽；
- 不能把同一轨迹在不同scope/候选中的共享成功重复称为独立事件。

如果 reachable 参考能达到200但严格候选不能，严格候选保持样本不足，不得借用参考候选事件过闸。

## 候选与训练隔离

- 保留冻结的P22网格、P22/体力网格、命中链、外部速度参考和两种scope；不得根据正式验证seed临时增加阈值。
- 开发seed用于筛选Pareto候选；只有冻结后才能使用新的holdout seed验证。
- 报告所有网格，但只有开发集Pareto前沿进入holdout。
- 初始值两种口径若改变Pareto候选，状态为`initial_distribution_blocked`。

## 正式报告

正式报告不得继续用全0 smoke表作为策略结论。第一页用中文展示：

- 哪些+6/+9/+12合法状态会被救回，各占自然装备池多少；
- 每个候选原始独立救回22事件数和加权ESS；
- M0与候选每十万总体力的Heroic22、联合22、循环数、维度裂缝/圣女体力；
- 每多1件Heroic22的圣女体力、损失循环和正式GS；
- 原生正式GS、满值转换可达GS、实际策略GS；
- 速度套/两件套与全套装范围差异；
- 两种初始值口径和稀有1速端点敏感性；
- 是否进入holdout或仍被阻断。

M1只能标为“可达性全救回参考”。若没有真正资源效率Oracle，明确写“未实现”，不得继续称其为Oracle。

## 回归测试

先补失败测试：

1. M1报告名不包含效率Oracle，`speed_oracle_gap_22`不再错误生成；
2. 固定100,000预算下边际体力公式不使用`total_stamina`相减；
3. 原始事件数与每十万加权产量严格分离；
4. 两个分片各1个事件时raw count=2，不能因归一化变成其他数；
5. 重复resume不重复轨迹ID或原始事件；
6. 至少一个真实合法+6/+9/+12差异状态被检测器识别；
7. Heroic +12 必须为4条副属性；三词条+12 witness被拒绝；
8. +12补词条前后速度值和speed rolls与+9一致；
9. speed rolls=4时强化历史包含+3/+6/+9三次合法速度命中；
10. M0在+9停止时，该路径的+12状态不得标记为M0自然到达；
11. 同一终局22+按first_rescue_node只归因一次，later_rescue_nodes不重复计产量；
12. 不可由合法历史到达的人工状态被拒绝；
13. stratum权重分解和总和正确；
14. 重要性抽样加权结果与大样本直接完整池在宽容差内一致；
15. ESS计算正确，单个超大权重样本不能伪装成200有效样本；
16. 公共状态仍进入联合资源和GS账本；
17. 候选共享轨迹只在各候选内部计一次；
18. 开发/holdout seed与resume目录隔离；
19. 新schema拒绝smoke v1分片；
20. 修正后的每十万体力账本继续守恒。

## 测试与Qt归因

实现完成后运行：

- 新增定向测试；
- 相关速度概率、Heroic回退、资源、评分、转换测试；
- `tools/run_all_tests.py`；
- 语法检查；
- `git diff --check`。

本轮 smoke 的全量入口被既有 `tests/test_gui_app.py` Qt原生失败中断。续作先把该文件隔离运行两次并保存日志，再重跑统一入口；不得因此修改GUI业务代码。若统一入口仍失败，报告退出码和最后成功测试文件，不能宣称全量通过，也不能通过发布闸门。

## 允许修改范围

优先限定：

- `tools/research_heroic_midgame_speed22.py`
- `tests/test_research_heroic_midgame_speed22.py`
- 新的正式研究JSON/Markdown和独立resume目录
- 必要的测试日志
- `e7-gear-enhance-plan.md`

不得修改正式代码和任何既有策略配置。完成后报告修改文件、指标修正前后差异、strata权重证据、事件数/ESS、正式结果、测试结果和剩余阻断项。
