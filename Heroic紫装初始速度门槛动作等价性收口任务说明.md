# Heroic 紫装初始速度门槛动作等价性收口任务说明

更新日期：2026-07-13

## 目标

停止继续扩大 H2/H3 `+0` 初始速度门槛样本，先证明并记录它们在当前自动强化二元动作下是否与 H4 等价。若等价，关闭本轮门槛研究，不再要求每候选 200 个 Heroic 22+事件；只有定义了真正改变强化/停止动作的新策略后，才另立稀有事件研究。

本任务仍只修改离线研究器、测试、报告和计划。不得修改正式策略、跳值表、lambda、评分、DP、资源模型、GUI 或前瞻批次。

## 已有证据

1. `candidate_action()` 只在 Heroic、非鞋、带速度、`+0` 初始 2/3 速返回研究覆盖；`+3` 及以后始终返回 `released`。
2. 正式 `early_speed_gamble_status()` 的初始门槛只参与 `+0`；`+3` 的 `continue_route` 只要求速度 `rolls>=2`，没有保存或检查初始门槛资格。
3. 自动研究路径把 `continue` 和 `cautious_continue` 都解释为“强化到下一复核节点”。当前普通早期回退使 H4 的非硬路线装备同样到达 `+3/+6`。
4. v2 smoke 中 H4、H3/H2速度套两件套、H3/H2全套装的循环数、圣女体力、到达+6、+6停止和22+完全相同。

因此，当前问题不是 Heroic 22+样本太少，而是候选没有改变实际二元动作。机械扩样只会重复相同策略。

## 执行要求

### 1. 动作等价性穷举

使用确定性状态矩阵，不依赖随机稀有事件，至少覆盖：

- Heroic 初始速度 `1/2/3/4`；
- 速度套、一个确认两件套、一个其他四件套、弱化套；
- 非鞋与鞋；
- `+3` 命中/未命中速度；命中速度增量 `1/2/3/4`；
- 普通体系回退分别会继续和停止的装备状态；
- `+6/+9/+12` 正式后续节点。

对每个状态分别输出：

- 速度路线标签；
- 前台建议 `continue/cautious_continue/stop`；
- 自动强化实际二元动作 `enhance/stop`；
- H4、H3、H2是否产生动作差异；
- 差异发生在哪个节点及原因。

### 2. 收口判断

若 H2/H3 相对 H4 只有标签差异，没有任何二元动作差异：

- 将研究状态写为 `no_binary_action_difference_under_current_policy`；
- 不运行每候选 200 个 Heroic 22+事件；
- 不输出虚假的 H2/H3 22+效率排序；
- 明确说明降低 `+0` 门槛本身不会改变当前自动强化产量或资源消耗。

若发现极少数真实动作差异：

- 先列出完整状态和自然权重；
- 只有差异会影响终局路径时才继续分层模拟；
- 200事件要求改为针对“差异 strata 的有效 Heroic 22+事件”，不能统计候选共享的相同事件凑数。

### 3. 转换可达 GS 口径补正

当前 `_terminal_metrics()` 只把“原生未正式命中、转换后正式命中”标为 conversion；这不足以支撑“满值转换后可达正式 GS”列。

离线报告应对每件到达 +15 的装备独立计算一次合法满值转换，包括原生已经正式命中的装备：

- `native_formal_gs`：原生普通正式体系；
- `conversion_reachable_formal_gs`：合法满值转换后可达到的最佳普通正式体系，只有提高时采用；
- `strategy_formal_gs`：实际策略是否转换并承担10万金币后的结果。

可达值只是敏感性，不自动产生转换成本。三列每件都只能计一次。未知、一速和无普通正式分类继续单列，禁止写入半肉兜底。

## 失败测试

先增加：

1. 完整状态矩阵中 H2/H3/H4 的前台标签可不同，但自动二元动作相同时被识别为等价；
2. v2 smoke 的五个主候选在相同公共轨迹下资源流和节点动作完全一致时，gate 返回 `no_binary_action_difference_under_current_policy`；
3. gate 为动作等价时禁止调度稀有事件正式扩样；
4. 原生已正式命中但合法满值转换进一步提高正式GS时，`conversion_reachable_formal_gs > native_formal_gs`；
5. 可达转换不产生10万金币，只有实际策略选择转换才产生；
6. 一速/未知分类不计入半肉；
7. 若构造一个真正改变 +3/+6 二元动作的研究候选，等价性检测必须能识别差异，避免检测器恒为等价。

## 报告要求

生成简短的人类可读收口报告，先回答：

- 为什么降低 H4 到 H3/H2 在当前自动强化中没有改变实际路线；
- “继续”和“谨慎继续”在前台含义不同，但自动强化为何都只前进一个节点；
- v2 smoke 五个候选的动作、资源和产量为何完全相同；
- 是否需要继续200事件扩样；
- 下一项真正有意义的研究应改变哪个节点。

若确认等价，推荐下一研究方向限定为以下二选一，不能在本任务直接实现：

1. `+6/+9/+12` 的 Heroic 22速可达性/速度命中路线；
2. 收紧普通 `+3` 回退后，由速度硬路线救回命中速度的装备。

第一项更直接对应22速最高优先级；第二项会同时改变非速度早期策略，范围更大。

## 验证

运行新增定向测试、相关速度/策略/资源/转换测试、`tools/run_all_tests.py`、语法检查和 `git diff --check`。更新 `e7-gear-enhance-plan.md`。不得创建前瞻批次。
