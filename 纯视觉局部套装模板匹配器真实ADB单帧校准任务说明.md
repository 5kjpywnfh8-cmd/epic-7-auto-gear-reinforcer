# 纯视觉局部套装模板匹配器真实 ADB 单帧校准任务说明

## 目标

在用户已打开目标装备详细页的前提下，使用一次真实 MuMu ADB 内存截图验证离线 `LocalSetIconTemplateRecognizer` 是否能从真实右侧套装局部区域产生唯一、高置信度的视觉候选。该结果只用于校准视觉候选，不代表字段或服务器确认；输出继续使用 `mode=visual_only`、`verification=unverified`。

## 唯一权威来源与输入

本文件是本轮唯一权威任务来源。必须完整遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md`、`纯视觉 ADB 局部模板与 PaddleOCR 只读识别任务说明.md`、`纯视觉套装局部裁剪与强化证据锚点修正任务说明.md`、`纯视觉套装图标模板补充任务说明.md` 和 `纯视觉局部套装模板匹配器任务说明.md`。

输入仅限：MuMu 官方 ADB 自动发现的唯一 `device`、一次 `exec-out screencap -p` 返回的 PNG 字节、本地已校验套装模板清单、现有局部区域定义和内存中的解析结果。不得把用户人工描述或旧截图当作本次证据。

## 授权范围

- 运行 MuMu 官方 `adb.exe devices -l`，只接受自动发现且状态为 `device` 的唯一设备；不写死、复用或主动传入任何 `192.168.x.x:5555` 地址。
- 仅对自动发现的序列执行一次 `exec-out screencap -p`；PNG 只在内存中校验、裁剪和解析，不落盘、不上传。
- 对右侧详情、`set_anchor`/`set` 和 `enhance` 局部区域运行一次本地模板匹配/已配置 PaddleOCR 解析，用于记录候选、置信度和 fail-closed 原因。

## 禁止修改与操作边界

- 禁止第二次截图、自动重试、ADB `input tap`/`swipe`/`keyevent`、页面导航、点击、强化、选材、重启、资源消耗、底层 Fribbels/PCAP/TCP 读取和任何外部上传。
- 禁止保存私人截图、局部裁剪、OCR 原始图像或游戏数据；只记录非图像元数据、哈希、尺寸、候选和拒绝原因。
- 不降低或绕过字段 `0.98` 门槛，不把低置信度套装、缺失 `+0`/`exp0/...` 或人工推断转为接受结果。
- 不修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛、ADB 设备发现、帧源、点击执行器或自动化规则；不触碰 `refs/heads/main - 副本`，不使用 `git add -A`。

## 执行步骤

1. 确认本地 Python/PaddleOCR 模型依赖已存在；缺失立即停止，不执行 ADB。
2. 执行 MuMu 官方 `adb.exe devices -l`，验证唯一 `device`；数量为零、多于一个或状态异常立即停止，不重试。
3. 执行一次 `exec-out screencap -p`，在内存中校验 PNG 签名、CRC、非黑屏、视口、字节数和 SHA-256；任一失败立即停止。
4. 使用当前已确认的右侧详情区域及 `set_anchor`/`set`、`enhance` 区域做一次内存裁剪；调用本地 `LocalSetIconTemplateRecognizer` 和现有局部 OCR 解析。任何越界、视口漂移、模板缺失、低分、歧义或字段矛盾均 fail-closed。
5. 只记录设备状态、帧元数据、区域尺寸、候选标识、分数/阈值、OCR 字段置信度、接受/拒绝原因和禁止操作核对结果；同步本文件和总计划。
6. 运行必要的离线定向测试、语法检查和 `git diff --check`，精确审阅变更后只暂存本任务文件与总计划，提交并推送当前分支。

## 产物

- 本文件的真实单帧元数据、匹配结果、拒绝原因和验证记录。
- `e7-gear-enhance-plan.md` 的同步状态。
- 不产生截图、裁剪图、OCR 图像或游戏数据文件。

## 验证要求

- 设备发现必须是唯一自动发现的 `device`；截图命令必须恰好执行一次。
- 所有 PNG/区域/模板输入通过既有结构校验；模板命中必须达到固定 `0.98`，并且唯一、无位置歧义。
- OCR 字段仍逐字段满足 `0.98`；缺失强化证据不得推断 `+0`。
- 报告真实退出码，区分模板命中、字段通过、fail-closed 和未执行项；不得宣称服务端确认或强化授权。

## 停止条件

依赖缺失、设备不唯一、PNG 损坏/黑屏/视口漂移、截图次数不符、区域越界、模板缺失/低分/歧义、OCR 低于门槛、字段缺失或任何禁止操作被触发时立即停止，不重试，不进入点击执行器或自动强化。

## 当前状态

`real_adb_set_template_matcher_single_frame_fail_closed_20260726`

执行模型：`gpt-5.6-terra + high`。

## 完成与验证记录（2026-07-26）

- 依赖导入通过：全新 Python 3.9 进程可导入 `paddle 2.6.2`、`paddleocr 2.10.0`；MuMu 官方 ADB 自动发现结果为唯一 `emulator-5554 device`。
- 仅执行一次 `exec-out screencap -p`，PNG 全程在内存中校验和裁剪：视口 `1280x720`，字节数 `808714`，SHA-256 `105fe23ed7dd4b1b828566eec5f362969cebab0339c152c5c00b3a1e54f0dd5f`；未落盘、未上传、未执行第二次截图。
- 套装局部区域为 `(819,547)-(1216,619)`、裁剪尺寸 `397x72`；`LocalSetIconTemplateRecognizer` 返回空锚点，未达到唯一 `0.98` 匹配阈值，不能把真实帧认定为命中套装模板。
- 本地 PaddleOCR 共返回 `35` 行。目标字段中英雄武器、85 级、攻击力 `100`、暴击率 `5%`、攻击力 `8%`、效果抗性 `8%`、装备分数 `28` 均达到 `0.98`；套装 `HitSet` 置信度为 `0.906965`（独立套装局部 `0.905730`），强化局部未识别 `+0` 或 `exp0/...`。
- 解析结果为 `accepted=false`，拒绝原因：`low_confidence:set`、`missing:enhance`。结果固定为 `mode=visual_only`、`verification=unverified`、`click_performed=false`，不得用人工描述补齐。
- PaddleOCR 初始化日志显示模型实际下载到默认 `C:\Users\orangine\.paddleocr`，未按预期完全命中 `D:\VScode\cultivation\e7_ocr_cache`；本轮不重试、不删除该缓存，后续需单独修复缓存路由并取得相应授权。
- 离线验证：局部模板/视觉定向测试 `52/52`、OCR 回归 `27/27`、`py_compile` 和 `git diff --check` 通过；未执行 ADB 输入、页面导航、点击、强化、选材、资源消耗、底层读取或外部上传。

## 未完成项与下一步

- 真实套装图标尚未达到唯一模板命中；强化证据仍缺少显式 `+0`/`exp0/...`。下一步应另建离线任务评估模板预处理/区域锚点与显式强化等级来源，先完成离线验证，再申请新的真实只读校准。
- 在套装和强化两项均通过固定门槛前，不得进入页面导航、受控点击或自动强化。
