# Heroic 中后期 22 速 v7 方差分配与正式扩样任务说明

## 目标

在 v6 互斥速度签名估计器已通过守恒验证的基础上，冻结候选等价组与独立分层分配，使用全新 seed 完成正式离线扩样。v6 pilot 仅用于设计分配，不并入正式估计。本任务仍不得修改正式强化策略、跳值表、lambda、评分、DP、GUI 或前瞻批次。

## 已确认的 v6 pilot 证据

- 2,500个互斥速度签名自然质量合计 `0.275`，与非鞋初始含速度装备的自然质量一致。
- `M1_reachable_22/all_sets` 的 `Heroic22+ - M0` 与 `weighted_rescued_22_per_100k` 均为 `0.1862738996`。
- 21个终局22+签名中，单seed、每签名1条时观察到14个候选救回22+；全部候选救回为41条，其中27条终局未达22，失败资源路径没有被删除。
- 当前正贡献ESS=14、最大单样本占比7.14%，只能说明最小pilot不足以发布，不能说明估计器失效或候选无价值。

## 第一阶段：候选动作等价性收口

正式扩样前先增加确定性动作矩阵，覆盖：

- `normal_85 Heroic` 合法非鞋 +6/+9/+12状态；
- 速度套、确认两件套及其他套装范围；
- 当前速度、速度rolls、最后一次是否命中速度；
- 精确P22、下一节点资源成本及基础回退 `continue/stop`；
- 稀有1速端点启用/移除。

对每个候选比较完整 `enhance/stop` 序列，而不是只比较pilot汇总值。预期验证以下等价组，但不得未经穷举直接写死：

- G1：`M1_reachable_22`、`M2_p22_0_5pct`、`M2_p22_1pct`、`M2_p22_2pct`、`M2_p22_5pct`、`M4_speed_hit_chain`、`M5_external_speed_reference`；
- G2：`M2_p22_10pct`、`M3_p22_per_stamina_0_002`；
- G0：`M0_current`、`M3_p22_per_stamina_0_005`、`M3_p22_per_stamina_0_01`。

仅当全矩阵动作完全一致时，正式模拟才允许每组只运行一个代表，其他名称作为 alias 输出。若存在任何差异状态，必须保存最小 witness 并拆开研究。

## 第二阶段：冻结 v7 分层分配

升级独立 schema/study/resume；拒绝读取 v3--v6 分片。分配必须在读取正式seed结果前冻结。

1. 保留v6的互斥速度签名及精确自然质量。
2. 对每个候选等价组，确定哪些签名在所有合法条件装备下增量恒为0；只有经过确定性证明的零差异签名才可从定向层排除。
3. 终局速度达到22+且候选可能救回的签名作为正产量层，提高每seed条件样本数。
4. 终局未达22但候选可能救回的签名作为失败成本层，必须继续抽样，不能因不产22+而删除。
5. 同一签名内自然抽取套装、部位、主属性、其余副属性、非速度跳值和+12合法第四词条。
6. 每个正式seed使用同一冻结配额；pilot seed `20260723` 及其分片不得进入正式汇总。

建议先以5个全新seed运行 allocation smoke。正产量层应至少保证每个签名每seed有多条条件样本；实际条数由达到原始救回事件、正贡献ESS和最大贡献占比的联合目标反推，不能仅设置总runs后按自然质量分配。

## 第三阶段：公共联合资源池

v6公共池每seed仅64条，不能用于正式完整线路报告。v7必须独立扩大公共自然池，并保持：

- 同一来源循环只计一次85体力；
- 每循环期望1 Epic、3.569929 Heroic、0.425下级石和127,500金币；
- 圣女3-7只补金币/经验瓶颈；
- Epic沿用正式路线，Heroic M0沿用基础回退；
- 定向层只添加候选相对M0的加权增量。

公共池规模以来源循环、圣女体力占比和M0正式GS的逐seed稳定性决定。至少5个独立seed；若关键账本相对95%半宽仍过大，应增加公共池而不是增加速度签名样本。

## 第四阶段：一致性与发布闸门

每个seed及合并结果必须断言：

```text
candidate Heroic22+ - M0 Heroic22+
== weighted_rescued_22_per_100k
```

并验证所有候选增量的强化经验、金币、出售回收、转换成本、正式GS及节点计数使用同一签名质量。

候选进入人工确认必须同时满足：

- 原始唯一救回22事件 `>=200`；
- 正贡献ESS `>=100`；
- 至少5个seed均有非零贡献；
- 最大单样本贡献 `<=5%`；
- Heroic22+相对M0的成对95%区间可计算且不跨0；
- 初始值合法均匀与强化概率代理两个模型分别通过；
- 资源、GS及22+主指标无单seed支配。

未达到闸门时只能报告继续离线或无效候选，不创建前瞻批次。达到后也只进入人工确认，不直接发布策略。

## 人类可读报告

主报告按每 `100,000` 总体力显示：

- 维度裂缝体力、次数、来源循环；
- 圣女3-7体力和次数；
- Heroic 22--27+产量；
- 各正式体系百里分与总百里分；
- 原生75+、满值转换可达75+、实际承担转换后的策略价值；
- 每新增1件22+所需圣女体力、损失的来源循环和正式GS；
- 每个候选等价组的代表、别名和最小差异witness；
- 五个seed逐项结果与95%区间。

22速继续单列，不折算进正式体系百里分。

## 验证

- 先运行新增候选等价矩阵、分配冻结、零差异签名排除和权重守恒测试。
- 再运行研究器、速度、策略、资源与联合账本相关测试。
- 运行语法检查和 `git diff --check`。
- 最后运行 `tools/run_all_tests.py`；既有GUI导入失败单独记录，不借本任务修改GUI。
- 不清理或覆盖用户现有未提交改动。
