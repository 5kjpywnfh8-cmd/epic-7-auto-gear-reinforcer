# 22速研究口径复核与修正任务说明

## 目标

修正《22速优先强化路线重校准任务说明.md》首次重跑中的现行基准、品质门槛组合和获取成本口径，再判断是否存在可进入独立前瞻验证的速度路线。本任务只修改离线研究器、研究测试与报告；不得修改正式策略、正式跳值表、lambda、评分、GUI或现有前瞻批次。

## 必须保留的正确结果

- normal_85 Epic强化速度概率：2/3/4各33.223%，5为0.332%，无1。
- normal_85 Heroic强化速度概率：1为0.332%，2/3/4各33.223%，无5。
- rift_85 Epic使用独立异界表，不得与normal_85合并。
- 旧0.7%分片失效；不得重新读取或合并。
- 稀有端点移除敏感性可以保留，但不能替代主概率模型。

## 已确认偏差

1. `speed_current`在研究器中对Epic/Heroic统一使用初始门槛2；正式当前规则实际为normal/rift Epic=2、normal Heroic=4。
2. 联合汇总只组合相同候选名，迫使Epic/Heroic使用相同`start_2/3/4`，没有评估品质独立门槛组合。
3. 研究样本只包含已带速度的非鞋装备，却直接计入完整85体力批次获取成本，遗漏非鞋、初始带速度和初始速度值的出现概率。
4. normal/rift Epic速度>=2是已冻结的项目权威硬路线，不能被`start_4`候选覆盖。
5. 全量测试没有取得完整成功码。

## 修正规则

### 真实当前基准

冻结一个唯一`speed_current_exact`：

- normal_85 Epic与rift_85 Epic：+0非鞋速度>=2继续到+3；
- normal_85 Heroic：+0非鞋速度>=4继续到+3；
- +3命中速度继续到+6；
- +3未命中退出速度保护并交给正式普通策略；
- +6以后交回当前正式策略。

测试必须直接断言三个来源/品质的+0门槛和+3/+6行为，不能只断言候选元数据。

### 品质独立候选

- normal/rift Epic的初始门槛2固定，不把3/4作为可发布候选；允许作为敏感性，但报告必须标明违反当前权威硬路线。
- Heroic可研究2/3/4，但当前对照固定为4。
- 联合候选必须显式命名品质组合，例如`reachable_epic2_heroic3`，不能按相同字符串交集隐式配对。
- normal Epic、normal Heroic、rift Epic分别排序；联合排序不得覆盖分来源结论。

### 两种成本口径

1. 条件边际强化效率：对象是已经获得、非鞋且带速度的真实胚子。排除装备获取成本，只计算从当前节点继续的金币、粉末、下级石、出售回收和圣女瓶颈。报告每100件速度胚子的22速数量、增量体力、每件22速的增量体力及正式价值损失。
2. 完整装备池效率：只有同时纳入无速度装备及其固定非速度策略，或者使用有证据的自然生成权重，才允许加入85体力来源成本。不得把51/53件库存筛选比例当自然掉率。若初始速度值概率仍无权威证据，只给参数化敏感性，不输出“真实副本每100体力产量”。

两个口径必须分别命名、分别排序，禁止在同一个分母中混合。

## 执行步骤

1. 先新增失败测试，复现Heroic当前门槛被错误设为2、联合候选强制同门槛、条件速度样本错误计入完整获取成本。
2. 最小修改离线研究器；不改正式模块。
3. 使用现有官方概率分片时，只有轨迹本身与候选动作完全解耦才可只读复用；任何包含旧动作或错误资源汇总的分片必须新建resume目录重算。
4. 分别输出normal Epic、normal Heroic、rift Epic和品质独立联合组合。
5. 报告绝对22速数量、22/23/24/25/27+、各节点到达/停止率、增量资源、正式体系价值、75+与输出60损失。
6. 对稀有端点保留/移除做相同轨迹成对比较。
7. 运行定向测试、`tools/run_all_tests.py`、语法检查和`git diff --check`；全量测试没有成功码时不得宣布完成。
8. 更新`e7-gear-enhance-plan.md`，明确旧报告哪些数字保留、哪些失效。

## 发布闸门

只有同时满足以下条件，才能创建新的速度前瞻验证批次：

- 相对`speed_current_exact`的22速边际效率成对95%区间不跨0；
- normal/rift Epic速度>=2硬路线未被削弱；
- Heroic门槛变化有独立分项收益，不靠错误联合权重获胜；
- 正式价值、75+和输出60的牺牲已量化并符合用户的速度第一优先级；
- 条件效率与完整装备池效率没有混用；
- 全量测试成功。

最终报告必须给出：根因、修正前后当前基准、品质独立候选、两种成本口径、分来源与联合结果、被否决候选、测试结果和是否达到前瞻验证闸门。
