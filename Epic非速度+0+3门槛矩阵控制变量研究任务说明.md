# Epic 非速度 +0/+3 门槛矩阵控制变量研究任务说明

## 目标

比较 `+0` 与 `+3` 有效 GS 止损门槛的间隔是否影响资源效率，重点验证用户提出的均衡组合 `+0 GS<=12、+3 GS<=17`。

本任务只做离线候选研究和新独立 `+0` 验证准备，不修改正式 Epic 策略、DP、资源、跳值、评分、GUI 或自动化流程。

## 为什么必须做矩阵

`+0`门槛会改变哪些装备能够继续到`+3`，因此：

- 固定 `+0`、只改变 `+3`，才能测量 `+3`门槛本身的边际效果；
- 改变 `+0`后再测 `+3`，得到的是完整路线效果，不能解释为单一节点效果；
- 只测试一组 `12/17`无法判断收益来自`+0`、`+3`还是两者联动。

## 冻结条件

只研究：

- `normal_85 Epic`；
- 非鞋；
- 初始速度 `<2`，已排除速度硬路线；
- `+0/+3`安全止损；
- `current_valid_max=2`；
- `+0`终局概率门槛固定 `0.002`；
- `+3`终局概率门槛固定 `0.01`；
- 转换改善门槛固定 `0`；
- STOVE官方离散跳值精确分支；
- `+6`以后正式 Epic DP；
- Heroic M1作为联合资源池共享组件。

不得在本任务同时改变有效词条、终局概率、转换价值、分类、部位或资源参数。

## 阈值矩阵

主矩阵预注册为：

- `T0 ∈ {10, 12, 14}`；
- `T3 ∈ {14, 16, 17, 18, 20}`；
- 优先报告满足 `T3 >= T0 + 4` 的11组，包括 `(12,17)`。

固定+0的单节点敏感性：

- `T0=12`，比较 `T3=16/17/18/20`，直接观察用户建议的`17`相对相邻门槛。

固定+3的单节点敏感性：

- `T3=18`，比较 `T0=10/12/14`，隔离提高`+0`门槛的影响。

完整路线矩阵：

- 每组都重新按该组 `T0`决定哪些状态进入`+3`；
- 再按该组 `T3`决定`+3`停止；
- 不得把固定 `T0=10`下的`+3`结果直接当成所有组合的`+3`结果。

## 数据与隔离

- 当前开发157件、冻结92件已用于上一安全候选；冻结92件已被读取，不能作为新矩阵的独立发布验证集。
- 矩阵筛选只使用开发组和同一真实胚子分布的离线精确分支。
- 矩阵及风险档位冻结后，收集新的真实 `normal_85 Epic` 非鞋 `+0` 快照作为独立 holdout；不要求保留或强化到`+3`。
- 新 holdout 至少48件，建议64件；导出后即使装备出售也不影响验证。
- 未来运行时真实`+0/+3`配对仍只作长期实测复核，不与本轮模型分支混合。

## 统一计算方式

所有矩阵组合使用：

- 同一批真实`+0`起点；
- 同一组官方`+3`概率分支；
- 同一五 seed；
- 同一 Heroic产出基线及±20%敏感性；
- 同一显式维度裂缝/圣女3-7资源池；
- 成对共同随机路径。

每组报告：

- `+0`停止率、+3停止率；
- 明确正效用误停、正效用召回、regret；
- 下一节点节省体力；
- 正式体系百里分、原生75+、转换75+、22+、输出60；
- 每10万总体力的维度裂缝体力、圣女3-7体力和循环数；
- 相对当前正式策略的成对95%区间。

## 风险档位

- 保守档：沿用当前 `10/14`，明确正效用误停必须为0；
- 均衡档：优先评估 `12/17`，但不得因节省体力自动发布，需由用户确认可接受的误停风险；
- 激进档：仅作敏感性，不进入默认策略，除非用户另行确认风险预算。

新矩阵不能复用上一候选的发布闸门结论。每个拟发布档位都必须在新的 `+0` holdout 上重新验证；若用户选择零误停，则必须满足误停0和召回100%。

## 验收

- 矩阵在读取新 holdout 前冻结；
- `(12,17)`有完整单节点和端到端结果；
- 固定+0与固定+3的控制变量结果分开；
- 所有组合使用共同随机路径，不能比较不同 seed 的独立噪声；
- 报告明确“模型精确+3分支”而非真实观测+3样本；
- 未通过新 holdout 或用户风险确认前，不修改正式策略。
