# 强化策略 V1 收口与 GUI 延期任务说明

## 用户决定

先完成强化策略 V1，再继续开发 GUI、OCR、ADB 或 Airtest 自动化。现有 PySide6 GUI 只作为手工输入、debug、样本管理和验收工具保留；不删除、不重构，也不新增自动化能力。

## “策略完成”的定义

策略不要求永远不再更新，而是达到一个可冻结、可验证、可供 OCR 调用的 V1 契约。以下项目全部完成后，才视为策略 V1 收口：

1. `output_8_13_tank_10_17` 的 128 件 Holdout 完成 development/frozen_validation 隔离验证，明确发布或淘汰。
2. Epic/Heroic、速度/非速度、鞋/非鞋、`+0/+3/+6/+9/+12` 的实际二元动作均有明确归属，不存在“谨慎继续”被自动端错误解释为跨节点继续。
3. 超高攻击/生命集中属性研究完成，明确是发布救回、仅 debug 提示还是淘汰。
4. 新套装全力/弱化及现有套装的体系映射有回归覆盖；未知套装必须拒绝自动决策。
5. 每 100,000 总体力报告至少分开正式百里分、22速、75+、裂缝体力、圣女体力和循环；条件池结果不得冒充账号全装备效率。
6. 当前正式策略版本、评分、跳值、资源模型、转换规则、`next_check_at` 和适用范围形成只写一次的 V1 冻结清单与哈希。
7. 建立完整回归矩阵，覆盖 normal_85 Epic/Heroic、rift_85 Epic、全部强化节点、鞋子、速度硬路线、M1、停止回收和转换边界。

## 执行顺序

### 第一项：完成 128 件 Holdout 验证

- 先审计 freeze/collection/manifest 哈希和 64/64 隔离。
- 对 development 64 件运行 `+0` Oracle、官方 `+3` 精确分支和联合资源池。
- 不使用 development 结果调参；候选规则保持冻结。
- 再一次性读取 frozen_validation 64 件，按预注册闸门给出通过或失败。

#### 验证执行状态（2026-07-20）

- 128 件已按冻结的 `64/64` manifest 完成隔离审计；候选、关键哈希、正式 DP、官方 `+3` 分支、五 seed、`runs=10` 和 Heroic M1 口径均保持冻结。
- 首次正式长跑已完成 development 分区的 320 个五 seed流量分片，但在第二分区完成前达到外层时限；续跑同样在整批分片返回前超时，未生成最终 JSON 或报告。
- 阻断定位为验证器等待一个分区全部作业返回后才写分片，无法充分复用已完成工作；不是闸门失败，也没有读取人工标签或反向调参。
- 续跑进一步确认逐件 `+0/+3` 状态计算也可能超过单次外层时限；允许的修复范围扩展为逐件状态和流量作业的原子缓存与续跑调度。不得改变候选、策略、DP、资源模型、评分、输入分区或预注册闸门。
- 联合汇总时发现 Phase B formal `runs=10` 的 400 个 Heroic M1 共享分片完整，但其 `input_hash` 使用正式研究报告记录的历史 Phase B 代码哈希；当前源码哈希已更新。仅允许按 formal 报告中的冻结哈希重算并校验这些只读分片，不得放宽 schema/候选/seed/rank/索引校验，也不得重写 Phase B 缓存。
- 只读缓存通过后，资源汇总暴露验证器字段映射错误：资源模型权威字段为 `expected_lower_stone_units`，不是 `expected_source_lower_stone_units`。仅修正验证器字段名，资源模型和联合批次口径不变。
- 首份验证报告已生成并通过全部闸门，但人类表格误展示了每100体力率且缺少22速/75+列；仅修正报告展示为每10万总体力，不改变 JSON 或闸门计算。

#### Holdout 完成记录（2026-07-20）

- 128 件已按不可变 manifest 完成 `development 64 / frozen_validation 64` 隔离验证；逐件 JSON 和人类报告已生成。
- frozen_validation 的 `+0` 明确正效用召回为 `100%`，官方 `+3` 概率加权明确正效用召回为 `100%`，纯输出 `+0` 明确正效用误停为 `0`；候选 regret 不高于当前正式策略，无高损失误停。
- 五 seed 联合资源池在 Heroic 产出基线及 `-20%/+20%` 敏感性下，候选相对当前正式价值区间均为正；8 个预注册闸门全部通过。
- 状态为 `validation_passed_pending_user_release`：仅允许等待用户确认是否发布候选；确认前不修改正式 Epic 策略。若用户确认，必须另建发布任务说明并执行哈希/回归冻结。
- 报告：[Holdout 报告](reports/epic_output_8_13_tank_10_17_holdout_validation_20260719.md)；逐件 JSON：[Holdout JSON](reports/epic_output_8_13_tank_10_17_holdout_validation_20260719.json)；验证器：[validate_epic_balanced_holdout.py](tools/validate_epic_balanced_holdout.py)。

#### 正式发布完成（2026-07-20）

- 用户已确认发布，`output_8_13_tank_10_17` 已写入正式 `normal_85 Epic` 非速度 `+0/+3` 路径。
- 正式版本：`baili-formal-dp-v1-epic-balanced`；4,610 个冻结状态动作对拍一致，全量测试真实退出码 0。
- 第一项已完成。当前最先执行的任务切换为第二项“超高攻击/生命集中属性研究”；GUI/OCR 继续延期。
- 发布报告：[epic_output_8_13_tank_10_17_release_20260720.md](reports/epic_output_8_13_tank_10_17_release_20260720.md)。

### 第二项：集中属性研究

- 完成超高攻击/生命 P90/P95/P97.5 阈值与 `+6/+9/+12` 救回研究。
- 只有总体价值和资源区间通过才发布；否则只保留 debug。
- p4 初筛已完成且不支持发布，但 25% 概率粒度使 `P>=10%` 与 `P>0` 完全等价，不能据此正式淘汰均衡档。
- 当时下一任务为[超高攻击生命集中属性救回概率精度收口任务说明.md](超高攻击生命集中属性救回概率精度收口任务说明.md)，现已被后文覆盖。

#### 概率精度收口执行状态（2026-07-20）

- 已切换至唯一权威任务说明《超高攻击生命集中属性救回概率精度收口任务说明.md》及其 v2 独立研究器。
- v2 小状态对拍和原子分片性能预检已通过；正式五 seed 聚合仍待完成。完成前继续冻结正式策略、Holdout、GUI/OCR/自动化，并不得进入第三项覆盖审计。

#### 概率精度收口完成（2026-07-20）

- v2 已完成五 seed、157 Epic、80 Heroic、每件10条主路径及联合资源池；`health_p975_balanced` 成对 95% 区间上界小于零，正式淘汰，不进入发布或 Holdout。
- 本轮没有修改正式策略、DP、评分、资源模型、跳值、GUI、OCR、自动化或 Holdout。第二项已完成，第三项策略覆盖审计是否启动仍由总计划的后续前置条件决定；GUI/OCR 继续延期。
- 报告：[epic_concentration_rescue_v2_exact_20260720.md](reports/epic_concentration_rescue_v2_exact_20260720.md)；任务说明：[超高攻击生命集中属性救回概率精度收口任务说明.md](超高攻击生命集中属性救回概率精度收口任务说明.md)。
- 主审计保留正式价值区间为负的淘汰方向，但发现每十万集中装备产量使用首 seed 资源分母统一缩放，且预注册的聚合/守恒/闸门测试未补齐。第二项暂为 `pending_v2_closure_audit`。
- 当时下一任务为[超高攻击生命集中属性v2正式收口审计补强任务说明.md](超高攻击生命集中属性v2正式收口审计补强任务说明.md)，现已被后文覆盖。
- 后续体系审计发现 v2 未实际调用 `_compatible`，生命救回混入非生命体系；v2 淘汰结论及上述只读补强任务均被覆盖。第二项状态改为 `pending_system_compatible_v3_rerun`。
- 当时下一任务为[超高攻击生命集中属性体系兼容修复与v3重算任务说明.md](超高攻击生命集中属性体系兼容修复与v3重算任务说明.md)，现已被后文覆盖。

#### 体系兼容 v3 重算执行状态（2026-07-20）

- v3 已完成 `1185/1185` 新 schema 分片：`157` 件 Epic、`80` 件 Heroic、5 个 seed、每件 10 条主路径；v2 分片和正式结论未复用。
- v3 在每个 `+6/+9/+12` 节点按当前 selected candidate 的正式分类和 `validGroup` 做生命体系兼容过滤，再运行官方概率精确 DP；`hpFlat` 只在所选规则认可时计入。
- 候选相对 `current_formal` 的五 seed 成对 95% 区间为 `[0.000111, 0.001693]` 百里分/100体力，区间下界大于 0。状态更新为 `health_p975_balanced_positive_requires_independent_validation`：只能设计独立验证，不得直接发布或修改正式策略。
- v2 的“正式淘汰”结论已被 v3 覆盖，仅保留为失效历史对照。第三项策略覆盖审计仍未启动，GUI/OCR/自动化继续延期。
- 报告：[v3 报告](reports/epic_concentration_rescue_v3_system_compatible_20260720.md)；任务说明：[超高攻击生命集中属性体系兼容修复与v3重算任务说明.md](超高攻击生命集中属性体系兼容修复与v3重算任务说明.md)；原始结果：[v3 JSON](reports/epic_concentration_rescue_v3_system_compatible_20260720.json)。

#### 有效词条绑定 v4 重算执行状态（2026-07-20）

- v3 的正向结果因 `health_valid_keys` 未进入 DP、剪枝、缓存和终局命中而失效；v4 已用 schema 4、新 study 和新 resume 完成 `1185/1185` 分片。
- v4 将 selected candidate 的 `CATEGORY_RULES/sourceRow/setGroup/validGroup` 与有效生命词条完整绑定；无效 `hpFlat` 只占命中槽位，不贡献生命集中 GS。v3/v4 旧分片、区间和独立验证资格不复用。
- v4 相对 `current_formal` 的五 seed 成对 95% 区间为 `[0.000031, 0.001715]` 百里分/100体力，仍为正。当前只记录离线结果，按本轮约束不设计独立验证、不进入第三项策略覆盖审计、不修改正式策略。
- 报告：[v4 报告](reports/epic_concentration_rescue_v4_bound_health_20260720.md)；任务说明：[超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md](超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md)；原始结果：[v4 JSON](reports/epic_concentration_rescue_v4_bound_health_20260720.json)。
- 主审计发现 v3 只用 `health_valid_keys` 做兼容布尔判断，概率 DP 与终局命中仍统计全部 `hpPct+hpFlat`；v3 正向结果和独立验证资格被覆盖。第二项状态改为 `pending_valid_stat_bound_v4_rerun`。
- 当时下一任务为[超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md](超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md)，现已被后文覆盖。
- v4 已通过有效词条绑定与15项回归，但发现 v2 基线84.4864与v3/v4基线116.3588未解释漂移。第二项状态改为 `pending_v4_baseline_bridge`，当前唯一下一任务为[超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md](超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md)。
- v4 只完成生命 P97.5 集中门槛；攻击集中门槛尚未正式研究，现有攻击内容仅为兼容规则契约，不得当作攻击候选结果或发布依据。基线桥接完成前不得启动攻击研究。

### 第三项：策略覆盖与完整口径审计

- 逐品质、来源、部位、节点列出实际动作归属。
- 补齐速度路线、鞋子与非速度路线的条件结果，明确哪些绝对效率仍为 `not_estimated`。
- 不为追求一个虚假的账号总数而混合缺少自然权重的来源。

### 第四项：冻结策略 V1

- 输出人类可读全流程规则表、机器可读策略清单、关键哈希和回归结果。
- 冻结后 OCR 只调用统一 `advise_gear` 契约，不自行复制门槛或分类。

## GUI 与自动化边界

- 暂停《OCR自动强化实施路线与第一阶段只读识别任务说明.md》的执行。
- 不启用现有禁用的 OCR/Airtest 按钮。
- 不安装 OCR、Airtest 或新的 GUI 依赖。
- 只允许修复阻断策略测试或样本验证的 GUI 测试基础设施问题，不做业务界面扩展。

## 完成产物

- Holdout 最终报告与原始 JSON。
- 超高攻击/生命研究报告。
- 策略覆盖矩阵和全流程人类可读规则表。
- 策略 V1 冻结 JSON、关键哈希和回归测试报告。
- 更新后的总计划；下一阶段才恢复 OCR 只读识别任务。

#### v4 基线漂移桥接收口（2026-07-21）

- 基线审计已完成：[报告](reports/epic_concentration_rescue_v4_baseline_drift_audit_20260721.md)、[原始 JSON](reports/epic_concentration_rescue_v4_baseline_drift_audit_20260721.json)、[任务说明](超高攻击生命集中属性v4基线漂移审计与独立验证前置任务说明.md)。
- 根因已确认：v2 Epic `+3` 分支循环只保留单个分支，五 seed 加权路径质量为 `40.245236/785`；v3/v4 完整覆盖为 `785/785`，Heroic 三版均为 `400/400`。因此 `84.4864` 仅作失效历史对照，`116.3588` 才是完整分支基线。
- 固定 gear/seed 三模式重放证明 baseline-only 与不可救回候选完全相同，v4 只有在救回动作改变时才改变 flow；v3/v4 `current_formal` 逐 seed 一致，资源守恒通过。
- 状态更新为 `baseline_bridge_complete_no_independent_validation`：允许未来另建独立验证任务，但本轮未建立验证集、未修改正式策略、未进入第三项覆盖审计；v4 正向区间仍不得直接发布。

#### 独立中期验证准备（2026-07-21）

- 已建立新的唯一权威任务说明：[超高攻击生命集中属性独立中期验证任务说明.md](超高攻击生命集中属性独立中期验证任务说明.md)，不复用 v4 开发样本、旧 Holdout、旧 Oracle 或历史已强化档案。
- 验证目标为 `health_p975_balanced`，固定 `128` 件真实中期状态并隔离为 `64 development / 64 frozen_validation`；范围为 `normal_85 Epic/Heroic`、非鞋、非速度、`+6/+9/+12`，终局需按用户授权继续强化至 `+15` 并重铸。
- 当前状态为 `ready_to_collect_user_authorization_20260721`。授权前只允许被动收集和字段完整性检查，不允许消耗游戏资源、读取验证指标、发布救回规则或修改正式策略。攻击集中门槛仍未正式研究，第三项覆盖审计继续暂停。

#### 攻击集中研究与 OCR 优先级调整（2026-07-21）

- 用户确认当前 `baili-formal-dp-v1-epic-balanced` 已足够作为实际使用基线；生命集中独立验证不再阻塞下一项工程，但生命救回仍未发布。
- 新的下一项权威任务为[超高攻击集中属性基础策略研究任务说明.md](超高攻击集中属性基础策略研究任务说明.md)：先研究攻击 P90/P95/P97.5 阈值和对应体系兼容的正式离线候选，未通过独立验证前不改正式策略。
- OCR 允许恢复第一阶段只读/影子采样，详见[OCR自动强化实施路线与第一阶段只读识别任务说明.md](OCR自动强化实施路线与第一阶段只读识别任务说明.md)。采样用于测量识别准确率、建议一致性和执行耗时；不得自动点击、消耗资源或反向污染离线研究。
- 手工强化和软件执行必须分开统计：策略价值按游戏资源事件计算，OCR/软件耗时、重试、人工确认和失败率作为独立工程指标。无人值守自动强化仍未授权。

#### 攻击集中正式离线研究收口（2026-07-21）

- 攻击集中研究已完成五 seed、`runs=10`、1185/1185 新 schema 分片及独立复跑；报告：[epic_attack_concentration_exact_r10_20260721.md](reports/epic_attack_concentration_exact_r10_20260721.md)，原始 JSON：[epic_attack_concentration_exact_r10_20260721.json](reports/epic_attack_concentration_exact_r10_20260721.json)。
- P90/P95 的成对正式价值区间上界均小于 0，正式淘汰；P97.5 区间跨零，效应未决。三个阈值均不创建独立 Holdout，不写入正式策略。
- 当前正式策略仍为 `baili-formal-dp-v1-epic-balanced`；OCR 仍限于只读/影子采样，不能因为攻击离线研究完成而启用自动点击。攻击研究、生命 v4、基线审计相关回归共 29 项通过。
- 推荐：不启用攻击集中救回，继续使用当前正式策略。P90/P95 已被正式离线结果淘汰，P97.5 的区间跨零，只能保留为未决研究，不得作为实际强化门槛。

#### OCR 第一阶段恢复（2026-07-21）

- 用户授权恢复 OCR 第一阶段只读/影子研发；任务说明：[OCR自动强化实施路线与第一阶段只读识别任务说明.md](OCR自动强化实施路线与第一阶段只读识别任务说明.md)。
- 允许截图采集接口、区域定义、规范化、结构化 schema、置信度拒绝和影子对拍基础；禁止 Airtest、自动点击、无人值守、资源消耗和策略修改。
- 没有真实截图语料时只能完成基础设施和合成测试，必须停止在 `waiting_for_real_screenshot_corpus`，不能把接口测试写成实际 OCR 准确率。

#### OCR 第一阶段基础设施收口（2026-07-21）

- 已完成只读基础：[ocr_capture.py](src/e7_enhance/ocr_capture.py)、[ocr_regions.py](src/e7_enhance/ocr_regions.py)、[ocr_normalize.py](src/e7_enhance/ocr_normalize.py) 及 [定向测试](tests/test_ocr_stage1_readonly.py)。模块只接收导入截图或合成字节，记录 SHA-256、字段原文/候选/置信度、规范化值、拒绝原因、耗时和重试；未实现 OCR 引擎、设备抓图、GUI 按钮、点击坐标或 Airtest。
- 只有字段置信度不低于 `0.98`、别名可规范化且现有 `build_gear_dict`/`Gear.from_dict` 合法时才生成建议哈希；其它结果 fail closed。合成 6 项回归验证建议输入一致性和无点击，但不构成真实准确率。
- 仓库未发现真实游戏截图语料，状态为 `waiting_for_real_screenshot_corpus`。V1 正式策略不受影响；下一阶段仍限真实截图加同件 Fribbels/人工 JSON 的只读影子对拍，自动点击继续禁止。

#### OCR 首批真实截图配对（2026-07-21）

- 14 张真实截图已由只读配对器持久化并记录哈希/尺寸/页面类型；[manifest](manual_acceptance/ocr_stage1/batch_001/manifest.json) 中仅 2 张唯一匹配同一件生命套 `+15` 头盔（`instanceId=2565468393`），其余 12 张均为旧 Fribbels 快照无候选的 `unmatched + stale_reference_possible`，未强配。
- 最小参考仅保存该一件装备必要字段，未保留账号、英雄、资源或整份原始响应。当前状态为 `paired_reference_waiting_for_ocr_engine`，不是 OCR 识别通过。
- V1 策略、GUI 业务、自动点击和资源消耗均未修改。下一阶段必须另建只读 OCR 引擎与字段级影子核对任务；在真实字段一致闸门通过前不得设计或启用点击。
- 截图必须来自装备详情页原始 PNG，并与同一时刻的 Fribbels/人工 JSON 按 `screenshot_id -> instanceId` 配对；首批建议 10-20 张多样样本。没有配对真值的截图只能作为未配对素材，不能进入准确率统计。

#### OCR 第一批真实截图进入配对（2026-07-21）

- 用户已提供 14 张真实截图，达到第一批建议数量；权威执行边界见[OCR第一批真实截图配对与只读影子核对任务说明.md](OCR第一批真实截图配对与只读影子核对任务说明.md)。
- 本批仅做原图持久化、重复页面分组、Fribbels 唯一实例匹配、最小真值导出和只读哈希核对。未安装或运行 OCR 引擎前不得形成准确率结论。
- 正式 V1 策略继续保持 `baili-formal-dp-v1-epic-balanced`；自动点击、资源消耗、GUI 业务扩展和策略门槛修改仍未授权。
