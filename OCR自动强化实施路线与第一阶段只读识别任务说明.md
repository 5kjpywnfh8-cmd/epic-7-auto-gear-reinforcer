# OCR 自动强化实施路线与第一阶段只读识别任务说明

> 状态更新（2026-07-21）：`stage1_read_only_implementation_authorized`。攻击集中离线研究已收口，用户正式授权组织 OCR 第一阶段研发。只读/影子模式允许执行；自动点击、Airtest、无人值守强化和游戏资源消耗仍暂停。

## 当前完成度

### 已具备

- 标准 `Gear` 数据模型、严格输入校验和多套 JSON/Fribbels 别名规范化。
- 正式评分、Epic/Heroic 节点策略、资源模型、模拟器、debug 和 PySide6 操作台。
- 完整 Fribbels 导入、真实样本档案和候选 Holdout 隔离。
- GUI 已预留 OCR 与 Airtest 按钮，但两者均保持禁用。

### 尚未实现

- 没有真实游戏截图语料、截图采集器、区域定位、OCR 引擎或字段级置信度。
- 没有 OCR 中文噪声规范化、候选纠错和人工确认界面。
- `NullAirtestAdapter` 不会连接模拟器或点击；项目没有 ADB 截图、窗口定位、强化按钮识别或点击坐标。
- 没有“点击一次 -> 重新截图 -> 校验强化等级/属性变化 -> 再决策”的闭环状态机。
- 没有资源上限、误识别拒绝、超时、重试、紧急停止、重复点击保护和操作审计。

按子系统估计，决策层约完成七成以上，但端到端安全自动强化只完成约三成。距离只读 OCR 建议还有两个主要里程碑；距离无人值守自动强化还有四个主要里程碑。百分比只用于说明工程结构，不是工期承诺。

## 实施顺序

### 阶段 1：只读截图与 OCR 规范化（当前允许执行）

- 只支持截图导入或 ADB 截图，不点击游戏。
- 建立套装、部位、品质、强化等级、主属性、四条副属性和数值的区域定位。
- OCR 输出保留原文、规范化值、候选项、字段置信度和截图哈希。
- 只读采样必须同时保存当前装备导出快照、OCR 规范化结果、`advise_gear` 建议哈希和人工确认结果；不触发强化、不消耗资源。
- 影子模式只比较 OCR 建议与导出 JSON/人工决策的一致性，并记录识别耗时、重试次数和拒绝原因；不得用影子样本反向修改正式门槛。
- 复用现有 `build_gear_dict`、`Gear.from_dict` 和合法性校验；未知或低置信度字段必须拒绝。
- GUI 显示截图、识别框和人工确认后的标准装备 JSON，再调用现有建议引擎。

第一阶段不依赖 `output_8_13_tank_10_17` 是否发布，可以与 Holdout 验证并行；它只负责把截图可靠转换为标准装备。

### 阶段 2：影子运行

- 连续读取游戏装备页面并给出建议，但仍不点击。
- 同一批装备同时使用 Fribbels/人工结构化数据作为真值，逐字段比较。
- 必须区分“正确识别”“低置信度拒绝”和“错误放行”；不得只报告平均字符准确率。
- 至少覆盖不同分辨率、窗口缩放、亮暗背景、强化等级、左右装备和新套装。

### 阶段 3：人工确认后单节点执行

- 用户确认装备字段和建议后，自动点击到下一个复核节点，不允许一次冲到 +15。
- 每次点击后必须重新截图，确认装备实例、强化等级、属性值和资源页面仍匹配预期。
- 任一字段变化异常、页面丢失、超时或置信度下降立即停止。

### 阶段 4：受限自动循环

- 只处理明确白名单范围，例如 `normal_85 Epic/Heroic` 和当前已发布节点策略。
- 增加单次最多处理件数、金币/强化材料下限、最大强化等级、每日预算、紧急停止和完整操作日志。
- 先在测试账号或可回滚环境运行；正式账号无人值守前必须完成足够的人工监督零危险错误记录。

## 发布闸门

- 套装、部位、品质、强化等级和所有属性字段必须整件完全一致；低置信度必须拒绝，不能猜测。
- OCR 结构经过 `Gear` 合法性校验后，建议结果必须与同一装备 JSON 输入完全一致。
- 点击前后装备实例必须一致；强化只到 `next_check_at`，不得越过复核节点。
- 连续监督运行中不得出现错误装备点击、重复点击、超节点强化或资源保护失效。
- 正式策略未冻结或关键哈希变化时，自动点击层必须自动禁用。

## 第一阶段产物

- OCR 截图语料目录和匿名化元数据清单。
- 独立 `ocr_capture`、`ocr_regions`、`ocr_normalize` 模块，不把坐标写入策略代码。
- OCR 结构化 JSON schema、字段置信度和拒绝原因。
- GUI 人工确认视图及回归测试。
- 人类可读识别报告；不接 Airtest 点击。

## 当前优先级

1. 当前先实现独立 `ocr_capture`、`ocr_regions`、`ocr_normalize`、结构化 schema、置信度拒绝和测试，不修改策略代码。
2. 若仓库没有真实截图语料，使用合成/固定夹具只能验证接口和规范化，状态必须标记 `waiting_for_real_screenshot_corpus`，不能报告识别准确率。
3. 取得真实截图后才运行只读/影子对拍；OCR 建议必须与同一装备 Fribbels/人工 JSON 的 `advise_gear` 结果一致。
4. OCR 影子运行通过后，才设计逐节点人工确认界面；仍不得自动点击。
5. 只有策略 V1 契约、OCR 字段置信度和人工确认回放稳定后，才评估受控自动点击；完成回读闭环和资源保护后才讨论无人值守。

## 本轮执行边界（2026-07-21）

- 代码任务交给子 agent，当前协作接口没有 `gpt-5.6-luna`，按用户回退要求使用 `gpt-5.6-terra + high`；主 agent 只做范围确认、审阅和阶段闸门。
- 先审计现有依赖和截图输入；未经用户单独授权不得安装 OCR/Airtest/ADB 新依赖、打开模拟器或抓取真实截图。
- 第一轮必须先交付可测试的只读模块、结构化输出和拒绝路径；找不到真实截图时在基础设施验证后停止，不得进入点击设计。

## 真实截图提供协议

- 分工固定为：用户手动保存截图并提供截图目录与同期完整 Fribbels 导出；agent 负责从装备字段匹配同件 `instanceId`、生成 manifest、处理歧义和执行逐字段对拍。用户不需要手工填写 OCR 结果。
- 截图必须是游戏装备详情页，不是装备列表；套装、部位、品质、装等、强化等级、主属性和全部可见副属性数值必须同时可读。
- 优先保存原始 PNG，不要压缩、锐化、涂改或重新排版装备文字；可以裁掉账号名、聊天和其他个人信息，但不能遮挡装备字段。
- 每张截图必须与同一时刻的 Fribbels/人工结构化 JSON 配对，至少包含稳定 `instanceId`、套装、部位、品质、强化等级、主属性、全部副属性和数值。截图中看不见的字段不能补猜。
- 建议先提供 `10-20` 张多样样本：不同套装（含全力/弱化）、不同品质和部位、`+0/+3/+6/+9/+12/+15`、不同主属性和不同副属性组合。每张截图只对应一件装备。
- 推荐目录结构：`manual_acceptance/ocr_stage1/<batch_id>/screenshots/` 保存图片，`references/` 保存同批 Fribbels/人工 JSON，`manifest.json` 保存 `screenshot_id -> instanceId -> reference_file` 映射。
- 文件名可使用 `screenshot_id`，例如 `ocr-0001.png`；不要把账号名、角色名或其他私密信息写入文件名。提交时同时给出批次目录路径和导出文件路径。
- 第一批只做字段级只读影子对拍：OCR 原文/规范化值与 JSON 真值逐字段比较，记录正确识别、低置信度拒绝和错误放行；不得因为截图样本直接修改门槛或启用点击。

## 第一阶段基础设施结果（2026-07-21）

- 实际执行模型：`gpt-5.6-terra + high`；当前协作接口无 `gpt-5.6-luna`，未误记为 Luna。
- 审计确认仓库无 `.png/.jpg/.jpeg/.webp/.bmp` 真实游戏截图语料，也没有现成 OCR 引擎、`ocr_capture`/`ocr_regions`/`ocr_normalize` 模块或 OCR 依赖；`requirements-gui.txt` 仅含 PySide6，GUI OCR/Airtest 按钮保持禁用，`NullAirtestAdapter` 不连接设备且不可点击。
- 已新增只读模块：[ocr_capture.py](src/e7_enhance/ocr_capture.py)、[ocr_regions.py](src/e7_enhance/ocr_regions.py)、[ocr_normalize.py](src/e7_enhance/ocr_normalize.py)。截图接口只读取用户导入文件/合成字节并记录 SHA-256、来源、耗时、重试和 `click_performed=false`；未连接 ADB、窗口或模拟器。
- 结构化结果固定包含原文、规范化值、候选项、字段级置信度、拒绝原因、耗时、重试、区域模板、标准 Gear JSON 与建议哈希。未知字段、低于 `0.98` 的字段置信度、数值错误或 `Gear` 结构校验失败均 fail closed，不生成可比较建议。
- 输出经现有 `build_gear_dict`、`Gear.from_dict`、结构/来源校验后才调用 `advise_gear`；同一结构化 Gear 可用稳定建议哈希对拍。模块没有复制策略门槛，也没有 GUI 按钮、点击坐标、Airtest 或资源消耗路径。
- 新增 [test_ocr_stage1_readonly.py](tests/test_ocr_stage1_readonly.py)：覆盖中英文/中文别名、百分号与空格噪声、数字、未知与低置信度拒绝、哈希、区域边界、schema、建议一致性和无点击保证。定向 6 项与语法检查通过；`git diff --check` 见本轮收口记录。
- 当前状态：`waiting_for_real_screenshot_corpus`（已被后文“第一批真实语料配对”收口记录覆盖，仅作历史状态）。当时未报告 OCR 准确率，未进入 GUI 人工确认设计、影子对拍实测或点击阶段。

## 第一批真实语料配对（2026-07-21）

- 第一批 14 张真实 PNG 已完成持久化与只读配对，详情见[配对任务说明](OCR第一批真实截图配对与只读影子核对任务说明.md)、[manifest](manual_acceptance/ocr_stage1/batch_001/manifest.json)和[报告](reports/ocr_stage1_batch_001_pairing_20260721.md)。状态从无语料更新为 `paired_reference_waiting_for_ocr_engine`。
- 8 个装备组中只有生命套 `+15` 头盔的详情页/副能力转换选择页唯一匹配 `instanceId=2565468393`；另 12 张在 2026-07-19 Fribbels 快照没有精确候选，按 `unmatched + stale_reference_possible` 保存。没有推测 instanceId，也没有把人工目读字段冒充 OCR。
- 当前已有真实截图和最小真值，但仍没有 OCR 引擎，因此没有准确率、置信度、建议一致性或影子运行结论。自动点击、GUI 按钮、ADB/Airtest 和游戏资源操作继续禁止。

## 第一批真实截图到件（2026-07-21）

- 用户已提供 14 张真实装备页面 PNG，并指定已有 Fribbels 导出 `D:\VScode\E7-tools\gear_read\gear_fribbels_20260719_221033.json` 作为待核对参考。
- 新建唯一权威任务说明：[OCR第一批真实截图配对与只读影子核对任务说明.md](OCR第一批真实截图配对与只读影子核对任务说明.md)。当前状态从 `waiting_for_real_screenshot_corpus` 更新为 `real_corpus_received_pairing_ready`。
- 本批先持久化原图、按装备/页面类型去重分组并执行唯一实例匹配；没有 OCR 引擎时只建立字段真值与 shadow manifest，不报告 OCR 准确率。
- 自动点击、Airtest、ADB 抓图、游戏资源消耗、正式策略修改和 GUI 业务扩展继续禁止。
