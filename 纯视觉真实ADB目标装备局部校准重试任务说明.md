# 纯视觉真实 ADB 目标装备局部校准重试任务说明

## 目标

针对上一次误打开“选择套装/管理”界面导致的 fail-closed，本轮在用户确认已切换到正确装备详细页后，执行一次新的真实、只读、内存内 ADB 截图校准。仅验证页面与局部视觉证据，不点击、不强化、不选材，也不把结果表述为服务器确认。

## 唯一权威来源

本文件是本轮唯一权威任务来源。同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md`、`纯视觉真实ADB目标装备局部校准任务说明.md` 及现有纯视觉/OCR接口。聊天内容不能替代或扩大本文件范围。

## 目标装备口径

- 红装/传说、85级、命中套武器。
- 主属性攻击力 100；副属性暴击率 5%、攻击力 8%、效果抗性 8%；官方装备分数 28。
- 装备名称不是强化策略输入，不要求名称识别或跨页面绑定。
- 未强化装备可能没有 `+0` 文字；仅记录明确的未满 15/可强化视觉证据，不能把 OCR 缺失臆测为 +0。

## 范围与一次性限制

1. 完整读取本文件后运行一次官方 MuMu `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`。
2. 只接受自动发现且状态为 `device` 的唯一设备，不写死或传入任何 `192.168.x.x:5555` 地址。
3. 设备闸门通过后仅执行一次由自动发现序列号派生的 `exec-out screencap -p`；PNG 只在内存中处理，不落盘、不上传。
4. 内存校验 PNG 结构/CRC/视口/方向/非黑屏/SHA-256，裁剪既有 `set_icon`、`set_text`、`level`、`enhance`、主属性、副属性和分数区域，复用模板与 PaddleOCR 的 `0.98` 门槛和 fail-closed 规则。
5. 本轮是单帧校准，不替代正式三帧稳定采样；不得为满足稳定帧而执行第二次截图。

## 明确禁止

- 禁止 `adb input tap`、`swipe`、`keyevent`，禁止导航、点击、强化、选材、资源消耗、重启游戏或 GUI 操作。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、外部上传和模型下载。
- 禁止降低/绕过 `0.98`、以名称或缺失字段补齐目标、读取或保存私人截图/归档；不得使用 `git add -A`，不得触碰 `refs/heads/main - 副本`。
- 这是一次独立重试；失败后停止，不在本任务内自动重试或再截图。

## 执行步骤

1. 读取本文件和上位任务规则，确认用户已将 MuMu 切换到正确装备详细页。
2. 运行一次 `adb devices -l`，设备不唯一或状态异常立即停止。
3. 执行唯一一次 `exec-out screencap -p`，在内存中完成 PNG 和区域校验。
4. 执行一次局部套装模板观测和一次本地 PaddleOCR 观测，记录原始字段、置信度、区域边界、缺失和矛盾。
5. 输出固定标记 `mode=visual_only`、`verification=unverified`、`click_performed=false`。
6. 将结果、风险、停止原因和下一步写回本文件与 `e7-gear-enhance-plan.md`；不修改代码。

## 产物与验证要求

- 产出不含截图文件的设备、PNG、视口、哈希和局部识别摘要。
- 设备、PNG、页面、区域、模板、字段、置信度或矛盾任一失败均 fail-closed。
- 真实单帧结果与离线测试分开报告；执行模型记录为 `gpt-5.6-terra + high`。

## 停止条件

文档未完整读取、设备闸门失败、PNG/视口/页面不匹配、模板缺失、字段低于 `0.98` 或字段矛盾时立即停止；不得截图第二次，也不得进入 ADB 点击接线。只有本轮证据通过后，才可另建受控点击任务并重新授权。

## 执行记录（2026-07-27）

- 设备发现通过：官方 MuMu `adb.exe devices -l` 返回唯一 `emulator-5554 device`，未使用网络序列号。
- 唯一一次截图通过：`exec-out screencap -p` 返回 `791019` 字节，视口 `1280x720`，PNG 结构/CRC/像素流/非黑屏校验通过，SHA-256 为 `b0da5db49cdb9ead4d63924210f92fad023a62cb35a567cc2c81e0f59ea03e97`。PNG 未落盘、未上传，未执行第二次截图。
- 详细页区域边界均在视口内；套装图标模板没有唯一候选。
- OCR 形成了部分详细页字段：`传说武器`（品质/部位 `0.995034`）、主属性攻击力 `100`（`0.998722`）、副属性生命 `201`、效果命中 `4%`、效果抗性 `6%`、速度 `4`、装备分数 `27`（对应字段均达到 `0.98`）。
- 套装文字为 `速度套装(0/4)`，置信度 `0.963903`，低于 `0.98`；强化证据缺失，等级未形成唯一 `85`。解析 `accepted=false`，拒绝原因为 `low_confidence:set`、`missing:enhance`、`unrecognized:level`。
- 当前帧的装备指纹不是任务目标的命中套武器、暴击率 5%、攻击力 8%、效果抗性 8%、分数 28；不能把该帧当作目标装备确认。
- 结果固定为 `mode=visual_only`、`verification=unverified`、`click_performed=false`；未执行 ADB 输入、导航、点击、强化、选材、资源消耗、底层读取或外部上传。

## 当前状态

`real_adb_target_local_calibration_retry_completed_fail_closed_target_mismatch_20260727`

执行模型：`gpt-5.6-terra + high`。

## 未完成项、风险与下一步

- 本次已确认是装备详细页，但当前显示的是另一件速度套武器，不是目标命中套武器；套装置信度、等级和强化证据也未通过。
- 本独立重试任务已结束，不再自动重试或截图第二次。不能进入 ADB 点击接线。
- 下一步需用户在 MuMu 中切换到目标命中套红装武器（攻击 100、暴击 5%、攻击 8%、效果抗性 8%、分数 28），然后另建任务说明并重新取得单次只读授权；正式点击前仍需通过稳定帧契约。
