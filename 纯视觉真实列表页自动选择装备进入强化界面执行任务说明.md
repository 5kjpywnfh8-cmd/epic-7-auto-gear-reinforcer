# 纯视觉真实列表页自动选择装备进入强化界面执行任务说明

## 目标

在用户明确授权的范围内，自动选择列表页中的目标生命套武器并导航进入其强化界面，全程通过官方 MuMu ADB 只读截图验证每一步页面状态，最终停在强化界面（不执行强化、不消耗资源）。

## 授权范围（2026-08-01 用户明确批准）

- 允许通过官方 MuMu ADB 获取帧进行验证（`adb devices -l` + `exec-out screencap -p`）。
- 允许发送至多两次真实点击（`adb shell input tap`）：
  1. 列表页点击目标卡片（candidate_card:13，x143-313, y451-568，中心 x228, y509）；
  2. 装备详情页点击「强化」按钮（位置由详情页帧分析后确定）。
- 本授权不包含：强化执行、选材、确认、出售、分解、装备、佩戴、滚动、翻页、任何资源消耗或游戏内其他操作。
- 授权失效条件：任一验证失败、页面不确定、设备不唯一或用户叫停时立即停止，不重试、不追加点击。

## 禁止修改项

- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout、新鲜快照契约或强化决策配置。
- 禁止降低 0.98 门槛、放宽固定区域、动态搜索目标卡片或隐式回退。
- 禁止修改 `D:\VScode\Epic-7-tools.v1.2`、MuMu 设置或 FribbelsE7Optimizer 安装目录。
- 不写死任何 `192.168.x.x:5555` 地址；设备必须由 `adb devices -l` 自动发现且唯一。

## 输入与权威口径

- 目标装备指纹（用户确认 + 已审定真值）：武器、传说（红）、85 级、+3、生命值套装、主属性攻击力 160、副属性攻击力 13% / 暴击率 3% / 速度 2 / 效果抗性 8%、装备分数 37。
- 列表页当前注册区域：`equipment_list_region_registry()`（4x5 网格），目标卡片 `candidate_card:13`（x143-313, y451-568）。
- 整卡指纹模板：`card_fingerprint_manifest.json`（`target_card_full_20260801_runtime.png`，sha256 `99efe5c2…`，threshold 0.98）。
- 详情页字段 OCR 区域：`ocr_regions.equipment_regions()`（只读，`click_coordinates=None`）。

## 执行步骤

1. 前置：读取本任务说明 + AGENTS.md；运行 `adb devices -l`，要求恰好一个 `device` 状态设备。
2. 列表页稳定采样：连续三帧，验证候选列表区域与目标卡片区域，`candidate_card:13` 的 crop 指纹命中 `99efe5c2…`（score 1.0）；任一帧不命中即停止。
3. 点击 1：构造 `NormalizedClickTarget`（x228/1280, y509/720），经 `NormalizedClickExecutor` + 新增 ADB tap 后端执行一次点击；不重试。
4. 详情页验证：点击后重新采样至少三帧，确认页面签名变化、目标指纹仍唯一且一致；用详情页 OCR 区域确认字段（武器/生命套/85/+3/攻击160/13%/3%/2/8%/37）。
5. 定位强化按钮：从详情页帧中检测「强化」按钮锚点（亮边框/文字/图标），计算其归一化中心；无唯一锚点即停止。
6. 点击 2：以点击 1 相同约束执行强化按钮点击；不重试。
7. 强化界面验证：重新采样至少三帧，确认页面签名再次变化且出现强化界面特征（强化按钮/材料行/确认区），记录验证帧快照。
8. 收尾：保存全部验证帧快照与 JSON 记录；同步任务说明与总计划。

## 产物

- 验证帧快照：`assets/visual/nav_to_enhance_20260801/`（列表/详情/强化三阶段各至少三帧 + 关键 crop）。
- 执行记录：`reports/visual_list_nav_to_enhance_20260801.md`。
- 本任务说明与 `e7-gear-enhance-plan.md` 的同步记录。

## 验证要求

- 每步页面状态必须有对应帧哈希、时间戳和判定理由；点击前后帧哈希不得复用。
- 目标卡片点击前后：列表帧 candidate_card:13 指纹命中 0.98；详情帧目标字段唯一且与指纹一致。
- 强化按钮点击前后：详情帧→强化帧页面签名变化，且强化帧出现强化界面特征。
- 所有真实操作命令必须记录：命令、时间、序列、输出摘要。

## 停止条件

- 设备不唯一、帧不稳定、指纹不命中、字段冲突、按钮锚点不唯一、页面未变化或变化异常时，立即 fail-closed 并停止；不重试、不追加点击。
- 用户叫停或授权条件变化时停止。

## 当前状态

`nav_to_enhance_authorized_execution_pending_20260801`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前接口不可用 Luna，实际执行模型为 `deepseek-v4-flash`（opencode），并在阶段记录保留该事实。

## 执行记录（2026-08-01）

- 用户明确授权：自动选择装备到进入强化界面，允许 adb 获取验证，目标为生命套武器。
- `adb devices -l`：唯一 `emulator-5554 device`（MuMu 12），通过自动发现，未使用任何 `192.168.x.x:5555`。
- 待执行：步骤 2-8（见上）。
