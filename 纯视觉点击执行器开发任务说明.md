# 纯视觉点击执行器开发任务说明

## 目标

在现有 `VisualOnlyEnhanceExecutor` 的注入式点击 seam 上，开发一个可离线验证、fail-closed 的归一化坐标点击执行器。该执行器只负责把已经通过视觉证据闸门的单次强化动作转发给外部窗口后端，不负责截图、OCR、页面导航、材料选择或策略决策。

## 范围

- 新增独立点击适配模块，定义窗口后端协议、归一化坐标校验、视口换算、显式单次授权和一次性调用状态。
- 让点击异常在调用前后都保持不可重试语义；后端异常由上层运行时按 `unknown_result` 处理。
- 新增公开单元测试，覆盖坐标换算、边界、未授权、重复调用、视口无效和后端异常。
- 保持现有 `VisualOnlyEnhanceExecutor`、`PureVisualSamplingExecutor` 和 `airtest_adapter` 的兼容接口；不改变策略、DP、评分、资源模型或 GUI 行为。

## 禁止修改与禁止操作

- 不连接或控制 MuMu/ADB，不启动游戏，不采集真实截图，不运行 OCR，不执行真实页面点击、强化、选材或资源消耗。
- 不接入 Airtest、PyAutoGUI、Win32 或其他桌面输入库；真实窗口后端只通过依赖注入协议提供。
- 不修改 MuMu/Fribbels 读取器、413 载荷分批、fresh snapshot 契约、正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。
- 不读取、恢复、暂存或上传私人归档；不修改或删除 `refs/heads/main - 副本`。
- 不使用 `git add -A`。

## 权威输入与模型

- 本文件是本轮点击执行器开发的唯一任务来源。
- 设计依据：[纯视觉运行时接线实现任务说明.md](纯视觉运行时接线实现任务说明.md)、[纯视觉采样方案设计任务说明.md](纯视觉采样方案设计任务说明.md)、`src/e7_enhance/visual_runtime.py` 及其公开测试。
- 目标节点仍为用户给定的红装 85 级武器、爆伤套、主属性攻击 100、生命 4%/爆伤 4%/暴击 5%/速度 2，`+0 -> +3`；资源上限粉末 2、下级强化石 1、上级强化石 0、金币 17600。
- 执行模型：`gpt-5.6-terra + high`。

## 执行步骤

1. 先审阅现有运行时 seam、airtest 占位接口和公开测试，建立接口约束。
2. 先补失败测试，再实现独立的归一化坐标点击器。
3. 只使用 fake backend 做离线验证；不得调用真实窗口或设备。
4. 运行定向测试、Python 语法检查和 `git diff --check`，精确审阅 diff。
5. 将完成项、验证结果、未完成项、风险和下一步同步回本文件与 `e7-gear-enhance-plan.md`，然后按项目规则精确提交并推送。

## 产物

- `src/e7_enhance/visual_click.py` 及对应公开测试。
- 本任务说明和总计划的状态记录。

## 验证要求

- 点击器单元测试全部通过；现有视觉运行时和 dry-run 回归不回退。
- Python 语法检查和 `git diff --check` 通过。
- 验证报告必须明确“仅 fake backend 离线验证”，不得写成真实点击或强化已验证。

## 停止条件

- 任何步骤需要真实截图、ADB/MuMu、OCR、桌面输入库、页面点击或资源操作时立即停止并报告。
- 坐标、视口、授权、调用次数或后端状态无法唯一确认时 fail-closed，禁止自动重试。

## 当前状态

`implementation_verified_offline_published_20260726`

- 已完成 `src/e7_enhance/visual_click.py` 与 `tests/test_visual_click.py`；实现归一化坐标换算、视口/授权校验、一次性尝试锁定和后端异常未知结果。
- 定向验证 `tests.test_visual_click`、`tests.test_visual_runtime`、`tests.test_visual_sampling` 共 `17/17` 通过；Python 语法检查和 `git diff --check` 通过。
- 仅使用 fake backend 离线验证；未连接 MuMu/ADB，未采集截图/OCR，未执行真实点击、强化、选材或资源消耗。
- 提交 `2a071f3 feat: add fail-closed visual click executor` 已推送至 `origin/codex/policy-v1-manifest-20260725`；Git 提交后 geometric repack 因既有异常引用 `refs/heads/main - 副本` 报警，但未修改该引用。
- 真实窗口后端仍需另建任务和明确运行授权；本任务未执行任何真实点击或强化。
