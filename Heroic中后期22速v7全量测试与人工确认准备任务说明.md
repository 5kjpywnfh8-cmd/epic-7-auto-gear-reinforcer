# Heroic 中后期 22 速 v7 全量测试与人工确认准备任务说明

## 目标

完成 v7 研究的发布前收口，但不修改正式强化策略。任务只处理：

1. 修正报告中紫装增量与红紫联合绝对量的展示口径；
2. 复现并解决统一全量测试的 Qt 异常退出；
3. 生成均衡档、激进档与当前M0的简明人工确认对比。

不得修改正式策略、跳值表、lambda、评分、DP、资源默认值、OCR、ADB、Airtest或GUI业务行为。

## 一、报告口径修正

当前 `reports/heroic_midgame_speed22_v7_formal_20260714.md` 的 `100,000总体力` 是完整联合线路：维度裂缝同时获得Epic/Heroic，再由圣女3-7补充强化资源。它不是只为Heroic投入的十万体力。

必须将报告拆为两类指标：

### 紫装候选相对 M0 的增量

- `Heroic 22+新增量/100,000联合总体力`；
- 额外圣女体力；
- 损失的来源循环；
- 正式GS、原生75+、转换可达75+、输出60变化；
- 五seed成对95%区间。

### 红紫联合绝对基线

- 维度裂缝体力、圣女体力、来源循环；
- 期望Epic/Heroic件数；
- 红紫联合正式GS、75+及输出60。

`联合22+` 只有在Epic绝对22+基线精度足够时才允许显示。当前每seed公共池128条且Epic 22+观测为0，不能把0写成真实绝对产量。先审核已有高精度Epic基线与v7来源、策略、概率、资源口径是否完全兼容：

- 兼容时可只读复用，并注明来源、样本量和区间；
- 不兼容时显示 `not_estimated`，不得为了填表猜测或把紫装增量复制成联合绝对值。

标题改为 `每100,000总体力（维度裂缝红紫联合产出 + 圣女补充）`。所有GS列注明是红紫联合绝对量还是Heroic候选增量。

## 二、全量测试收口

先阅读：

- `tools/run_all_tests.py`
- `tests/test_manual_sample_performance.py`
- `tests/test_gui_app.py`
- `reports/test_logs_20260713/test_manual_sample_performance_run{1,2,3}.*`

已知 run2/run3 日志均为5项通过且 `EXIT_CODE=0`，所以不能直接把该测试标为固定失败。

执行顺序：

1. 在全新进程中运行 `tests.test_manual_sample_performance` 至少两次并保存退出码。
2. 运行 `tools/run_all_tests.py`，保存逐文件开始/结束、退出码和失败文件。
3. 若仅组合运行失败，最小化顺序并确认是Qt对象生命周期、模块导入污染还是原生进程退出。
4. 先添加能复现问题的测试入口回归，再作最小修复。
5. 优先修测试清理或逐测试进程隔离；禁止修改真实样本管理器业务逻辑来迎合测试。
6. 修复后统一入口连续两次返回0，且没有跳过GUI测试，才算全量闸门通过。

如果无法稳定复现，但统一入口连续两次通过，应记录为环境波动及剩余风险，不提交无证据的GUI修改。

## 三、人工确认对比

生成一张简洁对比表，不替用户作选择：

|档位|候选|紫装22+新增/10万联合总体力|每新增1件22+圣女体力|损失来源循环|策略GS变化|
|---|---|---:|---:|---:|---:|
|维持当前|M0|0|不适用|0|0|
|均衡参考|M2 P22>=10% / all_sets|0.053294|137.469|1.617|-0.013515|
|激进参考|M1 P22>0 / all_sets|0.132990|213.783|2.515|-0.058901|

同时显示原生75+、转换75+和输出60变化，以及两个候选各自的五seed区间。明确：

- 均衡档只救回终局22+概率至少10%的状态；
- 激进档救回任何仍可达到22+的状态；
- 22速优先级单列，不折算进正式体系百里分；
- 当前统计通过不等于已经发布。

人工确认阶段只请求用户选择：`维持M0`、`均衡档`或`激进档`。用户选择前不得创建正式策略修改任务。

## 四、与 Epic B 方案的同池组合对比

不能把 B 的 `0.228514统一终局价值/100体力` 与 `0.132990件Heroic22+/100,000体力`按数字大小比较。B控制Epic早期非速度筛选，v7控制Heroic中后期速度救回，实际策略是两者叠加。

在不修改冻结B规则的前提下，使用相同联合来源、相同公共轨迹和相同显式资源池，比较：

- `B + Heroic M0`；
- `B + Heroic M2_p22_10pct/all_sets`；
- `B + Heroic M1_reachable_22/all_sets`。

统一复用此前22速价值锚点的 `curve_baseline` 与第二档低/中/高敏感性，报告：

- 统一终局价值/100体力；
- 紫装22+新增量/100,000联合总体力；
- 正式体系百里分、75+和输出60变化；
- 维度裂缝/圣女体力与来源循环；
- `B+M2 - B+M0`、`B+M1 - B+M0`、`B+M1 - B+M2` 的逐seed成对95%区间。

边际自检：M2抵消正式策略GS损失所需的22速价值约为 `0.2536`点/件，M1约为 `0.4429`点/件。该值只用于检查公式，不得代替完整联合重放。

## 五、验收

- 报告不再把Heroic增量误称为红紫联合绝对22+。
- 十万总体力明确拆分维度裂缝与圣女，并显示Epic/Heroic期望件数。
- 完成B+M0/B+M2/B+M1同池比较，禁止跨报告用不同单位直接排序。
- 全量测试入口连续两次返回0，或明确保留未解决阻断且不进入人工确认。
- 相关定向测试、语法检查和 `git diff --check` 通过。
- 未修改正式策略、GUI业务逻辑或前瞻批次。
