# MuMu 状态感知只读导航修复任务说明

## 目标

修复 MuMu 读取器无法稳定触发账号同步的问题：在执行只读读取前，通过截图/模板状态识别确认 Epic Seven 已进入可交互大厅，再导航到装备或英雄页面触发同步，随后沿既有抓包、Fribbels 解码和输出校验流程继续。成功标准是离线回归稳定恢复有效同步候选，且一次获准的真实读取生成新的同批输出；失败必须保持 fail-closed。

## 授权与范围

- 用户已授权：只读截图/模板状态识别；确认大厅后只读导航至装备或英雄页面；修复后一次标准真实读取及既有 Fribbels API 验证。
- 允许修改：必要的外部读取器状态识别、只读导航接线、读取器侧公开回归测试，以及本说明和总计划。
- 必须使用 `gpt-5.6-terra + high`，并如实记录。

## 禁止修改与操作

- 不强化、不选材、不改变装备、不导入档案、不消耗资源，不进入强化页输入。
- 不修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。
- 不读取、恢复、暂存或上传私人归档；不直接修改 PCAP、player_data.json、reader_result.json 或 snapshot。
- 不写死或复用 `192.168.x.x:5555`，不安装 Npcap，不改系统/模拟器网络设置或 Fribbels 安装目录。
- 不自动重试真实读取；离线验证未通过时停止并报告。

## 权威输入

- 根目录 `AGENTS.md`、`MuMu抓包读取与真实装备导入任务说明.md`、`e7-gear-enhance-plan.md`。
- 当前失败批次 `20260726_012048` 与历史成功批次的脱敏结构统计。
- 当前读取器入口：`D:\VScode\Epic-7-tools.v1.2\local_tools\read_mumu.py`；状态识别与导航所在读取器模块及其公开测试。
- 离线反馈回路：`tools/mumu_capture_sync_probe.py`。失败样本必须在修复前稳定红灯。

## 执行步骤

1. 读取读取器 `CONTEXT.md`（若存在）、入口、状态/ADB 封装和相关测试，确认正确 seam。
2. 建立或扩展确定性模拟 ADB/截图 fixture：非大厅不得导航，大厅状态才允许一次只读导航；断言导航前置条件和 fail-closed 行为。
3. 先运行回归测试得到修复前红灯；提出至少三个可证伪假设并记录在本说明与总计划。
4. 以最小改动实现状态识别和只读导航，保留自动设备发现、命令兼容及既有输出契约。
5. 依次运行回归测试、相关既有测试、失败 PCAP 离线探针、Python 3.9 语法检查和 `git diff --check`；清理临时调试日志。
6. 精确审阅改动，提交并推送；只有离线验证通过后，才按本说明既有授权执行一次标准真实读取与 Fribbels 验证。

## 待证假设

1. 若固定坐标点击落在公告/登录或非大厅 Unity 页面是主因，则只有截图匹配大厅模板后才执行导航，模拟非大厅输入应保持零点击并 fail-closed。
2. 若屏幕分辨率或窗口缩放导致固定坐标不稳定，则模板匹配应使用归一化裁剪/缩放，且同一状态在不同分辨率 fixture 中仍能通过。
3. 若截图采集或模板缺失导致误判，则截图命令失败、模板不存在或相似度低于阈值时必须停止导航并报告明确原因。

## 产物

- 读取器最小修复及公开回归测试。
- 脱敏诊断/验证报告，记录命令、退出码、状态识别结果和 fail-closed 原因。
- 本说明与 `e7-gear-enhance-plan.md` 的同步记录。

## 停止条件

- 无法构造稳定红灯、状态识别无法区分大厅、需要 OCR/GUI/强化页输入，或修复超出读取器最小范围时停止。
- 任何真实读取输出不是新鲜同批双文件、Fribbels 为 0 件、实例无法唯一配对或页面状态不明确时立即 fail-closed，不重试。

## 当前状态

`offline_fix_verified_awaiting_target_template_and_real_validation`

已完成读取器审计和最小离线修复。根因是 MuMu `wm size` 报告物理 `720x1280`、逻辑截图为 `1280x720`，旧读取器把逻辑坐标直接发送给 ADB，导致点击落点错误；现已按 `dumpsys input` 旋转值转换为物理输入坐标，并在导航前后执行 PNG 状态模板确认。

离线验证：外部读取器新增模拟 ADB/PNG 回归测试 `5/5` 通过；Python 3.9 语法检查通过；既有 `test_data_writer.py` 定向测试 `6/6` 通过。非大厅、缺少模板、截图失败和目标页不匹配均断言不点击或立即停止。

只读设备预检已执行：ADB 自动发现 `1` 个 `device`，截图采集成功；当前截图显示大厅及登录奖励/活动遮罩曾交替出现。尝试采集的参考模板包含遮罩或错误活动页，尚无可审阅、可复用的干净装备/英雄目标页模板，因此未启动标准抓包、未上传 Fribbels、未生成新的 player_data.json/reader_result.json/snapshot。

下一步：用户需提供一张干净的装备或英雄页面参考截图，或在保持只读授权下手动将游戏停在该页面以便采集目标模板；之后再运行一次标准读取和既有 Fribbels 验证。此前不得盲点、重试抓包或上传载荷。实际执行模型记录为 `gpt-5.6-terra + high`。
