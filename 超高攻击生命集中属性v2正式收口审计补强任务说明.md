# 超高攻击/生命集中属性 v2 正式收口审计补强任务说明

## 状态

`superseded_by_system_compatibility_v3_20260720`

本文件曾是 v2 只读补强的权威任务来源，现已被体系兼容 v3 任务覆盖，不得继续执行。v2 精确概率研究的人类产量聚合与预注册回归覆盖问题保留为 v3 的附带修复项。

> 后续主审计发现更早的体系兼容阻断：v2 候选路径未调用已导入的 `_compatible`，生命救回混入非生命体系。本任务尚未执行即被《超高攻击生命集中属性体系兼容修复与v3重算任务说明.md》覆盖，原有“只读且不重跑分片”边界不再适用。

## 目标

只读复核现有 v2 JSON 与 1,185 个完整分片，修正每十万总体力集中装备产量的聚合口径，补齐预注册测试和人类报告。不得生成新主路径、改变概率 DP、候选动作、正式价值差或资源模型。

## 已确认且保持不变

- 正式基线为 `baili-formal-dp-v1-epic-balanced`。
- `health_p975_balanced` 五 seed 成对正式价值差为 `-0.000665 [-0.001082,-0.000248]/100体力`；该发布闸门方向保持不变。
- v2 schema 2、157 Epic、80 Heroic、每件10条主路径、5 seed 和 1,185 个分片保持冻结。
- 精确概率 DP、Heroic `+12` 规则、一次重铸、10% 门槛和旧 p4 隔离保持不变。

## 必须修正

1. 当前报告生成器把五 seed 的 `incremental_target_high_per_batch` 均值及区间统一除以第一个 seed 的 `resource_pool.total_stamina`。必须改为每个 seed 分别计算 `incremental_target_high_per_batch * 100000 / candidate_total_stamina`，再汇总均值和 95% 区间；“每件新增所需体力”使用修正后的均值。
2. 人类报告补充 Epic/Heroic 分品质贡献、套装数、部位数、五 seed 非零覆盖，以及基线和候选逐 seed 的裂缝体力加圣女体力守恒检查。
3. 报告结论文字必须由实际 gate 状态生成，禁止保留“区间跨零”等硬编码旧句。
4. 正式 resume 复跑证据应保存 `scheduled_shards=0`、`reused_shards=1185`、聚合哈希与首次正式结果一致；不得只在任务说明中口头记录。
5. 原正式研究器文件哈希已进入 shard 输入哈希。优先新建只读审计/报告器读取正式 JSON 与分片，避免因仅修改展示代码导致 1,185 个分片全部失效。若必须修改原研究器，先证明不会错误复用旧分片；不得静默放宽哈希校验。

## 必须补齐的测试

- 手工可算的小状态概率与精确 DP 对拍，不能只用 `exact_health_probability()` 对拍同一实现生成的 distribution。
- 五 seed 逐 seed 归一后再汇总，构造不同资源分母以防止“首 seed 分母”回归。
- 基线与候选 seed 集合、输入哈希及同池配对不一致时拒绝审计。
- 每个 seed、每个方案均满足裂缝体力加圣女体力等于 100,000。
- 发布闸门分别覆盖区间上界小于0、跨0、下界大于0三种状态。
- 非负计数的人类报告下界不显示负数。
- resume/schema/研究器哈希不匹配时拒绝；正式复跑为0新分片且汇总一致。

## 产物

- 新的只读收口审计器及测试；
- v2 收口审计 JSON 与人类报告，保留原 v2 正式 JSON 作为冻结输入；
- 更新概率精度任务说明、本任务说明、`e7-gear-enhance-plan.md` 和《强化策略V1收口与GUI延期任务说明.md》；
- 报告明确区分“正式价值淘汰结论”和“修正后的解释性产量”。

## 禁止项

- 不修改正式策略、DP、评分、资源模型、跳值、GUI、OCR、自动化或 Holdout。
- 不扩展到攻击、OR、aggressive、其他分位数或 `+0/+3`。
- 不生成新的研究轨迹，不重跑1,185个正式分片，不进入第三项策略覆盖审计。

## 完成闸门

- 修正后的产量、覆盖、资源守恒、resume 证据和 gate 状态均写入新 JSON/报告。
- 定向测试、`tools/run_all_tests.py`、语法检查和 `git diff --check` 全部通过。
- 主 agent 审核接受后，才把集中属性研究标为 V1 第二项完成，并建立第三项独立任务说明。
