# Heroic 紫装中后期 22 速可达性路线研究任务说明

更新日期：2026-07-13

## 目标

在已经确认 `+0` 初始速度门槛 H2/H3 不改变自动二元动作后，研究真正会改变 Heroic 强化路线的节点：`+6/+9/+12`。

本任务比较当 `normal_85 Heroic` 基础回退原本准备停止时，是否应因终局 22 速仍可达而救回并继续，量化每十万总体力增加多少 Heroic 22+、额外消耗多少圣女资源、牺牲多少正式体系 GS。

只做离线研究。不得修改正式策略、跳值表、lambda、评分、DP、资源默认值、GUI、OCR、ADB、Airtest、自动点击或任何前瞻批次。

## 冻结基准

- `+0/+3` 完全使用当前正式路线，不再研究 H2/H3 标签门槛。
- Epic 全路径保持当前已发布策略和 DP，Heroic 候选不得影响 Epic。
- Heroic 从 `+6` 起的当前基准为 `baili_marginal_low` 基础回退，`dp_enabled=false`。
- 候选只能在 Heroic 基础回退原本返回 stop 时执行“速度救回”；基础回退本来继续的装备不得被候选停止。
- 候选只适用于非鞋且当前已有速度副属性的 `normal_85 Heroic`。
- +15 不再做继续决策；终局按85级重铸一次，90级不得重复重铸。
- 终局非鞋速度 `>=22` 为独立最高优先指标，不折算或混入正式 GS。

H4/H2/H3 初始门槛动作等价性结论保持封存：`no_binary_action_difference_under_current_policy`。本任务不得重新打开该问题。

## 精确 22 速概率

对每个 `+6/+9/+12` 当前状态精确枚举剩余事件：

- Heroic 速度强化跳值使用用户确认分布：`1=0.332%`、`2/3/4=33.223%`，无5；
- 每次已有副属性强化命中概率按当前副属性条数计算；
- Heroic `+12` 是补第4条副属性，不得伪造为强化命中；已有速度时补词条不能再次生成速度；
- `+15` 才再次强化已有副属性；
- 重铸速度收益按实际速度 rolls 计算且只加一次；
- 输出 `P(final_speed>=22)`、最高可达速度、期望终局速度和每个22/23/24/25/27+概率。

概率函数必须只依赖当前可观察状态：当前强化等级、当前速度、速度 rolls、已有副属性数、剩余节点和来源/品质。不得读取未来轨迹结果作决策。

## 候选路线

所有候选均为“基础回退 stop 时才可能救回”。冻结以下研究候选：

1. `M0_current`：不救回，当前 Heroic 基础回退。
2. `M1_reachable_22`：理论最高终局速度仍能达到22即救回。
3. `M2_p22_grid`：按精确 `P22` 门槛网格分别研究 `0.5%/1%/2%/5%/10%`，网格只是离线曲线，不是预设正式阈值。
4. `M3_p22_per_stamina_grid`：使用 `P22 / 下一节点净体力成本` 的冻结网格，比较概率相同但下一节点成本不同的状态；阈值必须在运行前写入结果元数据。
5. `M4_speed_hit_chain`：最近一次实际强化事件命中速度时救回；Heroic +12补词条不算一次速度命中，+12状态读取最近的真实强化事件。
6. `M5_external_speed_reference`：仅使用已记录外部表的 Heroic 速度阈值作纯速度参考，不混入其总GS/输出OR规则。

每个候选分别运行两种套装范围：

- `speed_2pc`：速度套加确认两件套（暴击、生命、防御、免疫、穿透、激流、命中、抵抗）；
- `all_sets`：所有合法套装。

弱化套加入免20速门槛的影响只作为单独 sensitivity，不并入 `speed_2pc` 主候选。由于目标已经是22速，套装速度潜力的20门槛不会改变是否达成22，但会影响低于22的正式/潜力旁证，必须分开说明。

## 速度效率上界

增加一个只作研究上界的 `speed_oracle`：在每个当前状态使用剩余随机分布和节点资源成本，计算继续/停止对终局22件数的最优动作。Oracle可以读取完整状态分布，但不能读取单条轨迹的未来随机结果。

Oracle只用于回答候选距离理论速度效率上界还有多远，不能直接发布为规则。若简化候选与Oracle动作差异很大，应报告主要差异状态，不得继续调参直到贴合开发集。

## 完整池和稀有事件分层

沿用已修正的 v2 正式基准、联合资源池和终局三种GS口径，但使用新 schema 和新 resume。

为避免 Heroic 22+过稀，按当前决策状态分层：

- 套装；
- 部位/主属性；
- +6/+9/+12；
- 当前速度；
- 速度 rolls；
- 当前副属性数；
- 基础回退 continue/stop；
- 两种初始值口径。

重点扩大“基础回退 stop 且候选可能救回”的差异 strata。每层必须保存：自然权重、条件轨迹数、条件22+数、加权贡献。不得把条件样本量当自然掉率。

初始副属性数值仍无直接精确概率证据，因此完整十万体力主结果分别运行：

- `legacy_initial_uniform`；
- `roll_distribution_as_initial_proxy`。

代理口径不得称为官方自然掉率。两种口径若改变候选 Pareto 排序，结论保持 `initial_distribution_blocked`；但条件 +6 状态的动作表和 P22 精确值仍可作为独立结果。

## 训练与验证隔离

- 概率网格和固定候选先冻结，不得根据正式验证 seed 临时改阈值。
- 若要从网格选择一个简化候选，只能在开发 seed 选择 Pareto 前沿，然后用全新 holdout seed 验证。
- 公共轨迹用于候选成对比较。
- 新 resume 支持原子保存、断点续跑、seed/分片去重和多进程；不得读取第一版错误分片、H2/H3等价性矩阵或旧条件库存分片作为自然权重。
- 每个进入正式比较的候选至少观察到 `200` 个“由速度救回产生的 Heroic 22+有效终局”，不是统计候选共享的普通22+凑数。
- 同时报告 seed 成对95%区间、低概率事件区间和相对半宽。

## 十万总体力报告

主报告继续使用完整线路 `100,000` 总体力：

```text
单循环总体力 = 85维度裂缝体力 + 该候选的圣女3-7瓶颈补充体力
循环数 = 100000 / 单循环总体力
维度裂缝体力 + 圣女体力 = 100000
```

每个候选必须用人类可读表展示：

- 循环数、维度裂缝体力/次数、圣女体力/次数；
- Epic/Heroic获得数；
- Heroic在+6/+9/+12由基础回退停止、基础回退继续、速度救回、最终仍停止的件数；
- Heroic及联合总池的22/23/24/25/27+件数；
- 相对 M0 多出的 Heroic 22+及95%区间；
- 原生正式总GS、合法满值转换可达总GS、实际策略承担转换成本后的总GS；
- 输出60、原生75+、转换75+；
- 粉末、下级石、强化金币、转换金币、出售回收与资源瓶颈。

另外给出：

```text
每增加1件Heroic 22+：
- 多消耗多少圣女体力；
- 少获得多少原生正式GS；
- 少获得多少满值转换可达正式GS；
- 多强化多少件到+9/+12/+15。
```

分母为0时显示“不适用”，禁止伪造无限值。

套装仍按单套条件报告；实际选择四套时取四个单套结果算术平均。允许输入四个明确套装生成演示，但不得建立账号刷图规划或所有套装全局等权假设。

## 决策口径

不再把速度和GS强行合成一个分数。采用 Pareto 顺序：

1. 第一目标：每十万总体力 Heroic 22+显著增加；
2. 第二目标：在22+效率统计近似的候选中，正式GS损失更少；
3. 第三目标：资源消耗和规则复杂度更低。

报告候选交换关系，由用户决定可接受的GS/资源代价。即使候选速度第一，也不能自动修改正式策略。

## 先补失败测试

至少覆盖：

1. 候选不改变 +0/+3，不重新引入H2/H3标签研究；
2. Epic、鞋子、无速度Heroic完全不受影响；
3. 基础回退continue时候选不得改为stop；只有基础回退stop时可以救回；
4. +6/+9/+12分别存在候选与M0不同的真实二元动作；
5. Heroic +12补词条不计为速度命中，且已有速度不能重复生成速度；
6. 官方Heroic速度跳值概率、无5速及概率和；
7. P22精确枚举在可手算边界状态上正确，包括必不可能、必达到和仅一次命中可达；
8. 重铸速度收益只计一次，90级不二次重铸；
9. `reachable_22`在最高可达<22时不救回，>=22时可救回；
10. P22网格边界和下一节点成本变化正确；
11. speed_2pc/all_sets/弱化敏感性范围严格分离；
12. strata自然权重守恒，条件样本只能加权后进入完整池；
13. 每个正式候选的差异Heroic22有效事件计数，不得用共享事件充数；
14. 原生/转换可达/实际策略GS分离且每件只计一次；
15. 十万总体力账本守恒；
16. 新schema拒绝旧resume，续跑不重复分片。

## 允许修改范围

优先新增独立工具与测试，例如：

- `tools/research_heroic_midgame_speed22.py`
- `tests/test_research_heroic_midgame_speed22.py`
- `reports/heroic_midgame_speed22_20260713.md/.json`
- 独立 resume 目录
- `e7-gear-enhance-plan.md`

允许只读复用 `research_heroic_speed_threshold_100k.py` 中已验证的正式基准、联合资源池、转换三口径和人类报告结构。不要继续把新逻辑堆进门槛收口工具；新研究器应保持独立。

## 发布闸门

以下全部满足，才允许建议建立新的独立速度前瞻批次：

- 候选相对 M0 的 Heroic 22+/十万总体力成对95%区间整体大于0；
- 至少200个差异Heroic22有效事件且区间相对半宽可接受；
- 两种初始值口径不改变推荐候选，或已有新的初始值权威证据；
- 资源账本守恒，Epic/Heroic来源没有重复计成本；
- 正式GS、75+、输出60损失已明确展示；
- 动作规则只依赖当前可观察状态，debug可解释；
- 全量测试成功退出。

否则保持离线状态，不创建前瞻批次、不修改正式路线。

## 最终报告

第一页必须直接回答：

- 当前 Heroic 在+6/+9/+12分别停止多少速度胚子；
- 哪个节点的速度救回贡献最多22+；
- 速度套/两件套范围与全套装范围各多多少22+；
- 每多一件22+付出多少总体力和正式GS；
- 哪个简化候选最接近速度Oracle；
- 两种初始值口径是否一致；
- 是否达到前瞻验证闸门。

机器字段、阈值网格、strata、seed、分片、哈希放在后文。完成后运行定向测试、相关策略/概率/资源/评分/转换测试、`tools/run_all_tests.py`、语法检查和 `git diff --check`，并更新计划。
