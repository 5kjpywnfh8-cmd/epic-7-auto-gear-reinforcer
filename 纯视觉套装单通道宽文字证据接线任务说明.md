# 纯视觉套装单通道宽文字证据接线任务说明

## 目标

按用户最新明确口径，将纯视觉装备详情页的套装字段调整为单通道可用：套装图标模板或局部套装文字 OCR 任一通道在当前稳定帧内唯一达到 `0.98`，即可提供套装视觉字段。当前实现选择已离线验证通过的宽文字裁切作为正式 `set_text` 区域。

## 唯一权威来源

本文件是本轮唯一权威任务来源。同时完整遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md` 和现有视觉运行时接口。聊天内容不能扩展本任务范围。

## 输入与权威口径

- 已完成的 [纯视觉套装局部裁剪与强化证据锚点修正任务说明](纯视觉套装局部裁剪与强化证据锚点修正任务说明.md) 及其 [离线报告](reports/visual_set_crop_comparison_20260727.md)。
- 其中用户指定未标注原图的宽文字裁切 `(916,540)-(1120,600)`（基准视口 `1280x720`）识别为 `速度套装(0/4) @ 0.981642`；紧文字裁切 `0.961027` 不通过，图标通道仍不通过。
- 宽文字区域的归一化边界固定为 `(0.715625, 0.75)-(0.875, 0.8333333333)`，不做动态搜索、缩放搜索或自动重试。
- 门槛维持 `0.98`，输出继续为 `mode=visual_only`、`verification=unverified`。

## 范围

1. 将默认 `set_text` 与 PaddleOCR 的正式局部文字裁切同步为上述宽区域；保留候选裁切的审计用途。
2. 将套装字段契约明确为单通道：图标模板与文字 OCR 任一通道唯一、高于等于 `0.98` 且能映射至既有标准套装值即可；另一通道的低分、缺失或无匹配不否决已通过字段。
3. 若两个通道都通过但标准化套装值不同，必须 `conflicting:set` 并 fail-closed；任一通过通道的区域越界、坏 PNG、非唯一、高于门槛的冲突候选或字段无法标准化时，均不得猜测或放行。
4. 补充定向公开/合成回归，覆盖：宽文字默认像素边界、文字单通道通过且图标未通过、图标单通道通过且文字未通过、双通道一致通过、双通道冲突和低于 `0.98` 拒绝。

## 禁止修改与操作

- 禁止降低或绕过 `0.98`、唯一性、稳定帧、视口、哈希、时间戳、冲突检查、`mode=visual_only`、`verification=unverified` 与 fail-closed 规则。
- 禁止修改正式策略、DP、评分、资源模型、GUI、强化决策、资源消耗、Holdout、ADB 帧源、点击执行器和底层装备读取器。
- 禁止 ADB、真实截图、PaddleOCR 真实推理、页面导航、ADB 输入、点击、强化、选材、资源消耗、游戏重启、Fribbels/PCAP/TCP/player_data 读取、联网、上传、模型下载和读取私人归档。
- 禁止将用户截图、裁切图或私人数据写入仓库；不得使用 `git add -A`，不得触碰 `refs/heads/main - 副本` 或 `refs/heads/master - 副本`。

## 执行步骤

1. 只读审阅 `ocr_regions.py`、`ocr_paddle.py`、视觉证据解析器、套装模板匹配器和对应公开测试，确认现有图标/文字字段流向。
2. 以最小改动更新默认宽文字区域和单通道套装字段汇合规则，保持已有 API 兼容。
3. 补充并运行定向公开测试、Python 3.9 语法检查和 `git diff --check`；精确审阅 diff。
4. 在本任务说明和总计划写回完成项、验证、未完成项、风险和下一步；只暂存本任务相关文件，精确提交并推送当前分支。

## 产物

- 宽 `set_text` 正式区域与对应的单通道套装证据实现。
- 定向公开回归测试。
- 本任务说明与总计划的状态记录。

## 停止条件

任何定向测试、语法检查、冲突处理、区域边界、门槛或兼容性验证失败时立即停止并保持 fail-closed。离线实现完成并不授权真实 ADB 校准、点击接线或自动强化；这些都必须另建任务说明并取得明确授权。

## 当前状态

`task_completed_visual_set_single_channel_wide_text_asset_id_mapping_20260727`

用户偏好模型为 `gpt-5.6-luna + max`，当前协作接口未提供 Luna；本任务按项目回退规则实际使用 `gpt-5.6-terra + high`。

完成项：默认 `set_text` 与 PaddleOCR 局部裁切已固定为 `(916,540)-(1120,600)`；视觉证据解析器已实现文字或图标单通道合格即提供标准化套装值，双通道不一致、图标多候选或无法标准化均以 `conflicting:set` / `unrecognized:set` fail-closed。保留 `mode=visual_only` 与 `verification=unverified`。真实 Fribbels 资产 ID 已新增受控映射 `speed -> set_speed -> SpeedSet`；未知或尚未审定的资产 ID 均拒绝，不推断其语义。

验证：公开/合成 `test_ocr_paddle.py`（15）、`test_visual_platform.py`（9）、`test_visual_set_template_matcher.py`（14）与 `test_set_crop_comparison.py`（5）均通过；其中覆盖 `speed` 图标单通道、双通道一致、双通道冲突、低于 `0.98` 与未知资产 ID 拒绝。Python 3.9 `py_compile` 与 `git diff --check` 均退出 `0`。未执行 ADB、真实截图或 OCR、页面交互、资源消耗、底层读取、联网或上传。

未完成项与风险：本任务未授权真实帧校准或真实 OCR 复验，宽裁切的离线结论不得外推为自动化强化授权。除 `speed` 外的 Fribbels 图标资产 ID 尚未在本任务中完成语义审定，继续 fail-closed。后续如需真实环境验证或扩展资产 ID 映射，必须先建立独立任务说明并取得明确授权。
