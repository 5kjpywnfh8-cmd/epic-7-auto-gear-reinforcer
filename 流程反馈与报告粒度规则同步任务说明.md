# 流程反馈与报告粒度规则同步任务说明

## 目标

将已批准的流程反馈、任务说明和独立报告的适用边界写入 `AGENTS.md`，使后续会话与子 agent 采用一致且不过度膨胀的文档粒度。

## 范围

- 只修改 `AGENTS.md` 的“文档同步”规则。
- 同步更新 `e7-gear-enhance-plan.md` 的任务状态与结果链接。

## 禁止修改项

- 不修改代码、测试、策略、GUI、OCR、自动化、数据或既有任务说明的技术内容。
- 不删除既有审计、可执行任务说明或报告分类要求。

## 输入与权威口径

- 用户于 2026-07-30 批准将“流程反馈与报告粒度”规则写入 agent 相关文档。
- `e7-gear-enhance-plan.md` 的“流程反馈与报告粒度（2026-07-30）”为本轮规则内容来源。

## 执行步骤

1. 在 `AGENTS.md` 的“文档同步”段增加精简、可执行的粒度条款。
2. 保持“讨论结论同步计划”“可执行任务先建任务说明”“报告区分结果性质”的原有约束。
3. 将完成状态、验证结果和下一步回写本任务说明与总计划。

## 产物

- 更新后的 `AGENTS.md`。
- 更新后的 `e7-gear-enhance-plan.md`。

## 验证要求

- 运行 `git diff --check` 检查本轮 Markdown 改动。
- 人工核对规则明确区分讨论记录、可执行任务说明与独立报告。

## 停止条件

- 规则已写入且不与既有文档同步、任务授权和结果分类约束冲突。
- 若规则需要扩大到代码或外部流程，停止并另建任务说明。

## 当前状态

`completed`。已在 `AGENTS.md` 的“文档同步”段写入三层文档粒度：讨论记录、可执行任务说明和独立报告；既有审计、授权和报告结果分类规则保持不变。验证：人工核对条款覆盖目标边界，`git diff --check` 退出码为 `0`（仅有既有 LF/CRLF 提示）。实际执行模型：`gpt-5.6-terra + medium`；用户偏好 `gpt-5.6-luna + max`，但当前协作接口未提供 Luna，按项目回退规则记录此替代。
