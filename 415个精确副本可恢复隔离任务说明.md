# 415 个精确副本可恢复隔离任务说明

## 当前状态

`completed_verified`

## 目标

将只读盘点中已确认与原件 SHA-256 完全一致的 `415` 个副本移出工作区，保留原相对路径、源文件哈希和恢复映射，降低工作区索引压力；不删除文件，不覆盖任何目标文件。

## 授权范围

- 用户已确认目标目录为：`C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\`。
- 源目录为当前工作区：`C:\Users\orangine\Documents\第七史诗强化装备脚本\`。
- 仅处理 `reports/workspace_duplicate_inventory_20260725.json` 中 `classification=exact_match` 的 `415` 项。
- 目标目录中按源文件相对路径建立同样的目录结构；每个源文件移动后都必须能按 manifest 恢复。
- 3 个大小分叉副本、`.git` 内部条目、当前 `31` 个已跟踪代码/文档改动、未列入报告的深层文件和任何非候选文件均排除。

## 禁止修改项

- 不删除源文件，不覆盖目标文件，不合并文件，不修改原件，不修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化规则。
- 不处理内容分叉项，不修复或删除 `main - 副本`、`master - 副本` 等 Git 内部引用。
- 不运行 `git clean`、`git reset`、`git restore`、`git gc`、`git prune`，不修改 Git 配置、忽略规则或索引设置。
- 不结束 OpenCode、Codex、编辑器、索引器或其他用户进程。
- 目标目录若已存在、非空、包含联接/重解析点，或任一源文件/原件哈希与报告不一致，立即 `fail_closed`，不移动任何文件。

## 输入与权威口径

- 本文档是实际隔离操作的唯一权威来源。
- 候选来源：[工作区副本只读盘点 JSON](reports/workspace_duplicate_inventory_20260725.json)；只接受其 `exact_match` 项，不凭名称重新推断候选。
- 盘点过程和未处理范围见：[工作区副本盘点与非破坏性整理任务说明](工作区副本盘点与非破坏性整理任务说明.md)。
- 项目规则和总计划分别见：[AGENTS.md](AGENTS.md) 与 [e7-gear-enhance-plan.md](e7-gear-enhance-plan.md)。

## 执行步骤

1. 读取 JSON 报告并验证 schema、候选数 `415`、每项源文件和对应原件存在、源/原件 SHA-256 与报告一致、目标父目录可写且隔离目录不存在。
2. 生成隔离 manifest，记录源相对路径、目标相对路径、源大小、源 SHA-256、原件相对路径和原件 SHA-256；状态为 `planned` 时不得移动。
3. 创建唯一目标目录及必要的子目录；每次移动前检查目标文件不存在，使用明确的绝对路径，不使用通配符。
4. 逐项移动 415 个候选文件；任何失败、哈希变化、目标冲突或源路径异常立即停止并保留已完成项清单。
5. 移动后逐项验证目标文件存在、目标 SHA-256 等于源 SHA-256、源路径不再存在、原件 SHA-256 未变化；生成结果报告。
6. 更新本文档与总计划为实际结果。只有结果验证通过后才允许恢复新功能开发。

## 产物

- `reports/workspace_isolation_manifest_20260725.json`
- `reports/workspace_isolation_result_20260725.json`
- `reports/workspace_isolation_result_20260725.md`
- 本文档与 `e7-gear-enhance-plan.md` 的状态同步记录。

## 验证要求

- 预检必须为 `415/415` 源文件存在、`415/415` 源哈希一致、`415/415` 原件哈希一致、目标冲突 `0`、联接/重解析点 `0`。
- 完成后必须为 `415/415` 目标存在且哈希一致、`415/415` 源路径不存在、`415/415` 原件保持一致。
- 必须保留源到目标的逐项恢复映射；不允许仅凭目录或文件名数量判定成功。
- Git 已跟踪改动集合、暂存区和 HEAD 不得因移动操作发生变化；移动操作本身不应将隔离文件混入当前 PR。

## 停止条件与恢复

- 预检任何一项失败时不创建目标目录、不移动文件。
- 执行中断时立即停止，不自动删除或回滚；依据 manifest 逐项核对已移动和未移动项，再建立新的恢复或继续任务。
- 目标文件冲突、文件被占用、权限不足、哈希不一致、磁盘空间不足或工作区明显卡顿时立即 `fail_closed`。
- 当前状态为 `authorized_preflight_pending; move_not_started`，完成预检并实际移动前不得声称已整理完成。

## 移动前预检通过（2026-07-25）

- 目标目录 `C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\` 不存在，父目录存在且目标位于工作区外。
- 报告中 `415` 个 `exact_match` 项全部通过源文件存在、对应原件存在、源/原件 SHA-256 与报告一致、目标路径无冲突检查；共核验 `830` 个哈希。
- Git 既有已跟踪改动仍为 `31` 个，暂存区为 `0`；预检未创建目标目录、未移动、未删除、未覆盖任何文件。
- `reports/workspace_isolation_manifest_20260725.json` 已生成，状态为 `planned`；实际移动完成前不得把任务写成已整理完成。

## 可恢复隔离移动完成并验证（2026-07-25）

- 按 manifest 移动 `415` 个候选至 `C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\`，实际移动 `415/415`，无冲突和错误。
- 移动后 `415/415` 目标文件存在且 SHA-256 一致，`415/415` 源路径已消失，`415/415` 对应原件保持一致；移动前后共核验 `1660` 个哈希。
- 结果见 [workspace_isolation_result_20260725.json](reports/workspace_isolation_result_20260725.json) 与 [Markdown](reports/workspace_isolation_result_20260725.md)；manifest 已更新为 `completed_verified`。
- 未删除、覆盖文件，未修改 Git 配置、引用、索引、正式策略或用户进程。恢复操作必须另建任务并重新授权。
- 当前状态更新为 `completed_verified`。剩余 3 个分叉副本、`.git` 内部条目、31 个既有改动和未盘点深层文件不在本次整理内。
