# 深层副本 SHA-256 盘点与可恢复隔离预检

生成时间：`2026-07-25T01:47:32.3220936+08:00`

## 盘点范围

- 范围：工作区全部文件，排除整个 `.git` 目录。
- 判定方式：疑似副本必须存在同目录原件；同时比较文件大小和两侧 SHA-256。
- 逐项清单：[CSV manifest](worktree_cleanup_deep_duplicate_manifest_20260725.csv)。
- 汇总：[JSON](worktree_cleanup_deep_duplicate_summary_20260725.json)。

## 结果

- 疑似副本：`127762` 个，合计 `1096187413` 字节。
- SHA-256 完全一致：`127759` 个，合计约 `1096` MB；这些仅进入可恢复隔离候选。
- 内容分叉：`3` 个，均为根目录任务计划/任务说明副本，保留原位，不移动、不删除、不覆盖。
- 原件缺失：`0`。
- `.git` 内另有 `52` 个名称含副本条目，已完全排除，不作为工作区清理对象。

## 内容分叉清单

| 疑似副本 | 原件 | 副本字节 | 原件字节 | 处置 |
| --- | --- | ---: | ---: | --- |
| `e7-gear-enhance-plan - 副本.md` | `e7-gear-enhance-plan.md` | 295322 | 312572 | 保留，待人工审阅 |
| `单件人工确认强化结果离线汇总器任务说明 - 副本.md` | `单件人工确认强化结果离线汇总器任务说明.md` | 4392 | 5904 | 保留，待人工审阅 |
| `单件人工确认强化结果验证任务说明 - 副本.md` | `单件人工确认强化结果验证任务说明.md` | 14644 | 26347 | 保留，待人工审阅 |

## 可恢复隔离预检

- 目标目录：`C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\`。
- 目标目录已存在，已有 `415` 个已授权精确副本。
- 新 manifest 中 `127759` 个 `exact_match=true` 相对路径与目标现有文件冲突：`0`。
- 移动方式：保留相对路径，把源文件移动到目标目录；不删除原件、不覆盖目标、不改变文件内容。
- 恢复方式：按 manifest 将目标文件移回对应源路径，并在恢复后重新校验 SHA-256。
- 失败条件：哈希变化、源文件缺失、目标冲突、权限/空间不足、出现联接点或任一步异常，立即停止并保留已处理清单。

## 处置边界

- 允许处理：仅 manifest 中 `exact_match=true` 且排除 `.git` 的 `127759` 个文件。
- 不处理：3 个内容分叉副本、`.git` 内条目、所有非副本未跟踪文件、已跟踪修改、私人数据的远程上传。
- 本操作是工作区外可恢复隔离，不是永久删除；隔离目录保留，后续不得自动清空。

## 当前状态

`exact_duplicate_isolation_completed_verified`

## 隔离完成结果

- 已按 manifest 可恢复移动并逐项验证 `127759/127759` 个 exact-match 文件。
- 每项均通过：源移动前 SHA-256 与 manifest 一致、目标文件 SHA-256 一致、源路径消失、目标无冲突。
- 工作区现只剩上述 `3` 个内容分叉副本；没有删除、覆盖或修改原件。
- 移动结果清单位于工作区外：
  `C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\worktree_cleanup_deep_duplicate_move_result_20260725.csv`。
- 由深层副本导致的审计失败已复现并消除：`test_historical_bridge_keeps_v3_v4_baseline_and_resource_conservation` 现在 `1/1` 通过。
