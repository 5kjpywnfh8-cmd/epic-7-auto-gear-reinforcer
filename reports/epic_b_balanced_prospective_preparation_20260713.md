# Epic 均衡候选前瞻验证准备（2026-07-13）

## 结论

- `B_global_current_gs` 在显式维度裂缝/圣女联合资源池的全部已要求敏感性中保持第一，可继续盲收新的 48 件独立样本；尚未发布为正式策略。
- 前瞻批次 `epic_b_balanced_prospective_20260713` 已冻结，状态为 `collecting_blind`，当前 `0/48`。没有读取或保存人工判断。
- 研究 lambda 为 `0.0022851391`（`437.6101` 体力/终局价值），仅锁定研究 Oracle，未写入 `DP_ASSIST_CONFIGS`。它重算旧 45 件 Oracle 后动作变化为 `0`。

## 冻结与去重

- 冻结 A/B/C 的规则源哈希、预测函数哈希与代码版本哈希；入批时只保存 A/B/C 动作预测。
- 导入必须带时区的 `exported_at`，且晚于冻结时点；仅接受 `normal_85 Epic +0/+3` 非速度装备。
- 与旧 345 件及本批次同时按实例 ID 和规范化状态指纹去重；速度硬路线、非法输入和重复项不占 48 件名额。

## 22 速与联合排序

- 终局非鞋速度 `<22` 没有独立速度价值；`>=22` 以现有一速曲线的增量为斜率，并以正式第二档锚点作为 22 速下限。
- 从第 6.4 节正式公式提取的第二档敏感性锚点为低/中/高 `1/9/11`；基线为原曲线的 22 速 `5` 分下限。
- 5 个 seed、299 Epic、80 Heroic、每件累计 5,000 轨迹、3,790 个原子分片。固定资源口径为 `85` 来源体力、`0.425` 来源下级石、`127,500` 来源金币和一次圣女 3-7 瓶颈。
- 在 4 个速度锚点、Heroic 基础回退/全停和 Heroic 产出率基线/±20% 的 24 个场景中，B 均第一；最窄 B-第二名 95% 成对区间为 `[0.000452, 0.001400]`，不跨 0。

## 解盲闸门

- 排序闸门已通过，但批次未收满 48 件，禁止解盲或用旧样本补足。
- 收满后，只能对冻结预测和冻结研究 Oracle 做前瞻对照；不得根据人工标签回调 B、速度映射、lambda 或门槛。

产物：`reports/riftslash_saint_pool_22speed_20260713.json`、`reports/epic_non_speed_dp_oracle_research_lambda_20260713.json`、`samples/epic_b_balanced_prospective_20260713.json`。
