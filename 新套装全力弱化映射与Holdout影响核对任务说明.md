# 新套装全力/弱化映射与 Holdout 影响核对

## 目的

核对新套装“全力”和“弱化”是否已经进入装备名称解析、百里分规则和当前独立 Holdout 的候选分类，避免把样本数量偏少误判为套装未识别。

## 核对结论

- `全力` 使用 `set_rage`，名称别名和内部代码均已存在。
- `弱化` 使用 `set_debuff`，名称别名和内部代码均已存在。
- `全力` 已进入基础百里表的速度支撑、输出、输出（必爆）和半肉（血防）规则。
- `弱化` 已进入基础百里表的命坦（双效）规则。
- 运行时规则加载后，`set_rage` 位于 `output`、`critless`、`bruiserHpDef`，`set_debuff` 位于 `hitTank`；这与基础表一致。

## 当前快照影响

2026-07-18 导入的可信 `fribbels_mumu_reader` 快照包含 2565 件装备，Holdout 接受 6 件。排除原因是历史实例、非 `+0`、范围外、鞋子和速度硬路线；这 6 件中没有全力或弱化样本。快照内全力装备总数为 15，弱化为 0；符合本次 Holdout 范围的 Epic 85 级 `+0` 子集里两者均为 0。

因此，当前 6/128 偏少不是新套装无法识别导致的，不需要修改候选哈希，也不需要作废当前 Holdout。继续收集时仍按冻结规则执行。

## 约束

- 本次只增加映射回归测试和核对记录，不修改正式策略、DP、资源模型、评分公式、GUI 或 Holdout 候选。
- 后续若修改套装映射，必须先更新本文件和基础百里表，再重新计算规则哈希并新建 Holdout 批次；不得在旧冻结批次中混入新规则样本。

## 验证

- 套装映射与规则加载回归测试通过。
- 后续应运行相关测试、语法检查和 `git diff --check`，并将结果追加到总计划。
