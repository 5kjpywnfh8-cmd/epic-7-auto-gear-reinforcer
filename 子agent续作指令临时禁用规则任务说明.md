# 子 agent 续作指令临时禁用规则任务说明

## 目标

临时停止主 agent 在每次回复末尾自动发布子 agent 续作指令，减少未被用户要求的交接文本，同时保留既有文档同步和显式交接格式。

## 范围

- 更新 `AGENTS.md`、`README.md` 与 `e7-gear-enhance-plan.md`。
- 明确自动发布禁用期间的触发条件、恢复条件和文档义务。

## 禁止修改项

- 不修改正式策略、DP、资源模型、评分、GUI、OCR、自动化或 Holdout。
- 不创建、启动、续跑或派发任何子 agent。
- 不取消独立任务说明与总计划同步要求。

## 输入与权威口径

- 用户于 2026-07-21 明确要求：“暂时就禁用自动发布子agent续作指令”。
- 本文档与更新后的 `AGENTS.md` 是临时禁用规则的权威口径。

## 执行步骤

1. 将 `AGENTS.md` 的自动发布要求改为仅在用户明确索要时触发。
2. 保留显式索要后的固定标题和 `text` 代码块格式。
3. 在 `README.md` 和总计划同步当前状态。
4. 执行文本检索和 `git diff --check`。

## 产物

- 更新后的 `AGENTS.md`。
- 更新后的 `README.md`。
- 更新后的 `e7-gear-enhance-plan.md`。
- 本任务说明。

## 验证要求

- 规则中不得再要求在存在下一任务时自动附带续作指令。
- 必须明确只有用户当前轮显式索要时才生成交接指令。
- 必须保留文档同步义务和显式交接时的原格式。
- `git diff --check` 必须通过。

## 停止条件

- 完成上述文档同步与验证后停止。
- 用户未明确恢复前，不得自动发布子 agent 续作指令。

## 当前状态

`superseded_for_internal_dispatch_20260726`：该任务说明仅保留为历史对照。其“禁用自动发布续作指令”的规则继续有效，但不得再被解读为限制内部子 agent 的自动创建、启动、续跑或派发；当前权威说明为《子agent自动执行恢复规则任务说明.md》。
