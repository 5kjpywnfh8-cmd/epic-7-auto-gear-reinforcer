# 超高攻击/生命集中属性体系兼容修复与 v3 重算任务说明

## 状态

`v3_invalid_valid_keys_not_bound_pending_v4_20260720`

本文件是 v3 阶段的历史任务记录，现已被有效词条绑定 v4 任务覆盖。v2 精确研究虽然导入了 `_compatible`，但候选救回路径没有调用它，导致生命集中规则对所有基线停止状态计算并执行救回。

## v3 执行记录

- 研究器：`tools/research_epic_concentration_rescue_exact.py`；schema `3`；全新 resume `reports/epic_concentration_rescue_v3_system_compatible_resume_20260720`。
- 正式分片：`1185/1185`，由 `157` 件 Epic 开发样本、`80` 件 Heroic 联合池和 `5` 个 seed 组成，每件 `10` 条官方主路径；v2 分片、resume、差值和淘汰结论未复用。
- 生命集中阈值：已重铸库存 `P97.5 = 37.7363`；每个 `+6/+9/+12` 节点先读取当前 `selected_candidate_snapshot` 的正式分类、合法性和 `validGroup`，再执行精确概率 DP。
- 五 seed 成对正式价值差（候选减基线）为 `0.000111 ~ 0.001693` 百里分/100体力的 95% 区间；区间下界大于零，状态为 `health_p975_balanced_positive_requires_independent_validation`。
- 每 100,000 总体力的候选增量生命集中装备为 `2.2455 [0.8541, 3.6370]` 件；该数值仅条件于固定研究池，不代表账号全装备产量。
- 每个 seed 均满足裂缝体力 + 圣女体力 = `100,000`；零新增续跑为 `scheduled_shards=0` 且汇总一致。
- 正式策略、DP、评分、资源模型、跳值、GUI、OCR、自动化和 Holdout 均未修改或读取；本轮未进入第三项策略覆盖审计。
- 报告：[v3 人类报告](reports/epic_concentration_rescue_v3_system_compatible_20260720.md)；原始数据：[v3 JSON](reports/epic_concentration_rescue_v3_system_compatible_20260720.json)。
- 验证：体系兼容与 v3 回归 `12` 项通过；语法检查、`git diff --check` 和 `tools/run_all_tests.py` 均通过，统一全量入口真实退出码为 `0` 并输出 `ALL TEST FILES PASSED`。

## 目标

在每个 `+6/+9/+12` 救回节点，先按该状态实际选中的正式候选分类验证体系兼容性，再计算生命 P97.5 精确概率。重跑 `health_p975_balanced` 与正式基线的五 seed 同池比较，确认体系过滤后是淘汰、未决还是值得独立验证。

## 体系兼容契约

- 体系判定必须使用救回节点当时的 `selected_candidate_snapshot(state)`，不能只读取初始装备分类或套装名称。
- 生命集中允许分类：`抗坦`、`纯肉`、`命坦`、`半肉(血防)`、`半肉(通用)`、`半肉(白字)`。
- 生命集中拒绝分类：`输出`、`输出(必爆)`、`双效`、`一速`、`unknown` 及任何未登记新分类。
- 生命集中值只统计所选正式规则认可的生命副属性。若所选规则不认可 `hpFlat`，不得用该词条跨过集中阈值；不得仅因粗粒度 `bruiser` 合组而放行全部生命词条。
- 攻击集中未来只能进入 `输出`、`输出(必爆)` 及明确认可攻击词条的半肉分类；`抗坦`、`纯肉`、`命坦`、`半肉(血防)`、`双效`、`unknown` 默认拒绝。攻击不在本轮 v3 运行范围内，只新增契约测试，后续若研究须另建任务。
- 套装和主属性仍须先通过正式候选的合法性检查；集中属性不能绕过正式套装、部位、主属性或副属性有效性规则。

## 固定控制变量

- 基线保持 `baili-formal-dp-v1-epic-balanced`。
- 生命 P97.5 阈值来源、157 Epic、80 Heroic、5 seed、每件10条主路径、官方概率 DP、Heroic `+12`、一次重铸和联合资源池口径保持不变。
- 只比较 `current_formal` 与 `health_p975_balanced (P>=10%)`。
- 不读取 Holdout，不扩展攻击、OR、aggressive、其他分位数或 `+0/+3`。

## 实现与隔离

1. 先增加失败测试，证明 v2 对输出/双效状态错误放行生命救回，并证明 v3 在概率计算前拒绝不兼容体系。
2. 兼容性判断、集中值和概率 DP 必须绑定同一个 selected candidate；不得用一个体系判定、另一个体系计分。
3. 使用 schema 3、独立 v3 resume、候选哈希和输入哈希。v2 的1,185个分片、动作、汇总与 resume 不得复用为 v3 正式结果。
4. 主路径 seed 可保持相同以作控制变量配对，但必须生成新的候选分片；报告逐分类列出 v2 被错误救回、v3 被过滤和 v3 实际救回的概率质量。
5. 同时修复 v2 主审计发现的报告问题：新增集中装备必须逐 seed 按各自候选总资源换算每10万总体力，再汇总区间；结论文字由 gate 状态生成。

## 必须测试

- 上述生命允许/拒绝分类逐项动作矩阵；未知分类 fail closed。
- 救回节点分类变化时按当前 state 重算，不复用 +0 分类。
- 所选规则不认可的 `hpFlat` 不进入生命集中值；认可时按官方 GS 权重计入。
- 手工可算状态与精确概率 DP 对拍，`0<P<10%` 不救回。
- 五 seed 同池、逐 seed 资源归一、裂缝+圣女=100,000、非负展示和三态 gate。
- v2 resume/schema 拒绝，v3 完整续跑为0新分片且汇总哈希一致。

## 人类报告

- 明确说明 v2 因缺少体系兼容过滤而被覆盖，不得继续引用其淘汰结论。
- 报告每个正式分类的自然状态质量、基线停止质量、v2错误救回质量、v3实际救回质量、正式价值差和新增生命集中装备。
- 报告 Epic/Heroic、套装、部位、救回节点、75+、22速、资源、裂缝/圣女体力和循环。
- 绝对产量继续标记为条件于固定研究池，不代表账号全装备效率。

## 决策闸门

- 五 seed 成对正式价值差区间上界小于0：淘汰体系兼容后的 `health_p975_balanced`。
- 区间跨0：效能未决，另建扩样任务。
- 区间下界大于0：只能进入独立验证设计，不得直接发布。

## 禁止项与完成条件

- 不修改正式策略、DP、评分、资源模型、正式跳值、GUI、OCR、自动化或 Holdout。
- 不复用 v2 正式结论，不把攻击研究并入本轮。
- 生成 v3 JSON、报告、完整新分片、回归测试并更新本任务、V1任务和总计划；定向测试、全量测试、语法检查和 `git diff --check` 全部通过后，交由主 agent 审核。

## 主审计覆盖（2026-07-20）

- v3 的兼容函数返回 `health_valid_keys`，但精确概率、缓存、剪枝和终局命中仍固定统计全部 `hpPct+hpFlat`，没有使用该集合。
- 因半肉血防/通用可能被不属于正式体系的 `hpFlat` 抬过阈值，v3 正向区间、产量和独立验证资格全部降级为失效历史结果。
- 当前唯一下一任务为[超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md](超高攻击生命集中属性有效词条绑定修复与v4重算任务说明.md)。
