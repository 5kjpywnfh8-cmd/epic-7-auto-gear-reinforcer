# 纯视觉列表页会话产物合并收口任务说明

## 当前状态

`merged_visual_list_session_output_into_current_worktree_20260801`

## 目标

把 Codex 会话 `019fa96f-f705-7130-98b6-030938f4f693`（worktree `C:\Users\orangine\.codex\worktrees\e67b\第七史诗强化装备脚本`，detached HEAD at `01b6b75`）的视觉列表页产物并入当前工作区 `c:\Users\orangine\Documents\第七史诗强化装备脚本`（分支 `codex/policy-v1-manifest-20260725`，HEAD at `01b6b75`），建立干净基线，避免未提交产物滞留 worktree 的丢失风险。

## 唯一权威来源

本文档是本轮合并收口的唯一权威任务说明。同时遵守根目录 `AGENTS.md` 与 `e7-gear-enhance-plan.md`。聊天内容不能替代或扩大本文档范围。

## 范围（已并入，未提交）

- 源码 7 个：`src/e7_enhance/visual_list_assets.py`、`visual_list_observer.py`、`visual_list_parser.py`、`visual_list_png_source.py`、`visual_list_production.py`、`visual_list_recognizer.py`、`visual_navigation.py`。
- 测试 8 个：`tests/test_visual_list_assets.py`、`test_visual_list_local_source.py`、`test_visual_list_observer.py`、`test_visual_list_parser.py`、`test_visual_list_png_source.py`、`test_visual_list_production.py`、`test_visual_list_recognizer.py`、`test_visual_navigation.py`。
- 资产目录 `assets/visual/list_reference/`（31 个文件，含原始截图与裁切）。
- 报告 10 份 `reports/visual_list_*.md`。
- 任务说明 21 个（e67b 独有，全为视觉列表页相关）。

## 明确未覆盖（保留当前工作区较新版本）

- `AGENTS.md`、`README.md`、`e7-gear-enhance-plan.md`。
- 共享任务说明：`子agent自动执行恢复规则任务说明.md`、`流程反馈与报告粒度规则同步任务说明.md`、`纯视觉新增等级与套装区域离线置信度校准任务说明.md`、`子agent连续指令禁用规则任务说明.md`、`纯视觉真实ADB目标装备局部校准重试任务说明.md`。
- 上述共享文件当前工作区版本包含更新的「子 agent 自动执行恢复」「流程反馈与报告粒度」规则，禁止用 e67b 旧版覆盖。

## 计划文档合并方式

当前计划 = HEAD(3030 行) + 流程反馈段(2026-07-30)；e67b 计划 = HEAD + 视觉列表段(2026-07-28 至 08-01)。合并结果 = HEAD + 流程反馈段 + 视觉列表段 + 本次合并收口记录；HEAD 前缀完整性已用 diff 验证。当前计划总行数 3382。

## 执行边界

- 本轮仅做文件复制与文档同步，不运行测试、不连接 ADB、不截图、不 OCR、不点击、不导航、不强化、不选材、不确认、不消耗资源、不联网、不上传、不下载模型。
- 未 `git add`、未 `git commit`、未推送；所有并入内容保持未跟踪状态，与项目「不得使用 `git add -A`」等规则一致。
- 未修改 e67b worktree 中的任何内容；合并为单向复制。

## 验证

- 21/21 e67b 独有任务说明复制后逐一确认存在。
- 7 源码、8 测试、31 资产、10 报告复制后确认存在。
- 计划文档 HEAD 前缀 diff 通过；`流程反馈与报告粒度` 段落仍存在；视觉列表段落已并入。
- `git diff --check` 通过（无新增空白错误）。
- 未对任何共享文件执行覆盖。

## 下一步（不在本轮范围）

当前仍处于 fail-closed 阻断：字段映射 manifest `incomplete`（缺 攻击%/暴击率%/效果抗性）、整卡指纹 manifest `missing`、生产 OCR/模板 reader 工厂 `not_configured`。真实三帧校准与点击/导航开发均未授权、未执行。是否继续离线字段映射/整卡模板审定由用户另行决定，需另建任务说明。
