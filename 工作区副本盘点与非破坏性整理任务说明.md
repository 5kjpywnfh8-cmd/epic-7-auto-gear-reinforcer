# 工作区副本盘点与非破坏性整理任务说明

## 当前状态

`partial_direct_inventory_complete_isolation_completed; deeper_unscanned`

## 目标

在不破坏、不移动、不覆盖任何现有文件的前提下，识别工作区大量“副本”文件、未跟踪内容和既有代码改动的来源与关系，降低持续索引造成的卡顿，并为后续恢复正常开发建立明确的权威文件边界。

## 范围

- 第一阶段仅进行分批、限速的只读盘点，不执行实际整理。
- 盘点根目录和各一级目录的文件数量、大小与“`- 副本`”命名分布；避免一次性递归扫描整个工作区。
- 对候选副本分批计算哈希，分类为“完全一致”“内容分叉”“缺少原件”“生成物或缓存”“暂时无法判断”。
- 记录 Git 已跟踪、未跟踪、忽略和既有修改状态；当前 `31` 个已跟踪改动必须保持未暂存、未回退、未覆盖。
- 输出保留、候选隔离和待人工判断清单；清单本身不构成移动或删除授权。

## 禁止修改项

- 不删除、移动、重命名、覆盖、压缩或清空任何工作区文件和目录。
- 不运行 `git clean`、`git reset`、`git checkout`、`git restore`、`git gc`、`git prune` 或其他会改变文件、索引、对象或引用的命令。
- 不修复或删除 `main - 副本`、`master - 副本` 等既有 Git 引用，不修改仓库级或全局 Git 配置、忽略规则和索引设置。
- 不暂存或提交当前 `31` 个既有改动，不把它们混入 batch `013` 草稿 PR。
- 不修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛、自动化规则或模拟器状态。
- 不自动结束 OpenCode、Codex、编辑器、索引器或其他用户进程。

## 输入与权威口径

- 项目规则：[AGENTS.md](AGENTS.md)。
- 总计划：[e7-gear-enhance-plan.md](e7-gear-enhance-plan.md)。
- 本任务的执行边界以本文档为唯一权威来源；聊天只用于授权是否开始只读盘点，以及后续是否执行可恢复隔离。
- batch `013` 已发布证据仍以[单件人工确认强化结果验证任务说明](单件人工确认强化结果验证任务说明.md)及草稿 PR #1 为准，本任务不得改写其结论。

## 执行步骤

1. 使用不枚举未跟踪文件的 Git 状态命令记录盘点前基线，确认当前分支、HEAD、远程和已跟踪改动数量。
2. 只枚举根目录和一级目录；每批控制范围并记录耗时。若系统再次明显卡顿，立即停止，不扩大递归深度。
3. 按目录分批识别“`- 副本`”候选，先比大小，再对大小一致的文件计算 SHA-256；不得仅凭名称认定可删除。
4. 将结果分类并生成机器可读 JSON 与人工审阅 Markdown，列出原件、候选副本、大小、哈希、Git 状态和建议动作。
5. 使用同一只读 Git 状态命令复核盘点前后工作区一致；任何非预期变化立即 `fail_closed`。
6. 向用户提交清单。只有用户逐项授权隔离目标、目标目录和恢复方式后，才允许建立新的实际整理任务；本任务不直接进入移动或删除。

## 产物

- `reports/workspace_duplicate_inventory_20260725.json`
- `reports/workspace_duplicate_inventory_20260725.md`
- 盘点命令、分批耗时、停止记录和 Git 前后状态摘要。

## 验证要求

- JSON 和 Markdown 的候选数量、文件路径、大小、哈希与分类必须一致。
- 每个“完全一致”结论必须有原件与副本双方 SHA-256，不允许仅用文件名或修改时间判断。
- 盘点前后已跟踪文件差异、暂存区和 HEAD 必须一致；产物文档除外，且产物发布前只能精确暂存明确文件。
- 必须明确报告权限拒绝、链接或联接点、读取失败、扫描中断和未完成目录，不能把不完整盘点写成完成。

## 停止条件

- CPU、磁盘或工作区响应再次明显恶化时立即停止当前批次。
- 发现目录联接、符号链接、循环路径、权限异常、文件在读取期间变化或哈希不稳定时立即 `fail_closed`。
- 发现零原件、多原件、内容分叉或无法判断来源时只标记待人工审阅，不移动、不删除。
- 未取得新的逐项整理授权前，达到只读报告产出后立即停止。

## 下一步

等待用户审核直接文件盘点清单。盘点完成并审核清单后，再决定是否建立“可恢复隔离”任务；完成整理闸门后才恢复新的功能开发。

## 分批只读盘点结果（2026-07-25）

- 按本任务授权完成根目录、`e7_enhance`、`manual_acceptance`、`reports` 的一级直接文件盘点；未递归进入更深层目录，避免触发大规模扫描。
- 共发现 `418` 个“`- 副本`”候选：`415` 个与对应原件 SHA-256 完全一致，`0` 个哈希分叉，`0` 个缺少原件，`3` 个大小分叉。分叉项为根目录 `e7-gear-enhance-plan - 副本.md`、`单件人工确认强化结果离线汇总器任务说明 - 副本.md`、`单件人工确认强化结果验证任务说明 - 副本.md`；三者均保留并转人工审阅。
- 一级直接文件统计为：根目录 `165` 个文件/`82` 个副本；`e7_enhance` `6`/`3`；`manual_acceptance` `62`/`31`；`reports` `608`/`302`。未发现目录联接或重解析点。
- 在已发现的 `418` 个候选中，Git 索引跟踪 `0` 个，`.gitignore` 命中 `126` 个，未跟踪且未被忽略 `292` 个。全工作区未跟踪文件扫描未执行，不能将本轮结果表述为全仓库盘点完成。
- Git 盘点前后 HEAD 与远程均为 `5f755af`，已跟踪修改仍为 `31` 个，暂存区为 `0`；`.git` 内另有 `7` 个名称含“副本”的内部条目，按禁止项未修复、未删除。
- 报告：[JSON](reports/workspace_duplicate_inventory_20260725.json) 与 [Markdown](reports/workspace_duplicate_inventory_20260725.md)。本轮未删除、移动、重命名、覆盖文件，未修改 Git 配置、引用、索引，也未结束用户进程。
- 当前状态更新为 `partial_direct_inventory_complete_full_workspace_untracked_scan_deferred; destructive_cleanup_not_authorized`。`415` 个精确副本只能作为可恢复隔离候选；实际隔离前必须另建任务并取得逐项目标、目标目录和恢复方式授权。

## 可恢复隔离任务已授权（2026-07-25）

- 用户已明确确认：只处理报告中 `415` 个 `exact_match` 候选，移至工作区外的 `C:\Users\orangine\Documents\第七史诗强化装备脚本_副本隔离_20260725\`，保留相对路径和恢复清单，不删除源文件。
- 独立操作说明为：[415 个精确副本可恢复隔离任务说明](415个精确副本可恢复隔离任务说明.md)。在该说明完成预检前，不得移动任何文件。
- 3 个内容分叉项、`.git` 内部条目、31 个既有改动和未盘点深层文件继续排除；目标目录存在、非空、冲突、联接点或任一哈希不一致时立即 `fail_closed`。
- 当前状态更新为 `partial_direct_inventory_complete_isolation_task_defined; destructive_cleanup_not_started`。

## 可恢复隔离移动前预检通过（2026-07-25）

- 独立隔离任务的目标目录不存在且位于工作区外；415 个 exact-match 候选、415 个对应原件均存在，830 个源/原件哈希全部与只读报告一致，目标冲突为 `0`。
- 隔离 manifest：[workspace_isolation_manifest_20260725.json](reports/workspace_isolation_manifest_20260725.json)，当前状态为 `planned`。
- 预检未创建目标目录，未移动、删除、重命名、覆盖文件；Git 既有 31 个改动和暂存区均保持不变。
- 当前状态更新为 `partial_direct_inventory_complete_isolation_preflight_passed; move_pending`。下一步仅执行 manifest 范围内的可恢复移动和移动后哈希验证。

## 415 个精确副本隔离完成（2026-07-25）

- 已按独立隔离任务将 `415` 个 exact-match 副本移至工作区外目标目录；目标文件、源路径和原件逐项验证通过，错误/冲突均为 `0`。
- 结果报告：[JSON](reports/workspace_isolation_result_20260725.json) 与 [Markdown](reports/workspace_isolation_result_20260725.md)；逐项恢复映射见 [manifest](reports/workspace_isolation_manifest_20260725.json)。
- 本轮没有删除或覆盖文件，未修改 Git 配置、引用、索引、正式策略或用户进程。3 个分叉副本、`.git` 内部条目、31 个既有改动和未盘点深层文件均保留/排除。
- 当前状态更新为 `partial_direct_inventory_complete_isolation_completed; deeper_unscanned`。工作区整理闸门已完成，可恢复功能开发；不自动处理剩余未盘点文件。
