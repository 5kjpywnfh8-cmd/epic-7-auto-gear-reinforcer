# 真实样本 GS 与满值转换任务说明

## 目标

修正真实样本管理器及早期候选 debug 的 GS 口径：新增当前重铸前有效 GS；转换候选按转换目标属性的满值计算转换后终局 GS，并用于转换候选的达标概率和同层排序。保留未转换原生值，确保结果可解释。

本任务只处理 GS 计算、早期候选概率/排序、debug、GUI 展示和测试。不得接入 OCR、ADB、Airtest 或自动点击，不做无关重构。

## 最新权威规则

1. 旧规则“转换后数值按期望均值估算”废止，改为按转换满值计算。
2. 转换满值必须取该目标属性在现有转换模型下、对应最终强化次数的合法最高值，不得继续调用平均跳值作为转换结果。
3. 若项目已有独立转换石数值表，必须使用该表的上界；不得把 `normal_85` / `rift_85` 普通强化跳值表误当成转换石表。若项目确实没有独立表，必须明确记录当前采用的满值上界来源和限制，不得静默编造数值。
4. 转换候选的终局 GS 使用“剩余强化路径完成后，再把唯一未命中属性转换为目标属性满值”的口径。
5. 必须同时保留未转换原生终局 GS，不能只显示转换后的乐观值。
6. `+0/+3` 三条命中、一条可转换仍为 `cautious_continue`；转换满值可以影响转换候选达标概率和同层排序，但不能绕过现有“四条完整命中 + 已发布校准规则”自动继续门槛。
7. 输出/坦克与双效/半肉的层级优先级保持不变；同一优先级层内按纳入满值转换后的终局达标概率排序。

## 字段定义

每个“体系 x 部位”候选输出：

- `current_pre_reforge_gs`：当前重铸前有效 GS，只累计当前属于候选体系的有效副属性，不含重铸、未来强化和转换。
- `current_reforged_gs`：当前重铸后有效 GS。85 级加入当前重铸收益，90 级不得重复重铸；85 级不受旧 `reforgeEligible` 值影响。
- `expected_final_gs_native`：终局预期有效 GS（未转换），即现有剩余强化 + 85 级重铸路径，不含转换。
- `conversion_max_gs_gain`：把唯一未命中属性转换为目标属性满值后增加的候选体系有效 GS。
- `expected_final_gs_after_max_conversion`：终局预期有效 GS（按转换满值）。非转换候选必须为 `null`，不能用 0 表示“不适用”。
- `expected_final_gs`：候选实际用于达标概率的终局 GS。转换候选取 `expected_final_gs_after_max_conversion`，非转换候选取 `expected_final_gs_native`。
- `terminal_reach_probability_native`：未转换原生达标概率。
- `terminal_reach_probability`：实际候选达标概率；转换候选必须按满值转换后的终局路径计算。
- 转换属性、转换目标、满值、满值来源和计算假设。

`official_score` 继续表示当前全副属性官方总分；上述 GS 均是“按候选体系的有效 GS”，GUI 中文必须明确区分。

## 终局强化与转换计算

1. 转换发生在终局：先处理所有剩余强化事件，再按待转换属性的最终强化次数取目标属性的转换满值。
2. 未来强化可能再次命中待转换属性，计算时不得永远固定使用当前 rolls。应按合法强化事件分布处理该属性的最终 rolls。
3. 不允许为了展示满值而调用完整早期精确 DP，导致 `+0/+3` GUI 明显变慢。应复用现有状态分布或建立只针对待转换属性命中次数的小型精确分布。
4. Heroic 必须遵守 `+12` 才补第 4 条；Epic 五个节点均强化既有副属性。normal_85 与 rift_85 的普通强化跳值保持独立。
5. 转换结果取满值，不再对转换石自身结果取均值；剩余强化仍按现有终局预期/概率模型处理。

## GUI 与 debug

真实样本详情至少中文显示：

- 当前全副属性官方总分；
- 当前重铸前有效 GS（按候选体系）；
- 当前重铸后有效 GS（按候选体系）；
- 终局预期有效 GS（未转换）；
- 终局预期有效 GS（按转换满值）；
- 未转换达标概率、按满值转换后的达标概率；
- 待转换属性、目标属性、转换满值、满值 GS 增益及数值来源。

非转换候选显示“不适用”，不要显示虚假的 0 分。旧记录缺少新字段时仍应能打开；重新计算/导入后补齐，不得破坏人工审核结论。

## 执行要求

1. 先复现 `建议结果/14.json`，证明当前 `expected_final_gs` 未计转换，且当前没有重铸前候选有效 GS。
2. 先新增最小失败测试，再修改代码。
3. GS 公式必须复用评分模块的标准化值和官方权重；不得在 GUI 复制公式。
4. 若新增转换满值表，独立建模并严格区分普通强化跳值表；先用测试锁定速度、百分比和固定值代表项。
5. 更新策略 debug、`debug_view_model`、真实样本 JSON/CSV/详情窗口和兼容读取路径。
6. 不得改变 07 的多跳速度硬路线，也不得让三条命中转换候选因满值直接自动继续。
7. 更新 README、规则审阅、人工验收说明和计划文档。
8. 运行相关测试及全量测试。

## 最低回归

- 当前重铸前 GS 不含重铸收益；85 级当前重铸后 GS正确增加收益；90 级重铸前后相同。
- 85 级 `reforgeEligible=true/false` 的 GS、概率和建议一致。
- 无效副属性不计入候选有效 GS，但仍计入全副属性官方总分。
- 非转换候选的转换后 GS 为 `null`，实际终局 GS 等于原生终局 GS。
- 转换候选使用对应目标属性满值，不再使用期望均值；实际终局 GS 等于按满值转换后的终局 GS。
- 未来强化命中待转换属性时，转换满值按最终 rolls 正确变化。
- `14.json` 仍为“谨慎继续 / 输出候选”，并同时展示原生与满值转换后的 GS/概率。
- `07.json` 保持原建议与路线。
- 真实样本人工结论保存、重启后不丢失。
- 相关测试和全量测试通过。

## 最终报告

- 旧值未计转换的代码证据；
- 转换满值表或满值来源；
- 当前重铸前/后、原生终局、满值转换终局的公式；
- 14 修复前后 GS、概率、排序和建议差异；
- 07 回归结果；
- GUI/持久化验证；
- 测试结果及剩余规则风险。
