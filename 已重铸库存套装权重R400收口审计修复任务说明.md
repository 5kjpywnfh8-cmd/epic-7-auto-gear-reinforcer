# 已重铸库存套装权重 R400 收口审计修复任务说明

## 状态

`r800_shards_complete_report_superseded_pending_weighting_fix`

## 背景与审计结论

R400 的 760/760 个新分片、五层桥接和四策略 JSON 已完成，库存主权重完整联合线路当前策略为 `8564.05 [7861.09, 9312.08]` 体力/正式百里分，R400 单独计算的相对半宽为 `8.5%`。但主 agent 收口审计确认原任务尚有三项未满足，不能标记为完全完成：

1. 原任务要求最终四策略聚合同时读取 R50 旧分片和全部新增独立分片；当前研究器只在 `r50_reference` 中记录 R50 路径，没有读取 R50 数据参与最终聚合。
2. 原始 JSON 已计算 `top4_equal`，且方向与库存主权重一致，但人类报告只输出 `uniform_observed_sets` 和 `reforged_inventory_all`，遗漏主要敏感性表。
3. 原任务要求报告原生 75+、转换 75+ 和 22 速；当前分片虽保存这些字段，最终 ledger、聚合和人类报告没有输出。

R200 是同一批 20 个 block 的 R400 前缀，不得与 R400 重复相加。R50 保持只读；初始修复阶段不生成新轨迹、不运行 R800。

## 目标

- 新增 R50 只读适配与哈希/schema/完整性检查，把 5 个 R50 seed cluster 与 20 个 R400 block cluster 合并用于四策略最终比较。
- 保留 R400 五层旧口径桥接；若 R50 缺少发布旧口径分子，不得伪造桥接字段，只在最终完整联合四策略比较中合并，并在报告中明确范围。
- 在库存主权重和 `top4_equal` 下完整输出四策略的绝对成本、每 10 万总体力正式百里分、成对差值、裂缝/圣女体力、循环数、原生 75+、转换 75+ 和 22 速。
- 重新计算合并后的 cluster ESS、最大单 cluster 贡献和正值约束 95% 区间。
- 只有合并后库存主权重当前策略的绝对成本相对半宽仍 `<=10%`，才保留“停止 R800”结论；否则按原任务阶梯继续 R800。

## 实现约束

- 先补失败测试，至少覆盖：R50 实际进入合并聚合、R200 不重复计入、缺失或哈希不一致的 R50 分片拒绝、top4 写入 Markdown、三项终局产量按每 10 万总体力换算。
- R50 目录和 R400 分片全部只读，不覆盖旧报告或旧 JSON；修正结果使用新 schema 和新文件名。
- `uniform_observed_sets` 仅作机制对照；主排序固定为 `reforged_inventory_all`，`top4_equal` 为主要敏感性。
- 继续比较当前正式、全局 12/17、输出 8/13+纯坦 10/17、输出 8/13+纯坦 12/17；共同路径成对区间不得改为独立样本区间。
- 不冻结候选、不启动或读取 128 件 holdout，不修改正式策略、DP、资源模型、评分、跳值、GUI、OCR 或自动化。

## 产物

- 修正后的研究器与专用测试。
- 独立的 R400 收口修正 JSON 和人类报告；不得静默覆盖原 R400 结果。
- 更新本任务、原扩样桥接任务和 `e7-gear-enhance-plan.md`，明确旧 R400 报告仅是修正前结果。

## 验证

- 定向测试先红后绿。
- 语法检查、`git diff --check`、`tools/run_all_tests.py`，记录真实退出码。
- 复跑时确认没有生成新轨迹；若合并后触发 R800，必须先把原因和预计分片数写回本任务后再执行。

## R800 触发记录（2026-07-18）

- 失败测试已补齐并通过，随后只读验证并合并 R50 的 5 个 seed cluster 与 R400 的 20 个 block cluster；R200 未读取且计数为 0，未生成任何新轨迹。
- 合并后库存主权重当前正式策略为 `7849.35` 体力/正式百里分，正值约束 cluster bootstrap 95% 区间 `[6707.59, 9118.88]`，相对半宽 `15.36%`，超过 `10%` 闸门。因此“停止 R800”的旧结论被本审计覆盖。
- 现在按原阶梯恢复 R800：预计生成 `20 block × 19 set × 2 rank = 760` 个新 `r800` 原子分片；每个 block/set/rank 为 800 条条件样本。R800 自身包含相同 block 的前缀样本，不得与 R200 或 R400 相加。R50/R200/R400 目录和旧报告保持只读。
- R800 完成后必须重新执行本审计器，读取 R50+R800 而非 R50+R400，重新判定 10% 闸门；不得混合阶段分片。

## R800 完成记录（2026-07-18）

- 已生成 `760/760` 个 `r800` 分片，并只读合并 R50+R800；R200 和 R400 均未纳入。新审计报告已包含库存主权重与 `top4_equal` 的四策略表、裂缝/圣女体力、循环、原生75+、转换75+和22速。
- 库存主权重当前正式策略为 `7811.16` 体力/正式百里分，95% 区间 `[6674.63, 9031.69]`，相对半宽 `15.09%`；`top4_equal` 当前策略为 `6605.05 [5747.09, 7607.74]`，相对半宽 `14.09%`。两者均未通过 10% 闸门。
- 本任务未授权 R1600，故停止于 R800，不生成后续轨迹、不冻结候选、不启动 holdout。下一阶段若要继续，必须先建立新的任务说明，明确 R1600 的分片规模、R50+R1600 的只读审计口径和停止条件。
- 修正产物：`reports/reforged_inventory_set_weight_r800_closure_audit_20260718.json`、`reports/reforged_inventory_set_weight_r800_closure_audit_20260718.md`；旧 R400 结果仅作修正前历史对照。

## 最终验证（2026-07-18）

- 审计器、旧桥接研究器与资源模型相关定向测试共 28 项通过；审计专用回归覆盖 R50 合并、R200 排除、分片完整性和哈希拒绝、`top4_equal` 与三项终局产量输出。
- `python -B -m py_compile tools/research_reforged_inventory_set_weight_r400_audit.py tests/test_research_reforged_inventory_set_weight_r400_audit.py` 退出码 `0`；`git diff --check` 退出码 `0`。
- `python -B tools/run_all_tests.py` 真实退出码 `0`，输出 `ALL TEST FILES PASSED`。其中 `test_gui_app.py::real_sample_import_selects_its_acceptance_batch` 首次出现既有 Qt 原生退出，隔离重试后通过；不影响全量结果，未修改 GUI。
- 当前状态保持 `r800_complete_absolute_cost_still_unstable_next_stage_not_authorized`：R50+R800 的库存主权重相对半宽为 `15.09%`，但本任务未授权 R1600，故不继续生成轨迹。

## 后续修复覆盖（2026-07-18）

- 本文的 `7811.16 [6674.63, 9031.69]`、`15.09%`、资源拆分和“仍需下一扩样阶段”均已被 [R800聚合权重与资源缩放修复任务说明](已重铸库存套装权重R800聚合权重与资源缩放修复任务说明.md) 覆盖。根因是 R50/R800 不等 sample_mass 被等权聚合，以及资源换算重复乘入 cluster 数。
- R800 分片本身继续有效；修复后的只读结论以 schema 2 的 `reports/reforged_inventory_set_weight_r800_weighted_closure_audit_20260718.{json,md}` 为准。不得根据本旧审计授权 R1600。

## 主 agent 二次收口审计（2026-07-18）

- R800 的 760/760 分片保留有效，但合并器把每 cluster 先归一化后等权相加，使 R50 在点估计中占20%，而按路径质量应仅占1.53846%；`7811.16`及15.09%区间因此作废。
- 每十万裂缝/圣女体力合计为2,500,000，确认资源拆分重复乘入25个cluster；三项终局产量虽未使用同一错误公式，仍须随新聚合统一复核。
- 只读 R800-only 诊断的库存主权重相对半宽为7.17%，表明当前不能依据作废的15.09%直接授权R1600。修正任务：[已重铸库存套装权重R800聚合权重与资源缩放修复任务说明.md](已重铸库存套装权重R800聚合权重与资源缩放修复任务说明.md)。
