# 纯视觉套装与强化证据真实 ADB 单帧校准任务说明

## 目标

在用户已将 MuMu 停留于目标装备详细页并明确授权后，使用上一阶段收窄后的套装与强化证据区域，执行一次真实、只读、内存内的 ADB PNG 单帧校准和 PaddleOCR 复验。仅判断视觉字段是否通过现有证据契约；结果固定标记 `mode=visual_only`、`verification=unverified`。

## 唯一权威来源

本文件是本轮唯一权威任务来源。ADB 设备发现和只读帧源继承《纯视觉 ADB 截图与受控点击迁移任务说明.md》；右侧详情布局继承《纯视觉右侧装备详情面板裁剪与 ADB 原始帧审查任务说明.md》；本轮区域与解析契约继承《纯视觉套装局部裁剪与强化证据锚点修正任务说明.md》及当前 `src/e7_enhance/ocr_paddle.py`。

## 授权范围

- 用户已确认目标装备详细页处于打开状态，并批准本次只读复验。
- 只运行 MuMu 官方 `adb.exe devices -l`，只接受自动发现且状态为 `device` 的唯一设备。
- 只执行一次由自动发现设备派生的 `exec-out screencap -p`。
- PNG 仅在内存中校验结构、CRC、视口、非黑屏、字节数和 SHA-256；仅对右侧详情、套装与强化局部区域调用一次本地 PaddleOCR。
- 复用 `0.98` 字段置信度门槛，不降低、不重试、不人工补齐缺失字段。

## 本次目标指纹

- 部位：武器；品质：英雄；等级：85；套装：命中套；强化：`+0`。
- 主属性：攻击力 `100`。
- 副属性：暴击率 `5%`、攻击力 `8%`、效果抗性 `8%`。
- 官方分数：`28`。

## 禁止修改与禁止操作

- 禁止第二次截图和自动重试；旧帧哈希仅作历史对照，不得单独据此判断页面未刷新。
- 禁止 ADB `input tap`、`swipe`、`keyevent`，禁止页面导航、点击、强化、选材、重启和资源消耗。
- 禁止截图落盘、外部上传、Fribbels、PCAP、TCP、`player_data.json` 和装备底层读取。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR `0.98` 门槛、Holdout、自动化规则、帧源和点击器。
- 不读取、恢复或暂存私人归档；不触碰 `refs/heads/main - 副本`；不使用 `git add -A`。

## 执行步骤

1. 在全新 Python 3.9.2 进程中配置已批准的 ASCII PaddleOCR 模型缓存，确认本地模型可用；失败立即停止，不执行 ADB。
2. 运行官方 MuMu `adb.exe devices -l`；设备不是唯一 `device` 时停止且不重试。
3. 执行一次 `exec-out screencap -p`，仅在内存中完成 PNG/视口/非黑屏/哈希校验。
4. 在同一内存帧上按当前详情、套装和强化候选区裁剪，执行一次本地 PaddleOCR；使用当前解析器和 `0.98` 门槛输出字段与拒绝原因。
5. 将设备、PNG 元数据、局部尺寸、OCR 行、字段置信度、目标指纹比对、停止原因和禁止操作核对写回本文件与 `e7-gear-enhance-plan.md`。
6. 运行必要的离线定向测试、语法检查和 `git diff --check`；精确审阅后只暂存本任务文件及总计划，提交并推送当前分支。

## 产物与验证要求

- 只记录非图像元数据和字段证据，不保存私人截图或游戏数据载荷。
- 明确区分识别通过、低置信度、字段缺失和字段矛盾；任一目标字段未通过即整体 fail-closed。
- 单帧结果不是稳定帧证据、服务器确认或强化授权。
- 执行模型记录为 `gpt-5.6-terra + high`。

## 停止条件

依赖、模型、设备、PNG、视口、非黑屏、区域、OCR、字段、置信度或目标指纹任一环节失败立即停止，不重试；任何输入、第二帧、点击或游戏操作均须另建任务说明并取得明确授权。

## 当前状态

`real_adb_single_frame_target_fields_matched_set_enhance_fail_closed_20260726`

## 执行记录（2026-07-26）

- 执行模型：`gpt-5.6-terra + high`；Python `3.9.2`、`paddle 2.6.2`、`paddleocr 2.10.0`；已批准 ASCII 缓存中的本地模型命中。
- 官方 MuMu ADB 自动发现唯一 `emulator-5554 device`，未写死或传入网络地址；只执行一次 `exec-out screencap -p`，没有第二次截图或自动重试。
- PNG 仅在内存中通过结构、CRC、非黑屏和视口校验：`1280x720`、`859033` 字节、SHA-256 `50499822de5a2b297f2dbb23489bf3461903cca73988c458afd6c10cd0cc2e3e`。该哈希不同于前次 `33af39341a808005be8742c0384a52e9b291b4680fef0c189148bc21ec8e6591`，仅证明取得了不同帧，不作为服务器状态证据。
- 本轮实际局部：右侧详情 `(768,58)-(1242,619)`，送 OCR 为 `948x1122`；套装 `(819,547)-(1216,619)`，送 OCR 为 `2382x432`；强化 `(819,115)-(1216,180)`，送 OCR 为 `2382x390`。共返回 `35` 行，截图和裁剪均未落盘。
- 通过 `0.98` 门槛并与目标指纹一致的字段：英雄武器 `0.999633`、85级 `0.999217`、主属性攻击力 `100`（字段最低 `0.998921`）、暴击率 `5%`（最低 `0.999182`）、攻击力 `8%`（最低 `0.999631`）、效果抗性 `8%`（最低 `0.999034`）、装备分数 `28`（`0.999905`）。
- 套装文本可规范化为命中套，但详情候选最高置信度仅 `0.906965`，收窄套装局部为 `0.905730`，均低于 `0.98`；强化局部没有识别到 `+0` 或 `exp0/...`。解析结果为 `accepted=false`，拒绝原因 `low_confidence:set`、`missing:enhance`。
- 结果保持 `mode=visual_only`、`verification=unverified`、`click_performed=false`。未执行 ADB 输入、导航、点击、强化、选材、重启、资源消耗、底层读取或外部上传。

## 离线验证

- 全部视觉测试 `52/52` 通过；全部 OCR 测试 `27/27` 通过。
- 相关 Python 模块 `py_compile` 通过；`git diff --check` 通过，仅报告用户既有文件的换行提示。

## 未完成项、风险与下一步

- 收窄套装文字裁剪没有提高本帧置信度，反而低于前一真实帧的 `0.971498`；不能通过降低门槛解决。下一阶段应离线评估套装图标模板证据或受控预处理，并继续保留文字 OCR 的独立置信度记录。
- 当前详细页没有形成显式 `+0`/`exp0/...` 证据；不能把“没有 `+N`”推断为 `+0`。需离线确认游戏在哪个页面明确显示强化等级，再决定是否需要另行授权只读页面导航和后验截图。
- 在套装和强化两项都通过前，不得进入目标绑定、受控点击或自动强化。

## 人工干预边界

- 模板校准需要一次人工确认：确认截图中的套装图标/文字区域和页面状态，或明确授权从一次真实 ADB 内存帧提取脱敏模板。用户提供的截图只能作为布局/模板参考，不能直接作为运行时字段证据。
- 正常运行不需要每次强化都人工确认；模板、页面锚点和 `0.98` 字段证据全部通过时，视觉决策可继续走自动闭环。
- 任一帧出现套装低置信度、强化等级缺失、页面不明确或字段矛盾时，系统必须停在 fail-closed，由人工重新确认页面或重新授权校准；不得自动猜测或降低门槛。
- 在获得明确的截图裁剪持久化授权前，真实账号截图和裁剪只允许内存处理；离线测试使用合成/公开 fixture。
