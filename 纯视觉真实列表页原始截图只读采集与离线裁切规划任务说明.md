# 纯视觉真实列表页原始截图只读采集与离线裁切规划任务说明

## 目标

在用户明确要求下，从当前 MuMu 武器列表页保存一张真实原始 PNG，随后仅在本地离线分析其布局并规划固定裁切区域。原始图和裁切均标记为 `reference_only`，不构成目标装备验证或生产模板。

## 范围

- 只运行官方 MuMu `adb.exe devices -l`，接受唯一状态为 `device` 的设备。
- 设备唯一后只读取一次 `exec-out screencap -p` 原始 PNG，并保存到本地 reference 资产目录。
- 仅对已保存的真实 PNG 做离线视觉检查、区域边界规划和裁切 manifest；不得把人工判断写成 OCR 字段通过。
- 可输出列表网格、卡片候选、标题/排序区域等固定裁切规划，后续另行审定。

## 禁止操作

- 禁止 `input`、点击、滚动、导航、详情页/强化页进入、选材、确认和资源消耗。
- 禁止 OCR/模板结果冒充当前目标验证、禁止生成生产模板、禁止把用户描述写成当前页面证据。
- 禁止自动重试、追加采样、联网、上传、下载模型或读取底层数据。

## 输入与授权

- 用户明确要求“先采集原始截图，再根据真实截图分析裁切和规划”；该授权仅覆盖本文件定义的一次只读原始截图采集及离线处理。
- 当前页面期望为武器列表；目标字段仍是武器、红装/传说、85、`+3`、生命值套装、攻击主属性、攻击 `13%`、暴击 `3%`、速度 `2`、抗性 `8%`、分数 `37`，但这些不替代截图证据。

## 产物

- 一张带来源、视口、时间和 SHA-256 的 `reference_only` 原始 PNG。
- 离线裁切规划 manifest 和分析报告；不生成生产卡片指纹模板。

## 停止条件

设备不唯一、PNG 无效、视口不是 `1280x720` 或任何需要输入/导航/额外采样的要求出现时立即 fail-closed。

## 当前状态

`raw_reference_captured_offline_crop_planning_completed_20260801`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口不可用 Luna，实际执行模型为 `gpt-5.6-terra + high`。

## 执行记录（2026-08-01）

- 用户授权一次原始截图只读采集；官方 MuMu ADB 发现唯一 `emulator-5554`，仅读取一次内存 PNG 并保存 reference-only 原始文件。
- 原始图视口为 `1280x720`，SHA-256 为 `a16464c412c9d3775fbcdada7942df3a10f6c998c9fe19037822a7d3de65f8f8`。
- 离线生成目标卡片、邻近上下文、列表网格和右侧详情四个固定 crop；manifest 和报告见 `assets/visual/list_reference/raw/weapon_list_raw_capture_manifest_20260801.json` 与 `reports/visual_list_raw_capture_crop_plan_20260801.md`。
- 未执行 OCR 通过判定、生产模板登记、输入、点击、滚动、导航、强化、选材、确认、资源操作、重试、联网或上传。
