# Heroic 紫装速度门槛研究基准与稀有事件修正任务说明

更新日期：2026-07-13

## 任务状态

上一轮《Heroic紫装速度门槛与十万体力产量研究任务说明.md》已完成第一版实现，但当前报告不能继续用于门槛判断。除报告已标记的初始值分布和 22+ 区间问题外，主 agent 复核发现正式后续路线没有被正确重放。

本任务先修正研究基准、资源账本和终局计数，再续跑稀有事件。仍然只允许修改离线研究器、对应测试、报告和计划；不得修改正式策略、跳值表、lambda、评分、DP、资源默认值、GUI 或前瞻批次。

## 已确认的实际偏差

### 1. `+6/+9/+12` 没有使用正式后续止损

当前研究器 `tools/research_heroic_speed_threshold_100k.py` 的 `_released_action()` 将：

```text
advise_gear(..., enable_dp_assist=False).recommendation != stop
```

统一映射为继续。该逻辑在每个节点重复执行，把“谨慎继续/基础保留建议”误当成后续节点的正式二元强化动作，并显式关闭了 Epic 可用的 DP。

第一版 H4 每十万总体力的节点结果为：

- 进入池：约 `677.15` 件；
- 到达 `+3`：约 `551.22` 件；
- 到达 `+6/+9/+12/+15`：全部仍为约 `551.22` 件；
- `+3/+6/+9/+12` 停止数全部为 `0`。

这不符合项目既有“+6/+9/+12 使用正式后续策略/精确 DP 或已发布回退”的研究口径。项目已有 `formal_followup_action()` 及其他研究器的正确调用路径，不能用关闭 DP 的公共展示建议替代。

### 2. 十万体力结果被错误后续路线放大

H4 第一版每个 85 体力来源循环额外需要约 `594.28` 圣女体力，导致十万总体力被拆成：

- 维度裂缝：`12,594.9`；
- 圣女 3-7：`87,405.1`；
- 仅约 `148.18` 个来源循环。

该账本算术上守恒，但输入的强化路径不正确，因此 `1.351` 件 22+、`42.84` 总百里分和全部候选差值均不得作为策略结论。修复后必须重新生成新 schema、新 resume 和新报告，禁止续用第一版分片。

### 3. 当前样本没有观察到任何 Heroic 22+

第一版 H4 的 `22+` 分来源为：

- normal Epic：`1.351/十万总体力`；
- normal Heroic：`0`。

因此 H2/H3 对 Heroic 门槛的速度收益没有被样本直接观察到。候选降低门槛只增加资源消耗、减少可完成循环数，于是点估计反而低于 H4。这个结果不能解释为“降低 Heroic 门槛不会增加 22+”。

默认每套每 seed 仅 `200` 条、5 seed 的规模不足以研究该稀有事件。不得只把同一低效全池采样机械扩大少量倍数。

### 4. “总百里分”和转换口径需要重新核对

当前 `terminal_bucket()` 在原生正式分类存在时直接选原生值，只有原生未正式命中时才选择转换；它没有独立展示“原生正式总百里分”和“合法满值转换后可达正式总百里分”。

当前未知/未列入 `FORMAL_CATEGORIES` 的分类会被强制写成“半肉(通用)”。一速分类或其他未知分类不得伪装成半肉。22+仍应独立计数；如果同一件同时存在合法普通正式体系，则可以在独立正式 GS 表中计一次，但必须取真实普通体系，不能用一速名称或半肉兜底替代。

## 修正后的正式基准

### +0/+3

- H4 必须逐件、逐节点与当前正式规则一致；H4 不应是“近似当前”。
- normal/rift Epic 非鞋初始速度 `>=2` 硬路线保持冻结；本任务来源仅 normal_85。
- normal Heroic 非鞋初始速度 `>=4` 为当前 H4；H2/H3 只改变指定套装范围内的 Heroic `+0` 门槛。
- `+3` 命中速度才强制进入 `+6`；未命中时交回当前普通策略，不得直接假设继续或停止。
- 非速度、鞋子和不在候选范围的装备完全交给同一当前基准。

### +6/+9/+12

- 使用项目既有正式二元后续决策接口，与 `tools/epic_non_speed_early_policy_pareto.py::formal_followup_action()`、`should_continue()` 和默认策略配置保持一致。
- normal_85 Epic 使用已发布的正式 DP/策略路线，不得传 `enable_dp_assist=False` 后把所有非 stop 展示建议当继续。
- normal_85 Heroic 没有已发布资源 lambda 时，必须使用现有明确的基础回退策略，并在报告写明 `dp_enabled=false`；不得臆造 Heroic lambda，也不得因此把所有“谨慎继续”自动冲到 +15。
- H4 与其他候选在 `+6/+9/+12` 必须调用完全相同的后续函数，候选差异只能来自授权的 Heroic `+0` 门槛。

## 稀有事件分层研究

采用自然概率分层/重要性采样，而不是只从完整池直接等待极少数 Heroic 22+：

1. 按套装、品质、部位、主属性、是否含速度、Heroic 初始速度 `1/2/3/4` 建立可追溯 strata；
2. 部位、主属性、副属性类型的自然权重使用 STOVE 已记录的合法候选等概率；速度鞋主属性不得出现速度副属性；
3. Heroic 初始速度值仍分别使用 `legacy_initial_uniform` 与 `roll_distribution_as_initial_proxy`，不得把代理称为官方自然概率；
4. 对候选真正发生差异的非鞋 Heroic 初始 2/3 速 strata 增加大量公共轨迹；
5. Epic、无速度、鞋子、Heroic 初始 1/4 速等公共部分可以单独稳定估计后按自然权重合并；
6. 完整十万体力绝对结果必须等于“公共基准 + 各差异 strata 的自然权重贡献”，不能把条件样本数当自然掉率；
7. 报告同时展示未加权条件成功率、自然权重和加权后的每十万总体力贡献，便于人工复核。

至少让每个主候选、每个初始值口径获得足够的加权 Heroic 22+有效事件。发布前最低统计要求：

- 每个主候选至少 `200` 个模拟 Heroic 22+有效终局事件；
- H2/H3 相对 H4 的成对差值使用公共轨迹；
- 95% 区间和相对半宽必须报告；
- 若两种初始值口径仍改变候选排序，继续保持 `initial_distribution_blocked`，不能用增加轨迹解决证据缺失。

## 资源与计数复核

每个修正后候选必须重新输出：

- 单循环到达/停止 `+0/+3/+6/+9/+12/+15` 数量；
- 单循环粉末、下级石、强化金币、转换金币、出售金币/经验；
- 85体力来源金币127,500和下级石0.425只记一次；
- 圣女金币/经验缺口取瓶颈；
- 十万总体力的循环数、维度裂缝体力/次数、圣女体力/次数；
- Epic/Heroic 分别的 22+、正式体系件数、正式 GS。

增加与历史联合资源研究的数量级对照：若 H4 单循环总体力仍显著高于已知 A/B/C/D 全线路范围，必须逐项解释是策略强化率差异还是账本偏差，不能只报告算术守恒。

## 百里分与转换输出

主报告必须分开：

1. `原生正式总百里分`：只按原生装备能够命中的一个普通正式体系计一次；
2. `满值转换后可达正式总百里分`：对每件装备评估一次合法满值转换，只有转换提高正式价值时才采用；仍只计一件；
3. `实际策略选择总百里分`：只有当离线策略明确承担10万金币转换成本时才计入资源池；
4. `22+速度件数`：独立，不折算 GS。

正式体系必须使用真实分类名称。`一速/速度专项`单列；未知分类进入 `unclassified` 并触发报告警告，禁止强制改写成半肉。

## 先补失败测试

修正工具前至少增加：

1. 构造一个正式后续在 +6 停止的轨迹，第一版 `_released_action` 会错误到 +15，修复后在 +6 停止；
2. 构造 +9/+12 停止轨迹，确认每个节点调用正式二元后续策略；
3. normal Epic 后续没有被 `enable_dp_assist=False` 关闭；
4. Heroic 未发布 lambda 时明确走基础回退且 `dp_enabled=false`；
5. 同一公共轨迹下 H4 与“无覆盖的 released 基准”逐节点动作完全一致；
6. H2/H3 与 H4 的唯一允许动作差异发生在授权套装、Heroic、非鞋、带速度、+0 初始 2/3 速；
7. 修复后的 smoke 中 +3/+6/+9/+12 至少存在可构造的停止路径，不再全为 0；
8. strata 权重之和为1，完整池直接小样本与分层加权结果在宽容差内一致；
9. 条件 Heroic 22+计数乘自然权重后才进入十万体力结果；
10. 原生、满值转换可达、实际转换三种 GS 不重复计件；
11. 一速或未知分类不进入半肉(通用)；
12. 新 schema 不读取第一版 resume，重复运行新 resume 不新增分片。

## 执行顺序

1. 先用失败测试复现“+3后全部到+15”和分类/转换问题；
2. 最小修改独立研究器，恢复正式后续路线；
3. 运行小规模 smoke，先审核节点停止率和单循环资源数量级；
4. smoke 通过后实现/验证 strata 自然权重；
5. 再运行正式多 seed 稀有事件研究；
6. 生成新报告和原始 JSON，并把第一版报告标记 `superseded_due_to_followup_baseline_error`；
7. 更新 `e7-gear-enhance-plan.md`。

## 允许和禁止范围

允许优先修改：

- `tools/research_heroic_speed_threshold_100k.py`
- `tests/test_research_heroic_speed_threshold_100k.py`
- 新 schema、新 resume、新报告和原始 JSON
- `e7-gear-enhance-plan.md`

只读复用正式策略、DP、资源、评分和转换接口。若共享正式实现确有偏差，只记录为独立问题，不得在本任务修改。

禁止修改正式策略、跳值表、lambda、评分、DP、资源默认值、GUI、OCR、ADB、Airtest、自动点击和任何前瞻批次。

## 最终报告要求

第一页先用中文回答：

- 第一版为什么出现87.4%体力用于圣女；
- 修正后的 H4 每十万总体力怎样拆分，节点在哪里止损；
- Heroic 对 22+的实际贡献是否已被观察到；
- H2/H3 比 H4多多少22+、少多少原生/转换后正式GS、多耗多少资源；
- 速度套/两件套范围与全部套装范围谁更有效；
- 两种初始值口径是否仍改变排序；
- 结论是可继续人工确认、仍被证据阻断，还是候选明确无收益。

报告不得继续引用第一版 `1.351/42.84/87,405.1` 为有效策略基准。机器字段、分层权重、seed、有效事件数和分片信息放在后文。

完成后运行定向测试、受影响策略/资源/评分/转换测试、`tools/run_all_tests.py`、语法检查和 `git diff --check`。全量测试必须取得成功退出码。
