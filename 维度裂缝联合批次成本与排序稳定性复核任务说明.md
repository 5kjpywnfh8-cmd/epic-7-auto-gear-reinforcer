# 维度裂缝联合批次成本与排序稳定性复核任务说明

## 目标

解决维度裂缝 Epic/Heroic 联合研究中 `83.7` 固定批次净成本与资源模型 `82.9` 计算结果不一致的问题，并把 100 轨迹 pilot 改为可续跑、可合并、带统计停止条件的稳定性研究。

本任务不得修改正式装备策略、Heroic 策略、DP 发布常量、评分分类、跳值表或 GUI 主流程。成本偏差必须先审计、先写失败测试，再作最小修复。

## 已确认输入

- 维度裂缝单次结算：40 体力。
- Epic 毛获取成本：85 体力/件，confirmed，宠物额外装备已包含。
- Heroic 毛获取成本：23.81 体力/件，estimated，宠物额外装备已包含。
- 宠物额外下级强化石：每次结算 0.20 概率获得 1 个。
- 下级强化石：1,500 基础强化经验，使用时消耗 14,400 金币。
- 日常材料使用：粉末与下级石按基础强化经验贡献 50/50 混合。
- Good/Great 对整批材料判定，宠物经验加成作用于最终经验，不降低金币。
- 圣女 3-7 日常金币/经验基线和金币、经验瓶颈折算保持现行权威值。
- 同一维度裂缝批次同时产出 Epic 与 Heroic，来源体力和来源强化石信用只能记一次。
- 现有联合工具硬编码 `83.7`；当前资源模型按单品质元数据返回约 `82.9`；两者不得继续并存。

## 第一阶段：成本只读审计

先复现并逐单位列出：

1. `83.7` 的原始推导、报告来源和当时使用的材料信用口径。
2. `82.9` 的当前代码路径、每个乘数、单位和舍入位置。
3. 85 体力批次对应 `85/40=2.125` 次结算，期望来源下级石为 `2.125×0.20=0.425` 个；确认当前实现是否错误地只使用一次 `0.20`。
4. 来源赠送石的信用应是完整 1,500 经验替代、50%石头侧机会成本，还是受14,400金币使用成本影响后的净信用。必须沿用已确认50/50批次模型，不得凭结果接近83.7倒推公式。
5. 金币和经验来自同一日常副本时继续按瓶颈折算，禁止把两种体力直接相加。
6. 来源石信用是否已在强化材料端再次抵扣；必须证明只记一次。

输出一张公式对照表，明确哪个值是正确联合批次净成本。若权威输入仍不足以唯一确定，只输出参数化公式和缺失输入，不得保留无依据硬编码值。

## 第二阶段：最小修复

对确认的实际偏差先新增最小失败测试，至少覆盖：

- 每次结算概率必须按批次期望结算次数缩放；
- Epic/Heroic 联合批次只记一次来源体力和一次来源石信用；
- 50/50经验混合及强化石使用金币口径；
- 联合工具与资源模型返回同一个净批次成本；
- 禁止 `Epic×单件净成本 + Heroic×单件净成本`；
- 旧单品质条件报告保持兼容并明确不能用于联合分母。

只修改资源来源信用的直接实现、联合研究读取路径和对应测试。联合工具必须从资源模型的显式“联合来源批次”接口读取，删除 `RIFTSLASH_JOINT_BATCH_NET_STAMINA=83.7` 硬编码。不得借机重构其他资源模型。

修复成本后，不得自动覆盖已发布 Epic/Heroic lambda；先标记现有校准是否因成本变化需要另行重跑。

## 第三阶段：可续跑稳定性研究

100轨迹结果保持 `pilot_not_for_release`，不得用于冻结候选或发布策略。

为联合研究增加：

- 按 seed、品质和轨迹分片；
- 单分片运行时间控制在环境120秒以内；
- 原子保存、断点续跑、seed去重和失败分片重试；
- 合并时累计原始分子/分母，禁止平均各分片比值；
- 所有 Epic 候选使用公共随机轨迹；
- Heroic 基础回退、Heroic全停、产出率基线/±20%使用同一组底层轨迹。

目标总量至少达到每件5,000条轨迹，但发布判断不只看固定数量。对每个敏感性场景计算 B、C 及其他候选的全链路价值率差值和95%置信区间；只有同一候选在全部场景中第一，且与第二名差值区间均不跨0，才判定排序稳定。若达到5,000仍跨0，继续扩样或明确“候选实质等价/无法区分”，不得强选第一名。

同时报告非零正式终局数量、分子/分母置信区间、前后半稳定性和多seed离散度。不得用单seed路径内样本伪装独立置信度。

## 数据与策略边界

- 本轮只研究 Epic 候选排序。
- Heroic 固定 `baili_marginal_low`、`dp_enabled=false`，并做全停敏感性；不得修改其门槛或发布 lambda。
- 终局分子默认只用正式体系百里分；仅终局速度 `>=22` 的一速装备允许使用校准速度价值。
- 75+ 不进入早期自动价值，多体系匹配数量不加分。
- 排序稳定前不冻结新的48件验证集。
- 不接 OCR、ADB、Airtest 或自动点击。

## 最终报告

必须包含：

- `82.9/83.7` 根因、完整公式和唯一采用值；
- 修改文件及成本影响范围；
- 修复前后联合批次成本与是否影响已发布校准；
- 分片数、seed、总轨迹、耗时、断点续跑证据；
- 各 Heroic/产出率场景下全部 Epic 候选价值率及成对差值置信区间；
- 排序是否稳定，是否允许进入48件独立验证；
- 定向测试、相关资源/策略测试和全量测试结果。

现有 `reports/epic_non_speed_joint_batch_20260712.md` 仍包含旧 Pareto 报告的“边际强化为主”“盲测尚未标注”等过期段落。最终产物必须使用独立联合报告结构，或明确清理这些冲突内容；标题、口径、数据状态和最终结论必须一致，不能让旧结论覆盖联合研究状态。
