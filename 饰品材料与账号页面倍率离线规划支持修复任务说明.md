# 饰品材料与账号页面倍率离线规划支持修复任务说明

## 目标

修复离线预算规划器对饰品材料及上级强化石的过度保守拒绝，并按当前账号页面已验证的 `1.166` 基础倍率生成确定性整数材料计划；Good/Great 期望层建立在该整数页面结果上，同时保持材料池和硬上限边界。

## 范围

- 为普通装备与饰品材料池分别支持粉末、下级强化石、上级强化石。
- 两类材料池使用相同离散常量：粉末 `100 EXP / 1,600` 金币、下级石 `1,500 EXP / 14,400` 金币、上级石 `4,500 EXP / 43,200` 金币。
- 材料 ID 和库存必须按 `common` / `accessory` 严格隔离，不得跨部位替代。
- 当前账号每个强化分段的有效经验按 `floor(基础经验 * 1166 / 1000)` 计算；不得使用二进制浮点近似。
- Good/Great 作为建立在该整数页面结果上的独立随机层：令普通结果 `N=floor(基础经验*1166/1000)`，则 Good 为 `floor(N*3/2)`、Great 为 `2*N`，期望为 `0.89*N + 0.055*floor(N*3/2) + 0.055*2*N`。忽略整数取整时随机层倍率为 `1.0825`、派生连续近似为 `1.166*1.0825 = 1.262195`，但逐批计算必须优先使用整数结果。
- 两条材料需求路径必须分开：离线执行规划器要求普通页面结果 `N` 已足额；长期资源模型的 `material_cost_for_level()` 仍按期望成本语义，在 `100` 基础经验网格上选择使上述分层期望达到标称需求的最小投入，再按三个整数结果计算溢出返还。
- 规划结果同时展示需求经验、投入基础经验、按账号倍率得到的有效经验、溢出、材料和金币。
- 保持现有库存、分段硬上限、累计硬上限、材料优先级及结构化 fail-closed 契约。

## 禁止修改项

- 不修改正式装备策略、DP 决策、评分、来源收益、材料机会成本、GUI、OCR、Holdout、策略 manifest 或自动化点击规则。
- 不修改正式装备策略、DP 状态转移、评分或自动化；本任务必须修正资源模型中把页面基础倍率、宠物倍率和 Good/Great 期望混成 `1.15395` 的计算错误。
- 不接入 MuMu、ADB、页面点击、材料选择或资源消耗。
- 不读取、恢复、暂存、提交或上传私人归档。
- 不支持当前范围外的传说强化石。

## 输入与权威口径

- [资源成本基线与 DP 审核任务说明](资源成本基线与DP审核任务说明.md)中的用户权威强化石表。
- [饰品与上级强化石离散材料证据复核报告](reports/饰品与上级强化石离散材料证据复核_20260725.md)及其已跟踪 batch `013` 截图证据。
- 用户于 `2026-07-25` 明确澄清：装备与饰品强化石规则相同，只是使用部位及材料池不同。
- 用户于 `2026-07-25` 授权当前账号离线预算先按页面总倍率 `1.166` 取整，再在资源期望计算中叠加随机层 `1.0825`。
- 代码与代码测试使用用户指定的 `gpt-5.6-terra + high`。

## 执行步骤

1. 在共享资源常量中补齐上级强化石经验与金币，并拆分页面基础倍率、随机层期望倍率和取整规则。
2. 将普通与饰品三种材料定义接入离线规划器，保持材料池隔离。
3. 将候选可达条件改为整数页面有效经验满足节点需求，并在结果中同时保留基础经验和页面有效经验。
4. 更新资源模型的逐批期望、连续资源率元数据、JSON/Markdown 契约及报告生成器；移除 `1.15395` 作为单一最终倍率的路径。
5. 补齐普通/饰品、粉末/下级/上级、五段节点、库存、优先级、分段与累计硬上限、跨池拒绝、页面取整和随机层叠加边界测试。
6. 运行定向测试、资源模型回归、CLI smoke、报告生成、Python 3.9 语法和 `git diff --check`。
7. 主 agent 只读审阅结果；通过后同步本任务与总计划，提交并推送当前分支。

## 产物

- `src/e7_enhance/resource_model.py`
- `src/e7_enhance/offline_budget_planner.py`
- 对应测试、CLI/报告生成器及验证 JSON/Markdown（仅在契约变化需要时修改）。
- 本任务说明与 `e7-gear-enhance-plan.md` 状态记录。

## 验证要求

- batch `013` 的 `1` 下级石加 `2` 粉末必须计算为基础经验 `1,700`、有效经验 `1,982`，足以覆盖 Epic `+0 -> +3` 的 `1,969` 需求，金币 `17,600`。
- 单独 `1,500 / 1,600 / 1,700` 基础经验必须分别得到 `1,749 / 1,865 / 1,982` 有效经验。
- 普通与饰品同配置结果除材料 ID 外一致；任何跨池材料、库存或硬上限键均 fail closed。
- 上级石必须按 `4,500 / 43,200` 参与整数规划，不得按比例动态推导。
- 页面基础结果必须按 `N=floor(基础经验 * 1166 / 1000)` 计算，随机结果必须为 `N / floor(1.5*N) / 2*N`，再按 `0.89/0.055/0.055` 加权；连续近似的随机层倍率为 `1.0825`、总倍率为 `1.262195`，不得再输出 `1.15395` 作为总倍率。
- batch `013` 的三组预览及资源成本回归必须按分层公式更新并通过。
- Epic `+0 -> +3` 的离线执行计划必须使用 `1,700` 基础经验得到普通页面 `1,982`；单级/长期资源期望路径不得复用该确定性门槛，而应证明其选择的是分层期望足额的最小 `100` 经验网格投入。
- 所有成功计划不得突破库存、分段或累计硬上限；报告不得将离线计划描述成实际操作授权。

## 停止条件

- 任何改动会改变正式装备策略、DP 状态转移或自动点击行为时立即停止并报告；资源模型的倍率分层修正属于本任务明确范围。
- 无法用整数算术复现三组页面预览时 fail closed。
- 定向测试或既有资源模型回归失败时不得提交或发布完成结论。
- 发现与本任务重叠的用户未提交改动时停止写入冲突文件并报告。

## 执行记录（2026-07-25）

- 已在已授权的既有实现上下文中完成：上级石常量 `4,500/43,200`、普通与饰品三材料池支持、材料池隔离、整数页面有效经验输出，以及 Good/Great 分层期望与连续元数据更新。
- 规划可达性固定为 `floor(基础经验 * 1166 / 1000)`；随机层不参与可达性判定。`minimum_gold` 仍为首要目标，因此同时允许时 `17` 粉末为 `27,200` 金币，正确优先于 `1` 上级石的 `43,200` 金币；上级石仅在允许集合或硬上限迫使时选择。
- 已同步旧资源模型回归值、上级石受支持/受限选择、跨池库存和硬上限键拒绝、页面 floor 与 Good/Great 整数边界测试。未修改正式策略、DP 状态转移、GUI、OCR、ADB、MuMu 或自动化。
- 验证通过：Python 3.9 `test_resource_model.py` 为 `16/16`、`test_offline_budget_planner.py` 为 `39/39`；临时目录报告生成通过（普通和饰品各 5 个相邻分段）；Python 3.9 语法检查和 `git diff --check` 均通过。未读取私人归档，未提交或推送。
- 主审阅后已更正长期资源模型语义：`material_cost_for_level()` 在 `100` 经验网格按三种整数结果的加权期望找最小投入。对标称 `1,969`，长期路径为 `1,600 -> 1,865 -> 2,018.835`，而执行规划器仍为 `1,700 -> 1,982`。溢出返还由普通/Good/Great 的对应整数结果计算。更新后资源模型 `16/16`、规划器 `39/39` 和 `git diff --check` 通过。
- 主审阅继续发现发布证据与兼容性缺口：五段有界矩阵当前仅覆盖 `common`，`accessory` 只有相邻节点结果；资源模块删除旧导出常量会破坏潜在外部导入；规划结果只有语义已改变的 `experience_overflow`，缺少显式页面溢出键。补齐两材料池独立五段矩阵、正确值兼容别名和显式页面溢出字段前保持未完成。
- 主审阅收口已完成：恢复 `PET_ENHANCE_EXP_MULTIPLIER=1.066`（仅历史组件）及映射到新连续近似 `1.262195` 的 `EXPECTED_ENHANCE_EXP_MULTIPLIER` 导出；二者均不恢复旧 `1.15395` 路径。`SegmentPlan.to_dict()` 同时输出 `page_effective_experience_overflow` 与兼容的 `experience_overflow`，值严格相同。
- 报告生成器保留既有 common 五段字段，并新增 `accessory_five_segment_reference_proposal`、`accessory_five_segment_exact_limit_result` 和 `accessory_five_segment_bounded_matrix`。两池分别覆盖 `+3/+6/+9/+12/+15`、库存、分段硬上限和累计硬上限；饰品矩阵仅包含 `accessory_*` 材料键，并在 Markdown 单独展示。
- 验证通过：资源模型 `16/16`、规划器 `39/39`、临时 JSON/Markdown 报告生成、Python 3.9 语法、双溢出键与饰品矩阵 smoke、`git diff --check`。未读取私人文件，未提交或推送。
- 主工作区最终复跑补充通过：共享研究桥接 `7/7`，另一个直接研究测试退出码 `0`。公开验证报告已刷新，common/accessory 五段矩阵各 `69` 个覆盖案例、`49` 个全局唯一请求、违规 `0`；CLI smoke 已刷新为 `offline_budget_planner/v2`，`+0 -> +3` 结果为 `1` 下级石加 `2` 粉末、`1,700 -> 1,982`、`17,600` 金币。

## 当前状态

`completed_validated_commit_push_pending`
