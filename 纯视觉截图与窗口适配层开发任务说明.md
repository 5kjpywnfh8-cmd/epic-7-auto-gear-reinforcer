# 纯视觉截图与窗口适配层开发任务说明

## 目标

在已发布的纯视觉运行时和归一化点击器之上，补齐可注入的截图采样与窗口适配层，使 `VisualOnlyEnhanceExecutor` 能接收稳定帧并把一次动作交给外部窗口驱动，同时保持 fail-closed 和离线可验证。

## 范围

- 新增 `VisualFrame`、`FrameSource`、稳定帧收集器和证据解析器协议。
- 提供 callback/file-backed 的离线适配实现：记录帧哈希、视口、时间戳和来源，不伪装成真实设备采样。
- 将稳定帧结果接入现有 `VisualSampler.capture(request)` seam；解析失败、帧不稳定、视口不一致或时间戳无效均停止。
- 为现有归一化点击器提供可注入窗口后端兼容测试；不改变 `VisualOnlyEnhanceExecutor` 的节点状态机。
- 新增公开测试，覆盖连续稳定帧、帧变化、视口漂移、来源异常、解析异常和点击后端调用边界。

## 禁止修改与禁止操作

- 不启动或控制 MuMu/ADB，不连接游戏，不读取或上传游戏数据，不采集真实截图，不运行真实 OCR，不执行真实页面点击、强化、选材或资源消耗。
- 不接入 Airtest、PyAutoGUI、Win32 或其他桌面输入/截图库；真实平台驱动只保留 callback 协议，后续另建任务。
- 不修改 MuMu/Fribbels 读取器、413 分批、fresh snapshot 契约、正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则。
- 不读取、恢复、暂存或上传私人归档；不修改或删除 `refs/heads/main - 副本`；不使用 `git add -A`。

## 权威输入与模型

- 本文件是本轮截图与窗口适配层开发的唯一任务来源。
- 设计依据：[纯视觉点击执行器开发任务说明.md](纯视觉点击执行器开发任务说明.md)、[纯视觉运行时接线实现任务说明.md](纯视觉运行时接线实现任务说明.md)、`src/e7_enhance/visual_runtime.py`、`src/e7_enhance/visual_click.py` 及公开测试。
- 目标节点仍为红装 85 级武器、爆伤套、主属性攻击 100、生命 4%/爆伤 4%/暴击 5%/速度 2，`+0 -> +3`；资源上限粉末 2、下级强化石 1、上级强化石 0、金币 17600。
- 执行模型：`gpt-5.6-terra + high`。

## 执行步骤

1. 审阅现有截图来源、视觉运行时、点击器和公开测试，冻结最小适配接口。
2. 先补失败测试，再实现帧模型、稳定收集器、callback/file-backed source 和解析器 seam。
3. 只用内存 fake source/backend 验证，不调用真实窗口、设备或 OCR。
4. 运行定向测试、Python 语法检查和 `git diff --check`，精确审阅 diff。
5. 将完成项、验证结果、未完成项、风险和下一步写回本文件与总计划，精确提交并推送。

## 产物

- `src/e7_enhance/visual_adapter.py` 及对应公开测试。
- 本任务说明与 `e7-gear-enhance-plan.md` 状态记录。

## 验证要求

- 稳定帧和错误闸门测试全部通过；现有视觉运行时、点击器和 dry-run 回归不回退。
- Python 语法检查和 `git diff --check` 通过。
- 报告必须明确“仅 callback/file-backed fake 离线验证”，不得写成真实截图、OCR、点击或强化已验证。

## 停止条件

- 任一步需要真实截图、ADB/MuMu、OCR、桌面输入、页面点击或资源操作时立即停止并报告。
- 帧哈希、视口、时间戳、解析结果或窗口状态无法唯一确认时 fail-closed，禁止自动重试。

## 当前状态

`implementation_verified_visual_adapter_offline_pending_commit_20260726`

- 已完成 `src/e7_enhance/visual_adapter.py` 与 `tests/test_visual_adapter.py`：`VisualFrame`、callback/file-backed source、稳定帧收集器、证据解析器 seam 和 `VisualAdapterSampler`。
- 帧哈希变化、视口漂移、来源漂移、时间戳倒退/无效、来源异常、非字节载荷、解析异常和证据绑定不一致均 fail-closed，采样异常不自动重试。
- 定向验证 `tests.test_visual_adapter`、`tests.test_visual_runtime`、`tests.test_visual_click`、`tests.test_visual_sampling` 共 `24/24` 通过；Python 语法检查通过；本轮目标文件的 `git diff --check` 通过。
- 仅使用 callback/file-backed fake 离线输入；未连接 MuMu/ADB，未采集真实截图/OCR，未执行真实窗口点击、强化、选材或资源消耗。
- 下一步：主 agent 精确暂存本任务文件，提交并推送；真实截图、OCR 和平台窗口驱动仍需另建任务与运行授权。
