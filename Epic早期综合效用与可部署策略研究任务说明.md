# Epic 早期综合效用与可部署策略研究任务说明

## 目标

审核当前 normal_85 Epic `+0/+3` 精确 DP 的价值函数是否足以代表装备强化的综合效能，并研究可部署的早期决策方式。

本任务先验证“Oracle 优化的目标是否正确”，再比较直接在线运行精确 DP 与蒸馏轻量规则。不得仅按人工标签准确率、单一 GS 门槛或单一节省体力指标选策略。

本轮是离线研究，不修改正式强化策略、DP 常量、资源默认值、跳值表或 GUI 主流程。

## 当前证据

- 45 件非速度真实样本中，当前全谨慎策略相对 Oracle 的动作一致率仅 `51.11%`，误强化 22 件、增量体力 `84.83`，人工复核覆盖 `100%`。
- 但当前策略总 utility regret 仅 `0.0746`，Oracle 正效用召回 `100%`；regret 是策略动作相对逐件 Oracle 最优动作损失的 utility 总和，越接近 0 越好。按当前 lambda 折算约为 `59.63` 体力的净效用损失，不等于 `84.83` 的毛误强化体力，因为部分误强化路径仍产生终局价值。
- `C_category_probability` 的旧“后续强化终局价值/100增量体力”由 `1.172` 提高到 `1.201`，但该指标未直接计入所有胚子获取成本，只能作边际对照，不能代表有限总体力下的完整性价比。
- 均衡档分子原则上只使用正式体系百里分。唯一独立例外是终局速度 `>=22` 的一速装备，需要把速度价值校准到正式百里分档位；终局速度 `<22` 不产生独立速度价值，只能按正式体系百里分计值。75+ 未来可期只保留为强化完成后的人工余量处理，不进入 `+0/+3` 自动继续价值。体系匹配数量本身也不增加价值。
- 当前低 regret 只证明策略接近现有价值函数的 Oracle，不证明该价值函数已经覆盖全部实际价值。

## 第一阶段：价值函数覆盖审计

先只读检查 `score_engine/evaluation`、正式分类表、`final_success_breakdown`、`terminal_value_for`、速度潜力、转换、重铸、资源模型和 route solver，不先修改代码。

建立覆盖矩阵，逐项确认是否进入终局价值、概率、成本或仅 debug：

- 正式分类有效 GS、分类×部位门槛和主属性限制；
- 75+ 未来可期只能作为终局 debug/人工余量强化提示，不得驱动 `+0/+3` 自动继续；
- 输出、必爆、抗坦、纯肉、命坦、双效、半肉等全部体系；
- 同一装备匹配多个体系时不得按匹配数量加分；优先级为速度最高，纯输出与纯坦克同属第二层并按达标概率排序，半肉低于纯体系，双效低于半肉；输出和坦克互斥，纯坦克最多允许一种效果属性；
- 现有速度潜力、套装资格和四件套 20 速度门槛保留为策略路径审计项，但均衡档独立速度终局价值只允许终局速度 `>=22`；低于 22 的终局不得用速度公式替代正式百里分；
- 85 级默认重铸及 90 级不二次重铸；
- 满值转换后的属性、合法性和一次 100,000 金币成本；
- 强化金币、50/50 粉末/下级石、饰品系数、出售回收和下一节点增量成本；
- normal_85/rift_85 独立跳值与来源/品质合法性；
- 多跳高值的尾部收益、失败概率和资源风险；
- 人工复核次数只作未来部署指标，不折算为金币、体力、强化材料或 DP utility。

对每项标记：已计入、部分计入、未计入、明确不应计入。提供代码证据和单位，检查重复计值与量纲不一致。

重点验证：

1. 每个终局默认只取正式体系百里分；只有终局速度 `>=22` 时才计算独立一速价值并与正式百里分取较高者，避免重复计值。以“终局 22 速度至少等于正式 GS 第二档”为最低锚点，并在 22 以上保留最终速度与速度跳数的高速度、多跳优势。不得给 22 以下速度终局独立加分。
2. 正式成功门槛以下直接记 0 是否错误否决部位受限但已满足正式体系的装备；75+ 和一般过渡装不作为早期自动继续的救回条件。
3. 满值转换是否真正改变终局 GS/体系和 utility，而不只是记录来源行与扣除成本。
4. 当前分类优先级是否遵守“速度 > 纯输出/纯坦克 > 半肉 > 双效”，以及同层输出/坦克按终局达标概率排序；不得给多体系通用性额外奖励。
5. 先只研究均衡档：使用当前已确认的日常收入、装备获取成本和强化成本，在固定总体力下比较完整产出效率。成功概率、P10/P50/P90、失败成本只作风险说明，不在本轮另加未经确认的风险惩罚。
6. 单件当前动作 debug 仍可把装备获取成本视为沉没项；但均衡策略模拟必须把所有进入筛选的胚子获取成本计入总投入，包括立即停止的装备。两种口径分别展示，禁止重复记账。

未知的账号需求、重复装备边际价值和套装需求权重不得猜测默认值。人工时间不是游戏资源成本，不进入本轮 utility。

## 第二阶段：综合 Pareto 研究

不要把所有指标强行相加成新的任意总分。分别报告并建立 Pareto 前沿：

- 期望终局正式价值；
- 75+ 概率仅作终局人工余量处理参考，不参与 Pareto 支配或候选动作；
- 高价值终局召回；
- utility regret；
- 平均/P90/最大增量体力；
- 误强化资源与误停价值；
- 全链路正式终局价值/100总投入体力作为主指标；后续强化终局价值/100增量体力仅作单件动作解释；
- 速度高价值召回；
- 转换后高价值召回；
- 人工复核覆盖率；
- 风险分位数。

预留高召回、均衡、省资源三档结构，但本轮只研究均衡档。均衡档严格使用当前已确认的日常收入与强化成本；高召回和省资源只保留现有对照结果，不调参、不发布。

资源效率必须拆成两种口径，并以完整产出为均衡档主指标：

1. 主指标：`全链路正式终局价值/100总投入体力 = 100 × 策略最终产出的期望终局价值总和 /（所有进入筛选的胚子获取体力 + 后续强化净体力）`。分子默认只用正式体系百里分；终局速度 `>=22` 时允许使用校准后的一速价值并与正式百里分取高。胚子成本必须计入所有候选，包括立即停止者。
2. 辅助指标：`后续强化终局价值/100增量体力 = 100 × 策略选择强化装备的期望终局价值总和 / 这些装备从当前节点开始的后续强化净体力总和`。该指标仅解释已经获得单件的继续/停止，不作为总体策略性价比的主排名。

均衡档默认使用已确认的 `riftslash_20_buff` 维度裂缝一刀 Buff 来源批次。该副本同一批体力同时产出 Epic 与 Heroic，禁止把两种品质的单件获取成本相加。以毛 `85.0` 体力归一化为一批时，期望获得 `1` 件 Epic；按现有 Heroic 毛成本 `23.81` 的 estimated 口径，同批期望 Heroic 数为 `85 / 23.81 ≈ 3.57`。来源副产品信用只在该批次抵扣一次，当前批次净成本按资源模型 Epic 归一化读取为 `83.7`；宠物额外装备已包含。

`23.81` 仍是 estimated。必须对 Heroic 产出率做至少 ±20% 敏感性，不得把 `3.57` 写成 confirmed。真实样本缺少 `gear_source` 时，只能标记为统一研究场景，不得回写或伪装成装备原始来源。

联合批次主公式：

`全链路价值率 = 100 × (Epic 终局正式价值 + 3.57 × Heroic 终局正式价值) / (83.7 批次净获取体力 + Epic 后续强化净体力 + 3.57 × Heroic 后续强化净体力)`。

实际计算必须使用期望产出率而不是先四舍五入。立即停止的 Epic/Heroic 仍属于批次产出，其出售金币/回收经验进入各自策略回收；来源下级强化石信用只能在批次获取端出现一次。

本轮只优化 Epic 候选策略，Heroic 固定使用当前未发布 lambda 下的基础回退策略，并单列其价值、强化消耗和回收。不得借本任务发布 Heroic lambda 或调 Heroic 门槛。另做“Heroic 全停止/当前基础回退”的边界敏感性，以显示紫装处理对 Epic 策略排序是否稳定。

报告必须同时给出：联合 Epic+Heroic 全链路价值率、Epic 策略在固定 Heroic 基线下的增量贡献、Epic/Heroic 各自后续强化成本、批次获取成本、强化材料/金币瓶颈、出售回收、来源副产品信用和转换成本占比。金币与经验同源产出仍按瓶颈折算，不得直接相加体力。

禁止使用 `Epic件数 × 83.7 + Heroic件数 × 22.5` 作为联合获取成本；这会对同一次维度裂缝体力和同一份来源副产品信用重复记账。现有每品质单件成本只适用于各品质独立校准或条件报告，不适用于联合批次分母。

现有 `expected_terminal_value_per_100_stamina` 属于辅助口径。发布 lambda 虽来自全链路校准，但本轮改变终局速度计值和候选策略后，必须重新研究全链路 `cost_per_terminal_value` 与对应 lambda 的稳定值；不得把现有 lambda 与新增获取成本机械叠加，也不得在本研究中直接发布新常量。

## 第三阶段：可部署方式

1. 基准测试单件 `+0/+3` 精确 DP 的冷启动、缓存后耗时、内存和终局状态数，并按普通/复杂装备分别统计 P50/P95/P99。
2. 如果精确 DP 已满足 GUI 单件判断的交互时延，应优先研究直接调用精确 DP，避免继续维护低质量静态门槛。
3. 如果精确 DP 不满足时延，再以冻结 Oracle 结果蒸馏轻量规则；特征只能使用判断时真实可得字段，包括套装、部位、主属性、当前副属性、跳数、体系命中、可行有效词条、来源和品质。
4. 轻量规则必须报告相对 Oracle 的 regret、误停尾部和召回，不能只报告动作准确率。
5. `continue` 与 `cautious_continue` 的游戏资源动作相同，都是强化到下一节点；后者仅表示需要人再看一次。人工复核率只评价未来自动操作便利性，不进入均衡档 DP 数学效用。

## 数据隔离

- 已审核的 45 件只用于现状审计和开发诊断，不能作为新候选的独立发布验证。
- 在调整价值函数或候选之前，先从剩余真实 `normal_85 Epic +0/+3` 非速度样本中冻结至少 48 件独立验证批次；记录稳定 ID/指纹，锁定后不得用于调参。
- 新 48 件应保持真实样本分布；另附部位、强化节点、套装、主属性、固定值和体系覆盖统计。覆盖不足只能报告，不能通过重复抽样伪装自然分布。
- 其余真实样本可作开发集；条件合成样本只用于合法性、边界和单调性测试。
- 先锁定候选预测与 Oracle 口径，再读取新批次人工标签；人工仍是第二对照。

## 修改边界

允许新增或修改离线研究工具、对应测试、报告和计划文档。若发现价值函数实际 bug，只记录最小复现和失败测试建议，本任务不得直接修改正式评分、route solver、策略或 GUI。

不得接入 OCR、ADB、Airtest 或自动点击。

## 验证与报告

- 新工具先补确定性、数据隔离、量纲、边界和不重复计成本测试。
- 运行相关离线、评分、DP、资源和策略测试；全量测试若仍受 Windows Qt 原生崩溃影响，分别独立进程执行全部测试文件，并完整报告限制。
- 更新 `e7-gear-enhance-plan.md`。

最终报告必须回答：

1. 当前低 regret 是否建立在不完整或阈值化过强的终局价值上。
2. 哪些实际价值已经考虑，哪些缺失，哪些不应进入单件强化决策。
3. 直接运行精确 DP 是否可满足实际 GUI 时延。
4. 当前日常资源基线下均衡档的效能和风险，以及未来高召回/省资源档需要哪些额外偏好参数。
5. 是否具备建立正式实现任务的证据；若不具备，明确缺少的数据或用户偏好，不得直接改策略。
