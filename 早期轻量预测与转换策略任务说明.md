# 早期轻量预测与转换策略任务说明

更新时间：2026-07-11

## 任务目标

修复第七史诗装备强化工具在 `+0 / +3` 的早期轻量预测：

1. 按体系、部位和终局正式评分判断潜力，不能使用统一副属性总分；
2. 三条有效副属性加一条未命中时，正确保留优先体系的转换候选；
3. 对 `建议结果/14.json`，输出体系必须优先于半肉；
4. 不改变已验收的 `建议结果/07.json` 结果。

本任务只处理策略与其调试信息，不处理 GUI 布局、OCR、ADB、Airtest 或自动点击。

## 权威规则

### 终局门槛

1. 《装备强化与评分规则审阅.md》第 6.4 节的“分类 × 部位” `GS` 矩阵是正式终局基础门槛表。
2. `GS` 指该分类有效副属性官方分；不同体系、部位、主属性限制、白字需求和可合法有效副属性数必须使用不同门槛。
3. 85 级装备按既有规则默认重铸为 90 级终局后，再以该矩阵判定是否达到对应分类的低档门槛。
4. 输出衣服等因部位副属性禁用规则最多只能形成三条有效副属性的情形，三条有效是合法正常路径，不能因未满四条自动否决。
5. 矩阵中为 `-` 的“分类 × 部位”没有正式终局公式；不得为其编造自动继续门槛。

### `+0 / +3` 判断

1. 不得把当前低强化 `GS` 与 90 级门槛直接比较，也不得把当前分按强化等级线性放大。
2. 对每个候选体系，依据当前副属性、剩余强化事件、来源/品质独立跳值表、均匀命中概率和 85 级重铸收益，计算终局达标概率：

   ```text
   P(最终 90 级该体系有效 GS >= 该体系 × 部位低档 GS 门槛)
   ```

3. 运行时 `+0 / +3` 保持轻量预测；精确 DP 只作为离线校准和既有 `+6 / +9 / +12` 路径的依据。不得在每次 `+0 / +3` 判断时直接跑完整精确 DP。
4. 离线校准分组至少区分：`item_source`、`rank`、`slot`、主属性约束、体系、套装组、当前有效副属性数、可行有效副属性数。普通 85 与异界 85 的跳值分布必须保持独立。
5. 没有足够精确 DP 标签支持的分组，保持“谨慎继续/待 +6 精确复核”，不得发布自动“继续”。

### 建议与转换候选

1. `+0 / +3` 四条副属性都命中同一体系，且该体系的早期校准条件已达到：建议 `continue`。
2. `+0 / +3` 三条副属性命中同一体系，且达到该体系/部位的早期校准条件：建议 `cautious_continue`。
3. `+0 / +3` 恰有一条未命中、但存在满足门槛的转换候选时：前台仍建议 `cautious_continue`；转换只作为 debug 和目标体系中的候选终局路径，不得提前显示 `convert`。
4. 未建立权威转换石结果分布前，不得把转换结果并入数值终局概率；只能据现有属性与合法终局强化路径计算概率。
5. 候选体系优先级分两层：
   - 高优先级：输出、输出（必爆）、抗坦、纯肉、命坦；
   - 低优先级：双效、半肉（血防/通用/白字）。
6. 同一优先级层内按终局达标概率从高到低排序；概率相同才使用稳定既有分类顺序。
7. 低优先级候选不得覆盖合格的高优先级候选；只有没有合格高优先级候选时才可成为首选。
8. 既有速度专门分类和 `07.json` 的多跳速度硬路线不在本任务中降级或重写。

## 唯一复现输入

`建议结果/14.json`：

- 普通 85 红装、暴击套武器、`+3`；
- 副属性为攻击%、暴率、爆伤、命中；
- 当前工具给出 `cautious_continue`，但错误选择“半肉（通用）”。

修复后：

- 建议仍为 `cautious_continue`；
- 首选目标/转换候选为输出；
- debug 明确说明攻击%、暴率、爆伤命中输出，命中为唯一未命中副属性；
- debug 说明输出优先于半肉的层级和终局达标概率依据。

## 允许与禁止修改

允许修改：

- `src/e7_enhance/enhance_policy.py`
- `src/e7_enhance/score_engine.py`
- `src/e7_enhance/lightweight_calibration.py`
- `src/e7_enhance/lightweight_calibration_rules.json`
- `tools/calibrate_full_category_lightweight.py`
- 必要的 `gui_support.py` 调试展示格式
- 直接相关的策略、校准和 GUI 支持测试
- 本任务说明、计划文档和必要的校准报告

禁止修改：

- `gui_app.py` 布局与控件行为；
- 装备合法性、强化节点、普通/异界跳值表、重铸规则、速度潜力规则；
- JSON 数据语义、导入导出格式、既有手工验收样本内容；
- OCR、ADB、Airtest、自动点击和外部服务；
- 与本任务无关的重构。

## 执行步骤

1. 阅读本文件、`e7-gear-enhance-plan.md`、`装备强化与评分规则审阅.md` 第 5 至 8 节，以及相关源码和测试。
2. 使用 `建议结果/14.json` 原样复现当前结果；记录策略名称、基础建议、轻量决策、完整分类诊断、有效/无效副属性、当前候选排序及 `07.json` 对照结果。
3. 找到“必须四条完整命中才成为候选”和“半肉被选为回退目标”的具体代码证据。
4. 从审阅文档第 6.4 节提取或复用静态“分类 × 部位”终局低档 `GS` 门槛，不重新发明一张统一分数表。
5. 先新增最小失败回归测试，再实施代码修改。
6. 扩展候选评估，使其针对每个体系计算：
   - 当前命中副属性、未命中副属性；
   - 该部位可行有效副属性数；
   - 终局低档 `GS` 门槛；
   - 终局预期 `GS`、理论上下界和终局达标概率；
   - 是否为转换候选；
   - 优先级层、同层概率排序依据和拒绝原因。
7. 使用既有精确 DP 和合法跳值规则生成/扩展离线校准标签；不得合并 normal/rift 跳值表。只有有足够训练与保留集证据的具体分组才能发布自动 `continue`。
8. 更新轻量预测：将四条命中、三条命中、单条未命中转换候选和部位可行词条数纳入决策；不足证据时保守复核。
9. 更新 debug 展示，确保上述候选信息能以中文说明；不删除现有 DP、速度潜力、完整分类和强化命中分析。
10. 更新计划文档；若产生独立校准结论，新增 Markdown/报告并从计划文档链接。

## 必须新增的回归测试

1. `建议结果/14.json`：`+3` 建议为 `cautious_continue`，输出是首选转换候选，半肉不能压过输出；debug 可解释三条命中、一条未命中、层级与概率。
2. 四条命中同一体系且已有校准继续证据：`+0` 和 `+3` 均为 `continue`。
3. 三条命中、满足对应体系/部位早期条件、但非四条完整命中：`+0` 和 `+3` 为 `cautious_continue`。
4. 输出衣服三条合法有效副属性：不因无法出现第四条有效副属性而被错误否决；按输出衣服自己的终局门槛和概率判断。
5. 输出与坦克同时合格：高优先级层内按终局达标概率排序。
6. 合格输出/坦克候选与双效/半肉候选同时存在：低优先级候选不得覆盖高优先级候选。
7. 没有足够校准证据、无正式部位公式或终局理论上界不足的候选：不得错误 `continue`。
8. `建议结果/07.json` 仍为 `lightweight_prediction + continue`，路线仍为“多跳速度高价值路线”。
9. `+6 / +9 / +12` 的精确 DP、normal/rift 跳值分离、85 默认重铸、90 不重复重铸、速度潜力和现有部位合法性测试均不回归。

## 验证

至少运行：

```powershell
python -m unittest tests.test_gui_support -v
python -m unittest discover -s tests -q
```

若校准器新增或修改，运行定向校准并保存种子、样本量、分组覆盖、训练/保留集结果；不得把未完成或不足证据的校准表写成已发布规则。

## 最终报告要求

报告必须列出：

1. 根因与代码/样本证据；
2. 使用的静态终局门槛及其出处；
3. `+0 / +3` 如何从终局门槛得到轻量预测的校准证据；
4. 14 修复前后建议、目标体系、转换候选和 debug 差异；
5. 07 与所有策略回归的结果；
6. 修改文件、测试结果、校准范围与剩余人工验收项；
7. 未有足够证据而保持人工复核的分组。
