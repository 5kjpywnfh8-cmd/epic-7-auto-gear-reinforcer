# 纯视觉真实装备列表页三帧只读校准任务说明

## 目标

在用户于 2026-07-29 明确授权后，对当前 MuMu 装备列表页进行一次受控真实只读校准：验证唯一设备、固定三帧内存截图、稳定页面/局部锚点，以及目标期望指纹的纯视觉列表页证据。结果固定为 `mode=visual_only`、`verification=unverified`，不表示服务器状态或操作成功。

## 范围

- 先只读审阅已落盘的列表页局部识别接线，确认其可由三帧内存 PNG 产生完整 `EquipmentListVisualParser` 观测；若不存在，立刻 fail-closed，且不运行 ADB。
- 仅当接线完整时，运行一次官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`；仅在唯一状态为 `device` 的 MuMu 上，派生固定三次 `exec-out screencap -p`。
- PNG 只在内存中用于视口、稳定哈希、局部区域和已注册的局部识别；不落盘、不上传、不进行全屏盲 OCR、不重试。
- 目标期望指纹：武器、红装/传说、85级、`+3`、生命值套装、攻击主属性、攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`、装备分数 `37`。

## 禁止修改项

- 禁止 ADB `input`、`tap`、`swipe`、`keyevent`，以及窗口输入、导航、点击、强化、选材、确认、重启游戏或资源消耗。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、外部上传、模型下载、截图落盘、全屏盲 OCR、第二次采样批次和自动重试。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、真实帧源或自动化后端；禁止将用户描述、历史截图或离线 fixture 写成当前真实页面证据。

## 输入与权威口径

- 本文件是本轮唯一权威任务来源；同时遵守根目录 `AGENTS.md` 与 `e7-gear-enhance-plan.md`。
- 用户已明确授权本文件所列的一次真实只读校准；该授权不覆盖任何输入、导航、点击、强化、确认或资源消耗。
- 必须保持字段与锚点 `>=0.98`、三帧稳定、唯一目标和 fail-closed。任一前置不足都停止，不得以坐标猜测或扩大识别区域绕过。

## 执行步骤

1. 只读审阅列表解析器、三帧采样器和局部识别接线；确认存在从当前内存 PNG 到完整列表页观测的受控路径。
2. 若步骤 1 通过，运行一次官方 ADB 设备发现；非唯一、非 `device` 或发现失败立即停止。
3. 由唯一发现的设备采集固定三帧内存 PNG，校验 PNG、视口、时间顺序、稳定哈希和非黑屏；不保存、不重试。
4. 在相同三帧上执行已注册的局部页面/锚点/候选卡片/字段识别，传给 `EquipmentListVisualParser`；任一字段、唯一性、边界或置信度不通过即停止。
5. 同步本任务说明、旧只读校准任务说明和总计划，记录是否执行 ADB、真实观测结果或明确的 fail-closed 原因；不执行导航或输入。

## 产物

- 一次真实只读校准记录，或在设备接触前/后明确可审计的 fail-closed 原因。
- 本任务说明、[纯视觉真实装备列表唯一目标只读校准任务说明.md](纯视觉真实装备列表唯一目标只读校准任务说明.md) 与 `e7-gear-enhance-plan.md` 的状态同步。

## 验证要求

- 任何真实观察结论都必须来自同一唯一设备、同一视口、固定三帧稳定记录和已注册局部识别；不能由用户文字或 fixture 替代。
- 只有锚点、局部区域、候选可见边界、目标唯一性和全部字段逐项 `>=0.98` 时，才可报告真实列表页视觉证据；仍不得产生点击点或发送输入。
- 明确记录未执行的输入、导航、点击、强化、选材、确认和资源操作。

## 停止条件

- 缺少从内存 PNG 到列表页局部观测的接线、设备不唯一/不可用、截图或稳定性失败、局部识别不足、字段冲突/低置信度/目标不唯一时立即停止。
- 需要任何输入、导航、点击、文件落盘、全屏 OCR、上传、下载、第二批采样或资源操作时立即停止并另建任务。

## 当前状态

`historical_fail_closed_superseded_by_offline_memory_png_list_observer_20260730`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口未提供 Luna，本轮实际执行模型为 `gpt-5.6-terra + high`。

## 授权记录（2026-07-29）

- 用户明确回复“授权下一步”。本任务将该授权限定为上述一次真实列表页只读校准，不推定任何更广泛的自动化或资源操作权限。

## 接口审阅与停止记录（2026-07-29）

- 只读审阅确认 `EquipmentListVisualParser` 只接收调用方已产生的纯内存列表观测；仓库中除其自身和合成单元测试外，没有调用者将 `VisualFrame`、`StableFrames` 或内存 PNG 转换为该观测。
- 现有 `AdbLocalRecognitionEvidenceParser` 只委托 `LocalRecognitionEvidenceParser`，后者使用 `equipment_regions()`、`parse_backpack_enhance_lines()` 和右侧详情面板局部区域；它不能产生列表页锚点、候选卡片、可见边界、滚动状态、候选字段或 `EquipmentListVisualParser` 所需的输入。
- 因“内存 PNG 到列表页局部观测接线”缺失，按步骤 1 和停止条件在设备接触前 fail-closed：未运行 `adb.exe devices -l`，未截图，未执行 OCR 推理、输入、导航、点击、强化、选材、确认、资源消耗、底层读取、上传或模型下载。
- 当前状态：`real_list_readonly_calibration_not_started_fail_closed_missing_memory_png_list_observer_20260729`。本次一次性授权未被用于任何真实读写；下一步必须另建纯离线“列表页内存 PNG 局部观测器”开发任务，使用合成/公开 fixture 建立从稳定帧到解析器输入的受控接线和回归，之后才能重新申请新的真实只读授权。

## 后续状态（2026-07-30）

- 上述 `real_list_readonly_calibration_not_started_fail_closed_missing_memory_png_list_observer_20260729` 已被《纯视觉装备列表页内存PNG局部观测器离线开发任务说明.md》的 `offline_memory_png_list_observer_development_completed_20260730` 覆盖，仅作历史对照。
- 离线观测器现可把稳定内存 PNG、固定注册局部区域和注入式局部识别结果受控传给 `EquipmentListVisualParser`；该完成记录不产生任何真实页面证据，亦未运行 ADB、截图、OCR、输入、导航、点击或资源操作。
- 本任务原有一次性真实只读授权已终止且不得复用。继续真实校准前必须新建任务说明并重新取得明确授权；新授权不包含输入、导航、点击、强化、选材、确认或资源消耗。

## 历史状态补充（2026-07-30）

- 本任务仅作历史对照。其后的[纯视觉真实装备列表页三帧只读校准第二次任务说明](纯视觉真实装备列表页三帧只读校准第二次任务说明.md)已经独立审阅，并在 ADB 前因缺少真实 `ListPageLocalRecognizer` 实现和已审定的列表页区域注册而 fail-closed。
- 未运行设备发现或截图；该第二次授权同样已经终止且不得复用。不得把本任务或第二次任务的离线观测器/fixture 当作当前 MuMu 视觉证据。
