# 纯视觉真实 Windows 只读预检任务说明

## 目标

在用户已人工打开的 MuMu/Epic Seven 装备详情页上，完成一次真实 Windows 窗口只读预检：唯一定位目标窗口，采集当前客户区截图，并在内存中执行局部页面/装备详情识别。该任务只验证视觉输入链路，不进入强化运行。

## 范围

- 使用 Computer Use Windows API 列出已存在应用和窗口，要求目标窗口唯一且由 API 返回。
- 调用窗口状态截图能力，读取当前已打开的装备详情页；截图只用于本轮内存识别，不落盘、不上传。
- 复用 `src/e7_enhance/visual_platform.py` 的局部区域和证据绑定边界，记录视口、截图标识、时间戳、来源和字段置信度。
- 只做页面状态、目标装备指纹和可识别字段的预检；结果仍标记为 `mode=visual_only`、`verification=unverified`。

## 禁止修改与禁止操作

- 不启动、重启、激活、移动或点击 Epic Seven/MuMu 窗口；不发送键盘、鼠标、ADB 输入。
- 不强化、不选材、不改装备、不消耗资源，不读取或上传游戏底层数据，不调用 Fribbels 或其他外部接口。
- 不保存、读取、恢复、暂存或上传私人归档；不修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout、自动化规则或新鲜快照契约。
- 不修改或删除 `refs/heads/main - 副本`；不得使用 `git add -A`。

## 权威输入

- 本文件是本轮唯一任务来源。
- 设计和离线接线依据：`纯视觉采样方案设计任务说明.md`、`纯视觉运行时接线实现任务说明.md`、`纯视觉真实截图与窗口后端接线任务说明.md`、`纯视觉只读截图与局部识别接入任务说明.md`。
- 用户授权口径：当前已人工打开背包中的一件装备详情页，允许进行只读截图和局部状态识别。

## 执行步骤

1. 记录当前任务说明、总计划和 Git 状态，确认没有未授权的窗口输入。
2. 通过 `list_apps()`/`list_windows()` 选择唯一已返回的 MuMu/Epic Seven 目标窗口；候选不唯一或不存在即停止。
3. 对该窗口调用 `get_window_state({ include_screenshot: true })`，只读取截图；不调用激活、点击、键盘或滚动 API。
4. 在内存中检查截图 PNG/视口/时间戳和装备详情局部锚点，尝试绑定目标指纹与字段证据；任一失败统一 fail-closed，禁止自动重试。
5. 精确审阅预检输出，将完成项、验证结果、未完成项、风险和下一步同步回本文件与 `e7-gear-enhance-plan.md`，再提交并推送文档变更。

## 产物

- 一次性真实窗口只读预检报告（仅聊天和 Markdown 摘要，不保存截图或底层载荷）。
- 本文件与总计划中的状态记录。

## 验证要求

- 目标窗口唯一、截图成功且为 PNG 字节；视口、来源和时间戳可绑定。
- 局部识别仅在区域有效、模板/解析器存在、字段置信度达到现有 `0.98` 门槛且字段无矛盾时通过。
- 结果必须明确区分真实截图预检和 fake/offline 测试，不得写成服务端确认或强化成功。

## 停止条件

- 需要任何窗口输入、页面导航、设备控制、网络上传、底层数据读取、强化、选材或资源操作时立即停止。
- 无法唯一确认窗口、截图、稳定页面、局部区域、字段置信度或目标指纹时 fail-closed，禁止自动重试。

## 预检结果

- `list_apps()` 返回 MuMu Android 设备窗口唯一候选：`MuMu安卓设备`（窗口 ID `3276970`）；未激活该窗口。
- 对该已返回窗口调用 `get_window_state({ include_screenshot: true })` 时，Windows 截图后端返回：`SetIsBorderRequired failed: 不支持此接口 (0x80004002)`。
- 未获得 PNG 截图、视口或时间戳，因此未执行局部模板/OCR、装备指纹绑定或字段判定；没有点击、导航、底层读取、上传或资源操作。

## 当前状态

`real_windows_capture_failed_fail_closed_20260726`

- 执行模型记录：`gpt-5.6-terra + high`。
- 离线适配层和局部识别接线已验证；真实 Windows 截图驱动实例化因系统捕获接口不支持而失败。
- 下一步必须另建“Windows 只读截图驱动兼容修复”任务，先实现并离线验证一个不依赖 `SetIsBorderRequired` 的只读捕获后端，再重新取得真实预检授权；在此之前不得继续调用截图、切换窗口或进入强化。
