# 策略与自动强化执行边界报告修订任务说明

## 目标

根据已完成的审阅结论和用户明确批准，形成《策略与自动强化执行边界报告》修订稿，准确区分当前已冻结的策略 V1、当前仍有效的单件单节点授权契约，以及未来多节点自动强化尚未实现的执行要求。

## 范围

- 保留已经核验成立的三类装备策略路由、Epic 早期止损、Heroic 边界和一次转换限制。
- 将 V1 manifest、未知套装 fail closed、Epic `+0/+3` 规则可见性移入“已完成前置条件”。
- 明确“单件目标上限 `+15`”是未来候选执行契约，当前仍按“一件装备、一个复核节点、到点重新授权”执行。
- 补充新鲜快照、离散整数预算、硬上限、非标准节点、幂等状态机和故障恢复要求。
- 同步总计划和审阅报告的修订状态。

## 禁止修改项

- 不修改正式策略、DP、评分、资源模型、转换规则、GUI、OCR、Holdout、策略 manifest 或自动化规则。
- 不实现预算器、执行状态机或模拟器控制。
- 不操作模拟器，不选择材料，不发送 ADB 输入，不消耗游戏资源。
- 不读取、恢复、暂存、提交或上传私人证据归档。
- 不把报告修订解释为后续开发授权或实际强化授权。

## 模型与执行分工

- 本轮仅由主 agent 完成 Markdown 修订，不执行代码任务。
- 用户指定后续所有代码实现和代码测试任务使用 `gpt-5.6-terra + high`；启动子 agent 时必须记录实际模型与 reasoning effort，若该模型不可用则停止并报告，不得静默换用其他模型。

## 输入与权威口径

- 用户提供的《策略与自动强化执行边界报告》全文。
- [策略与自动强化执行边界审阅报告](reports/策略与自动强化执行边界审阅_20260725.md)。
- [策略 V1 manifest](reports/policy_v1_manifest_20260725.md)及同名 JSON；当前核心 SHA-256 为 `050041e19c7626d20deb73e95e2894ce48a5c0e7cd685a4ae471914d7d97423b`。
- [策略 V1 剩余冻结闸门收口任务说明](策略V1剩余冻结闸门收口任务说明.md)。
- 根目录 `AGENTS.md` 和 `e7-gear-enhance-plan.md`。

## 执行步骤

1. 按审阅结论重组原报告，将当前事实、未来需求和禁止事项分开陈述。
2. 修正自动点击闸门状态，避免把已完成的 V1 项目继续列为待办。
3. 固定当前授权契约，并把未来 `+15` 多节点模型标记为尚待独立开发和授权的候选契约。
4. 明确预算、状态刷新、非标准节点和幂等恢复的 fail-closed 要求。
5. 更新审阅报告和总计划，运行文档一致性检查。

## 产物

- [策略与自动强化执行边界报告修订稿](reports/策略与自动强化执行边界报告_修订稿_20260725.md)。
- 更新后的[审阅报告](reports/策略与自动强化执行边界审阅_20260725.md)。
- 更新后的 `e7-gear-enhance-plan.md`。

## 验证要求

- 修订稿不得把 `ready` 解释为自动执行就绪。
- 已完成项和未实现项必须与当前 manifest、资源模型及单件汇总器边界一致。
- 当前授权契约、未来候选契约和实际操作授权必须明确分离。
- Markdown 链接、UTF-8 内容和 `git diff --check` 必须通过。

## 停止条件

- 若修订需要改变正式策略数值、资源常量、OCR 门槛或自动化行为，立即停止并另立任务。
- 若发现当前 manifest 状态或哈希已漂移，停止使用既有结论并重新核验。
- 未取得新的独立开发授权前，不开始预算器、状态机或模拟器试点。

## 当前状态

`completed_revision_docs_only_validated`
