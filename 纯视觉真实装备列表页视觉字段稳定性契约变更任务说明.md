# 纯视觉真实装备列表页视觉字段稳定性契约变更任务说明

## 目标

将装备列表页三帧只读校准的稳定性定义从“PNG 字节 SHA-256 完全相同”改为“固定三帧经同一局部识别器解析后的页面视觉字段签名完全相同”。保留视口、来源、时间单调、页面锚点、滚动边界、目标唯一、字段完整和最低置信度门槛。

## 范围

- 新增列表页专用采样帧批次类型/收集入口，允许动画或渲染噪声造成的 PNG 哈希变化。
- 列表页观测器对三帧分别执行已注入的局部 OCR/模板识别，并比较规范化视觉字段签名。
- 视觉字段签名至少包含页面锚点、列表滚动/边界、候选身份、候选视觉指纹和全部目标字段值；置信度仍逐帧校验且不得参与等值放宽。
- 将哈希变化记录为采样元数据，不再把它单独作为列表页稳定失败原因。
- 更新列表相关离线测试、任务说明、总计划和验证报告。

## 禁止修改项

- 不修改强化策略、DP、评分、资源模型、GUI、Holdout 或详情页真实强化执行契约。
- 不连接 ADB，不截图，不调用 `input`/`tap`/`swipe`/`keyevent`，不导航、不选材、不强化、不确认、不消耗资源。
- 不使用 fixture、历史截图或人工字段冒充真实页面证据。
- 不放宽页面/视口/字段/唯一性/置信度/来源和时间校验，不加入自动重试或第二批采样。

## 输入与权威口径

- 本文档、根目录 `AGENTS.md` 和 `e7-gear-enhance-plan.md`。
- 用户确认的目标装备字段：武器、红装/传说、85 级、强化 `+3`、生命值套装、主属性攻击、副属性攻击 `13%`、暴击 `3%`、速度 `2`、效果抗性 `8%`、分数 `37`。
- 列表页局部注册表、`RegisteredListPageLocalRecognizer`、`InMemoryPngListPageObservationSource` 为唯一离线识别边界。

## 执行步骤

1. 只读审阅现有 `StableFrames`、列表页观测器/解析器和测试。
2. 实现列表页专用采样批次，使三帧可保留不同 PNG 哈希但仍校验来源、视口和时间。
3. 让列表页观测器逐帧识别，并对规范化视觉字段签名做严格三帧一致性比较；任一字段缺失、冲突、候选集合变化或置信度不足立即 fail-closed。
4. 增加不同 PNG 哈希但视觉字段一致的通过测试，以及字段变化、候选变化、页面锚点变化和时间/视口漂移的拒绝测试。
5. 运行列表相关测试、全部 `test_visual*.py`、Python 3.9 语法检查和 `git diff --check`。
6. 本任务完成后，若要重新读取真实 MuMu，必须另建一次性只读校准任务说明并重新取得授权。

## 产物

- 列表页专用采样/稳定性实现与测试。
- 本任务说明、`e7-gear-enhance-plan.md` 更新和离线验证报告。

## 停止条件

- 任何需要设备、输入、联网、真实 OCR 推理、动态区域或额外采样的步骤立即停止。
- 视觉字段签名无法完整、唯一、逐帧一致或置信度低于 `0.98` 时 fail-closed。
- 发现既有详情页/强化执行契约被迫改变时停止并报告，不跨范围修改。

## 当前状态

`implementation_complete_offline_verified`

## 模型记录

用户偏好为 `gpt-5.6-luna + max`；当前协作接口不可用 Luna，本任务实际使用 `gpt-5.6-terra + high`。

## 实施回传与审阅（2026-08-01）

- 子 agent 已以 `gpt-5.6-terra + high` 提交列表页专用三帧批次、逐帧局部识别和视觉字段签名比较；未连接 ADB、未截图、未发送输入。
- 初步定向离线验证为列表相关 `28/28`、导航 `6/6` 通过，但主 agent 审阅发现导航层必须显式要求“视觉字段已由上游三帧比较通过”的证据标记，不能因页面类型为列表页而直接接受不同哈希。
- 主 agent 已补上 `visual_fields_stable=true` 证据标记、候选槽位进入签名以及“无标记拒绝、有标记通过、详情页仍拒绝哈希变化”的回归。
- 最终离线验证：`test_visual*.py` `108/108` 通过，Python 3.9 `py_compile` 退出码 `0`，`git diff --check` 退出码 `0`（仅既有 LF/CRLF 警告）。详细结果见[离线验证报告](reports/visual_list_visual_stability_offline_20260801.md)。
- 下一步不是复用旧授权直接接入真实设备；如需重新校准，必须另建一次性只读任务说明并重新取得授权，且先确认列表页无加载/滚动/动画导致的字段变化。
