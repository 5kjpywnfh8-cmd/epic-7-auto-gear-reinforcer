# 单件人工确认强化结果离线汇总器任务说明

## 目标

实现一个只读离线工具，读取单件人工确认操作目录中的 `operation_manifest.json`，严格校验操作证据，并汇总基线属性、影子建议、实际强化结果、资源变化和停止原因。

## 范围

- 只读取本地操作 manifest 及其引用的同批配对 JSON。
- 输出机器可读 JSON 摘要和 Markdown 报告。
- 支持单个 manifest 和操作目录递归发现，当前仅汇总 `operation_manifest.json`。
- 首个正式输入为 `manual_acceptance/single_item_confirmation/operation_004_plus3_20260723_153606/operation_manifest.json`；当前已复用校验 batch `013` 的独立操作包。

## 禁止修改项

- 不连接或操作 MuMu，不运行 ADB，不读取新的游戏数据。
- 不调用或修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化规则。
- 不修改可信快照、OCR 原始结果、操作证据、正式样本库或现有 manifest。
- 不把离线验证结论写入正式策略；`verified` 仅表示证据包内部一致。

## 输入与权威口径

- 本说明是本任务唯一执行来源，同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md` 和《单件人工确认强化结果验证任务说明.md》。
- 操作 manifest 必须为 `single_item_confirmation.operation/1.0`。
- 基线属性必须从 manifest 的 `trusted_reference.pairing_output` 引用的同批配对结果读取，不接受人工填写的实例 ID或基线替代值。
- 可信快照固定为 `20260722_132045`，其 `player_data.json` SHA-256 为 `3D74E2D69147D5A1053F0EBCCC217ED2C64842FA2CE91CCE6413E5FEA364185B`，同批 `reader_result.json` SHA-256 为 `3831F40814C015166794BF44FBD55D153668532D549E612AFF431C24E42F900B`。
- OCR 最低字段置信度必须不低于 `0.98`；唯一候选、建议哈希、操作授权和资源边界必须可验证。

## 执行步骤

1. 先读取本说明、项目规则、总计划和开发技能；确认只做本地离线工作。
2. 先补定向失败测试，覆盖有效 manifest、配对/哈希/资源闸门失败和多 manifest 汇总。
3. 实现独立汇总模块和 CLI；校验失败时输出 `fail_closed` 记录，不生成通过结论。
4. 使用 batch `004` 操作包运行 CLI，生成独立 JSON 和 Markdown 报告。
5. 运行相关测试、CLI smoke 和 `git diff --check`。

## 产物

- `src/e7_enhance/single_item_confirmation.py`
- `tools/summarize_single_item_confirmation.py`
- `tests/test_single_item_confirmation.py`
- `reports/single_item_confirmation_summary_20260723.json`
- `reports/single_item_confirmation_summary_20260723.md`
- `reports/single_item_confirmation_summary_batch_013_20260724.json`
- `reports/single_item_confirmation_summary_batch_013_20260724.md`
- `reports/single_item_confirmation_summary_20260724.json`
- `reports/single_item_confirmation_summary_20260724.md`

## 验证要求

- 有效操作包必须明确报告：基线与实际属性、变化字段、影子建议、目标复核节点、授权材料、资源消耗、停止原因和正式策略未修改声明。
- 任一关键证据缺失、哈希不符、候选非唯一、置信度低于 `0.98`、资源超限、动作多于一次或节点不一致时，记录必须为 `fail_closed`，退出码非零。
- 不能把 `verified` 写成正式策略验证、模型校准或自动化发布结论。

## 停止条件

- 输入不是操作 manifest、引用配对文件不存在或关键字段无法严格核验时停止该记录并 fail closed。
- 不访问模拟器、不发送输入、不扩展到下一件装备或下一复核节点。

## 当前状态

`completed_verified_batches_004_013_offline_summary`

## 完成记录（2026-07-23）

- 已实现 `src/e7_enhance/single_item_confirmation.py` 和 `tools/summarize_single_item_confirmation.py`；模块不导入正式策略、DP、评分、资源模型、GUI 或模拟器。
- 已覆盖 manifest schema、设备/页面/配对/OCR/可信快照/建议哈希、基线属性、结果属性、资源上限、单次动作、复核节点和立即停止证据；失败项统一输出 `fail_closed`。
- batch `004` 实际操作包离线汇总结果为 `verified`：`manifest_count=1`、`verified_count=1`、`fail_closed_count=0`，正式策略修改为 `false`。
- 定向测试 `tests.test_single_item_confirmation` 为 `5/5` 通过；三份新增 Python 文件 `py_compile` 通过；CLI smoke 退出码 `0`；目标文件 `git diff --check` 通过（仅既有换行格式提示）。
- 报告：[JSON](reports/single_item_confirmation_summary_20260723.json) 和 [Markdown](reports/single_item_confirmation_summary_20260723.md)。

## batch 013 复用验证记录（2026-07-24）

- 未修改汇总器代码；直接读取 `operation_013_plus3_20260724_112042/operation_manifest.json` 及其引用的同批 `pair_batch_013.json`。
- 013 单包结果为 `verified`：`manifest_count=1`、`verified_count=1`、`fail_closed_count=0`，退出码 `0`。
- 根目录合并结果为 `verified`：batch `004/013` 共 `2/2` 操作包通过，`fail_closed_count=0`，旧 004 证据未被破坏。
- `tests/test_single_item_confirmation.py` 已把根目录发现和合并断言从单一 004 更新为 004/013 两个操作包；最终 `5/5` 通过、退出码 `0`。生产汇总器代码保持不变。
- 报告：[013 JSON](reports/single_item_confirmation_summary_batch_013_20260724.json)、[013 Markdown](reports/single_item_confirmation_summary_batch_013_20260724.md)、[合并 JSON](reports/single_item_confirmation_summary_20260724.json) 与 [合并 Markdown](reports/single_item_confirmation_summary_20260724.md)。
- 当前状态：`completed_verified_batches_004_013_offline_summary`。该结论仅表示离线证据内部一致，不修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化规则。
