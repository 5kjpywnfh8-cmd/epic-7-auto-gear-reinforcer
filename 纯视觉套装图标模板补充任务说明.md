# 纯视觉套装图标模板补充任务说明

## 目标

为纯视觉 ADB 装备详情识别补充一套可离线分发的套装图标候选模板。模板来源固定为用户指定的公开 Fribbels-Epic-7-Optimizer 仓库，运行时不得联网下载；模板只作为局部识别输入，不等同于字段确认或服务器确认。

## 唯一权威来源

本文件是本轮唯一权威任务来源。视觉帧、ADB 设备发现、禁止输入和 `mode=visual_only`/`verification=unverified` 继承《纯视觉 ADB 截图与受控点击迁移任务说明.md》；局部区域、PaddleOCR 和 `0.98` 门槛继承《纯视觉套装局部裁剪与强化证据锚点修正任务说明.md》及现有实现。

## 范围

- 从 `fribbels/Fribbels-Epic-7-Optimizer` 的固定公开版本获取 `app/assets/set*.png` 套装图标。
- 在仓库内保存二进制模板及来源/尺寸/SHA-256 清单；运行时仅读取本地文件。
- 提供只读模板清单加载接口，校验模板路径、PNG 签名、尺寸和哈希；缺失、损坏或哈希不匹配必须 fail-closed。
- 补充公开测试，覆盖完整清单、来源固定、PNG/哈希校验、未知套装和损坏文件拒绝。
- 不实现模板匹配算法，不改变现有 `LocalTemplateRecognizer` 注入接口；后续匹配器必须另建任务说明。

## 禁止修改与禁止操作

- 不修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛、自动化规则、ADB 帧源或点击执行器。
- 不执行 ADB、截图、OCR、页面导航、点击、强化、选材、资源消耗、底层读取、Fribbels/PCAP/TCP 上传或私人归档操作。
- 不在运行时访问 GitHub 或其他网络地址；不把用户截图直接写成机器字段证据。
- 不触碰 `refs/heads/main - 副本`，不使用 `git add -A`；只暂存本任务文件、模板资产、实现和测试。

## 输入与权威口径

- 来源仓库：`https://github.com/fribbels/Fribbels-Epic-7-Optimizer`。
- 来源路径：`app/assets/set*.png`；本轮固定抓取当前公开主线内容，并在清单记录每个原始 blob SHA 与下载 URL。
- 当前目标装备为命中套装；因此 `sethit.png` 必须在清单中，但模板集同时覆盖来源仓库现有全部 `set*.png` 文件。
- 模板只表示视觉候选，最终字段仍须通过局部模板/文字证据、稳定帧和既有 `0.98` 门槛；结果继续为 `mode=visual_only`、`verification=unverified`。

## 执行步骤

1. 完整阅读本文件、`AGENTS.md`、`e7-gear-enhance-plan.md` 和前置纯视觉任务说明，审阅现有模板注入接口。
2. 下载并校验来源图标，写入本地模板目录和固定清单；不保留临时外部缓存。
3. 增加只读清单加载/校验接口，不改变现有匹配器协议。
4. 补充离线测试，覆盖正常加载、缺失/损坏/哈希不匹配和清单来源固定。
5. 精确审阅 diff，运行定向测试、语法检查和 `git diff --check`；不得连接设备。
6. 将完成项、验证结果、未完成项、风险和下一步写回本文件与总计划。

## 产物

- `assets/visual/set_icons/fribbels/*.png` 离线模板文件。
- 模板来源与校验清单及只读加载器。
- 公开测试和验证记录。

## 验证要求

- 每个模板必须是有效 PNG，尺寸和 SHA-256 与清单一致。
- 清单必须包含 `sethit`，且来源仓库/原始路径/固定版本完整。
- 任何单个模板校验失败都必须拒绝整套清单，不得降级为部分可信模板。

## 停止条件

来源文件不存在、下载内容不是 PNG、尺寸/哈希无法固定、清单加载失败或任一测试失败时立即停止并 fail-closed；不得进入真实 ADB、模板匹配或点击任务。

## 完成项与验证记录（2026-07-26）

- 从 Fribbels 主分支提交 `b291cbbc415f11abede146859edc7b67d26e9c4b` 固定获取 `app/assets/set*.png` 共 26 个，包含当前目标 `sethit.png`；清单记录仓库、原始路径、raw URL、blob SHA、完整文件 SHA-256、尺寸和字节数。
- 资产写入 `assets/visual/set_icons/fribbels/`，运行时不请求网络。上游部分 PNG 在首个合法 `IEND` 后带有资源尾缀；保留原始完整字节并校验完整文件哈希，只解析首个 PNG 流的 IHDR/CRC/IEND。
- 新增 `src/e7_enhance/visual_templates.py` 的只读加载器：校验清单 schema、固定仓库/提交、路径不越界、PNG 结构、尺寸、字节数、SHA-256 和 `sethit` 必需项；任一项失败都拒绝整套模板。
- 新增 `tests/test_visual_templates.py`，覆盖默认 26 项完整清单、命中套模板、缺失文件、哈希损坏、路径篡改和无网络加载。
- 定向模板测试 `5/5` 通过；视觉 ADB/平台/运行时/采样/适配回归 `43/43` 通过；OCR 相关回归 `27/27` 通过；来源提交 tree 校验 `26/26` blob 无差异；`py_compile` 和 `git diff --check` 通过。
- 本轮未执行 ADB、截图、OCR、模板匹配、页面导航、点击、强化、选材、资源消耗、底层读取或外部上传。

## 未完成项、风险与下一步

- 本轮只提供离线资产和校验加载器，尚未实现 `LocalTemplateRecognizer` 的图像匹配，也未证明真实 ADB 帧中的套装图标能达到 `0.98` 识别门槛。
- 上游仓库根目录未声明 LICENSE；清单保留 `source_license=not_declared_in_repository_root`，后续分发前需完成许可证/再分发审查。
- 下一步需另建任务说明，设计并离线验证基于这些模板的局部匹配器；匹配器通过后，才可申请一次新的真实只读 ADB 单帧校准。受控点击、页面导航和自动强化仍不在本任务范围内。

## 当前状态

`offline_set_icon_assets_verified_pending_local_matcher_20260726`

执行模型：`gpt-5.6-terra + high`。
