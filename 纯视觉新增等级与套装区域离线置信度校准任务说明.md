# 纯视觉新增等级与套装区域离线置信度校准任务说明

## 目标

基于用户提供的 `C:\Users\orangine\Downloads\before_page(1).png` 新布局参考图，离线验证新增装备等级区域、调整后的套装图标区域和套装文字区域能否在现有视觉契约下形成稳定字段；随后用上一轮速度套截图 `C:\Users\orangine\Downloads\before_page.png` 做同口径复测。仅在离线证据明确后决定是否进入后续实现或新的真实只读校准。

## 唯一权威来源

本文件是本轮唯一权威任务来源。同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md`、现有纯视觉 OCR/ADB 任务说明及当前视觉接口。聊天内容不能替代或扩大本文件范围。

## 输入与验证口径

- 输入仅限用户明确提供的两张本地截图：`before_page(1).png`（新增区域参考）和 `before_page.png`（上一轮速度套复测）。
- 只做离线内存裁切、PaddleOCR/模板候选观测、字段置信度记录和公开测试；不得把截图写入仓库、不得读取私人归档或游戏底层数据。
- 装备等级必须来自右侧详情装备图标左上角的独立局部；套装必须分别记录图标模板候选与 `set_text` 文字候选，二者不能以广域 OCR 缺失互相补齐。
- 置信度门槛固定为 `0.98`，唯一性、字段矛盾、视口/区域越界或依赖缺失均 fail-closed；不得降低或绕过门槛。
- 结果继续标记 `mode=visual_only`、`verification=unverified`、`click_performed=false`。

## 范围

1. 审阅现有 `ocr_regions.py`、`ocr_paddle.py`、套装模板匹配器、公开测试和当前任务记录。
2. 依据新截图建立候选 `level`、`set_icon`、`set_text` 局部边界，并在内存中验证边界覆盖和视口缩放。
3. 记录两张截图的等级 OCR 置信度、套装文字置信度、套装模板唯一性和拒绝原因；复测上一轮速度套是否达到完整字段契约。
4. 若候选局部达到契约，可在本任务内做最小离线区域/测试接线；不得执行真实 ADB 校准。

## 禁止修改与禁止操作

- 禁止 ADB `devices`、`exec-out screencap -p`、`input tap`、`swipe`、`keyevent`、页面导航、点击、强化、选材、资源消耗和游戏重启。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、外部上传、模型下载和联网。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR `0.98` 门槛、Holdout 或自动化规则。
- 禁止把装备名称、截图中的人工标注文字或 OCR 缺失推断为等级/强化证据。
- 不得使用 `git add -A`，不得触碰 `refs/heads/main - 副本`，不得回退用户已有改动。

## 执行步骤

1. 完整读取本文件及上位文档，确认模型记录为 `gpt-5.6-terra + high`。
2. 离线检查两张输入截图的尺寸、PNG 结构和视口一致性；截图只在内存中处理。
3. 用新布局候选局部分别运行一次等级 OCR、套装文字 OCR 和套装图标模板观测；不得为提高结果自动重试。
4. 用同一候选局部复测 `before_page.png` 的速度套，输出字段置信度、唯一性和 fail-closed 原因。
5. 若需要代码接线，只做最小区域定义/测试变更；精确审阅 diff，运行定向测试、语法检查和 `git diff --check`。
6. 将完成项、验证结果、未完成项、风险、当前状态和下一步写回本文件与 `e7-gear-enhance-plan.md`。

## 产物

- 本任务的离线区域坐标、置信度和拒绝原因记录。
- 如有必要，最小区域定义/测试变更及其定向测试结果。
- 不生成或提交截图副本，不输出可用于点击的坐标。

## 停止条件

- 输入图损坏、尺寸/方向不符、区域越界、PaddleOCR/模板依赖缺失、任一字段低于 `0.98`、候选不唯一或字段矛盾时立即 fail-closed。
- 任一停止条件触发，不能进入真实 ADB 只读校准、点击接线或自动强化；必须另建任务说明并重新取得授权。

## 执行记录（2026-07-27）

- 两张输入均为 `1280x720` PNG；SHA-256 分别为 `before_page(1).png = F49342ACE44582FA8CCAC1F7576D7663947D86F0A0DFDA692407B38DCF8941B7`、`before_page.png = EB15A762C9D3A5100492DC3FBB23739710946457A1D1894B3D7014B36E7D355A`。只读内存处理，未生成裁剪文件。
- 以用户标注推导的装备等级局部 `(876,172)-(923,218)`：`before_page(1).png` 识别 `85 @ 0.995268`，`before_page.png` 识别 `85 @ 0.990371`，均达到 `0.98`。
- 以用户标注推导的套装文字局部 `(916,544)-(1034,592)`：`速度套装(0/4) @ 0.935075`、`0.940840`，均低于 `0.98`；不能作为套装确认。
- 以套装图标候选局部 `(875,540)-(925,596)` 运行现有 `LocalSetIconTemplateRecognizer`：两张图均无唯一候选。
- 现有模板 bundle 可加载 26 个条目，但匹配器把完整模板文件直接交给严格 PNG 解码：26 个中 17 个在 `IEND` 后含上游附加字节而解码失败，8 个因当前解码器不支持其 PNG 格式，只有 `weakening` 可解码；因此 `speed` 模板尚未真正进入像素匹配，空候选是实现缺口而非本次截图的置信度结论。
- 现有完整 Paddle 链：`before_page(1).png` 解析拒绝 `missing:enhance`、`unrecognized:level`、`unrecognized:set`；`before_page.png` 解析拒绝 `low_confidence:set`、`missing:enhance`。局部等级结果已证明新的等级区域可行，但完整速度套仍未达标。
- 结果继续按 `mode=visual_only`、`verification=unverified`、`click_performed=false` 处理；未执行 ADB、截图、点击、强化、选材、资源消耗、底层读取、上传或模型下载。

## 当前状态

`offline_visual_level_pass_set_text_low_template_decoder_gap_fail_closed_20260727`

执行模型：`gpt-5.6-terra + high`。

## 未完成项、风险与下一步

- 新等级局部已经通过离线 `0.98`，但速度套文字未通过，图标模板尚未真正比较，强化证据仍缺失；速度套当前不能进入自动强化或 ADB 点击接线。
- 必须另建离线修复任务：先让模板匹配器只解码首个合法 PNG 流并覆盖不支持格式，补充真实模板回归；再评估套装文字局部的固定裁切/预处理，仍保持 `0.98`。
- 离线修复和公开测试通过后，才可另建真实只读校准任务并重新取得授权；本任务不执行真实 ADB 或自动重试。
