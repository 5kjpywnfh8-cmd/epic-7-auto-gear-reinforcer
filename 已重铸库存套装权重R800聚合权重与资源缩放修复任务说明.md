# 已重铸库存套装权重 R800 聚合权重与资源缩放修复任务说明

## 状态

`completed_weighted_closure_stable_no_policy_release`

## 根因

R800 收口审计报告的轨迹分片完整，但最终聚合存在两个确定性错误，因此 `7811.16 [6674.63, 9031.69]` 与相对半宽 `15.09%` 不得用于扩样决策：

1. `_cluster_flow()` 先把每个 cluster 除以自身 `samples_per_set`，随后 `_aggregate()` 将 5 个 R50 cluster 与 20 个 R800 cluster 等权相加。R50 每 cluster 仅 50 条、R800 每 cluster 800 条；旧实现使 R50 占总点估计的 `5/25=20%`，而按实际独立路径质量应为 `5×50/(5×50+20×800)=1.53846%`。这违反“先按 seed/block 汇总分子分母，再计算总体比值”的预注册口径。
2. `_aggregate()` 使用 `cycles=100000×cluster_count/sum(denominator)` 后，又乘 `sum(rift_stamina)` 和 `sum(saint_stamina)`，重复乘入 cluster 数。旧报告裂缝与圣女体力合计为 `2,500,000`，不是 `100,000`。

只读诊断显示：不混入 R50、仅使用 20 个同规模 R800 cluster 时，库存主权重当前策略为 `8507.19 [7910.44, 9131.16]`、相对半宽 `7.17%`；top4 为 `6783.69 [6333.20, 7302.45]`、相对半宽 `7.14%`。按路径质量计算的 R50+R800 点估计约为库存 `8449.15`、top4 `6769.57`。这些只是修复前诊断，不得直接写成最终报告值。

## 目标

- 为每个来源 cluster 显式保存 `sample_mass`：R50 为 50，R800 为 800；所有分子、分母、资源和终局产量先乘该质量，再求总体比值。
- 绝对区间改为按来源分层的 cluster bootstrap：R50 层固定重采样 5 个 cluster，R800 层固定重采样 20 个 cluster，每个被抽中的 cluster 保留自身 `sample_mass`。不得让 bootstrap 随机改变 R50/R800 设计配比。
- 正确计算：
  - `stamina_per_baili = Σ(m_i×stamina_i) / Σ(m_i×baili_i)`；
  - `formal_baili_per_100k = 100000×Σ(m_i×baili_i) / Σ(m_i×stamina_i)`；
  - `rift/saint/75+/22速 per_100k = 100000×Σ(m_i×对应量_i) / Σ(m_i×stamina_i)`；
  - `cycles_per_100k = 100000×Σ(m_i) / Σ(m_i×stamina_i)`。
- 每个策略与场景强制验证 `rift_stamina_per_100k + saint_stamina_per_100k = 100000`，允许的仅是浮点误差。
- 使用现有 R50+R800 分片重新输出库存主权重与 `top4_equal` 四策略结果；R200/R400 不得计入。

## 测试要求

- 先新增失败测试，覆盖不等 cluster 样本量的手工分子/分母、R50 路径质量占比 `1.53846%`、按来源分层 bootstrap、十万体力守恒、循环与三项终局产量缩放。
- 增加回归断言，确保旧的 `cycles×sum(rift)` 双重 cluster 乘法不能恢复。
- 旧 R800 报告必须标记为 `superseded_due_to_unequal_cluster_weight_and_resource_scaling_error`；修复结果使用新 schema、新 JSON 和新 Markdown，不得静默覆盖。

## 停止条件

- 本任务只读聚合现有分片，不生成新轨迹，不运行 R1600。
- 修正后的库存主权重相对半宽若 `<=10%`，停止扩样并完成收口。
- 若仍 `>10%`，先报告 R50 层、R800 层和跨层的方差分解，再建立独立任务决定增加 R 值还是增加 seed cluster；不得自动运行 R1600。
- 不冻结候选、不启动或读取 holdout，不修改正式策略、DP、资源模型、评分、跳值、GUI、OCR 或自动化。

## 产物与验证

- 修正后的只读审计器与专用测试。
- 新的 R50+R800 聚合 JSON、人类报告及方差/守恒诊断。
- 更新本任务、上一任务和 `e7-gear-enhance-plan.md`。
- 运行定向测试、语法检查、`git diff --check` 和 `tools/run_all_tests.py`，记录真实退出码。

## 完成记录（2026-07-18）

- 已先新增失败测试并确认旧实现失败，再修复为 schema `2` 的只读聚合。R50 五个 seed cluster 固定 `sample_mass=50`，R800 二十个 block cluster 固定 `sample_mass=800`；R200/R400 未读取、未计入，未生成新轨迹。
- 所有正式百里分、总体力、裂缝/圣女体力、循环、原生75+、转换75+与22速均先按 `sample_mass` 求和。绝对成本和成对变化均改为保持 R50/R800 层数量与质量固定的来源分层 cluster bootstrap。
- 库存主权重当前正式策略为 `8449.15 [7879.12, 9083.25]` 体力/正式百里分，相对半宽 `7.13%`；`top4_equal` 为 `6769.57 [6325.45, 7284.80]`，相对半宽 `7.08%`。均通过 `<=10%` 闸门，停止扩样，不运行 R1600。
- 每个场景、每个策略均验证 `裂缝体力 + 圣女体力 = 100000`（仅浮点误差）。新产物为 `reports/reforged_inventory_set_weight_r800_weighted_closure_audit_20260718.{json,md}`；旧 `reforged_inventory_set_weight_r800_closure_audit_20260718.md` 保持 `superseded_due_to_unequal_cluster_weight_and_resource_scaling_error`。

## 最终验证（2026-07-18）

- 新增审计器专用回归 9 项通过，包含先红后绿的非等 cluster 质量、R50 路径质量占比、来源分层 bootstrap、十万体力守恒、循环和三项终局产量缩放，以及旧双重 cluster 乘法回归保护。
- `python -B -m py_compile tools/research_reforged_inventory_set_weight_r400_audit.py tests/test_research_reforged_inventory_set_weight_r400_audit.py`、`git diff --check` 均为退出码 `0`。
- `python -B tools/run_all_tests.py` 真实退出码 `0`，输出 `ALL TEST FILES PASSED`。`test_gui_app.py::real_sample_import_selects_its_acceptance_batch` 首次 Qt 原生退出后由既有隔离重试通过；本轮未修改 GUI。

## 主 agent 最终收口审计（2026-07-18）

- 已核对 sample_mass、来源分层 bootstrap、每十万体力守恒、四策略共同路径区间及 R50/R800 设计质量。库存主权重和 top4 的相对半宽分别为7.13%和7.08%，本研究线停止扩样，不运行R1600。
- 库存主权重与top4对四策略的效率方向一致。该结果只完成离线统计收口，不自动冻结候选；下一步必须由用户在效率与误停风险之间重新选择档位。
- 主 agent 只读核验和文档差异检查通过；未再次运行全量测试，最终测试结论沿用本任务已记录的真实退出码0。
- 口径澄清：研究器 `_scope_gear()` 对 Epic/Heroic 均强制非鞋且初始无速度，故`8449.15`只表示非速度条件池的完整联合成本，不是账号全装备平均效率；22速为0是结构性范围结果，不得解释为账号不会产出22速。
