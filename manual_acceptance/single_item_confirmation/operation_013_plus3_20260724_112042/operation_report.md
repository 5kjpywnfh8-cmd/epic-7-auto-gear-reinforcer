# batch 013 单件人工确认强化结果

## 结论

- 状态：`completed_target_013_plus3_stopped`
- 目标：batch `013` / 配对器实例 `4182712419` / 85 级 Epic 速度套项链
- 实际操作：仅执行一次 `+0 -> +3` 强化确认；到达 `+3` 后停止，未继续到 `+6`

## 操作前闸门

- MuMu 进程正常，ADB 每次输入前均重新检查，接受恰好一个 `device`，未保存设备地址。
- Computer Use 窗口截图持续返回已知接口错误 `0x80004002`；按既有授权回退为不指定设备的 ADB 二进制流截图。
- 当前页面字段与 `pair_batch_013.json` 一致：85 级 Epic 速度套项链，主属性生命值 `12%`，副属性防御力 `8%`、速度 `3`、效果抗性 `6%`、效果命中 `6%`，游戏官方展示装备分数 `30`。
- 可信快照 `20260722_132045` 的 `player_data.json` 与同批 `reader_result.json` 哈希均一致；候选数 `1`，最低 OCR 置信度 `0.990471`，建议哈希一致。
- 最终预览为粉末 `2`、下级强化石 `1`、上级强化石 `0`、金币 `17,600`，目标 `+3`。数量弹窗按总数设置；中间一次“登记 1”未追加粉末，最终截图发现后改为总数 `2`，未发生提前消耗。

## 实际结果

| 字段 | 操作前 | 操作后 |
|---|---:|---:|
| 强化等级 | +0 | +3 |
| 主属性生命值 | 12% | 19% |
| 防御力 | 8% | 8% |
| 速度 | 3 | 7 |
| 效果抗性 | 6% | 6% |
| 效果命中 | 6% | 6% |
| 游戏官方展示装备分数 | 30 | 41 |

本次副属性强化命中速度，`3 -> 7`（`+4`）。主属性生命值同步由 `12% -> 19%` 增长；游戏官方展示装备分数由 `30 -> 41`，仅作为结果记录，不作为本次强化策略输入，也不改变正式评分模型。影子建议为 `continue`、下次复核 `+3`、目标“速度潜力”；实际到达同一复核节点，仅作为单件证据一致性记录。

## 资源与停止

- 粉末：`2`，未超过上限 `2`
- 下级强化石：`1`，未超过上限 `1`
- 上级强化石：`0`，未超过上限 `0`
- 金币：`346,670,175 -> 346,652,575`，消耗 `17,600`
- 停止原因：`review_checkpoint_plus3_reached; no further operation sent`

## 证据

- 操作前：[before_authorized.png](before_authorized.png)，SHA-256 `8f50f1608579c9b094e41211f5145460c459c04bf5be2f0c9c48cf5e925b1e57`
- 操作后：[after.png](after.png)，SHA-256 `37ea95c5f5e7e1abee68499dde2d3322e33f283b3011221e123255c89d71c376`
- 机器可读记录：[operation_manifest.json](operation_manifest.json)

本次结果不修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR 门槛或自动化规则。
