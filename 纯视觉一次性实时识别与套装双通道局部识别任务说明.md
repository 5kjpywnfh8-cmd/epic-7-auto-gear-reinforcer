# 纯视觉一次性实时识别与套装双通道局部识别任务说明

## 目标

将纯视觉强化识别口径收敛为“每个强化节点都对当前页面做一次新鲜视觉采样”，不要求跨运行或跨页面保存装备绑定。装备名称不属于强化策略输入，可以缺省；套装证据拆成更窄的图标模板通道和独立文字 OCR 通道，任一通道不满足契约即保持 fail-closed。

## 唯一权威来源

本文件是本轮唯一权威任务来源。同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md` 以及现有视觉/OCR 接口。聊天中的口径不能替代本文件。

## 范围

- 明确一次性实时识别的接口语义：同一强化操作内只接受当前稳定帧、当前节点和当前资源证据；不建立跨运行装备档案绑定。
- 保留操作级 `operation_id`、稳定帧哈希和视觉指纹，用于拒绝旧帧、页面漂移和点击前后证据错配；这不是持久化装备绑定。
- 将装备名称从必需识别字段中明确排除；品质、部位、等级、套装、主属性、副属性、分数、强化节点和资源/按钮证据仍按各自契约校验。
- 将套装局部裁剪拆成 `set_icon` 与 `set_text`：图标通道使用本地模板匹配，文字通道使用局部 PaddleOCR；保留兼容回退入口但不扩大到全屏盲猜。
- 仅做离线实现、合成 PNG/文本测试、语法检查和 `git diff --check`；不执行真实 ADB、截图、OCR 推理、页面导航、ADB 输入、点击、强化、选材、资源消耗、底层读取、外部上传或模型下载。

## 禁止修改与禁止操作

- 不修改正式策略、DP、评分、资源模型、GUI、Holdout、OCR `0.98` 门槛、ADB 帧源、点击执行器或自动化规则。
- 不把装备名称缺失当作身份确认，不用名称缺失推断其它字段；不把套装文字或图标候选单独表述为服务器确认。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、私人归档和真实截图落盘/上传。
- 禁止 `adb input tap/swipe/keyevent`、页面导航、强化、选材、重启游戏和资源消耗；不使用 `git add -A`，不触碰 `refs/heads/main - 副本`。

## 输入与权威口径

- 现有 `VisualFrame`、`RegionSample`、`LocalTemplateRecognizer`、Paddle 行解析器、模板资产清单和公开合成 fixture。
- `0.98` 是不可下调的字段/模板门槛；输出继续标记 `mode=visual_only`、`verification=unverified`。
- 一次性实时识别只改变绑定生命周期，不改变 fail-closed：当前帧缺字段、低置信度、区域越界、模板并列、字段矛盾或旧帧均拒绝。

## 执行步骤

1. 审阅现有视觉运行时、OCR 区域、套装模板匹配器和公开测试，确认装备名称未被隐式设为必需字段。
2. 为红装详情布局增加更窄的 `set_icon`/`set_text` 局部区域；模板匹配器优先使用 `set_icon`，OCR 只读取 `set_text`，兼容旧 `set_anchor`/`set` 注入名。
3. 补充公开测试：装备名称缺失仍可通过；图标/文字各自区域越界、损坏、低置信度、并列命中和缺失均 fail-closed；完整适配链仍保持视觉未确认协议。
4. 精确审阅 diff，运行定向测试、Python 3.9 语法检查和 `git diff --check`；失败立即停止。
5. 将完成项、验证结果、未完成项、风险和下一步写回本文件与总计划。真实只读校准和受控点击必须另建任务说明并重新授权。

## 产物

- 套装图标/文字双通道局部区域定义及离线匹配/OCR 接口调整。
- 一次性实时识别和装备名称可缺省的公开回归测试。
- 本文件与 `e7-gear-enhance-plan.md` 的状态记录。

## 停止条件

任一区域、PNG、视口、模板、字段、置信度、矛盾检查或测试失败时立即 fail-closed；不得降低门槛、自动重试或进入真实 ADB/点击链路。

## 当前状态

`offline_realtime_visual_set_dual_channel_verified_pending_real_frame_calibration_20260727`

执行模型：`gpt-5.6-terra + high`。

## 完成项与验证记录（2026-07-27）

- `ocr_regions.py` 将红装详情候选区域升级为 v2，增加互相独立且更窄的 `set_icon` 与 `set_text`；保留 `set`、`set_anchor` 和既有区域，不增加点击坐标。
- `visual_set_template_matcher.py` 优先使用 `set_icon`，仅在该区域不存在时兼容 `set_anchor`/`set`。模板结果保留候选 ID、匹配分数、阈值、局部锚点和来源区域，仅作为视觉模板锚点，不写作套装字段或服务器确认。
- `ocr_paddle.py` 的实际局部 OCR 入口改为 `set_text`，并在套装字段记录 `set_text_local` 或旧入口来源；`set_name`、`set_anchor`、`set` 和未标注的旧注入输入仍兼容。已拒绝将广域 `right_detail_panel` 的套装文本替代缺失局部证据。
- 套装图标候选和套装文字字段保持独立审计：模板锚点进入 `anchors`，OCR 文字进入 `visible_fields.set`；没有候选合并，也没有将图标候选当作字段确认。任一局部区域缺失、越界、损坏、低于 `0.98` 或模板并列时保持 fail-closed。
- `REQUIRED_VISIBLE_FIELDS` 未包含名称；公开完整链路确认名称缺失不触发门禁，同时继续保留 `operation_id`、稳定帧哈希和视觉指纹，未增加跨运行持久化绑定。
- 离线验证均退出码 0：定向视觉/OCR `37/37`、全部 `test_visual_*.py` `65/65`、全部 `test_ocr*.py` `30/30`；Python 3.9 `py_compile` 与 `git diff --check` 通过。
- 本轮未执行真实 ADB、截图、PaddleOCR 推理、页面导航、ADB 输入、点击、强化、选材、资源消耗、底层读取、外部上传或模型下载。

## 未完成项、风险与下一步

- `set_icon` 与 `set_text` 是离线候选裁剪，尚未由新的真实只读帧校准；不能宣称真实套装图标或文字已达到 `0.98`。
- 图标模板候选与 OCR 套装文字没有自动合并或相互推断；任何真实帧缺失任一既有视觉门槛时必须继续 fail-closed，维持 `mode=visual_only`、`verification=unverified`。
- 后续真实只读校准或受控点击必须另建任务说明并获得授权；本任务不进入该阶段。总计划由主 agent 审核本任务产物后另行同步。
