# Epic 非速度 +3 运行时采集接线任务说明

## 目标

把已经完成的 `epic_plus3_prospective` 采集核心接到项目现有的真实装备状态输入路径，使日常查看同一装备的 `+0` 和 `+3` 状态时能够自动形成前瞻配对，不再要求用户手工编写 `runtime-observation.json` 包装文件。

本任务只增加只读观察和样本记录副作用，不改变任何强化建议、自动动作、GUI 布局或游戏交互。

## 预审计证据

- `src/e7_enhance/epic_plus3_prospective.py` 已实现过滤、配对、去重、原子写入、进度和 manifest。
- `tools/collect_epic_plus3_prospective.py` 只能通过 `--record-json` 接收人工准备的 observation。
- 当前 `src/` 中没有正式运行路径调用 `record_observation`；调用者仅有该 CLI 和测试。
- 因此当前状态应表述为 `collector_core_ready_runtime_adapter_missing`，不能表述为“日常被动采集已开始”。

## 第一阶段：确认可信输入路径

先只读审计现有装备导入和建议计算路径，确认哪一条输入同时满足：

- 来自完整真实装备导出，而不是手工填写表单；
- 保留稳定实例 ID；
- 能在 `+0` 和 `+3` 两个节点再次观察同一实例；
- 包含完整副属性、roll history、来源和强化等级；
- 在 `+3` 建议计算前可以记录状态。

优先接入共享的真实装备导入/解析层。不得接入模拟器、条件生成器、人工样本回放、旧 Oracle 数据或批量研究工具。

若当前项目没有满足条件的真实输入路径，停止实现并明确报告阻断；不得用手工表单、随机生成或旧样本伪装运行时采集。

## 第二阶段：运行时适配器

在可信输入路径存在时，增加隔离的适配器，将完整装备状态转换为采集 observation：

- `sample_source=prospective_runtime`；
- 自动生成带时区的 `observed_at`；
- 透传完整装备、实例 ID、`itemSource` 和 `gearSource`；
- 只调用采集模块，不复制过滤、指纹或配对逻辑；
- 采集异常不得改变、阻断或重算原正式建议；
- 采集结果只写数据文件和诊断日志，不显示候选预测或 Oracle 结果。

## +0 实际强化语义

现有采集器要求 `+0` observation 携带 `actual_enhancement=true`，但仅在展示建议时无法证明玩家已经执行强化。

不得在建议展示阶段伪造该字段。应采用以下任一可证实方案：

1. 若运行路径存在“强化已实际执行”的可靠事件，在该事件后记录 `+0`；或
2. 允许把符合正式 enhance 建议的 `+0` 先保存为未计数 pending；只有同一实例后来真实出现 `+3` 时，才以状态迁移证明实际强化并形成有效 pair。

推荐方案 2。pending 不得计入有效样本、64 件门槛或 manifest；长期未出现 `+3` 的 pending 可以保留，但不能解释为已强化。

## 接线边界

- 允许在现有完整真实装备成功导入后增加不可见观察钩子；
- 不改变 GUI 布局、按钮、建议文案或业务动作；
- 不接入 OCR、ADB、Airtest、自动点击或设备控制；
- 不在 `advise_gear` 内写文件，避免模拟器和离线研究产生伪样本；
- 不让生产建议依赖采集数据文件是否存在或写入是否成功；
- 默认数据文件继续使用 `samples/epic_plus3_prospective_runtime_20260714.json`，如需配置路径应保持向后兼容。

## 回归测试

先增加失败测试，再作最小接线。至少覆盖：

- 正式运行路径此前没有调用采集器的证据；
- 完整真实 `+0` 导入只形成 pending，不伪造实际强化；
- 同实例真实 `+3` 导入后形成一个有效 pair；
- 重复导入不会重复计数；
- 手工表单、模拟、回放和旧样本不会进入数据集；
- 采集写入失败时，正式建议内容和返回值完全不变；
- 接线前后同一装备的 `recommendation`、`next_check_at` 和 debug 决策字段一致；
- 不触发任何设备操作或额外点击。

## 验收与报告

完成后必须报告：

- 实际接入的文件、函数和事件时点；
- 为什么该输入能证明是真实装备状态；
- `+0` pending 与实际 pair 的证明语义；
- 用一组隔离测试数据演示 `0 pending / 0 pair -> 1 pending / 0 pair -> 0 pending / 1 pair`；
- 正式建议零变化的回归结果；
- 当前真实有效配对进度。

本任务仍不运行 Oracle、五 seed 联合资源池、候选研究或每十万总体力指标，不创建新 48 件批次，不修改正式策略。
