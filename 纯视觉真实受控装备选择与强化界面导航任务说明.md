# 纯视觉真实受控装备选择与强化界面导航任务说明

## 目标

在不连接设备、不截图、不发送输入的前提下，完成阶段初始化后的离线前置核对：审阅现有列表定位、唯一目标证据、坐标解析，以及两次单次点击后的新鲜后验契约；明确真实导航尚缺的条件。

## 范围

- 仅只读审阅现有视觉运行时、列表/目标证据、坐标解析、点击器与测试。
- 仅更新本任务说明和 `e7-gear-enhance-plan.md` 的状态、核对结果与下一步。
- 不实施代码功能变更，不运行测试，不连接 ADB 或其他设备。

## 禁止修改项

- 禁止 ADB、截图、OCR 推理、窗口输入、页面导航、点击、强化、选材、确认或任何资源消耗。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout、新鲜快照契约或自动化规则。
- 禁止把历史截图、离线 fixture、旧识别结果或用户描述写成当前列表页或唯一目标已验证。

## 输入与权威口径

- 用户委托内容指定本文件为本轮唯一权威来源；原委托要求的同名文件在开始时未落盘，现补建本文件以满足项目文档门槛。
- 同时遵守根目录 `AGENTS.md` 和 `e7-gear-enhance-plan.md` 的最新记录；后者中被标记为历史对照的内容不构成当前真实页面证据。
- 视觉结论始终保持 `mode=visual_only`、`verification=unverified` 与 fail-closed；字段门槛为唯一且 `>=0.98`。

## 执行步骤

1. 只读审阅视觉运行时、点击执行器、列表定位/目标证据和测试中的现有契约。
2. 核对单次点击许可、坐标换算、调用次数限制与点击后新鲜后验的实现/测试覆盖。
3. 记录当前真实导航缺失的具体条件；任何缺项均停止于离线结论。
4. 同步本文件和总计划，不申请或执行真实导航。

## 产物

- 本任务说明中的离线核对记录。
- 总计划中的阶段状态与明确阻断条件。

## 验证要求

- 所有结论必须可定位到现有代码、测试或计划记录。
- 必须明确记录未运行 ADB、截图、输入、导航、强化、选材、确认和资源操作。

## 停止条件

- 未提供当前列表页的视觉证据，或无法形成唯一目标字段、页面锚点、坐标解析、点击许可及每次点击后新鲜后验帧契约时，停止并等待用户明确确认。
- 未取得新的明确真实操作授权时，不得进入受控真实导航或点击。

## 当前状态

`offline_prerequisite_audit_completed_waiting_current_list_visual_fields_20260728`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口未提供 Luna，本轮实际执行模型为 `gpt-5.6-terra + high`。

## 阶段初始化记录（2026-07-28）

- 原委托要求的同名任务说明在根目录未找到；在未使用聊天指令替代文档的前提下，已按项目规则补建本文件并将范围锁定为离线审阅。
- 本阶段尚未审阅代码或测试，未连接 ADB、截图、发送输入、导航、强化、选材、确认或消耗资源。

## 离线前置核对结果（2026-07-28）

### 已有可复用契约

- `src/e7_enhance/visual_runtime.py` 的 `VisualEvidenceGate` 只接受唯一目标（`candidate_count == 1`）、字段集合与置信度一一对应且均 `>=0.98`、唯一强化页、可见目标、两个局部锚点、可见资源预览和三帧稳定记录。它只适用于已经到达 `enhance_equipment` 页的证据。
- `VisualOnlyEnhanceExecutor.run_node()` 对一个节点执行“点击前稳定采样 -> 明确单次授权 -> 一次点击 -> 点击后稳定采样”；点击后要求采集时间严格递增、样本 ID 与帧哈希均未重用、目标字段/视觉指纹仍一致、节点和资源账本符合预期。异常、旧帧或后验不通过均返回 `unknown_result`/fail-closed，且同一 `(operation_id, from_node, to_node)` 不可重试。
- `src/e7_enhance/visual_adapter.py` 的 `StableFrameCollector` 固定采集至少三帧，并要求同一源、同一视口、逐字节同帧哈希和不倒退的时间戳；`VisualAdapterSampler` 会核对解析结果的帧记录与稳定集合一致。
- `src/e7_enhance/visual_click.py` 的 `NormalizedClickExecutor` 只接收显式授权的归一化点，并在调用后端前锁定一次性尝试；视口或坐标无效时拒绝，后端异常后结果未知且不可重试。

### 真实导航的明确缺口

1. 没有列表页的页面类型、局部锚点、OCR 区域、候选排序/去重、滚动边界或唯一目标定位器。现有 `ocr_regions.equipment_regions()` 明确只定义单件详情页，并声明 `click_coordinates=None`。
2. 没有“列表 -> 选中目标 -> 装备详情 -> 强化页”的状态机、每一跳前后的页面签名/稳定帧契约，或与唯一目标视觉指纹的持续绑定。现有运行时仅接受已经确认的 `enhance_equipment` 页面。
3. 没有从已验证按钮锚点推导的选择装备、进入强化页或强化按钮坐标；归一化点击器只转换调用方提供的任意点，未把点与当前页面签名、目标、按钮识别或列表行绑定。
4. 没有真实输入后端接线：`NormalizedClickExecutor` 只定义注入式 `WindowClickBackend` 协议，`airtest_adapter.py` 仍明确禁用；本轮不允许补接 ADB/窗口输入。
5. 当前没有用户确认的列表页视觉字段或唯一目标指纹。历史详情帧的目标/套装/等级/强化识别失败只作历史对照，不能证明当前列表可定位。
6. 两个节点可在同一 `VisualOnlyEnhanceExecutor` 内依次调用 `run_node()`，第一节点后验帧会被记录并拒绝复用；但第二节点的 `resource_before`/计划成本由调用者重新提供，未自动与第一节点的 `expected_after` 账本绑定。真实两次点击前必须补充跨节点账本连续性、操作级授权和每次后验成功后才允许下一节点的契约。
7. `VisualEvidenceGate` 单独接收证据时只校验哈希格式和数量；“三帧字节相同”由 `VisualAdapterSampler`/`StableFrameCollector` 保证。因此真实接线必须强制经该采样器，不得直接构造证据绕过稳定帧验证。

## 本轮验证

- 只读代码与公开测试审阅完成：`src/e7_enhance/visual_runtime.py`、`visual_adapter.py`、`visual_click.py`、`ocr_regions.py`、`airtest_adapter.py` 及其视觉单元测试。
- 未运行测试，以遵守本任务限定的“只读前置核对”；未连接 ADB、截图、发送输入、导航、强化、选材、确认或消耗资源。

## 下一步

等待用户明确提供或确认当前列表页的视觉字段与唯一目标信息。之后必须另建受控真实导航任务说明，先离线定义并验证列表页定位、页面跃迁、按钮锚点与两节点连续后验契约，再单独取得真实操作授权。

## 用户待确认信息（2026-07-29）

本轮只需要确认或提供状态信息，不构成 ADB、截图、输入、导航、点击、强化、选材、确认或资源消耗授权：

1. MuMu 当前是否停在装备列表页；若不是，请说明当前页面名称。
2. 要选中的唯一目标在列表中可见的字段：至少部位、品质、等级、套装、强化等级、主属性和可见副属性/装备分数；装备名称可选。
3. 该目标在当前列表中是否唯一；若有相似项，提供可见的区分字段。
4. 当前列表页是否已经显示可点击的目标行，还是需要先通过筛选、滚动或其它页面导航才能看到它。

本阶段仍只接收描述或用户主动提供的截图；任何真实读取、导航或点击均需在后续独立任务说明中取得单独明确授权。

## 用户状态确认记录（2026-07-29）

- 用户确认 MuMu 当前为装备列表页；目标不存在相似装备，用户认为无需额外区分；且不需要筛选、滚动或切换界面。
- 用户进一步澄清：当前实际停在装备列表；目标装备存在可进入的详情页，但当前未进入该详情页。此前“目标装备已经打开”的歧义已被本记录覆盖。
- “目标行”仅指装备列表中代表目标装备的单个条目/卡片，不假定其已被定位或可安全点击。
- 当前状态更新为 `offline_prerequisite_audit_completed_current_list_confirmed_waiting_target_visual_fields_20260729`；仍禁止 ADB、截图、输入、导航、点击、强化、选材、确认和资源消耗。

## 用户提供的目标指纹（2026-07-29）

- 部位：武器；品质：红装/传说；等级：85；当前强化：`+3`；套装：生命值套装。
- 主属性：攻击力；副属性：攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`；装备分数：`37`。
- 该指纹是后续列表定位和详情/强化页连续性校验的期望值；它尚未来自当前页面的稳定视觉采样，不能单独构成唯一定位、目标已选中、页面已进入或点击授权的证明。
- 当前状态更新为 `offline_prerequisite_audit_completed_target_fingerprint_received_waiting_controlled_navigation_task_20260729`。后续必须先建立并离线验证列表定位、页面跃迁和点击后验接线任务，再取得该任务要求的明确真实操作授权。
