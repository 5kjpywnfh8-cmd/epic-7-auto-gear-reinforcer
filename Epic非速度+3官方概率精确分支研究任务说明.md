# Epic 非速度 +3 官方概率精确分支研究任务说明

## 目标

使用已有独立真实 `normal_85 Epic +0` 快照作为自然胚子分布，并按已确认的官方强化概率精确枚举每件装备的全部 `+3` 分支，替代无法完成的“已出售旧装备真实 `+0/+3` 配对”要求。

本任务恢复 Epic 非速度 `+0/+3` 高置信度止损研究，但不修改正式策略。未来运行时真实配对继续保留为长期模型复核，不再作为当前研究启动的硬阻断。

## 方法学依据

- 旧装备虽已出售，但保存的真实 `+0` 快照仍能代表实际胚子分布。
- `+0 -> +3` 只发生一次副属性强化事件；在目标词条选择概率和该词条离散跳值概率已知时，可以无 Monte Carlo 误差地枚举完整条件分布。
- 这种分支不是任意合成装备，而是每件真实 `+0` 装备在已确认游戏规则下的全部合法下一状态。
- 报告必须称为“真实 +0 条件下的模型精确 +3 分支”，不得称为实际观察到的真实 `+3` 样本。

## 数据隔离

沿用预检已排除旧 Oracle/盲测45个实例后的固定分割。旧预检将规范化的 `boot` 与字符串 `Boots` 比较，误把24件鞋子计入；按本任务明确的非鞋范围修正后为：

- 开发组：157件独立真实 `+0`；
- 冻结验证组：92件独立真实 `+0`。

要求：

- 开发组用于构造、筛选和冻结候选；
- 候选规则、特征、epsilon和预测哈希冻结前，不得对99件冻结组运行 Oracle 或候选预测；
- 分支枚举器规则在读取冻结组前冻结；
- 条件生成装备、旧45件、旧盲测、旧0/48批次和未来运行时pair均不混入本轮174/99分割。

## 官方 +3 跳值分布

权威来源为 [STOVE装备概率资料核对记录.md](STOVE装备概率资料核对记录.md)，normal_85 Epic 强化事件使用：

- 固定攻击：33=`6.103%`；34--45各=`7.353%`；46=`5.662%`；
- 攻击%、防御%、生命%、效果命中%、效果抗性%：4--8各=`20%`；
- 固定防御：28--34各=`14.265%`；35=`0.143%`；
- 固定生命：157和202各=`1.133%`；158--201各=`2.221%`；
- 速度：2/3/4各=`33.223%`；5=`0.332%`；
- 暴击率：3/4/5各=`33.333%`；
- 暴击伤害：4/5/6/7各=`25%`。

公告小数展示存在舍入误差，每种词条内部单独归一化。不得直接复用现有 `roll_range()` 的均匀采样，也不得直接复用当前 `OFFICIAL_ROLL_DISTRIBUTIONS` 常量：该常量把固定攻击33和46错误写成相同概率，并且不是正式共享权威表。

## 精确分支生成

对每件真实 Epic `+0`：

1. 保留原套装、部位、主属性、四条副属性、roll count、来源和实例指纹。
2. 逐一枚举四条副属性作为 `+3` 命中目标。
3. 目标选择概率沿用项目现行 Epic 强化模型的等概率 `1/4`；在报告中标记其代码依据。若审计发现项目没有已确认依据，则必须单列为 `model_assumption` 并做合理敏感性，不得伪称 STOVE 直接证明。
4. 对命中词条枚举其全部官方跳值，分支概率为 `P(target) * P(delta | stat)`。
5. 每个分支正确更新副属性值、`rolls + 1`、`enhance=3` 和 checkpoint=3 的 roll history。
6. 每件装备全部分支概率之和必须在数值容差内等于1。

禁止随机抽取单个 `+3` 结果，也禁止把所有分支当作等权独立装备。所有指标必须使用分支概率加权。

## +0 与 +3 分节点研究

- `+0`：在真实装备层比较停止和继续的期望效用。
- `+3`：仅对 `+0` 候选实际继续的装备进入加权分支，分别评估每个分支的停止/继续动作。
- 两个节点分别报告样本装备数、加权状态质量、继续率、停止率、正效用召回、明确正效用误停和 regret。
- 下游 `+6/+9/+12/+15` 继续复用已冻结正式 Epic DP 和资源模型。
- Epic速度 `>=2` 硬路线、Heroic M1、rift_85、评分、转换和资源规则保持冻结。

## Oracle 与候选流程

1. 先对174件开发组生成精确分支并运行 Oracle。
2. 只研究“当前谨慎继续 + 明确高置信度负效用才停止”的浅规则；`+0`和`+3`必须分别建模。
3. 冻结 epsilon、特征、规则、阈值和预测哈希。
4. 冻结后一次性对99件冻结组生成同一权威分支并评估。
5. 冻结验证中明确正效用误停必须为0，总 regret不得高于当前正式策略。
6. 通过节点闸门后，才运行五 seed 联合维度裂缝/圣女资源池和每十万总体力指标。

## 报告口径

人类可读报告必须包含：

- 为什么装备已出售不影响真实 `+0` 条件期望；
- 为什么精确分支不同于伪造自然掉落样本；
- 官方跳值表、归一化误差和目标选择假设；
- `+0`和`+3`分节点 Oracle 与候选指标；
- 开发157件、冻结92件的数据隔离证据，以及旧预检鞋子过滤偏差；
- 当前正式策略、旧B候选和新候选对照；
- 每10万总体力的正式体系百里分、75+、22+、维度裂缝体力、圣女3-7体力和循环数；
- 未来真实pair仅作为实测一致性复核，不混入本轮验证。

## 测试与范围

先增加失败测试，至少覆盖：

- 每种词条的官方离散概率和归一化；
- 固定攻击33/46概率不同；
- 固定生命端点和范围正确；
- 四目标乘离散跳值的总概率为1；
- 分支正确更新roll history和roll count；
- 加权结果与穷举手算小例一致；
- 开发/冻结隔离；
- 正式策略输出零变化。

本任务不得修改正式跳值表、正式策略、DP lambda、资源默认值、GUI、OCR、ADB、Airtest或自动点击。
