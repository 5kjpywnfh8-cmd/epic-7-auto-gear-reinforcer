# Epic 非速度早期策略精确 DP 审计任务说明

## 目标

审计 normal_85 Epic 非速度装备在 `+0/+3` 的继续、谨慎继续和停止策略。人工标签只作为玩家偏好与风险验收，不作为数学最优真值；主判据为当前状态继续到下一强化节点相对立即停止的 DP 效用增量。

本轮只做离线审计和候选比较，不修改正式强化策略、DP 常量、资源模型、跳值表或 GUI。

## 已确认事实

- 旧 22 件 holdout 和新 24 件 blind 均已完成人工审核，共 46 件真实样本。
- 新 24 件人工结果为：继续 2、谨慎继续 8、停止 14；当前正式策略全部为谨慎继续。
- 人工判断可能不是单位资源效率最优，不能按人工准确率直接选择或训练门槛。
- Epic 非鞋初始速度 `>=2` 属于已发布速度硬路线，必须从本次非速度研究中剥离。
- `continue` 与 `cautious_continue` 的实际资源动作都是强化到下一节点；差别是前者可自动执行，后者要求下一节点人工复核。
- 已获得装备的获取成本属于当前决策的沉没成本，在 `continue_utility - stop_utility` 中不得重复扣除；已发布 normal_85 Epic lambda 已包含全链路资源价值校准。

## 审计要求

1. 先只读检查现有报告、原始指标、46 件样本、候选预测、精确 DP/route solver 与相关测试，不先修改代码。
2. 不得按数组顺序关联记录。为每件装备建立稳定实例 ID；没有稳定 ID 时，使用规范化装备指纹，并在报告中列出指纹字段和重复处理规则。
3. 对旧 22 件、新 24 件及合并 46 件分别审计；明确排除速度硬路线、非法输入和无法唯一关联的记录，不能静默丢弃。
4. 对每件装备从真实当前状态计算：
   - 立即停止 utility；
   - 强化到下一节点并执行现行后续复核的 continue utility；
   - `utility_margin = continue_utility - stop_utility`；
   - 下一节点和端到端增量金币、材料经验及体力等价；
   - 终局正式分类价值、速度潜力价值及两者取高结果；
   - 合法满值转换路径及 100,000 金币转换成本；
   - 最优路径、到达各节点概率和停止节点。
5. 使用 normal_85 Epic 独立跳值表和当前已发布资源参数，不得混用 rift_85；装备来源成本不作为已获得单件装备的新增边成本。
6. 优先调用项目现有精确 DP。若状态空间只能用蒙特卡洛近似，必须明确标注“近似 oracle”，使用公共随机轨迹、固定 seed、置信区间和可复跑参数，不得把近似结果写成精确值。
7. Oracle 的资源动作判定：margin 明确为正则强化，明确为负则停止；只有数值误差、置信区间跨零或已确认参数敏感性跨零时才标记人工复核。不得自行编造一个固定 margin 阈值来扩大谨慎继续范围。
8. 三分类展示只用于操作层：明确正效用且远离边界可标记继续；边界不确定标记谨慎继续；明确负效用标记停止。主统计仍以“强化/停止”二分类为准。

## 候选比较

至少比较：

- 当前正式全谨慎基线；
- `B_global_current_gs`；
- `C_category_probability`；
- 报告中其余全部冻结候选；
- DP oracle。

每个候选必须同时输出：

- 相对 DP oracle 的动作一致率；
- 总 utility regret、平均 regret、P90 regret 和最大 regret；
- 误强化件数及其浪费的增量体力；
- 误停件数及其损失的期望终局价值；
- 高价值终局召回率；
- GS/100 体力；
- 相对人工标签的动作一致率和三分类一致率；
- 需要人工复核的覆盖率。

人工与 DP 分歧必须分类为：人工偏保守但 DP 正效用、人工偏激进但 DP 负效用、DP 边界不确定、输入或关联异常。逐件列出证据，不得直接用人工标签覆盖 DP。

## 数据隔离

- 46 件已查看人工标签的样本只能用于审计，不能再作为独立发布验证集。
- 若根据这 46 件调整任何候选或门槛，必须先从剩余真实 normal_85 Epic 中冻结至少 48 件新的独立验证批次，再读取其人工标签。
- 合成条件样本只用于合法性、单调性和稀疏组合覆盖，不得冒充真实掉落、人工标签或独立验证集。
- 本轮证据不足时保持正式策略不变，不得为了提高人工一致率直接发布 B。

## 实施与验证

1. 若发现工具缺少上述只读审计能力，先补最小失败测试，再最小修改离线工具；不得修改正式策略文件。
2. 输出机器可读 JSON 和中文 Markdown 报告，逐件结果必须能通过稳定 ID/指纹回查。
3. 更新 `e7-gear-enhance-plan.md`，记录数据版本、排除项、oracle 口径、候选排序和是否具备下一阶段资格。
4. 运行离线工具定向测试、相关 DP/资源/策略测试和全量测试；报告命令、数量和结果。

## 最终报告

必须包含：

- 为什么人工标签不是唯一真值；
- 46 件稳定关联与排除结果；
- DP oracle 的计算口径和是否真正精确；
- 当前策略及全部冻结候选的效用、遗憾、资源和人工对照；
- 人工与 DP 分歧样本明细；
- 是否值得建立正式策略修改任务；
- 若值得，明确需要冻结的新 48 件验证集，不能在本任务直接改正式策略。
