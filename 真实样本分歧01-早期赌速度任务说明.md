# 真实样本分歧 01：早期赌速度任务说明

## 目标

根据第一轮真实样本修复 `+0/+3` 的早期赌速度路线。只处理“初始速度是否值得强化一次”和“+3 未跳速度后是否退出速度路线”；不得顺带修改正式体系 GS 门槛、75+ 未来可期概率、白字主属性鞋通用规则或部位受限体系门槛。

## 权威样本

唯一复现数据源为 `manual_acceptance/real_sample_records.json`，按 `snapshot_id` 读取：

- `snapshot-09e26a5b532f5cb2`：`+0` 红装毁灭套衣服，初始速度 5，人工 `continue`。
- `snapshot-d6784ef1d24815df`：`+0` 紫装追击套项链，初始速度 4，人工 `continue`。
- `snapshot-c573444cd19cd9c3`：`+0` 紫装追击套戒指，初始速度 4，人工 `continue`。
- `snapshot-3eee02a609e1e806`：`+3` 红装先手套戒指，速度仍为 4、`rolls=1`，即 +3 未跳速度；未命中体系且分低，人工 `stop`。

边界反例：

- `snapshot-b6abed008bef3ff6`：`+0` 紫装先手套鞋，防御白字主属性、速度 4，人工 `stop`。
- `snapshot-639d421f7a018374`：`+0` 紫装追击套鞋，生命%主属性、速度 4，但未命中体系且分低，人工 `stop`。
- `建议结果/07.json`：现有多跳速度高价值路线，必须保持 `continue`。

## 已确认规则

1. `+0` 非鞋装备中，至少以下初始速度应允许赌到 `+3`：红装速度 5、紫装速度 4。
2. 上述样本应输出明确的“早期赌速度”路线和 `continue`，不能仅因尚未形成正式体系而统一谨慎继续。
3. 不得从这 3 件样本推断“所有红装只要有速度都继续”或“所有紫装速度 4 都继续”。低于样本阈值的速度必须保持现状，等待更多人工数据。
4. 非速度主属性鞋不套用这条非鞋赌速度硬路线。鞋子必须继续按主属性、体系和分数独立判断；本任务不得把两个停止反例改成继续。
5. 到 `+3` 后，若速度 `rolls` 没有增加，早期速度赌博路线结束。之后只能依据正式体系、终局 75+ 后备和其他合法策略决定，不能因为仍有初始速度而继续受速度保护。
6. 到 `+3` 后若速度确实被强化，才允许继续走赌速度路线；debug 必须显示当前速度、速度 rolls、本次是否命中速度及下一检查点。
7. 速度主属性鞋不能出现速度副属性，不进入本任务路线。

## 执行要求

1. 先复现上述 6 个真实样本和 07，记录当前建议、目标体系、速度值、速度 rolls、强化命中分析和选择路线。
2. 先新增失败回归测试，再修改代码。
3. 建立显式的早期速度路线判断，不得通过抬高全局 GS 或篡改正式体系分类间接得到结果。
4. `+3` 是否跳速度必须依据真实 `rolls`/强化命中分析，不能只看速度最终数值是否大于初始阈值。
5. debug 至少显示：速度路线资格、部位资格、品质阈值、当前速度、速度 rolls、+3 是否命中速度、路线结束原因、下一检查点。
6. 不改变当前重铸前/后 GS、满值转换 GS、正式低档概率和终局 75+ 概率的计算。
7. 不修改 OCR、ADB、Airtest、自动点击或 GUI 样本存储架构。
8. 更新人工验收报告、规则审阅和计划文档。
9. 运行相关测试及全量测试。

## 最低验收

- 三个 `+0` 正向样本均为 `continue / 早期赌速度`。
- 两个鞋子反例保持人工预期 `stop`，不会被速度 4 错误保护。
- `+3` 戒指未跳速度样本退出速度路线并为 `stop`；debug 明确说明 +3 未命中速度。
- 新增一个 +3 确实命中速度的最小测试，确认仍可继续赌速度。
- 07 保持 `continue / 多跳速度高价值路线`。
- 14 的谨慎继续/输出候选不回归。
- 相关测试和全量测试通过。

## 最终报告

- 根因与代码证据；
- 正向样本和鞋子反例的修复前后建议；
- +3 命中/未命中速度的路线差异；
- 07、14 回归结果；
- 修改文件；
- 测试结果；
- 尚未覆盖的红装低初始速度、其他紫装速度阈值。
