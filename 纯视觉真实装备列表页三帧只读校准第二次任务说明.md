# 纯视觉真实装备列表页三帧只读校准第二次任务说明

## 目标

在用户于 2026-07-30 明确批准后，对当前 MuMu 装备列表页执行一次新的受控真实只读校准前置审阅；仅在完整、已注册的真实列表页局部识别器存在时，才自动发现唯一设备并读取固定三帧内存 PNG，验证列表页和唯一目标视觉字段。

结果固定为 `mode=visual_only` 与 `verification=unverified`，不表示服务器状态或任何操作成功。

## 范围

- 先只读审阅列表页内存 PNG 观测器、真实局部识别器实现和已注册区域配置，确认它们能从三帧真实内存 PNG 产生完整 `EquipmentListVisualParser` 观测。
- 仅当上述接线完整，运行一次官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`；仅在唯一状态为 `device` 的 MuMu 上，采集固定三次 `exec-out screencap -p`。
- PNG 仅在内存中用于视口、哈希、稳定性和已注册的局部识别；不落盘、不上传、不全屏盲 OCR、不重试。
- 目标期望指纹：武器、红装/传说、85 级、`+3`、生命值套装、攻击主属性、攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`、装备分数 `37`。

## 禁止修改项

- 禁止 ADB `input`、`tap`、`swipe`、`keyevent`，以及窗口输入、导航、点击、强化、选材、确认、重启游戏或资源消耗。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层读取器、外部上传、模型下载、截图落盘、全屏盲 OCR、第二次采样批次和自动重试。
- 禁止修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、真实帧源或自动化后端；禁止将用户描述、历史截图或离线 fixture 写成当前真实页面证据。

## 输入与权威口径

- 本文件是本轮唯一权威任务来源；同时遵守根目录 `AGENTS.md` 与 `e7-gear-enhance-plan.md`。
- 用户的“批准，下一步”仅授权本文件中的一次真实只读校准前置审阅及在接线完整时的设备发现/三帧读取；不覆盖任何输入、导航、点击、强化、确认或资源消耗。
- 必须保持锚点和每个期望字段唯一且 `>=0.98`、三帧稳定及 fail-closed。任一前置不足都停止，不得以坐标猜测、全屏 OCR 或模拟识别结果绕过。

## 执行步骤

1. 只读审阅 `InMemoryPngListPageObserver`、`StableFrames`、ADB 截图器、真实 `ListPageLocalRecognizer` 实现和已注册的列表页区域配置。若识别器或配置仅存在于合成测试，立刻在 ADB 前 fail-closed。
2. 仅当步骤 1 完整通过，运行一次官方 ADB 设备发现；非唯一、非 `device` 或发现失败立即停止。
3. 由唯一设备采集固定三帧内存 PNG，校验 PNG、视口、时间顺序、稳定哈希和非黑屏；不保存、不重试。
4. 在同一三帧上执行已注册的局部页面/锚点/候选卡片/字段识别，传给 `EquipmentListVisualParser`；任一字段、唯一性、边界或置信度不通过即停止。
5. 同步本任务说明、上一版三帧只读校准任务说明和总计划，记录是否执行 ADB、真实观察结果或明确 fail-closed 原因；不执行导航或输入。

## 产物

- 一次真实只读校准记录，或在设备接触前/后明确可审计的 fail-closed 原因。
- 本任务说明、[纯视觉真实装备列表页三帧只读校准任务说明.md](纯视觉真实装备列表页三帧只读校准任务说明.md) 与 `e7-gear-enhance-plan.md` 的状态同步。

## 验证要求

- 任何真实观察结论必须来自同一唯一设备、同一视口、固定三帧稳定记录和已注册的真实局部识别；用户文字、fixture 或合成识别器不得替代。
- 只有锚点、局部区域、候选可见边界、目标唯一性和全部字段逐项 `>=0.98` 时，才可报告真实列表页视觉证据；仍不得产生点击点或发送输入。
- 明确记录未执行的输入、导航、点击、强化、选材、确认和资源操作。

## 停止条件

- 缺少已落盘的真实 `ListPageLocalRecognizer`、已注册真实列表区域、设备不唯一/不可用、截图或稳定性失败、局部识别不足、字段冲突/低置信度/目标不唯一时立即停止。
- 需要任何输入、导航、点击、文件落盘、全屏 OCR、上传、下载、第二批采样或资源操作时立即停止并另建任务。

## 当前状态

`second_real_list_readonly_calibration_not_started_fail_closed_missing_real_list_recognizer_20260730`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口未提供 Luna。本任务实际执行模型按项目规定记录为 `gpt-5.6-terra + high`。

## 授权记录（2026-07-30）

- 用户明确回复“批准，下一步”。本授权严格限于本任务的只读前置审阅，以及仅在前置完整时的一次设备发现和固定三帧内存截图。
- 该授权不允许控制模拟器界面：禁止输入、点击、滑动、按键、导航、强化、选材、确认和任何资源消耗。

## 接口审阅与停止记录（2026-07-30）

- 只读检索确认 `src/e7_enhance/visual_list_observer.py` 只声明 `ListPageLocalRecognizer` 协议和注册区域数据类型；生产源码中没有它的实现、没有已注册的真实列表页区域配置，也没有 `InMemoryPngListPageObserver` 的生产调用者。
- 唯一构造注册区域并调用观测器的代码在 `tests/test_visual_list_observer.py`，且识别器为明确命名的 `FakeRecognizer`。合成测试不能作为真实页面识别器或区域配置的替代。
- 因步骤 1 与停止条件不通过，已在 ADB 前 fail-closed：未运行 `adb.exe devices -l`，未读取内存截图，未执行 OCR 推理、输入、导航、点击、强化、选材、确认、资源消耗、底层读取、上传或下载。
- 本次一次性只读授权未用于任何设备操作，已终止且不得复用。后续必须先另建纯离线任务，实现并回归验证真实列表页局部识别器和受审定的区域注册；在完成后重新申请新的只读校准授权。

## 后续离线前置完成记录（2026-07-30）

- 上段“缺少真实列表页局部识别器和区域注册”的结论已被后续[纯视觉装备列表页真实局部识别器与区域注册离线开发任务说明](纯视觉装备列表页真实局部识别器与区域注册离线开发任务说明.md)覆盖，仅作本次 ADB 前停止的历史对照。
- 后续离线任务新增生产源码内可实例化的 `RegisteredListPageLocalRecognizer`、版本化固定读取区域和视口闸门，并在公开/合成 fixture 上通过回归；它不构成当前 MuMu 页面证据，也不恢复本任务已经终止的一次性只读授权。
