# 纯视觉装备列表页真实局部识别器与区域注册离线开发任务说明

## 目标

补齐装备列表页的生产级、局部范围受控的 `ListPageLocalRecognizer` 实现和固定区域注册配置，使 `InMemoryPngListPageObserver` 不再只能由测试中的 `FakeRecognizer` 驱动。

本任务仅交付离线识别契约与公开/合成 fixture 回归；不宣称当前 MuMu 列表页、目标唯一性或字段识别已经通过。它是重新申请一次真实三帧只读校准的唯一前置，不包含点击或导航。

## 范围

- 仅修改 `src/e7_enhance` 中直接服务于列表页局部识别和区域注册的最小模块、对应公开/合成 fixture 测试、本任务说明与 `e7-gear-enhance-plan.md`。
- 定义一个生产源码内可实例化的列表页局部识别器。它只能在调用方给定的已注册局部区域中读取已注入的局部文字/模板观察，构造列表锚点、区域分数、滚动边界、候选卡片与字段观察；不得生成点击点或屏幕坐标。
- 定义受审计的固定区域注册清单和视口适配闸门。未知视口、未注册卡片、未知页面边界、字段冲突、低置信度或任何局部读取异常都必须 fail-closed。
- 目标期望指纹：武器、红装/传说、85 级、`+3`、生命值套装、攻击主属性、攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`、装备分数 `37`。

## 禁止修改项

- 禁止 ADB、真实截图、真实 OCR 推理、窗口输入、导航、点击、强化、选材、确认、资源消耗、底层读取、上传和模型下载。
- 禁止全屏 OCR、动态扩大区域、坐标猜测、硬编码点击坐标、自动重试，或把用户文字、历史截图、测试 fixture 写成真实页面证据。
- 禁止修改既有详情页 OCR 语义、正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout、真实帧源或自动化后端。

## 输入与权威口径

- 本文件是本轮唯一权威任务来源；同时遵守根目录 `AGENTS.md` 与 `e7-gear-enhance-plan.md`。
- 用户本轮“批准，下一步”只授权本离线开发任务，不构成设备发现、截图、OCR 推理、输入、导航、点击、强化或资源操作授权。
- 所有结果保持 `mode=visual_only`、`verification=unverified`；锚点和目标字段逐项必须唯一且 `>=0.98`，任一不确定性均 fail-closed。
- 公开/合成 fixture 只验证模块契约，不能替代后续真实三帧的视觉证据。

## 执行步骤

1. 只读审阅 `visual_list_observer`、`visual_list_parser`、详情页局部识别接口和现有测试，确认可复用边界；不得触达设备。
2. 新增固定、版本化的列表页区域注册与视口兼容性校验，区域只能表达归一化的读取范围，不得成为点击地图。
3. 实现可实例化的局部识别器：从已注入的局部观察中严格解析锚点、区域得分、页面边界与候选卡字段；输入来源缺失、区域名不匹配、重复/冲突字段、未知 token 或置信度不足时拒绝。
4. 仅用公开/合成 fixture 覆盖：正常唯一目标、未注册区域、未知视口、低置信度、候选/字段冲突、未知滚动边界、识别器异常，以及与 `InMemoryPngListPageObserver` 的完整连接。不得调用真实 OCR 引擎。
5. 运行定向测试、相关视觉测试、Python 3.9 语法检查和 `git diff --check`；先完成主 agent 审阅，再同步本任务说明、被本任务承接的第二次真实只读校准任务说明和总计划。

## 产物

- 可实例化的生产级列表页局部识别器与固定区域注册配置。
- 对应公开/合成测试，证明识别器能够受控驱动 `InMemoryPngListPageObserver`，并在不确定时 fail-closed。
- 本任务说明和总计划中的完成记录、验证结果、剩余风险与下一步。

## 验证要求

- 生产模块不得把 `FakeRecognizer`、测试 fixture 或用户描述作为识别结果来源。
- 识别器只接受已注册区域内的局部观察；不得接受裸边界、点击点或动态候选范围。
- 未知视口、局部区域、页面边界、候选卡、字段或置信度时必须在 `NavigationPageEvidence` 前 fail-closed。
- 测试不得依赖当前 MuMu 页面、真实截图、真实 OCR 结果、设备、网络或文件落盘。

## 停止条件

- 需要 ADB、真实截图、真实 OCR 推理、输入、导航、点击、强化、选材、确认、资源操作、全屏 OCR、模型下载、上传或动态坐标搜索时立即停止，并另建任务。
- 无法在不改变详情页 OCR 语义、真实帧采集或自动化后端的前提下实现时立即停止。

## 当前状态

`offline_real_list_local_recognizer_and_region_registry_development_completed_main_reviewed_20260730`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口未提供 Luna。本任务实际执行模型按项目规定记录为 `gpt-5.6-terra + high`。

## 阶段初始化记录（2026-07-30）

- 第二次真实列表页三帧只读校准在 ADB 前 fail-closed，原因是生产源码仅有 `ListPageLocalRecognizer` 协议、无真实识别器和区域注册配置；其一次性只读授权已终止，不能复用。
- 本任务承接该缺口，只允许离线实现和公开/合成回归。完成后仍必须新建任务并重新取得一次真实三帧只读校准授权；即使只读校准通过，列表选中与进入强化界面的每次真实点击仍须后续独立任务与逐次授权。

## 完成记录（2026-07-30）

- 新增 `src/e7_enhance/visual_list_recognizer.py`。`RegisteredListPageLocalRecognizer` 是生产源码内可实例化的 `ListPageLocalRecognizer`：它要求版本化固定注册表、严格的 `1280x720` 视口和精确匹配的局部读取范围，再将注入式局部观察转换为锚点、区域得分、滚动边界和候选字段。未知区域/视口/令牌、字段冲突、低置信度、边界不明或读取异常均以明确原因 fail-closed。
- `equipment_list_region_registry()` 提供唯一的版本化固定读取清单：列表标题、候选列表和六个候选卡区域。它只表达归一化读取范围，不含点击点、屏幕坐标转换、输入或导航。
- `visual_list_observer.py` 增加注册表视口闸门和 `ListPageLocalRecognitionError` 的原因透传；未知的生产识别拒绝会在 `NavigationPageEvidence` 构造前终止。
- 新增 `tests/test_visual_list_recognizer.py`，以合成内存 PNG 端到端覆盖正常唯一目标、未知视口、未注册区域、低置信度、未知页面边界、冲突/未知字段和观察源异常。
- 实际执行模型为 `gpt-5.6-terra + high`，原因是当前协作接口未提供用户偏好的 `gpt-5.6-luna + max`。本轮未执行 ADB、真实截图、真实 OCR、输入、导航、点击、强化、选材、确认、资源操作、底层读取、上传或下载。

## 主 agent 审阅与验证（2026-07-30）

- 主 agent 已只读审阅 `visual_list_recognizer.py`、`visual_list_observer.py` 和新增测试。生产识别器无法扩张调用方区域、接受裸点击点或绕过字段白名单；观察源输出不完整或非注册时在观察器构造证据前停止。
- `C:\\Users\\orangine\\AppData\\Local\\Programs\\Python\\Python39\\python.exe -m unittest discover -s tests -p 'test_visual_list_recognizer.py' -v`：`5/5` 通过，退出码 `0`；列表相关三组测试共 `18/18` 通过。
- `C:\\Users\\orangine\\AppData\\Local\\Programs\\Python\\Python39\\python.exe -m unittest discover -s tests -p 'test_visual*.py' -v`：`98/98` 通过，退出码 `0`。Python 3.9 `py_compile`：退出码 `0`；`git diff --check`：退出码 `0`，仅提示既有 `e7-gear-enhance-plan.md` 的 LF/CRLF 转换警告。

## 剩余风险与下一步

- 固定注册表和合成观察源仅说明离线接口可用，尚未从当前 MuMu 列表页取得任何锚点、候选卡、字段或唯一性视觉证据；它们不得被描述为已校准的真实页面布局。
- `RegisteredListPageLocalRecognizer` 严格消费一个局部观察源，但当前只定义该观察源的协议，尚未实现从内存 PNG 固定局部裁切直接运行本地 OCR/模板读取的生产适配器。因此现在发起真实三帧只读校准仍会在 ADB 前因“直接局部观察源缺失”停止，不能把合成源替代为真实证据。
- 第二次真实只读校准的授权已终止，不能复用。下一步须另建并取得授权完成“内存局部 OCR 观察源”离线开发；该任务通过后，才可新建一次真实三帧只读校准并取得新的明确授权。任何阶段均不得发送模拟器输入；真实点击仍需其后的独立任务和逐次授权。
