# GUI 布局修复与协作交接总结

更新时间：2026-07-10

## 本次任务结论

PySide6 GUI 的布局稳定性和中文调试可读性修复已完成。装备规则、评分、重铸、速度潜力、轻量预测、精确 DP 与样本策略均未修改。

`建议结果/07.json` 通过 GUI 加载后的建议仍为“继续”。

## 原始根因

1. 主区域使用无约束的双栏 `QHBoxLayout`，左右区域会受内容尺寸影响。
2. 调试详情使用 `QFormLayout + QLabel` 逐项展示。长值、嵌套结构和原始 JSON 会导致重叠、截断与横向溢出。
3. 副属性表格没有固定列宽，左侧被压缩时三列需要横向滚动才能完整访问。
4. 调试数据直接呈现内部字段名、枚举值和原始 JSON，不适合人工判断。

## 实施内容

### 稳定布局

- 主界面改为水平 `QSplitter`：左侧装备录入，右侧建议与调试详情。
- 左右区域均设置最小宽度，初始分栏比例固定；导入、判断、样本切换、调试展开/收起不会主动改变主窗口或分栏尺寸。
- 左侧录入字段放入仅纵向滚动的区域；“判断”按钮固定在录入区域底部。
- 副属性表固定三列宽度：属性 210、数值 90、强化次数 100；关闭其横向滚动条。
- 顶部功能入口保留，并改为多行工具栏以适应较窄窗口。

### 建议与调试展示

- 调试详情改为只读 `QPlainTextEdit`，按控件宽度换行，仅内部纵向滚动。
- 新增 `format_debug_details()`，将策略、DP、速度潜力、轻量预测、完整分类诊断、有效/无效副属性和强化命中分析格式化为中文分节文本。
- 对嵌套列表和字典采用缩进展示；内部字段名及常见内部枚举值转换为中文。
- 建议原因也改为固定高度、可换行的只读文本区域，避免挤压或重叠。

### `07.json` GUI 加载兼容

`建议结果/07.json` 含有宽松 JSON 解析才可读取的控制字符。GUI 的单文件加载路径现在会在批量解析出现 `JSONDecodeError` 时复用既有的 `load_gear_file()` 宽松加载路径。

- 不修改 JSON 文件内容。
- 不修改装备表单、策略输入、导出格式或建议结果。
- 此改动仅保证 GUI 能加载已有单文件结果包，以完成布局回归验证。

## 修改文件

| 文件 | 作用 |
| --- | --- |
| `src/e7_enhance/gui_app.py` | 分栏布局、左侧滚动录入、固定副属性列、右侧调试文本区、`07.json` GUI 加载回退。 |
| `src/e7_enhance/gui_support.py` | 中文调试详情格式化，仅供 GUI 展示使用。 |
| `tests/test_gui_app.py` | 长调试、左侧可见性、连续操作、`07.json`、全部验收样本与非法导入回归。 |
| `tests/test_gui_support.py` | 中文调试格式与导出结构不变回归。 |

## 验证结果

- GUI 测试：5 项通过。
- GUI 支持层测试：15 项通过。
- 全量测试：129 项通过。
- `py_compile`：通过。
- 1920x1280 和 1366x768 离屏截图：
  - 左侧四条副属性和“判断”按钮可见；
  - 右侧长调试内容仅在内部纵向滚动；
  - 主窗口与 splitter 尺寸不随内容和样本切换变化；
  - 未观察到副属性表横向滚动或字段重叠。

## 剩余人工验收

离屏 Qt 环境没有可用的中文字体回退，截图中的中文显示为方框。这不影响控件的实际字符串或自动化中文断言，但仍需在目标 Windows 桌面环境确认：

1. 系统中文字体正常渲染；
2. 100%、125%、150% 缩放下的窗口尺寸和分栏比例；
3. 手动拖动 splitter 后，再导入、判断、切换样本时比例保持不变；
4. 真实文件选择框导入 `建议结果/07.json` 与非法样本提示。

## 后续协作约定

后续由主 agent 负责与用户讨论需求、确认范围并给出完整的可执行指令；用户可将该指令交给子 agent 执行。

主 agent 给子 agent 的指令应至少包含：

1. 目标与验收标准；
2. 允许修改和禁止修改的文件/模块；
3. 必须先检查的现状、样本和测试；
4. 必须新增或更新的回归测试；
5. 需要执行的验证命令；
6. 最终报告中必须提供的证据、风险和待人工验收项。

子 agent 完成后应返回：修改文件、根因、修复前后行为、测试结果、未完成项与阻塞原因。主 agent 再基于这些证据与用户确认下一步，不把未验证的推测当作完成结论。
