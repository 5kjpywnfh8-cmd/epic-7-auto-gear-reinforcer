# 真实样本管理性能与筛选优化任务说明

## 目标

修复真实样本管理窗口在导入、保存或连续审核时容易短暂无响应的问题，并将强化等级筛选拆为独立等级。只处理真实样本管理 GUI、存储与测试；不得修改评分、强化策略、轻量预测、精确 DP、OCR、ADB、Airtest 或自动点击。

## 已确认现状

- 实际记录库 `manual_acceptance/real_sample_records.json` 约 23 MB，包含 425 个独立样本和完整建议/debug。
- 当前强化筛选只有“+0 / +3”“全部未满强化”“全部”，无法单独查看 +0 或 +3。
- `import_records()` 在 GUI 主线程同步解析、计算 425 件建议、写入文件并刷新表格。
- 每次 `save_review()` 都同步重写整份 JSON、重建 CSV，然后调用 `refresh_records()` 重建 425 行、9 列表格。
- 当前机器离屏测量：窗口初始化约 188 ms；425 件首次导入并刷新约 674 ms；单次保存并刷新约 231 ms；普通筛选约 20 至 50 ms。实际桌面环境叠加磁盘扫描和连续操作后会出现明显卡顿，因此必须以“事件循环仍能响应”作为验收，而不是只比较总耗时。

## 强化等级筛选规则

强化等级必须独立筛选，不再使用合并的“+0 / +3”选项。至少提供：

- `+0`
- `+3`
- `+6`
- `+9`
- `+12`
- `+15`
- `全部未满强化`（`enhance < 15`）
- `全部`

单级选项必须严格等于所选强化等级。筛选后仍应与来源、品质、部位、套装、审核状态和一致性筛选正确叠加。

## 执行要求

1. 先建立能够复现“GUI 主线程被阻塞”的自动化反馈环：使用实际 425 件文件或等价批量夹具，在导入和保存期间运行 Qt 定时心跳，修复前应检测到事件循环无法继续处理，修复后心跳能够运行。不得只写一个无法捕获无响应现象的浅层单元测试。
2. 增加强化等级失败测试：+0 只显示 +0，+3 只显示 +3，其他节点同理；“全部未满强化”排除 +15；不得保留含义不清的合并筛选。
3. 对导入路径做耗时分段测量：JSON 读取/解析、格式转换与校验、策略建议生成、JSON 保存、CSV 生成、表格刷新。报告实际瓶颈后再修改。
4. 耗时的批量导入和持久化不得阻塞 GUI 事件循环。采用项目现有 PySide6 方式实现后台任务，所有 QWidget 更新必须回到主线程；禁止让多个导入/保存任务并发覆盖记录文件。
5. 后台任务期间显示明确中文状态和进度，禁用会产生冲突的导入/保存控件；完成和失败后恢复控件。异常必须回到 GUI 中文提示，不能静默失败。
6. 保持 JSON 为唯一事实来源、原子写入和 CSV 自动导出，不改变现有记录字段、实例身份规则、人工审核结果或兼容边界。
7. 保存单条人工审核后，不得无条件清空并重建整张表。优先只更新受影响行；如果当前筛选导致该行不再可见，则只移除该行并选择合理的相邻项。
8. 表格批量更新时避免逐单元格触发昂贵的尺寸重算、信号和重绘。保持当前中文列、筛选组合和详情内容，不做无关界面重构。
9. 不以删除完整 debug 或降低建议信息完整度换取性能。若调整 JSON 格式或缓存，必须证明重启 GUI 后数据、建议、debug 和人工结论完全一致。
10. 更新 `manual_acceptance/README.md` 和 `e7-gear-enhance-plan.md`，记录新的单级筛选和后台状态行为。

## 最低验收

- 导入实际 425 件文件后仍为 425 个样本、425 个快照、0 错误，不发生身份错误归并。
- 导入和保存期间窗口可以移动、重绘，Qt 心跳仍运行，不出现“未响应”。
- `+0` 显示 417 件；`+3` 显示 8 件；两者不合并。
- 单级 +6/+9/+12/+15 筛选可用于后续强化快照。
- 保存审核后人工结论立即显示且持久化，重启后仍存在；不会重建整表造成明显卡顿。
- 现有来源、品质、部位、套装、审核状态和不一致筛选不回归。
- 相关测试及全量测试通过。

## 最终报告

- 无响应的根因与分段耗时证据；
- 修改文件和行为范围；
- 修复前后的事件循环响应、导入、保存和筛选测量；
- 单级强化筛选的实际计数；
- 相关测试与全量测试结果；
- 仍需人工验收的操作和风险。
