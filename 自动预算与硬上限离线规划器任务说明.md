# 自动预算与硬上限离线规划器任务说明

## 目标

实现一个纯 Python、无副作用、确定性的离线强化预算规划模块：根据装备材料池、当前标准节点、目标标准节点、允许材料、库存、材料优先级和人工硬上限，生成整数材料分段计划、累计预计消耗、建议硬上限及结构化 fail-closed 结果。

该模块只负责离线计算和验证，不负责授权，也不连接或控制任何游戏界面。

## 范围

- 支持标准节点 `0/3/6/9/12/15`，并按相邻节点拆分从当前节点到目标节点的计划。
- 区分普通装备与饰品材料池，拒绝跨材料池组合。
- 仅使用已有代码或已验证证据能够证明经验值、金币和消耗语义的整数材料。
- 支持允许材料集合、库存、确定性优先级、分段硬上限和累计硬上限。
- 明确分离预计消耗、建议硬上限和用户已审核硬上限；规划结果不得自行表示已授权。
- 输出机器可读 JSON、可读 Markdown 报告和稳定失败原因。
- 用定向测试及无新增依赖的有界穷举/性质测试证明任何成功计划均不超过库存和硬上限。

## 模块接口约束

- 外部 seam 应保持为一个主要规划入口；调用方只提交一个不可变请求并获得计划或结构化失败结果。
- 请求至少包含：材料池、当前节点、目标节点、允许材料、库存、材料优先级，以及可选的用户已审核材料/金币硬上限。
- 结果至少包含：规则版本、模式（仅建议或已按硬上限验证）、逐段材料组合、逐段经验/金币、累计预计消耗、累计建议硬上限、审核硬上限核对结果和失败原因。
- 材料组合搜索、整数舍入、确定性排序、分段聚合和上限证明全部隐藏在模块内部，不向调用方暴露搜索过程。
- 同一输入必须得到字节级稳定的规范化 JSON 结果；不得依赖当前时间、全局随机数、网络、文件系统或环境状态。

## 规划与安全规则

1. 当前节点和目标节点必须属于 `0/3/6/9/12/15`，目标必须严格大于当前节点；其他情况 fail closed。
2. 只允许使用请求中明确列出的材料；库存、硬上限和材料数量必须是非负整数。
3. 任何成功计划的材料数量不得超过库存、分段硬上限或累计硬上限，金币不得超过对应硬上限。
4. 在所有满足约束的整数计划中，先最小化金币，再最小化经验溢出，再最小化材料总数，最后按用户给定材料优先级确定稳定结果；实际目标排序必须在报告中显式记录并由测试固定。
5. 若同一材料组合无法证明在真实界面规则下满足目标节点，必须拒绝，不能依赖期望值、分数材料或概率补偿。
6. 未提供审核硬上限时，只能输出 `proposal_only`；提供完整硬上限并通过核对时可输出 `validated_against_hard_limits`，但两者都不构成实际操作授权。
7. 建议硬上限必须与预计消耗分字段输出；不得用预计值冒充用户审核上限。
8. 上级强化石当前没有已发布的离散模型。即使请求允许，也必须返回明确的 `unsupported_material`，不得猜测换算或静默忽略。
9. 库存不足、允许集合为空、优先级缺失/重复、材料池不匹配、硬上限字段不完整、无可行整数组合、预览需求不一致时均 fail closed。
10. 该模块不得读取实时库存、截图、OCR、ADB、MuMu、私人证据或可信快照。

## 禁止修改项

- 不修改正式策略、DP、评分、现有资源模型常量或计算语义、转换规则、GUI、OCR、Holdout、策略 manifest 或自动化规则。
- 不把期望资源模型改写为实际执行模型，不把 batch `004/013` 的单次结果外推为未验证的通用常量。
- 不实现点击、材料选择、确认弹窗、设备发现、多节点执行状态机或故障恢复。
- 不操作模拟器，不发送 ADB 输入，不消耗游戏资源。
- 不读取、恢复、暂存、提交或上传私人证据归档。
- 不安装新依赖，不联网获取规则。

## 输入与权威口径

- [策略与自动强化执行边界报告修订稿](reports/策略与自动强化执行边界报告_修订稿_20260725.md)。
- `src/e7_enhance/resource_model.py` 和 `tests/test_resource_model.py` 仅用于核对现有边界；不得把其中期望值或分数单位直接当作离散执行规则。
- 仓库内已跟踪、可公开复跑的强化规则、常量和测试；若无法证明真实整数规则，必须在报告中标记阻断并保持 fail closed。
- 根目录 `AGENTS.md` 和 `e7-gear-enhance-plan.md`。

## 模型与执行分工

- 代码实现和代码测试使用用户指定的 `gpt-5.6-terra + high`。
- 子 agent 必须完整读取本任务说明并将其作为本轮唯一权威来源；实际模型和 reasoning effort 必须写入本任务说明、报告和总计划。
- 主 agent 只负责只读审阅代码、测试和报告，决定是否满足闸门，并同步文档。

## 执行步骤

1. 只读核对现有资源模型、公开强化常量、包结构、CLI 和测试模式，列出可证明与不可证明的离散规则。
2. 在现有目录风格下设计一个小 interface、深 implementation 的离线规划模块，避免复制或修改正式资源模型。
3. 先补失败与边界测试，再实现整数组合搜索、确定性排序、分段聚合和硬上限核对。
4. 新增最小 CLI 或报告生成入口，生成规范化 JSON 与 Markdown 证据。
5. 运行定向测试、语法检查、CLI smoke、`git diff --check`；只有定向结果稳定后才决定是否运行无需私人夹具的扩大测试。
6. 子 agent 返回后，主 agent 只读审阅实现和证据，再更新本任务状态及总计划。

## 产物

- 离线预算规划模块及必要的最小导出。
- 对应定向测试和有界穷举/性质测试。
- 一个不读取私人数据的 CLI 或报告生成入口。
- `reports/` 下的 JSON 与 Markdown 验证报告。
- 更新后的本任务说明和 `e7-gear-enhance-plan.md`。

## 验证要求

- 覆盖两个材料池和全部五个相邻标准节点。
- 覆盖建议模式、完整硬上限验证模式及所有规划与安全规则的失败分支。
- 有界穷举/性质测试必须断言：任何成功结果的材料和金币都不超过库存、分段硬上限和累计硬上限；任何输出材料都属于允许集合及正确材料池。
- 上级强化石必须稳定 fail closed。
- 同一输入重复运行的规范化 JSON 完全一致。
- Python 3.9 兼容、定向测试退出码 `0`、语法检查通过、CLI smoke 退出码符合结果语义、`git diff --check` 通过。
- 若扩大测试需要私人归档夹具，立即停止；本任务不包含临时恢复授权。

## 停止条件

- 无法从公开、已跟踪输入证明某种材料的真实整数经验或金币规则时，不得猜测；将该材料标记为不支持并继续完成可证明部分。
- 若实现需要修改正式资源模型、策略、OCR、GUI 或自动化，立即停止并报告范围冲突。
- 若测试发现成功结果可能越过任何硬上限，状态必须保持 `not_ready`，不得进入下一阶段。
- 若出现工作区外文件、私人证据或非本任务修改，保持原样，不暂存、不回退。

## 当前状态

`completed_offline_budget_planner_validated`

首次 `gpt-5.6-terra + high` 实现完成后，独立同模型只读审阅发现当前版本尚未满足闸门：非法 `segment_hard_limits` 和 `preview_base_experience` 类型会被静默视为未提供；仅提供分段或累计一类硬上限也会被标记为已完整验证；五段计划缺少库存、单段和累计不足的有界安全证据；CLI/Markdown 仍会回显未经安全化的错误详情。以上问题修复并通过回归前保持 `not_ready`，不得进入下一阶段。

修复已由 `gpt-5.6-terra + high` 完成：规划器 `27/27`、资源模型回归 `15/15`、CLI 回归 `1/1`、Python 3.9 语法、报告生成、成功/失败 CLI smoke 和 `git diff --check` 均通过；五段有界矩阵 `81` 组中成功 `16`、fail closed `65`、越限 `0`。当前等待二次只读审阅，尚未提交或推送。

二次只读审阅未发现新的实现类 P1/P2，但发现验收证据仍缺“每个分段分别改变两种材料上限”和“累计两种材料分别改变”的有界维度。现有 81 组矩阵不足以单独证明这些输入维度；补齐测试和报告前继续保持 `pending`。

测试证据缺口已由 `gpt-5.6-terra + high` 补齐：五段分段材料上限 `40` 组、累计材料上限 `8` 组、库存材料 `8` 组，总计 `56` 组，其中成功 `30`、fail closed `26`、越限 `0`；规划器测试更新为 `29/29`。当前等待最终独立审阅和复跑，尚未提交或推送。

最终复核发现两项 P2 证据精度问题：当某段计划材料量为 `0` 时，`planned_minus_one` 与 `zero` 输入重复但报告仍按四个标签计数；矩阵成功断言也未强制返回完整五段及固定端点。修复前保持 `pending`，不得将重复标签计为独立证据。

两项证据问题已由 `gpt-5.6-terra + high` 修复：原始标签候选 `56`，alias/duplicate `3`，独立整数输入 `53`；分段 `37`、累计 `8`、库存 `8`，成功 `28`、fail closed `25`、结构或越限违规 `0`。所有成功结果均检查恰好五段、固定路线、唯一端点和全部库存/分段/累计上限。规划器测试更新为 `30/30`，当前等待最终聚焦复核。

聚焦复核发现仍需修正两项 P2：`53` 是覆盖轴案例数，跨维度全局规范化请求只有 `40` 个，报告必须同时给出覆盖案例、全局唯一请求和重复请求计数；成功结果 oracle 还必须精确拒绝额外、未知或跨池材料键。修复前继续保持 `pending`。

两项问题已由 `gpt-5.6-terra + high` 修复：报告现在同时输出 raw 标签 `56`、alias `3`、覆盖案例 `53`、全局唯一请求 `40`、重复请求案例 `13`；覆盖案例成功/失败为 `28/25`，全局唯一请求成功/失败为 `15/25`。oracle 已校验材料池、允许集合及所有材料键集合，synthetic 回归覆盖缺失、额外、未知、跨池和错误结果材料池，违规 `0`；规划器测试 `34/34`。当前等待最终收口审阅。

最终 `gpt-5.6-terra + high` 独立只读审阅未发现新的 P1/P2，任务完成闸门通过。规划器 `34/34`、资源模型回归 `15/15`、CLI 回归 `1/1`、Python 3.9 语法、报告生成、成功/失败 CLI smoke 和 `git diff --check` 均通过；未运行依赖私人夹具的全量测试。饰品离散材料和上级强化石继续 fail closed，本任务不构成模拟器操作或实际强化授权。

本任务产物已由提交 `c5b2379` 推送至 `origin/codex/policy-v1-manifest-20260725`；本轮未包含私人证据或归档文件。
