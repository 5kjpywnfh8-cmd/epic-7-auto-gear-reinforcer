# 策略与自动强化执行边界报告审阅任务说明

## 目标

对用户提供的《策略与自动强化执行边界报告》进行只读审阅，并与当前策略 V1 manifest、正式策略实现、资源模型和既有单件操作证据核对，明确可接受结论、需要修订的执行契约、当前已完成闸门和下一阶段开发顺序。

## 范围

- 核对 `normal_85 Epic/Heroic`、`rift_85 Epic` 在 `+0/+3/+6/+9/+12/+15` 的正式决策路由。
- 核对 Epic 非速度 `+0/+3` 三组阈值、四项联合止损条件、未知候选体系与未知套装边界。
- 核对 Heroic 不启用正式资源 DP、M1 22 速救回和一次转换限制的现有实现证据。
- 审阅“单件尝试至 `+15`、逐节点复核、自动预算、硬上限和自动点击”的未来执行模型。
- 只新增审阅文档并同步总计划，不实现下一阶段功能。

## 禁止修改项

- 不修改正式策略、DP、评分、资源模型、转换规则、GUI、OCR、Holdout、策略 manifest 或自动化规则。
- 不操作模拟器，不选择材料，不发送 ADB 输入，不消耗游戏资源。
- 不把本次审阅结论解释为自动强化授权或下一阶段实现授权。
- 不读取、恢复、暂存、提交或上传私人证据归档。

## 输入与权威口径

- 用户在当前会话提供的《策略与自动强化执行边界报告》全文。
- [策略 V1 manifest](reports/policy_v1_manifest_20260725.md)及同名 JSON；当前核心 SHA-256 为 `050041e19c7626d20deb73e95e2894ce48a5c0e7cd685a4ae471914d7d97423b`。
- [策略 V1 剩余冻结闸门收口任务说明](策略V1剩余冻结闸门收口任务说明.md)。
- `src/e7_enhance/enhance_policy.py`、`epic_early_stop.py`、`lightweight_calibration.py`、`calibration.py`、`resource_model.py`、`single_item_confirmation.py` 及对应定向测试。
- 根目录 `AGENTS.md` 和 `e7-gear-enhance-plan.md`。

## 执行步骤

1. 核对当前 Git、V1 manifest 和总计划状态，确认冻结闸门是否已完成。
2. 将报告中的策略路由、阈值、动作语义和 Heroic 边界与当前实现逐项比较。
3. 审计现有资源模型和单件汇总器是否已经提供可执行的离散预算、事前硬上限和自动点击状态机。
4. 识别状态变化后可信快照刷新、非标准节点、材料离散组合、幂等点击和故障恢复等执行缺口。
5. 输出审阅报告，同步总计划，只记录当前最先需要建立的下一任务，不启动开发。

## 产物

- [策略与自动强化执行边界审阅报告](reports/策略与自动强化执行边界审阅_20260725.md)。
- 更新后的 `e7-gear-enhance-plan.md`。

## 验证要求

- 当前 V1 状态、路由和阈值必须能由 manifest JSON/Markdown 与定向测试证明。
- 对“尚未实现”的判断必须能由代码边界证明，不得把研究资源模型或事后汇总器误写成自动预算/事前执行拦截器。
- 文档链接、UTF-8 内容和 `git diff --check` 必须通过。
- 本轮没有业务代码变更，不要求重跑含私人夹具的全量测试；只运行不依赖私人数据的相关定向测试。

## 停止条件

- 若审阅需要改变正式策略、资源常量、OCR 门槛或自动化行为，立即停止，只把它列为后续独立任务候选。
- 若报告与当前 manifest 冲突，以当前已验证并推送的 manifest 为现状依据，并明确标记报告对应段落需要更新。
- 若下一阶段没有新的独立任务说明和用户授权，不开始实现或模拟器操作。

## 当前状态

`completed_review_only_no_runtime_change`
