# 真实样本管理工具任务说明

更新时间：2026-07-11

## 任务目标

在现有 PySide6 GUI 中新增一个简单、可持续使用的“真实样本管理”工具，解决以下问题：

1. 从兼容装备 JSON 中筛选未强化完成装备，不要求人工重填装备属性；
2. 自动运行当前策略并保存工具建议、目标体系和关键 debug；
3. 人工只填写判断、分歧原因和备注；
4. 记录可在重启后继续编辑，并自动导出 CSV 供 Excel/WPS 查看；
5. 不改变任何装备规则、评分、轻量预测或精确 DP 行为。

本任务不接 OCR、ADB、Airtest、自动点击或外部服务。

## 产品形态

在现有主窗口增加“真实样本管理”入口，打开独立 `QDialog`。不要向主界面继续堆叠表格或破坏现有稳定分栏。

对话框建议分为三部分：

1. 左侧样本列表：编号、来源、品质、强化等级、套装、部位、工具建议、人工判断、一致性、审核状态；
2. 右侧详情：装备字段、工具建议、目标体系、关键理由、候选体系和 debug 摘要；
3. 底部人工记录区：人工判断、是否一致、分歧原因、备注、保存/更新。

选择样本时自动运行或读取当前策略结果。“判断”仅在装备字段被人工修改后重新计算，不作为人工记录按钮。

## 导入与筛选

第一版只接受本项目现有加载器已经支持的 JSON：

- 单件装备对象；
- `items`、`gears` 或 `equipment` 集合；
- 已保存的建议结果包。

导入后提供筛选：

- 默认：`+0 / +3`；
- 可选：全部未满强化（`enhance < 15`）；
- 来源：普通 85、异界 85；
- 品质：红装、紫装；
- 部位、套装、审核状态；
- 仅显示工具与人工不一致。

第三方导出格式不得凭空猜测。若用户提供实际 Fribbels/其他导出样本，再为该格式新增独立只读适配器和测试。原始导出文件始终只读，不能原地修改。

## 数据存储

使用 JSON 作为唯一事实来源，不引入数据库。建议文件：

```text
manual_acceptance/real_sample_records.json
manual_acceptance/real_sample_records.csv
```

CSV 由 JSON 自动生成，只用于查看和筛选；不得要求用户手工复制装备字段。

建议记录结构：

```json
{
  "version": 1,
  "records": [
    {
      "sample_id": "stable-id",
      "source_file": "原始导出文件名",
      "gear": {},
      "suggestion": {},
      "summary_view": {},
      "debug_view": {},
      "human_review": {
        "decision": "",
        "consistency": "",
        "reason": "",
        "note": ""
      },
      "review_status": "pending",
      "created_at": "",
      "updated_at": ""
    }
  ]
}
```

要求：

1. 保存采用临时文件加原子替换，避免程序中断损坏记录；
2. 导入时按规范化装备指纹去重；若装备有稳定 `code`，优先使用 `code`；
3. 同一装备后续强化节点若有稳定 `code`，作为同一 `sample_id` 的新快照；没有稳定标识时，不自动误合并，允许人工关联；
4. 不修改原始导出文件和现有回归样本；
5. 旧的 `manual_test_records.csv` 保留为兼容模板，不作为新工具的数据源。

## 人工记录交互

人工只需要填写：

- 人工判断：继续、谨慎继续、停止、保留、转换/过渡、不确定；
- 一致性：一致、不完全一致、不一致；
- 分歧原因：目标体系错误、转换候选错误、继续/停止分歧、门槛不合理、输入识别错误、debug 不清楚、其他；
- 备注：自由文本。

工具建议和人工判断完全相同时，可默认“一致”；不同时默认“不一致”，但允许人工改成“不完全一致”。

点击“保存记录”后自动写入 JSON 并重新生成 CSV。CSV 至少包含：

```text
样本编号、来源文件、装备来源、品质、强化等级、套装、部位、主属性、
四条副属性及强化次数、工具建议、目标体系、工具理由、人工判断、
一致性、分歧原因、备注、审核状态、更新时间
```

## 允许与禁止修改

允许修改：

- `src/e7_enhance/gui_app.py`：仅增加入口和独立对话框；
- 新增独立样本存储/导出模块，例如 `src/e7_enhance/manual_sample_store.py`；
- 必要的 `gui_support.py` 读取与中文展示辅助；
- GUI、存储、导入、CSV 导出相关测试；
- 本说明、计划和人工验收文档。

禁止修改：

- `enhance_policy.py`、`score_engine.py`、`lightweight_calibration.py`、route solver 和所有策略规则；
- JSON 装备语义和现有导入导出格式；
- `建议结果/07.json`、`建议结果/14.json` 与现有手工验收样本；
- GUI 主分栏、现有布局尺寸稳定性和中文 debug 行为；
- OCR、ADB、Airtest、自动点击、联网和数据库。

## 执行要求

1. 先阅读现有 GUI、`gui_support.py`、人工验收说明和相关测试；列出当前已有能力与缺口。
2. 先新增失败回归测试，再实现最小功能。
3. 样本存储逻辑放入独立模块，不把 JSON/CSV 操作堆进 GUI 类。
4. 所有可见文字使用中文；长装备/debug 内容放入内部滚动区域，不改变主窗口尺寸或 splitter 比例。
5. 导入非法装备时逐条报告错误，合法装备仍可继续导入；不得因一条错误丢失整批记录。
6. 选择样本时复用 `suggest_from_form`，不得复制或改写策略逻辑。
7. 自动保存工具结果时保留完整 suggestion，同时生成简洁中文视图；不得删除 debug 数据。
8. 完成后更新人工验收说明和计划文档。

## 最低回归测试

1. 导入单件、`items/gears/equipment` 集合和建议结果包；筛选 `+0/+3` 与 `enhance < 15`。
2. 自动记录全部装备属性、强化次数、工具建议、目标体系和关键理由，不需要人工输入属性。
3. 人工判断、分歧原因和备注保存后，关闭并重开仍存在。
4. JSON 为事实来源，CSV 自动生成且字段完整、中文可读。
5. 重复导入不产生重复记录；同 `code` 后续强化状态形成关联快照；无 `code` 不误合并。
6. 一条非法样本不会阻止同批合法样本导入，并显示中文错误摘要。
7. 选择、筛选、保存记录不会改变主窗口尺寸和 splitter 比例。
8. `07.json`、`14.json` 导入后策略结果与当前保持一致。
9. 现有 GUI 测试、策略测试和全量测试全部通过。

## 最终报告

必须包含：

1. 修改文件与模块边界；
2. 数据文件格式、去重和快照关联规则；
3. 导入、筛选、人工记录和 CSV 导出实测结果；
4. GUI 布局稳定性与中文可读性结果；
5. 相关测试和全量测试结果；
6. 第三方导出格式尚未支持的限制及下一步所需样本。
