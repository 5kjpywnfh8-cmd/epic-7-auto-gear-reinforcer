# 纯视觉局部套装模板匹配器任务说明

## 目标

在已验证的本地 Fribbels 套装图标资产之上，设计并实现一个只读、离线、fail-closed 的局部模板匹配器，接入现有 `LocalTemplateRecognizer` 协议。匹配器只提供视觉候选锚点，不把套装候选表述为字段或服务器确认；所有运行时证据继续使用 `mode=visual_only`、`verification=unverified`。

## 唯一权威来源与输入

本文件是本轮唯一权威任务来源。实现必须完整遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md`、`纯视觉 ADB 局部模板与 PaddleOCR 只读识别任务说明.md`、`纯视觉套装图标模板补充任务说明.md` 及现有 `VisualFrame`、`RegionSample`、`LocalTemplateRecognizer`、`LocalRecognitionEvidenceParser` 接口。

输入只允许来自已注入的内存 `RegionSample.payload` 和本地 `load_set_icon_templates()` 返回值。模板根目录、清单来源和哈希校验由现有加载器负责；运行时不得联网、读取私人归档或写入截图。

## 范围

- 新增离线局部图像匹配器及其最小内部表示，优先匹配 `set`/`set_anchor` 局部区域。
- 复用现有 PNG 内存解码/视口校验能力；模板与样本尺寸不一致时只允许明确的受控缩放或直接 fail-closed，禁止按全屏坐标猜测。
- 输出符合现有模板锚点协议的映射，包含稳定名称、候选标识、分数、固定阈值和正亮度比例；输出必须能被现有证据解析器和运行时门禁消费。
- 增加 fake PNG/合成模板和公开离线测试，覆盖命中、缺失模板、损坏输入、视口/区域异常、低分数、并列歧义和不满足唯一锚点的 fail-closed 分支。
- 精确审阅 diff，运行定向测试、语法检查和 `git diff --check`；同步本文件与总计划，精确提交并推送当前分支。

## 禁止修改与操作边界

- 不修改正式策略、DP、评分、资源模型、GUI、OCR 引擎、`0.98` 字段门槛、Holdout、ADB 设备发现、帧采样器、点击执行器或自动化规则。
- 不执行 ADB、截图、OCR、页面导航、`input tap`/`swipe`/`keyevent`、强化、选材、重启游戏、资源消耗、底层 Fribbels/PCAP/TCP 读取或任何外部上传。
- 不降低或绕过 `0.98` 字段门槛；模板匹配阈值必须在实现和测试中显式记录，不能把低分候选转为接受结果。
- 不使用 `git add -A`，不触碰 `refs/heads/main - 副本`，不读取、恢复、暂存或上传私人归档。

## 执行步骤

1. 完整审阅现有模板加载器、局部 PNG 解码、区域清单、证据解析器、运行时门禁和公开测试。
2. 设计最小匹配接口：只接收内存 PNG 区域样本和已校验本地模板，输出候选锚点；对格式、尺寸、旋转、空白、区域越界和歧义统一 fail-closed，不自动重试。
3. 实现匹配器并接入现有 `LocalTemplateRecognizer` 注入点，保持既有 fake recognizer 与调用方兼容；不改变证据 schema 或门禁语义。
4. 增加离线合成测试，证明目标模板命中、非目标/低分/并列结果拒绝，以及完整 `VisualAdapterSampler -> AdbLocalRecognitionEvidenceParser -> VisualEvidenceGate` 链路仍保持视觉未确认状态。
5. 精确审阅变更并执行验证；将完成项、退出码、未完成项、风险和下一步写回本文件及总计划。

## 产物

- 局部套装模板匹配器实现及公开测试。
- 本文件的完成/验证记录。
- `e7-gear-enhance-plan.md` 的同步状态和任务链接。

## 验证要求

- 模板资产加载器和现有视觉/OCR 回归不得退化。
- 命中必须提供可审计的候选标识、分数和阈值；低分、缺失、损坏、尺寸/视口漂移和并列最高候选必须拒绝。
- 报告真实测试退出码；不得把离线合成命中写成真实 ADB 或服务器确认。

## 停止条件

任一文档未完整读取、接口不兼容、模板加载失败、PNG 解析失败、匹配阈值无法固定、歧义未能 fail-closed、任一定向测试失败或 diff 检查失败时立即停止，不进入真实 ADB 校准、点击执行器或自动强化。

## 当前状态

`offline_local_set_template_matcher_verified_pending_real_readonly_calibration_20260726`

执行模型：`gpt-5.6-terra + high`。

## 完成与验证记录（2026-07-26）

- 新增 `src/e7_enhance/visual_set_template_matcher.py`：仅消费注入的内存 `RegionSample.payload` 与已校验的本地模板加载器；优先 `set_anchor`、回退 `set`，输出候选标识、分数、固定 `0.98` 阈值与正亮度比例。模板阈值不可由调用方降低。
- 样本 PNG 与局部区域像素边界必须完全一致；缺模板、损坏 PNG、越界/尺寸漂移、低分、跨模板并列和同模板多位置并列均返回空锚点，不自动重试或推断。
- 新增公开合成测试 `tests/test_visual_set_template_matcher.py`，覆盖命中、`set` 回退、上述 fail-closed 分支，以及 `VisualAdapterSampler -> AdbLocalRecognitionEvidenceParser -> VisualEvidenceGate` 离线链路；该链路断言结果仍为 `mode=visual_only`、`verification=unverified`，不代表真实 ADB 或服务端确认。
- 验证退出码均为 `0`：`python -m unittest tests.test_visual_set_template_matcher tests.test_visual_templates tests.test_visual_adb_recognition tests.test_visual_platform tests.test_visual_adapter tests.test_visual_runtime tests.test_visual_click tests.test_visual_sampling`（52 项）；`python -m py_compile src/e7_enhance/visual_set_template_matcher.py tests/test_visual_set_template_matcher.py`；`git diff --check`。
- 未执行真实 MuMu/ADB、截图、OCR、导航、点击、强化、选材、资源消耗、Fribbels/PCAP/TCP 读取或外部上传。匹配器是可注入的局部候选组件，实际证据链仍需后续页面锚点组合；真实帧校准需另建任务说明并取得单独授权。
