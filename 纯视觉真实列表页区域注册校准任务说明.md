# 纯视觉真实列表页区域注册校准任务说明

## 目标

将 `equipment_list_region_registry()`（`src/e7_enhance/visual_list_recognizer.py`）中注册的 `candidate_card:N` 区域从当前假设网格校准为真实列表页布局，使整卡指纹模板的运行时 SHA-256 匹配能够命中，为 `card_fingerprint_manifest.json` 从 `missing` 升级至 `audited_complete` 扫清区域层面阻断。

## 背景与基线（2026-08-01 真实三帧采集结论）

- 真实三帧均 `1280x720`，目标卡片区域（x143-313, y451-568，170x117）与详情面板区域（x820-1280, y300-480）三帧像素完全一致（mean_diff=0.00）。
- 目标卡片整卡 crop 已存 `assets/visual/list_reference/capture_20260801/target_card_full_20260801.png`（`reference_only`），raw 像素 sha256 `13531f05…`、PNG sha256 `acf1d02e…`；采集记录见同目录 `capture_manifest_20260801.json`。
- 当前注册区域 `candidate_card:1` = normalized `{left:0.08, top:0.22, right:0.48, bottom:0.44}` = 像素 512x160（x102-614, y158-317），与真实卡片 170x117 不匹配 → 运行时 SHA-256 无法命中。
- 真实帧显示列表为 4 列网格（列中心约 x=180/349/520/691）多行布局，与注册假设的 2x3 网格不同；目标卡片位于第 1 列、y451-568 行。

## 范围

- 仅授权一次真实采集：官方 MuMu `adb.exe devices -l` + 恰好三帧 `exec-out screencap -p`（固定 `1280x720`），用于布局网格与卡片边界分析。
- 离线分析真实帧，确定每张候选卡片像素边界，更新 `equipment_list_region_registry()` 的 normalized bounds（区域名称与 schema 语义保持不变）。
- 同步更新受区域断言影响的测试（`test_visual_list_recognizer.py`、`test_visual_list_png_source.py`、`test_visual_list_local_source.py`、`test_visual_list_production.py` 等）。
- 校准后验证整卡指纹匹配：用与 `build_audited_card_template_reader`/`ManifestCardFingerprintReader` 一致的哈希口径，确认新注册区域产出的 crop 与已存整卡 crop 字节一致（SHA-256 命中）。
- 仅当 SHA-256 命中验证通过时，才将 `card_fingerprint_manifest.json` 更新为 `audited_complete` 并登记该整卡模板（含来源、视口、边界、SHA-256、阈值、`reference_only`→生产候选转换依据）；未通过则保持 `missing` 并记录原因。
- 在 `reports/` 生成校准报告，并同步本任务说明与 `e7-gear-enhance-plan.md`。

## 禁止修改项

- 禁止 `input`、点击、滚动、导航、进入详情/强化页、选材、确认和资源消耗；禁止自动重试、追加采样、联网、上传、下载模型。
- 禁止修改 `field_mapping_manifest.json` 的状态或映射（攻击%/暴击率%/效果抗性% 图标语义审定不在本任务范围，仍保持 `incomplete`）。
- 禁止降低 `0.98` 置信度门槛、禁止用用户描述、历史截图或测试 fixture 冒充真实帧证据、禁止在 SHA-256 命中未验证时登记模板。
- 禁止改动 `visual_list_observer.py` 的 `RegisteredListPageRegions`/`RegisteredListRegion` 校验契约、`visual_list_parser.py` 的 `REQUIRED_ANCHORS`/`REQUIRED_REGIONS` 与 `visual_list_production.py` 的装配逻辑；区域集合内若需增减 `candidate_card:N` 数量，须在报告中说明理由并同步全部受影响测试。
- 不得回退、覆盖或清理工作区中与本任务无关的既有未提交改动。

## 输入与权威口径

- 新采集的三帧内存 PNG 是本轮布局分析的唯一权威证据；`capture_manifest_20260801.json` 的目标卡片边界记录为交叉参照，不作为唯一来源。
- 采集命令与前置校验按 `AGENTS.md`「MuMu 装备数据抓取」：执行前先 `adb.exe devices -l`，仅接受唯一状态为 `device` 的设备；不写死、不复用任何 `192.168.x.x:5555` 地址。
- 区域 schema：`LIST_PAGE_REGION_SCHEMA_VERSION = "e7_enhance.equipment_list_regions/1"` 保持；边界校准不改变区域语义，须在函数 docstring 与报告中注明校准证据来源与日期。
- 整卡指纹哈希口径以 `visual_list_assets.py` 中 `build_audited_card_template_reader`/模板校验实现为准（读源码确认，不得假设）。

## 执行步骤

1. 用户批准本文件授权后：`adb.exe devices -l` 发现唯一 `device`。
2. 采集三帧 PNG 到项目内临时路径，校验视口 `1280x720`、SHA-256 并记录；目标卡片区与详情面板区三帧逐字节比对（期望 mean_diff=0.00，允许顶栏/时钟极小差异）。
3. 离线分析布局：检测卡片亮边框与列网格，确定每张可见候选卡片的像素边界，映射到 `candidate_card:N`（或按证据调整数量并记录理由）。
4. 更新 `equipment_list_region_registry()` 的 bounds；更新受影响测试断言。
5. 验证整卡指纹命中：按资产层哈希口径计算新注册区域 crop 与已存整卡 crop 的一致性（SHA-256 命中），记录真实结果。
6. 若命中：更新 `card_fingerprint_manifest.json` 至 `audited_complete` 并登记模板；否则保持 `missing`。
7. 运行 `test_visual_list*.py` 全量、`py_compile`、`git diff --check`，报告真实退出码。
8. 写报告到 `reports/`，同步任务说明与总计划；清理临时文件。

## 产物

- 校准后的 `equipment_list_region_registry()` 代码改动与受影响测试改动。
- `reports/` 校准报告：设备发现、三帧哈希与稳定性、布局网格分析、新旧区域对照、SHA-256 命中验证结果、模板登记或保持 `missing` 的决定及理由。
- 若通过：更新后的 `card_fingerprint_manifest.json`（`audited_complete` + 模板条目）。

## 验证要求

- `test_visual_list*.py` 全绿（报告真实退出码），`py_compile` 退出码 0，`git diff --check` 退出码 0。
- 整卡指纹 SHA-256 命中验证必须给出真实输出；未命中必须说明差异（区域边界、视口、哈希口径）。

## 停止条件

设备不唯一、视口非 `1280x720`、三帧不稳定、布局网格无法确定、校准后区域无法覆盖已存整卡 crop、或任何需要输入/导航/额外采样的要求出现时立即 fail-closed：不登记模板、不生成默认区域、不降低门槛，报告原因后结束本轮。

## 授权要求

执行真实采集前需要用户明确批准本文件定义的一次性三帧只读采集；该授权不包含任何点击或导航，且使用后不可复用。

## 当前状态

`region_calibration_completed_card_fingerprint_audited_full_card_template_registered_20260801`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口不可用 Luna，实际执行按最近可用替代（`gpt-5.6-terra + high` 或本会话实际模型）如实记录。本会话实际模型为 `deepseek-v4-flash`（opencode 环境默认），已如实记录。

## 执行记录（2026-08-01）

- 用户批准一次性三帧只读采集。官方 MuMu ADB 发现唯一 `emulator-5554`，三次 `exec-out screencap -p` 均 `1280x720`（SHA-256：`bf0e6890…`/`ba0166c4…`/`51c6b5ff…`）。
- 三帧稳定性成立：目标卡片区域（x143-313, y451-568）与详情面板区域（x820-1280, y300-480）逐帧像素完全一致（mean_diff=0.00）；列表头部仅有极小差异（0.03，顶栏数字/时钟）。
- 离线分析真实帧确定列表网格：5 行卡片（行顶 y=65/193/322/451/580，行高 117）× 4 列卡片（列左 x=143/313/483/653，列宽 170）；目标卡片位于行 4 列 1（x143-313, y451-568）。
- 关键发现：已存整卡资产 `capture_20260801/target_card_full_20260801.png` 为 PIL 编码（PNG sha256 `acf1d02e…`），与运行时 `InMemoryPngRegionExtractor` 的 filter-0 zlib 编码不一致，导致 SHA-256 指纹无法命中。本轮按运行时编码重建模板资产。
- 更新 `equipment_list_region_registry()`：从假设 2×3 网格（6 卡）校准为真实 4×5 网格（20 卡），`candidate_card:13` 精确对位目标卡片（x143-313, y451-568）；`list_header_region` 与 `candidate_list_region` 同步更新。
- 按运行时编码生成模板资产 `list_reference/target_card_full_20260801_runtime.png`（170×117，sha256 `99efe5c2…`），`card_fingerprint_manifest.json` 从 `missing` 更新为 `audited_complete` 并登记该模板（threshold 0.98）。
- 命中验证：`InMemoryPngRegionExtractor` 在 `candidate_card:13` 区域的三帧 crop SHA-256 均为 `99efe5c2…`，`ManifestCardFingerprintReader.match_template` 返回 `score 1.0 / threshold 0.98`，整卡指纹唯一命中。
- 同步受影响测试：4 个测试文件的记录构造扩展至 20 卡；`test_visual_list_assets.py` 更新为「field mapping 仍 fail-closed、card fingerprint 已 audited 且可命中」；`test_visual_list_local_source.py` 更新 header pixel bounds 断言。
- 验证：`test_visual_list*.py` 46/46、`test_visual*.py` 126/126、`py_compile` 退出码 0、`git diff --check` 退出码 0。
- 未发送输入、未点击、未滚动、未导航、未强化、未选材、未确认、未消耗资源、未联网、未上传或下载模型；未修改 `field_mapping_manifest.json`（三个缺失字段图标语义仍 `incomplete`，属另一独立阻断）。
