# Epic 均衡候选前瞻验证准备任务说明

## 目标

为 `B_global_current_gs` 建立真正独立的 normal_85 Epic `+0/+3` 非速度前瞻验证。在读取任何新人工标签前，先冻结候选规则、补齐终局22速价值、按联合批次新成本重校准研究用 lambda，并解决全量测试入口问题。

本任务不发布正式策略，不覆盖正式 lambda，不修改 Heroic 策略、跳值表、评分分类或 GUI 主流程。

## 当前证据与限制

- 联合批次净成本已唯一化为 `80.607613` 体力。
- 5 seed、每件5,000轨迹的正式体系联合研究中，B 在 Heroic 基础回退/全停和产出率基线/±20%六个场景均第一，且相对 C 的95%成对区间均大于0。
- 该研究分子明确未计终局速度 `>=22` 的一速价值，因此 B 目前只获得“正式体系模型下候选”的资格。
- 现有真实 Epic 共345件：299件已进入开发/联合研究，46件已用于旧holdout/盲测。没有剩余48件真正独立样本，禁止从旧299件重新抽样冒充验证。
- 全量 `unittest discover` 仍被 `test_gui_app.py` 顶层导入和 Qt 跨测试复用阻断，正式策略任务前必须恢复可靠全量验证。
- 显式联合资源池已完成：主分母固定为 `85 + 一次圣女瓶颈补充体力`，每批含0.425来源石和127,500来源金币；B在Heroic基础回退/全停、产出率基线/±20%、本批/长期资源池下均第一，最窄B-C 95%区间 `[0.000464, 0.001364]`。后续直接复用该账本和共同轨迹，不得回退到80.607613标量分母。
- 当前工作区尚无 `epic_b_balanced_prospective_20260713` 或等价的新批次文件，前瞻样本收集进度为0/48。

## 第一阶段：冻结候选与前瞻数据协议

1. 保存 B 的完整规则快照、代码版本、参数、特征、速度硬路线排除条件和候选预测函数哈希；同时冻结 A、C 作为对照。
2. 新建验收批次 ID，例如 `epic_b_balanced_prospective_20260713`，状态为 `collecting_blind`。
3. 只接收冻结时点之后新导出的真实 `normal_85 Epic +0/+3` 非速度装备；使用实例 ID 优先、规范化指纹兜底，与旧345件及批次内部严格去重。
4. 目标为48件合法非速度装备。速度硬路线、非法输入、重复状态单列排除，不占48件名额。
5. 收集期间可以保存装备输入，但不得显示或填写人工判断，不得用新样本调整 B、速度映射、lambda 或门槛。
6. 每件在入批时锁定 A/B/C 动作预测；DP Oracle 可在新 lambda 研究完成后统一补充，但必须在人工解盲前完成并锁定。

## 第二阶段：终局22速价值研究

均衡档普通装备只用正式体系百里分。终局非鞋速度 `<22` 不产生独立速度价值；终局速度 `>=22` 才允许作为一速价值与正式百里分取高。

基线使用项目现有一速分数曲线：

- 22~24：`5S-105`
- 25~26：`10S-225`
- >=27：`20S-490`

用户权威锚点为“22速至少相当于正式第二档”。因此研究中：

- 一速评级从22开始不得低于正式第二档；
- 数值主场景保留现有一速曲线；
- 另从第6.4节正式分类×部位中提取第二档门槛对应百里分的低位/中位/高位，做22速数值锚点敏感性；25+仍保持曲线单调和高速度优势；
- 若 B 在全部合理锚点下仍第一，则无需在本任务决定唯一数值映射；若排序翻转，停止验证解盲并提交用户选择具体速度锚点，不得自行发布。

该阶段先改离线研究工具和测试，不得直接修改正式 `score_one_speed/tier_one_speed`。

## 第三阶段：新成本下的校准研究

1. 使用联合批次净成本 `80.607613`、Epic/Heroic共同来源记账、正式体系价值和22速敏感性，重新估计研究用 `cost_per_terminal_value` 与候选 lambda。
2. 来源下级石信用只在联合获取端一次；14,400金币仍在石头实际使用端。
3. 合并多seed原始总成本/总价值，不平均seed比值；报告非零终局数、95%区间和前后半稳定性。
4. 新值只写研究报告，不覆盖 `DP_ASSIST_CONFIGS`。若相对已发布 Epic lambda 的变化会改变旧45件 Oracle 动作，逐件列出。
5. 用更新后的研究 lambda 为新48件生成冻结 Oracle 对照，但人工标签不覆盖 Oracle。

## 第四阶段：重跑联合排序

沿用现有5 seed、5,000轨迹和可续跑分片框架，在以下场景重跑或复用共同轨迹重算：

- Heroic 基础回退/全停；
- Heroic产出率基线/±20%；
- 一速价值基线及第二档低位/中位/高位锚点。
- 显式资源池固定开启：85来源体力、0.425来源石、127,500来源金币及一次圣女瓶颈；旧标量口径仅作历史对照。

只有 B 在全部场景第一，且相对第二名的95%成对区间均大于0，才允许把已收集的48件批次状态从 `collecting_blind` 改为 `ready_for_manual_review`。否则保持封存，不读取人工标签。

## 第五阶段：测试基础设施

先复现全量 `unittest discover` 的顶层导入和 Qt 复用问题。对实际测试基础设施偏差补最小回归，限定修改测试导入/隔离方式，不修改 GUI 业务代码。目标是：

- 标准 `python -m unittest discover -s tests` 可稳定执行；或
- 若 Windows Qt 必须进程隔离，提供项目内统一全量测试脚本，逐文件独立进程执行并正确汇总退出码。

不得通过跳过 GUI 测试或吞掉原生崩溃伪造全量通过。

## 最终报告

必须包含：

- A/B/C 冻结规则版本和新批次去重证据；
- 当前收集进度，未满48件时不得伪造完成；
- 22速基线及第二档锚点敏感性结果；
- 新成本下研究 lambda、稳定性和旧Oracle动作变化；
- 全部联合场景的B-第二名成对区间；
- 是否允许对48件解盲；
- 全量测试入口修复和测试结果。

不得接入 OCR、ADB、Airtest 或自动点击。
