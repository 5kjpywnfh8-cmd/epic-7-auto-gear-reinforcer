# 纯视觉真实 ADB 宽套装文字只读校准任务说明

## 目标

验证离线已收口的装备详情 OCR 契约能否在新鲜 MuMu ADB 稳定帧集合上形成完整、可审计的视觉证据。稳定帧沿用现有 `MIN_STABLE_FRAMES=3`，固定采样三帧且不把失败当作重试。重点复验当前正式宽套装文字区域 `(916,540)-(1120,600)`（基准视口 `1280x720`），同时复用等级、品质、部位、主属性、副属性、装备分数和强化状态的既有局部区域。结果只能标记 `mode=visual_only`、`verification=unverified`，不得表述为服务器确认、底层装备状态或强化成功。

## 唯一权威来源

本文件是本轮唯一权威任务来源。同时完整遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md`、`纯视觉 ADB 截图与受控点击迁移任务说明.md`、`纯视觉套装单通道宽文字证据接线任务说明.md` 及当前视觉运行时接口。聊天内容不能替代或扩大本文件范围。

## 已通过的前置闸门

- 离线默认 `set_text` 已固定为归一化 `(0.715625,0.75)-(0.875,0.8333333333)`，对应 `1280x720` 像素 `(916,540)-(1120,600)`。
- 套装文字或受控图标 `speed -> set_speed -> SpeedSet` 任一通道唯一达到 `0.98` 即可提供套装字段；双通道冲突、候选不唯一、未知资产 ID、区域越界或字段矛盾继续 fail-closed。
- 定向公开/合成测试共 `57` 项通过：OCR 15、视觉平台 9、套装 matcher 14、套装裁切比较 5、ADB 识别适配 8、ADB 帧源 6；Python 3.9 语法检查与 `git diff --check` 通过。

## 授权范围

- 用户已明确批准进入下一步 ADB 接线；本轮仅允许真实只读校准。
- 运行 MuMu 官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`，只接受自动发现且状态为 `device` 的唯一设备；不得写死、复用或主动传入任何 `192.168.x.x:5555` 地址。
- 仅执行由自动发现设备派生的固定三帧 `exec-out screencap -p` 稳定采样，按现有 `PlatformFrameSource`、稳定帧、视口、帧哈希、时间戳和局部区域校验接入；这三次是稳定性契约的一部分，不得因失败追加采样。截图和裁切只在内存中处理，不落盘。
- 允许对右侧装备详情局部运行一次本地 PaddleOCR 和已审定的套装文字/图标识别，保留原始置信度、来源和拒绝原因。

## 禁止修改与禁止操作

- 禁止 ADB `input tap`、`swipe`、`keyevent`，禁止页面导航、点击、强化、选材、重启游戏和资源消耗。
- 禁止 Fribbels、PCAP、TCP、`player_data.json`、底层装备读取器、模型下载、联网和任何外部上传。
- 禁止降低或绕过 `0.98` 门槛、稳定帧/视口/区域校验、唯一性和冲突检查；禁止自动重试或人工补齐缺失字段。
- 禁止修改正式策略、DP、评分、资源模型、GUI、Holdout、自动化规则、ADB 帧源或点击执行器。若发现代码探针 wiring 错误，立即停止并记录，不在本轮隐式修复后重试。
- 禁止读取、恢复、暂存或上传私人归档；不得触碰 `refs/heads/main - 副本` 或 `refs/heads/master - 副本`；不得使用 `git add -A`。

## 输入与执行步骤

1. 完整阅读本文件、`AGENTS.md`、`e7-gear-enhance-plan.md` 和前置视觉任务说明；确认当前分支与工作区范围。
2. 确认 MuMu 已停留在目标装备详情页；若页面不明确、不是详情页或目标字段无法核对，停止并请求用户重新打开，不进行导航。
3. 在全新 Python 3.9 进程中确认本地 Paddle/PaddleOCR 模型缓存可导入；依赖或模型不可用立即停止，不执行截图。
4. 执行一次官方 ADB 设备发现并派生固定三帧只读截图采样。按既有帧源完成 PNG 结构/CRC、视口、黑屏、旧帧、时间戳、稳定性和区域边界检查；任一失败立即停止，不追加采样。
5. 在同一内存帧上运行当前详情字段解析和套装单通道汇合，记录每个字段的文本/图标来源、置信度、标准化值、唯一性及拒绝原因。
6. 只有所有必需字段均唯一达到 `0.98` 且目标指纹一致时，才记录 `visual_readonly_calibration=passed`；否则记录 fail-closed，不能进入点击执行器。
7. 仅运行与本轮相关的离线定向测试、语法检查和 `git diff --check`；精确审阅差异，把完成项、验证结果、未完成项、风险和下一步写回本文件与总计划。除任务文档/报告外不得修改源码。

## 产物与验证要求

- 只记录设备发现、帧元数据、局部尺寸、字段结果、置信度、目标指纹比较和停止原因；不保存真实截图、裁切图或游戏数据。
- 明确区分离线合成验证与真实只读校准；真实校准不得写成服务器确认或强化结果。
- 单帧/稳定帧校准通过也不授权 ADB 点击、页面导航、强化、选材或资源操作；点击必须另建任务说明并再次取得明确授权。
- 执行模型记录为 `gpt-5.6-terra + high`；若不可用必须如实记录替代模型。

## 停止条件

依赖、页面、设备、PNG、视口、黑屏、旧帧、稳定帧、区域、模板、OCR、字段、置信度、唯一性或目标指纹任一环节失败立即停止并 fail-closed；不得自动重试、降低门槛或猜测。任何需要输入、导航、点击、强化、选材、资源消耗、联网或外部传输的动作均超出本任务。

## 当前状态

`ready_for_real_adb_readonly_wide_set_calibration_new_session_20260727`

## 未完成项、风险与下一步

- 离线 OCR/套装单通道契约已收口，但尚未证明当前宽套装区域在新鲜 ADB 帧上达到 `0.98`；真实校准是当前唯一剩余识别闸门。
- 真实校准通过后，下一步仍需另建“只读预检后单次受控 ADB 点击”任务说明并取得新授权；本任务不得进入点击或自动强化。
