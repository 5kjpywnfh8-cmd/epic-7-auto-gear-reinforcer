# OCR 字段命名与规范化说明

更新时间：2026-07-11

## 结论

当前项目中的英文名称分为三层，不能混为一谈：

| 层级 | 示例 | 用途 |
| --- | --- | --- |
| 策略内部键 | `atkPct`、`crit`、`set_speed`、`weapon` | 评分、校验、DP 和策略计算。 |
| JSON/兼容别名 | `AttackPercent`、`CriticalHitChancePercent`、`SpeedSet`、`Weapon` | 接收现有样本、GUI 和部分 Fribbels/历史数据格式。 |
| OCR 原始文字 | 可能为“攻击力%”“暴击率”“速度套装”或游戏英文界面文字 | 未来从截图读取，当前尚无真实 OCR 语料验证。 |

现有英文别名是项目与既有 JSON/Fribbels 风格的兼容名称，不能宣称是从真实游戏截图 OCR 得到的权威英文词表。

## 当前已支持

- 副属性支持多种英文别名，例如 `AttackPercent`、`Attack %`、`CritChance`、`CriticalHitChancePercent`、`Effect Resistance`。
- 套装支持内部代码、常见英文名和部分中文名，例如 `SpeedSet`、`Speed`、`速度`。
- 部位支持常见英文与中文，例如 `Weapon`、`helmet`、`武器`、`头盔`。
- 品质支持 `Epic`、`Heroic`、`Rare` 及部分中文别名。
- 所有输入最终会规范化为稳定内部键；策略不依赖 OCR 原文。

## 已知缺口

1. `STAT_TYPE_TO_KEY` 当前基本没有中文副属性别名。OCR 若直接输出“攻击%”“暴击率”“效果命中”等中文文本，不能保证被模型识别。
2. GUI 中文套装名与规则中文别名存在差异：
   - GUI“穿透”，规则别名主要为“贯穿”；
   - GUI“保护”，规则别名主要为“守护”；
   - GUI“先手”，规则别名主要为“开战”；
   - GUI“追击”，规则别名主要为“追加”。
3. GUI 品质显示“史诗”，现有品质别名主要为“传说”；“红装/紫装”等口语名称也不是完整规范化输入。
4. OCR 常见噪声尚未处理：全角/半角 `%`、空格、换行、冒号、数字粘连、简繁体、相似字和低置信度结果。
5. 当前 OCR 按钮仍为禁用占位，项目没有真实截图语料、识别器或字段级置信度。

## 对未来 OCR 的影响

内部英文键不需要改名，也不应为适配 OCR 而修改策略层。正确架构应为：

```text
截图/OCR 原始文字
-> OCR 字段分区与原文保留
-> OCR 专用规范化映射
-> 现有 JSON 标准字段
-> Gear 输入校验
-> 人工确认
-> 评分/轻量预测/精确 DP
```

只要新增独立 OCR 规范化层，内部键不会影响识别。若跳过规范化层、直接把 OCR 字符串传给 `Gear.from_dict`，上述中文名称和噪声会造成未知属性、未知套装或错误分类。

## 未来 OCR 实施要求

1. 先收集真实游戏中文/英文截图和实际导出样本，建立字段词表；不能只凭当前英文代码名推断画面文字。
2. 新增独立的 OCR 映射模块，不在 `rules.py` 或策略代码中混入截图坐标和模糊识别逻辑。
3. OCR 输出同时保存：原文、规范化值、置信度、候选项和截图区域。
4. 套装、部位、主属性、副属性、数值、强化等级、品质分别规范化；“命中”作为套装和副属性时必须依赖字段区域消歧。
5. 低置信度或未知词不得自动猜测，必须进入人工确认。
6. 人工确认后的结构复用现有 `build_gear_dict`、`Gear.from_dict` 和严格合法性校验。
7. OCR 不得直接触发强化、资源消耗或自动点击。

## 最低词表测试

未来实现 OCR 前，至少覆盖：

- 中英文套装名及 GUI/规则差异词；
- 中英文六部位；
- 固定值、百分比、暴击率、暴击伤害、效果命中、效果抗性、速度；
- `Epic/Heroic`、史诗/传说、英雄、红装/紫装等实际画面或用户输入词；
- 半角 `%`、全角 `％`、多余空格和换行；
- 未知词与低置信度必须拒绝自动确认；
- 规范化后装备规则和策略结果与手动输入完全一致。

## 当前行动

OCR 尚未进入实现阶段。下一步应先使用真实导出文件完成样本管理，再在用户提供真实截图语料后建立 OCR 词表和规范化层；无需重命名当前策略内部键。
