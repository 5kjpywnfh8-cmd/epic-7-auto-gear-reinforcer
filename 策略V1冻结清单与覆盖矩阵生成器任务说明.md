# 策略 V1 冻结清单与覆盖矩阵生成器任务说明

## 权威口径

- 本文档是本轮功能开发的唯一任务说明，同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md` 和《强化策略V1收口与GUI延期任务说明.md》。
- 用户已要求在工作区清理完成后恢复功能开发；本轮按总计划未完成的策略 V1 覆盖与冻结产物开始，不扩展到模拟器或自动强化。
- 当前由主 agent 执行；未创建或派发子 agent。

## 目标

实现确定性的只读策略 manifest 生成器，输出机器可读 JSON 与人类可读 Markdown，至少记录：

1. 正式策略版本和默认策略映射。
2. 影响策略行为的源码、配置和权威规则文件 SHA-256。
3. 支持的装备来源、品质、等级、强化节点、部位和套装范围。
4. 各来源/品质/节点及 Heroic 速度救回例外的决策路由所有者。
5. 冻结就绪状态与未满足条件；证据不完整时只能输出 `not_ready`。

## 范围

- 新增独立 `policy_manifest` 模块和命令行入口。
- 新增定向测试，验证输出确定性、哈希变化检测、覆盖矩阵完整性和未知输入拒绝声明。
- 生成本轮 JSON/Markdown 报告并同步本任务说明和总计划。
- 使用现有 Python 标准库和项目常量，不安装依赖。
- 用户于 2026-07-25 明确授权：仅将统一全量测试所需的精确私人夹具从工作区外归档临时复制回原路径；复制前校验逐文件 SHA-256，测试后删除临时副本并复核归档原件与集合 SHA-256 不变。临时夹具不得暂存、提交或上传。

## 禁止修改项

- 不修改正式策略动作、DP、评分、资源模型、资源常量、跳值、转换规则或 `next_check_at`。
- 不修改 GUI、OCR、ADB、Airtest、自动化、Holdout、真实样本或人工操作证据。
- 不把 manifest 自身写成正式发布结论；发现缺口时不得通过放宽验证掩盖。
- 除本任务已授权的精确测试夹具外，不读取、恢复或复制工作区外私人归档；任何私人证据均不得暂存、提交或上传。

## 输入与权威口径

- 策略版本与默认映射：`src/e7_enhance/strategy_defaults.py`。
- 决策入口与路由：`src/e7_enhance/enhance_policy.py`。
- 输入合法性：`src/e7_enhance/models.py`。
- 分类、套装、部位与评分规则：`src/e7_enhance/rules.py`、`src/e7_enhance/score_engine.py`、`装备强化与评分规则审阅.md`。
- DP、资源和轻量校准：`src/e7_enhance/calibration.py`、`src/e7_enhance/resource_model.py`、`src/e7_enhance/lightweight_calibration_rules.json`。
- 当前正式版本固定从代码读取，不在生成器中复制版本字符串。

## 执行步骤

1. 先补失败测试，固定 schema、确定性排序、行为文件集合、支持维度和节点路由覆盖。
2. 实现纯函数生成 manifest，并对缺失文件、重复/缺失覆盖行、未知路由或哈希异常 fail closed。
3. 实现 CLI，原子写入 JSON/Markdown；同一输入重复生成必须保持核心 manifest 哈希一致。
4. 运行定向测试和 CLI smoke，生成报告。
5. 运行全量测试和 `git diff --check`，同步文档后精确提交并推送。

## 产物

- `src/e7_enhance/policy_manifest.py`
- `tools/export_policy_v1_manifest.py`
- `tests/test_policy_manifest.py`
- `reports/policy_v1_manifest_20260725.json`
- `reports/policy_v1_manifest_20260725.md`

## 验证要求

- JSON 必须可解析，schema 固定，所有集合稳定排序。
- 所有行为输入文件必须存在且记录 SHA-256；修改任一输入后核心 manifest 哈希必须变化。
- 覆盖矩阵必须覆盖 `normal_85 Epic/Heroic`、`rift_85 Epic`、`+0/+3/+6/+9/+12/+15`，并明确鞋/非鞋、速度/非速度的 Heroic 路由差异。
- 输出必须区分 `ready` 与 `not_ready`；当前代码无法证明的 V1 条件必须列入缺口。
- 定向测试、CLI smoke、全量测试和 `git diff --check` 必须报告真实结果。

## 停止条件

- 发现必须改变策略行为才能生成报告时停止，不在本任务内修改策略。
- 发现现有常量之间冲突、覆盖矩阵存在零行或多行所有者、行为输入缺失或哈希无法稳定复现时 fail closed。
- 测试失败时不生成发布结论、不提交失败产物。

## 当前状态

`completed`

## 实现记录（2026-07-25）

- 已新增纯只读模块 `src/e7_enhance/policy_manifest.py` 和 CLI `tools/export_policy_v1_manifest.py`；未修改现有策略行为文件。
- manifest 记录 `10` 个行为输入文件 SHA-256、`3` 个来源/品质组合、`21` 条节点路由，以及默认策略、支持等级/部位/套装和自动化边界。
- 核心 manifest SHA-256 为 `565ecd81d9f1354129bbc91ebbd54f291c33249b83371e0be7a5f3316d6c2adb`；生成日期不参与核心哈希。
- 当前冻结状态为 `not_ready`：`unknown_set_not_rejected_by_advise_gear` 与 `full_regression_matrix_evidence_not_attached` 两项未通过。本任务只报告缺口，不修改策略或放宽闸门。
- Python 3.9 定向测试 `7/7` 通过，Python 3.9 语法检查通过，CLI smoke 退出码 `0`，JSON/Markdown 报告已生成并解析通过。

## 全量验证前置阻断（2026-07-25）

- 扩大定向回归时，`tests/test_enhance_policy.py` 的 1 个测试方法因直接读取已归档的私人 `manual_acceptance/real_sample_records.json`，产生 5 个 `FileNotFoundError`；同批其余策略断言未失败。
- 不恢复、不读取工作区外私人归档。允许的最小修复是使用现有测试工厂构造与已记录验收语义等价的 Epic/Heroic、`+0/+3`、鞋/非鞋速度案例，保持原路线与建议断言不变。
- 当前状态：`private_fixture_dependency_identified_hermetic_test_fix_in_progress`。

## 全量测试私人夹具阻断（2026-07-25）

- 将首个策略用例改为自包含后，`tests/test_enhance_policy.py` 已恢复 `43/43` 通过；未读取私人归档。
- 统一全量入口真实退出码为 `1`：新 `test_policy_manifest.py` 通过，但另有 `13` 个既有测试组继续依赖已归档的真实样本库、3 个真实样本文件、1 份 Holdout JSON 或 batch `004` 操作包。失败均表现为输入不存在及其下游断言，不是新模块策略断言失败。
- 当前任务禁止读取工作区外私人归档，不能静默恢复这些输入，也不能通过跳过测试虚构全量通过。
- 推荐的最小安全方案：取得用户明确授权后，只把测试所需的精确文件临时复制到原工作区路径；运行统一全量测试；随后删除临时副本并验证归档原件 SHA-256 不变、Git 未跟踪项恢复为 `0`。全程不暂存、不提交、不上传私人文件。
- 当前状态：`targeted_tests_passed_full_suite_blocked_awaiting_temporary_private_fixture_authorization`。

## 全量测试临时夹具授权（2026-07-25）

- 用户已明确授权临时恢复统一全量测试的精确依赖：`manual_acceptance/real_sample_records.json`、3 个真实样本 JSON、1 个 Holdout JSON、batch `004` 单件操作包，以及 batch `004`/`013` 操作包分别引用的 `preflight_20260723_102914/pair_batch_004.json` 和 `pair_batch_013.json` 配对基线。batch `013` 操作包本身已由当前 HEAD 跟踪，无需从私人归档恢复。
- 执行边界固定为：按归档恢复清单校验来源哈希，目标原路径必须不存在；只复制上述依赖；运行 `Python39 -B tools/run_all_tests.py`；随后删除临时副本，复核归档原件逐文件与集合 SHA-256 不变，并确认这些私人文件未进入暂存区、提交或远程。
- 任一来源缺失、哈希不匹配、目标冲突、测试范围新增私人依赖或清理后残留时立即 fail closed。
- 首轮按 9 文件清单运行统一全量测试，真实退出码为 `1`，耗时 `270.2` 秒；除 `test_single_item_confirmation.py` 外其余测试组全部通过。该组 `2` 个失败和 `2` 个错误均由遗漏配对基线导致，未出现新模块断言失败。
- 首轮 9 个临时副本随后全部删除；归档 `339/339` 个文件、`223321159` 字节与逐文件 SHA-256 重新验证通过，集合 SHA-256 仍为 `1b33e73a3cfdb243bb1cf91bb58d0300ae727c9152a7698463f192fd94f9b76a`，Git 状态中无私人夹具。
- 第二轮先补 `pair_batch_004.json` 并运行单件组定向测试：底层 batch `004` 验证恢复通过，但聚合用例因当前 HEAD 已跟踪的 batch `013` 操作包缺少其 `pair_batch_013.json` 配对基线而 `1/5` 失败；不是代码断言或私人证据内容冲突。
- 加入唯一匹配的 `pair_batch_013.json` 后，单件组定向测试 `5/5` 通过；随后统一全量入口真实退出码为 `0`，耗时 `270.3` 秒，输出 `ALL TEST FILES PASSED`。其中一个 Qt 用例两次以进程退出码 `3221226505` 失败，既有 runner 第三次重试通过。
- 全量测试后已删除全部 `11` 个临时副本；归档 `339/339` 个文件、`223321159` 字节和逐文件 SHA-256 最终复核通过，集合 SHA-256 保持 `1b33e73a3cfdb243bb1cf91bb58d0300ae727c9152a7698463f192fd94f9b76a`。暂存区为空，Git 未跟踪列表只包含本任务的 6 个非私人产物。
- 全量 runner 通过仅完成本实现的回归验证，不改变 manifest 内 `full_regression_matrix_evidence_not_attached` 的机器可读证据闸门；冻结状态继续为 `not_ready`，且 `unknown_set_not_rejected_by_advise_gear` 仍待后续独立策略任务处理。
- 功能、测试、报告和文档已由提交 `335dc6e` 推送至 `origin/codex/policy-v1-manifest-20260725`；提交后的 geometric repack 仅因既有异常引用 `refs/heads/main - 副本` 报错，不影响提交对象或推送，且未按任务边界修改该引用。
- 当前状态：`completed`。

## 后续覆盖说明（2026-07-25）

- 本文记录的 `not_ready` 与两个剩余缺口仅代表提交 `335dc6e` 时的历史状态，已由后续独立任务[策略V1剩余冻结闸门收口任务说明](策略V1剩余冻结闸门收口任务说明.md)覆盖。
- 当前权威 manifest 已达到 `5/5 ready`；本文其余生成器初始实现、历史测试和私人夹具校验记录继续保留作历史对照。
