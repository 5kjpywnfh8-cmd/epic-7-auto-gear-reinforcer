# MuMu 截图入口链路与只读导航模板任务说明

## 目标

在已授权的只读范围内，将 Epic Seven 的真实读取前置导航从单次固定点击扩展为可验证的入口链路：确认大厅截图后，进入右上角四方格控制界面，再进入背包或英雄页面，最后以目标页面模板确认状态并继续既有抓包/Fribbels 读取。失败必须 fail-closed。

## 范围与授权

- 用户已授权：使用提供的大厅、背包、英雄和入口截图做状态模板；允许截图识别和只读页面导航；允许一次标准真实读取及向既有 Fribbels 解码接口上传本账号 TCP 载荷。
- 本任务只修改外部 MuMu 读取器的状态识别、导航步骤、命令参数和公开回归测试，以及本说明和总计划。
- 使用模型：`gpt-5.6-terra + high`。

## 禁止项

- 不强化、不选材、不改变装备、不导入档案、不消耗资源，不进入强化页输入。
- 不做 OCR 字段采集；不修改正式策略、DP、评分、资源模型、GUI、Holdout 或自动化规则。
- 不读取、恢复、暂存或上传私人归档；不写死或复用 `192.168.x.x:5555`；不修改异常引用 `refs/heads/main - 副本`。
- 模板不作为账号数据提交；真实读取失败时不得重试或以历史输出冒充新鲜证据。

## 模板输入

- 大厅模板：`C:\Users\orangine\AppData\Local\Temp\codex-clipboard-954a2acc-841e-4b1c-8ac3-4dfb24d2e448.png`（用户图四）。
- 背包目标模板：`C:\Users\orangine\AppData\Local\Temp\codex-clipboard-5c7345b1-1ba1-4d62-b882-2bcf80b8669d.png`（用户图一）。
- 装备保管箱参考：`C:\Users\orangine\AppData\Local\Temp\codex-clipboard-8a1f087c-7208-4d51-b4d0-4c6d9bbef950.png`（用户图二），仅作页面辨识参考。
- 英雄目标模板：`C:\Users\orangine\AppData\Local\Temp\codex-clipboard-e7825ba9-f4d9-490f-9f5f-50520dcecc08.png`（用户图三/六）。
- 玩家名参考：用户图七显示为 `O橙U子O`，仅作人工核对，不作为 OCR 或身份输入。

## 执行步骤

1. 先审阅现有读取器和公开测试，建立导航链路的离线红灯/绿灯回归。
2. 在读取器中实现：大厅模板确认；一次点击右上角四方格；控制界面入口点击（背包或英雄）；目标模板二次确认；任一步骤截图失败、模板缺失、相似度不足或页面不明均零点击/停止。
3. 保留 MuMu 自动设备发现、旋转坐标换算、既有 PCAP/Fribbels 输出契约。
4. 运行公开回归、Python 3.9 语法检查和 `git diff --check`，精确审阅后提交并推送根目录变更。
5. 若没有控制界面整屏模板，先在大厅模板确认后只点击一次四方格，采集一张临时菜单截图并人工审阅；菜单未确认时停止，不抓包。
6. 仅离线验证和菜单模板审阅通过后，按《MuMu抓包读取与真实装备导入任务说明.md》运行一次标准真实读取；检查同批 `player_data.json`、`reader_result.json`、PCAP、SHA-256、装备数量、实例 ID 和 `raw`。任何 `0 items` 或输出不新鲜都 fail-closed。

## 产物

- 外部读取器最小导航改动及公开测试。
- 本说明、`e7-gear-enhance-plan.md` 同步记录。
- 真实读取成功时才生成新的同批输出和验证报告；失败只保留完整诊断，不产生可执行建议。

## 停止条件

- 无法可靠识别大厅、控制界面或目标页面；需要 OCR/GUI 或超出只读导航范围；或 Fribbels 解码为 0 件、双文件不同批、实例不唯一、`raw` 校验失败时立即停止。

## 当前状态

`visual_entry_chain_authorized_menu_navigation_blocked_fail_closed`

已收到用户提供的界面截图并批准按链路采集。现有读取器只有一次固定导航点击，下一步先完成链路的离线实现与回归，再进行一次真实只读验证。

只读 ADB 截图核对（未发送输入）显示当前页面为用户图八的个人资料弹窗，模板相似度约 `0.9863`；与大厅图四相似度约 `0.6196`，因此当前不满足大厅前置条件，禁止盲点或启动真实抓包。离线链路实现和回归已通过；下一步只允许在确认大厅后采集控制界面整屏临时模板。

一次获准的大厅确认后四方格探针得到大厅相似度 `0.9705`，但点击后仍停留大厅。只读 `wm size` 为物理 `720x1280`，而 `dumpsys input` 活动 Viewport 的 `logicalFrame/physicalFrame` 均为 `1280x720`；已修复为读取活动帧并做归一化映射。修复后当前大厅因频道提示、资源数字和角色动画相似度约 `0.87`，仍明显高于资料弹窗 `0.6196`，整屏阈值已调整为 `0.85`。

在大厅确认后仅尝试了三种有依据的四方格坐标：活动帧直映射 `(1239,35)`、图标中心 `(1248,24)`、轴交换 `(35,1245)`；结果均未进入控制界面，最后一次相似度仍为 `0.8686`。读取器已 fail-closed 停止，未启动抓包、未上传 Fribbels、未进入背包/英雄页面。当前需要用户手动点击四方格并停在控制界面，或提供控制界面整屏截图，才能继续。

## 2026-07-26 用户补充控制界面整屏模板

- 用户提供大厅闭合并标出四方格入口的截图 `C:\Users\orangine\AppData\Local\Temp\codex-clipboard-e0043741-2388-405c-a7a2-4733b090c06d.png`，以及四方格展开后的控制界面整屏截图 `C:\Users\orangine\AppData\Local\Temp\codex-clipboard-6724cdde-7cd3-4432-8123-75cfe1ef4e31.png`；后者对应最初截图序列中的“图四”，前者不是最初序列的装备保管箱“图二”。
- 新截图确认控制面板展开后左下方公文包图标为背包入口。按 908x510 参考图归一化，菜单入口中心暂记为 `(0.735, 0.820)`，仅用于只读导航预检和真实读取参数。
- 用户再次明确批准：只读截图/模板识别；确认大厅后可点击右上角四方格并进入背包页面触发同步。仍禁止强化、选材、装备变更、导入、OCR 字段采集和资源消耗。
- 当前状态更新为 `visual_entry_chain_menu_template_received_ready_for_readonly_validation`。下一步顺序：模板有效性/CLI fail-closed 预检 -> 公开导航回归 -> 精确审阅 -> 一次标准真实读取；任何大厅、菜单或背包模板不匹配均停止。

## 2026-07-26 唯一一次真实读取结果

- 离线回归 `10/10`、模板预检通过，ADB 自动发现 `emulator-5554`，读取前大厅相似度 `0.9754`；按本说明执行一次 240 秒标准读取，未传固定设备地址。
- 批次 `20260726_115345` 的 PCAP 已落盘：`mumu_capture.pcap` 为 `18,823,101` 字节；读取器诊断为 `packet_count=7433`、`tcp_payload_bytes=18,283,414`、`udp_payload_bytes=18,181`、`tcp_payload_groups=534`，观测端口含 `443/9997/51390/5138/80/442/8081/53`。
- Fribbels 解码请求返回 HTTP `413 Request Entity Too Large`，未生成本批 `fribbels_raw_response.json`、`reader_result.json`、snapshot 或新的 `current/player_data.json`；当前 `current` 仍为 2026-07-22 历史文件，不得作为本任务新鲜证据。
- 本次失败分类为接口请求体上限，不是 `0 items`，也没有证据证明导航目标页未进入；已停止重试、配对、导入和任何游戏内操作。若要继续，必须另建任务先设计并验证受接口上限约束的载荷裁剪/分批方案，不能在本任务中盲目重传。
- 当前状态更新为 `real_validation_failed_fribbels_413_oversized_payload_20260726_115345`。
