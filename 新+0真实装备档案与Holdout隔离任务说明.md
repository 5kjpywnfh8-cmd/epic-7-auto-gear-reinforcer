# 新 +0 真实装备档案与 Holdout 隔离

## 目的

在不污染当前独立 Holdout 的前提下，长期保存新的真实 `normal_85 Epic +0` 装备，供后续套装分布、速度路线和体系样本研究使用。

## 规则

- 只接受冻结时间之后的可信完整 Fribbels 导出或 `fribbels_mumu_reader` 快照。
- 只保存 `normal_85 Epic +0`；保留全部部位、速度硬路线和非四副属性装备。
- 每件保留原始装备、实例 ID、规范化指纹、来源文件哈希、导出时间和范围标记。
- 使用实例 ID与规范化指纹双重去重；不记录人工判断、策略动作、Oracle、百里分或效率。
- 当前 Holdout 仍只读取自己的 `collection` 文件；真实档案不会自动回填 Holdout，也不会改变候选哈希。

## 当前导入

默认档案为 `samples/epic_plus0_real_archive_20260719.json`。现有 2026-07-18 可信快照可以作为首个档案来源；其中鞋子、初速速度路线等此前被 Holdout 排除的装备会在档案中保留并标记。

首个导入已完成：保留 22 件新 `normal_85 Epic +0`，其中 6 件同时满足当前 Holdout、5 件为鞋子、11 件为速度硬路线。它们来自 `set_chase`、`set_opener` 和 `set_coop`；全力和弱化本次仍没有符合范围的新样本。

2026-07-19 03:10:33 的新 PCAP 已由现有 `fribbels_mumu_reader` 生成可信快照并导入。它与 2026-07-18 快照的装备 ID 集合均为 2565 件，新增 0、删除 0、装备内容变化 0；只有 `imported_at` 改变，游戏数据 `export_time` 仍为 2026-06-14 16:03:50。因此本次档案仍为 22 件、Holdout 仍为 6/128，不应把这次抓包计为新装备批次。

2026-07-19 22:10:33 的 `gear_fribbels_20260719_221033.json` 包含 2653 件装备，已导入。档案本次新增 355 件，累计 377 件；Holdout 本次新增 122 件，累计达到 128/128 并生成 64/64 manifest。Holdout 现在只进入 `ready_for_frozen_validation`，在候选哈希冻结和验证流程完成前，不读取标签、Oracle 或效率指标。

## 后续使用

档案达到足够规模后，可按新任务说明重新分层研究。任何正式候选研究必须重新冻结样本范围、规则哈希和排除条件，不得把档案数据直接当作 Holdout 验证结果。
