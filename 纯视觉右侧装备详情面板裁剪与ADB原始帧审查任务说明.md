# 纯视觉右侧装备详情面板裁剪与 ADB 原始帧审查任务说明

## 目标

修正纯视觉装备详情识别对页面布局的假设：目标装备字段位于背包页面右侧详情面板，不在左侧背包网格。开发阶段必须审查 ADB 导出的原始 PNG 帧（优先内存解析，离线 fake/公开帧可落盘）以校准局部区域、锚点和字段映射，并保持 `0.98` 字段置信度门槛与 fail-closed 行为。

## 唯一权威来源

本文件是本轮唯一权威任务来源。ADB 帧源、设备发现和禁止输入继承《纯视觉 ADB 截图与受控点击迁移任务说明.md》；局部裁剪、模板锚点和 PaddleOCR 解析继承《纯视觉 ADB 局部模板与 PaddleOCR 只读识别任务说明.md》；目标页面参考为用户于 2026-07-26 提供的背包详情截图。

## 范围

- 审查现有 `ocr_paddle.py`、局部区域定义、识别测试和 ADB PNG 内存适配，确认左侧网格与右侧详情面板的坐标关系。
- 新增或调整右侧详情面板的局部区域/锚点定义，复用现有字段解析、`0.98` 门槛、帧哈希、视口和 fail-closed 约束。
- 补充 fake/公开 PNG 原始帧审查测试：裁剪边界、锚点缺失、区域越界、旋转/视口漂移、字段缺失和低置信度均应停止。
- 将真实 ADB 原始 PNG 仅作为内存输入审查；不得把私人真实截图写入仓库或持久化目录。需要人工查看时使用用户提供的截图或脱敏/合成 fixture。

## 禁止修改与禁止操作

- 不修改正式策略、DP、评分、资源模型、GUI、OCR `0.98` 门槛、Holdout 或自动化规则。
- 不使用 Fribbels、PCAP、TCP、`player_data.json`、底层装备读取器或任何外部上传。
- 不执行 ADB `input tap`、`swipe`、`keyevent`，不导航、强化、选材、重启游戏或消耗资源。
- 不保存私人 ADB 截图，不读取或恢复私人归档，不触碰 `refs/heads/main - 副本`，不使用 `git add -A`。
- 本轮只做离线实现和验证；真实 ADB 复验如需再次截图，必须在实现完成后另行记录授权并执行一次。

## 输入与权威口径

- 用户目标指纹：命中套武器、红装/传说、85级、`+0`、主属性攻击力 `100`、副属性暴击率 `5%`/攻击力 `8%`/效果抗性 `8%`、官方分数 `28`。
- 用户截图仅用于布局和字段位置参考，不是服务器确认；运行结果继续标记 `mode=visual_only`、`verification=unverified`。
- 真实 ADB PNG 必须由自动发现的唯一 `device` 通过 `exec-out screencap -p` 获取，并在内存中校验 PNG、视口、非黑屏和帧哈希。

## 执行步骤

1. 审阅现有代码和公开测试，固定右侧详情面板的候选归一化区域及锚点证据，不降低既有门槛。
2. 使用 fake/公开 PNG 在内存中运行原始帧审查和局部 OCR，验证右侧详情字段能进入现有解析器；不把人工截图硬编码为正式结果。
3. 实现最小区域/锚点调整并补定向测试；任何区域、锚点、PNG、字段或置信度失败都 fail-closed，禁止自动重试。
4. 精确审阅 diff，运行定向测试、Python 语法检查和 `git diff --check`。
5. 实现验证通过后，若需要真实确认，再更新任务记录并取得一次新的只读 ADB 截图授权；真实帧仍只在内存处理。

## 产物

- 本任务说明、`e7-gear-enhance-plan.md` 同步记录。
- 右侧详情局部区域/锚点实现及公开测试；原始帧审查结果和失败原因记录。
- 不产生私人截图文件、游戏数据载荷或点击执行结果。

## 停止条件

依赖、PNG、视口、区域、锚点、字段、置信度或测试任一环节失败立即停止；不得自动重试或越过实现验证进入点击/强化。

## 当前状态

`offline_right_detail_crop_candidate_verified_pending_real_raw_frame_calibration_20260726`

执行模型：`gpt-5.6-terra + high`。

## 离线实现与验证记录（2026-07-26）

- `ocr_paddle.py` 已将原左侧背包网格候选区替换为右侧详情候选区，并保留 `BACKPACK_ENHANCE_PANEL`/`BACKPACK_SET_NAME` 兼容别名；`ocr_regions.py` 增加详情头部和分数锚点，仍不提供点击坐标。
- `1280x720` 候选详情裁剪为 `(768,58)-(1242,619)`，套装候选带为 `(768,547)-(1242,619)`；`1920x1080` 按归一化比例缩放，竖屏或明显非 16:9 视口 fail-closed。坐标来自用户布局截图推导，尚未由真实 ADB 原始帧校准。
- 新增公开 fake PNG 的内存裁剪/锚点越界审查测试；未保存私人截图。定向 `test_ocr_paddle.py` `10/10`、`test_visual_adb_recognition.py` `7/7`、全部 `test_visual*.py` `52/52`、全部 `test_ocr*.py` `25/25` 通过；`py_compile` 和 `git diff --check` 通过。
- 本阶段未执行真实 ADB、PaddleOCR、点击、导航、强化、选材、资源消耗或上传。下一步需另行取得一次只读授权，用真实 ADB PNG 在内存中审查原始帧并校准候选区域；在此之前不进入受控点击或自动强化。
