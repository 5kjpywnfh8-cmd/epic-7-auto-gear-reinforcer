# 多节点新鲜快照与重新配对契约任务说明

## 目标

为单件人工确认强化结果验证建立多节点状态契约：装备在任一强化节点完成后，必须使用该节点的新鲜权威状态重新配对、重新核验装备身份和页面字段，才能进入下一节点判断。

本任务先实现离线契约、验证器和回归证据，不连接或控制 GUI、OCR、ADB、MuMu，也不执行强化、选材或资源消耗。

## 范围

- 覆盖标准节点 `0/3/6/9/12/15`，并拒绝非标准强化等级。
- 定义每个节点的权威快照包：同一批次的 `player_data.json`、同批 `reader_result.json`、各自 SHA-256、采集标识和页面/装备字段。
- 校验节点之间的快照新鲜性，拒绝把前一节点的可信快照、配对结果或建议哈希直接复用于后一节点。
- 校验同批双文件严格唯一配对、装备实例唯一性、页面状态、强化等级、目标装备身份和必需字段置信度。
- 为通过与 fail-closed 结果提供稳定的机器可读记录和可读验证报告。
- 与现有单件汇总、背包配对和 MuMu 数据结构保持兼容；若接口需要扩展，必须保持旧只读报告可复跑。

## 契约要求

每个节点记录至少应绑定以下信息：

- `operation_id`、前一节点和当前节点、预期下一节点；
- 当前节点唯一快照/采集标识、采集时间和来源状态；
- `player_data.json` 与 `reader_result.json` 的路径标识、同批标识和 SHA-256；
- 页面类型及页面明确性；
- 装备实例 ID、品质、等级、套装、部位、主属性、副属性和强化等级；
- 所有必需 OCR 字段的置信度，以及当前策略/建议哈希（若该节点生成建议）；
- 与前一节点的身份连续性、节点顺序和验证结果。

新鲜性定义必须可验证，不得只依赖口头说明：

1. 当前节点必须有独立的采集标识和同批双文件，不能只引用上一节点的路径或哈希。
2. 当前节点不得复用上一节点的已信任配对包或建议哈希；旧哈希、旧批次、旧节点状态直接 fail closed。
3. 预期发生强化后，当前装备实例必须仍唯一存在，强化等级必须等于预期节点；身份改变、等级不符或状态无法证明时停止。
4. 若来源只提供旧快照、缺少采集版本/时间、双文件不同批、哈希不匹配或无法证明采集顺序，必须 fail closed，不能用截图或人工指定实例 ID 补救。

## Fail-closed 规则

以下任一情况都必须返回结构化 `fail_closed`，不得生成可执行的继续建议：

- 零候选、多候选或装备实例 ID 不唯一；
- 页面不明确、页面类型错误或当前页面不是目标装备详情/强化状态页面；
- 任一必需字段置信度低于 `0.98`、字段缺失或字段互相矛盾；
- `player_data.json` 与 `reader_result.json` 非同批、哈希不一致、schema 不支持或 `raw` 校验失败；
- 旧节点快照、旧配对结果、旧建议哈希或旧资源余额被当作当前节点证据；
- 当前节点不是 `0/3/6/9/12/15`，节点顺序跳跃，或 `+15` 后仍要求继续；
- 当前实例与已授权目标身份不一致；
- 当前结果未知、点击后状态不确定或缺少可验证的操作结果。

`fail_closed` 只能停止流程并记录原因，不能自动重试点击、自动选材、自动改变目标或降级为模糊匹配。

## 禁止修改项

- 不修改正式策略、DP、评分、资源模型、策略 manifest、Holdout、GUI、OCR 识别门槛或自动化规则。
- 不改变当前 OCR 必需字段置信度 `0.98`、零/多候选和哈希不一致的 fail-closed 语义。
- 不实现强化点击、材料选择、资源扣减、持久化执行状态机或无人值守恢复；这些属于后续独立任务。
- 不操作模拟器，不发送 ADB 输入，不运行真实装备采集，不读取或恢复私人归档。
- 不写死、记录、复用或输出任何 `192.168.x.x:5555` 地址。
- 不修改与本任务无关的工作区文件，不安装新依赖，不联网获取规则。

## 输入与权威口径

- 根目录 `AGENTS.md` 和 `e7-gear-enhance-plan.md`。
- [策略与自动强化执行边界报告修订稿](reports/策略与自动强化执行边界报告_修订稿_20260725.md)。
- [自动预算与硬上限离线规划器任务说明](自动预算与硬上限离线规划器任务说明.md)及其已发布的离线规划结果；本任务只消费其节点和边界语义，不改写预算规则。
- `src/e7_enhance/ocr_backpack_pair.py`、`src/e7_enhance/mumu_player_data.py`、`src/e7_enhance/single_item_confirmation.py` 及其公开测试，用于核对现有配对、快照和报告接口。
- 可信快照 `20260722_132045` 仅可作为历史只读配对证据；它不能充当强化后任何新节点的当前权威状态。

## 模型与执行分工

- 代码实现和代码测试使用用户指定的 `gpt-5.6-terra + high`；实际使用模型和 reasoning effort 必须写入本任务、报告和总计划。
- 主 agent 负责审阅接口、代码、测试和报告，并在完成后同步任务状态与总计划。
- 本任务说明是本阶段唯一权威来源；聊天内容不能扩展任务范围。

## 执行步骤

1. 只读审计现有配对器、MuMu 数据适配器、单件汇总器及测试，确定最小兼容接口和当前证据缺口。
2. 先设计节点快照包和验证结果 schema，明确新鲜性、顺序、身份连续性和哈希绑定规则。
3. 先补零/多候选、旧快照复用、跨批配对、哈希不一致、低置信度、节点跳跃、实例漂移和未知结果的失败测试。
4. 使用现有项目抽象实现纯离线验证入口；不得在本任务中接入 GUI/OCR/ADB/MuMu 或实际操作状态机。
5. 生成不含私人证据的 JSON/Markdown 验证报告，报告区分通过契约、fail-closed 和未覆盖输入。
6. 运行定向测试、Python 3.9 语法检查、必要的公开 smoke 和 `git diff --check`。
7. 主 agent 只读审阅全部改动和证据；通过后更新本任务与总计划，再提交并推送。

## 产物

- 多节点快照/重新配对离线验证模块或现有模块的最小兼容扩展。
- 对应的定向失败测试、成功测试和结构化契约回归矩阵。
- `reports/` 下不含私人证据的 JSON 与 Markdown 验证报告。
- 更新后的本任务说明和 `e7-gear-enhance-plan.md`。

## 验证要求

- 成功路径至少覆盖 `0 -> 3 -> 6 -> 9 -> 12 -> 15` 的节点顺序和同一实例连续性。
- 失败路径必须覆盖零候选、多候选、页面不明确、低于 `0.98`、跨批、哈希不一致、旧快照复用、旧建议哈希复用、节点跳跃、实例漂移和未知结果。
- 测试必须断言：任何 fail-closed 输入都不生成可执行继续建议，不触发点击/选材接口，也不改变正式策略输入。
- 同一规范化输入重复运行必须产生稳定 JSON；错误输出不得泄露私人路径、设备地址或原始敏感载荷。
- Python 3.9 兼容、定向测试退出码 `0`、语法检查通过、报告生成通过、`git diff --check` 通过。
- 不使用私人归档夹具；若公开夹具不足以证明契约，保持 `not_ready` 并记录缺口。

## 停止条件

- 需要真实 MuMu、ADB、截图、OCR 采集或用户手动操作才能继续时，停止并等待新的明确授权。
- 无法从已跟踪输入证明快照新鲜性、同批关系、唯一身份或节点顺序时，保持 fail closed，不猜测。
- 需要修改正式策略、OCR 门槛、GUI、预算语义或自动化行为时，停止并报告范围冲突。
- 任何测试显示旧证据可以被误当作新节点，或失败输入可以生成继续建议时，状态保持 `not_ready`，不得提交为完成。

## 当前状态

`fresh_snapshot_contract_implemented_validated_commit_push_pending`

- 用户已明确授权开始代码实现；实际代码与测试由 `gpt-5.6-terra + high` 子 agent 执行。
- 新增 `src/e7_enhance/fresh_snapshot_contract.py`，实现纯离线 `0/3/6/9/12/15` 节点契约、同批双 JSON 规范化内容 SHA-256 绑定、采集顺序、页面和 `0.98` OCR 闸门、唯一重配对、raw 强化等级、实例连续性、旧快照/旧建议哈希失效和结构化 fail closed。任何非 `+0` 起始链均因无法证明前置连续性而 fail closed。
- `src/e7_enhance/ocr_backpack_pair.py` 仅最小扩展 `current_item_main_value` 匹配模式，用于 `+3` 至 `+15` 当前主属性重配对；旧 `+0` 只读配对接口保持兼容。
- 新增 `tests/test_fresh_snapshot_contract.py` 和公开 `not_ready` 报告 `reports/fresh_snapshot_contract_validation_20260725.json/.md`。报告不输出私人路径、设备地址、双 JSON 载荷或原始证据。
- Python 3.9 公开定向验证为新契约 `9/9`、配对器 `6/6`、MuMu 数据适配器 `7/7`，语法检查和 `git diff --check` 通过。旧 `test_single_item_confirmation.py` 依赖工作区未提供的固定私人归档样本，按边界不读取、不恢复、不作为本轮失败。
- 本轮未修改正式策略、DP、评分、资源模型、GUI、OCR 门槛、Holdout 或自动化规则；未连接 GUI/OCR/ADB/MuMu，未强化、选材或消耗资源。当前等待精确提交和推送。
