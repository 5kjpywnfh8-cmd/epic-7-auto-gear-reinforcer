# 纯视觉真实装备列表页视觉字段三帧只读校准第六次任务说明

## 目标

在取得本任务定义的一次性明确授权后，对当前 MuMu 装备列表页执行一次纯只读三帧视觉字段校准。稳定性以三帧经同一生产 OCR/模板链路解析后的视觉字段签名一致为准，PNG 字节哈希仅作审计元数据。

## 范围

- 仅运行官方 MuMu `adb.exe devices -l`，接受唯一状态为 `device` 的设备。
- 仅从该设备读取固定三帧 `exec-out screencap -p`，PNG 全程保留在进程内存。
- 仅使用固定 `1280x720` 列表区域、显式生产 OCR/模板读取器、`RegisteredListPageLocalRecognizer` 和 `>=0.98` 置信度门槛。
- 仅校验页面/视口/来源/时间单调、视觉字段签名一致、目标唯一和字段完整；输出通过或 fail-closed。

## 禁止操作

- 禁止 `input`、`tap`、`swipe`、`keyevent`、窗口输入、点击、导航、强化、选材、确认和任何资源消耗。
- 禁止截图落盘、自动重试、追加采样、第二批采样、动态区域搜索、底层数据读取、联网、上传和模型下载。
- 禁止把用户描述、历史截图、离线 fixture 或人工字段注入写成当前真实页面证据。
- 校准通过也不授权选择装备、进入详情页或进入强化界面；这些动作必须另建任务说明并重新取得逐次授权和新鲜后验。

## 输入与权威口径

- 本文件为本轮真实校准的唯一权威来源，同时遵守根目录 `AGENTS.md`、`e7-gear-enhance-plan.md` 和列表页视觉字段稳定性契约变更任务说明。
- 目标指纹：武器、红装/传说、85 级、强化 `+3`、生命值套装、主属性攻击力；副属性攻击力 `13%`、暴击率 `3%`、速度 `2`、效果抗性 `8%`；装备分数 `37`。
- 用户已确认当前在装备列表页，目标详情页存在但未打开，无相似装备且无需筛选、滚动或切页；这些是期望输入，不替代真实视觉证据。
- 生产接线入口为 `build_production_list_page_pipeline`；读取器缺失、异常、字段不完整或任何门槛失败均必须 fail-closed。

## 执行步骤

1. 在得到一次性明确授权后，仅执行官方 `C:\Program Files\Netease\MuMu Player 12\shell\adb.exe devices -l`，确认唯一 `device`。
2. 设备唯一后，仅读取三帧内存 PNG，并校验 PNG、非黑帧和固定 `1280x720` 视口。
3. 对三帧分别运行固定区域 OCR/模板读取器和列表识别器，比较页面锚点、边界、候选视觉指纹、字段值规范化签名和目标唯一性；PNG 哈希只记录，不作为稳定性判定。
4. 任一来源、视口、时间、字段、唯一性或 `>=0.98` 门槛失败，立即 fail-closed，不重试、不追加采样。
5. 记录设备发现、三帧元数据、字段签名、结果/拒绝原因及未执行的禁止操作，并同步本任务说明、总计划和报告。

## 产物

- 一次性三帧只读校准报告，或明确的 fail-closed 报告。
- 本任务说明和 `e7-gear-enhance-plan.md` 的执行记录、状态、剩余风险和下一步。

## 验证要求

- 设备必须唯一且状态为 `device`；不得写死或主动传入模拟器地址。
- 三帧必须满足固定来源、视口、时间单调、视觉字段签名一致、字段完整、目标唯一和所有置信度 `>=0.98`。
- 报告必须明确未发送输入、未点击、未导航、未强化、未选材、未确认、未消耗资源，且未落盘截图、上传或重试。

## 停止条件

- 设备不唯一、PNG/视口无效、读取器缺失或异常、页面不匹配、视觉字段不一致、目标不唯一、字段缺失/冲突、置信度低于 `0.98` 或任何异常时立即停止并 fail-closed。
- 任何需要输入、点击、导航、联网、落盘或额外采样的要求立即停止并记录阻断。

## 当前状态

`real_list_readonly_calibration_fail_closed_missing_production_reader_20260801`

## 模型记录

用户偏好 `gpt-5.6-luna + max`；当前协作接口不可用 Luna，本任务实际执行模型记录为 `gpt-5.6-terra + high`。

## 建立记录（2026-08-01）

- 用户批准进入下一步；该批准用于建立本任务和同步计划，尚未执行设备发现、截图或任何真实设备操作。
- 此前第五次真实只读授权因生产读取器缺失在 ADB 前置闸门结束，旧授权不可复用。
- 执行本文件第 1 至 4 步前，必须由用户明确确认“一次性只读校准”范围；该确认不包含点击或导航。

## 执行记录（2026-08-01）

- 用户已明确确认一次性只读校准。
- 官方 MuMu `adb.exe devices -l` 退出码为 `0`，发现唯一 `emulator-5554`，状态为 `device`。
- 恰好读取三帧内存 `exec-out screencap -p`；三帧均通过 PNG、非黑帧和 `1280x720` 视口校验，时间戳单调，未落盘。三帧哈希相同，但哈希仅作审计。
- `build_production_list_page_pipeline` 因缺少显式生产 OCR/模板读取器工厂返回 `ProductionListPageReaderUnavailable`；按契约在 OCR/模板识别前 fail-closed。
- 未执行局部 OCR/模板推理、输入、点击、导航、强化、选材、确认、资源消耗、重试、追加采样、联网、上传或模型下载。详见[第六次校准报告](reports/visual_list_visual_fields_calibration_sixth_20260801.md)。

## 下一步

另建真实生产 OCR/模板引擎实例接线任务并重新取得一次性只读授权；不得复用本次授权。校准通过仍不授权点击或导航。
