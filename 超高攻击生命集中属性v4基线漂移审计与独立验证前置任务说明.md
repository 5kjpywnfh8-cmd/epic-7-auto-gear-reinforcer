# 超高攻击/生命集中属性 v4 基线漂移审计与独立验证前置任务说明

## 状态

`completed_baseline_bridge_no_independent_validation_20260721`

本文件是本轮唯一权威任务来源。v4 有效词条绑定已通过实现和回归检查，正式价值差为正；但 v2 的 `current_formal=84.4864` 与 v3/v4 的 `current_formal=116.3588` 不一致，必须先完成基线桥接，暂不设计或收集独立验证。

## 目标

解释并验证 v2/v3/v4 基线差异，确认 v4 的 `current_formal` 和候选增量使用同一套 Epic +3 分支、官方主路径、Heroic 路径和资源池口径。基线桥接通过后，才允许建立独立验证任务；不修改正式策略。

## 固定事实

- v4 正式成对价值差为 `[0.000031,0.001715]/100体力`，只能作为待桥接的离线信号。
- v4 schema 4、1,185 个分片、5 seed、157 Epic、80 Heroic、每件10条主路径和有效词条绑定保持冻结。
- v2/v3 分片和结论不可复用为验证依据；它们只用于定位基线差异。

## 必须审计

1. 逐 seed 对比 v2/v3/v4 `current_formal` 的 flow、资源池、Epic/Heroic 分解、+3 分支数量/概率和路径数量。
2. 检查 v2/v3/v4 的 Epic 分支循环是否完整覆盖所有官方 +3 分支，不能只使用最后一个分支；核对每个分支概率和为1。
3. 检查 `current_formal` 是否完全不读取生命候选兼容性、阈值、有效键或候选动作；基线必须在同输入下稳定。若实现修复改变基线，必须记录原因并用独立重放确认。
4. 对同一固定 seed/gear 运行基线-only、候选阈值不可能救回和 v4 三种模式，确认基线 flow 一致、候选差值只来自救回动作。
5. 逐 seed 重新核对裂缝体力加圣女体力、资源字段、Epic/Heroic 权重和每10万总体力换算。
6. 把已确认的旧 bug、修复前后代码哈希和允许比较的结果写入桥接 JSON/报告；未解释的漂移不得进入独立验证。

## 独立验证前置闸门

- 基线差异有可复现的代码原因，并由独立重放或冻结历史证据确认；
- v4 基线和候选增量在同一来源/同一资源池下可重算；
- 五 seed 成对区间、每10万产量和资源守恒重新通过；
- v4 回归及基线分支概率、同池配对、resume 哈希测试全部通过；
- 只有以上全部通过，才新建独立中期真实样本验证任务。

## 禁止项

- 不修改正式策略、DP、评分、资源模型、正式跳值、GUI、OCR、自动化或 Holdout。
- 不发布集中属性救回，不建立或收集独立验证集，不进入第三项策略覆盖审计。
- 不把 v4 正向区间直接写入正式配置或 lambda。

## 产物

- 基线桥接审计器、测试、JSON 和人类报告；
- 更新 v4 任务说明、V1 收口说明和 `e7-gear-enhance-plan.md`；
- 明确记录是否允许进入下一条独立验证任务。

## 2026-07-21 执行结果

- 审计器：[audit_epic_concentration_baseline_drift.py](tools/audit_epic_concentration_baseline_drift.py)；测试：[test_audit_epic_concentration_baseline_drift.py](tests/test_audit_epic_concentration_baseline_drift.py)。
- v2/v3/v4 的 Epic `current_formal` 加权路径质量分别为 `40.245236/785`、`785/785`、`785/785`；Heroic 三版均为 `400/400`。当前 Epic 完整枚举覆盖 `157` 件、`4496` 个官方 `+3` 分支，每件概率和均为 `1`。
- 逐 seed 重放确认：v2 `84.4864` 是 Epic 只保留一个 `+3` 分支造成的欠权重历史值；v3/v4 `116.3588` 是完整分支循环后的共同基线。策略哈希和资源模型哈希在三版相同，漂移不是 Heroic 权重、资源模型或生命候选造成的。
- 固定 Epic `gear_index=51`、Heroic `gear_index=14`、`seed=20260715` 三模式重放中，baseline-only 与 `1e9` 不可救回候选的 flow/动作序列完全相同；v4 仅在救回动作改变时产生 flow 差异。资源检查逐 seed 均满足裂缝体力加圣女体力等于 `100000`。
- 修复前后估计器哈希、官方分支模块哈希和审计器哈希已写入 JSON/报告；v2 结果及其候选差值只保留为失效历史对照，v3/v4 `current_formal` 允许作为后续同口径离线基线。

## 闸门结论

- 基线漂移已由旧 Epic `+3` 单分支欠权重 bug 可复现解释，并通过固定 gear/seed 独立重放确认。
- 本轮禁止项均未触发：未修改正式策略、DP、评分、资源模型、跳值、GUI、OCR、自动化或 Holdout；未建立独立验证；未进入第三项策略覆盖审计。
- 允许后续另建独立验证任务，但不能把 v4 正向区间直接发布或写入正式配置；下一任务必须先创建新的独立 `*任务说明.md` 并同步总计划。
- 验证记录：审计定向测试 `3` 项通过；集中属性 v4 回归与本审计合计 `18` 项通过；`tools/run_all_tests.py` 真实退出码 `0` 且输出 `ALL TEST FILES PASSED`；语法检查、`git diff --check` 和五 seed 资源守恒检查通过。
