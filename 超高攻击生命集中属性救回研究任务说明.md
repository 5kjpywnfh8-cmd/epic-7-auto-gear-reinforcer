# 超高攻击/生命集中属性救回研究任务说明

## 状态

`superseded_by_probability_precision_closure_20260720`

本任务的 p4 离线研究已完成，安全结论为“不发布”。主审计确认其概率分辨率不足以正式淘汰 `P>=10%` 均衡档；当前最先执行项已切换为《超高攻击生命集中属性救回概率精度收口任务说明.md》，本文件仅作历史研究记录。

## 结论

当前正式评分已经把攻击%、固定攻击、生命%和固定生命按官方 GS 权重计入对应体系；纯输出和纯坦也有独立分类。因此普通高攻击、高生命装备已经受到保护。

当前缺口是“单方向极端集中价值”：装备的攻击或生命副属性非常集中，但其他有效词条不足，使总有效 GS、分类门槛或终局达标概率偏低时，正式策略不会给予额外救回。这类装备可能适用于非暴击攻击倍率、烧伤/炸弹、生命倍率输出、治疗和护盾角色。

## 研究边界

- 先做离线研究，不修改正式策略、DP、评分、资源模型、GUI 或已完成的 Holdout 产物。
- 只计算副属性，不把主属性、套装加成或英雄面板加入集中值。
- 攻击集中 GS 为 `atkPct + atkFlat` 的官方 GS 贡献；生命集中 GS 为 `hpPct + hpFlat` 的官方 GS 贡献。
- 固定值继续使用正式官方权重：攻击 `3.46/39`，生命 `3.09/174`，不得直接与百分比裸值相加。
- 攻击路线必须与现有输出/必爆输出体系兼容；生命路线必须与现有坦克或生命半肉体系兼容。套装用途只做合法性过滤，不额外增加价值。

## 候选动作

基线动作保持不变。候选仅在基线准备停止时救回：

- `concentration_balanced`：终局重铸后达到超高攻击或生命阈值的目标概率 `>=10%`；p4 实现使用随机采样，未达到精确概率口径。
- `concentration_aggressive`：终局可达概率 `>0`，仅作激进敏感性。
- 分别研究攻击、生命和二者 OR，不允许直接把 OR 候选作为发布结论。

第一阶段只研究 `+6/+9/+12`，因为此时已经能观察到真实命中集中趋势。只有中期节点稳定提高总体效用后，才扩展到 `+0/+3` 的终局概率救回。

## 阈值来源

不得直接猜一个百分比门槛发布。先从用户已重铸库存计算攻击集中 GS、生命集中 GS 的 P90/P95/P97.5 分位数，作为阈值网格；库存分位只表示实际保留偏好，必须同时用官方自然跳值分布做敏感性。

## 必须报告

- 每 100,000 总体力新增超高攻击件数、超高生命件数及二者去重总数。
- 每获得一件集中属性装备所需总体力。
- 正式百里分变化、75+变化、22速变化和强化资源变化。
- 各节点救回数、首次救回节点、误救回成本及成对 95% 区间。
- Epic/Heroic 分品质结果和裂缝/圣女体力守恒。
- 当前正式基线固定为已经发布 `output_8_13_tank_10_17` 的 `baili-formal-dp-v1-epic-balanced`。旧发布前策略只作历史对照，不再把 `output_8_13_tank_10_17` 写成离线候选。
- 不读取或反向利用已经完成的 Holdout frozen_validation 结果调节集中属性阈值。

## 发布闸门

- 至少 5 个独立 seed 均有非零贡献。
- 成对总体价值差 95% 区间下界大于 0。
- 正式百里分损失必须明确，并由用户选择均衡或激进风险档。
- 新增集中属性产量不能只来自单个套装、部位或单一高权重事件。
- 未通过闸门时只保留 debug 提示，不发布自动救回。

## 本轮执行记录（2026-07-20）

- 新增离线研究器：[tools/research_epic_concentration_rescue.py](tools/research_epic_concentration_rescue.py)；新增定向测试：[tests/test_research_epic_concentration_rescue.py](tests/test_research_epic_concentration_rescue.py)。
- 研究器只读取开发分区 `normal_85 Epic` 157 件与联合研究 Heroic 80 件；明确不读取 Holdout frozen_validation。
- 外部重铸库存冻结 SHA-256 为 `2BB32F384FBE3D926F5274D50AB24DC8766C2AF87AA1ABC0B6946A47EEAE1DB7`，阈值由 Epic/Heroic level=90、+15 库存的 P90/P95/P97.5 插值计算，不手工指定。
- 官方离散跳值用于后续路径；Epic `+0->+3` 使用精确官方分支，Heroic `+12` 只补第四条副属性；候选仅在基线 `stop` 的 `+6/+9/+12` 救回。
- 已完成单 seed、每件1条主路径、每个状态2条终局概率路径的 smoke，主流程可运行；该 smoke 仅验证账本与实现，不作为候选排序或发布依据。
- 正式研究参数拟定为五个独立 seed、每件10条主路径、每状态32条终局概率路径；正式报告和原始 JSON 尚未生成。
- 当前禁止修改项仍为正式策略、DP 常量、资源模型、评分、跳值表、GUI、OCR、自动化及 Holdout 产物。

## 正式离线结果（2026-07-20）

- 五个 seed 已完成；每件10条主路径、每状态4条官方离散终局路径。原始结果：[epic_concentration_rescue_20260720.json](reports/epic_concentration_rescue_20260720.json)，人类报告：[epic_concentration_rescue_20260720.md](reports/epic_concentration_rescue_20260720.md)。
- 外部库存阈值为：攻击 P90/P95/P97.5=`23.00/26.99/32.00`，生命 P90/P95/P97.5=`30.00/33.92/37.74`。
- 主场景五 seed 成对结果全部不支持救回：攻击 P90/P95/P97.5 balanced 相对正式价值区间分别为 `[-0.051204,-0.014247]`、`[-0.029823,-0.007602]`、`[-0.013127,-0.004513]`；生命对应为 `[-0.013879,-0.003447]`、`[-0.005111,-0.001066]`、`[-0.001180,-0.000046]`（正式百里分/100体力）。OR 行同样未获得正增量。
- 研究精度仍不足以发布：每状态仅4条终局路径，`P>0` 与 `P>=10%` 在非零状态上不可区分，因此 aggressive 与 balanced 数值相同；绝对每100,000总体力 CI 仅作条件性离线参考。
- 发布闸门未通过：不创建候选冻结、不启动新 Holdout、不写入自动救回；正式 `baili-formal-dp-v1-epic-balanced`、Heroic M1、资源模型、评分、跳值、GUI/OCR/自动化均保持不变。

## 主审计与后续（2026-07-20）

- p4 的概率只能取 `0/25/50/75/100%`，导致所有非零状态上 aggressive 与 balanced 动作完全相同；因此只能得出“没有候选获得发布证据”，不能得出“真实 balanced 已完成淘汰”。
- `_official_paths()` 使用官方离散跳值随机抽样，不是精确枚举；后续报告必须按真实估计器命名。
- `health_p975_balanced` 的旧成对区间 `[-0.001180,-0.000046]/100体力` 最接近零，作为唯一优先精度复核项。
- 后续唯一权威任务：[超高攻击生命集中属性救回概率精度收口任务说明.md](超高攻击生命集中属性救回概率精度收口任务说明.md)。
